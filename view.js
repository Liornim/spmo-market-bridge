// GENERATED from view.html / radar.html by build-view.mjs — edit those, not this file.
export const VIEW_HTML = "<!doctype html>\n<html lang=\"he\" dir=\"rtl\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width,initial-scale=1,viewport-fit=cover\">\n<title>נרות</title>\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link href=\"https://fonts.googleapis.com/css2?family=Heebo:wght@400;600;800&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{\n    --paper:#F2F4F7; --ink:#1B2430; --muted:#5B6673; --rule:#D6DBE2; --well:#FFFFFF;\n    --up:#0F766E; --up-soft:#D7EFEC; --down:#B42318; --down-soft:#F6DAD6; --flat:#8A94A0;\n    --fresh:#0F766E; --aging:#B45309; --stale:#B42318;\n  }\n  *{box-sizing:border-box}\n  html,body{margin:0;background:var(--paper);color:var(--ink);font-family:Heebo,system-ui,sans-serif;font-variant-numeric:tabular-nums;-webkit-text-size-adjust:100%}\n  body{padding:0 0 env(safe-area-inset-bottom)}\n  .top{display:flex;align-items:center;gap:10px;padding:14px 16px 8px}\n  select{font:inherit;font-weight:600;font-size:16px;background:var(--well);color:var(--ink);border:1px solid var(--rule);border-radius:10px;padding:8px 10px;appearance:none;-webkit-appearance:none;min-width:0}\n  #sym{font-weight:800;font-size:20px;letter-spacing:.02em}\n  .spacer{flex:1}\n  .fresh{display:flex;align-items:center;gap:6px;font-size:13px;color:var(--muted);white-space:nowrap}\n  .fresh i{width:9px;height:9px;border-radius:50%;background:var(--flat);flex:none}\n  .fresh.fresh-ok i{background:var(--fresh)} .fresh.fresh-aging i{background:var(--aging)} .fresh.fresh-stale i{background:var(--stale)}\n\n  .hero{padding:4px 16px 0}\n  .px{display:flex;align-items:baseline;gap:12px;direction:ltr;justify-content:flex-end}\n  .px b{font-size:44px;font-weight:800;line-height:1;letter-spacing:-.02em}\n  .px span{font-size:17px;font-weight:600}\n  .px .up{color:var(--up)} .px .down{color:var(--down)}\n  .px small{font-size:13px;color:var(--muted);font-weight:400}\n  .stats{display:flex;flex-wrap:wrap;gap:6px 18px;margin:10px 0 0;font-size:13px;color:var(--muted)}\n  .stats b{color:var(--ink);font-weight:600;direction:ltr;unicode-bidi:isolate}\n\n  .chart{margin:12px 0 0;background:var(--well);border-top:1px solid var(--rule);border-bottom:1px solid var(--rule)}\n  svg{display:block;width:100%;height:auto;touch-action:pan-y}\n  .wick{stroke-width:1} .body{stroke:none}\n  .up-c{fill:var(--up);stroke:var(--up)} .down-c{fill:var(--down);stroke:var(--down)} .flat-c{fill:var(--flat);stroke:var(--flat)}\n  .grid{stroke:var(--rule);stroke-width:1} .gridtxt{fill:var(--muted);font-size:11px;font-family:inherit}\n  .vol{opacity:.55}\n  .cursor{stroke:var(--ink);stroke-width:1;stroke-dasharray:3 3;opacity:.6}\n  .pick{fill:var(--ink);font-size:12px;font-weight:600}\n  .pickbg{fill:var(--well);stroke:var(--rule)}\n\n  table{width:100%;border-collapse:collapse;direction:ltr;font-size:13.5px;margin-top:8px}\n  th{position:sticky;top:0;background:var(--paper);color:var(--muted);font-weight:600;text-align:right;padding:8px 6px;border-bottom:1px solid var(--rule);font-size:12px}\n  td{padding:7px 6px;text-align:right;border-bottom:1px solid var(--rule);white-space:nowrap}\n  th:first-child,td:first-child{text-align:left;padding-left:16px;font-weight:600}\n  th:last-child,td:last-child{padding-right:16px}\n  tr.up td.c{color:var(--up)} tr.down td.c{color:var(--down)}\n  tr.hot{background:var(--up-soft)} tr.hot.down{background:var(--down-soft)}\n  tr.last td{font-weight:600}\n  .mini{display:inline-block;width:10px;height:22px;position:relative;vertical-align:middle}\n  .mini i{position:absolute;left:4.5px;width:1px;background:currentColor;top:0;bottom:0}\n  .mini b{position:absolute;left:1px;right:1px;background:currentColor;border-radius:1px}\n  .mini.up{color:var(--up)} .mini.down{color:var(--down)} .mini.flat{color:var(--flat)}\n  td.x{color:var(--muted)} td.x.hi{color:var(--ink);font-weight:600}\n\n  .foot{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:14px 16px 20px;font-size:13px;color:var(--muted)}\n  button{font:inherit;font-size:13px;background:var(--well);border:1px solid var(--rule);color:var(--ink);border-radius:10px;padding:7px 12px}\n  button:focus-visible,select:focus-visible{outline:2px solid var(--ink);outline-offset:2px}\n  .msg{padding:40px 16px;text-align:center;color:var(--muted);line-height:1.6}\n  .msg b{color:var(--ink)}\n\n  .tabs{display:flex;gap:6px;padding:0 16px 8px}\n  .tab{font:inherit;font-size:14px;font-weight:600;background:transparent;border:1px solid var(--rule);border-radius:10px;padding:7px 14px;color:var(--muted);position:relative}\n  .tab.on{background:var(--ink);color:var(--paper);border-color:var(--ink)}\n  .tab .n{position:absolute;top:-6px;left:-6px;min-width:18px;height:18px;border-radius:9px;background:var(--down);color:#fff;font-size:11px;line-height:18px;text-align:center;padding:0 5px}\n  [hidden]{display:none!important}\n\n  .st{padding:6px 16px 0}\n  .st .big{display:flex;align-items:center;gap:12px;flex-wrap:wrap}\n  .st .big b{font-size:34px;font-weight:800;line-height:1.1}\n  .st .big small{font-size:14px;color:var(--muted)}\n  .st .big.UP b{color:var(--up)} .st .big.DOWN b{color:var(--down)} .st .big.RANGE b{color:var(--muted)} .st .big.ATTEMPT b{color:var(--aging)}\n  .lv{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-top:12px}\n  .lv div{background:var(--well);border:1px solid var(--rule);border-radius:12px;padding:10px 12px}\n  .lv small{display:block;color:var(--muted);font-size:12px}\n  .lv b{font-size:20px;font-weight:800;direction:ltr;unicode-bidi:isolate;display:block}\n  .lv span{font-size:12px;color:var(--muted)}\n  .lv .r b{color:var(--down)} .lv .s b{color:var(--up)}\n  .now{margin-top:12px;background:var(--well);border:1px solid var(--rule);border-radius:12px;padding:12px}\n  .now .tier{display:inline-block;font-size:12px;font-weight:600;border-radius:6px;padding:2px 8px;background:var(--rule);color:var(--ink)}\n  .now .tier.WATCH{background:#FDF3D9;color:#7A4B00} .now .tier.IMPORTANT{background:#FCE4CF;color:#8A3A00} .now .tier.CHECK{background:var(--down-soft);color:var(--down)}\n  .now .ask{margin-top:10px;padding:10px 12px;border-radius:10px;background:var(--ink);color:var(--paper);font-weight:600;display:flex;align-items:center;justify-content:space-between;gap:10px}\n  .now .ask button{background:var(--paper);color:var(--ink);border:none;font-weight:600}\n  .now ul{margin:8px 0 0;padding:0 18px 0 0;font-size:14px;line-height:1.7}\n  .swl{margin-top:12px;font-size:13px;color:var(--muted);line-height:1.8}\n  .swl b{direction:ltr;unicode-bidi:isolate;color:var(--ink);font-weight:600}\n  .swl .HH,.swl .HL{color:var(--up)} .swl .LH,.swl .LL{color:var(--down)}\n  .log{margin-top:14px;border-top:1px solid var(--rule)}\n  .ev{display:grid;grid-template-columns:52px 1fr auto;gap:8px 10px;padding:9px 16px;border-bottom:1px solid var(--rule);align-items:baseline;font-size:14px}\n  .ev .t{color:var(--muted);font-size:13px} .ev .p{direction:ltr;unicode-bidi:isolate;font-weight:600}\n  .ev .d{font-size:12px;color:var(--muted);grid-column:2/4}\n  .ev.CHECK{background:var(--down-soft)} .ev.IMPORTANT{background:#FCE4CF} .ev.WATCH{background:#FDF3D9}\n  .ev.ask .p::before{content:'🤖 ';font-weight:400}\n  .empty{padding:24px 16px;color:var(--muted);text-align:center;line-height:1.6}\n  .note{padding:14px 16px 0;font-size:12px;color:var(--muted);line-height:1.6}\n\n  .bl{margin:10px 16px 0;background:var(--ink);color:var(--paper);border-radius:14px;padding:14px 14px 12px}\n  .bl .act{display:flex;align-items:baseline;justify-content:space-between;gap:10px}\n  .bl .act b{font-size:26px;font-weight:800;letter-spacing:.02em}\n  .bl .act.LONG b{color:#5EEAD4} .bl .act.AVOID b{color:#FCA5A5} .bl .act.WAIT b{color:#CBD5E1} .bl .act.LONGWATCH b{color:#FDE68A}\n  .bl .conf{font-size:13px;opacity:.85} .bl .conf b{font-size:20px;font-weight:800}\n  .bl .rsn{font-size:13px;opacity:.8;margin-top:2px}\n  .bl .lv2{display:grid;grid-template-columns:auto 1fr auto;gap:6px 10px;margin-top:12px;font-size:13.5px;align-items:baseline}\n  .bl .lv2 span:first-child{opacity:.7} .bl .lv2 b{direction:ltr;unicode-bidi:isolate;font-weight:700} .bl .lv2 i{font-style:normal;opacity:.7;font-size:12px;direction:ltr;unicode-bidi:isolate}\n  .bl .ind{display:flex;flex-wrap:wrap;gap:6px 14px;margin-top:12px;padding-top:10px;border-top:1px solid rgba(255,255,255,.15);font-size:12.5px;opacity:.9}\n  .bl .ind b{direction:ltr;unicode-bidi:isolate;font-weight:600}\n  .bl .why{font-size:11.5px;opacity:.6;margin-top:8px;line-height:1.5}\n  .vwap{fill:none;stroke:#7C3AED;stroke-width:1.2;stroke-dasharray:4 3} .ema9{fill:none;stroke:#0EA5E9;stroke-width:1} .ema20{fill:none;stroke:#F59E0B;stroke-width:1}\n  .legend{display:flex;gap:12px;font-size:11px;color:var(--muted);padding:6px 16px 0}\n  .legend i{display:inline-block;width:14px;height:0;border-top:2px solid;vertical-align:middle;margin-left:4px}\n  @media (prefers-reduced-motion:no-preference){ .px b{transition:color .3s} }\n</style>\n</head>\n<body>\n<div class=\"top\">\n  <select id=\"sym\" aria-label=\"סימבול\"></select>\n  <select id=\"date\" aria-label=\"תאריך\"></select>\n  <div class=\"spacer\"></div>\n  <div class=\"fresh\" id=\"fresh\"><i></i><span>טוען</span></div>\n</div>\n<div class=\"tabs\">\n  <button class=\"tab on\" id=\"tabBars\" type=\"button\">נרות</button>\n  <button class=\"tab\" id=\"tabStruct\" type=\"button\">מבנה <span class=\"n\" id=\"badge\" hidden>0</span></button>\n</div>\n<div id=\"paneBars\">\n<div class=\"hero\">\n  <div class=\"px\"><b id=\"last\">—</b><span id=\"chg\"></span><small>מהפתיחה</small></div>\n  <div class=\"stats\" id=\"stats\"></div>\n</div>\n<div class=\"chart\"><svg id=\"svg\" viewBox=\"0 0 400 300\" preserveAspectRatio=\"none\"></svg></div>\n<div class=\"legend\"><span><i style=\"border-color:#7C3AED;border-top-style:dashed\"></i>VWAP</span><span><i style=\"border-color:#0EA5E9\"></i>EMA9</span><span><i style=\"border-color:#F59E0B\"></i>EMA20</span></div>\n<div id=\"tbl\"></div>\n</div>\n<div id=\"paneStruct\" hidden>\n  <div class=\"top\" style=\"padding-top:0\"><select id=\"sens\" aria-label=\"רגישות\"><option value=\"sensitive\">רגיש</option><option value=\"balanced\" selected>מאוזן</option><option value=\"quiet\">שקט</option></select><div class=\"spacer\"></div><span class=\"fresh\" id=\"sensNote\"></span></div>\n  <div class=\"bl\" id=\"bl\"></div>\n  <div class=\"st\" id=\"st\"></div>\n  <div class=\"log\" id=\"log\"></div>\n  <div class=\"note\">Swings מאושרים K=3 נרות מכל צד; נר שהוא גם swing high וגם swing low (outside) נספר כרמה אבל לא כמבנה. התנגדות ותמיכה = הגבוה/הנמוך של 60 הנרות האחרונים, לא כולל 5 האחרונים. אחרי שבירת תמיכה מאושרת, הרמה שנשברה הופכת ל-reclaim והתמיכה היא הנמוך מאז. \"בדיקת התנגדות\" רק עד 0.75× טווח ממוצע מהרמה. פריצה = סגירה מעל ההתנגדות ברווח, ו\"אושר\" = גם הנר הבא סגר מעל. 🤖 מופיע רק על רמות של גבוה/נמוך היום, על שבירת מבנה של מגמה שהוכרזה, ועל נפח + מומנטום. כל אירוע נשפט מול הרמות של הנר הקודם. vol× כאן הוא מול ממוצע הנרות עד אותו רגע. על הילוך אקראי טהור המנוע מפיק כ-3 קריאות ביום במצב מאוזן — זה רצפת הרעש.</div>\n</div>\n<script>\n// bars-vault structure engine — pure function over one day's 1-minute bars.\n// Inlined into view.html by build-view.mjs; unit-tested directly in node.\n//\n// analyze(rows, { K: 3 }) -> { bars, swings, state, events }\n//   rows: [{ time, open, high, low, close, volume }] in chronological order.\n//\n// Swing confirmation is K bars on each side (default 3, per the owner's\n// \"K=3, no exceptions\" rule; the spec that inspired this used 2). A swing is\n// only usable from the bar that confirms it, so nothing here looks ahead:\n// the state at bar i is computed from bars 0..i.\nfunction analyze(allRows, opts) {\n  var K = (opts && opts.K) || 3;\n  // A flat zero-volume bar is an end-of-session filler, not market activity.\n  // It must not touch momentum, wicks, EMA, volume or scoring.\n  //\n  // ONE EXCEPTION: the 16:00 bar. The closing auction prints there and the feed\n  // often reports it flat with no volume attached, so the old rule threw away\n  // the OFFICIAL CLOSE and left 15:59 standing in for it. Measured on WMT: the\n  // only session whose close matched the provider's daily candle was the one\n  // whose last bar was 16:00, and dropping that bar moved 108.42 to 108.45.\n  var isAuction = function (r) { return r.time === '16:00'; };\n  var rows = allRows.filter(function (r) {\n    return isAuction(r) || !(r.volume === 0 && r.high === r.low);\n  });\n  var n = rows.length, skipped = allRows.length - n;\n  if (!n) return null;\n\n  // ---- per-bar derived values (causal: volume average is of bars 0..i)\n  var bars = [], volSum = 0, rangeSum = 0, dayHigh = -Infinity, dayLow = Infinity;\n  var pvSum = 0, ema9 = null, ema20 = null, k9 = 2 / 10, k20 = 2 / 21, belowVwapRun = 0;\n  for (var i = 0; i < n; i++) {\n    var r = rows[i], range = r.high - r.low, body = Math.abs(r.close - r.open);\n    volSum += r.volume; rangeSum += range;\n    dayHigh = Math.max(dayHigh, r.high); dayLow = Math.min(dayLow, r.low);\n    // VWAP from the open, EMA9/EMA20 on closes, 20-bar average range.\n    pvSum += (r.high + r.low + r.close) / 3 * r.volume;\n    var vwap = volSum > 0 ? pvSum / volSum : r.close;\n    ema9 = ema9 == null ? r.close : r.close * k9 + ema9 * (1 - k9);\n    ema20 = ema20 == null ? r.close : r.close * k20 + ema20 * (1 - k20);\n    var w20 = rows.slice(Math.max(0, i - 19), i + 1), atr20 = w20.reduce(function (s, x) { return s + (x.high - x.low); }, 0) / w20.length;\n    // (atr20 is needed by the EMA test below, so it is computed first)\n    var aboveVwap = r.close > vwap, vwapReclaim = aboveVwap && belowVwapRun >= 3, vwapLoss = !aboveVwap && belowVwapRun === 0 && i > 0 && bars[i - 1].aboveVwap;\n    belowVwapRun = aboveVwap ? 0 : belowVwapRun + 1;\n    // EMA is only bullish/bearish when the two are meaningfully apart (0.15x the\n    // average range) AND the fast one is moving that way. Otherwise: neutral.\n    var prevEma9 = i > 0 ? bars[i - 1].ema9 : ema9;\n    var sep = Math.abs(ema9 - ema20), minSep = 0.15 * (atr20 || range || 1);\n    // The closing auction prints a huge single-minute volume. It is marked so\n    // scoring and attention can ignore it; it is still shown as information.\n    var auction = r.time >= '15:55';\n    var align = 'neutral';\n    if (sep >= minSep) {\n      if (r.close > ema9 && ema9 > ema20 && ema9 > prevEma9) align = 'bull';\n      else if (r.close < ema9 && ema9 < ema20 && ema9 < prevEma9) align = 'bear';\n    }\n    var pct = function (x) { return range > 0 ? Math.round(x / range * 1000) / 10 : 0; };\n    bars.push({\n      i: i, time: r.time, open: r.open, high: r.high, low: r.low, close: r.close, volume: r.volume,\n      range: range, body: pct(body), uw: pct(r.high - Math.max(r.open, r.close)), lw: pct(Math.min(r.open, r.close) - r.low),\n      dir: r.close > r.open ? 'up' : r.close < r.open ? 'down' : 'flat',\n      volx: volSum > 0 ? Math.round(r.volume / (volSum / (i + 1)) * 100) / 100 : 0,\n      avgRange: rangeSum / (i + 1),\n      closePos: range > 0 ? (r.close - r.low) / range : 0.5,      // 1 = closed at high\n      vwap: vwap, ema9: ema9, ema20: ema20, atr20: atr20, aboveVwap: aboveVwap, vwapReclaim: vwapReclaim, vwapLoss: vwapLoss, align: align, auction: auction,\n      newDayHigh: r.high >= dayHigh && (i === 0 || r.high > Math.max.apply(null, rows.slice(0, i).map(function (x) { return x.high; }))),\n      dayHigh: dayHigh, dayLow: dayLow\n    });\n  }\n\n  // ---- swings, confirmed K bars later\n  var swings = [];\n  for (var s = K; s < n - K; s++) {\n    var hiL = true, loL = true, hiR = true, loR = true;\n    for (var k = 1; k <= K; k++) {\n      if (!(rows[s].high > rows[s - k].high)) hiL = false;\n      if (!(rows[s].high >= rows[s + k].high)) hiR = false;\n      if (!(rows[s].low < rows[s - k].low)) loL = false;\n      if (!(rows[s].low <= rows[s + k].low)) loR = false;\n    }\n    if (hiL && hiR) swings.push({ i: s, time: rows[s].time, kind: 'H', price: rows[s].high, confirmedAt: s + K });\n    if (loL && loR) swings.push({ i: s, time: rows[s].time, kind: 'L', price: rows[s].low, confirmedAt: s + K });\n  }\n  swings.sort(function (a, b) { return a.i - b.i; });\n  // An outside / expansion bar that is both a swing high and a swing low gives\n  // no order information (which came first?), so it is a level but never structure.\n  var byBar = {};\n  swings.forEach(function (w) { byBar[w.i] = (byBar[w.i] || 0) + 1; });\n  swings.forEach(function (w) { if (byBar[w.i] > 1) w.outside = true; });\n  // A swing only earns HH/LH/HL/LL if it moved at least MIN_MOVE x the average\n  // bar range past the previous swing of its kind; smaller wiggles are EQ and\n  // count as levels but never as structure.\n  var MIN_MOVE = (opts && opts.minMove) || 1.5, prevH = null, prevL = null;\n  swings.forEach(function (w) {\n    if (w.outside) { w.label = 'OUT'; return; }\n    var tol = MIN_MOVE * bars[w.i].avgRange;\n    if (w.kind === 'H') { w.label = !prevH ? 'H' : w.price > prevH.price + tol ? 'HH' : w.price < prevH.price - tol ? 'LH' : 'EQ'; if (w.label !== 'EQ') prevH = w; }\n    else { w.label = !prevL ? 'L' : w.price > prevL.price + tol ? 'HL' : w.price < prevL.price - tol ? 'LL' : 'EQ'; if (w.label !== 'EQ') prevL = w; }\n  });\n\n  // ---- sequential walk: levels, trend, events\n  //\n  // Levels for breakout/support events are WINDOW extremes, not the nearest\n  // micro-swing: the highest high / lowest low of the last 60 bars, excluding\n  // the most recent 5 (a high made a minute ago is not resistance yet).\n  // Swings still drive trend, HL/LL structure, and the \"structure break\" event.\n  var WIN = 60, AGE = 5, COOLDOWN = 30, MARGIN = (opts && opts.margin) || 0.15, RUNS = (opts && opts.runs) || 3, TEST_RANGE = 0.75;\n  var events = [], prevState = null, lastBreakout = null, states = [], fired = {}, trendRun = 0, runTrend = null, announced = null, broken = null;\n  var cooled = function (type, b) { return fired[type] == null || b - fired[type] > COOLDOWN; };\n  var mark = function (type, b) { fired[type] = b; };\n\n  for (var b = 0; b < n; b++) {\n    var bar = bars[b], next = bars[b + 1] || null, margin = MARGIN * bar.avgRange;\n    var conf = swings.filter(function (w) { return w.confirmedAt <= b; });\n    var structural = function (w) { return w.label !== 'EQ' && w.label !== 'OUT'; };\n    var SH = conf.filter(function (w) { return w.kind === 'H' && structural(w); }), SL = conf.filter(function (w) { return w.kind === 'L' && structural(w); });\n    var lastSH = SH[SH.length - 1] || null, lastSL = SL[SL.length - 1] || null;\n\n    var wEnd = b - AGE, wStart = Math.max(0, b - WIN), res = null, sup = null, reclaimed = null;\n    if (wEnd >= wStart && wEnd >= 0) {\n      var w = rows.slice(wStart, wEnd + 1), hiIdx = 0, loIdx = 0;\n      for (var q = 1; q < w.length; q++) { if (w[q].high > w[hiIdx].high) hiIdx = q; if (w[q].low < w[loIdx].low) loIdx = q; }\n      if (w[hiIdx].high > bar.close) res = { price: w[hiIdx].high, time: w[hiIdx].time, kind: 'window' };\n      if (w[loIdx].low < bar.close) sup = { price: w[loIdx].low, time: w[loIdx].time, kind: 'window' };\n    }\n    // After a confirmed support break the broken level is a reclaim level, not\n    // support; support is the lowest low made since the break.\n    if (broken) {\n      if (bar.low < broken.low) { broken.low = bar.low; broken.lowTime = bar.time; }\n      if (bar.close > broken.level + MARGIN * bar.atr20) broken.above++; else broken.above = 0;\n      if (broken.above >= 2) { reclaimed = broken.level; broken = null; } else if (b - broken.i > 60) broken = null;\n    }\n    if (broken && broken.low < bar.close) sup = { price: broken.low, time: broken.lowTime, kind: 'postbreak' };\n    if (broken && sup && sup.kind !== 'postbreak' && sup.price >= broken.level - 1e-9) sup = broken.low < bar.close ? { price: broken.low, time: broken.lowTime, kind: 'postbreak' } : null;\n\n    var trend = 'RANGE', reason = 'אין רצף';\n    if (lastSH && lastSL && lastSH.label !== 'H' && lastSL.label !== 'L') {\n      if (lastSH.label === 'HH' && lastSL.label === 'HL') { trend = 'UP'; reason = 'HH + HL'; }\n      else if (lastSH.label === 'LH' && lastSL.label === 'LL') { trend = 'DOWN'; reason = 'LH + LL'; }\n      else reason = lastSH.label + ' + ' + lastSL.label;\n    } else if (!lastSH || !lastSL) reason = 'עדיין אין מספיק swings';\n\n    // ---- events, judged against the PREVIOUS bar's levels (no look-ahead on levels;\n    // the only forward look is the 2-bar confirmation, which is reported as such).\n    var pres = prevState ? prevState.res : null, psup = prevState ? prevState.sup : null;\n    var pTrend = prevState ? prevState.trend : null, pSL = prevState ? prevState.lastSL : null, pSH = prevState ? prevState.lastSH : null;\n    var ev = [], score = 0;\n\n    if (pres && bar.high > pres.price && bar.close > pres.price + margin && cooled('breakout', b)) {\n      var confirmed = next ? next.close > pres.price : null;\n      var strong = bar.body > 65 && bar.volx > 1.2;\n      score += confirmed ? 5 : 2;\n      ev.push({ type: strong && confirmed ? 'strong_breakout' : 'breakout', level: pres.price, confirmed: confirmed, strong: strong });\n      lastBreakout = { i: b, level: pres.price, confirmed: confirmed, dayHigh: pres.price >= prevState.bar.dayHigh - 1e-9 };\n      if (confirmed) mark('breakout', b);\n    } else if (pres && bar.high > pres.price && bar.close < pres.price && bar.uw >= 50 && cooled('rejected', b)) {\n      ev.push({ type: 'rejected', level: pres.price, wick: bar.uw }); mark('rejected', b);\n    } else if (pres && !ev.length && bar.close <= pres.price && pres.price - bar.close <= TEST_RANGE * bar.atr20) {\n      ev.push({ type: 'testing', level: pres.price });\n    }\n    if (lastBreakout && b > lastBreakout.i && b - lastBreakout.i <= 10) {\n      if (bar.close < lastBreakout.level) { score -= 3; ev.push({ type: 'failed', level: lastBreakout.level, confirmed: lastBreakout.confirmed, dayHigh: lastBreakout.dayHigh }); lastBreakout = null; }\n      else if (bar.low <= lastBreakout.level + margin && bar.close >= lastBreakout.level && cooled('retest_hold', b)) { ev.push({ type: 'retest_hold', level: lastBreakout.level }); mark('retest_hold', b); }\n    }\n    if (psup && bar.low < psup.price && bar.close < psup.price - margin && cooled('support_break', b)) {\n      var confS = next ? next.close < psup.price : null;\n      score -= 2; ev.push({ type: 'support_break', level: psup.price, confirmed: confS });\n      if (confS) { mark('support_break', b); broken = { level: psup.price, i: b, low: bar.low, lowTime: bar.time, above: 0 }; }\n    }\n    // Structure break: the level the trend rests on gives way.\n    // Only an announced trend (two agreeing swings) can be \"broken\".\n    var pAnnounced = prevState ? prevState.announced : null;\n    if (pAnnounced === 'UP' && pSL && bar.close < pSL.price - margin && cooled('structure_break', b)) {\n      score -= 2; ev.push({ type: 'structure_break', level: pSL.price, side: 'bear', confirmed: next ? next.close < pSL.price : null }); mark('structure_break', b);\n    } else if (pAnnounced === 'DOWN' && pSH && bar.close > pSH.price + margin && cooled('structure_break', b)) {\n      score += 2; ev.push({ type: 'structure_break', level: pSH.price, side: 'bull', confirmed: next ? next.close > pSH.price : null }); mark('structure_break', b);\n    }\n    conf.forEach(function (w) {\n      if (w.confirmedAt !== b || w.label === 'EQ') return;\n      if (w.label === 'OUT') { if (w.kind === 'H') ev.push({ type: 'outside_bar', level: w.price, at: w.time }); return; }\n      ev.push({ type: (w.label.toLowerCase()) + '_confirmed', level: w.price, at: w.time });\n    });\n    if (bar.volx >= 1.5 && !bar.auction) { score += 2; ev.push({ type: 'volume_spike', volx: bar.volx }); }\n    else if (bar.volx >= 1.5 && bar.auction) ev.push({ type: 'auction_volume', volx: bar.volx });\n    var momentum = bar.body >= 75 && bar.range > 1.5 * bar.avgRange;\n    if (momentum) ev.push({ type: 'momentum', body: bar.body, range: bar.range });\n    if (reclaimed != null) ev.push({ type: 'level_reclaim', level: reclaimed });\n    if (bar.vwapReclaim) ev.push({ type: 'vwap_reclaim', level: bar.vwap });\n    if (bar.vwapLoss) ev.push({ type: 'vwap_loss', level: bar.vwap });\n    if (bar.body >= 70) score += 1;\n    if (bar.closePos >= 0.8) score += 1;\n    if (bar.newDayHigh) score += 1;\n    if (bar.uw > 55) score -= 2;\n    // Trend change is announced on the SECOND structural swing that agrees\n    // (HH+HL is the trend; the next HH or HL is the confirmation).\n    var structConfirmedNow = conf.some(function (w) { return w.confirmedAt === b && structural(w); });\n    if (structConfirmedNow) {\n      if (trend === runTrend) trendRun++; else { runTrend = trend; trendRun = 1; }\n      if (trendRun === RUNS && (trend === 'UP' || trend === 'DOWN') && cooled('trend_change', b)) {\n        ev.push({ type: 'trend_change', from: pTrend === trend ? 'RANGE' : pTrend, to: trend }); mark('trend_change', b); announced = trend;\n      }\n    }\n    if (announced && trend !== announced) announced = null;\n\n    var attempt = (pres && Math.abs(pres.price - bar.close) <= TEST_RANGE * bar.atr20) || (lastBreakout && b - lastBreakout.i <= 1);\n    var nearLow = sup ? bar.close - sup.price : bar.close - bar.dayLow;\n    var bounce = (trend === 'DOWN' || broken) && bar.dir === 'up' && nearLow <= 1.5 * bar.atr20;\n    var state = attempt ? 'ATTEMPT' : bounce ? 'BOUNCE' : trend;\n    var tier = score >= 7 ? 'CHECK' : score >= 5 ? 'IMPORTANT' : score >= 3 ? 'WATCH' : 'NONE';\n    // Ask = worth a fresh look: only day-extreme levels, only confirmed.\n    var pDayHigh = prevState ? prevState.bar.dayHigh : Infinity, pDayLow = prevState ? prevState.bar.dayLow : -Infinity;\n    var ask = ev.some(function (e) {\n      if (e.type === 'strong_breakout' || e.type === 'breakout') return e.confirmed === true && e.level >= pDayHigh - 1e-9;\n      if (e.type === 'rejected') return e.level >= pDayHigh - 1e-9;\n      if (e.type === 'failed') return e.confirmed === true && e.dayHigh === true;\n      if (e.type === 'support_break') return e.confirmed === true && e.level <= pDayLow + 1e-9;\n      if (e.type === 'structure_break') return e.confirmed === true;\n      if (e.type === 'trend_change') return true;\n      return false;\n    }) || (bar.volx >= 1.5 && !bar.auction && momentum);\n    var loggable = ask || tier !== 'NONE' || ev.some(function (e) {\n      return e.type === 'failed' || e.type === 'rejected' || e.type === 'support_break' || e.type === 'structure_break' || e.type === 'trend_change' || e.type === 'retest_hold' || /breakout/.test(e.type);\n    });\n\n    var st = { i: b, time: bar.time, close: bar.close, trend: trend, reason: reason, state: state, res: res, sup: sup,\n      lastSH: lastSH, lastSL: lastSL, announced: announced, reclaim: broken ? broken.level : null, score: score, tier: tier, ask: ask, events: ev, bar: bar };\n    states.push(st);\n    if (loggable) events.push(st);\n    prevState = st;\n  }\n\n  // The UNFILTERED rows too. Coverage is a question about the session that was\n  // loaded, not about the subset the analysis chose to use: a no-trade minute\n  // is dropped here on purpose, and counting the remainder made a complete\n  // 335-bar session report as 186 with holes that were never there.\n  return { bars: bars, rawBars: allRows, swings: swings, states: states, state: states[n - 1],\n    events: events, skippedBars: skipped };\n}\n\n// ---------------------------------------------------------------- radar layer\n//\n// Short-term momentum, kept separate from structure on purpose: a confirmed\n// downtrend can still be bouncing.\n//   PUSHING   pressing higher with the structure\n//   PULLBACK  easing back inside an up structure\n//   RECOVERY  bouncing off the lows against a down structure\n//   SELLING   pressing lower\n//   FLAT      no net movement worth naming\nfunction momentum(A, look) {\n  if (!A || !A.state) return { label: 'FLAT', net: 0 };\n  var L = look || 5, bars = A.bars, n = bars.length, w = bars.slice(Math.max(0, n - L));\n  var b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  var net = (b.close - w[0].open) / atr;                       // in range units\n  var ups = w.filter(function (x) { return x.dir === 'up'; }).length;\n  var struct = A.state.announced || A.state.trend;\n  var lowRun = Math.min.apply(null, w.map(function (x) { return x.low; }));\n  var highRun = Math.max.apply(null, w.map(function (x) { return x.high; }));\n  var offLow = (b.close - lowRun) / atr, offHigh = (highRun - b.close) / atr;\n  var label = 'FLAT';\n  if (net >= 0.8 && ups >= L / 2) label = struct === 'DOWN' ? 'RECOVERY' : 'PUSHING';\n  else if (net <= -0.8 && ups <= L / 2) label = struct === 'UP' ? 'PULLBACK' : 'SELLING';\n  else if (offLow >= 1 && offHigh <= 0.5) label = struct === 'DOWN' ? 'RECOVERY' : 'PUSHING';\n  else if (offHigh >= 1 && offLow <= 0.5) label = struct === 'UP' ? 'PULLBACK' : 'SELLING';\n  return { label: label, net: net, offLow: offLow, offHigh: offHigh };\n}\n\n// Tactical levels: what is actually within reach, preferring levels that price\n// has interacted with more than once. Everything is measured in R = one average\n// 1-minute range, so \"0.3R away\" means the same thing on any symbol.\nfunction tactical(A, maxR) {\n  if (!A || !A.state) return { support: null, resistance: null, above: [], below: [], atr: 1 };\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length, atr = b.atr20 || b.avgRange || 1, MAX = (maxR || 3) * atr;\n  var cand = [];\n  var add = function (price, why, weight, kind) {\n    if (price == null || !isFinite(price)) return;\n    cand.push({ price: price, why: why, weight: weight, kind: kind });\n  };\n\n  // A prior HIGH only becomes support once price broke above it and came back\n  // to it without losing it — resistance-turned-support needs evidence, not\n  // just \"price happens to be above it now\". Same, inverted, for prior lows.\n  var acceptedAsSupport = function (level, fromIdx) {\n    var closedAbove = 0, retest = false;\n    for (var i = fromIdx; i < n; i++) {\n      if (bars[i].close > level) closedAbove++;\n      if (closedAbove >= 2 && bars[i].low <= level + 0.25 * atr && bars[i].close >= level) retest = true;\n    }\n    return closedAbove >= 3 && retest;\n  };\n  var acceptedAsResistance = function (level, fromIdx) {\n    var closedBelow = 0, retest = false;\n    for (var i = fromIdx; i < n; i++) {\n      if (bars[i].close < level) closedBelow++;\n      if (closedBelow >= 2 && bars[i].high >= level - 0.25 * atr && bars[i].close <= level) retest = true;\n    }\n    return closedBelow >= 3 && retest;\n  };\n\n  A.swings.forEach(function (w) {\n    if (w.confirmedAt > n - 1) return;\n    var age = n - 1 - w.i; if (age > 120) return;\n    var base = (w.label === 'OUT' || w.label === 'EQ') ? 2 : 3;\n    var recency = age <= 30 ? 2 : age <= 60 ? 1 : 0;\n    if (w.kind === 'H') {\n      if (w.price > b.close) add(w.price, 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time, base + recency, 'res');\n      else if (acceptedAsSupport(w.price, w.i)) add(w.price, 'התנגדות שהפכה לתמיכה ' + w.time, base + recency + 1, 'sup');\n    } else {\n      if (w.price < b.close) add(w.price, 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time, base + recency, 'sup');\n      else if (acceptedAsResistance(w.price, w.i)) add(w.price, 'תמיכה שהפכה להתנגדות ' + w.time, base + recency + 1, 'res');\n    }\n  });\n  if (S.res && S.res.price > b.close) add(S.res.price, 'גבוה 60 נרות', 3, 'res');\n  if (S.sup && S.sup.price < b.close) add(S.sup.price, S.sup.kind === 'postbreak' ? 'הנמוך מאז השבירה' : 'נמוך 60 נרות', S.sup.kind === 'postbreak' ? 4 : 3, S.sup.kind === 'postbreak' ? 'sup' : 'sup');\n  if (b.dayHigh > b.close) add(b.dayHigh, 'גבוה היום', 3, 'res');\n  if (b.dayLow < b.close) add(b.dayLow, 'נמוך היום', 3, 'sup');\n  if (S.reclaim != null) add(S.reclaim, 'רמה שנשברה', 4, S.reclaim > b.close ? 'res' : 'sup');\n  add(b.vwap, 'VWAP', 2, b.vwap > b.close ? 'res' : 'sup');\n  cand[cand.length - 1].soft = true;   // VWAP is context, never a setup level on its own\n\n  // touches and rejections add weight; distance never does.\n  cand.forEach(function (c) {\n    var touch = 0, reject = 0;\n    bars.forEach(function (x) {\n      if (x.low <= c.price + 0.25 * atr && x.high >= c.price - 0.25 * atr) {\n        touch++;\n        if (c.kind === 'res' && x.high > c.price && x.close < c.price) reject++;\n        if (c.kind === 'sup' && x.low < c.price && x.close > c.price) reject++;\n      }\n    });\n    c.touches = touch; c.rejects = reject;\n    c.weight += Math.min(3, Math.floor(touch / 4)) + Math.min(2, reject);\n    c.r = (c.price - b.close) / atr;\n  });\n\n  // Price sitting exactly on a level is the important case, so the side test\n  // allows a small epsilon rather than requiring strictly above/below.\n  var eps = 0.05 * atr;\n  var side = function (want) {\n    return cand.filter(function (c) { return c.kind === want && (want === 'res' ? c.price > b.close - eps : c.price < b.close + eps); })\n      .filter(function (c) { return Math.abs(c.price - b.close) <= MAX; });\n  };\n  // Strongest level wins; distance is only the tie-breaker, and a much closer\n  // level of near-equal strength is preferred.\n  var pick = function (want) {\n    var list = side(want).sort(function (x, y) { return (y.weight - x.weight) || (Math.abs(x.r) - Math.abs(y.r)); });\n    if (!list.length) return null;\n    var best = list[0];\n    list.slice(1, 4).forEach(function (c) { if (c.weight >= best.weight - 1 && Math.abs(c.r) < Math.abs(best.r) - 0.3) best = c; });\n    return best;\n  };\n  var all = function (want) {\n    return side(want).sort(function (x, y) { return want === 'res' ? x.price - y.price : y.price - x.price; });\n  };\n  return { support: pick('sup'), resistance: pick('res'), above: all('res'), below: all('sup'), atr: atr, candidates: cand };\n}\n\n// Distance in R, kept readable at small values. Below 0.05R it is \"on the level\".\nfunction fmtR(r) {\n  var a = Math.abs(r);\n  if (a < 0.05) return 'על הרמה';\n  return (r >= 0 ? '+' : '-') + (a < 1 ? a.toFixed(2) : a.toFixed(1)) + 'R';\n}\n\n// A support is only \"holding\" if price is not driving through it. Deterministic:\n// a big bearish body closing near its low, meaningfully below the level, or two\n// consecutive closes below it.\nfunction aggressiveBreak(A, level) {\n  var bars = A.bars, n = bars.length, b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  if (b.close < level - 0.5 * atr) return true;\n  if (n >= 2 && bars[n - 2].close < level && b.close < level) return true;\n  if (b.dir === 'down' && b.body >= 60 && b.closePos <= 0.25 && b.close < level - 0.15 * atr) return true;\n  return false;\n}\n\n// ---- execution plan: a small deterministic state machine.\n// States: WAITING_FOR_ZONE, IN_ZONE, READY_PARTIAL, WAITING_FOR_CONFIRMATION,\n//         READY_ADD, ACTIVE, TAKE_PROFIT_AREA, DO_NOT_CHASE, FAILED, NO_SETUP\nvar PLAN_TEXT = {\n  WAITING_FOR_ZONE: ['המתן', 'מחכים לכניסה לאזור'],\n  IN_ZONE: ['המתן', 'בתוך האזור — צריך בלימה'],\n  READY_PARTIAL: ['כניסה חלקית', 'האזור מוחזק'],\n  WAITING_FOR_CONFIRMATION: ['החזק', 'מחכים לאישור להוספה'],\n  READY_ADD: ['הוסף', 'האישור התקבל'],\n  ACTIVE: ['החזק', 'מהלך פעיל'],\n  TAKE_PROFIT_AREA: ['מימוש', 'אזור מימוש ראשון'],\n  DO_NOT_CHASE: ['לא לרדוף', 'המחיר התרחק מהכניסה'],\n  FAILED: ['להימנע', 'ה-setup בוטל'],\n  NO_SETUP: ['המתן', 'אין setup']\n};\nfunction executionPlan(A, market) {\n  if (!A || !A.state) return null;\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length, atr = b.atr20 || b.avgRange || 1;\n  var T = tactical(A), BL = bottomLine(A, market), mom = momentum(A);\n  var P = { state: 'NO_SETUP', kind: null, zone: null, entry: null, addAbove: null,\n    invalidation: null, target: null, atr: atr, price: b.close };\n\n  var sup = T.support, res = T.resistance;\n  var bearish = S.announced === 'DOWN' && mom.label !== 'RECOVERY';\n  // A setup needs a real structural level and a tape with some range in it.\n  // A dead-flat chop whose whole day spans one average bar is not a setup,\n  // however close price sits to \"a level\".\n  var dayRangeR = (b.dayHigh - b.dayLow) / atr;\n  var hasTape = dayRangeR >= 2;\n  // Whether the level is actually holding is decided by the state machine\n  // below (holding / aggressiveBreak), not by momentum: the first green bar\n  // off support is exactly the partial-entry moment, and a 5-bar momentum\n  // reading still says SELLING there.\n  var pullbackOk = !bearish && !!sup && !sup.soft && hasTape && sup.weight >= 5;\n  var breakoutOk = !bearish && !!res && !res.soft && hasTape &&\n    (S.trend !== 'DOWN' || mom.label === 'RECOVERY') && (BL.confidence >= 3 || mom.label === 'PUSHING' || S.trend === 'UP');\n\n  // Which setup is in play: the nearer opportunity wins.\n  var dSup = sup ? Math.abs(b.close - sup.price) / atr : Infinity;\n  var dRes = res ? Math.abs(res.price - b.close) / atr : Infinity;\n  if (pullbackOk && dSup <= 3 && (dSup <= dRes || !breakoutOk)) {\n    P.kind = 'pullback';\n    P.zone = [sup.price - 0.15 * atr, sup.price + 0.30 * atr];\n    P.entry = sup.price + 0.05 * atr;\n    var nextUp = T.above.filter(function (c) { return c.price > P.zone[1] + 0.1 * atr; })[0];\n    P.addAbove = nextUp ? nextUp.price : sup.price + 0.7 * atr;\n    var lowerLevels = T.below.filter(function (c) { return c.price < sup.price - 0.05 * atr; });\n    P.invalidation = lowerLevels.length ? Math.max(lowerLevels[0].price, sup.price - 0.9 * atr) : sup.price - 0.45 * atr;\n    P.target = res ? res.price : b.dayHigh;\n    P.levelWhy = sup.why;\n  } else if (breakoutOk && dRes <= 3) {\n    P.kind = 'breakout';\n    P.zone = [res.price, res.price + 0.20 * atr];\n    P.entry = res.price + 0.10 * atr;\n    P.addAbove = res.price + 0.5 * atr;\n    P.invalidation = Math.max(res.price - 0.6 * atr, sup ? sup.price : res.price - 0.9 * atr);\n    var beyond = T.above.filter(function (c) { return c.price > res.price + 0.2 * atr; })[0];\n    P.target = beyond ? beyond.price : res.price + 1.5 * atr;\n    P.levelWhy = res.why;\n  }\n\n  // A setup that just broke must still read FAILED, even though the level it\n  // rested on is now above price and no longer offered as support.\n  // Losing the last structural low by a clear margin is a broken setup even if\n  // the 60-bar window never registered a support_break (the window low can sit\n  // far below the level the setup actually rested on).\n  var lostLevel = !!(S.lastSL && b.close < S.lastSL.price - 0.3 * atr && (n - 1 - S.lastSL.i) <= 25);\n  var justBroke = lostLevel || A.states.slice(-6).some(function (s) {\n    return s.events.some(function (e) {\n      return (e.type === 'support_break' && e.confirmed === true)\n        || (e.type === 'structure_break' && e.side === 'bear' && e.confirmed === true)\n        || (e.type === 'failed');\n    });\n  });\n  if (!P.kind) {\n    if (justBroke) { P.state = 'FAILED'; P.invalidation = null; return decorate(P); }\n    P.state = 'NO_SETUP'; P.reason = bearish ? 'מבנה דובי — אין setup לונג' : 'אין רמה קרובה';\n    return decorate(P);\n  }\n\n  var brokeInvalid = b.close < P.invalidation || lostLevel;\n  var agg = aggressiveBreak(A, P.kind === 'pullback' ? sup.price : P.invalidation);\n  var inZone = b.close >= P.zone[0] && b.close <= P.zone[1];\n  // \"Holding\" = the last two bars did not lose the level. This, not momentum,\n  // is what turns being in the zone into a partial entry.\n  var lvl = P.kind === 'pullback' ? sup.price : P.zone[0];\n  var holding = P.kind !== 'pullback' || (bars.slice(-2).every(function (x) { return x.low >= lvl - 0.35 * atr; }) && b.close >= lvl - 0.1 * atr);\n  var aboveAdd = b.close > P.addAbove;\n  var confirmedAdd = aboveAdd && n >= 2 && bars[n - 2].close > P.addAbove;\n  var nearTarget = P.target && (P.target - b.close) / atr <= 0.3;\n  var extended = b.close > P.entry + 0.5 * atr;\n\n  if (brokeInvalid || (agg && !inZone && b.close < P.zone[0])) P.state = 'FAILED';\n  else if (agg && inZone) P.state = 'FAILED';\n  else if (nearTarget && (aboveAdd || P.kind === 'breakout')) P.state = 'TAKE_PROFIT_AREA';\n  else if (confirmedAdd) P.state = extended && !nearTarget ? 'ACTIVE' : 'READY_ADD';\n  else if (aboveAdd) P.state = 'WAITING_FOR_CONFIRMATION';\n  else if (inZone && holding) P.state = 'READY_PARTIAL';\n  else if (inZone) P.state = 'IN_ZONE';\n  else if (extended) P.state = 'DO_NOT_CHASE';\n  else P.state = 'WAITING_FOR_ZONE';\n\n  return decorate(P);\n\n  function decorate(P) {\n    var t = PLAN_TEXT[P.state] || PLAN_TEXT.NO_SETUP;\n    P.action = t[0]; P.headline = t[1];\n    var z = P.zone ? P.zone.map(function (x) { return x.toFixed(2); }).join('–') : null;\n    P.waitPrice = null; P.nextStep = null; P.ifConfirmed = null; P.ifFailed = null; P.short = null;\n    if (P.state === 'NO_SETUP') { P.short = P.reason; return P; }\n    switch (P.state) {\n      case 'WAITING_FOR_ZONE':\n        P.waitPrice = z;\n        P.nextStep = P.kind === 'pullback' ? 'ירידה לאזור ' + z + ' בלי שבירה אגרסיבית' : 'סגירה מעל ' + P.zone[0].toFixed(2);\n        P.ifConfirmed = 'כניסה חלקית ב-' + P.entry.toFixed(2);\n        P.ifFailed = 'שבירה מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'מחכים ל-' + (P.kind === 'pullback' ? z : P.zone[0].toFixed(2));\n        break;\n      case 'IN_ZONE':\n        P.waitPrice = z;\n        P.nextStep = 'בתוך האזור — צריך בלימה לפני כניסה';\n        P.ifConfirmed = 'בלימה מעל ' + P.zone[0].toFixed(2) + ' — כניסה חלקית';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'באזור ' + z + ' — צריך בלימה';\n        break;\n      case 'READY_PARTIAL':\n        P.waitPrice = z;\n        P.nextStep = 'האזור מוחזק — כניסה חלקית ב-' + P.entry.toFixed(2);\n        P.ifConfirmed = 'חזרה מעל ' + P.addAbove.toFixed(2) + ' — הוספה';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'כניסה חלקית ' + z;\n        break;\n      case 'WAITING_FOR_CONFIRMATION':\n        P.waitPrice = P.addAbove.toFixed(2);\n        P.nextStep = 'נר סגירה נוסף מעל ' + P.addAbove.toFixed(2);\n        P.ifConfirmed = 'הוספה';\n        P.ifFailed = 'חזרה מתחת ' + P.zone[0].toFixed(2) + ' — להחזיק בלבד';\n        P.short = 'אישור מעל ' + P.addAbove.toFixed(2);\n        break;\n      case 'READY_ADD':\n        P.waitPrice = P.addAbove.toFixed(2);\n        P.nextStep = 'הוספה מעל ' + P.addAbove.toFixed(2);\n        P.ifConfirmed = 'יעד ' + (P.target ? P.target.toFixed(2) : '—');\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'הוסף מעל ' + P.addAbove.toFixed(2);\n        break;\n      case 'ACTIVE':\n        P.waitPrice = P.target ? P.target.toFixed(2) : null;\n        P.nextStep = 'מהלך פעיל לעבר ' + (P.target ? P.target.toFixed(2) : 'היעד');\n        P.ifConfirmed = 'מימוש חלקי באזור היעד';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'יעד הבא ' + (P.target ? P.target.toFixed(2) : '—');\n        break;\n      case 'TAKE_PROFIT_AREA':\n        P.waitPrice = P.target.toFixed(2);\n        P.nextStep = 'אזור מימוש ' + P.target.toFixed(2);\n        P.ifConfirmed = 'מימוש חלקי';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'מימוש ' + P.target.toFixed(2);\n        break;\n      case 'DO_NOT_CHASE':\n        P.pullbackZone = [P.entry, P.entry + 0.35 * atr];\n        P.waitPrice = P.pullbackZone.map(function (x) { return x.toFixed(2); }).join('–');\n        P.nextStep = 'פולבק ל-' + P.waitPrice;\n        P.ifConfirmed = 'כניסה חלקית בפולבק';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'לא לרדוף · פולבק ' + P.waitPrice;\n        break;\n      case 'FAILED':\n        P.waitPrice = null;\n        if (P.invalidation == null) { P.nextStep = 'הרמה נשברה — אין כניסה'; P.ifConfirmed = 'setup חדש נדרש'; P.short = 'setup נכשל'; return P; }\n        P.nextStep = 'אין כניסה — הרמה נשברה';\n        P.ifConfirmed = 'setup חדש נדרש';\n        P.ifFailed = null;\n        P.short = 'setup נכשל';\n        break;\n    }\n    return P;\n  }\n}\n\n// One radar row. Status follows the execution state — the single source of\n// truth — so the row can never say CLOSE while the plan says there is nothing\n// to do.\nfunction radarRow(symbol, A, market, freshness) {\n  if (!A || !A.state) return { symbol: symbol, status: 'NO DATA', score: 0, why: 'אין נתונים', nearestR: Infinity, freshness: freshness || 'NO DATA' };\n  var S = A.state, b = S.bar, T = tactical(A), mom = momentum(A), BL = bottomLine(A, market), P = executionPlan(A, market);\n  var near = [T.support, T.resistance].filter(Boolean).map(function (l) { return Math.abs(l.r); });\n  var nearest = near.length ? Math.min.apply(null, near) : Infinity;\n  var ended = freshness === 'SESSION ENDED';\n\n  var status, why = P && P.short ? P.short : 'אין setup';\n  var st = P ? P.state : 'NO_SETUP';\n  if (freshness === 'NO DATA') { status = 'NO DATA'; why = 'אין נתונים'; }\n  else if (st === 'FAILED' || (S.announced === 'DOWN' && st === 'NO_SETUP')) { status = 'AVOID'; why = st === 'FAILED' ? 'setup נכשל' : 'מגמת ירידה מוכרזת'; }\n  else if (st === 'ACTIVE' || st === 'TAKE_PROFIT_AREA') status = 'ACTIVE';\n  else if (st === 'READY_PARTIAL' || st === 'READY_ADD') status = 'READY';\n  // CLOSE needs proximity AND a reason to look: a live plan step, not distance.\n  else if (st === 'WAITING_FOR_CONFIRMATION' || st === 'DO_NOT_CHASE') status = 'CLOSE';\n  else if (st === 'WAITING_FOR_ZONE' && nearest <= 1.0 && (BL.confidence >= 3 || mom.label === 'RECOVERY' || mom.label === 'PUSHING' || (b.volx >= 1.3 && !b.auction))) status = 'CLOSE';\n  else if (st === 'WAITING_FOR_ZONE') status = nearest <= 2 || BL.setup ? 'WATCH' : 'QUIET';\n  else status = 'QUIET';\n\n  // Rank by execution urgency first (spec 31), then status, then distance.\n  var URGENCY = { READY_PARTIAL: 0, READY_ADD: 1, TAKE_PROFIT_AREA: 2, ACTIVE: 3, WAITING_FOR_CONFIRMATION: 4, DO_NOT_CHASE: 5, WAITING_FOR_ZONE: 6, IN_ZONE: 6, NO_SETUP: 8, FAILED: 9 };\n  return {\n    symbol: symbol, status: status, why: why, score: BL.confidence, price: b.close,\n    structure: S.announced || S.trend, momentum: mom.label, nearestR: nearest,\n    support: T.support, resistance: T.resistance, volx: b.volx, auction: b.auction,\n    vwapAbove: b.aboveVwap, align: b.align, atr: T.atr, freshness: freshness || 'LIVE',\n    bl: BL, tactical: T, mom: mom, plan: P, action: P ? P.action : 'המתן',\n    pressure: (typeof pressure === 'function') ? pressure(A, { tactical: T, plan: P }) : null,\n    urgency: URGENCY[st] == null ? 7 : URGENCY[st], sessionEnded: ended\n  };\n}\n\nvar STATUS_ORDER = ['READY', 'CLOSE', 'ACTIVE', 'WATCH', 'QUIET', 'AVOID', 'NO DATA'];\nfunction sortRadar(list, mode) {\n  var arr = list.slice();\n  if (mode === 'symbol') return arr.sort(function (a, b) { return a.symbol < b.symbol ? -1 : 1; });\n  if (mode === 'score') return arr.sort(function (a, b) { return b.score - a.score || a.nearestR - b.nearestR; });\n  if (mode === 'volume') return arr.sort(function (a, b) { return (b.volx || 0) - (a.volx || 0); });\n  if (mode === 'distance') return arr.sort(function (a, b) { return a.nearestR - b.nearestR; });\n  return arr.sort(function (a, b) {\n    return ((a.urgency == null ? 7 : a.urgency) - (b.urgency == null ? 7 : b.urgency))\n      || (STATUS_ORDER.indexOf(a.status) - STATUS_ORDER.indexOf(b.status))\n      || (b.score - a.score) || (a.nearestR - b.nearestR);\n  });\n}\n\n// Market context from the analyses of SPY / QQQ (and optionally a sector ETF).\n// Bullish needs VWAP and EMA alignment to agree across the index ETFs.\nfunction marketContext(list) {\n  var have = list.filter(function (x) { return x && x.state; });\n  if (!have.length) return { label: 'Unavailable', score: 0, parts: [] };\n  var score = 0, parts = [];\n  have.forEach(function (x) {\n    var b = x.state.bar, s = 0;\n    s += b.aboveVwap ? 1 : -1;\n    s += b.align === 'bull' ? 1 : b.align === 'bear' ? -1 : 0;\n    if (x.state.announced === 'UP') s += 1; else if (x.state.announced === 'DOWN') s -= 1;\n    score += s; parts.push({ symbol: x.symbol, score: s, aboveVwap: b.aboveVwap, align: b.align, trend: x.state.trend, close: b.close, vwap: b.vwap });\n  });\n  var per = score / have.length;\n  return { label: per >= 1.5 ? 'Bullish' : per <= -1.5 ? 'Bearish' : 'Neutral', score: score, parts: parts };\n}\n\n// Bottom line: structure first, indicators only move confidence.\n// Never emits LONG without a confirmed structural event.\nfunction bottomLine(A, market) {\n  if (!A || !A.state) return null;\n  var S = A.state, b = S.bar, n = A.states.length, atr = b.atr20 || b.avgRange || 1;\n  var recent = function (type, within, pred) {\n    for (var i = n - 1; i >= Math.max(0, n - 1 - within); i--) {\n      var hit = A.states[i].events.find(function (e) { return e.type === type && (!pred || pred(e)); });\n      if (hit) return { e: hit, ago: n - 1 - i };\n    }\n    return null;\n  };\n  var conf = function (e) { return e.confirmed === true; };\n  var breakout = recent('breakout', 3, conf) || recent('strong_breakout', 3, conf);\n  var bullBreak = recent('structure_break', 3, function (e) { return e.side === 'bull' && e.confirmed === true; });\n  var retest = recent('retest_hold', 5);\n  var rejected = recent('rejected', 5), failed = recent('failed', 5);\n  var bearBreak = recent('structure_break', 10, function (e) { return e.side === 'bear' && e.confirmed === true; });\n  var supBreak = recent('support_break', 10, conf);\n  var vwapReclaim = recent('vwap_reclaim', 3);\n\n  // Levels above: every confirmed swing high of the last 90 bars, the 60-bar\n  // high, the day high and any reclaim level — ascending, merged if closer\n  // than a quarter range. First = watch, second = trigger, third = strong.\n  var above = [], below = [];\n  A.swings.forEach(function (w) {\n    if (w.kind !== 'H' || w.confirmedAt > n - 1 || n - 1 - w.i > 90 || w.price <= b.close) return;\n    above.push({ price: w.price, reason: 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time });\n  });\n  if (S.res) above.push({ price: S.res.price, reason: 'גבוה 60 נרות' + (S.res.time ? ' ' + S.res.time : '') });\n  if (b.dayHigh > b.close) above.push({ price: b.dayHigh, reason: 'גבוה היום' });\n  if (S.reclaim != null && S.reclaim > b.close) above.push({ price: S.reclaim, reason: 'רמה שנשברה — reclaim' });\n  if (S.sup) below.push({ price: S.sup.price, reason: S.sup.kind === 'postbreak' ? 'הנמוך מאז השבירה' : 'נמוך 60 נרות' + (S.sup.time ? ' ' + S.sup.time : '') });\n  if (S.lastSL && S.lastSL.price < b.close && !(S.reclaim != null && S.lastSL.price >= S.reclaim)) below.push({ price: S.lastSL.price, reason: 'swing ' + S.lastSL.label + ' ' + S.lastSL.time });\n  if (b.dayLow < b.close) below.push({ price: b.dayLow, reason: 'נמוך היום' });\n  var merge = function (arr, asc) {\n    arr.sort(function (x, y) { return asc ? x.price - y.price : y.price - x.price; });\n    var out = [];\n    arr.forEach(function (x) { var last = out[out.length - 1]; if (last && Math.abs(x.price - last.price) <= 0.25 * atr) { if (!/swing/.test(last.reason) && /swing/.test(x.reason)) last.reason = x.reason; return; } out.push(x); });\n    return out;\n  };\n  above = merge(above, true); below = merge(below, false);\n  var watch = above[0] || null, trigger = above[1] || null, strong = above[2] || null, invalid = below[0] || null;\n  var dist = function (lv) { return lv ? { pts: lv.price - b.close, atr: (lv.price - b.close) / atr } : null; };\n\n  // Structural setup?\n  var trendUp = S.trend === 'UP', announcedUp = S.announced === 'UP', announcedDown = S.announced === 'DOWN';\n  var setup = trendUp || !!breakout || !!bullBreak || !!retest;\n\n  // Confidence\n  var c = 0, why = [];\n  if (breakout || bullBreak) { c += 3; why.push('+3 פריצה/שבירה מבנית מאושרת'); }\n  if (trendUp) { c += 2; why.push('+2 HH/HL'); }\n  if (b.aboveVwap) { c += 1; why.push('+1 מעל VWAP'); } else { c -= 2; why.push('-2 מתחת VWAP'); }\n  if (b.align === 'bull') { c += 1; why.push('+1 EMA שורי'); } else if (b.align === 'bear') { c -= 1; why.push('-1 EMA דובי'); }\n  var volOk = b.volx >= 1.2 || (breakout && A.states[n - 1 - breakout.ago].bar.volx >= 1.2);\n  if (volOk) { c += 1; why.push('+1 נפח'); }\n  if (market && market.label === 'Bullish') { c += 1; why.push('+1 שוק'); } else if (market && market.label === 'Bearish') { c -= 2; why.push('-2 שוק נגד'); }\n  if (retest) { c += 1; why.push('+1 בדיקה חוזרת'); }\n  if (rejected) { c -= 2; why.push('-2 דחייה'); }\n  if (bearBreak || supBreak || failed) { c -= 3; why.push('-3 ביטול/שבירה'); }\n  c = Math.max(0, Math.min(10, c));\n\n  // Action\n  var action = 'WAIT', reason = 'אין setup מבני';\n  if (announcedDown || bearBreak || supBreak) { action = 'AVOID'; reason = announcedDown ? 'מגמת ירידה מוכרזת' : 'תמיכה/מבנה נשברו'; }\n  else if ((breakout || bullBreak) && c >= 6) { action = 'LONG'; reason = (breakout ? 'פריצה' : 'שבירת מבנה שורית') + ' מאושרת · ביטחון ' + c; }\n  else if (breakout || bullBreak) { action = 'LONG WATCH'; reason = 'פריצה מאושרת אבל ביטחון ' + c + '/10'; }\n  else if (setup && watch && (watch.price - b.close) <= 1.0 * atr) { action = 'LONG WATCH'; reason = 'מגמה עולה, ' + ((watch.price - b.close) / atr).toFixed(1) + '× טווח מהרמה הראשונה'; }\n  else if (setup && vwapReclaim) { action = 'LONG WATCH'; reason = 'VWAP נכבש מחדש בתוך מגמה עולה'; }\n  else if (setup) { action = 'WAIT'; reason = 'מגמה עולה, רחוק מהטריגר'; }\n\n  return {\n    action: action, reason: reason, confidence: c, why: why,\n    watch: watch ? { price: watch.price, reason: watch.reason, dist: dist(watch) } : null,\n    trigger: trigger ? { price: trigger.price, reason: trigger.reason, dist: dist(trigger) } : null,\n    strong: strong ? { price: strong.price, reason: strong.reason, dist: dist(strong) } : null,\n    reclaim: S.reclaim != null ? { price: S.reclaim, dist: dist({ price: S.reclaim }) } : null,\n    invalidation: invalid ? { price: invalid.price, reason: invalid.reason, dist: dist(invalid) } : null,\n    vwap: b.vwap, aboveVwap: b.aboveVwap, ema9: b.ema9, ema20: b.ema20, align: b.align, atr: atr,\n    market: market ? market.label : 'Unavailable', setup: setup\n  };\n}\n\n// Multi-day layer, time-of-day volume, path probability and the plain-language\n// \"what now\" block. Kept in its own file so engine.cjs stays the intraday core.\n\n// ---------------------------------------------------------------- volume by time of day\n// The closing auction is not comparable to a normal minute, so relative volume\n// is measured against the SAME minute on previous days. With no history the\n// baseline is null and callers fall back to the session average.\nfunction volumeBaseline(daysRows) {\n  var sum = {}, cnt = {}, days = 0;\n  (daysRows || []).forEach(function (rows) {\n    if (!rows || !rows.length) return;\n    days++;\n    rows.forEach(function (r) { sum[r.time] = (sum[r.time] || 0) + r.volume; cnt[r.time] = (cnt[r.time] || 0) + 1; });\n  });\n  if (days < 2) return null;\n  var by = {};\n  Object.keys(sum).forEach(function (t) { by[t] = sum[t] / cnt[t]; });\n  return { byTime: by, days: days };\n}\nfunction volxTod(bar, baseline) {\n  if (!baseline || !baseline.byTime[bar.time]) return null;\n  var b = baseline.byTime[bar.time];\n  return b > 0 ? Math.round(bar.volume / b * 100) / 100 : null;\n}\n\n// ---------------------------------------------------------------- daily layer\n// daysRows: array of one day's 1-minute rows, oldest first, today last.\n// A US regular session is 390 one-minute bars, 09:30 to 15:59, plus the 16:00\n// auction print. Anything materially short of that is a session still being\n// collected or one that was fetched mid-day — and aggregating it produces a\n// daily candle that is simply wrong, with no outward sign.\n//\n// Measured: a WMT session truncated at 13:20 reported a high of 106.44 when the\n// real high was 106.78, and that number reached the screen as \"yesterday's\n// high\". A partial session is now marked and excluded from anything that claims\n// to be a completed day.\nvar SESSION_BARS = 390, MIN_COVERAGE = 0.85;\n// Counting bars alone is the wrong test. A thinly traded name has no print in\n// some minutes and the feed omits them, so a COMPLETE session can arrive with\n// 343 bars — PGR, BLK and HON were all refused that way. What actually\n// distinguishes a complete session is that it SPANS the session: it starts at\n// the open and runs to the close. A day truncated at 13:20 fails that however\n// many bars it has.\nfunction sessionSpans(rows) {\n  if (!rows || !rows.length) return false;\n  var times = rows.map(function (r) { return r.time; }).sort();\n  return times[0] <= '09:35' && times[times.length - 1] >= '15:55';\n}\nfunction sessionCoverage(rows) {\n  if (!rows || !rows.length) return 0;\n  var regular = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n  return regular.length / SESSION_BARS;\n}\nfunction aggregateSession(rows) {\n  var o = rows[0].open, c = rows[rows.length - 1].close;\n  var h = -Infinity, l = Infinity, v = 0;\n  rows.forEach(function (r) { h = Math.max(h, r.high); l = Math.min(l, r.low); v += r.volume; });\n  var cov = sessionCoverage(rows), spans = sessionSpans(rows);\n  return { date: rows[0].date, open: o, high: h, low: l, close: c, volume: v, range: h - l,\n    closeStrength: h > l ? (c - l) / (h - l) : 0.5,\n    bars: rows.length, coverage: Math.round(cov * 1000) / 1000, spans: spans,\n    complete: spans && cov >= MIN_COVERAGE,\n    hasAuction: rows.some(function (r) { return r.time === '16:00'; }),\n    source: 'aggregated' };\n}\n\nfunction dailyContext(daysRows, opts) {\n  var o2 = opts || {};\n  var days = (daysRows || []).filter(function (r) { return r && r.length; });\n  if (!days.length) return null;\n  var bars = days.map(aggregateSession);\n  // An authoritative daily candle, when the caller has one, replaces the\n  // aggregation for that date entirely. Never a mix of the two.\n  if (o2.authoritative) {\n    var auth = {};\n    (o2.authoritative || []).forEach(function (b) { auth[b.date] = b; });\n    bars = bars.map(function (b) {\n      var a = auth[b.date];\n      if (!a) return b;\n      return { date: b.date, open: a.open, high: a.high, low: a.low, close: a.close,\n        volume: a.volume, range: a.high - a.low,\n        closeStrength: a.high > a.low ? (a.close - a.low) / (a.high - a.low) : 0.5,\n        bars: b.bars, coverage: b.coverage, complete: true, hasAuction: b.hasAuction,\n        source: 'authoritative' };\n    });\n  }\n  // Which of these IS today? Before the open there is no session for today at\n  // all, and treating the newest stored row as \"today\" pushed every previous-day\n  // value one session further back than its label claimed. The caller passes the\n  // current trading date; without one the old positional rule stands, but the\n  // result says which rule was used.\n  var todayDate = o2.todayDate || null;\n  var hasToday = todayDate ? bars.some(function (b) { return b.date === todayDate; }) : true;\n  var todayIdx = todayDate\n    ? (hasToday ? bars.map(function (b) { return b.date; }).indexOf(todayDate) : bars.length)\n    : bars.length - 1;\n  var today = bars[todayIdx] || bars[bars.length - 1] || null;\n  // \"Previous\" must mean the previous COMPLETED session. A partial day is not a\n  // day, and silently using one is how a truncated high became yesterday's high.\n  var prior = bars.slice(0, todayIdx).filter(function (b) { return b.complete; });\n  var excluded = bars.slice(0, todayIdx).filter(function (b) { return !b.complete; });\n  var atrDaily = prior.length ? prior.reduce(function (s, b) { return s + b.range; }, 0) / prior.length : today.range;\n  // Daily trend over the prior sessions: higher closes and higher lows.\n  var trend = 'RANGE', why = 'אין רצף יומי';\n  if (prior.length >= 3) {\n    var last3 = prior.slice(-3);\n    var upCloses = last3.filter(function (b, i) { return i === 0 || b.close > last3[i - 1].close; }).length;\n    var upLows = last3.filter(function (b, i) { return i === 0 || b.low > last3[i - 1].low; }).length;\n    if (upCloses >= 3 && upLows >= 2) { trend = 'UP'; why = 'סגירות ושפלים עולים'; }\n    else if (upCloses <= 1 && upLows <= 1) { trend = 'DOWN'; why = 'סגירות ושפלים יורדים'; }\n    else why = 'מעורב';\n  }\n  var prev = prior.length ? prior[prior.length - 1] : null;\n  // Multi-day levels: prior highs/lows that price reacted to more than once.\n  var levels = [];\n  prior.forEach(function (b) { levels.push({ price: b.high, why: 'גבוה ' + b.date }, { price: b.low, why: 'נמוך ' + b.date }); });\n  var gap = prev ? today.open - prev.close : 0;\n  return { days: bars.length, priorDays: prior.length, trend: trend, trendWhy: why,\n    incomplete: excluded.map(function (b) { return { date: b.date, bars: b.bars,\n      coverage: Math.round(b.coverage * 100) + '%' }; }),\n    hasToday: hasToday, todayDate: todayDate,\n    previousDate: prior.length ? prior[prior.length - 1].date : null,\n    dateRule: todayDate ? 'previous = newest COMPLETE session before ' + todayDate\n      : 'previous = the row before the last (no trading date supplied)',\n    sources: bars.map(function (b) { return { date: b.date, source: b.source, complete: b.complete }; }),\n    prevHigh: prev ? prev.high : null, prevLow: prev ? prev.low : null, prevClose: prev ? prev.close : null,\n    atrDaily: atrDaily, levels: levels, gap: gap, today: today,\n    closeStrength: prev ? prev.closeStrength : null };\n}\n\n\n\n// ---------------------------------------------------------------- level state\n// The current condition of a level, decided from where price is now and what it\n// did on its way here. A support that was lost and then regained reads as\n// reclaimed — never as \"breaking\", which is what it did ten candles ago.\nvar LEVEL_TEXT = {\n  approaching: 'מתקרב', testing: 'נבחנת', broken: 'נשברה', reclaimed: 'נכבשה מחדש',\n  reclaiming: 'נשברה וחוזרת מעליה', held: 'נשמרת', rejected: 'נדחתה', lost: 'אבדה', far: 'רחוקה'\n};\nfunction levelState(A, level, isSupport, look) {\n  if (!A || !A.state || level == null) return null;\n  var bars = A.bars, n = bars.length, b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  var w = bars.slice(Math.max(0, n - (look || 20)));\n  var d = (b.close - level) / atr;                       // + = price above the level\n  var near = w.filter(function (x) { return x.low <= level + 0.25 * atr && x.high >= level - 0.25 * atr; });\n  var closedBelow = w.filter(function (x) { return x.close < level - 0.15 * atr; }).length;\n  var closedAbove = w.filter(function (x) { return x.close > level + 0.15 * atr; }).length;\n  var lastTwoBelow = n >= 2 && bars[n - 1].close < level - 0.1 * atr && bars[n - 2].close < level - 0.1 * atr;\n  var lastTwoAbove = n >= 2 && bars[n - 1].close > level + 0.1 * atr && bars[n - 2].close > level + 0.1 * atr;\n  var recoveries = w.filter(function (x) { return x.low < level && x.close > level; }).length;\n  // Bars whose LOW went through a support, or whose HIGH went through a\n  // resistance, without closing beyond it: pierced, not merely approached.\n  var pierced = isSupport\n    ? w.filter(function (x) { return x.low < level - 0.02 * atr; }).length\n    : w.filter(function (x) { return x.high > level + 0.02 * atr; }).length;\n  var rejections = w.filter(function (x) { return x.high > level && x.close < level; }).length;\n\n  var state;\n  if (isSupport) {\n    // \"Held\" must mean the level was never given up. Requiring only two\n    // recoveries let a support that CLOSED below five times in a row still read\n    // as held: GOOGL closed 337.31, 337.31, 337.17, 337.32, 337.275 under\n    // 337.57 and came back at 337.75, which is a break and a reclaim, not a\n    // level that was defended.\n    if (lastTwoBelow) state = 'broken';\n    else if (closedBelow >= 2 && lastTwoAbove) state = 'reclaimed';\n    else if (closedBelow >= 2 && d > 0) state = 'reclaiming';\n    // Closes are not the whole story. GOOGL's 338.51 was traded through on four\n    // separate bars — lows of 338.4233, 338.4096, 338.43, 338.42 — and still\n    // read as held because none of them CLOSED below. A level price went\n    // through is not a level that was defended.\n    else if (closedBelow === 0 && pierced === 0 && recoveries >= 2 && d > 0) state = 'held';\n    else if (closedBelow === 0 && pierced >= 2 && d > 0) state = 'reclaiming';\n    else if (Math.abs(d) <= 0.25) state = 'testing';\n    else if (Math.abs(d) <= 1.0) state = 'approaching';\n    else state = 'far';\n  } else {\n    if (lastTwoAbove) state = 'broken';                  // resistance broken = price accepted above\n    // \"Lost\" means the level stopped functioning. A resistance with price\n    // sitting UNDER it is still a resistance: it was rejected, not lost.\n    else if (closedAbove >= 2 && lastTwoBelow && d >= 0) state = 'lost';\n    else if (closedAbove >= 2 && lastTwoBelow) state = 'rejected';\n    else if (rejections >= 1 && d < 0) state = 'rejected';\n    else if (Math.abs(d) <= 0.25) state = 'testing';\n    else if (Math.abs(d) <= 1.0) state = 'approaching';\n    else state = 'far';\n  }\n  return { price: level, state: state, text: LEVEL_TEXT[state], distanceR: d,\n    touches: near.length, recoveries: recoveries, rejections: rejections, isSupport: !!isSupport };\n}\n\n// ---------------------------------------------------------------- buying / selling pressure\n//\n// IMPORTANT: this bar feed carries no order book and no trade-by-trade tape.\n// There is no bid/ask, no Level 2, no per-trade aggressor flag. What follows is\n// pressure INFERRED FROM CANDLES: where each minute closed inside its own range,\n// weighted by that minute's volume. Every result is labelled `source: 'candles'`\n// so the UI can say so and never imply it read the book.\n//\n//   buyVolume  = volume * (close - low)  / (high - low)\n//   sellVolume = volume * (high - close) / (high - low)\n//\n// which is the standard close-location value applied to volume. A minute that\n// closes on its high counts fully as buying, on its low fully as selling.\nfunction pressure(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, bars = A.bars, n = bars.length;\n  if (!n) return null;\n  var WIN = c.window || 15, RECENT = c.recent || 5;\n  var b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n\n  var split = function (list) {\n    var buy = 0, sell = 0;\n    list.forEach(function (x) {\n      var rng = x.high - x.low;\n      // A zero-range minute has no information about who won it.\n      if (rng <= 0 || !x.volume) return;\n      var clv = (x.close - x.low) / rng;\n      buy += x.volume * clv; sell += x.volume * (1 - clv);\n    });\n    var tot = buy + sell;\n    return { buy: buy, sell: sell, total: tot, buyPct: tot > 0 ? buy / tot * 100 : 50 };\n  };\n\n  // The closing auction is excluded: it is one print, not a minute of trading.\n  var usable = bars.filter(function (x) { return !x.auction; });\n  var win = usable.slice(-WIN), recent = usable.slice(-RECENT), prior = usable.slice(-(WIN), -RECENT);\n  if (win.length < 3) return null;\n\n  var W = split(win), R = split(recent), P = prior.length >= 3 ? split(prior) : null;\n  var buyPct = Math.round(W.buyPct), sellPct = 100 - buyPct;\n\n  // Strengthening / weakening is measured per side, on volume actually traded,\n  // not on the percentage alone: a side can gain share while doing less.\n  var perBar = function (s, list) { return list.length ? s / list.length : 0; };\n  var dBuy = P ? perBar(R.buy, recent) - perBar(P.buy, prior) : 0;\n  var dSell = P ? perBar(R.sell, recent) - perBar(P.sell, prior) : 0;\n  var scale = P ? Math.max(perBar(P.buy, prior) + perBar(P.sell, prior), 1) : 1;\n  // A side is only strengthening if it is doing MORE and taking MORE of the\n  // flow. Without the share test a volume surge would read as both sides\n  // strengthening at once, which tells the trader nothing.\n  var dShare = P ? R.buyPct - P.buyPct : 0;\n  var trendOf = function (d, shareDelta) {\n    var rel = d / scale;\n    if (rel >= 0.15 && shareDelta > -3) return 'מתחזקים';\n    if (rel <= -0.15 || shareDelta < -8) return 'נחלשים';\n    return 'ללא שינוי';\n  };\n  var buyersTrend = trendOf(dBuy, dShare), sellersTrend = trendOf(dSell, -dShare);\n\n  // Is the nearby level being defended or absorbed?\n  var T = c.tactical || tactical(A);\n  // Level condition comes from levelState(), which is anchored to the current\n  // price. Volume only adds the \"absorbed\" nuance on top of it.\n  var levelVerdict = function (level, isSupport) {\n    if (!level) return null;\n    var ls = levelState(A, level.price, isSupport);\n    if (!ls) return null;\n    var near = usable.filter(function (x) {\n      return x.low <= level.price + 0.3 * atr && x.high >= level.price - 0.3 * atr;\n    }).slice(-12);\n    var vol = near.length ? near.reduce(function (s, x) { return s + x.volume; }, 0) / near.length : 0;\n    var avgVol = usable.reduce(function (s, x) { return s + x.volume; }, 0) / usable.length;\n    var heavy = avgVol > 0 && vol / avgVol >= 1.2;\n    var parked = near.filter(function (x) { return Math.abs(x.close - level.price) <= 0.2 * atr; }).length;\n    var absorbed = heavy && parked >= 3 && (ls.state === 'testing' || ls.state === 'approaching');\n    return { price: level.price, state: ls.state, verdict: absorbed ? 'נבלעת' : ls.text,\n      distanceR: ls.distanceR, touches: near.length, heavy: heavy,\n      held: ls.recoveries, through: isSupport ? (ls.state === 'broken' ? 2 : 0) : (ls.state === 'broken' ? 2 : 0) };\n  };\n  var sup = levelVerdict(T.support, true), res = levelVerdict(T.resistance, false);\n\n  // Does the flow agree with the setup on the table?\n  var plan = c.plan || null;\n  var bullishSetup = plan && ['WAITING_FOR_ZONE', 'IN_ZONE', 'READY_PARTIAL', 'WAITING_FOR_CONFIRMATION', 'READY_ADD', 'ACTIVE', 'TAKE_PROFIT_AREA'].indexOf(plan.state) >= 0;\n  var side = buyPct >= 58 ? 'buyers' : buyPct <= 42 ? 'sellers' : 'balanced';\n  // Who is winning now and which way that is heading are two different facts.\n  // \"Buyers 64%\" plus \"buyers weakening\" is not support for a long — it is a\n  // lead that is being given back, and the wording has to say so.\n  // When BOTH sides are fading, the buyers' edge may be widening but their\n  // pressure is not strengthening. Saying it is contradicts the two trend\n  // labels printed directly above it.\n  // Who leads and how each side is moving are separate facts, and the sentence\n  // must be built from BOTH. Saying \"buyers strengthening\" because the SELLERS\n  // faded, while the buyers' own trend reads unchanged, contradicts the two\n  // labels printed directly above it.\n  var UP = 'מתחזקים', DOWN = 'נחלשים', SAME = 'ללא שינוי';\n  var leaderTrend = side === 'sellers' ? sellersTrend : buyersTrend;\n  var otherTrend = side === 'sellers' ? buyersTrend : sellersTrend;\n  // 'own'      the leading side is itself doing more\n  // 'relative' the leader is unchanged or fading, but the other side fades faster\n  // 'fading'   the leading side is giving its lead back\n  var direction = leaderTrend === UP ? 'own'\n    : leaderTrend === DOWN ? (otherTrend === DOWN ? 'relative' : 'fading')\n    : otherTrend === DOWN ? 'relative'\n    : otherTrend === UP ? 'fading' : 'steady';\n\n  var leadWord = side === 'sellers' ? 'המוכרים' : 'הקונים';\n  var conclusion;\n  if (side === 'balanced') {\n    conclusion = buyersTrend === UP && sellersTrend !== UP ? 'הכוחות שקולים, הקונים משתפרים'\n      : sellersTrend === UP && buyersTrend !== UP ? 'הכוחות שקולים, המוכרים משתפרים'\n      : 'הכוחות שקולים';\n  } else if (direction === 'own') {\n    conclusion = leadWord + ' מובילים והלחץ שלהם מתחזק';\n  } else if (direction === 'relative') {\n    // The leader is not stronger. The gap widened because the other side eased.\n    conclusion = leadWord + ' מובילים; היתרון היחסי שלהם משתפר משום ש'\n      + (side === 'sellers' ? 'הקונים' : 'המוכרים') + ' נחלשים'\n      + (leaderTrend === DOWN ? ', אך גם הלחץ שלהם עצמו נחלש' : '');\n  } else if (direction === 'fading') {\n    conclusion = leadWord + ' עדיין מובילים, אבל הלחץ שלהם נחלש';\n  } else {\n    conclusion = leadWord + ' מובילים, ללא שינוי בעוצמה';\n  }\n  var agreement = !bullishSetup ? 'לא רלוונטי'\n    : side === 'buyers' ? (direction === 'own' ? 'תומך'\n        : direction === 'fading' ? 'תומך אך נחלש'\n        : direction === 'relative' ? 'תומך במידה' : 'תומך')\n    : side === 'sellers' ? 'סותר'\n    : (buyersTrend === 'נחלשים' ? 'נוטה נגד' : 'ניטרלי');\n\n  // Net contribution to the up-side, in the same units the probability model uses.\n  var tilt = 0;\n  if (side === 'buyers') tilt += 1; else if (side === 'sellers') tilt -= 1;\n  if (buyersTrend === 'מתחזקים') tilt += 1;\n  if (buyersTrend === 'נחלשים') tilt -= 1;\n  if (sellersTrend === 'מתחזקים') tilt -= 1;\n  if (sellersTrend === 'נחלשים') tilt += 1;\n  if (sup && (sup.state === 'held' || sup.state === 'reclaimed')) tilt += 1;\n  if (sup && (sup.state === 'broken' || sup.verdict === 'נבלעת')) tilt -= 1;\n  if (res && res.state === 'broken') tilt += 1;\n  if (res && (res.state === 'rejected' || res.verdict === 'נבלעת')) tilt -= 1;\n\n  return {\n    source: 'candles',                       // never 'orderbook' — we have none\n    hasOrderBook: false, hasTape: false,\n    buyPct: buyPct, sellPct: sellPct,\n    side: side, buyersTrend: buyersTrend, sellersTrend: sellersTrend,\n    support: sup, resistance: res, agreement: agreement, tilt: tilt,\n    direction: direction, conclusion: conclusion,\n    window: win.length, recentWindow: recent.length,\n    summary: 'קונים ' + buyPct + '% · מוכרים ' + sellPct + '%',\n    trendSummary: 'קונים ' + buyersTrend + ' · מוכרים ' + sellersTrend\n  };\n}\n\n// ---------------------------------------------------------------- features\n// A compact description of \"situations like this one\", used both to bucket\n// history and to score the model fallback.\nfunction features(A, ctx, upper, lower) {\n  var S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var vx = (ctx && ctx.baseline) ? volxTod(b, ctx.baseline) : null;\n  if (vx == null) vx = b.volx;\n  var mom = momentum(A);\n  return {\n    structure: S.announced || S.trend,\n    vwap: b.aboveVwap ? 'above' : 'below',\n    ema: b.align,\n    momentum: mom.label,\n    vol: vx >= 1.5 ? 'high' : vx >= 0.8 ? 'normal' : 'low',\n    daily: ctx && ctx.daily ? ctx.daily.trend : 'NA',\n    dUp: Math.round(((upper - b.close) / atr) * 2) / 2,\n    dDown: Math.round(((b.close - lower) / atr) * 2) / 2,\n    tod: b.time < '10:30' ? 'open' : b.time < '14:30' ? 'mid' : 'late',\n    flow: (function () { var p = (ctx && ctx.pressure) || pressure(A, {}); return p ? p.side : 'NA'; })()\n  };\n}\nfunction bucketKey(f, coarse) {\n  return coarse\n    ? [f.structure, f.vwap, f.daily].join('|')\n    : [f.structure, f.vwap, f.ema, f.momentum, f.vol, f.daily, f.dUp, f.dDown].join('|');\n}\n\n// ---------------------------------------------------------------- calibration\n// Replays the loaded historical days and records, for each situation, whether\n// price rose by the required distance before falling by the other one, within\n// the horizon. These are real observed outcomes from the symbol's own history —\n// when there are too few of them the caller must not show a percentage.\nfunction calibrate(daysRows, opts) {\n  var o = opts || {}, horizon = o.horizon || 60, step = o.step || 2;\n  var table = {}, coarse = {}, total = 0;\n  (daysRows || []).forEach(function (rows) {\n    if (!rows || rows.length < 120) return;\n    var A = analyze(rows, o.engine || { K: 3 });\n    if (!A) return;\n    var bars = A.bars, n = bars.length;\n    for (var i = 60; i < n - horizon; i += step) {\n      var b = bars[i], atr = b.atr20 || b.avgRange || 1;\n      // sample a spread of target distances so the table can answer different asks\n      [0.5, 1, 1.5, 2].forEach(function (du) {\n        [0.5, 1, 1.5, 2].forEach(function (dd) {\n          var up = b.close + du * atr, dn = b.close - dd * atr, hit = null;\n          for (var k = i + 1; k <= i + horizon && k < n; k++) {\n            if (bars[k].high >= up) { hit = 'up'; break; }\n            if (bars[k].low <= dn) { hit = 'down'; break; }\n          }\n          if (!hit) return;\n          var sub = { state: A.states[i], bar: b };\n          var f = features({ state: A.states[i], bars: bars.slice(0, i + 1), swings: A.swings, states: A.states.slice(0, i + 1) },\n            { daily: o.daily, baseline: o.baseline }, up, dn);\n          [bucketKey(f, false), 'C:' + bucketKey(f, true) + '|' + f.dUp + '|' + f.dDown].forEach(function (key, idx) {\n            var t = idx === 0 ? table : coarse;\n            t[key] = t[key] || { n: 0, up: 0 };\n            t[key].n++; if (hit === 'up') t[key].up++;\n          });\n          total++;\n        });\n      });\n    }\n  });\n  return { table: table, coarse: coarse, total: total };\n}\n\n// ---------------------------------------------------------------- probability\n// Answers ONE defined question: does price reach `upper` before `lower`, within\n// `horizonMin` minutes. Empirical when the symbol's own history has enough\n// comparable cases; otherwise a transparent weighted model, reported as a bias\n// with a confidence score rather than a fake percentage.\n// Pull an estimate toward 50 in proportion to how much the model actually\n// knows. Full confidence (>=70) keeps the number as computed.\nfunction shrink(pct, confidence) {\n  var f = Math.max(0, Math.min(1, confidence / 70));\n  return Math.round(50 + (pct - 50) * f);\n}\n\n// How much of the session that SHOULD have happened by now do we actually hold?\n// A window that starts at 11:02 on a day that opened at 09:30 is not a session:\n// its open is not the open, its VWAP is not the VWAP, and its volume is a\n// fraction. Everything derived from the session is refused until this passes.\nfunction sessionCompleteness(rows, opts) {\n  var o = opts || {};\n  var out = { firstBar: null, lastBar: null, receivedMinutes: 0, expectedMinutes: null,\n    coveragePct: null, fromOpen: false, complete: false };\n  if (!rows || !rows.length) return out;\n  var reg = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n  if (!reg.length) return out;\n  var times = reg.map(function (r) { return r.time; }).sort();\n  out.firstBar = times[0];\n  out.lastBar = times[times.length - 1];\n  var seen = {}; reg.forEach(function (r) { seen[r.time] = 1; });\n  out.receivedMinutes = Object.keys(seen).length;\n  // Minutes that should exist between the open and the newest bar we hold.\n  var toMin = function (t) { var p = t.split(':'); return (+p[0]) * 60 + (+p[1]); };\n  out.expectedMinutes = Math.max(1, toMin(out.lastBar) - toMin('09:30') + 1);\n  out.coveragePct = Math.round(out.receivedMinutes / out.expectedMinutes * 100);\n  out.fromOpen = out.firstBar <= '09:35';\n  out.missingFromOpen = out.fromOpen ? 0 : toMin(out.firstBar) - toMin('09:30');\n  // Holes AFTER the first bar are a separate failure and were not reported at\n  // all: 49 bars scattered between 11:02 and 13:44 is not the same thing as a\n  // clean window starting late. Both numbers, always.\n  out.spanMinutes = toMin(out.lastBar) - toMin(out.firstBar) + 1;\n  out.missingInside = Math.max(0, out.spanMinutes - out.receivedMinutes);\n  out.missingTotal = out.missingFromOpen + out.missingInside;\n  out.contiguous = out.missingInside === 0;\n  // Both conditions: it must start at the open AND have most of the minutes in\n  // between. Either alone lets a hole through.\n  out.complete = out.fromOpen && out.coveragePct >= (o.minCoverage || 85);\n  return out;\n}\n\nfunction pathProbability(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = c.tactical || tactical(A);\n  var upper = c.upper != null ? c.upper : (T.resistance ? T.resistance.price : b.close + atr);\n  var lower = c.lower != null ? c.lower : (T.support ? T.support.price : b.close - atr);\n  var horizon = c.horizonMin || 60;\n  var f = features(A, c, upper, lower);\n  var out = { upper: upper, lower: lower, horizonMin: horizon, features: f, why: [] };\n\n  // A probability is only meaningful if the two levels are far enough apart to\n  // mean something for this instrument. Asking whether price reaches 146.63\n  // before 146.62 — one cent, a fraction of a normal minute's range — produces\n  // a confident-looking number about nothing.\n  // The question only means \"up or down\" when the two levels BRACKET the price.\n  // With both below it, \"reaches 187.34 before 187.27\" asks which small decline\n  // happens first — and reporting that as UP 78% reads as a claim the stock will\n  // rise. It also hands the model a free +6 for \"the upper level is closer\",\n  // which is pure geometry: three cents against ten.\n  var px = b.close;\n  out.price = px;\n  out.bracketed = lower < px && px < upper;\n  // \"Reaches 338.66 before 338.63\" with price at 338.61 asks whether the far\n  // level is touched before the near one on the SAME side, which cannot happen\n  // on a continuous path — 338.63 is passed on the way. The bracket test below\n  // catches it, but say plainly why rather than reporting it as a coincidence.\n  out.samSideOrdering = (upper > px && lower > px && lower < upper)\n    || (upper < px && lower < px && lower > upper);\n  if (!out.bracketed) {\n    out.up = null; out.down = null; out.confidence = 0;\n    out.notDirectional = true;\n    out.side = upper <= px ? 'both_below' : 'both_above';\n    if (out.samSideOrdering)\n      out.why.push('הרמות באותו צד והרחוקה נמצאת אחרי הקרובה — לא ניתן להגיע לאחת לפני השנייה בדרך רציפה');\n    out.why.push(upper <= px\n      ? 'שתי הרמות מתחת למחיר — השאלה אינה עלייה מול ירידה אלא איזו ירידה מגיעה קודם, ולכן לא מוצגת כהסתברות כיוון'\n      : 'שתי הרמות מעל המחיר — השאלה אינה עלייה מול ירידה, ולכן לא מוצגת כהסתברות כיוון');\n    return out;\n  }\n\n  var band = Math.abs(upper - lower);\n  var MIN_BAND_ATR = 0.25;\n  if (atr > 0 && band < MIN_BAND_ATR * atr) {\n    out.up = null; out.down = null; out.confidence = 0; out.meaningless = true;\n    out.band = band; out.minBand = MIN_BAND_ATR * atr;\n    out.why.push('הרמות קרובות מדי זו לזו (' + (Math.round(band * 100) / 100) +\n      ') ביחס לתנודה הרגילה — הסתברות כאן חסרת משמעות');\n    return out;\n  }\n\n  // 1. empirical\n  if (c.calibration) {\n    var exact = c.calibration.table[bucketKey(f, false)];\n    var coarse = c.calibration.coarse['C:' + bucketKey(f, true) + '|' + f.dUp + '|' + f.dDown];\n    var hit = (exact && exact.n >= 30) ? exact : (coarse && coarse.n >= 30) ? coarse : null;\n    if (hit) {\n      out.source = 'empirical'; out.n = hit.n;\n      out.up = Math.round(hit.up / hit.n * 100);\n      out.confidence = Math.min(90, 40 + Math.round(Math.min(hit.n, 400) / 8));\n      out.raw = out.up;\n      out.up = shrink(out.raw, out.confidence);\n      out.down = 100 - out.up;\n      out.lowConfidence = out.confidence < 50;\n      out.why.push(hit.n + ' מקרים דומים בהיסטוריה של הנייר');\n      return out;\n    }\n    out.sampled = (exact ? exact.n : 0) + (coarse ? coarse.n : 0);\n  }\n\n  // 2. transparent model. Each layer moves the odds; none of it is invented,\n  // and the result is labelled as a model bias, not an observed frequency.\n  var score = 0, add = function (pts, txt) { if (!pts) return; score += pts; out.why.push((pts > 0 ? '+' : '') + pts + ' ' + txt); };\n  add(f.structure === 'UP' ? 2 : f.structure === 'DOWN' ? -2 : 0, 'מבנה תוך-יומי ' + f.structure);\n  add(f.daily === 'UP' ? 2 : f.daily === 'DOWN' ? -2 : 0, 'מגמה רב-יומית ' + f.daily);\n  add(f.vwap === 'above' ? 1 : -1, 'מחיר ' + (f.vwap === 'above' ? 'מעל' : 'מתחת') + ' VWAP');\n  add(f.ema === 'bull' ? 1 : f.ema === 'bear' ? -1 : 0, 'EMA ' + f.ema);\n  add(f.momentum === 'PUSHING' || f.momentum === 'RECOVERY' ? 1 : f.momentum === 'SELLING' ? -1 : 0, 'מומנטום ' + f.momentum);\n  add(c.market === 'Bullish' ? 1 : c.market === 'Bearish' ? -1 : 0, 'שוק ' + (c.market || 'לא ידוע'));\n  var pres = c.pressure || pressure(A, { tactical: T });\n  if (pres) {\n    out.pressure = pres;\n    add(Math.max(-3, Math.min(3, pres.tilt)), 'לחץ קונים/מוכרים ' + pres.buyPct + '/' + pres.sellPct);\n  }\n  // Distance asymmetry: the nearer level is simply likelier to be reached.\n  var dU = (upper - b.close) / atr, dD = (b.close - lower) / atr;\n  var geom = dU + dD > 0 ? (dD - dU) / (dU + dD) : 0;\n  add(Math.round(geom * 3), 'הרמה ' + (geom > 0 ? 'העליונה' : 'התחתונה') + ' קרובה יותר');\n  var pct = Math.round(100 / (1 + Math.exp(-score / 3.2)));\n  out.source = 'model';\n  out.confidence = Math.max(20, Math.min(55, 25 + Math.abs(score) * 3));\n  out.raw = Math.max(10, Math.min(90, pct));\n  // A number like 87% next to \"confidence 43\" reads as certainty the model does\n  // not have. The displayed probability is pulled toward 50 in proportion to\n  // the confidence, so the two can never tell different stories.\n  out.up = shrink(out.raw, out.confidence);\n  out.down = 100 - out.up;\n  out.lowConfidence = out.confidence < 50;\n  out.bias = out.up >= 60 ? 'נטייה שורית' : out.up <= 40 ? 'נטייה דובית' : 'ללא נטייה ברורה';\n  return out;\n}\n\n// ---------------------------------------------------------------- what now\n// Plain language. No HH/HL/VWAP/R in the user-facing strings.\nvar ACTIONS = {\n  DO_NOT_BUY: 'לא לקנות',\n  START_WATCHING: 'להתחיל לעקוב',\n  WAIT_FOR_CONFIRMATION: 'לחכות לאישור',\n  ENTRY_AVAILABLE: 'אפשר להיכנס',\n  HOLD: 'להחזיק',\n  TAKE_PARTIAL: 'לממש חלק',\n  MOVE_STOP: 'להעלות סטופ',\n  EXIT: 'לצאת',\n  SETUP_CANCELLED: 'ה-setup בוטל',\n  SESSION_CLOSED: 'המסחר הסתיים',\n  DATA_STALE: 'נתונים לא עדכניים — לא לסחור',\n  WATCH_ONLY: 'לא לסחור — רק לעקוב',\n  DO_NOT_CHASE: 'לא לרדוף — להמתין לפולבק'\n};\nvar PLAN_TO_ACTION = {\n  NO_SETUP: 'DO_NOT_BUY',\n  WAITING_FOR_ZONE: 'START_WATCHING',\n  IN_ZONE: 'WAIT_FOR_CONFIRMATION',\n  READY_PARTIAL: 'ENTRY_AVAILABLE',\n  WAITING_FOR_CONFIRMATION: 'WAIT_FOR_CONFIRMATION',\n  READY_ADD: 'ENTRY_AVAILABLE',\n  ACTIVE: 'HOLD',\n  TAKE_PROFIT_AREA: 'TAKE_PARTIAL',\n  DO_NOT_CHASE: 'DO_NOT_CHASE',\n  FAILED: 'SETUP_CANCELLED'\n};\nfunction n2(x) { return x == null ? null : (Math.round(x * 100) / 100).toFixed(2); }\nfunction scenarioLabelFor(P, watch) {\n  if (!P || !P.kind) return 'ללא setup';\n  if (P.kind === 'breakout') return 'פריצה מעל ' + n2(watch);\n  return P.zone ? 'חזרה לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) : 'פולבק';\n}\n\nfunction whatNow(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = c.tactical || tactical(A);\n  var P = c.plan || executionPlan(A, { label: c.market || 'Neutral' });\n  var ended = c.sessionEnded;\n\n  var watch = T.resistance ? T.resistance.price : (P && P.zone ? P.zone[1] : b.dayHigh);\n  var hasZone = !!(P && P.zone && P.entry != null && P.addAbove != null);\n  var lower = T.support ? T.support.price : (P && P.invalidation != null ? P.invalidation : b.dayLow);\n  var prob = c.probability || pathProbability(A, { tactical: T, upper: watch, lower: lower,\n    horizonMin: 60, daily: c.daily, baseline: c.baseline, calibration: c.calibration, market: c.market });\n\n  // ---- one active scenario only. The plan already picked pullback OR\n  // breakout; whatNow must never narrate the other one as a live instruction.\n  var scenario = !P || !P.kind ? { kind: 'none', label: 'אין setup פעיל' }\n    : P.kind === 'breakout' ? { kind: 'breakout', label: 'פריצה מעל ' + n2(watch) }\n    : { kind: 'pullback', label: 'חזרה לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) };\n\n  // ---- no-edge gate. When the model's own numbers say there is nothing here,\n  // it must not offer an entry \"if X happens\".\n  var edge = c.edge || null;\n  var noEdge = !!(edge && edge.noEdge);\n  var stale = !!c.stale;\n  // The model's own cancellation rule, applied to the model's own price. If the\n  // last close is already through the level that voids the idea, the idea is\n  // void — it cannot still read START_WATCHING.\n  var cancelAt = (c.levels && c.levels.tacticalStop != null) ? c.levels.tacticalStop\n    : (P && P.invalidation != null ? P.invalidation : null);\n  var alreadyCancelled = !ended && cancelAt != null && b.close != null && b.close < cancelAt;\n  // Staleness outranks the plan. A setup derived from bars that stopped hours\n  // ago is a description of a market that has already moved.\n  var actionKey = ended ? 'SESSION_CLOSED'\n    : stale ? 'DATA_STALE'\n    : alreadyCancelled ? 'SETUP_CANCELLED'\n    : noEdge ? 'WATCH_ONLY'\n    : (PLAN_TO_ACTION[P ? P.state : 'NO_SETUP'] || 'DO_NOT_BUY');\n  var W = { action: actionKey, actionText: ACTIONS[actionKey], sessionEnded: !!ended,\n    stale: stale, cancelled: alreadyCancelled, price: b.close, watch: watch,\n    // A probability computed from stale bars is not a probability about now.\n    // A probability is a claim about the next hour. After the close there is no\n    // next hour, and on stale bars it is a claim about a market that has moved.\n    probability: (stale || ended || (prob && (prob.meaningless || prob.notDirectional))) ? null : prob,\n    probabilityRaw: prob || null,\n    plan: P, scenario: (stale || alreadyCancelled) ? { kind: 'none', label: 'אין setup פעיל' } : scenario,\n    noEdge: noEdge, edge: edge, up: [], down: [], why: [] };\n\n  // The one line that matters\n  if (stale) W.next = 'הנרות האחרונים ישנים — אין כאן איתות חי. הרמות למטה הן לעיון בלבד.';\n  else if (alreadyCancelled) W.next = 'המחיר כבר מתחת ל-' + n2(cancelAt) + ' — התרחיש בוטל. לחכות למבנה חדש.';\n  else if (ended) W.next = 'המסחר הסתיים — הרמות למטה הן לקראת המסחר הבא, לא להוראה עכשיו';\n  else if (noEdge) W.next = 'אין כרגע יתרון מספיק לכניסה. לעקוב אם המחיר מגיע ל-' + n2(watch) + '.';\n  else if (P && P.state === 'FAILED') W.next = 'אין כניסה. צריך מצב חדש לפני שמסתכלים שוב.';\n  else if (P && (P.state === 'READY_PARTIAL' || P.state === 'READY_ADD')) W.next = 'המחיר במקום שתכננו — ' + P.headline;\n  else W.next = 'לשים לב אם המחיר מגיע ל-' + n2(watch);\n\n  // If it goes up\n  var above = T.above.filter(function (l) { return l.price > b.close + 0.05 * atr; });\n  // The narrative must quote the SAME targets the levels block shows. Deriving\n  // them separately here is how the text came to say 'second target 146.66'\n  // while the levels said 146.73.\n  var canon = c.levels || null;\n  var t1 = (canon && canon.target1 != null) ? canon.target1\n    : (above[1] ? above[1].price : watch + 1.2 * atr);\n  var t2 = (canon && canon.target2 != null) ? canon.target2\n    : (above[2] && above[2].price >= t1 + 0.2 * atr ? above[2].price : t1 + 1.5 * atr);\n  // Two targets that print as the same number are not two targets. On quiet or\n  // low-priced instruments the rounded levels collided.\n  // Compare what will actually be PRINTED. Two levels 0.004 apart are distinct\n  // numbers that render as the same string, which is what the reader sees.\n  if (t1 != null && t2 != null && n2(t1) === n2(t2)) t2 = t1 + Math.max(0.01, 1.0 * atr);\n  if (stale) {\n    // Reference only. The last idea is described in the past tense, never as\n    // something to act on.\n    W.up.push('התרחיש האחרון שנרשם: ' + scenarioLabelFor(P, watch) + ' — לעיון בלבד');\n    W.up.push('אין הוראת כניסה על נתונים ישנים');\n  } else if (noEdge && !ended) {\n    // What would have to change before any entry is even discussed.\n    W.up.push('אם המחיר עולה מעל ' + n2(watch) + ' ונשאר שם — ייבחן מחדש');\n    W.up.push('כרגע אין הוראת כניסה');\n  } else if (!ended) {\n    if (P && P.state === 'READY_PARTIAL' && hasZone) {\n      W.up.push('אם המחיר נשאר מעל ' + n2(P.zone[0]) + ' — אפשר להיכנס בחלק מהסכום סביב ' + n2(P.entry));\n      W.up.push('אישור חזק יותר מעל ' + n2(P.addAbove) + ' (המערכת אינה יודעת אם יש פוזיציה)');\n    } else if (scenario.kind === 'breakout' && P.entry != null) {\n      W.up.push('אם עובר את ' + n2(watch) + ' ונשאר מעל — אפשר להיכנס סביב ' + n2(P.entry));\n    } else if (scenario.kind === 'pullback' && hasZone) {\n      W.up.push('אם המחיר יורד לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) + ' ונבלם — כניסה חלקית ב-' + n2(P.entry));\n      W.up.push('אישור חזק יותר מעל ' + n2(P.addAbove));\n    } else {\n      W.up.push('אם עולה מעל ' + n2(watch) + ' ונשאר שם — להתחיל לעקוב מקרוב');\n    }\n    if (!noEdge) { W.up.push('יעד ראשון ' + n2(t1) + ' — שם לממש חלק'); W.up.push('יעד שני ' + n2(t2)); }\n  } else {\n    W.up.push('הרמה החשובה למעלה הייתה ' + n2(watch));\n  }\n\n  // If it goes down\n  var below = T.below.filter(function (l) { return l.price < b.close - 0.05 * atr; });\n  var s1 = T.support ? T.support.price : (below[0] ? below[0].price : b.close - atr);\n  var s2 = below.find(function (l) { return l.price < s1 - 0.1 * atr; });\n  var s3 = P && P.invalidation != null ? P.invalidation : (s2 ? s2.price - 0.5 * atr : s1 - atr);\n  if (stale) {\n    // The DOWN block describes the downside, in the past tense. It must not\n    // repeat the UP block's lines — that duplication reached the screen.\n    W.down.push('התמיכה האחרונה שנרשמה: ' + n2(s1) + ' — לעיון בלבד');\n    W.down.push('אין רמת ביטול פעילה על נתונים ישנים');\n  } else if (noEdge && !ended) {\n    W.down.push('אם יורד ל-' + n2(s1) + ' — לבדוק אם קונים נכנסים שם');\n    W.down.push('מתחת ' + n2(s3) + ' — התרחיש החיובי יורד מהפרק');\n  } else if (!ended) {\n    W.down.push('אם יורד ל-' + n2(s1) + ' — לבדוק אם יש קונים שעוצרים את הירידה');\n    if (s2) W.down.push('אם שובר את ' + n2(s2.price) + ' — התרחיש החיובי נחלש');\n    W.down.push('אם יורד מתחת ' + n2(s3) + ' — לבטל, לא להיכנס');\n  } else {\n    W.down.push('התמיכה החשובה הייתה ' + n2(s1));\n  }\n\n  // Why, in plain words\n  var d = c.daily;\n  if (d) W.why.push('מגמה של כמה ימים: ' + (d.trend === 'UP' ? 'עולה' : d.trend === 'DOWN' ? 'יורדת' : 'ללא כיוון'));\n  W.why.push('היום: ' + (S.trend === 'UP' ? 'עולה' : S.trend === 'DOWN' ? 'יורד' : 'ללא כיוון'));\n  var mom = momentum(A);\n  W.why.push('בדקות האחרונות: ' + ({ PUSHING: 'לוחץ למעלה', PULLBACK: 'נסוג', RECOVERY: 'מתאושש', SELLING: 'נמכר', FLAT: 'שקט' }[mom.label]));\n  W.why.push(b.aboveVwap ? 'מעל ממוצע היום' : 'מתחת לממוצע היום');\n  var vx = c.baseline ? volxTod(b, c.baseline) : null;\n  if (vx != null) W.why.push('מחזור מול אותה דקה בימים קודמים: ' + vx.toFixed(1) + '×');\n  var pres = c.pressure || pressure(A, { tactical: T, plan: P });\n  if (pres) {\n    W.pressure = pres;\n    W.why.push('בדקות האחרונות ' + (pres.side === 'buyers' ? 'הקונים חזקים יותר' : pres.side === 'sellers' ? 'המוכרים חזקים יותר' : 'הכוחות שקולים')\n      + ' (' + pres.buyPct + '/' + pres.sellPct + ')');\n    if (pres.support && pres.support.state !== 'far')\n      W.why.push('התמיכה ב-' + n2(pres.support.price) + ' ' + pres.support.verdict);\n    if (pres.resistance && pres.resistance.state !== 'far')\n      W.why.push('ההתנגדות ב-' + n2(pres.resistance.price) + ' ' + pres.resistance.verdict);\n  }\n  // Identical lines are never informative twice, whichever branch produced them.\n  var uniq = function (list) { var seen = {}; return list.filter(function (x) {\n    if (seen[x]) return false; seen[x] = 1; return true; }); };\n  W.up = uniq(W.up); W.down = uniq(W.down); W.why = uniq(W.why);\n  return W;\n}\n\n\n// ---------------------------------------------------------------- ticker state\n//\n// ONE snapshot per symbol per refresh. Every UI surface — the radar row and the\n// detail sheet — renders from this object, so a score can never differ between\n// them. Nothing downstream recalculates.\n//\n// Level roles are explicit and each level has exactly one job:\n//   watch                 the price we are waiting for\n//   entry                 where a position would actually be opened\n//   target1 / target2     profit areas, always above entry for a long\n//   tacticalInvalidation  cancels the immediate entry idea\n//   hardStop              structure is broken; the whole idea is off\n//   probUpper / probLower the two boundaries the probability question uses\nvar STATE_VERSION = 4;\n\nfunction buildTickerState(symbol, A, ctx) {\n  var c = ctx || {};\n  var now = c.now || Date.now();\n  if (!A || !A.state) {\n    return { symbol: symbol, state_version: STATE_VERSION, calculated_at: now, data_through: null,\n      valid: false, violations: [{ code: 'NO_DATA', severity: 'block', text: 'אין נתונים' }],\n      status: 'NO DATA', action: 'DO_NOT_BUY', actionText: ACTIONS.DO_NOT_BUY };\n    }\n  var S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = tactical(A);\n  var plan = executionPlan(A, { label: c.market || 'Neutral' });\n  var pres = pressure(A, { tactical: T, plan: plan });\n\n  // --- levels, derived once\n  var lv = { watch: null, entry: null, target1: null, target2: null,\n    tacticalInvalidation: null, hardStop: null, probUpper: null, probLower: null };\n  if (plan && plan.kind) {\n    lv.watch = plan.kind === 'breakout' ? plan.zone[0] : plan.zone[1];\n    lv.entry = plan.entry;\n    lv.tacticalInvalidation = plan.kind === 'breakout' ? plan.zone[0] - 0.2 * atr : plan.zone[0];\n    lv.hardStop = plan.invalidation;\n  } else {\n    lv.watch = T.resistance ? T.resistance.price : b.dayHigh;\n    lv.tacticalInvalidation = T.support ? T.support.price : b.dayLow;\n    lv.hardStop = lv.tacticalInvalidation - 0.5 * atr;\n  }\n  // A long is cancelled on the way DOWN, so the level hit first must be the\n  // entry cancellation and the structural one must sit BELOW it. Reported the\n  // other way round — entry at 187.27, structure at 187.28 — the whole setup\n  // died before its own entry rule could ever fire.\n  if (lv.tacticalInvalidation != null && lv.hardStop != null && lv.hardStop >= lv.tacticalInvalidation) {\n    lv.hardStop = lv.tacticalInvalidation - Math.max(0.02, 0.3 * atr);\n    lv.hardStopAdjusted = true;\n  }\n  // Targets are always resolved ABOVE the entry. A level at or below the entry\n  // can be the watch level or resistance, but never target 1.\n  // With no entry planned, targets are measured from the WATCH level, not from\n  // the current price — otherwise the first level above price is both the thing\n  // we are waiting for and the first target, which is meaningless.\n  // The add trigger and target 1 cannot be the same price. One says \"commit\n  // more here\", the other says \"take some off here\", and printing both against\n  // 187.47 is an instruction to do two opposite things at once.\n  var addAt = plan && plan.addAbove != null ? plan.addAbove : null;\n  var anchor = lv.entry != null ? lv.entry : (lv.watch != null ? lv.watch : b.close);\n  var aboveEntry = T.above.filter(function (x) { return x.price >= anchor + 0.3 * atr; })\n    .sort(function (x, y) { return x.price - y.price; });\n  lv.target1 = aboveEntry[0] ? aboveEntry[0].price : anchor + 1.0 * atr;\n  // A target that lands on the add trigger is pushed to the next level above it,\n  // or one ATR beyond — the trigger keeps its role, the target moves.\n  if (addAt != null && Math.abs(lv.target1 - addAt) < 0.005) {\n    var beyond = aboveEntry.filter(function (x) { return x.price > addAt + 0.005; });\n    lv.target1 = beyond[0] ? beyond[0].price : addAt + Math.max(0.02, 0.5 * atr);\n    lv.target1MovedFromAdd = true;\n  }\n  lv.target2 = aboveEntry[1] && aboveEntry[1].price >= lv.target1 + 0.3 * atr ? aboveEntry[1].price : lv.target1 + 1.0 * atr;\n  lv.probUpper = lv.watch;\n  lv.probLower = lv.tacticalInvalidation;\n\n  var prob = pathProbability(A, { tactical: T, upper: lv.probUpper, lower: lv.probLower, horizonMin: 60,\n    daily: c.daily, baseline: c.baseline, calibration: c.calibration, market: c.market, pressure: pres });\n\n  // ---- edge check. The recommendation has to agree with the model's own\n  // numbers: a low score, a coin-flip probability, low confidence and balanced\n  // flow together mean there is nothing to act on, whatever the plan says.\n  var row0 = radarRow(symbol, A, c.marketCtx || { label: c.market || 'Neutral' }, c.freshness);\n  var score0 = row0.bl ? row0.bl.confidence : 0;\n  var weak = [];\n  if (score0 <= 2) weak.push('ציון setup נמוך');\n  if (prob && prob.meaningless) weak.push('הרמות קרובות מדי למדידה');\n  if (prob && prob.notDirectional) weak.push('הרמות אינן מקיפות את המחיר — אין הסתברות כיוון');\n  if (prob && prob.up != null && Math.abs(prob.up - 50) <= 8) weak.push('הסיכויים שקולים');\n  if (prob && prob.confidence < 50) weak.push('ביטחון נמוך');\n  if (pres && pres.side === 'balanced') weak.push('קונים ומוכרים מאוזנים');\n  var structureFlat = (row0.structure === 'RANGE') && (row0.momentum === 'FLAT');\n  if (structureFlat) weak.push('אין מבנה ואין מומנטום');\n  var edge = { noEdge: weak.length >= 4, reasons: weak, score: score0,\n    probUp: prob.up, confidence: prob.confidence, flow: pres ? pres.side : 'NA' };\n\n  // ---- staleness is a hard gate, not a label.\n  // Bars that stopped arriving hours ago describe a market that has moved on.\n  // Showing an entry, a probability or a buyer/seller split from them reads as\n  // live guidance about a price that no longer exists, so none of it survives.\n  // On a one-minute feed, four minutes of lag is long enough for price to have\n  // left the entry zone entirely. An entry offered on data that old is an\n  // instruction to trade a price that may no longer exist.\n  var ENTRY_MAX_AGE = 180;                       // three minutes\n  var STALE_SECONDS = 300;                       // five minutes of missing bars\n  var ageSec = (c.staleSeconds != null) ? c.staleSeconds\n    : (b.unix != null && c.nowUnix != null ? c.nowUnix - b.unix : null);\n  var isStale = c.sessionEnded ? false\n    : (c.freshness === 'STALE' || (ageSec != null && ageSec > STALE_SECONDS));\n  // Not stale enough to freeze the whole state, but too old to act on.\n  var tooOldToEnter = !c.sessionEnded && ageSec != null && ageSec > ENTRY_MAX_AGE;\n\n  // And the session itself must be present. A window that starts at 11:02 on a\n  // day that opened at 09:30 has no session open, no session VWAP and a\n  // fraction of the volume — every figure derived from it is wrong while\n  // looking entirely clean.\n  // Measure the rows that were LOADED, not the ones analysis kept. A no-trade\n  // minute is deliberately excluded from the analysis and is still a minute we\n  // hold — counting the filtered set turned a complete session into \"186 of 335\n  // with 57 holes\".\n  var cover = sessionCompleteness((A && (A.rawBars || A.bars)) || [], {});\n  var sessionIncomplete = !c.sessionEnded && !isStale && A && A.bars && A.bars.length > 0\n    && !cover.complete;\n\n  var W = whatNow(A, { tactical: T, plan: plan, market: c.market, daily: c.daily,\n    baseline: c.baseline, calibration: c.calibration, pressure: pres, probability: prob,\n    sessionEnded: c.sessionEnded, levels: lv, edge: edge, stale: isStale });\n\n  // Nothing actionable may remain on the state when there is no edge, so the\n  // technical block underneath cannot contradict the headline.\n  if (edge.noEdge && !c.sessionEnded) {\n    lv.entry = null; lv.target1 = null; lv.target2 = null;\n  }\n  // A closed session is a reference view, not an order. The levels stay visible\n  // as context, but nothing actionable survives — otherwise the headline says\n  // the market is shut while an entry price sits underneath it.\n  if (c.sessionEnded) { lv.entry = null; lv.target1 = null; lv.target2 = null; }\n  if (isStale) { lv.entry = null; lv.target1 = null; lv.target2 = null; }\n  // Too old to act on, or built from part of a session: the levels stay as\n  // context but no entry is offered, and the headline says which it is.\n  // Staleness is the stronger statement and must not be overwritten by the\n  // weaker one: data that is five minutes old is not merely \"delayed\".\n  if (!isStale && !c.sessionEnded && (tooOldToEnter || sessionIncomplete)) {\n    lv.entry = null; lv.target1 = null; lv.target2 = null;\n    W.action = 'WAIT';\n    W.actionText = sessionIncomplete\n      ? 'סשן חלקי — לא לסחור'\n      : 'נתונים באיחור — להמתין לנר טרי';\n    W.next = sessionIncomplete\n      ? ('נטענו ' + cover.receivedMinutes + ' דקות מתוך ' + cover.expectedMinutes\n         + ' — חסרות ' + cover.missingFromOpen + ' מהפתיחה ועוד ' + cover.missingInside\n         + ' בתוך החלון, אז הפתיחה, ה-VWAP, המבנה והמחזור אינם של הסשן')\n      : ('הנר האחרון בן ' + Math.round(ageSec) + ' שניות; בפיד של דקה זה מספיק כדי שהמחיר יעזוב את האזור');\n    // The WHAT-IF lines are built before this gate and still carried \"you can\n    // enter around 338.68\" with targets underneath a headline that says not to\n    // trade. Rewrite them as observations: the levels are worth naming, the\n    // instruction is not.\n    var obs = function (list) {\n      return (list || []).map(function (x) {\n        return String(x)\n          .replace(/אפשר להיכנס/g, 'רמה לצפייה')\n          .replace(/כניסה חלקית/g, 'אזור לצפייה')\n          .replace(/לממש/g, 'רמת יעד לשעבר')\n          .replace(/להוסיף/g, 'רמת אישור');\n      }).filter(function (x) { return !/להיכנס|לקנות|לממש חלק/.test(x); });\n    };\n    W.up = obs(W.up); W.down = obs(W.down);\n    W.up.unshift(sessionIncomplete\n      ? 'הרמות מוצגות לצפייה בלבד — הסדרה חסרה ' + cover.missingTotal + ' דקות'\n      : 'הרמות מוצגות לצפייה בלבד — הנתונים אינם טריים מספיק לפעולה');\n  }\n  var row = row0;\n\n  var st = {\n    symbol: symbol, state_version: STATE_VERSION, calculated_at: now,\n    data_through: { time: b.time, date: b.date || c.date || null, unix: b.unix != null ? b.unix : null },\n    price: b.close, atr: atr,\n    status: row.status, score: row.bl ? row.bl.confidence : 0,\n    action: W.action, actionText: W.actionText,\n    structure: row.structure, momentum: row.momentum,\n    coverage: cover, sessionIncomplete: sessionIncomplete, tooOldToEnter: tooOldToEnter,\n    levels: lv, plan: plan, tactical: T, pressure: pres, probability: W.probability,\n    probabilityRaw: W.probabilityRaw || null, whatNow: W,\n    scenario: W.scenario, edge: edge, noEdge: edge.noEdge,\n    levelStates: { support: T.support ? levelState(A, T.support.price, true) : null,\n                   resistance: T.resistance ? levelState(A, T.resistance.price, false) : null },\n    row: row, sessionEnded: !!c.sessionEnded, freshness: c.freshness || 'LIVE',\n    stale: isStale, data_age_seconds: ageSec\n  };\n  // Keep the row pointing at the same snapshot so nothing can drift.\n  row.score = st.score; row.state = st; row.pressure = pres;\n  if (edge.noEdge && !c.sessionEnded) {\n    row.status = row.status === 'AVOID' ? 'AVOID' : 'WATCH';\n    row.why = 'אין יתרון — רק מעקב';\n    st.status = row.status; st.action = 'WATCH_ONLY'; st.actionText = ACTIONS.WATCH_ONLY;\n  }\n  // The row is what the board shows, so it must carry the same verdict.\n  if (isStale) {\n    row.status = 'AVOID';\n    row.why = 'נתונים לא עדכניים — לא לסחור';\n    st.status = 'AVOID';\n    st.action = 'DATA_STALE'; st.actionText = ACTIONS.DATA_STALE;\n    st.probability = null;\n    st.pressure = pres ? Object.assign({}, pres, { current: false }) : pres;\n  }\n  var v = validateState(st);\n  st.violations = v.violations; st.valid = v.valid;\n  if (!v.valid) {\n    st.status = 'AVOID';\n    st.action = 'DO_NOT_BUY';\n    st.actionText = 'המתן — מצב המודל לא עקבי';\n    st.whatNow.actionText = st.actionText;\n    st.whatNow.next = 'הנתונים סותרים את עצמם, לכן לא מוצגת הוראת מסחר. יחושב מחדש בעדכון הבא.';\n    st.whatNow.up = []; st.whatNow.down = [];\n    // Nothing actionable may survive a blocked state.\n    st.levels = Object.assign({}, st.levels, { entry: null, target1: null, target2: null });\n  }\n  return st;\n}\n\n// ---------------------------------------------------------------- validation\n// Runs before anything is shown. A blocking violation replaces the trade\n// instruction with an explicit \"state inconsistent\" message rather than\n// letting a wrong instruction reach the screen.\nfunction validateState(st) {\n  var v = [], add = function (code, severity, text) { v.push({ code: code, severity: severity, text: text }); };\n  var lv = st.levels, p = st.price, plan = st.plan, atr = st.atr;\n  var actionable = ['ENTRY_AVAILABLE', 'HOLD', 'TAKE_PARTIAL'].indexOf(st.action) >= 0\n    || ['READY', 'ACTIVE'].indexOf(st.status) >= 0;\n\n  if (lv.entry != null) {\n    if (lv.target1 != null && lv.target1 <= lv.entry) add('TARGET_BELOW_ENTRY', 'block', 'יעד ראשון ' + n2(lv.target1) + ' אינו מעל הכניסה ' + n2(lv.entry));\n    if (lv.target2 != null && lv.target1 != null && lv.target2 <= lv.target1) add('TARGET2_BELOW_TARGET1', 'block', 'יעד שני אינו מעל יעד ראשון');\n    if (lv.hardStop != null && lv.hardStop >= lv.entry) add('STOP_ABOVE_ENTRY', 'block', 'הסטופ ' + n2(lv.hardStop) + ' אינו מתחת לכניסה ' + n2(lv.entry));\n    if (lv.tacticalInvalidation != null && lv.hardStop != null && lv.hardStop > lv.tacticalInvalidation)\n      add('STOP_ORDER', 'warn', 'הסטופ המבני אינו מתחת לביטול הטקטי');\n    // an entry the price has already left behind must not read as available\n    if (actionable && p > lv.entry + 0.6 * atr) add('ENTRY_BEHIND_PRICE', 'block', 'המחיר כבר רחוק מעל אזור הכניסה');\n  }\n  if (lv.probUpper != null && lv.probLower != null && lv.probUpper <= lv.probLower)\n    add('PROB_BOUNDS', 'block', 'גבולות ההסתברות אינם בסדר הנכון');\n  if (lv.watch != null && lv.target1 != null && Math.abs(lv.watch - lv.target1) < 1e-9)\n    add('LEVEL_DOUBLE_ROLE', 'block', 'אותה רמה משמשת גם כרמת מעקב וגם כיעד');\n\n  // descriptive text must agree with where price is\n  var pf = st.pressure;\n  if (pf && pf.support && pf.support.verdict === 'נשברת' && p > pf.support.price + 0.05 * st.atr)\n    add('TEXT_CONTRADICTS_PRICE', 'block', 'נטען שהתמיכה נשברת בזמן שהמחיר מעליה');\n  if (pf && pf.resistance && pf.resistance.verdict === 'נפרצת' && p < pf.resistance.price - 0.05 * st.atr)\n    add('TEXT_CONTRADICTS_PRICE', 'block', 'נטען שההתנגדות נפרצת בזמן שהמחיר מתחתיה');\n\n  // the headline and the technical block must describe the same situation\n  var passive = ['DO_NOT_BUY', 'WATCH_ONLY', 'SESSION_CLOSED', 'SETUP_CANCELLED'].indexOf(st.action) >= 0;\n  if (passive && lv.entry != null) add('ACTION_VS_ENTRY', 'block', 'ההוראה היא להמתין בזמן שמוצגת רמת כניסה');\n  if (passive && st.whatNow && st.whatNow.up.some(function (s) { return /אפשר להיכנס|כניסה חלקית ב-/.test(s); }))\n    add('ACTION_VS_INSTRUCTION', 'block', 'ההוראה היא להמתין בזמן שמוצגת הוראת כניסה');\n  if (st.noEdge && lv.target1 != null) add('NO_EDGE_TARGETS', 'block', 'אין יתרון אבל מוצגים יעדים');\n  // exactly one entry scenario may be narrated at a time\n  if (st.whatNow) {\n    var entryLines = st.whatNow.up.filter(function (s) { return /אפשר להיכנס|כניסה חלקית ב-/.test(s); });\n    var mentionsBoth = entryLines.some(function (s) { return /עובר את/.test(s); })\n      && entryLines.some(function (s) { return /יורד לאזור/.test(s); });\n    if (mentionsBoth) add('TWO_SCENARIOS', 'block', 'מוצגים שני תרחישי כניסה במקביל');\n  }\n  // a level description must match its current state\n  if (st.levelStates && st.levelStates.support && st.pressure && st.pressure.support) {\n    if (st.pressure.support.verdict === 'נשברה' && st.levelStates.support.state !== 'broken')\n      add('LEVEL_TEXT_STALE', 'block', 'תיאור התמיכה אינו תואם את מצבה הנוכחי');\n  }\n\n  // status vs plan\n  if (st.status === 'ACTIVE' && plan && plan.state === 'FAILED') add('STATUS_VS_PLAN', 'block', 'הסטטוס פעיל בזמן שה-setup בוטל');\n  if (st.status === 'READY' && st.action === 'DO_NOT_BUY') add('ACTION_VS_STATUS', 'block', 'סטטוס מוכן מול הוראה לא לקנות');\n\n  // pressure conclusion vs its own numbers\n  if (pf && pf.agreement === 'תומך' && pf.direction === 'deteriorating')\n    add('PRESSURE_CONCLUSION', 'block', 'נטען שהלחץ תומך בזמן שהוא נחלש');\n  if (pf && pf.agreement === 'תומך' && pf.buyPct < 50)\n    add('PRESSURE_CONCLUSION', 'block', 'נטען שהקונים תומכים בזמן שהם במיעוט');\n\n  // probability vs confidence\n  var pr = st.probability;\n  // `pr.up` is null when the probability was suppressed, and `null <= 20` is\n  // true in JavaScript — which made a *withheld* probability look like an\n  // extreme one and invalidated the whole state.\n  if (pr && pr.up != null && pr.confidence < 50 && (pr.up >= 80 || pr.up <= 20))\n    add('PROB_CONFIDENCE', 'block', 'הסתברות קיצונית עם ביטחון נמוך');\n  if (pr && pr.upper != null && lv.probUpper != null && Math.abs(pr.upper - lv.probUpper) > 1e-9)\n    add('PROB_STALE_LEVELS', 'block', 'ההסתברות חושבה מול רמות אחרות מאלה שמוצגות');\n\n  // ---- staleness. A state built from bars that stopped arriving is not a\n  // valid live state, whatever else it says.\n  if (st.stale && st.action !== 'DATA_STALE' && st.action !== 'SESSION_CLOSED')\n    add('STALE_BUT_ACTIONABLE', 'block', 'הנתונים ישנים אך המסך מציג הוראה חיה');\n  if (st.stale && lv && (lv.entry != null || lv.target1 != null))\n    add('STALE_WITH_LEVELS', 'block', 'הנתונים ישנים אך מוצגת רמת כניסה');\n  if (st.stale && pr && pr.up != null)\n    add('STALE_WITH_PROBABILITY', 'block', 'הנתונים ישנים אך מוצגת הסתברות כאילו היא נוכחית');\n\n  // ---- a setup whose cancellation level the price has already crossed\n  var cancelLv = (lv && lv.tacticalStop != null) ? lv.tacticalStop\n    : (st.plan && st.plan.invalidation != null ? st.plan.invalidation : null);\n  var LIVE_ACTIONS = ['START_WATCHING', 'WAIT_FOR_CONFIRMATION', 'ENTRY_AVAILABLE', 'HOLD', 'TAKE_PARTIAL'];\n  if (!st.sessionEnded && cancelLv != null && st.price != null && st.price < cancelLv\n      && LIVE_ACTIONS.indexOf(st.action) >= 0)\n    add('CANCELLED_BUT_ACTIVE', 'block', 'המחיר כבר מתחת לרמת הביטול אך התרחיש מוצג כפעיל');\n\n  // ---- the narrative targets must be the levels block's targets\n  if (st.whatNow && Array.isArray(st.whatNow.up) && lv && lv.target1 != null && lv.target2 != null) {\n    var txt = st.whatNow.up.join(' ');\n    var m1 = txt.match(/\\u05d9\\u05e2\\u05d3 \\u05e8\\u05d0\\u05e9\\u05d5\\u05df ([\\d.]+)/);\n    var m2 = txt.match(/\\u05d9\\u05e2\\u05d3 \\u05e9\\u05e0\\u05d9 ([\\d.]+)/);\n    var near = function (a, b2) { return Math.abs(Number(a) - Number(b2)) < 0.005; };\n    if (m1 && !near(m1[1], lv.target1)) add('TARGET1_TEXT_MISMATCH', 'block', 'יעד ראשון בטקסט שונה מהיעד ברמות');\n    if (m2 && !near(m2[1], lv.target2)) add('TARGET2_TEXT_MISMATCH', 'block', 'יעד שני בטקסט שונה מהיעד ברמות');\n  }\n\n  return { valid: !v.some(function (x) { return x.severity === 'block'; }), violations: v };\n}\n\n\n// ---------------------------------------------------------------- analysis pack\n//\n// Everything another trader (or another model) needs to re-do the analysis from\n// scratch: the raw data AND the decision state, from ONE snapshot. Any field the\n// system does not have says NOT AVAILABLE rather than being quietly dropped.\nvar NA = 'NOT AVAILABLE';\nfunction nz(v, d) { return v == null || v === '' || (typeof v === 'number' && !isFinite(v)) ? NA : (d != null && typeof v === 'number' ? v.toFixed(d) : String(v)); }\n\n// Buckets belong to the exchange clock: 09:30, 09:35, ... 11:00, 11:05. The\n// grouping was already correct, but the LABEL came from the first bar that\n// happened to fall inside — so a window starting at 11:02 produced a bar called\n// \"11:02\", which is not a five-minute timeframe at all. A bucket missing some\n// of its minutes is marked rather than silently presented as whole.\nfunction aggregate(rows, minutes) {\n  var out = [], bucket = null;\n  var label = function (m) {\n    var h = Math.floor(m / 60), mm = m % 60;\n    return String(h).padStart(2, '0') + ':' + String(mm).padStart(2, '0');\n  };\n  (rows || []).forEach(function (r) {\n    var m = parseInt(r.time.slice(0, 2), 10) * 60 + parseInt(r.time.slice(3), 10);\n    var key = Math.floor(m / minutes);\n    if (!bucket || bucket.key !== key) {\n      if (bucket) out.push(bucket);\n      bucket = { key: key, time: label(key * minutes), firstBar: r.time, minutes: 1,\n        open: r.open, high: r.high, low: r.low, close: r.close, volume: r.volume, date: r.date };\n    } else {\n      bucket.high = Math.max(bucket.high, r.high); bucket.low = Math.min(bucket.low, r.low);\n      bucket.close = r.close; bucket.volume += r.volume; bucket.minutes++;\n    }\n  });\n  if (bucket) out.push(bucket);\n  out.forEach(function (b) { b.partial = b.minutes < minutes; });\n  return out;\n}\nfunction candleLine(sym, r, avgVol) {\n  var range = r.high - r.low, pct = function (x) { return range > 0 ? Math.round(x / range * 1000) / 10 : 0; };\n  var dir = r.close > r.open ? 'BULL' : r.close < r.open ? 'BEAR' : 'FLAT';\n  return [sym, r.date || '', r.time, r.open, r.high, r.low, r.close, r.volume, dir,\n    pct(Math.abs(r.close - r.open)), pct(r.high - Math.max(r.open, r.close)), pct(Math.min(r.open, r.close) - r.low),\n    Math.round(range * 10000) / 10000, avgVol > 0 ? Math.round(r.volume / avgVol * 100) / 100 : 0].join(',');\n}\nvar CANDLE_HEADER = 'symbol,date,time,open,high,low,close,volume,dir,body_pct,upper_wick_pct,lower_wick_pct,range,vol_x';\n\nfunction analysisPack(ctx) {\n  var c = ctx || {}, st = c.snap;\n  var L = [], add = function (s) { L.push(s == null ? '' : s); };\n  var head = function (t) { add(''); add('===== ' + t + ' ====='); };\n  if (!st) return 'NO SNAPSHOT — ' + NA;\n  var A = c.analysis, b = st.price != null ? st : null;\n  var bar = A && A.state ? A.state.bar : null;\n  var lv = st.levels || {}, P = st.plan, W = st.whatNow, pr = st.probability, pf = st.pressure;\n  var rows = c.rows || [], atr = st.atr;\n\n  add('FULL ANALYSIS PACK · ' + st.symbol);\n  add('Generated from one atomic snapshot: state_version ' + st.state_version + ', calculated_at ' + new Date(st.calculated_at).toISOString());\n\n  head('1. TIME & DATA STATE');\n  add('Symbol: ' + st.symbol);\n  add('Date (ET): ' + nz(st.data_through && st.data_through.date || c.date));\n  add('Last closed candle (ET): ' + nz(st.data_through && st.data_through.time));\n  add('Current price (last close): ' + nz(st.price, 2));\n  add('Quote timestamp: ' + nz(c.quoteTime) + '   (the feed is 1-minute bars; there is no separate quote stream)');\n  add('Data age / stale seconds: ' + nz(c.staleSeconds));\n  add('Freshness: ' + nz(st.freshness));\n  add('Session: ' + nz(c.session) + '   (PRE / REGULAR / AFTER / CLOSED)');\n  add('Data source: Yahoo Finance 1-minute bars via Cloudflare Worker');\n\n  head('2. MODEL DECISION');\n  add('Status: ' + nz(st.status));\n  add('Primary action: ' + nz(st.actionText) + ' [' + nz(st.action) + ']');\n  add('Setup Score: ' + nz(st.score) + '/10');\n  add('Active setup type: ' + (st.stale ? 'none (data stale)'\n    : (st.scenario ? st.scenario.kind + ' — ' + st.scenario.label : NA)));\n  add('Setup state: ' + (st.stale ? 'FROZEN (last known: ' + (P ? P.state : NA) + ') — reference only'\n    : (P ? P.state : NA)));\n  add('Reason: ' + (W ? nz(W.next) : NA));\n  add('Model state valid: ' + (st.valid ? 'yes' : 'NO — ' + (st.violations || []).map(function (v) { return v.code; }).join(', ')));\n  if (st.noEdge) add('No-edge reasons: ' + ((st.edge && st.edge.reasons) || []).join(' · '));\n\n  head('3. PRICE LEVELS AND THEIR ROLES');\n  // While frozen, every level is a record of what WAS, not a price to act on.\n  // The labels say so, because a reader skimming the pack reads the label.\n  var FROZEN = !!st.stale;\n  var role = function (name, val, note) {\n    var label = FROZEN ? 'LAST KNOWN ' + name : name;\n    var n = FROZEN ? (note ? note + ' — REFERENCE ONLY' : 'REFERENCE ONLY') : note;\n    add(label + ': ' + (val == null ? NA : val.toFixed(2) + (atr ? '  (' + fmtR((val - st.price) / atr) + ')' : '')) + (n ? '  — ' + n : ''));\n  };\n  if (FROZEN) add('NOTE: data is stale. Everything in this section is the last recorded state, not a current instruction.');\n  role('Watch level', lv.watch, FROZEN ? 'the price that was being waited for' : 'the price being waited for');\n  role('Entry zone low', P && P.zone ? P.zone[0] : null);\n  role('Entry zone high', P && P.zone ? P.zone[1] : null);\n  role('Planned entry', lv.entry);\n  role('Breakout / reclaim trigger', P && P.kind === 'breakout' ? lv.watch : (st.plan && st.plan.addAbove != null ? st.plan.addAbove : null), FROZEN ? 'confirmed an add while the setup was live'\n      : (lv.entry == null) ? 'a level to watch — there is no position to add to'\n      : 'confirms an add');\n  role('Tactical support', st.tactical && st.tactical.support ? st.tactical.support.price : null, st.tactical && st.tactical.support ? st.tactical.support.why : '');\n  role('Tactical resistance', st.tactical && st.tactical.resistance ? st.tactical.resistance.price : null, st.tactical && st.tactical.resistance ? st.tactical.resistance.why : '');\n  role('Entry cancellation (tactical)', lv.tacticalInvalidation);\n  role('Setup invalidation / structural stop', lv.hardStop);\n  role('Stop', lv.hardStop, 'same as structural invalidation; no separate money stop is computed');\n  role('Target 1', lv.target1);\n  role('Target 2', lv.target2);\n  add('Target 3: ' + NA);\n  role('Reclaim level (broken and being retested)', st.plan && st.plan.state !== 'FAILED' && A && A.state ? A.state.reclaim : null);\n  add('');\n  add('Level states (approaching / testing / held / broken / reclaimed / rejected / lost / far):');\n  ['support', 'resistance'].forEach(function (k) {\n    var s = st.levelStates && st.levelStates[k];\n    add('  ' + k + ': ' + (s ? s.price.toFixed(2) + ' — ' + s.state + ' (' + LEVEL_TEXT[s.state] + '), ' + fmtR(s.distanceR) + ', touches ' + s.touches + ', recoveries ' + s.recoveries + ', rejections ' + s.rejections : NA));\n  });\n\n  head('4. WHAT-IF' + (FROZEN ? ' — FROZEN, REFERENCE ONLY' : ''));\n  add('NOW: ' + (W ? W.actionText + ' — ' + W.next : NA));\n  add('IF UP:');\n  ((W && W.up) || []).forEach(function (s) { add('  - ' + s); });\n  if (!W || !W.up.length) add('  ' + NA);\n  add('IF DOWN:');\n  ((W && W.down) || []).forEach(function (s) { add('  - ' + s); });\n  if (!W || !W.down.length) add('  ' + NA);\n  // The same rule as FROZEN, for every state where no entry is planned. I fixed\n  // this for stale data and left the identical contradiction standing when the\n  // session is incomplete: \"Planned entry: NOT AVAILABLE\" above \"Confirms\n  // entry: partial entry on the pullback\" tells you not to trade and how to\n  // trade in the same breath.\n  var NO_ENTRY = !FROZEN && (lv.entry == null);\n  var noEntryWhy = st.sessionIncomplete ? 'the session is incomplete'\n    : st.tooOldToEnter ? 'the data is too old to act on'\n    : st.sessionEnded ? 'the session is closed'\n    : 'no entry is planned';\n  if (NO_ENTRY) {\n    add('Confirms entry: ' + NA + ' — ' + noEntryWhy);\n    add('Cancels entry: ' + NA + ' — there is no entry to cancel');\n    add('Cancels the whole setup: ' + NA + ' — ' + noEntryWhy);\n    add('After entry: ' + NA + ' — ' + noEntryWhy);\n    add('At target 1: ' + NA + ' — ' + noEntryWhy);\n    add('Confirmation strengthens above: ' + NA + ' — ' + noEntryWhy);\n    add('Exit when: ' + NA + ' — ' + noEntryWhy);\n    add('The levels above are reference only: with no entry there is nothing to add to, take off, or exit.');\n  } else if (FROZEN) {\n    // None of these may be phrased as something to do. They are the last\n    // recorded rules of a setup that is no longer live.\n    add('Confirms entry: ' + NA + ' — setup frozen (data stale)');\n    add('After entry: ' + NA + ' — setup frozen (data stale)');\n    add('At target 1: ' + NA + ' — setup frozen (data stale)');\n    add('Confirmation strengthens above: ' + NA + ' — setup frozen (data stale)');\n    add('Exit when: ' + NA + ' — setup frozen (data stale)');\n    add('LAST KNOWN entry cancellation: ' + (lv.tacticalInvalidation != null ? lv.tacticalInvalidation.toFixed(2) + '  — REFERENCE ONLY' : NA));\n    add('LAST KNOWN setup invalidation: ' + (lv.hardStop != null ? lv.hardStop.toFixed(2) + '  — REFERENCE ONLY' : NA));\n    add('These are the rules the setup had when the data stopped. They are not active.');\n  } else {\n  add('Confirms entry: ' + (P ? nz(P.ifConfirmed) : NA));\n  add('Cancels entry: ' + (lv.tacticalInvalidation != null ? 'close below ' + lv.tacticalInvalidation.toFixed(2) : NA));\n  add('Cancels the whole setup: ' + (lv.hardStop != null ? 'close below ' + lv.hardStop.toFixed(2) : NA));\n  add('After entry: ' + (P ? nz(P.nextStep) : NA));\n  add('At target 1: ' + (lv.target1 != null ? 'take partial at ' + lv.target1.toFixed(2) : NA));\n  add('Confirmation strengthens above: ' + (P && P.addAbove != null ? P.addAbove.toFixed(2) + ' (two closes); position state unknown to the system' : NA));\n  add('Exit when: ' + (lv.hardStop != null ? 'close below ' + lv.hardStop.toFixed(2) + ', or at target' : NA));\n  }\n\n  head('4b. SESSION COVERAGE');\n  var cv = st.coverage || {};\n  add('First regular bar: ' + (cv.firstBar || NA));\n  add('Last regular bar: ' + (cv.lastBar || NA));\n  add('Expected minutes: ' + (cv.expectedMinutes != null ? cv.expectedMinutes : NA));\n  add('Received unique minutes: ' + (cv.receivedMinutes != null ? cv.receivedMinutes : NA));\n  add('Coverage: ' + (cv.coveragePct != null ? cv.coveragePct + '%' : NA));\n  add('Starts at the open: ' + (cv.fromOpen ? 'yes' : 'NO — missing ' + (cv.missingFromOpen || '?') + ' minutes from 09:30'));\n  add('Holes inside the loaded window: ' + (cv.missingInside\n    ? cv.missingInside + ' minutes missing between ' + cv.firstBar + ' and ' + cv.lastBar\n      + ' — the series is not continuous, so structure, momentum and the buyer/seller split describe a broken sequence'\n    : 'none'));\n  add('Total missing minutes: ' + (cv.missingTotal != null ? cv.missingTotal : NA));\n  add('Session values usable: ' + (cv.complete ? 'yes' : 'NO — open, gap, VWAP, EMA, relative volume and any entry are derived from a partial window and are not the session\\'s'));\n\n  head('5. PROBABILITY');\n  // A suppressed probability must still say WHAT was asked and WHY there is no\n  // number. \"NOT AVAILABLE\" on its own leaves the reader unable to tell a stale\n  // feed from a question that was never answerable.\n  if (!pr) {\n    var praw = (st && st.probabilityRaw) || null;\n    add('UP: ' + NA + '   DOWN: ' + NA);\n    add('Event measured: ' + (praw && praw.upper != null\n      ? 'price reaches ' + praw.upper.toFixed(2) + ' before ' + praw.lower.toFixed(2) : NA));\n    add('Horizon: ' + (praw ? praw.horizonMin + ' minutes' : NA));\n    add('Confidence: ' + NA + ' — no probability is being claimed');\n    add('Source: ' + NA);\n    // Only the reason for the SUPPRESSION, never the half-computed scoring\n    // inputs — those describe a number we have decided not to trust.\n    var suppressWhy = (praw && (praw.notDirectional || praw.meaningless))\n      ? praw.why.join(' | ')\n      : (st && st.stale) ? 'the data is stale'\n      : (st && st.coverage && !st.coverage.complete) ? 'the session is incomplete — the inputs are not the session\\'s'\n      : 'not answerable in this state';\n    add('Why: ' + suppressWhy);\n    add('Scoring inputs: ' + NA + ' — withheld with the probability they produced');\n  } else {\n    add('UP: ' + pr.up + '%   DOWN: ' + pr.down + '%');\n    add('Event measured: price reaches ' + pr.upper.toFixed(2) + ' before ' + pr.lower.toFixed(2));\n    add('Horizon: ' + pr.horizonMin + ' minutes');\n    add('Confidence: ' + pr.confidence + '/100' + (pr.lowConfidence ? '  (LOW — displayed % is pulled toward 50)' : ''));\n    add('Source: ' + (pr.source === 'empirical' ? 'empirical, ' + pr.n + ' comparable cases in this symbol\\'s own loaded history'\n      : 'MODEL ESTIMATE — not a measured frequency' + (pr.raw != null ? ' (raw ' + pr.raw + '%, shrunk for confidence)' : '')));\n    add('Why: ' + pr.why.join(' | '));\n    if (st.coverage && !st.coverage.complete)\n      add('NOTE: these inputs come from a partial window and the probability above should not be relied on.');\n  }\n\n  head('6. BUYERS / SELLERS (inferred from candles)');\n  if (!pf) add(NA); else {\n    add('Buyers: ' + pf.buyPct + '%   Sellers: ' + pf.sellPct + '%   (window ' + pf.window + ' bars)');\n    add('Buyers: ' + pf.buyersTrend + '   Sellers: ' + pf.sellersTrend + '   Direction: ' + pf.direction);\n    add('Conclusion: ' + pf.conclusion);\n    add('Agreement with the setup: ' + pf.agreement);\n    add('METHOD: close location inside each bar weighted by that bar\\'s volume. NOT order flow. hasOrderBook=' + pf.hasOrderBook + ' hasTape=' + pf.hasTape);\n  }\n\n  head('7. ORDER BOOK / LEVEL 2');\n  var bk = c.book;\n  if (!bk || bk.error || !bk.summary || bk.summary.bid_pct == null) {\n    add(NA + ' — the Cboe book did not return data for this symbol at this time');\n  } else {\n    var s = bk.summary;\n    add('PARTIAL BOOK — CBOE ONLY');\n    add('Venues returning data: ' + (s.venues_ok.join(', ') || NA) + '   Not answering: ' + (s.venues_failed.join(', ') || 'none'));\n    add('Missing from this book: ARCA, NYSE, Nasdaq and all other venues. This is NOT the NBBO and NOT consolidated depth.');\n    add('Book timestamp: ' + nz(bk.fetched_at));\n    add('Best bid: ' + nz(s.best_bid, 2) + '   Best ask: ' + nz(s.best_ask, 2) + '   Spread: ' + nz(s.spread, 4));\n    add('Total bid depth: ' + s.bid_shares + ' shares   Total ask depth: ' + s.ask_shares + ' shares');\n    add('Imbalance: ' + s.bid_pct + '% bid / ' + s.ask_pct + '% ask  (net ' + s.imbalance + ')');\n    add('Levels available: ' + s.depth_levels + ' per side per venue (' + NA + ' beyond 5 — the public viewer publishes 5)');\n    add('');\n    add('venue,side,level,price,shares');\n    (bk.venues || []).forEach(function (v) {\n      if (v.error) { add(v.venue + ',ERROR,,,' + v.error); return; }\n      (v.asks || []).forEach(function (l, i) { add(v.venue + ',ask,' + (i + 1) + ',' + l.price + ',' + l.shares); });\n      (v.bids || []).forEach(function (l, i) { add(v.venue + ',bid,' + (i + 1) + ',' + l.price + ',' + l.shares); });\n    });\n    add('');\n    add('Depth change over the last 30-60s: ' + (c.bookPrev ? 'bid ' + (s.bid_shares - c.bookPrev.bid_shares) + ', ask ' + (s.ask_shares - c.bookPrev.ask_shares) : NA + ' (no earlier snapshot held)'));\n  }\n\n  head('8. TAPE / TIME & SALES');\n  add('TAPE NOT AVAILABLE — this feed carries 1-minute bars only. No per-trade prints, no at-bid/at-ask flags, no aggressive volume split.');\n\n  head('9. STRUCTURE & MOMENTUM');\n  // Structure is a claim about a SEQUENCE. With minutes missing in the middle\n  // the sequence is not the market's, so the reading is qualified rather than\n  // stated — a swing \"confirmed\" across a hole may never have happened.\n  var BROKEN = (st.coverage && st.coverage.contiguous === false)\n    ? '  [SERIES NOT CONTINUOUS — ' + st.coverage.missingInside + ' minutes missing inside the window; this reading is not reliable]'\n    : '';\n  add('Main structure: ' + nz(st.structure) + (A && A.state ? '  (' + A.state.reason + ')' : '') + BROKEN);\n  add('Announced trend: ' + (A && A.state ? nz(A.state.announced) : NA) + BROKEN);\n  add('Short momentum: ' + nz(st.momentum) + (st.row && st.row.mom ? '   move over last 5 bars: ' + st.row.mom.net.toFixed(2) + 'R' : ''));\n  add('Recent swings (K=3 confirmed):');\n  var sw = A ? A.swings.filter(function (w) { return w.confirmedAt <= A.bars.length - 1; }).slice(-10) : [];\n  if (!sw.length) add('  ' + NA);\n  sw.forEach(function (w) { add('  ' + w.label + '  ' + w.price.toFixed(2) + '  @' + w.time + (w.outside ? '  (outside bar — not counted as structure)' : '')); });\n\n  head('10. INDICATORS');\n  // These are all session-scoped. Computed over a partial window they are not\n  // what their names say, so every line carries the warning rather than the\n  // reader being expected to remember a note from six sections earlier.\n  var PART = (st.coverage && !st.coverage.complete) ? '  [PARTIAL WINDOW — NOT A SESSION INDICATOR]' : '';\n  if (PART) add('NOTE: the session is incomplete — every figure below describes the loaded window only.');\n  if (!bar) add(NA); else {\n    add('VWAP: ' + bar.vwap.toFixed(2) + PART + '   price is ' + (bar.aboveVwap ? 'ABOVE' : 'BELOW') + '   distance: ' + (st.price - bar.vwap).toFixed(2) + ' (' + fmtR((st.price - bar.vwap) / atr) + ')');\n    add('EMA9: ' + bar.ema9.toFixed(2) + PART + '   EMA20: ' + bar.ema20.toFixed(2) + '   state: ' + bar.align +\n      '   separation: ' + Math.abs(bar.ema9 - bar.ema20).toFixed(3) + ' (minimum for a call: ' + (0.15 * atr).toFixed(3) + ')');\n    add('Average candle range (20 bars): ' + atr.toFixed(4) + '   = 1R throughout this pack');\n    add('Relative volume (vs session average): ' + bar.volx.toFixed(2) + 'x' + PART + (bar.auction ? '  [CLOSING AUCTION BAR — excluded from scoring]' : ''));\n    add('Time-of-day normalised volume: ' + (c.baseline ? (volxTod(bar, c.baseline) != null ? volxTod(bar, c.baseline).toFixed(2) + 'x vs the same minute on ' + c.baseline.days + ' prior days' : NA) : NA + ' (no prior days loaded)'));\n    var dl = function (name, v) { return v == null ? name + ': ' + NA : name + ': ' + (st.price - v).toFixed(2) + ' (' + fmtR((st.price - v) / atr) + ')'; };\n    add('Distance from levels — ' + dl('support', st.tactical && st.tactical.support ? st.tactical.support.price : null) +\n      '   ' + dl('resistance', st.tactical && st.tactical.resistance ? st.tactical.resistance.price : null));\n  }\n\n  head('11. MARKET CONTEXT');\n  var mk = c.marketCtx;\n  if (!mk || !mk.parts || !mk.parts.length) add(NA); else {\n    add('Regime: ' + mk.label);\n    mk.parts.forEach(function (p) {\n      add('  ' + p.symbol + ': ' + p.close.toFixed(2) + '   VWAP ' + p.vwap.toFixed(2) + ' (' + (p.aboveVwap ? 'above' : 'below') + ')   EMA ' + p.align + '   trend ' + p.trend);\n    });\n    add('Sector ETF: ' + (c.sectorEtf || NA));\n    add('Does the market support this symbol: ' + (mk.label === 'Bullish' ? 'supports a long' : mk.label === 'Bearish' ? 'contradicts a long' : 'neutral'));\n  }\n\n  head('12. MULTI-DAY CONTEXT');\n  if (st.coverage && !st.coverage.complete)\n    add(\"NOTE: today's row is built from a PARTIAL session (\" + (st.coverage.coveragePct || 0)\n      + '% of the minutes since the open) — it is not a daily candle.');\n  var d = c.daily;\n  if (!d) add(NA + ' — no prior sessions loaded'); else {\n    add('Sessions loaded: ' + d.days + ' (prior: ' + d.priorDays + ')');\n    // The daily trend is computed over COMPLETED sessions only — today's row is\n    // excluded by dailyContext. Say so, because a reader looking at a partial\n    // row whose low is already below yesterday's will otherwise read the trend\n    // as a claim about it.\n    add('Daily trend: ' + d.trend + ' (' + d.trendWhy + ')'\n      + '  [computed over the ' + (d.priorDays || 0) + ' COMPLETED sessions before today'\n      + ((st.coverage && !st.coverage.complete) ? \"; today's partial row is excluded\" : '') + ']');\n    add('Previous day: high ' + nz(d.prevHigh, 2) + '  low ' + nz(d.prevLow, 2) + '  close ' + nz(d.prevClose, 2));\n    add('Gap today vs previous close: ' + ((st.coverage && !st.coverage.complete)\n      ? NA + ' — the session open is missing, so there is nothing to measure the gap from'\n      : nz(d.gap, 2)));\n    add('Average daily range: ' + nz(d.atrDaily, 2));\n    add('');\n    add('date,open,high,low,close,volume');\n    (c.dailyBars || []).forEach(function (x) { add([x.date, x.open, x.high, x.low, x.close, x.volume].join(',')); });\n    if (!(c.dailyBars || []).length) add(NA);\n    var hi = (c.dailyBars || []).reduce(function (m, x) { return Math.max(m, x.high); }, -Infinity);\n    var lo = (c.dailyBars || []).reduce(function (m, x) { return Math.min(m, x.low); }, Infinity);\n    add('');\n    add('Multi-day high/low: ' + (isFinite(hi) ? hi.toFixed(2) + ' / ' + lo.toFixed(2) : NA));\n  }\n\n  head('13. EVENTS');\n  var evs = A ? A.events.slice(-40).reverse() : [];\n  if (!evs.length) add(NA);\n  evs.forEach(function (s2) {\n    var parts = s2.events.map(function (e) {\n      return e.type + (e.level != null ? ' @' + e.level.toFixed(2) : '') + (e.confirmed === true ? ' [confirmed]' : e.confirmed === false ? ' [unconfirmed]' : '') + (e.side ? ' ' + e.side : '');\n    });\n    if (parts.length) add(s2.time + '  ' + s2.close.toFixed(2) + '  ' + parts.join(' · ') + '  (score ' + s2.score + ')');\n  });\n\n  head('14. POSITION');\n  var pos = c.position;\n  if (!pos) {\n    add('Position: NOT AVAILABLE — this application does not track positions, orders or account state.');\n    add('Quantity / average entry / P&L / current stop / invested / remaining / partials taken: ' + NA);\n  } else {\n    add('Position: ' + nz(pos.state));\n    add('Quantity: ' + nz(pos.qty) + '   Average entry: ' + nz(pos.avg, 2));\n    add('P&L: ' + nz(pos.pnl, 2) + ' (' + nz(pos.pnlPct, 2) + '%)   Current stop: ' + nz(pos.stop, 2));\n    add('Invested: ' + nz(pos.invested) + '   Remaining planned: ' + nz(pos.remaining) + '   Partials taken: ' + nz(pos.partials));\n  }\n\n  head('15. CANDLES — LAST 50 ONE-MINUTE (closed only)');\n  var avgVol = rows.length ? rows.reduce(function (a2, x) { return a2 + x.volume; }, 0) / rows.length : 0;\n  add(CANDLE_HEADER);\n  rows.slice(-50).forEach(function (r) { add(candleLine(st.symbol, r, avgVol)); });\n  if (!rows.length) add(NA);\n\n  head('16. CANDLES — LAST 20 FIVE-MINUTE (aggregated from 1-minute)');\n  var f5 = aggregate(rows, 5).slice(-20);\n  add(CANDLE_HEADER);\n  var avg5 = f5.length ? f5.reduce(function (a2, x) { return a2 + x.volume; }, 0) / f5.length : 0;\n  f5.forEach(function (r) { add(candleLine(st.symbol, r, avg5)); });\n  if (!f5.length) add(NA);\n\n  head('17. CANDLES — LAST 10 DAILY');\n  add('symbol,date,open,high,low,close,volume');\n  (c.dailyBars || []).slice(-10).forEach(function (x) { add([st.symbol, x.date, x.open, x.high, x.low, x.close, x.volume].join(',')); });\n  if (!(c.dailyBars || []).length) add(NA + ' — no prior sessions loaded');\n\n  head('END OF PACK');\n  add('Fields marked ' + NA + ' are genuinely absent from this data feed, not omitted.');\n  return L.join('\\n');\n}\n\n\n// Candidate scoring — \"is this stock worth my attention today?\"\n//\n// THREE SEPARATE QUESTIONS, and this file must never blur them:\n//\n//   Candidate Score  is this worth WATCHING today?      0-100, from history\n//   Live Interest    is it still worth live attention?  0-100, from the session\n//   Setup Score      is a trade developing RIGHT NOW?   0-10, engine.cjs\n//\n// A stock can be a strong candidate with no setup at all: a quiet coil under a\n// multi-day high is exactly that. Equally, a live symbol can hold a setup and\n// have stopped being interesting, because the range died.\n//\n// Everything here is computed from CLOSED sessions. Nothing in this file may\n// produce a live instruction, and yesterday's execution state (ACTIVE, FAILED,\n// WAITING, DO_NOT_CHASE) is deliberately not carried in: structure and levels\n// survive a session boundary, decisions do not.\n\n\n// ---------------------------------------------------------------- daily bars\n// One bar per session out of per-minute rows.\n// A US regular session is 390 one-minute bars. A set materially shorter than\n// that is a session still being collected, or one fetched mid-day — and\n// aggregating it yields a daily candle that is wrong with no outward sign.\nvar SESSION_BARS = 390, MIN_COVERAGE = 0.85;\n// Counting bars alone is the wrong test. A thinly traded name has no print in\n// some minutes and the feed omits them, so a COMPLETE session can arrive with\n// 343 bars — PGR, BLK and HON were all refused that way. What actually\n// distinguishes a complete session is that it SPANS the session: it starts at\n// the open and runs to the close. A day truncated at 13:20 fails that however\n// many bars it has.\nfunction sessionSpans(rows) {\n  if (!rows || !rows.length) return false;\n  var times = rows.map(function (r) { return r.time; }).sort();\n  return times[0] <= '09:35' && times[times.length - 1] >= '15:55';\n}\nfunction toDaily(daysRows, opts) {\n  var o2 = opts || {};\n  var auth = {};\n  (o2.authoritative || []).forEach(function (b) { auth[b.date] = b; });\n  var out = (daysRows || []).filter(function (r) { return r && r.length; }).map(function (rows) {\n    var h = -Infinity, l = Infinity, v = 0;\n    rows.forEach(function (r) { h = Math.max(h, r.high); l = Math.min(l, r.low); v += r.volume; });\n    var o = rows[0].open, c = rows[rows.length - 1].close;\n    var regular = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n    var cov = regular.length / SESSION_BARS;\n    var spans = sessionSpans(rows);\n    var a = auth[rows[0].date];\n    if (a) return { date: a.date, open: a.open, high: a.high, low: a.low, close: a.close,\n      volume: a.volume, range: a.high - a.low,\n      closePos: a.high > a.low ? (a.close - a.low) / (a.high - a.low) : 0.5,\n      bars: rows.length, coverage: Math.round(cov * 1000) / 1000, complete: true, source: 'authoritative' };\n    return { date: rows[0].date, open: o, high: h, low: l, close: c, volume: v,\n      range: h - l, closePos: h > l ? (c - l) / (h - l) : 0.5, bars: rows.length,\n      coverage: Math.round(cov * 1000) / 1000, spans: spans,\n      complete: spans && cov >= MIN_COVERAGE, source: 'aggregated' };\n  });\n  // An incomplete session is dropped rather than scored: every component here\n  // reads high, low, close or volume, and all four are wrong on a partial day.\n  return out.filter(function (b) { return b.complete; });\n}\n\n// ---------------------------------------------------------------- components\n// Each returns { points, max, why } so the score can always be explained. A\n// number with no reasons attached is not usable for a decision.\n\n// Multi-day direction: higher closes AND higher lows, or the mirror.\nfunction trendComponent(daily) {\n  var out = { points: 0, max: 25, why: null, label: 'RANGE' };\n  if (daily.length < 4) { out.why = null; return out; }\n  var w = daily.slice(-4);\n  var upC = 0, upL = 0, dnC = 0, dnL = 0;\n  for (var i = 1; i < w.length; i++) {\n    if (w[i].close > w[i - 1].close) upC++; else if (w[i].close < w[i - 1].close) dnC++;\n    if (w[i].low > w[i - 1].low) upL++; else if (w[i].low < w[i - 1].low) dnL++;\n  }\n  if (upC >= 2 && upL >= 2) { out.points = 12 + Math.min(13, (upC + upL) * 3); out.label = 'UP';\n    out.why = 'מגמה רב-יומית עולה — ' + upC + ' סגירות ו-' + upL + ' שפלים עולים'; }\n  else if (dnC >= 2 && dnL >= 2) { out.points = 10 + Math.min(10, (dnC + dnL) * 2); out.label = 'DOWN';\n    out.why = 'מגמה רב-יומית יורדת — ' + dnC + ' סגירות ו-' + dnL + ' שיאים יורדים'; }\n  else { out.points = 4; out.label = 'RANGE'; out.why = 'ללא כיוון רב-יומי ברור'; }\n  return out;\n}\n\n// Enough daily movement to be worth watching at all. A name whose whole day is\n// 0.4% cannot pay for the attention, however clean its structure.\nfunction rangeComponent(daily) {\n  var out = { points: 0, max: 15, why: null, atrPct: null };\n  if (!daily.length) return out;\n  var w = daily.slice(-5);\n  var avg = w.reduce(function (s, b) { return s + b.range; }, 0) / w.length;\n  var px = w[w.length - 1].close || 1;\n  var pct = avg / px * 100;\n  out.atrPct = Math.round(pct * 100) / 100;\n  // This is the average over five sessions, not today's range. Calling it\n  // \"daily range\" made it look wrong against a single day's high-low.\n  var last = w[w.length - 1];\n  out.lastRangePct = px > 0 ? Math.round(last.range / px * 10000) / 100 : null;\n  out.window = w.length;\n  if (pct >= 3) { out.points = 15; out.why = 'טווח ממוצע רחב (' + out.window + ' ימים) — ' + out.atrPct + '% מהמחיר'; }\n  else if (pct >= 1.8) { out.points = 12; out.why = 'טווח ממוצע טוב (' + out.window + ' ימים) — ' + out.atrPct + '%'; }\n  else if (pct >= 1.0) { out.points = 7; out.why = 'טווח ממוצע בינוני (' + out.window + ' ימים) — ' + out.atrPct + '%'; }\n  else { out.points = 1; out.why = 'טווח ממוצע צר (' + out.window + ' ימים) — ' + out.atrPct + '%, קשה להוציא ממנו מהלך'; }\n  return out;\n}\n\n// A contraction into a decision point is the classic pre-move condition: the\n// last two sessions narrower than the ones before them.\nfunction coilComponent(daily, atrPct, relVol) {\n  var out = { points: 0, max: 12, why: null, coiled: false };\n  if (daily.length < 5) return out;\n  // A dead stock is narrow and quiet too. Contraction only means something when\n  // the name has enough daily range to matter and the volume has not collapsed\n  // with it — otherwise this rewards exactly the stocks worth ignoring.\n  if (atrPct != null && atrPct < 1.0) { out.why = null; return out; }\n  if (relVol != null && relVol < 0.6) { out.why = null; return out; }\n  var recent = daily.slice(-2), prior = daily.slice(-5, -2);\n  var r = recent.reduce(function (s, b) { return s + b.range; }, 0) / recent.length;\n  var p = prior.reduce(function (s, b) { return s + b.range; }, 0) / prior.length;\n  if (!(p > 0)) return out;\n  var ratio = r / p;\n  if (ratio <= 0.6) { out.points = 12; out.coiled = true; out.why = 'התכווצות טווח חדה לפני הפתיחה — ' + Math.round(ratio * 100) + '% מהממוצע'; }\n  else if (ratio <= 0.8) { out.points = 8; out.coiled = true; out.why = 'התכווצות טווח — ' + Math.round(ratio * 100) + '% מהממוצע'; }\n  return out;\n}\n\n// Where the last session closed inside its own range. A close on the high into\n// a multi-day high is the setup that most often continues.\nfunction closeComponent(daily) {\n  var out = { points: 0, max: 10, why: null };\n  if (!daily.length) return out;\n  var last = daily[daily.length - 1];\n  if (last.closePos >= 0.85) { out.points = 10; out.why = 'סגר בשיא היום — ' + Math.round(last.closePos * 100) + '% מהטווח'; }\n  else if (last.closePos >= 0.7) { out.points = 7; out.why = 'סגר בחלק העליון של היום'; }\n  else if (last.closePos <= 0.15) { out.points = 6; out.why = 'סגר בשפל היום — חולשה שמעניינת לשורט או לריבאונד'; }\n  else if (last.closePos <= 0.3) { out.points = 4; out.why = 'סגר בחלק התחתון של היום'; }\n  return out;\n}\n\n// Volume behind the last session, against its own recent average.\nfunction volumeComponent(daily) {\n  var out = { points: 0, max: 13, why: null, relVol: null };\n  if (daily.length < 3) return out;\n  var last = daily[daily.length - 1], prior = daily.slice(-6, -1);\n  if (!prior.length) return out;\n  var avg = prior.reduce(function (s, b) { return s + b.volume; }, 0) / prior.length;\n  if (!(avg > 0)) return out;\n  var rel = last.volume / avg;\n  out.relVol = Math.round(rel * 100) / 100;\n  if (rel >= 1.8) { out.points = 13; out.why = 'מחזור חריג בסשן האחרון — ×' + out.relVol; }\n  else if (rel >= 1.3) { out.points = 10; out.why = 'מחזור מוגבר — ×' + out.relVol; }\n  else if (rel >= 0.9) { out.points = 5; out.why = 'מחזור רגיל — ×' + out.relVol; }\n  else { out.points = 2; out.why = 'מחזור דל — ×' + out.relVol + ', עניין נמוך'; }\n  return out;\n}\n\n// Sitting just under a multi-day high (or over a low) is a decision point: the\n// session opens with something to resolve.\nfunction levelComponent(daily) {\n  var out = { points: 0, max: 25, why: null, levels: [] };\n  if (daily.length < 2) return out;\n  var last = daily[daily.length - 1], prior = daily.slice(0, -1);\n  var px = last.close, atr = daily.slice(-5).reduce(function (s, b) { return s + b.range; }, 0) / Math.min(5, daily.length);\n  if (!(atr > 0)) return out;\n\n  var hi = Math.max.apply(null, prior.map(function (b) { return b.high; }));\n  var lo = Math.min.apply(null, prior.map(function (b) { return b.low; }));\n  var pd = prior[prior.length - 1];\n\n  // Each level carries the session it came from. \"Yesterday\" meant 09-02 here\n  // and 09-03 in the range figure on the same card — the reader had no way to\n  // know which.\n  var pdDate = pd.date ? ' ' + pd.date.slice(5).replace('-', '/') : '';\n  out.levels = [\n    { name: 'גבוה רב-יומי', price: hi, date: null },\n    { name: 'גבוה סשן קודם' + pdDate, price: pd.high, date: pd.date },\n    { name: 'סגירת סשן קודם' + pdDate, price: pd.close, date: pd.date },\n    { name: 'נמוך סשן קודם' + pdDate, price: pd.low, date: pd.date },\n    { name: 'נמוך רב-יומי', price: lo, date: null }\n  ].filter(function (x) { return isFinite(x.price); });\n  out.priorDate = pd.date || null;\n\n  // The same guard: \"right under the multi-day high\" is meaningless when the\n  // multi-day range is a fraction of a percent.\n  var spanPct = (hi - lo) / (px || 1) * 100;\n  if (spanPct < 1.0) { out.points = 2; out.why = 'הטווח הרב-יומי צר מדי מכדי שרמה תהיה נקודת הכרעה'; return out; }\n  var dHi = (hi - px) / atr, dLo = (px - lo) / atr;\n  // WHAT THE LAST SESSION DID AT THE LEVEL, not just where it closed relative\n  // to it. A day that traded THROUGH the multi-day high and closed back under\n  // it is a rejected breakout — the opposite of a coil waiting to resolve — and\n  // scoring both as \"right under the level, decision point\" gave a failed\n  // breakout the maximum available points. Measured: AAPL reached 330.81 above\n  // a 328.40 level and closed 328.21; NVDA reached 230.40 above 229.26 and\n  // closed 228.45. Both were being ranked as pre-breakout.\n  var touchedHi = last.high >= hi - 0.05 * atr;\n  var brokeHi = last.high > hi;\n  var heldHi = px > hi;\n  var brokeLo = last.low < lo, heldLo = px < lo;\n  out.event = heldHi ? 'breakout_held'\n    : brokeHi ? 'breakout_failed'\n    : touchedHi ? 'testing_high'\n    : heldLo ? 'breakdown_held'\n    : brokeLo ? 'breakdown_recovered'\n    : dHi <= 0.35 ? 'approaching_high'\n    : dLo <= 0.35 ? 'approaching_low' : 'mid_range';\n\n  if (out.event === 'breakout_held') { out.points = 22;\n    out.why = 'פרץ את הגבוה הרב-יומי וסגר מעליו — פריצה שצריכה אישור'; }\n  else if (out.event === 'breakout_failed') { out.points = 6;\n    // Name the LEVEL and the price reached separately. Putting last.high in the\n    // parentheses read as though 83.60 were the multi-day high, while the level\n    // printed two lines below said 83.12 — the same card contradicting itself.\n    out.why = 'ניסה לפרוץ את הגבוה הרב-יומי ' + hi.toFixed(2)\n      + ', הגיע ל-' + last.high.toFixed(2) + ' ונסגר ב-' + px.toFixed(2) + ' מתחתיו — פריצה שנדחתה'; }\n  else if (out.event === 'testing_high') { out.points = 20;\n    out.why = 'נגע בגבוה הרב-יומי בלי לפרוץ — נבחן'; }\n  else if (out.event === 'approaching_high') { out.points = 25;\n    out.why = 'ממש מתחת לגבוה הרב-יומי, בלי ניסיון פריצה — נקודת הכרעה בפתיחה'; }\n  else if (dHi > 0.35 && dHi <= 1) { out.points = 18; out.why = 'קרוב לגבוה הרב-יומי'; }\n  else if (out.event === 'breakdown_held') { out.points = 16;\n    out.why = 'סגר מתחת לנמוך הרב-יומי — שבירה'; }\n  else if (out.event === 'breakdown_recovered') { out.points = 14;\n    out.why = 'שבר את הנמוך הרב-יומי ' + lo.toFixed(2)\n      + ', ירד עד ' + last.low.toFixed(2) + ' וחזר מעליו — שבירה שנדחתה'; }\n  else if (out.event === 'approaching_low') { out.points = 17;\n    out.why = 'ממש מעל הנמוך הרב-יומי — נקודת הכרעה'; }\n  else { out.points = 5; out.why = 'באמצע הטווח הרב-יומי'; }\n  return out;\n}\n\n// ---------------------------------------------------------------- the score\n//\n// candidateScore(daysRows) -> { score, reasons, structure, levels, ... }\n//\n// daysRows: an array of sessions, oldest first, each an array of 1-minute rows.\n// Every session here is CLOSED. Nothing about a live setup enters this.\nfunction candidateScore(daysRows, opts) {\n  var o = opts || {};\n  var all = (daysRows || []).filter(function (r) { return r && r.length; });\n  var daily = toDaily(all, o);\n  var dropped = all.length - daily.length;\n  if (!daily.length) {\n    return { score: null, reasons: [], structure: null, levels: [], sessions: 0,\n      incomplete_sessions: dropped,\n      note: dropped ? 'הסשנים הקיימים אינם שלמים — לא ניתן לחשב' : 'אין נתונים היסטוריים' };\n  }\n  var range = rangeComponent(daily), volume = volumeComponent(daily);\n  var parts = {\n    trend: trendComponent(daily),\n    range: range,\n    coil: coilComponent(daily, range.atrPct, volume.relVol),\n    close: closeComponent(daily),\n    volume: volume,\n    level: levelComponent(daily)\n  };\n  var got = 0, max = 0;\n  Object.keys(parts).forEach(function (k) { got += parts[k].points; max += parts[k].max; });\n  // Normalised so a symbol with only three sessions is not punished for the\n  // components that need five — its max is smaller, not its score.\n  var score = Math.round(got / max * 100);\n  // A stock whose daily range cannot pay for a trade is not a candidate, no\n  // matter how tidy its structure looks. This is a ceiling, not a deduction:\n  // the components stay honest and the verdict is capped.\n  var capped = null;\n  if (range.atrPct != null && range.atrPct < 0.8 && score > 30) { capped = score; score = 30; }\n  else if (range.atrPct != null && range.atrPct < 1.2 && score > 55) { capped = score; score = 55; }\n  var capWindow = range.window || 5;\n\n  // Reasons, strongest first, so the screen can show the top two.\n  var reasons = Object.keys(parts)\n    .map(function (k) { return { key: k, points: parts[k].points, text: parts[k].why }; })\n    .filter(function (x) { return x.text; })\n    .sort(function (a, b) { return b.points - a.points; });\n\n  var last = daily[daily.length - 1];\n  return {\n    score: score, reasons: reasons, parts: parts,\n    cappedFrom: capped,\n    capReason: capped != null ? 'הציון הוגבל: טווח ממוצע ' + capWindow + ' ימים של '\n      + range.atrPct + '% קטן מכדי להצדיק מעקב' : null,\n    structure: parts.trend.label,\n    structureWhy: parts.trend.why,\n    levels: parts.level.levels,\n    atrPct: parts.range.atrPct, avgRangeWindow: parts.range.window,\n    lastRangePct: parts.range.lastRangePct,\n    levelEvent: parts.level.event, priorDate: parts.level.priorDate,\n    relVol: parts.volume.relVol, coiled: parts.coil.coiled,\n    sessions: daily.length, incomplete_sessions: dropped,\n    incompleteNote: dropped ? dropped + ' סשנים לא שלמים לא נכללו בחישוב' : null,\n    dataSource: daily.every(function (b) { return b.source === 'authoritative'; }) ? 'authoritative'\n      : daily.some(function (b) { return b.source === 'authoritative'; }) ? 'mixed' : 'aggregated',\n    lastClose: last.close, lastDate: last.date, closePos: Math.round(last.closePos * 100),\n    daily: daily\n  };\n}\n\n// Is the interest improving or fading? The same score computed one session\n// earlier, compared with today's. Without at least three sessions there is\n// nothing to compare and the honest answer is that we do not know.\nfunction candidateTrend(daysRows) {\n  var rows = (daysRows || []).filter(function (r) { return r && r.length; });\n  if (rows.length < 3) return { direction: 'unknown', delta: null, text: 'אין מספיק ימים להשוואה' };\n  var now = candidateScore(rows), before = candidateScore(rows.slice(0, -1));\n  if (now.score == null || before.score == null) return { direction: 'unknown', delta: null, text: 'אין מספיק ימים להשוואה' };\n  var d = now.score - before.score;\n  return {\n    direction: d >= 6 ? 'improving' : d <= -6 ? 'fading' : 'steady',\n    delta: d,\n    text: d >= 6 ? 'העניין מתחזק (+' + d + ')' : d <= -6 ? 'העניין נחלש (' + d + ')' : 'ללא שינוי מהותי'\n  };\n}\n\n// ---------------------------------------------------------------- live interest\n//\n// For symbols already on the watchlist, during the session: is this still worth\n// the attention it is taking? This is NOT the setup score — a symbol can hold a\n// valid setup and still have gone quiet, and a symbol with no setup can be the\n// most alive thing on the screen.\nfunction liveInterest(A, ctx) {\n  var c = ctx || {};\n  if (!A || !A.state) return { score: null, reasons: [], verdict: 'אין נתונים' };\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length;\n  var atr = b.atr20 || b.avgRange || 1;\n  var reasons = [], score = 0;\n\n  // Movement over the last half hour, judged BOTH against the symbol's own\n  // average bar and against its price. Measuring only in ATR units made a dead\n  // stock look active: when the average bar is a cent, six cents is \"6R\" and\n  // nothing at all has happened.\n  var w = bars.slice(-30);\n  var hi = Math.max.apply(null, w.map(function (x) { return x.high; }));\n  var lo = Math.min.apply(null, w.map(function (x) { return x.low; }));\n  var swing = (hi - lo) / atr;\n  var swingPct = b.close > 0 ? (hi - lo) / b.close * 100 : 0;\n  var moving = swingPct >= 0.25;                  // a quarter percent in 30 min\n  if (swing >= 6 && moving) { score += 30; reasons.push('תנועה חזקה בחצי השעה האחרונה'); }\n  else if (swing >= 3 && moving) { score += 20; reasons.push('תנועה סבירה בחצי השעה האחרונה'); }\n  else if (swing >= 1.5 && moving) { score += 10; reasons.push('תנועה מוגבלת'); }\n  else { reasons.push('כמעט ללא תנועה בחצי השעה האחרונה (' + (Math.round(swingPct * 100) / 100) + '%)'); }\n\n  // is volume still arriving\n  var recentVol = w.reduce(function (s, x) { return s + x.volume; }, 0) / Math.max(1, w.length);\n  var dayVol = bars.reduce(function (s, x) { return s + x.volume; }, 0) / Math.max(1, n);\n  var rel = dayVol > 0 ? recentVol / dayVol : 0;\n  if (rel >= 1.3) { score += 25; reasons.push('מחזור מתגבר (×' + (Math.round(rel * 100) / 100) + ')'); }\n  else if (rel >= 0.8) { score += 15; reasons.push('מחזור יציב'); }\n  else { score += 3; reasons.push('המחזור דועך (×' + (Math.round(rel * 100) / 100) + ')'); }\n\n  // proximity to a level that matters\n  var T = c.tactical || tactical(A);\n  var near = [T.support, T.resistance].filter(Boolean).map(function (l) { return Math.abs(l.r); });\n  var d = near.length ? Math.min.apply(null, near) : Infinity;\n  if (d <= 0.5) { score += 25; reasons.push('צמוד לרמה משמעותית'); }\n  else if (d <= 1.5) { score += 15; reasons.push('קרוב לרמה'); }\n  else if (isFinite(d)) { score += 5; reasons.push('רחוק מרמות'); }\n\n  // a structure worth following\n  var st = S.announced || S.trend;\n  if (st === 'UP' || st === 'DOWN') { score += 20; reasons.push('מבנה תוך-יומי מוכרז (' + st + ')'); }\n  else { score += 5; reasons.push('ללא מבנה תוך-יומי'); }\n\n  // Nothing else can rescue a symbol that is not moving: structure and levels\n  // are only worth watching on something that travels.\n  if (!moving) { score = Math.min(score, 30); reasons.push('אין תנועה — לא מצדיק תשומת לב חיה'); }\n  score = Math.max(0, Math.min(100, score));\n  return {\n    score: score, reasons: reasons, movingPct: Math.round(swingPct * 100) / 100,\n    verdict: score >= 60 ? 'שווה תשומת לב חיה' : score >= 35 ? 'עניין בינוני' : 'כבר לא מעניין',\n    quiet: score < 35, swingR: Math.round(swing * 10) / 10, volRel: Math.round(rel * 100) / 100\n  };\n}\n\n// ---------------------------------------------------------------- suggestions\n//\n// Recommends only. Nothing here moves a symbol on its own — the trader decides,\n// and a screen that silently reshuffled the watchlist would be worse than one\n// that said nothing.\n//\n//   watch: [{ symbol, candidate, live }]\n//   scan:  [{ symbol, candidate, trend }]\nfunction suggestions(watch, scan, opts) {\n  var o = opts || {};\n  var PROMOTE_AT = o.promoteAt == null ? 60 : o.promoteAt;\n  var DEMOTE_AT = o.demoteAt == null ? 35 : o.demoteAt;\n\n  var weakest = (watch || []).filter(function (x) {\n    return x.live && x.live.score != null && x.live.score < DEMOTE_AT;\n  }).sort(function (a, b) { return a.live.score - b.live.score; });\n\n  var promote = (scan || []).filter(function (x) {\n    if (!x.candidate || x.candidate.score == null) return false;\n    if (x.candidate.score < PROMOTE_AT) return false;\n    // Improving matters as much as high: a score that is high and fading is\n    // yesterday's story.\n    return !x.trend || x.trend.direction !== 'fading';\n  }).sort(function (a, b) {\n    var d = b.candidate.score - a.candidate.score;\n    if (d) return d;\n    return (b.trend && b.trend.delta || 0) - (a.trend && a.trend.delta || 0);\n  });\n\n  var room = Math.max(0, (o.max == null ? 40 : o.max) - (watch || []).length);\n  return {\n    promote: promote.slice(0, 8).map(function (x, i) {\n      // Only pair a promotion with a removal when the list is actually full.\n      var swap = (!room && weakest[i]) ? weakest[i] : null;\n      return {\n        symbol: x.symbol, score: x.candidate.score,\n        why: (x.candidate.reasons || []).slice(0, 2).map(function (r) { return r.text; }),\n        direction: x.trend ? x.trend.direction : 'unknown',\n        directionText: x.trend ? x.trend.text : null,\n        insteadOf: swap ? swap.symbol : null,\n        insteadOfWhy: swap ? swap.live.verdict + ' (' + swap.live.score + ')' : null\n      };\n    }),\n    demote: weakest.slice(0, 8).map(function (x) {\n      return { symbol: x.symbol, score: x.live.score, why: x.live.reasons.slice(0, 2),\n        verdict: x.live.verdict };\n    }),\n    room: room,\n    note: room ? 'יש מקום — הוספה לא מחייבת הסרה' : 'המעקב מלא: הוספה מחייבת הסרה'\n  };\n}\n\n\n// ============================================================================\n// BUY DECISION CARD — an additional view. Nothing here is read by any other\n// surface, and this file reads the state the application has already built.\n//\n// The premise that shapes everything: we do NOT know the price the trader is\n// looking at in his broker. So the card must never say \"if it reaches X, buy\" —\n// that is a limit order, and it assumes we can see the market. It says instead:\n// given the price ON YOUR SCREEN RIGHT NOW, here is the decision.\n//\n// Four states only. BUY, WAIT, RECHECK, NO SETUP. RECHECK is not a refusal —\n// it means the snapshot no longer covers the price being seen, and the answer\n// has to be recomputed from newer candles. A lower price may be a better\n// entry; a higher one may be a breakout. Neither is knowable from here.\n// ============================================================================\n\nfunction buyCard(st, A, ctx) {\n  var c = ctx || {};\n  var NA = '—';\n  var n2 = function (x) { return x == null ? NA : (+x).toFixed(2); };\n\n  var out = {\n    symbol: st && st.symbol || (c.symbol || NA),\n    snapshotTime: c.snapshotTime || null,\n    lastBarTime: null, lastClose: null,\n    coverage: null, decision: 'NO SETUP', quality: NA, confidence: NA,\n    map: [], validity: null, reasons: [], levels: [], blocked: null\n  };\n  if (!st || !A || !A.state) { out.blocked = 'אין ניתוח זמין'; return out; }\n\n  var bars = (A.rawBars && A.rawBars.length) ? A.rawBars : A.bars;\n  var last = bars && bars.length ? bars[bars.length - 1] : null;\n  var b = A.state.bar;\n  var atr = (b && (b.atr20 || b.avgRange)) || 0;\n  out.lastBarTime = last ? last.time : null;\n  out.lastClose = last ? last.close : null;\n  out.coverage = st.coverage || null;\n\n  // ---- data safety comes before anything else -----------------------------\n  // An unusable session cannot produce a BUY, whatever the setup looks like.\n  var cov = st.coverage || {};\n  if (st.stale) out.blocked = 'נתונים ישנים — לא ניתן להחליט';\n  else if (st.sessionIncomplete)\n    out.blocked = 'סשן חלקי (' + (cov.receivedMinutes || 0) + '/' + (cov.expectedMinutes || 0)\n      + ' דקות) — לא ניתן להחליט';\n  else if (st.tooOldToEnter) out.blocked = 'הנר האחרון ישן מדי להחלטה';\n  else if (!last) out.blocked = 'אין נרות';\n  if (out.blocked) {\n    out.decision = 'RECHECK';\n    out.reasons.push(out.blocked);\n    return out;\n  }\n\n  // ---- what the existing analysis already decided --------------------------\n  var P = st.plan || null;\n  var lv = st.levels || {};\n  var px = out.lastClose;\n\n  // The BUY range is the zone the existing plan already identified, not a\n  // percentage invented around the last price. Without a plan there is no zone\n  // and therefore no buy.\n  var zone = (P && P.zone && P.zone.length === 2) ? P.zone.slice() : null;\n  var hasEdge = !!(P && zone && !st.noEdge);\n\n  // Validity comes from STRUCTURE, not from a band drawn around the last price.\n  // 616.42-617.08 around 616.75 is arbitrary symmetry dressed up as analysis:\n  // it says nothing about where the reading actually stops holding. The real\n  // boundaries are the levels the setup depends on — the zone, the\n  // invalidation, the nearest support and resistance. Where none of those\n  // exist, there is no validity range and none is invented.\n  var T = st.tactical || null;\n  var bounds = [];\n  var addBound = function (v, why) {\n    if (v == null || !isFinite(v)) return;\n    bounds.push({ price: +(+v).toFixed(2), why: why });\n  };\n  if (T && T.support) addBound(T.support.price, 'תמיכה');\n  if (T && T.resistance) addBound(T.resistance.price, 'התנגדות');\n  if (lv.tacticalInvalidation != null) addBound(lv.tacticalInvalidation, 'ביטול');\n  if (lv.watch != null) addBound(lv.watch, 'רמת מעקב');\n  if (P && P.zone && P.zone.length === 2) { addBound(P.zone[0], 'אזור'); addBound(P.zone[1], 'אזור'); }\n\n  var below = bounds.filter(function (x) { return x.price < px; }).sort(function (a, b2) { return b2.price - a.price; })[0];\n  var above = bounds.filter(function (x) { return x.price > px; }).sort(function (a, b2) { return a.price - b2.price; })[0];\n  var vLow = below ? below.price : null;\n  var vHigh = above ? above.price : null;\n  out.validity = (vLow != null && vHigh != null)\n    ? { low: vLow, high: vHigh, lowWhy: below.why, highWhy: above.why } : null;\n\n  // ---- the decision --------------------------------------------------------\n  if (!hasEdge) {\n    // NO SETUP is its own answer, not a weak version of one. There is nothing\n    // whose validity could be mapped, so there is no price map, no validity\n    // range and no confidence — confidence in a setup that does not exist is a\n    // number about nothing. And it is never RECHECK: RECHECK means an ACTIVE\n    // setup no longer covers the price, which presupposes a setup.\n    out.decision = 'NO SETUP';\n    out.reasons.push(st.why || 'אין setup קנייה כרגע');\n    out.map = [];\n    out.validity = null;\n    out.quality = 'אין';\n    out.confidence = NA;\n    out.hasMap = false;\n  } else {\n    var zLow = Math.min(zone[0], zone[1]), zHigh = Math.max(zone[0], zone[1]);\n    // Does the buy zone overlap the band this snapshot can speak about?\n    var overlaps = zHigh >= vLow && zLow <= vHigh;\n    out.decision = overlaps ? 'BUY' : 'WAIT';\n    if (!overlaps) out.reasons.push('אזור הקנייה (' + n2(zLow) + '–' + n2(zHigh)\n      + ') מחוץ לטווח שהכרטיס יכול לדבר עליו');\n  }\n\n  // ---- the price decision map ---------------------------------------------\n  // Only where there IS an active setup, and only where structure gives real\n  // boundaries. Read as: the number you are looking at in your broker.\n  var rows = [];\n  var canMap = hasEdge && out.validity != null;\n  out.hasMap = canMap;\n  if (canMap) {\n  if (out.decision === 'BUY') {\n    var zl = Math.max(vLow, Math.min(zone[0], zone[1]));\n    var zh = Math.min(vHigh, Math.max(zone[0], zone[1]));\n    rows.push({ from: zl, to: zh, decision: 'BUY', text: 'קונה' });\n    if (zh < vHigh) rows.push({ from: +(zh + 0.01).toFixed(2), to: vHigh, decision: 'WAIT', text: 'לא קונה' });\n    if (zl > vLow) rows.push({ from: vLow, to: +(zl - 0.01).toFixed(2), decision: 'WAIT', text: 'לא קונה' });\n  } else if (out.decision === 'WAIT' || out.decision === 'NO SETUP') {\n    rows.push({ from: vLow, to: vHigh, decision: out.decision === 'WAIT' ? 'WAIT' : 'NO SETUP',\n      text: out.decision === 'WAIT' ? 'לא קונה' : 'אין setup' });\n  }\n  rows.sort(function (x, y) { return y.from - x.from; });\n  rows.push({ above: vHigh, decision: 'RECHECK', text: 'לבדוק מחדש' });\n  rows.push({ below: vLow, decision: 'RECHECK', text: 'לבדוק מחדש' });\n  }\n  out.map = rows;\n\n  // ---- supporting readings, all taken from what already exists -------------\n  if (out.decision !== 'NO SETUP') {\n    out.quality = out.decision === 'BUY' ? 'תקין' : 'חלש';\n    out.confidence = (st.score != null)\n      ? (st.score >= 7 ? 'גבוה' : st.score >= 4 ? 'בינוני' : 'נמוך') : NA;\n  }\n\n  out.structure = st.structure || (A.state.announced || A.state.trend) || NA;\n  out.momentum = st.momentum || NA;\n  out.vwap = b ? b.vwap : null;\n  out.aboveVwap = b ? !!b.aboveVwap : null;\n  out.ema9 = b ? b.ema9 : null;\n  out.ema20 = b ? b.ema20 : null;\n  out.market = c.market || NA;\n\n  var pf = st.pressure || null;\n  if (pf) {\n    // The pressure object exposes buyPct/sellPct. I read buyers/sellers and got\n    // undefined, so the card showed \"buyers weakening\" with no numbers beside\n    // it — the exact thing the rule forbids.\n    out.buyersPct = pf.buyPct != null ? pf.buyPct : pf.buyers;\n    out.sellersPct = pf.sellPct != null ? pf.sellPct : pf.sellers;\n    out.buyersTrend = pf.buyersTrend; out.sellersTrend = pf.sellersTrend;\n  }\n\n  // Nearby levels that actually bear on a buy decision.\n  if (T) {\n    if (T.support) out.levels.push({ name: 'תמיכה קרובה', price: T.support.price });\n    if (T.resistance) out.levels.push({ name: 'התנגדות קרובה', price: T.resistance.price });\n  }\n  if (lv.watch != null) out.levels.push({ name: 'רמת מעקב', price: lv.watch });\n\n  // ---- a real explanation, not a label ------------------------------------\n  // \"אין setup קנייה כרגע\" restates the verdict. The reader needs to know WHY\n  // from the same facts printed above: where the price finished, what the\n  // structure is doing, and which side is gaining.\n  out.why = (function () {\n    var parts = [];\n    if (px != null) parts.push('המחיר ' + (c.closed ? 'סיים ב-' : 'עומד על ') + px.toFixed(2));\n    var mom = { PUSHING: 'בדחיפה', PULLBACK: 'בתוך pullback', RECOVERY: 'בהתאוששות',\n      SELLING: 'תחת מכירות', FLAT: 'ללא מומנטום' }[out.momentum];\n    var strc = { UP: 'מבנה עולה', DOWN: 'מבנה יורד', RANGE: 'בטווח' }[out.structure];\n    if (strc) parts.push(strc);\n    if (mom) parts.push(mom);\n    if (pf && pf.buyersTrend && pf.sellersTrend)\n      parts.push('הקונים ' + pf.buyersTrend + ' והמוכרים ' + pf.sellersTrend);\n    var lead = parts.join(', ') + '.';\n    var verdict = out.decision === 'NO SETUP' ? ' כרגע אין setup קנייה מספיק ברור.'\n      : out.decision === 'WAIT' ? ' התנאים לא מצדיקים קנייה במחיר הנוכחי.'\n      : out.decision === 'BUY' ? ' התנאים מצדיקים קנייה בטווח המסומן.'\n      : ' צריך נרות חדשים כדי להחליט.';\n    return lead + verdict;\n  })();\n\n  // What the trader is actually asking. When the market is shut this is not a\n  // rejection — there is simply nothing to act on.\n  out.buyNow = c.closed ? '— המסחר סגור'\n    : out.decision === 'BUY' ? 'כן, בטווח המסומן'\n    : out.decision === 'WAIT' ? 'לא'\n    : out.decision === 'RECHECK' ? '— צריך נרות חדשים'\n    : '— אין setup';\n\n  // A live price map only means something while a live price exists.\n  if (c.closed) { out.hasMap = false; out.map = []; out.validity = null; }\n  out.marketClosed = !!c.closed;\n\n  // A probability is shown only if it measures THIS decision. The path model\n  // answers \"which of two levels is touched first\", which is a different\n  // question, so it is deliberately not carried over.\n  out.probability = null;\n  out.probabilityNote = 'הסתברות לא מוצגת: המודל הקיים מודד אירוע אחר';\n\n  if (!out.reasons.length && st.why) out.reasons.push(st.why);\n  return out;\n}\n\n// The same card as plain text. Built from the card object rather than scraped\n// from the DOM, so what is copied is exactly what was decided — a copy that can\n// drift from the screen is worse than none.\nfunction buyCardText(card, opts) {\n  var o = opts || {};\n  var NA = '—';\n  var n2 = function (x, d) { return x == null ? NA : (+x).toFixed(d == null ? 2 : d); };\n  var L = [];\n  var DEC = { BUY: 'BUY — אפשר לקנות', WAIT: 'WAIT — לא קונה כרגע',\n    RECHECK: 'RECHECK — לבדוק מחדש', 'NO SETUP': 'NO SETUP — אין setup' };\n\n  L.push(card.symbol + (card.marketClosed\n    ? ' · סגירה' + (card.snapshotTime ? ' ' + card.snapshotTime : '')\n    : (card.snapshotTime ? ' · תמונת מצב ' + card.snapshotTime : '')));\n  var cov = card.coverage || {};\n  L.push('נר אחרון: ' + n2(card.lastClose, 3)\n    + (cov.receivedMinutes != null ? ' · ' + cov.receivedMinutes + '/' + cov.expectedMinutes\n      + ' נרות ' + (cov.complete ? 'תקין' : 'חסר') : ''));\n  L.push('');\n  L.push(DEC[card.decision] || card.decision);\n  if (card.blocked) L.push(card.blocked);\n  L.push('');\n  L.push('מצב קנייה: ' + card.quality);\n  L.push('ביטחון: ' + card.confidence);\n\n  if (card.hasMap && card.validity) {\n    L.push('');\n    L.push(o.live ? 'מחיר Trading שלך עכשיו' : 'מפת מחיר');\n    (card.map || []).forEach(function (r) {\n      var range = r.above != null ? ('מעל ' + n2(r.above))\n        : r.below != null ? ('מתחת ' + n2(r.below))\n        : (n2(r.from) + '–' + n2(r.to));\n      L.push('  ' + range + '  ->  ' + r.decision + ' — ' + r.text);\n    });\n    L.push('');\n    L.push('תקפות הכרטיס: ' + n2(card.validity.low) + '–' + n2(card.validity.high)\n      + ' (' + card.validity.lowWhy + ' / ' + card.validity.highWhy + ')');\n    L.push('מחוץ לטווח -> RECHECK עם הנרות החדשים.');\n    L.push('מחיר נמוך יותר עשוי ליצור setup טוב יותר, גבוה יותר עשוי ליצור פריצה.');\n  }\n\n  L.push('');\n  L.push('למה אין BUY');\n  if (card.blocked) L.push('  ' + card.blocked);\n  L.push('  ' + (card.why || (card.reasons || []).join(' ')));\n\n  L.push('');\n  L.push('נתונים');\n  L.push('  מבנה: ' + card.structure);\n  L.push('  מומנטום: ' + card.momentum);\n  L.push('  ' + (card.vwap == null ? 'VWAP: ' + NA\n    : (card.aboveVwap ? 'מחיר מעל VWAP ' : 'מחיר מתחת VWAP ') + n2(card.vwap)));\n  L.push('  EMA9 / EMA20: ' + n2(card.ema9) + ' / ' + n2(card.ema20));\n  L.push('  שוק: ' + card.market);\n\n  if (card.buyersPct != null) {\n    L.push('');\n    L.push('קונים / מוכרים: ' + card.buyersPct + '% / ' + card.sellersPct + '%');\n    var dir = [];\n    if (card.buyersTrend) dir.push('קונים ' + card.buyersTrend);\n    if (card.sellersTrend) dir.push('מוכרים ' + card.sellersTrend);\n    if (dir.length) L.push('כיוון הכוח: ' + dir.join(' · '));\n  }\n\n  if ((card.levels || []).length) {\n    L.push('');\n    L.push('רמות');\n    card.levels.forEach(function (l) { L.push('  ' + l.name + ': ' + n2(l.price)); });\n  }\n\n  L.push('');\n  L.push('החלטה');\n  L.push('  BUY NOW: ' + card.buyNow);\n  if (!card.hasMap) L.push('  מפת מחיר: '\n    + (card.marketClosed ? 'לא מוצגת כשהמסחר סגור' : 'לא מוצגת — אין setup'));\n  L.push('');\n  L.push(card.probabilityNote);\n  return L.join('\\n');\n}\n\n\n</script>\n<div class=\"foot\">\n  <span id=\"refresh\"></span>\n  <span><button id=\"order\">ישן למעלה</button> <button id=\"pause\">השהה רענון</button></span>\n</div>\n\n<script>\n(function(){\n  var qs=function(s){return document.querySelector(s)};\n  var parts=location.pathname.split('/').filter(Boolean);            // view, SYM, DATE\n  var SYM=(parts[1]||'').toUpperCase(), DATE=parts[2]||'';\n  var rows=[], newestFirst=true, paused=false, timer=null, left=60, picked=-1;\n  var MARKET=['SPY','QQQ'], SECTOR={NVDA:'SMH',AVGO:'SMH',AMD:'SMH',AAPL:'XLK',MSFT:'XLK',GOOGL:'XLC',META:'XLC',AMZN:'XLY',TSLA:'XLY',JPM:'XLF','BRK-B':'XLF'};\n  var market={};   // symbol -> analyze() result or null\n  var symSel=qs('#sym'), dateSel=qs('#date');\n\n  function fmtK(v){return v>=1e6?(v/1e6).toFixed(2)+'M':v>=1e3?Math.round(v/1e3)+'K':String(v)}\n  function ago(s){if(s==null)return '';if(s<60)return 'לפני '+s+' שנ׳';var m=Math.floor(s/60);if(m<60)return 'לפני '+m+' דק׳';return 'לפני '+Math.floor(m/60)+' שע׳'}\n  function bust(u){return u+(u.indexOf('?')>0?'&':'?')+'ts='+Date.now()}\n  function j(u){return fetch(bust(u),{cache:'no-store'}).then(function(r){return r.json()})}\n  function derive(r){\n    var range=r.high-r.low, body=Math.abs(r.close-r.open);\n    var pct=function(x){return range>0?Math.round(x/range*1000)/10:0};\n    r.dir=r.close>r.open?'up':r.close<r.open?'down':'flat';\n    r.body=pct(body); r.uw=pct(r.high-Math.max(r.open,r.close)); r.lw=pct(Math.min(r.open,r.close)-r.low); r.range=range;\n    return r;\n  }\n\n  function loadSymbols(){\n    return j('/').then(function(h){\n      var list=h.tracked||[]; if(SYM&&list.indexOf(SYM)<0)list.unshift(SYM); if(!SYM)SYM=list[0]||'NVDA';\n      symSel.innerHTML=list.map(function(s){return '<option'+(s===SYM?' selected':'')+'>'+s+'</option>'}).join('');\n    });\n  }\n  function loadDates(){\n    return j('/days/'+SYM).then(function(d){\n      var ds=(d.days||[]).map(function(x){return x.date});\n      if(DATE&&ds.indexOf(DATE)<0)ds.unshift(DATE);\n      if(!DATE)DATE=ds[0]||'';\n      dateSel.innerHTML=ds.map(function(x){return '<option value=\"'+x+'\"'+(x===DATE?' selected':'')+'>'+x.slice(5).replace('-','/')+'</option>'}).join('');\n    });\n  }\n  function loadMarket(){\n    var syms=MARKET.concat(SECTOR[SYM]?[SECTOR[SYM]]:[]);\n    return Promise.all(syms.map(function(s){\n      return j('/day/'+s+(DATE?'/'+DATE:'')+'?format=json').then(function(d){var r=(d.rows||[]).map(derive);var A=r.length?analyze(r,engineOpts()):null;if(A)A.symbol=s;market[s]=A},function(){market[s]=null});\n    }));\n  }\n  function load(){\n    var u='/day/'+SYM+(DATE?'/'+DATE:'')+'?format=json';\n    return loadMarket().then(function(){return j(u)}).then(function(d){\n      rows=(d.rows||[]).map(derive); DATE=d.date||DATE;\n      if(dateSel.value!==DATE&&DATE){ if(![].some.call(dateSel.options,function(o){return o.value===DATE})){var o=document.createElement('option');o.value=DATE;o.textContent=DATE.slice(5).replace('-','/');dateSel.insertBefore(o,dateSel.firstChild)} dateSel.value=DATE }\n      render(d.stale_seconds);\n      history.replaceState(null,'','/view/'+SYM+'/'+DATE);\n    });\n  }\n\n  function render(stale){\n    var f=qs('#fresh'); var span=f.querySelector('span');\n    if(!rows.length){f.className='fresh';span.textContent='אין נתונים';qs('#tbl').innerHTML='<div class=\"msg\"><b>אין נרות ל-'+SYM+'</b><br>ל-'+(DATE||'היום')+' עדיין לא נשמר כלום.</div>';qs('#svg').innerHTML='';qs('#last').textContent='—';qs('#chg').textContent='';qs('#stats').innerHTML='';return}\n    var last=rows[rows.length-1], first=rows[0];\n    var closedSession=last.time>='15:59';\n    f.className='fresh '+(closedSession?'fresh-ok':stale<=180?'fresh-ok':stale<=600?'fresh-aging':'fresh-stale');\n    span.textContent=closedSession?'סשן הסתיים':'נר אחרון '+ago(stale);\n\n    var chg=(last.close-first.open)/first.open*100;\n    qs('#last').textContent=last.close.toFixed(2);\n    var c=qs('#chg'); c.textContent=(chg>=0?'+':'')+chg.toFixed(2)+'%'; c.className=chg>=0?'up':'down';\n    var hi=Math.max.apply(null,rows.map(function(r){return r.high})), lo=Math.min.apply(null,rows.map(function(r){return r.low}));\n    var ups=rows.filter(function(r){return r.dir==='up'}).length, downs=rows.filter(function(r){return r.dir==='down'}).length;\n    var vol=rows.reduce(function(a,r){return a+r.volume},0);\n    var hiBar=rows.reduce(function(a,r){return r.high>a.high?r:a},rows[0]), loBar=rows.reduce(function(a,r){return r.low<a.low?r:a},rows[0]);\n    qs('#stats').innerHTML=\n      '<span>גבוה <b>'+hi.toFixed(2)+'</b> ב-<b>'+hiBar.time+'</b></span>'+\n      '<span>נמוך <b>'+lo.toFixed(2)+'</b> ב-<b>'+loBar.time+'</b></span>'+\n      '<span>נרות <b>'+rows.length+'</b></span>'+\n      '<span>עולים <b>'+ups+'</b> יורדים <b>'+downs+'</b></span>'+\n      '<span>נפח <b>'+fmtK(vol)+'</b></span>';\n    drawChart();\n    drawTable();\n    drawStruct();\n  }\n\n  // ---------------- structure tab\n  var seenAsk = {}, badgeCount = 0;\n  var SENS = { sensitive:{K:3,minMove:1.0,runs:2}, balanced:{K:3,minMove:1.5,runs:3}, quiet:{K:3,minMove:2.0,runs:3} };\n  var SENS_NOTE = { sensitive:'swing חדש מ-1× טווח ממוצע, מגמה אחרי 2 swings', balanced:'swing מ-1.5× טווח, מגמה אחרי 3', quiet:'swing מ-2× טווח, מגמה אחרי 3' };\n  function engineOpts(){return SENS[qs('#sens').value]||SENS.balanced}\n  var TYPE = {\n    strong_breakout:['🟢','פריצה חזקה'], breakout:['🟢','פריצה'], testing:['🟡','בדיקת התנגדות'], rejected:['🔴','פריצה נדחתה'],\n    failed:['🔴','פריצה כשלה'], retest_hold:['🟢','בדיקה חוזרת החזיקה'], support_break:['🔴','שבירת תמיכה'],\n    hl_confirmed:['🟢','HL אושר'], ll_confirmed:['🔴','LL אושר'], hh_confirmed:['🟢','HH אושר'], lh_confirmed:['🔴','LH אושר'],\n    volume_spike:['⚡','קפיצת נפח'], momentum:['⚡','נר מומנטום'], trend_change:['↔','שינוי מגמה'],\n    outside_bar:['◇','נר outside / expansion — לא נספר כמבנה'], level_reclaim:['🟢','רמה שנשברה נכבשה מחדש'], vwap_reclaim:['🟢','VWAP נכבש מחדש'], vwap_loss:['🔴','VWAP אבד']\n  };\n  var TREND = { UP:['🟢','מגמת עלייה'], DOWN:['🔴','מגמת ירידה'], RANGE:['🟡','טווח'], ATTEMPT:['⚡','ניסיון פריצה'], BOUNCE:['🔴','ירידה / ניסיון קפיצה'] };\n  function evText(e){\n    var t=TYPE[e.type]||['•',e.type], s=t[0]+' '+t[1];\n    if(e.level!=null)s+=' '+e.level.toFixed(2);\n    if(e.type==='trend_change')s+=' '+e.from+' → '+e.to;\n    if(e.confirmed===true)s+=' · אושר בנר הבא'; else if(e.confirmed===false)s+=' · לא אושר'; else if(e.confirmed===null&&/breakout/.test(e.type))s+=' · ממתין לאישור';\n    if(e.wick!=null)s+=' · פתיל עליון '+e.wick+'%';\n    if(e.volx!=null)s+=' ×'+e.volx.toFixed(2);\n    return s;\n  }\n  function pctDist(a,b){return b?((a-b)/b*100).toFixed(2)+'%':'—'}\n  function drawStruct(){\n    var A=rows.length?analyze(rows,engineOpts()):null, st=qs('#st'), log=qs('#log');\n    qs('#sensNote').textContent=SENS_NOTE[qs('#sens').value]||'';\n    if(!A){st.innerHTML='<div class=\"empty\">אין נרות לניתוח.</div>';log.innerHTML='';qs('#bl').innerHTML='';return}\n    var S=A.state, tr=TREND[S.state]||TREND.RANGE, b=S.bar;\n    var syms=MARKET.concat(SECTOR[SYM]?[SECTOR[SYM]]:[]), mk=marketContext(syms.map(function(s){return market[s]})), BL=bottomLine(A,mk);\n    var ACT={WAIT:'המתן',LONG:'LONG','LONG WATCH':'LONG WATCH',AVOID:'להימנע'};\n    var lvRow=function(label,L){if(!L)return '<span>'+label+'</span><b>—</b><i></i>';return '<span>'+label+'</span><b>'+L.price.toFixed(2)+'</b><i>'+(L.dist.atr>=0?'+':'')+L.dist.atr.toFixed(1)+'× טווח · '+L.reason+'</i>'};\n    var mkTxt=mk.label==='Bullish'?'שורי':mk.label==='Bearish'?'דובי':mk.label==='Neutral'?'ניטרלי':'לא זמין';\n    var mkDetail=mk.parts.map(function(p){return p.symbol+(p.aboveVwap?' ↑VWAP':' ↓VWAP')}).join(' · ');\n    qs('#bl').innerHTML='<div class=\"act '+BL.action.replace(' ','')+'\"><b>'+ACT[BL.action]+'</b><span class=\"conf\">ביטחון <b>'+BL.confidence+'</b>/10</span></div>'+\n      '<div class=\"rsn\">'+BL.reason+'</div>'+\n      '<div class=\"lv2\">'+lvRow('LONG WATCH >',BL.watch)+lvRow('LONG TRIGGER >',BL.trigger)+lvRow('STRONG >',BL.strong)+(BL.reclaim?'<span>RECLAIM</span><b>'+BL.reclaim.price.toFixed(2)+'</b><i>רמה שנשברה · '+(BL.reclaim.dist.atr>=0?'+':'')+BL.reclaim.dist.atr.toFixed(1)+'× טווח</i>':'')+lvRow('INVALIDATION',BL.invalidation)+'</div>'+\n      '<div class=\"ind\"><span>VWAP <b>'+BL.vwap.toFixed(2)+'</b> '+(BL.aboveVwap?'✅ מעל':'❌ מתחת')+'</span><span>EMA9 <b>'+BL.ema9.toFixed(2)+'</b> EMA20 <b>'+BL.ema20.toFixed(2)+'</b> '+(BL.align==='bull'?'✅ שורי':BL.align==='bear'?'❌ דובי':'מעורב')+'</span><span>טווח ממוצע <b>'+BL.atr.toFixed(2)+'</b></span><span>שוק: '+mkTxt+(mkDetail?' <small style=\"opacity:.7\">('+mkDetail+')</small>':'')+'</span></div>'+\n      '<div class=\"why\">'+BL.why.join(' · ')+'</div>';\n    var h='<div class=\"big '+S.state+'\"><b>'+tr[0]+' '+tr[1]+'</b><small>'+((S.state==='ATTEMPT'||S.state==='BOUNCE')&&S.state!==S.trend?'מגמה: '+TREND[S.trend][1]+' · ':'')+S.reason+'</small></div>';\n    h+='<div class=\"lv\"><div class=\"r\"><small>התנגדות</small><b>'+(S.res?S.res.price.toFixed(2):'—')+'</b><span>'+(S.res?(S.res.time?'גבוה 60 נרות · '+S.res.time:'גבוה היום')+' · '+pctDist(S.res.price,b.close)+' מהמחיר':'המחיר מעל כל הרמות')+'</span></div>';\n    h+='<div class=\"s\"><small>תמיכה</small><b>'+(S.sup?S.sup.price.toFixed(2):'—')+'</b><span>'+(S.sup?(S.sup.kind==='postbreak'?'הנמוך מאז השבירה · '+S.sup.time:S.sup.time?'נמוך 60 נרות · '+S.sup.time:'נמוך היום')+' · '+pctDist(b.close,S.sup.price)+' מתחת למחיר':'המחיר מתחת לכל הרמות')+'</span></div></div>';\n    h+='<div class=\"now\"><div><span class=\"tier '+S.tier+'\">ציון '+S.score+' · '+(S.tier==='NONE'?'שקט':S.tier)+'</span> <span style=\"color:var(--muted);font-size:13px\">נר '+b.time+' · '+(b.dir==='up'?'עולה':b.dir==='down'?'יורד':'שטוח')+' · גוף '+b.body+'% · פתיל עליון '+b.uw+'% · תחתון '+b.lw+'% · נפח ×'+b.volx.toFixed(2)+'</span></div>';\n    if(S.events.length)h+='<ul>'+S.events.map(function(e){return '<li>'+evText(e)+'</li>'}).join('')+'</ul>';\n    if(S.ask)h+='<div class=\"ask\"><span>🤖 שווה ניתוח עכשיו</span><button type=\"button\" id=\"copy\">העתק לצ׳אט</button></div>';\n    else h+='<div style=\"margin-top:10px\"><button type=\"button\" id=\"copy\">העתק מצב לצ׳אט</button></div>';\n    h+='</div>';\n    var sw=A.swings.filter(function(w){return w.confirmedAt<=rows.length-1}).slice(-8);\n    h+='<div class=\"swl\">swings אחרונים: '+(sw.length?sw.map(function(w){return '<span class=\"'+w.label+'\">'+w.label+'</span> <b>'+w.price.toFixed(2)+'</b> '+w.time}).join(' · '):'עדיין אין')+'</div>';\n    st.innerHTML=h;\n    qs('#copy').onclick=function(){var txt=brief(A);(navigator.clipboard?navigator.clipboard.writeText(txt):Promise.reject()).then(function(){qs('#copy').textContent='הועתק'},function(){prompt('העתק:',txt)})};\n    // event log, newest first\n    var evs=A.events.slice().reverse();\n    log.innerHTML=evs.length?evs.map(function(s){\n      var head=s.events.length?evText(s.events[0]):'ציון '+s.score;\n      var rest=s.events.slice(1).map(evText).join(' · ');\n      return '<div class=\"ev '+s.tier+(s.ask?' ask':'')+'\"><span class=\"t\">'+s.time+'</span><span>'+head+'</span><span class=\"p\">'+s.close.toFixed(2)+'</span>'+(rest||s.score>=3?'<span class=\"d\">'+(rest?rest+' · ':'')+'ציון '+s.score+'</span>':'')+'</div>';\n    }).join(''):'<div class=\"empty\">עדיין לא קרה משהו ששווה לרשום.</div>';\n    // badge: new ask-level events since the tab was last opened\n    var newAsk=0; A.events.forEach(function(s){if(s.ask&&!seenAsk[s.time]){newAsk++}});\n    if(newAsk&&qs('#paneStruct').hidden){badgeCount=newAsk;var bg=qs('#badge');bg.textContent=String(badgeCount);bg.hidden=false;document.title='🤖 '+SYM;if(navigator.vibrate)navigator.vibrate(150)}\n  }\n  function brief(A){\n    var S=A.state,b=S.bar,first=rows[0];\n    var syms=MARKET.concat(SECTOR[SYM]?[SECTOR[SYM]]:[]), mk=marketContext(syms.map(function(s){return market[s]})), BL=bottomLine(A,mk);\n    var lv=function(L){return L?L.price.toFixed(2)+' ('+(L.dist.atr>=0?'+':'')+L.dist.atr.toFixed(1)+'x range) — '+L.reason:'—'};\n    var L=[SYM+' '+DATE+' '+b.time+' ET',\n      'ACTION: '+BL.action+' — '+BL.reason,\n      'LONG WATCH: > '+lv(BL.watch), 'LONG TRIGGER: > '+lv(BL.trigger), 'STRONG CONFIRMATION: > '+lv(BL.strong),\n      'RECLAIM LEVEL: '+(BL.reclaim?BL.reclaim.price.toFixed(2)+' (broken support)':'—'),\n      'INVALIDATION / CURRENT LOW: '+lv(BL.invalidation),\n      'VWAP: '+BL.vwap.toFixed(2)+(BL.aboveVwap?' ✅ above':' ❌ below')+' · EMA9 '+BL.ema9.toFixed(2)+' / EMA20 '+BL.ema20.toFixed(2)+' ('+BL.align+') · AVG RANGE '+BL.atr.toFixed(2),\n      'MARKET: '+mk.label+(mk.parts.length?' ('+mk.parts.map(function(p){return p.symbol+(p.aboveVwap?'>':'<')+'VWAP '+p.align}).join(', ')+')':''),\n      'CONFIDENCE: '+BL.confidence+'/10 ['+BL.why.join(', ')+']',\n      '',\n      'Price '+b.close.toFixed(2)+' ('+((b.close-first.open)/first.open*100).toFixed(2)+'% from open) · day H '+b.dayHigh.toFixed(2)+' L '+b.dayLow.toFixed(2),\n      'State: '+S.state+(S.state!==S.trend?' (trend '+S.trend+')':'')+' — '+S.reason,\n      'Resistance: '+(S.res?S.res.price.toFixed(2)+(S.res.time?' (60-bar high, '+S.res.time+')':' (day high)'):'none above'),\n      'Support: '+(S.sup?S.sup.price.toFixed(2)+(S.sup.time?' (60-bar low, '+S.sup.time+')':' (day low)'):'none below'),\n      'Last bar: '+b.dir.toUpperCase()+' body '+b.body+'% uw '+b.uw+'% lw '+b.lw+'% vol x'+b.volx.toFixed(2)+' range '+b.range.toFixed(2)+' (avg '+b.avgRange.toFixed(2)+')',\n      'Score '+S.score+' → '+S.tier+(S.ask?' · ASK':''),\n      'Events now: '+(S.events.length?S.events.map(evText).join(' | '):'none')];\n    var sw=A.swings.filter(function(w){return w.confirmedAt<=rows.length-1}).slice(-8);\n    L.push('Swings (K=3): '+(sw.length?sw.map(function(w){return w.label+' '+w.price.toFixed(2)+'@'+w.time}).join(', '):'none yet'));\n    var recent=A.events.slice(-5).reverse().filter(function(s){return s.i!==S.i});\n    if(recent.length)L.push('Recent: '+recent.map(function(s){return s.time+' '+(s.events.length?evText(s.events[0]):'score '+s.score)}).join(' | '));\n    L.push('Source: '+location.origin+'/day/'+SYM+'/'+DATE+'?ts='+Date.now());\n    return L.join('\\n');\n  }\n  qs('#tabBars').onclick=function(){qs('#paneBars').hidden=false;qs('#paneStruct').hidden=true;this.classList.add('on');qs('#tabStruct').classList.remove('on')};\n  qs('#tabStruct').onclick=function(){qs('#paneBars').hidden=true;qs('#paneStruct').hidden=false;this.classList.add('on');qs('#tabBars').classList.remove('on');\n    var bg=qs('#badge');bg.hidden=true;badgeCount=0;document.title=SYM;if(rows.length){var A=analyze(rows,engineOpts());A.events.forEach(function(s){if(s.ask)seenAsk[s.time]=true})}};\n  qs('#sens').onchange=function(){seenAsk={};drawStruct()};\n\n  function drawChart(){\n    var svg=qs('#svg'); var W=Math.max(320,svg.clientWidth||360), H=300, PH=210, VH=60, top=14, gap=10;\n    svg.setAttribute('viewBox','0 0 '+W+' '+H);\n    var n=rows.length, padL=8, padR=52, cw=(W-padL-padR)/Math.max(n,60), bw=Math.max(1.5,Math.min(6,cw*0.65));\n    var hi=Math.max.apply(null,rows.map(function(r){return r.high})), lo=Math.min.apply(null,rows.map(function(r){return r.low}));\n    var span=(hi-lo)||1; hi+=span*0.04; lo-=span*0.04; span=hi-lo;\n    var y=function(p){return top+(hi-p)/span*PH};\n    var vmax=Math.max.apply(null,rows.map(function(r){return r.volume}))||1;\n    var out=[];\n    // horizontal grid: 4 levels\n    for(var g=0;g<=4;g++){var p=lo+span*g/4, yy=y(p); out.push('<line class=\"grid\" x1=\"'+padL+'\" x2=\"'+(W-padR)+'\" y1=\"'+yy.toFixed(1)+'\" y2=\"'+yy.toFixed(1)+'\"/><text class=\"gridtxt\" x=\"'+(W-padR+6)+'\" y=\"'+(yy+4).toFixed(1)+'\">'+p.toFixed(2)+'</text>')}\n    // hour ticks\n    rows.forEach(function(r,i){ if(r.time.slice(3)==='00'||r.time==='09:30'){var x=padL+i*cw+cw/2; out.push('<line class=\"grid\" x1=\"'+x.toFixed(1)+'\" x2=\"'+x.toFixed(1)+'\" y1=\"'+top+'\" y2=\"'+(top+PH+gap+VH)+'\"/><text class=\"gridtxt\" x=\"'+(x+3).toFixed(1)+'\" y=\"'+(H-2)+'\">'+r.time+'</text>')}});\n    // candles + volume\n    rows.forEach(function(r,i){\n      var x=padL+i*cw+cw/2, cls=r.dir+'-c';\n      out.push('<line class=\"wick '+cls+'\" x1=\"'+x.toFixed(1)+'\" x2=\"'+x.toFixed(1)+'\" y1=\"'+y(r.high).toFixed(1)+'\" y2=\"'+y(r.low).toFixed(1)+'\"/>');\n      var yo=y(r.open), yc=y(r.close), bt=Math.min(yo,yc), bh=Math.max(1,Math.abs(yo-yc));\n      out.push('<rect class=\"body '+cls+'\" x=\"'+(x-bw/2).toFixed(1)+'\" y=\"'+bt.toFixed(1)+'\" width=\"'+bw.toFixed(1)+'\" height=\"'+bh.toFixed(1)+'\" data-i=\"'+i+'\"/>');\n      var vh=r.volume/vmax*VH;\n      out.push('<rect class=\"vol '+cls+'\" x=\"'+(x-bw/2).toFixed(1)+'\" y=\"'+(top+PH+gap+VH-vh).toFixed(1)+'\" width=\"'+bw.toFixed(1)+'\" height=\"'+vh.toFixed(1)+'\"/>');\n    });\n    // VWAP / EMA overlays from the engine\n    var A=analyze(rows,engineOpts());\n    if(A){var line=function(key,cls){var d='';A.bars.forEach(function(bb,i){var v=bb[key];if(v==null||v<lo||v>hi){return}var x=padL+i*cw+cw/2;d+=(d?' L':'M')+x.toFixed(1)+' '+y(v).toFixed(1)});if(d)out.push('<path class=\"'+cls+'\" d=\"'+d+'\"/>')};\n      line('ema20','ema20');line('ema9','ema9');line('vwap','vwap')}\n    // last close marker\n    var lr=rows[rows.length-1], ly=y(lr.close);\n    out.push('<line class=\"cursor\" x1=\"'+padL+'\" x2=\"'+(W-padR)+'\" y1=\"'+ly.toFixed(1)+'\" y2=\"'+ly.toFixed(1)+'\"/>');\n    out.push('<rect class=\"pickbg\" x=\"'+(W-padR+2)+'\" y=\"'+(ly-9).toFixed(1)+'\" width=\"'+(padR-4)+'\" height=\"18\" rx=\"4\"/><text class=\"pick\" x=\"'+(W-padR+6)+'\" y=\"'+(ly+4).toFixed(1)+'\">'+lr.close.toFixed(2)+'</text>');\n    // picked bar readout\n    if(picked>=0&&picked<n){var pr=rows[picked], px=padL+picked*cw+cw/2;\n      out.push('<line class=\"cursor\" x1=\"'+px.toFixed(1)+'\" x2=\"'+px.toFixed(1)+'\" y1=\"'+top+'\" y2=\"'+(top+PH)+'\"/>');\n      var label=pr.time+'  O '+pr.open.toFixed(2)+'  H '+pr.high.toFixed(2)+'  L '+pr.low.toFixed(2)+'  C '+pr.close.toFixed(2)+'  V '+fmtK(pr.volume);\n      var lw=label.length*6.4+12, lx=Math.min(Math.max(padL,px-lw/2),W-padR-lw);\n      out.push('<rect class=\"pickbg\" x=\"'+lx.toFixed(1)+'\" y=\"2\" width=\"'+lw.toFixed(1)+'\" height=\"16\" rx=\"4\"/><text class=\"pick\" x=\"'+(lx+6).toFixed(1)+'\" y=\"13\" style=\"font-size:11px\">'+label+'</text>');\n    }\n    svg.innerHTML=out.join('');\n    svg.onclick=function(e){var pt=svg.getBoundingClientRect(); var xr=(e.clientX-pt.left)/pt.width*W; var i=Math.floor((xr-padL)/cw); if(i>=0&&i<n){picked=i;drawChart();var tr=document.getElementById('r'+i);if(tr){tr.scrollIntoView({block:'center',behavior:'smooth'})}}};\n  }\n\n  function drawTable(){\n    var avg=rows.reduce(function(a,r){return a+r.volume},0)/rows.length;\n    var order=newestFirst?rows.slice().reverse():rows;\n    var h='<table><thead><tr><th>שעה</th><th>פתיחה</th><th>גבוה</th><th>נמוך</th><th>סגירה</th><th>נפח</th><th>×</th><th>גוף</th><th></th></tr></thead><tbody>';\n    order.forEach(function(r){\n      var i=rows.indexOf(r), x=avg>0?r.volume/avg:0;\n      var cls=r.dir+(i===rows.length-1?' last':'')+(x>=2?' hot':'')+(i===picked?' picked':'');\n      // mini candle: body placed by open/close within the bar's own range\n      var top=r.range>0?(r.high-Math.max(r.open,r.close))/r.range*100:0, bh=r.range>0?Math.max(4,r.body):50;\n      h+='<tr id=\"r'+i+'\" class=\"'+cls+'\"><td>'+r.time+'</td><td>'+r.open.toFixed(2)+'</td><td>'+r.high.toFixed(2)+'</td><td>'+r.low.toFixed(2)+'</td><td class=\"c\">'+r.close.toFixed(2)+'</td><td>'+fmtK(r.volume)+'</td><td class=\"x'+(x>=2?' hi':'')+'\">'+x.toFixed(1)+'</td><td>'+r.body+'%</td><td><span class=\"mini '+r.dir+'\"><i></i><b style=\"top:'+top.toFixed(0)+'%;height:'+bh.toFixed(0)+'%\"></b></span></td></tr>';\n    });\n    qs('#tbl').innerHTML=h+'</tbody></table>';\n    qs('#tbl').onclick=function(e){var tr=e.target.closest('tr[id]');if(tr){picked=+tr.id.slice(1);drawChart();drawTable()}};\n  }\n\n  // refresh: only while the session may still be running\n  function tick(){\n    var live=rows.length&&rows[rows.length-1].time<'15:59'&&DATE===todayET();\n    var el=qs('#refresh');\n    if(!live){el.textContent='';return}\n    if(paused){el.textContent='רענון מושהה';return}\n    left--; el.textContent='רענון בעוד '+left+' שנ׳';\n    if(left<=0){left=60;load().catch(function(){})}\n  }\n  function todayET(){var p=new Intl.DateTimeFormat('en-CA',{timeZone:'America/New_York',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date()).reduce(function(a,x){a[x.type]=x.value;return a},{});return p.year+'-'+p.month+'-'+p.day}\n\n  symSel.onchange=function(){SYM=symSel.value;DATE='';picked=-1;loadDates().then(load)};\n  dateSel.onchange=function(){DATE=dateSel.value;picked=-1;load()};\n  qs('#order').onclick=function(){newestFirst=!newestFirst;this.textContent=newestFirst?'ישן למעלה':'חדש למעלה';drawTable()};\n  qs('#pause').onclick=function(){paused=!paused;this.textContent=paused?'המשך רענון':'השהה רענון';tick()};\n  window.addEventListener('resize',function(){if(rows.length)drawChart()});\n\n  loadSymbols().then(loadDates).then(load).catch(function(e){qs('#tbl').innerHTML='<div class=\"msg\"><b>לא הצלחתי לטעון.</b><br>'+String(e)+'</div>'});\n  timer=setInterval(tick,1000);\n})();\n</script>\n</body>\n</html>\n";
export const RADAR_HTML = "<!doctype html>\n<html lang=\"he\" dir=\"rtl\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width,initial-scale=1,viewport-fit=cover\">\n<title>Market Radar</title>\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link href=\"https://fonts.googleapis.com/css2?family=Heebo:wght@400;600;800&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{\n    --paper:#F2F4F7; --ink:#1B2430; --muted:#5B6673; --rule:#D6DBE2; --well:#FFFFFF;\n    --up:#0F766E; --down:#B42318; --flat:#8A94A0;\n    --s-quiet:#8A94A0; --s-watch:#B7791F; --s-close:#C2410C; --s-ready:#0F766E; --s-active:#1D4ED8; --s-avoid:#B42318; --s-none:#98A2B3;\n  }\n  *{box-sizing:border-box}\n  /* THE root cause of the clipped card. Nothing stopped the PAGE from becoming\n     wider than the screen, and a mobile browser then widens the whole layout\n     viewport to fit it. A position:fixed overlay is placed against that widened\n     viewport, so the card appeared shifted and cut at the edge while the list\n     showed through beside it — and every fix inside the card was powerless,\n     because the card was not the thing overflowing. */\n  html,body{margin:0;background:var(--paper);color:var(--ink);font-family:Heebo,system-ui,sans-serif;font-variant-numeric:tabular-nums;-webkit-text-size-adjust:100%;\n    max-width:100%;overflow-x:hidden}\n  body{padding-bottom:calc(24px + env(safe-area-inset-bottom))}\n  .num{direction:ltr;unicode-bidi:isolate}\n  button,select{font:inherit}\n  button:focus-visible,select:focus-visible,[tabindex]:focus-visible{outline:2px solid var(--ink);outline-offset:2px}\n\n  header{position:sticky;top:0;z-index:20;background:var(--paper);border-bottom:1px solid var(--rule)}\n  .hrow{display:flex;align-items:center;gap:10px;padding:12px 16px 8px}\n  .hrow h1{font-size:19px;font-weight:800;margin:0;letter-spacing:.01em}\n  .spacer{flex:1}\n  .build{font-size:10px;color:var(--muted);direction:ltr;unicode-bidi:isolate;opacity:.8}\n  .audit{font-size:11px;font-weight:800;padding:3px 8px;border-radius:7px;cursor:pointer}\n  .audit[hidden]{display:none}\n  .audit.ok{background:#D7EFEC;color:var(--up)}\n  .audit.warn{background:#FBEED0;color:#8A5B12}\n  .audit.bad{background:#F6DAD6;color:var(--down)}\n  .auditbox{margin:6px 10px;padding:10px 12px;background:var(--well);border:1px solid var(--rule);border-radius:11px;font-size:12.5px;line-height:1.55}\n  .auditbox[hidden]{display:none}\n  .auditbox b{font-weight:800}\n  .auditbox .f{padding:5px 0;border-top:1px solid var(--rule)}\n  .auditbox .f:first-of-type{border-top:none}\n  .mini-btn{background:var(--well);border:1px solid var(--rule);border-radius:9px;padding:6px 10px;font-size:13px;color:var(--ink)}\n  .strip{display:flex;gap:8px;overflow-x:auto;padding:0 16px 8px;font-size:12.5px;color:var(--muted);scrollbar-width:none}\n  .strip::-webkit-scrollbar{display:none}\n  .strip b{color:var(--ink);font-weight:600}\n  .strip .chip{background:var(--well);border:1px solid var(--rule);border-radius:8px;padding:4px 9px;white-space:nowrap}\n  .strip .chip.up b{color:var(--up)} .strip .chip.down b{color:var(--down)}\n  .counts{display:flex;gap:6px;overflow-x:auto;padding:0 16px 10px;scrollbar-width:none}\n  .counts::-webkit-scrollbar{display:none}\n  .cnt{border-radius:8px;padding:4px 9px;font-size:12px;font-weight:600;color:#fff;white-space:nowrap;border:none}\n  .cnt[data-on=\"0\"]{opacity:.35}\n  .cnt.sel{outline:2px solid var(--ink);outline-offset:1px}\n  .ctrl{display:flex;align-items:center;gap:8px;padding:0 16px 10px;font-size:13px;color:var(--muted)}\n  .ctrl select{background:var(--well);border:1px solid var(--rule);border-radius:9px;padding:6px 9px;font-size:13px;color:var(--ink)}\n\n  .rows{padding:6px 10px 0}\n  .row{display:grid;grid-template-columns:60px 1fr 74px;gap:8px 10px;background:var(--well);border:1px solid var(--rule);border-left:4px solid var(--s-none);border-radius:12px;padding:10px 12px;margin-bottom:8px;cursor:pointer;align-items:center}\n  .row:active{transform:scale(.995)}\n  .row .sym{font-weight:800;font-size:16px;letter-spacing:.02em}\n  .row .px{font-size:13px;color:var(--muted);font-weight:600}\n  .row .mid{min-width:0}\n  .row .st{display:inline-block;font-size:11.5px;font-weight:800;color:#fff;border-radius:6px;padding:2px 7px;letter-spacing:.03em}\n  .row .why{font-size:13px;margin-top:3px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}\n  .row .meta{font-size:11.5px;color:var(--muted);margin-top:3px;display:flex;gap:8px;flex-wrap:wrap}\n  .row .meta b{color:var(--ink);font-weight:600}\n  .row .rt{text-align:left}\n  .row .score{font-size:20px;font-weight:800;line-height:1}\n  .row .score small{font-size:11px;font-weight:600;color:var(--muted)}\n  .row svg{display:block;width:74px;height:26px;margin-top:4px}\n  .row.stale{opacity:.6}\n  .QUIET{border-left-color:var(--s-quiet)} .WATCH{border-left-color:var(--s-watch)} .CLOSE{border-left-color:var(--s-close)}\n  .READY{border-left-color:var(--s-ready)} .ACTIVE{border-left-color:var(--s-active)} .AVOID{border-left-color:var(--s-avoid)}\n  .bg-QUIET{background:var(--s-quiet)} .bg-WATCH{background:var(--s-watch)} .bg-CLOSE{background:var(--s-close)}\n  .bg-READY{background:var(--s-ready)} .bg-ACTIVE{background:var(--s-active)} .bg-AVOID{background:var(--s-avoid)} .bg-NODATA{background:var(--s-none)}\n  .spark .line{fill:none;stroke:var(--ink);stroke-width:1.2}\n  .spark .lvl{stroke:var(--rule);stroke-width:1;stroke-dasharray:2 2}\n  .spark .sup{stroke:var(--up)} .spark .res{stroke:var(--down)}\n  .spark .dot{fill:var(--ink)}\n\n  .sheet{position:fixed;inset:0;z-index:40;background:rgba(20,26,34,.62);display:flex;align-items:flex-end}\n  .sheet[hidden]{display:none}\n  /* min-width:0 — a flex item will not shrink below its content without it, so\n     width:100% is not enough: content wider than the screen pushes the whole\n     panel sideways and everything inside is cut at the edge. This only ENABLES\n     shrinking; content that already fits is unaffected. */\n  .panel{background:var(--paper);width:100%;min-width:0;max-width:100vw;max-height:92vh;overflow:auto;border-radius:18px 18px 0 0;padding-bottom:calc(20px + env(safe-area-inset-bottom))}\n  .grab{width:38px;height:4px;background:var(--rule);border-radius:2px;margin:8px auto 0}\n  .phead{display:flex;align-items:center;gap:10px;padding:10px 16px 0}\n  .phead .s{font-size:22px;font-weight:800} .phead .p{font-size:22px;font-weight:800}\n  .phead .chg{font-size:14px;font-weight:600}\n  .phead .chg.up{color:var(--up)} .phead .chg.down{color:var(--down)}\n  .pstat{display:flex;align-items:center;gap:10px;padding:10px 16px 0;flex-wrap:wrap}\n  .pstat .st{font-size:13px;font-weight:800;color:#fff;border-radius:7px;padding:4px 10px}\n  .pstat .sc{font-size:13px;color:var(--muted)} .pstat .sc b{font-size:18px;color:var(--ink)}\n  .pstat .fr{font-size:12px;color:var(--muted);display:flex;align-items:center;gap:5px}\n  .pstat .fr i{width:8px;height:8px;border-radius:50%;background:var(--flat)}\n  .fr.LIVE i{background:var(--up)} .fr.DELAYED i{background:var(--s-watch)} .fr.STALE i{background:var(--down)}\n  .two{display:grid;grid-template-columns:1fr 1fr;gap:8px;padding:12px 16px 0}\n  .card{background:var(--well);border:1px solid var(--rule);border-radius:12px;padding:9px 11px}\n  .card small{display:block;font-size:11.5px;color:var(--muted)} .card b{font-size:17px;font-weight:800}\n  .card span{font-size:11.5px;color:var(--muted)}\n  .lvls{padding:12px 16px 0}\n  .lvls .l{display:grid;grid-template-columns:auto 1fr auto;gap:4px 10px;align-items:baseline;padding:6px 0;border-bottom:1px solid var(--rule);font-size:13.5px}\n  .lvls .l span:first-child{color:var(--muted);font-size:12px} .lvls .l b{font-weight:700} .lvls .l i{font-style:normal;font-size:11.5px;color:var(--muted)}\n  .bar{margin:14px 16px 0;background:var(--well);border:1px solid var(--rule);border-radius:12px;padding:14px 12px 10px}\n  .bar svg{display:block;width:100%;height:56px}\n  .bar .t{fill:var(--muted);font-size:9.5px} .bar .now{fill:var(--ink);font-size:10.5px;font-weight:700}\n  .sec{padding:12px 16px 0;display:flex;flex-wrap:wrap;gap:6px 14px;font-size:12.5px;color:var(--muted)}\n  .sec b{color:var(--ink);font-weight:600}\n  .acts{display:flex;gap:8px;padding:14px 16px 0;flex-wrap:wrap}\n  .acts button{background:var(--ink);color:var(--paper);border:none;border-radius:10px;padding:9px 13px;font-size:13px;font-weight:600}\n  .acts .ghost{background:var(--well);color:var(--ink);border:1px solid var(--rule)}\n  .menu{margin:8px 16px 0;background:var(--well);border:1px solid var(--rule);border-radius:12px;overflow:hidden}\n  .menu[hidden]{display:none}\n  .menu button{display:block;width:100%;text-align:right;background:none;border:none;border-bottom:1px solid var(--rule);padding:11px 14px;font-size:14px;color:var(--ink)}\n  .menu button:last-child{border-bottom:none}\n  .menu button small{color:var(--muted);font-size:12px;margin-right:6px}\n  .feed{margin-top:14px;border-top:1px solid var(--rule)}\n  .fmodes{display:flex;gap:6px;padding:10px 16px}\n  .fmodes button{background:var(--well);border:1px solid var(--rule);border-radius:9px;padding:5px 11px;font-size:12.5px;color:var(--muted)}\n  .fmodes button.on{background:var(--ink);color:var(--paper);border-color:var(--ink)}\n  .ev{display:grid;grid-template-columns:48px 1fr auto;gap:8px;padding:9px 16px;border-bottom:1px solid var(--rule);font-size:13.5px;align-items:baseline;border-right:3px solid transparent}\n  .ev .t{color:var(--muted);font-size:12.5px}\n  .ev.g{border-right-color:var(--up)} .ev.r{border-right-color:var(--down)} .ev.o{border-right-color:var(--s-close)}\n  .ev.y{border-right-color:var(--s-watch)} .ev.b{border-right-color:var(--s-active)} .ev.n{border-right-color:var(--rule)}\n  .empty{padding:22px 16px;text-align:center;color:var(--muted);line-height:1.6}\n  /* Buy decision card — styles scoped to .buycard so nothing outside it moves.\n     Every other block in the panel supplies its own 16px side padding; this one\n     did not, so its text ran to the very edge and read as though it were\n     floating over the radar behind it. */\n  .buycard{padding:14px 16px 16px;background:var(--paper);max-width:100%;overflow-x:hidden;box-sizing:border-box}\n  .buycard *{min-width:0;max-width:100%;box-sizing:border-box}\n  .bchead{font-size:15px;font-weight:600;color:var(--muted)}\n  .bchead b{font-size:24px;font-weight:800;color:var(--ink);display:block;margin-bottom:1px}\n  .bcsub{font-size:12.5px;color:var(--muted);margin-top:2px;direction:ltr;unicode-bidi:isolate;text-align:right}\n  .bcverdict{margin:14px 0 10px;padding:18px 12px;border-radius:14px;font-size:20px;font-weight:800;text-align:center;line-height:1.3}\n  .bcverdict.buy{background:#D7EFEC;color:var(--up)}\n  .bcverdict.wait{background:#F6DAD6;color:var(--down)}\n  .bcverdict.recheck{background:#FBEED0;color:#8A5B12}\n  .bcverdict.nosetup{background:var(--rule);color:var(--muted)}\n  .bcblock{font-size:12.5px;color:var(--down);text-align:center;margin-bottom:6px}\n  .bcmeta{display:flex;flex-wrap:wrap;gap:6px 18px;font-size:13px;color:var(--muted);margin:2px 0 2px;\n    padding:10px 12px;background:var(--well);border:1px solid var(--rule);border-radius:10px}\n  .bcmeta b{color:var(--ink)}\n  .bcsec{font-size:11.5px;font-weight:800;color:var(--muted);margin:16px 0 7px;letter-spacing:.04em}\n  .bcmap{border:1px solid var(--rule);border-radius:11px;overflow:hidden}\n  .bcrow{display:grid;grid-template-columns:minmax(0,auto) minmax(0,1fr);align-items:center;gap:10px;\n    padding:10px 12px;border-top:1px solid var(--rule);font-size:13.5px}\n  .bcrow .bcd{text-align:left}\n  .bcrow:first-child{border-top:none}\n  .bcrow .bcr{font-weight:800;direction:ltr;unicode-bidi:isolate}\n  .bcrow.buy{background:#EAF7F5} .bcrow.wait{background:#FCEFED}\n  .bcrow.recheck{background:#FDF6E6} .bcrow.nosetup{background:var(--well)}\n  .bcvalid{margin-top:10px;padding:10px 12px;background:var(--well);border:1px solid var(--rule);border-radius:11px;font-size:13px;font-weight:700}\n  .bcvalid small{display:block;font-weight:400;color:var(--muted);margin-top:5px;line-height:1.5}\n  .bcwhy{font-size:13.5px;line-height:1.6;padding:10px 12px;background:var(--well);\n    border:1px solid var(--rule);border-radius:10px}\n  /* A grid, not space-between. space-between pushes the value to the far edge,\n     and when the panel is wider than the screen that edge is off-screen: the\n     labels stayed visible while PULLBACK was cut to BACK and the EMA pair to\n     \"7 / 616.79\". A two-column grid keeps both parts inside the card. */\n  .bctech{display:grid;grid-template-columns:minmax(0,auto) minmax(0,1fr);gap:7px 12px;font-size:13px;\n    color:var(--muted);padding:11px 12px;background:var(--well);border:1px solid var(--rule);border-radius:10px}\n  .bctech span{display:contents}\n  .bctech span>b{text-align:left;direction:ltr;unicode-bidi:isolate;overflow-wrap:anywhere;color:var(--ink)}\n  .bctech b{color:var(--ink)}\n  .bcnote{font-size:11.5px;color:var(--muted);margin-top:12px;padding-top:8px;border-top:1px solid var(--rule)}\n  .toast{position:fixed;bottom:24px;right:50%;transform:translateX(50%);background:var(--ink);color:var(--paper);padding:9px 16px;border-radius:10px;font-size:13px;z-index:60}\n  .toast[hidden]{display:none}\n\n  .plan{margin:12px 16px 0;background:var(--ink);color:var(--paper);border-radius:14px;padding:14px}\n  .plan .act{display:flex;align-items:baseline;justify-content:space-between;gap:10px}\n  .plan .act b{font-size:26px;font-weight:800}\n  .plan .act .st2{font-size:12px;font-weight:700;opacity:.75;letter-spacing:.04em}\n  .plan .hl{font-size:13px;opacity:.8;margin-top:2px}\n  .plan .wait{margin-top:12px;background:rgba(255,255,255,.1);border-radius:10px;padding:10px 12px}\n  .plan .wait small{display:block;font-size:11.5px;opacity:.75} .plan .wait b{font-size:22px;font-weight:800}\n  .plan .steps{margin-top:10px;display:grid;gap:5px;font-size:13px}\n  .plan .steps div{display:grid;grid-template-columns:76px 1fr;gap:8px}\n  .plan .steps span{opacity:.7;font-size:12px}\n  .plan.FAILED .act b,.plan.DO_NOT_CHASE .act b{color:#FCA5A5}\n  .plan.READY_PARTIAL .act b,.plan.READY_ADD .act b{color:#5EEAD4}\n  .plan.ACTIVE .act b,.plan.TAKE_PROFIT_AREA .act b{color:#93C5FD}\n  /* Alerts sit in a narrow column at the side and never span the screen, so\n     they cannot cover the rows or the decision block underneath. */\n  .frozen{padding:11px 16px;background:#F6DAD6;border-bottom:1px solid #E4B4AD;font-size:12.5px;line-height:1.6}\n  .frozen b{display:block;color:var(--down);font-size:13.5px;margin-bottom:3px}\n  .frozen div{color:var(--muted)}\n  .alerts{position:fixed;top:auto;bottom:12px;right:8px;left:auto;z-index:70;display:flex;flex-direction:column;\n    gap:6px;pointer-events:none;width:min(260px,62vw);max-height:46vh;overflow:hidden}\n  .al{background:var(--well);border:1px solid var(--rule);border-right:5px solid var(--s-active);border-radius:11px;\n    padding:7px 10px;box-shadow:0 4px 14px rgba(20,26,34,.14);pointer-events:auto;font-size:12.5px;opacity:.97}\n  .al.high{border-right-color:var(--s-ready)} .al.bad{border-right-color:var(--s-avoid)} .al.med{border-right-color:var(--s-close)}\n  .al b{font-size:13.5px;font-weight:800;display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}\n  .al div{font-size:11.5px;color:var(--muted);margin-top:1px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}\n  .al .x{position:absolute;inset-inline-start:6px;top:5px;color:var(--muted);font-size:14px;line-height:1;padding:2px 5px}\n  .al{position:relative;padding-inline-start:26px}\n  .log2{margin:0 16px;background:var(--well);border:1px solid var(--rule);border-radius:12px;overflow:hidden}\n  .log2[hidden]{display:none}\n  .log2 button{display:block;width:100%;text-align:right;background:none;border:none;border-bottom:1px solid var(--rule);padding:9px 12px;font-size:13px;color:var(--ink)}\n  .log2 button:last-child{border-bottom:none}\n  .log2 small{color:var(--muted);margin-left:6px}\n  .range{margin:8px 16px 0;background:var(--well);border:1px solid var(--rule);border-radius:12px;padding:12px}\n  .range[hidden]{display:none}\n  .range label{font-size:12px;color:var(--muted);display:block;margin-bottom:3px}\n  .range .r2{display:grid;grid-template-columns:1fr 1fr;gap:8px}\n  .range select{width:100%;background:var(--paper);border:1px solid var(--rule);border-radius:9px;padding:8px;font-size:13px}\n  .range .sum{font-size:12.5px;color:var(--muted);margin-top:9px;line-height:1.5}\n  .range .go{margin-top:9px;background:var(--ink);color:var(--paper);border:none;border-radius:10px;padding:9px 14px;font-size:13px;font-weight:600}\n  .sess{background:var(--s-avoid);color:#fff;font-size:12px;font-weight:700;text-align:center;padding:5px}\n  .sess[hidden]{display:none}\n\n  .wn{margin:12px 16px 0;background:var(--well);border:1px solid var(--rule);border-radius:14px;overflow:hidden}\n  .wn .top2{padding:14px;background:var(--ink);color:var(--paper)}\n  .wn .top2 .a{font-size:27px;font-weight:800;line-height:1.15}\n  .wn .top2 .n{font-size:14px;margin-top:6px;opacity:.9;line-height:1.5}\n  .wn .scen{font-size:11.5px;opacity:.75;margin-top:6px;letter-spacing:.02em}\n  .wn.WATCH_ONLY .top2 .a{color:#CBD5E1}\n  .wn.ENTRY_AVAILABLE .top2 .a{color:#5EEAD4} .wn.SETUP_CANCELLED .top2 .a,.wn.EXIT .top2 .a{color:#FCA5A5}\n  .wn.TAKE_PARTIAL .top2 .a,.wn.HOLD .top2 .a{color:#93C5FD} .wn.SESSION_CLOSED .top2 .a{color:#FDE68A}\n  .odds{padding:12px 14px 0}\n  .odds .bar2{display:flex;height:26px;border-radius:8px;overflow:hidden;font-size:12px;font-weight:700;color:#fff}\n  .odds .u{background:var(--up);display:flex;align-items:center;justify-content:center}\n  .odds .d{background:var(--down);display:flex;align-items:center;justify-content:center}\n  .odds .q{font-size:12px;color:var(--muted);margin-top:6px;line-height:1.5}\n  .odds .q b{color:var(--ink)}\n  .paths{padding:12px 14px 4px;display:grid;gap:10px}\n  .pth{border-radius:10px;padding:10px 12px;font-size:13.5px;line-height:1.65}\n  .pth.u{background:#E6F4F1} .pth.d{background:#FBEAE7}\n  .pth h4{margin:0 0 4px;font-size:12.5px;font-weight:800;color:var(--muted)}\n  .pth div{margin-top:2px}\n  .wn .why2{padding:10px 14px 14px;font-size:12.5px;color:var(--muted);line-height:1.6;border-top:1px solid var(--rule)}\n  details.raw{margin:12px 16px 0}\n  details.raw>summary{cursor:pointer;font-size:13px;color:var(--muted);padding:8px 0}\n\n  .flow{padding:12px 14px 0}\n  .flow .fb{display:flex;height:22px;border-radius:7px;overflow:hidden;font-size:11.5px;font-weight:700;color:#fff}\n  .flow .fb .b{background:var(--up);display:flex;align-items:center;justify-content:center}\n  .flow .fb .s{background:var(--down);display:flex;align-items:center;justify-content:center}\n  .flow .ft{font-size:12.5px;color:var(--muted);margin-top:6px;line-height:1.6}\n  .flow .ft b{color:var(--ink)}\n  .book{padding:10px 14px 0}\n  .book .bb{display:flex;height:20px;border-radius:7px;overflow:hidden;font-size:11px;font-weight:700;color:#fff}\n  .book .bb .b{background:var(--up);display:flex;align-items:center;justify-content:center}\n  .book .bb .a{background:var(--down);display:flex;align-items:center;justify-content:center}\n  .book .lv3{display:grid;grid-template-columns:1fr auto 1fr;gap:2px 10px;margin-top:8px;font-size:12px;direction:ltr}\n  .book .lv3 .h{color:var(--muted);font-size:11px;font-weight:600}\n  .book .lv3 .bq{text-align:right;color:var(--up)} .book .lv3 .aq{text-align:left;color:var(--down)}\n  .book .lv3 .px2{text-align:center;font-weight:600}\n  .book .meta2{font-size:11.5px;color:var(--muted);margin-top:7px;line-height:1.55}\n  .book .warn{font-size:11px;color:var(--muted);opacity:.9;margin-top:4px}\n  .flow .src{font-size:11px;color:var(--muted);opacity:.85;margin-top:4px}\n  .row .fl{font-size:11px;margin-top:2px}\n  .row .fl b{font-weight:700}\n  .row .fl .up{color:var(--up)} .row .fl .dn{color:var(--down)}\n  @media(min-width:720px){\n    .alerts{width:300px;bottom:16px;right:16px}\n    .row{grid-template-columns:80px 1fr 120px 90px}\n    .rows{max-width:900px;margin:0 auto}\n    .panel{max-width:720px;margin:0 auto;border-radius:18px;max-height:88vh}\n    .sheet{align-items:center}\n  }\n</style>\n</head>\n<body>\n<header>\n  <div class=\"sess\" id=\"sess\" hidden>הסשן הסתיים — התצוגה היא מצב בסגירה, לא איתות חי</div>\n  <div class=\"hrow\">\n    <h1>Market Radar</h1>\n    <div class=\"spacer\"></div>\n    <span id=\"clock\" style=\"font-size:12px;color:var(--muted)\"></span>\n    <button class=\"mini-btn\" id=\"reload\" type=\"button\">רענן</button>\n    <span class=\"build\" title=\"גרסת העמוד\">v138  (2026-09-05 16:01Z)</span>\n    <span class=\"audit\" id=\"audit\" hidden></span>\n  </div>\n  <div class=\"strip\" id=\"strip\"></div>\n  <div class=\"counts\" id=\"counts\"></div>\n  <div class=\"ctrl\">\n    <span>מיון</span>\n    <select id=\"sort\">\n      <option value=\"attention\" selected>תשומת לב</option>\n      <option value=\"score\">ציון Setup</option>\n      <option value=\"distance\">מרחק לרמה</option>\n      <option value=\"volume\">נפח</option>\n      <option value=\"symbol\">סימבול</option>\n    </select>\n    <select id=\"sens\">\n      <option value=\"sensitive\">רגיש</option>\n      <option value=\"balanced\" selected>מאוזן</option>\n      <option value=\"quiet\">שקט</option>\n    </select>\n    <div class=\"spacer\"></div>\n    <select id=\"every\" aria-label=\"רענון אוטומטי\">\n      <option value=\"0\">רענון: כבוי</option>\n      <option value=\"3\">3 שנ׳</option>\n      <option value=\"5\">5 שנ׳</option>\n      <option value=\"10\">10 שנ׳</option>\n      <option value=\"15\">15 שנ׳</option>\n      <option value=\"30\">30 שנ׳</option>\n      <option value=\"60\" selected>60 שנ׳</option>\n      <option value=\"120\">2 דק׳</option>\n    </select>\n    <a class=\"mini-btn\" href=\"/scan\" style=\"text-decoration:none\">סריקה · 100</a>\n    <a class=\"mini-btn\" href=\"/bars\" style=\"text-decoration:none\">נרות</a>\n    <button class=\"mini-btn\" id=\"alertsBtn\" type=\"button\">🔔</button>\n    <span id=\"prog\"></span>\n  </div>\n  <div class=\"ctrl\" id=\"quota\" hidden></div>\n  <div class=\"frozen\" id=\"frozen\" hidden></div>\n  <div class=\"log2\" id=\"alertLog\" hidden></div>\n</header>\n\n<div class=\"auditbox\" id=\"auditbox\" hidden></div>\n<div class=\"rows\" id=\"rows\"><div class=\"empty\">טוען…</div></div>\n\n<div class=\"sheet\" id=\"sheet\" hidden>\n  <div class=\"panel\" id=\"panel\"><div class=\"grab\"></div></div>\n</div>\n<div class=\"alerts\" id=\"alerts\"></div>\n<div class=\"toast\" id=\"toast\" hidden></div>\n\n<script>\n// bars-vault structure engine — pure function over one day's 1-minute bars.\n// Inlined into view.html by build-view.mjs; unit-tested directly in node.\n//\n// analyze(rows, { K: 3 }) -> { bars, swings, state, events }\n//   rows: [{ time, open, high, low, close, volume }] in chronological order.\n//\n// Swing confirmation is K bars on each side (default 3, per the owner's\n// \"K=3, no exceptions\" rule; the spec that inspired this used 2). A swing is\n// only usable from the bar that confirms it, so nothing here looks ahead:\n// the state at bar i is computed from bars 0..i.\nfunction analyze(allRows, opts) {\n  var K = (opts && opts.K) || 3;\n  // A flat zero-volume bar is an end-of-session filler, not market activity.\n  // It must not touch momentum, wicks, EMA, volume or scoring.\n  //\n  // ONE EXCEPTION: the 16:00 bar. The closing auction prints there and the feed\n  // often reports it flat with no volume attached, so the old rule threw away\n  // the OFFICIAL CLOSE and left 15:59 standing in for it. Measured on WMT: the\n  // only session whose close matched the provider's daily candle was the one\n  // whose last bar was 16:00, and dropping that bar moved 108.42 to 108.45.\n  var isAuction = function (r) { return r.time === '16:00'; };\n  var rows = allRows.filter(function (r) {\n    return isAuction(r) || !(r.volume === 0 && r.high === r.low);\n  });\n  var n = rows.length, skipped = allRows.length - n;\n  if (!n) return null;\n\n  // ---- per-bar derived values (causal: volume average is of bars 0..i)\n  var bars = [], volSum = 0, rangeSum = 0, dayHigh = -Infinity, dayLow = Infinity;\n  var pvSum = 0, ema9 = null, ema20 = null, k9 = 2 / 10, k20 = 2 / 21, belowVwapRun = 0;\n  for (var i = 0; i < n; i++) {\n    var r = rows[i], range = r.high - r.low, body = Math.abs(r.close - r.open);\n    volSum += r.volume; rangeSum += range;\n    dayHigh = Math.max(dayHigh, r.high); dayLow = Math.min(dayLow, r.low);\n    // VWAP from the open, EMA9/EMA20 on closes, 20-bar average range.\n    pvSum += (r.high + r.low + r.close) / 3 * r.volume;\n    var vwap = volSum > 0 ? pvSum / volSum : r.close;\n    ema9 = ema9 == null ? r.close : r.close * k9 + ema9 * (1 - k9);\n    ema20 = ema20 == null ? r.close : r.close * k20 + ema20 * (1 - k20);\n    var w20 = rows.slice(Math.max(0, i - 19), i + 1), atr20 = w20.reduce(function (s, x) { return s + (x.high - x.low); }, 0) / w20.length;\n    // (atr20 is needed by the EMA test below, so it is computed first)\n    var aboveVwap = r.close > vwap, vwapReclaim = aboveVwap && belowVwapRun >= 3, vwapLoss = !aboveVwap && belowVwapRun === 0 && i > 0 && bars[i - 1].aboveVwap;\n    belowVwapRun = aboveVwap ? 0 : belowVwapRun + 1;\n    // EMA is only bullish/bearish when the two are meaningfully apart (0.15x the\n    // average range) AND the fast one is moving that way. Otherwise: neutral.\n    var prevEma9 = i > 0 ? bars[i - 1].ema9 : ema9;\n    var sep = Math.abs(ema9 - ema20), minSep = 0.15 * (atr20 || range || 1);\n    // The closing auction prints a huge single-minute volume. It is marked so\n    // scoring and attention can ignore it; it is still shown as information.\n    var auction = r.time >= '15:55';\n    var align = 'neutral';\n    if (sep >= minSep) {\n      if (r.close > ema9 && ema9 > ema20 && ema9 > prevEma9) align = 'bull';\n      else if (r.close < ema9 && ema9 < ema20 && ema9 < prevEma9) align = 'bear';\n    }\n    var pct = function (x) { return range > 0 ? Math.round(x / range * 1000) / 10 : 0; };\n    bars.push({\n      i: i, time: r.time, open: r.open, high: r.high, low: r.low, close: r.close, volume: r.volume,\n      range: range, body: pct(body), uw: pct(r.high - Math.max(r.open, r.close)), lw: pct(Math.min(r.open, r.close) - r.low),\n      dir: r.close > r.open ? 'up' : r.close < r.open ? 'down' : 'flat',\n      volx: volSum > 0 ? Math.round(r.volume / (volSum / (i + 1)) * 100) / 100 : 0,\n      avgRange: rangeSum / (i + 1),\n      closePos: range > 0 ? (r.close - r.low) / range : 0.5,      // 1 = closed at high\n      vwap: vwap, ema9: ema9, ema20: ema20, atr20: atr20, aboveVwap: aboveVwap, vwapReclaim: vwapReclaim, vwapLoss: vwapLoss, align: align, auction: auction,\n      newDayHigh: r.high >= dayHigh && (i === 0 || r.high > Math.max.apply(null, rows.slice(0, i).map(function (x) { return x.high; }))),\n      dayHigh: dayHigh, dayLow: dayLow\n    });\n  }\n\n  // ---- swings, confirmed K bars later\n  var swings = [];\n  for (var s = K; s < n - K; s++) {\n    var hiL = true, loL = true, hiR = true, loR = true;\n    for (var k = 1; k <= K; k++) {\n      if (!(rows[s].high > rows[s - k].high)) hiL = false;\n      if (!(rows[s].high >= rows[s + k].high)) hiR = false;\n      if (!(rows[s].low < rows[s - k].low)) loL = false;\n      if (!(rows[s].low <= rows[s + k].low)) loR = false;\n    }\n    if (hiL && hiR) swings.push({ i: s, time: rows[s].time, kind: 'H', price: rows[s].high, confirmedAt: s + K });\n    if (loL && loR) swings.push({ i: s, time: rows[s].time, kind: 'L', price: rows[s].low, confirmedAt: s + K });\n  }\n  swings.sort(function (a, b) { return a.i - b.i; });\n  // An outside / expansion bar that is both a swing high and a swing low gives\n  // no order information (which came first?), so it is a level but never structure.\n  var byBar = {};\n  swings.forEach(function (w) { byBar[w.i] = (byBar[w.i] || 0) + 1; });\n  swings.forEach(function (w) { if (byBar[w.i] > 1) w.outside = true; });\n  // A swing only earns HH/LH/HL/LL if it moved at least MIN_MOVE x the average\n  // bar range past the previous swing of its kind; smaller wiggles are EQ and\n  // count as levels but never as structure.\n  var MIN_MOVE = (opts && opts.minMove) || 1.5, prevH = null, prevL = null;\n  swings.forEach(function (w) {\n    if (w.outside) { w.label = 'OUT'; return; }\n    var tol = MIN_MOVE * bars[w.i].avgRange;\n    if (w.kind === 'H') { w.label = !prevH ? 'H' : w.price > prevH.price + tol ? 'HH' : w.price < prevH.price - tol ? 'LH' : 'EQ'; if (w.label !== 'EQ') prevH = w; }\n    else { w.label = !prevL ? 'L' : w.price > prevL.price + tol ? 'HL' : w.price < prevL.price - tol ? 'LL' : 'EQ'; if (w.label !== 'EQ') prevL = w; }\n  });\n\n  // ---- sequential walk: levels, trend, events\n  //\n  // Levels for breakout/support events are WINDOW extremes, not the nearest\n  // micro-swing: the highest high / lowest low of the last 60 bars, excluding\n  // the most recent 5 (a high made a minute ago is not resistance yet).\n  // Swings still drive trend, HL/LL structure, and the \"structure break\" event.\n  var WIN = 60, AGE = 5, COOLDOWN = 30, MARGIN = (opts && opts.margin) || 0.15, RUNS = (opts && opts.runs) || 3, TEST_RANGE = 0.75;\n  var events = [], prevState = null, lastBreakout = null, states = [], fired = {}, trendRun = 0, runTrend = null, announced = null, broken = null;\n  var cooled = function (type, b) { return fired[type] == null || b - fired[type] > COOLDOWN; };\n  var mark = function (type, b) { fired[type] = b; };\n\n  for (var b = 0; b < n; b++) {\n    var bar = bars[b], next = bars[b + 1] || null, margin = MARGIN * bar.avgRange;\n    var conf = swings.filter(function (w) { return w.confirmedAt <= b; });\n    var structural = function (w) { return w.label !== 'EQ' && w.label !== 'OUT'; };\n    var SH = conf.filter(function (w) { return w.kind === 'H' && structural(w); }), SL = conf.filter(function (w) { return w.kind === 'L' && structural(w); });\n    var lastSH = SH[SH.length - 1] || null, lastSL = SL[SL.length - 1] || null;\n\n    var wEnd = b - AGE, wStart = Math.max(0, b - WIN), res = null, sup = null, reclaimed = null;\n    if (wEnd >= wStart && wEnd >= 0) {\n      var w = rows.slice(wStart, wEnd + 1), hiIdx = 0, loIdx = 0;\n      for (var q = 1; q < w.length; q++) { if (w[q].high > w[hiIdx].high) hiIdx = q; if (w[q].low < w[loIdx].low) loIdx = q; }\n      if (w[hiIdx].high > bar.close) res = { price: w[hiIdx].high, time: w[hiIdx].time, kind: 'window' };\n      if (w[loIdx].low < bar.close) sup = { price: w[loIdx].low, time: w[loIdx].time, kind: 'window' };\n    }\n    // After a confirmed support break the broken level is a reclaim level, not\n    // support; support is the lowest low made since the break.\n    if (broken) {\n      if (bar.low < broken.low) { broken.low = bar.low; broken.lowTime = bar.time; }\n      if (bar.close > broken.level + MARGIN * bar.atr20) broken.above++; else broken.above = 0;\n      if (broken.above >= 2) { reclaimed = broken.level; broken = null; } else if (b - broken.i > 60) broken = null;\n    }\n    if (broken && broken.low < bar.close) sup = { price: broken.low, time: broken.lowTime, kind: 'postbreak' };\n    if (broken && sup && sup.kind !== 'postbreak' && sup.price >= broken.level - 1e-9) sup = broken.low < bar.close ? { price: broken.low, time: broken.lowTime, kind: 'postbreak' } : null;\n\n    var trend = 'RANGE', reason = 'אין רצף';\n    if (lastSH && lastSL && lastSH.label !== 'H' && lastSL.label !== 'L') {\n      if (lastSH.label === 'HH' && lastSL.label === 'HL') { trend = 'UP'; reason = 'HH + HL'; }\n      else if (lastSH.label === 'LH' && lastSL.label === 'LL') { trend = 'DOWN'; reason = 'LH + LL'; }\n      else reason = lastSH.label + ' + ' + lastSL.label;\n    } else if (!lastSH || !lastSL) reason = 'עדיין אין מספיק swings';\n\n    // ---- events, judged against the PREVIOUS bar's levels (no look-ahead on levels;\n    // the only forward look is the 2-bar confirmation, which is reported as such).\n    var pres = prevState ? prevState.res : null, psup = prevState ? prevState.sup : null;\n    var pTrend = prevState ? prevState.trend : null, pSL = prevState ? prevState.lastSL : null, pSH = prevState ? prevState.lastSH : null;\n    var ev = [], score = 0;\n\n    if (pres && bar.high > pres.price && bar.close > pres.price + margin && cooled('breakout', b)) {\n      var confirmed = next ? next.close > pres.price : null;\n      var strong = bar.body > 65 && bar.volx > 1.2;\n      score += confirmed ? 5 : 2;\n      ev.push({ type: strong && confirmed ? 'strong_breakout' : 'breakout', level: pres.price, confirmed: confirmed, strong: strong });\n      lastBreakout = { i: b, level: pres.price, confirmed: confirmed, dayHigh: pres.price >= prevState.bar.dayHigh - 1e-9 };\n      if (confirmed) mark('breakout', b);\n    } else if (pres && bar.high > pres.price && bar.close < pres.price && bar.uw >= 50 && cooled('rejected', b)) {\n      ev.push({ type: 'rejected', level: pres.price, wick: bar.uw }); mark('rejected', b);\n    } else if (pres && !ev.length && bar.close <= pres.price && pres.price - bar.close <= TEST_RANGE * bar.atr20) {\n      ev.push({ type: 'testing', level: pres.price });\n    }\n    if (lastBreakout && b > lastBreakout.i && b - lastBreakout.i <= 10) {\n      if (bar.close < lastBreakout.level) { score -= 3; ev.push({ type: 'failed', level: lastBreakout.level, confirmed: lastBreakout.confirmed, dayHigh: lastBreakout.dayHigh }); lastBreakout = null; }\n      else if (bar.low <= lastBreakout.level + margin && bar.close >= lastBreakout.level && cooled('retest_hold', b)) { ev.push({ type: 'retest_hold', level: lastBreakout.level }); mark('retest_hold', b); }\n    }\n    if (psup && bar.low < psup.price && bar.close < psup.price - margin && cooled('support_break', b)) {\n      var confS = next ? next.close < psup.price : null;\n      score -= 2; ev.push({ type: 'support_break', level: psup.price, confirmed: confS });\n      if (confS) { mark('support_break', b); broken = { level: psup.price, i: b, low: bar.low, lowTime: bar.time, above: 0 }; }\n    }\n    // Structure break: the level the trend rests on gives way.\n    // Only an announced trend (two agreeing swings) can be \"broken\".\n    var pAnnounced = prevState ? prevState.announced : null;\n    if (pAnnounced === 'UP' && pSL && bar.close < pSL.price - margin && cooled('structure_break', b)) {\n      score -= 2; ev.push({ type: 'structure_break', level: pSL.price, side: 'bear', confirmed: next ? next.close < pSL.price : null }); mark('structure_break', b);\n    } else if (pAnnounced === 'DOWN' && pSH && bar.close > pSH.price + margin && cooled('structure_break', b)) {\n      score += 2; ev.push({ type: 'structure_break', level: pSH.price, side: 'bull', confirmed: next ? next.close > pSH.price : null }); mark('structure_break', b);\n    }\n    conf.forEach(function (w) {\n      if (w.confirmedAt !== b || w.label === 'EQ') return;\n      if (w.label === 'OUT') { if (w.kind === 'H') ev.push({ type: 'outside_bar', level: w.price, at: w.time }); return; }\n      ev.push({ type: (w.label.toLowerCase()) + '_confirmed', level: w.price, at: w.time });\n    });\n    if (bar.volx >= 1.5 && !bar.auction) { score += 2; ev.push({ type: 'volume_spike', volx: bar.volx }); }\n    else if (bar.volx >= 1.5 && bar.auction) ev.push({ type: 'auction_volume', volx: bar.volx });\n    var momentum = bar.body >= 75 && bar.range > 1.5 * bar.avgRange;\n    if (momentum) ev.push({ type: 'momentum', body: bar.body, range: bar.range });\n    if (reclaimed != null) ev.push({ type: 'level_reclaim', level: reclaimed });\n    if (bar.vwapReclaim) ev.push({ type: 'vwap_reclaim', level: bar.vwap });\n    if (bar.vwapLoss) ev.push({ type: 'vwap_loss', level: bar.vwap });\n    if (bar.body >= 70) score += 1;\n    if (bar.closePos >= 0.8) score += 1;\n    if (bar.newDayHigh) score += 1;\n    if (bar.uw > 55) score -= 2;\n    // Trend change is announced on the SECOND structural swing that agrees\n    // (HH+HL is the trend; the next HH or HL is the confirmation).\n    var structConfirmedNow = conf.some(function (w) { return w.confirmedAt === b && structural(w); });\n    if (structConfirmedNow) {\n      if (trend === runTrend) trendRun++; else { runTrend = trend; trendRun = 1; }\n      if (trendRun === RUNS && (trend === 'UP' || trend === 'DOWN') && cooled('trend_change', b)) {\n        ev.push({ type: 'trend_change', from: pTrend === trend ? 'RANGE' : pTrend, to: trend }); mark('trend_change', b); announced = trend;\n      }\n    }\n    if (announced && trend !== announced) announced = null;\n\n    var attempt = (pres && Math.abs(pres.price - bar.close) <= TEST_RANGE * bar.atr20) || (lastBreakout && b - lastBreakout.i <= 1);\n    var nearLow = sup ? bar.close - sup.price : bar.close - bar.dayLow;\n    var bounce = (trend === 'DOWN' || broken) && bar.dir === 'up' && nearLow <= 1.5 * bar.atr20;\n    var state = attempt ? 'ATTEMPT' : bounce ? 'BOUNCE' : trend;\n    var tier = score >= 7 ? 'CHECK' : score >= 5 ? 'IMPORTANT' : score >= 3 ? 'WATCH' : 'NONE';\n    // Ask = worth a fresh look: only day-extreme levels, only confirmed.\n    var pDayHigh = prevState ? prevState.bar.dayHigh : Infinity, pDayLow = prevState ? prevState.bar.dayLow : -Infinity;\n    var ask = ev.some(function (e) {\n      if (e.type === 'strong_breakout' || e.type === 'breakout') return e.confirmed === true && e.level >= pDayHigh - 1e-9;\n      if (e.type === 'rejected') return e.level >= pDayHigh - 1e-9;\n      if (e.type === 'failed') return e.confirmed === true && e.dayHigh === true;\n      if (e.type === 'support_break') return e.confirmed === true && e.level <= pDayLow + 1e-9;\n      if (e.type === 'structure_break') return e.confirmed === true;\n      if (e.type === 'trend_change') return true;\n      return false;\n    }) || (bar.volx >= 1.5 && !bar.auction && momentum);\n    var loggable = ask || tier !== 'NONE' || ev.some(function (e) {\n      return e.type === 'failed' || e.type === 'rejected' || e.type === 'support_break' || e.type === 'structure_break' || e.type === 'trend_change' || e.type === 'retest_hold' || /breakout/.test(e.type);\n    });\n\n    var st = { i: b, time: bar.time, close: bar.close, trend: trend, reason: reason, state: state, res: res, sup: sup,\n      lastSH: lastSH, lastSL: lastSL, announced: announced, reclaim: broken ? broken.level : null, score: score, tier: tier, ask: ask, events: ev, bar: bar };\n    states.push(st);\n    if (loggable) events.push(st);\n    prevState = st;\n  }\n\n  // The UNFILTERED rows too. Coverage is a question about the session that was\n  // loaded, not about the subset the analysis chose to use: a no-trade minute\n  // is dropped here on purpose, and counting the remainder made a complete\n  // 335-bar session report as 186 with holes that were never there.\n  return { bars: bars, rawBars: allRows, swings: swings, states: states, state: states[n - 1],\n    events: events, skippedBars: skipped };\n}\n\n// ---------------------------------------------------------------- radar layer\n//\n// Short-term momentum, kept separate from structure on purpose: a confirmed\n// downtrend can still be bouncing.\n//   PUSHING   pressing higher with the structure\n//   PULLBACK  easing back inside an up structure\n//   RECOVERY  bouncing off the lows against a down structure\n//   SELLING   pressing lower\n//   FLAT      no net movement worth naming\nfunction momentum(A, look) {\n  if (!A || !A.state) return { label: 'FLAT', net: 0 };\n  var L = look || 5, bars = A.bars, n = bars.length, w = bars.slice(Math.max(0, n - L));\n  var b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  var net = (b.close - w[0].open) / atr;                       // in range units\n  var ups = w.filter(function (x) { return x.dir === 'up'; }).length;\n  var struct = A.state.announced || A.state.trend;\n  var lowRun = Math.min.apply(null, w.map(function (x) { return x.low; }));\n  var highRun = Math.max.apply(null, w.map(function (x) { return x.high; }));\n  var offLow = (b.close - lowRun) / atr, offHigh = (highRun - b.close) / atr;\n  var label = 'FLAT';\n  if (net >= 0.8 && ups >= L / 2) label = struct === 'DOWN' ? 'RECOVERY' : 'PUSHING';\n  else if (net <= -0.8 && ups <= L / 2) label = struct === 'UP' ? 'PULLBACK' : 'SELLING';\n  else if (offLow >= 1 && offHigh <= 0.5) label = struct === 'DOWN' ? 'RECOVERY' : 'PUSHING';\n  else if (offHigh >= 1 && offLow <= 0.5) label = struct === 'UP' ? 'PULLBACK' : 'SELLING';\n  return { label: label, net: net, offLow: offLow, offHigh: offHigh };\n}\n\n// Tactical levels: what is actually within reach, preferring levels that price\n// has interacted with more than once. Everything is measured in R = one average\n// 1-minute range, so \"0.3R away\" means the same thing on any symbol.\nfunction tactical(A, maxR) {\n  if (!A || !A.state) return { support: null, resistance: null, above: [], below: [], atr: 1 };\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length, atr = b.atr20 || b.avgRange || 1, MAX = (maxR || 3) * atr;\n  var cand = [];\n  var add = function (price, why, weight, kind) {\n    if (price == null || !isFinite(price)) return;\n    cand.push({ price: price, why: why, weight: weight, kind: kind });\n  };\n\n  // A prior HIGH only becomes support once price broke above it and came back\n  // to it without losing it — resistance-turned-support needs evidence, not\n  // just \"price happens to be above it now\". Same, inverted, for prior lows.\n  var acceptedAsSupport = function (level, fromIdx) {\n    var closedAbove = 0, retest = false;\n    for (var i = fromIdx; i < n; i++) {\n      if (bars[i].close > level) closedAbove++;\n      if (closedAbove >= 2 && bars[i].low <= level + 0.25 * atr && bars[i].close >= level) retest = true;\n    }\n    return closedAbove >= 3 && retest;\n  };\n  var acceptedAsResistance = function (level, fromIdx) {\n    var closedBelow = 0, retest = false;\n    for (var i = fromIdx; i < n; i++) {\n      if (bars[i].close < level) closedBelow++;\n      if (closedBelow >= 2 && bars[i].high >= level - 0.25 * atr && bars[i].close <= level) retest = true;\n    }\n    return closedBelow >= 3 && retest;\n  };\n\n  A.swings.forEach(function (w) {\n    if (w.confirmedAt > n - 1) return;\n    var age = n - 1 - w.i; if (age > 120) return;\n    var base = (w.label === 'OUT' || w.label === 'EQ') ? 2 : 3;\n    var recency = age <= 30 ? 2 : age <= 60 ? 1 : 0;\n    if (w.kind === 'H') {\n      if (w.price > b.close) add(w.price, 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time, base + recency, 'res');\n      else if (acceptedAsSupport(w.price, w.i)) add(w.price, 'התנגדות שהפכה לתמיכה ' + w.time, base + recency + 1, 'sup');\n    } else {\n      if (w.price < b.close) add(w.price, 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time, base + recency, 'sup');\n      else if (acceptedAsResistance(w.price, w.i)) add(w.price, 'תמיכה שהפכה להתנגדות ' + w.time, base + recency + 1, 'res');\n    }\n  });\n  if (S.res && S.res.price > b.close) add(S.res.price, 'גבוה 60 נרות', 3, 'res');\n  if (S.sup && S.sup.price < b.close) add(S.sup.price, S.sup.kind === 'postbreak' ? 'הנמוך מאז השבירה' : 'נמוך 60 נרות', S.sup.kind === 'postbreak' ? 4 : 3, S.sup.kind === 'postbreak' ? 'sup' : 'sup');\n  if (b.dayHigh > b.close) add(b.dayHigh, 'גבוה היום', 3, 'res');\n  if (b.dayLow < b.close) add(b.dayLow, 'נמוך היום', 3, 'sup');\n  if (S.reclaim != null) add(S.reclaim, 'רמה שנשברה', 4, S.reclaim > b.close ? 'res' : 'sup');\n  add(b.vwap, 'VWAP', 2, b.vwap > b.close ? 'res' : 'sup');\n  cand[cand.length - 1].soft = true;   // VWAP is context, never a setup level on its own\n\n  // touches and rejections add weight; distance never does.\n  cand.forEach(function (c) {\n    var touch = 0, reject = 0;\n    bars.forEach(function (x) {\n      if (x.low <= c.price + 0.25 * atr && x.high >= c.price - 0.25 * atr) {\n        touch++;\n        if (c.kind === 'res' && x.high > c.price && x.close < c.price) reject++;\n        if (c.kind === 'sup' && x.low < c.price && x.close > c.price) reject++;\n      }\n    });\n    c.touches = touch; c.rejects = reject;\n    c.weight += Math.min(3, Math.floor(touch / 4)) + Math.min(2, reject);\n    c.r = (c.price - b.close) / atr;\n  });\n\n  // Price sitting exactly on a level is the important case, so the side test\n  // allows a small epsilon rather than requiring strictly above/below.\n  var eps = 0.05 * atr;\n  var side = function (want) {\n    return cand.filter(function (c) { return c.kind === want && (want === 'res' ? c.price > b.close - eps : c.price < b.close + eps); })\n      .filter(function (c) { return Math.abs(c.price - b.close) <= MAX; });\n  };\n  // Strongest level wins; distance is only the tie-breaker, and a much closer\n  // level of near-equal strength is preferred.\n  var pick = function (want) {\n    var list = side(want).sort(function (x, y) { return (y.weight - x.weight) || (Math.abs(x.r) - Math.abs(y.r)); });\n    if (!list.length) return null;\n    var best = list[0];\n    list.slice(1, 4).forEach(function (c) { if (c.weight >= best.weight - 1 && Math.abs(c.r) < Math.abs(best.r) - 0.3) best = c; });\n    return best;\n  };\n  var all = function (want) {\n    return side(want).sort(function (x, y) { return want === 'res' ? x.price - y.price : y.price - x.price; });\n  };\n  return { support: pick('sup'), resistance: pick('res'), above: all('res'), below: all('sup'), atr: atr, candidates: cand };\n}\n\n// Distance in R, kept readable at small values. Below 0.05R it is \"on the level\".\nfunction fmtR(r) {\n  var a = Math.abs(r);\n  if (a < 0.05) return 'על הרמה';\n  return (r >= 0 ? '+' : '-') + (a < 1 ? a.toFixed(2) : a.toFixed(1)) + 'R';\n}\n\n// A support is only \"holding\" if price is not driving through it. Deterministic:\n// a big bearish body closing near its low, meaningfully below the level, or two\n// consecutive closes below it.\nfunction aggressiveBreak(A, level) {\n  var bars = A.bars, n = bars.length, b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  if (b.close < level - 0.5 * atr) return true;\n  if (n >= 2 && bars[n - 2].close < level && b.close < level) return true;\n  if (b.dir === 'down' && b.body >= 60 && b.closePos <= 0.25 && b.close < level - 0.15 * atr) return true;\n  return false;\n}\n\n// ---- execution plan: a small deterministic state machine.\n// States: WAITING_FOR_ZONE, IN_ZONE, READY_PARTIAL, WAITING_FOR_CONFIRMATION,\n//         READY_ADD, ACTIVE, TAKE_PROFIT_AREA, DO_NOT_CHASE, FAILED, NO_SETUP\nvar PLAN_TEXT = {\n  WAITING_FOR_ZONE: ['המתן', 'מחכים לכניסה לאזור'],\n  IN_ZONE: ['המתן', 'בתוך האזור — צריך בלימה'],\n  READY_PARTIAL: ['כניסה חלקית', 'האזור מוחזק'],\n  WAITING_FOR_CONFIRMATION: ['החזק', 'מחכים לאישור להוספה'],\n  READY_ADD: ['הוסף', 'האישור התקבל'],\n  ACTIVE: ['החזק', 'מהלך פעיל'],\n  TAKE_PROFIT_AREA: ['מימוש', 'אזור מימוש ראשון'],\n  DO_NOT_CHASE: ['לא לרדוף', 'המחיר התרחק מהכניסה'],\n  FAILED: ['להימנע', 'ה-setup בוטל'],\n  NO_SETUP: ['המתן', 'אין setup']\n};\nfunction executionPlan(A, market) {\n  if (!A || !A.state) return null;\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length, atr = b.atr20 || b.avgRange || 1;\n  var T = tactical(A), BL = bottomLine(A, market), mom = momentum(A);\n  var P = { state: 'NO_SETUP', kind: null, zone: null, entry: null, addAbove: null,\n    invalidation: null, target: null, atr: atr, price: b.close };\n\n  var sup = T.support, res = T.resistance;\n  var bearish = S.announced === 'DOWN' && mom.label !== 'RECOVERY';\n  // A setup needs a real structural level and a tape with some range in it.\n  // A dead-flat chop whose whole day spans one average bar is not a setup,\n  // however close price sits to \"a level\".\n  var dayRangeR = (b.dayHigh - b.dayLow) / atr;\n  var hasTape = dayRangeR >= 2;\n  // Whether the level is actually holding is decided by the state machine\n  // below (holding / aggressiveBreak), not by momentum: the first green bar\n  // off support is exactly the partial-entry moment, and a 5-bar momentum\n  // reading still says SELLING there.\n  var pullbackOk = !bearish && !!sup && !sup.soft && hasTape && sup.weight >= 5;\n  var breakoutOk = !bearish && !!res && !res.soft && hasTape &&\n    (S.trend !== 'DOWN' || mom.label === 'RECOVERY') && (BL.confidence >= 3 || mom.label === 'PUSHING' || S.trend === 'UP');\n\n  // Which setup is in play: the nearer opportunity wins.\n  var dSup = sup ? Math.abs(b.close - sup.price) / atr : Infinity;\n  var dRes = res ? Math.abs(res.price - b.close) / atr : Infinity;\n  if (pullbackOk && dSup <= 3 && (dSup <= dRes || !breakoutOk)) {\n    P.kind = 'pullback';\n    P.zone = [sup.price - 0.15 * atr, sup.price + 0.30 * atr];\n    P.entry = sup.price + 0.05 * atr;\n    var nextUp = T.above.filter(function (c) { return c.price > P.zone[1] + 0.1 * atr; })[0];\n    P.addAbove = nextUp ? nextUp.price : sup.price + 0.7 * atr;\n    var lowerLevels = T.below.filter(function (c) { return c.price < sup.price - 0.05 * atr; });\n    P.invalidation = lowerLevels.length ? Math.max(lowerLevels[0].price, sup.price - 0.9 * atr) : sup.price - 0.45 * atr;\n    P.target = res ? res.price : b.dayHigh;\n    P.levelWhy = sup.why;\n  } else if (breakoutOk && dRes <= 3) {\n    P.kind = 'breakout';\n    P.zone = [res.price, res.price + 0.20 * atr];\n    P.entry = res.price + 0.10 * atr;\n    P.addAbove = res.price + 0.5 * atr;\n    P.invalidation = Math.max(res.price - 0.6 * atr, sup ? sup.price : res.price - 0.9 * atr);\n    var beyond = T.above.filter(function (c) { return c.price > res.price + 0.2 * atr; })[0];\n    P.target = beyond ? beyond.price : res.price + 1.5 * atr;\n    P.levelWhy = res.why;\n  }\n\n  // A setup that just broke must still read FAILED, even though the level it\n  // rested on is now above price and no longer offered as support.\n  // Losing the last structural low by a clear margin is a broken setup even if\n  // the 60-bar window never registered a support_break (the window low can sit\n  // far below the level the setup actually rested on).\n  var lostLevel = !!(S.lastSL && b.close < S.lastSL.price - 0.3 * atr && (n - 1 - S.lastSL.i) <= 25);\n  var justBroke = lostLevel || A.states.slice(-6).some(function (s) {\n    return s.events.some(function (e) {\n      return (e.type === 'support_break' && e.confirmed === true)\n        || (e.type === 'structure_break' && e.side === 'bear' && e.confirmed === true)\n        || (e.type === 'failed');\n    });\n  });\n  if (!P.kind) {\n    if (justBroke) { P.state = 'FAILED'; P.invalidation = null; return decorate(P); }\n    P.state = 'NO_SETUP'; P.reason = bearish ? 'מבנה דובי — אין setup לונג' : 'אין רמה קרובה';\n    return decorate(P);\n  }\n\n  var brokeInvalid = b.close < P.invalidation || lostLevel;\n  var agg = aggressiveBreak(A, P.kind === 'pullback' ? sup.price : P.invalidation);\n  var inZone = b.close >= P.zone[0] && b.close <= P.zone[1];\n  // \"Holding\" = the last two bars did not lose the level. This, not momentum,\n  // is what turns being in the zone into a partial entry.\n  var lvl = P.kind === 'pullback' ? sup.price : P.zone[0];\n  var holding = P.kind !== 'pullback' || (bars.slice(-2).every(function (x) { return x.low >= lvl - 0.35 * atr; }) && b.close >= lvl - 0.1 * atr);\n  var aboveAdd = b.close > P.addAbove;\n  var confirmedAdd = aboveAdd && n >= 2 && bars[n - 2].close > P.addAbove;\n  var nearTarget = P.target && (P.target - b.close) / atr <= 0.3;\n  var extended = b.close > P.entry + 0.5 * atr;\n\n  if (brokeInvalid || (agg && !inZone && b.close < P.zone[0])) P.state = 'FAILED';\n  else if (agg && inZone) P.state = 'FAILED';\n  else if (nearTarget && (aboveAdd || P.kind === 'breakout')) P.state = 'TAKE_PROFIT_AREA';\n  else if (confirmedAdd) P.state = extended && !nearTarget ? 'ACTIVE' : 'READY_ADD';\n  else if (aboveAdd) P.state = 'WAITING_FOR_CONFIRMATION';\n  else if (inZone && holding) P.state = 'READY_PARTIAL';\n  else if (inZone) P.state = 'IN_ZONE';\n  else if (extended) P.state = 'DO_NOT_CHASE';\n  else P.state = 'WAITING_FOR_ZONE';\n\n  return decorate(P);\n\n  function decorate(P) {\n    var t = PLAN_TEXT[P.state] || PLAN_TEXT.NO_SETUP;\n    P.action = t[0]; P.headline = t[1];\n    var z = P.zone ? P.zone.map(function (x) { return x.toFixed(2); }).join('–') : null;\n    P.waitPrice = null; P.nextStep = null; P.ifConfirmed = null; P.ifFailed = null; P.short = null;\n    if (P.state === 'NO_SETUP') { P.short = P.reason; return P; }\n    switch (P.state) {\n      case 'WAITING_FOR_ZONE':\n        P.waitPrice = z;\n        P.nextStep = P.kind === 'pullback' ? 'ירידה לאזור ' + z + ' בלי שבירה אגרסיבית' : 'סגירה מעל ' + P.zone[0].toFixed(2);\n        P.ifConfirmed = 'כניסה חלקית ב-' + P.entry.toFixed(2);\n        P.ifFailed = 'שבירה מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'מחכים ל-' + (P.kind === 'pullback' ? z : P.zone[0].toFixed(2));\n        break;\n      case 'IN_ZONE':\n        P.waitPrice = z;\n        P.nextStep = 'בתוך האזור — צריך בלימה לפני כניסה';\n        P.ifConfirmed = 'בלימה מעל ' + P.zone[0].toFixed(2) + ' — כניסה חלקית';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'באזור ' + z + ' — צריך בלימה';\n        break;\n      case 'READY_PARTIAL':\n        P.waitPrice = z;\n        P.nextStep = 'האזור מוחזק — כניסה חלקית ב-' + P.entry.toFixed(2);\n        P.ifConfirmed = 'חזרה מעל ' + P.addAbove.toFixed(2) + ' — הוספה';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'כניסה חלקית ' + z;\n        break;\n      case 'WAITING_FOR_CONFIRMATION':\n        P.waitPrice = P.addAbove.toFixed(2);\n        P.nextStep = 'נר סגירה נוסף מעל ' + P.addAbove.toFixed(2);\n        P.ifConfirmed = 'הוספה';\n        P.ifFailed = 'חזרה מתחת ' + P.zone[0].toFixed(2) + ' — להחזיק בלבד';\n        P.short = 'אישור מעל ' + P.addAbove.toFixed(2);\n        break;\n      case 'READY_ADD':\n        P.waitPrice = P.addAbove.toFixed(2);\n        P.nextStep = 'הוספה מעל ' + P.addAbove.toFixed(2);\n        P.ifConfirmed = 'יעד ' + (P.target ? P.target.toFixed(2) : '—');\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'הוסף מעל ' + P.addAbove.toFixed(2);\n        break;\n      case 'ACTIVE':\n        P.waitPrice = P.target ? P.target.toFixed(2) : null;\n        P.nextStep = 'מהלך פעיל לעבר ' + (P.target ? P.target.toFixed(2) : 'היעד');\n        P.ifConfirmed = 'מימוש חלקי באזור היעד';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'יעד הבא ' + (P.target ? P.target.toFixed(2) : '—');\n        break;\n      case 'TAKE_PROFIT_AREA':\n        P.waitPrice = P.target.toFixed(2);\n        P.nextStep = 'אזור מימוש ' + P.target.toFixed(2);\n        P.ifConfirmed = 'מימוש חלקי';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'מימוש ' + P.target.toFixed(2);\n        break;\n      case 'DO_NOT_CHASE':\n        P.pullbackZone = [P.entry, P.entry + 0.35 * atr];\n        P.waitPrice = P.pullbackZone.map(function (x) { return x.toFixed(2); }).join('–');\n        P.nextStep = 'פולבק ל-' + P.waitPrice;\n        P.ifConfirmed = 'כניסה חלקית בפולבק';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'לא לרדוף · פולבק ' + P.waitPrice;\n        break;\n      case 'FAILED':\n        P.waitPrice = null;\n        if (P.invalidation == null) { P.nextStep = 'הרמה נשברה — אין כניסה'; P.ifConfirmed = 'setup חדש נדרש'; P.short = 'setup נכשל'; return P; }\n        P.nextStep = 'אין כניסה — הרמה נשברה';\n        P.ifConfirmed = 'setup חדש נדרש';\n        P.ifFailed = null;\n        P.short = 'setup נכשל';\n        break;\n    }\n    return P;\n  }\n}\n\n// One radar row. Status follows the execution state — the single source of\n// truth — so the row can never say CLOSE while the plan says there is nothing\n// to do.\nfunction radarRow(symbol, A, market, freshness) {\n  if (!A || !A.state) return { symbol: symbol, status: 'NO DATA', score: 0, why: 'אין נתונים', nearestR: Infinity, freshness: freshness || 'NO DATA' };\n  var S = A.state, b = S.bar, T = tactical(A), mom = momentum(A), BL = bottomLine(A, market), P = executionPlan(A, market);\n  var near = [T.support, T.resistance].filter(Boolean).map(function (l) { return Math.abs(l.r); });\n  var nearest = near.length ? Math.min.apply(null, near) : Infinity;\n  var ended = freshness === 'SESSION ENDED';\n\n  var status, why = P && P.short ? P.short : 'אין setup';\n  var st = P ? P.state : 'NO_SETUP';\n  if (freshness === 'NO DATA') { status = 'NO DATA'; why = 'אין נתונים'; }\n  else if (st === 'FAILED' || (S.announced === 'DOWN' && st === 'NO_SETUP')) { status = 'AVOID'; why = st === 'FAILED' ? 'setup נכשל' : 'מגמת ירידה מוכרזת'; }\n  else if (st === 'ACTIVE' || st === 'TAKE_PROFIT_AREA') status = 'ACTIVE';\n  else if (st === 'READY_PARTIAL' || st === 'READY_ADD') status = 'READY';\n  // CLOSE needs proximity AND a reason to look: a live plan step, not distance.\n  else if (st === 'WAITING_FOR_CONFIRMATION' || st === 'DO_NOT_CHASE') status = 'CLOSE';\n  else if (st === 'WAITING_FOR_ZONE' && nearest <= 1.0 && (BL.confidence >= 3 || mom.label === 'RECOVERY' || mom.label === 'PUSHING' || (b.volx >= 1.3 && !b.auction))) status = 'CLOSE';\n  else if (st === 'WAITING_FOR_ZONE') status = nearest <= 2 || BL.setup ? 'WATCH' : 'QUIET';\n  else status = 'QUIET';\n\n  // Rank by execution urgency first (spec 31), then status, then distance.\n  var URGENCY = { READY_PARTIAL: 0, READY_ADD: 1, TAKE_PROFIT_AREA: 2, ACTIVE: 3, WAITING_FOR_CONFIRMATION: 4, DO_NOT_CHASE: 5, WAITING_FOR_ZONE: 6, IN_ZONE: 6, NO_SETUP: 8, FAILED: 9 };\n  return {\n    symbol: symbol, status: status, why: why, score: BL.confidence, price: b.close,\n    structure: S.announced || S.trend, momentum: mom.label, nearestR: nearest,\n    support: T.support, resistance: T.resistance, volx: b.volx, auction: b.auction,\n    vwapAbove: b.aboveVwap, align: b.align, atr: T.atr, freshness: freshness || 'LIVE',\n    bl: BL, tactical: T, mom: mom, plan: P, action: P ? P.action : 'המתן',\n    pressure: (typeof pressure === 'function') ? pressure(A, { tactical: T, plan: P }) : null,\n    urgency: URGENCY[st] == null ? 7 : URGENCY[st], sessionEnded: ended\n  };\n}\n\nvar STATUS_ORDER = ['READY', 'CLOSE', 'ACTIVE', 'WATCH', 'QUIET', 'AVOID', 'NO DATA'];\nfunction sortRadar(list, mode) {\n  var arr = list.slice();\n  if (mode === 'symbol') return arr.sort(function (a, b) { return a.symbol < b.symbol ? -1 : 1; });\n  if (mode === 'score') return arr.sort(function (a, b) { return b.score - a.score || a.nearestR - b.nearestR; });\n  if (mode === 'volume') return arr.sort(function (a, b) { return (b.volx || 0) - (a.volx || 0); });\n  if (mode === 'distance') return arr.sort(function (a, b) { return a.nearestR - b.nearestR; });\n  return arr.sort(function (a, b) {\n    return ((a.urgency == null ? 7 : a.urgency) - (b.urgency == null ? 7 : b.urgency))\n      || (STATUS_ORDER.indexOf(a.status) - STATUS_ORDER.indexOf(b.status))\n      || (b.score - a.score) || (a.nearestR - b.nearestR);\n  });\n}\n\n// Market context from the analyses of SPY / QQQ (and optionally a sector ETF).\n// Bullish needs VWAP and EMA alignment to agree across the index ETFs.\nfunction marketContext(list) {\n  var have = list.filter(function (x) { return x && x.state; });\n  if (!have.length) return { label: 'Unavailable', score: 0, parts: [] };\n  var score = 0, parts = [];\n  have.forEach(function (x) {\n    var b = x.state.bar, s = 0;\n    s += b.aboveVwap ? 1 : -1;\n    s += b.align === 'bull' ? 1 : b.align === 'bear' ? -1 : 0;\n    if (x.state.announced === 'UP') s += 1; else if (x.state.announced === 'DOWN') s -= 1;\n    score += s; parts.push({ symbol: x.symbol, score: s, aboveVwap: b.aboveVwap, align: b.align, trend: x.state.trend, close: b.close, vwap: b.vwap });\n  });\n  var per = score / have.length;\n  return { label: per >= 1.5 ? 'Bullish' : per <= -1.5 ? 'Bearish' : 'Neutral', score: score, parts: parts };\n}\n\n// Bottom line: structure first, indicators only move confidence.\n// Never emits LONG without a confirmed structural event.\nfunction bottomLine(A, market) {\n  if (!A || !A.state) return null;\n  var S = A.state, b = S.bar, n = A.states.length, atr = b.atr20 || b.avgRange || 1;\n  var recent = function (type, within, pred) {\n    for (var i = n - 1; i >= Math.max(0, n - 1 - within); i--) {\n      var hit = A.states[i].events.find(function (e) { return e.type === type && (!pred || pred(e)); });\n      if (hit) return { e: hit, ago: n - 1 - i };\n    }\n    return null;\n  };\n  var conf = function (e) { return e.confirmed === true; };\n  var breakout = recent('breakout', 3, conf) || recent('strong_breakout', 3, conf);\n  var bullBreak = recent('structure_break', 3, function (e) { return e.side === 'bull' && e.confirmed === true; });\n  var retest = recent('retest_hold', 5);\n  var rejected = recent('rejected', 5), failed = recent('failed', 5);\n  var bearBreak = recent('structure_break', 10, function (e) { return e.side === 'bear' && e.confirmed === true; });\n  var supBreak = recent('support_break', 10, conf);\n  var vwapReclaim = recent('vwap_reclaim', 3);\n\n  // Levels above: every confirmed swing high of the last 90 bars, the 60-bar\n  // high, the day high and any reclaim level — ascending, merged if closer\n  // than a quarter range. First = watch, second = trigger, third = strong.\n  var above = [], below = [];\n  A.swings.forEach(function (w) {\n    if (w.kind !== 'H' || w.confirmedAt > n - 1 || n - 1 - w.i > 90 || w.price <= b.close) return;\n    above.push({ price: w.price, reason: 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time });\n  });\n  if (S.res) above.push({ price: S.res.price, reason: 'גבוה 60 נרות' + (S.res.time ? ' ' + S.res.time : '') });\n  if (b.dayHigh > b.close) above.push({ price: b.dayHigh, reason: 'גבוה היום' });\n  if (S.reclaim != null && S.reclaim > b.close) above.push({ price: S.reclaim, reason: 'רמה שנשברה — reclaim' });\n  if (S.sup) below.push({ price: S.sup.price, reason: S.sup.kind === 'postbreak' ? 'הנמוך מאז השבירה' : 'נמוך 60 נרות' + (S.sup.time ? ' ' + S.sup.time : '') });\n  if (S.lastSL && S.lastSL.price < b.close && !(S.reclaim != null && S.lastSL.price >= S.reclaim)) below.push({ price: S.lastSL.price, reason: 'swing ' + S.lastSL.label + ' ' + S.lastSL.time });\n  if (b.dayLow < b.close) below.push({ price: b.dayLow, reason: 'נמוך היום' });\n  var merge = function (arr, asc) {\n    arr.sort(function (x, y) { return asc ? x.price - y.price : y.price - x.price; });\n    var out = [];\n    arr.forEach(function (x) { var last = out[out.length - 1]; if (last && Math.abs(x.price - last.price) <= 0.25 * atr) { if (!/swing/.test(last.reason) && /swing/.test(x.reason)) last.reason = x.reason; return; } out.push(x); });\n    return out;\n  };\n  above = merge(above, true); below = merge(below, false);\n  var watch = above[0] || null, trigger = above[1] || null, strong = above[2] || null, invalid = below[0] || null;\n  var dist = function (lv) { return lv ? { pts: lv.price - b.close, atr: (lv.price - b.close) / atr } : null; };\n\n  // Structural setup?\n  var trendUp = S.trend === 'UP', announcedUp = S.announced === 'UP', announcedDown = S.announced === 'DOWN';\n  var setup = trendUp || !!breakout || !!bullBreak || !!retest;\n\n  // Confidence\n  var c = 0, why = [];\n  if (breakout || bullBreak) { c += 3; why.push('+3 פריצה/שבירה מבנית מאושרת'); }\n  if (trendUp) { c += 2; why.push('+2 HH/HL'); }\n  if (b.aboveVwap) { c += 1; why.push('+1 מעל VWAP'); } else { c -= 2; why.push('-2 מתחת VWAP'); }\n  if (b.align === 'bull') { c += 1; why.push('+1 EMA שורי'); } else if (b.align === 'bear') { c -= 1; why.push('-1 EMA דובי'); }\n  var volOk = b.volx >= 1.2 || (breakout && A.states[n - 1 - breakout.ago].bar.volx >= 1.2);\n  if (volOk) { c += 1; why.push('+1 נפח'); }\n  if (market && market.label === 'Bullish') { c += 1; why.push('+1 שוק'); } else if (market && market.label === 'Bearish') { c -= 2; why.push('-2 שוק נגד'); }\n  if (retest) { c += 1; why.push('+1 בדיקה חוזרת'); }\n  if (rejected) { c -= 2; why.push('-2 דחייה'); }\n  if (bearBreak || supBreak || failed) { c -= 3; why.push('-3 ביטול/שבירה'); }\n  c = Math.max(0, Math.min(10, c));\n\n  // Action\n  var action = 'WAIT', reason = 'אין setup מבני';\n  if (announcedDown || bearBreak || supBreak) { action = 'AVOID'; reason = announcedDown ? 'מגמת ירידה מוכרזת' : 'תמיכה/מבנה נשברו'; }\n  else if ((breakout || bullBreak) && c >= 6) { action = 'LONG'; reason = (breakout ? 'פריצה' : 'שבירת מבנה שורית') + ' מאושרת · ביטחון ' + c; }\n  else if (breakout || bullBreak) { action = 'LONG WATCH'; reason = 'פריצה מאושרת אבל ביטחון ' + c + '/10'; }\n  else if (setup && watch && (watch.price - b.close) <= 1.0 * atr) { action = 'LONG WATCH'; reason = 'מגמה עולה, ' + ((watch.price - b.close) / atr).toFixed(1) + '× טווח מהרמה הראשונה'; }\n  else if (setup && vwapReclaim) { action = 'LONG WATCH'; reason = 'VWAP נכבש מחדש בתוך מגמה עולה'; }\n  else if (setup) { action = 'WAIT'; reason = 'מגמה עולה, רחוק מהטריגר'; }\n\n  return {\n    action: action, reason: reason, confidence: c, why: why,\n    watch: watch ? { price: watch.price, reason: watch.reason, dist: dist(watch) } : null,\n    trigger: trigger ? { price: trigger.price, reason: trigger.reason, dist: dist(trigger) } : null,\n    strong: strong ? { price: strong.price, reason: strong.reason, dist: dist(strong) } : null,\n    reclaim: S.reclaim != null ? { price: S.reclaim, dist: dist({ price: S.reclaim }) } : null,\n    invalidation: invalid ? { price: invalid.price, reason: invalid.reason, dist: dist(invalid) } : null,\n    vwap: b.vwap, aboveVwap: b.aboveVwap, ema9: b.ema9, ema20: b.ema20, align: b.align, atr: atr,\n    market: market ? market.label : 'Unavailable', setup: setup\n  };\n}\n\n// Multi-day layer, time-of-day volume, path probability and the plain-language\n// \"what now\" block. Kept in its own file so engine.cjs stays the intraday core.\n\n// ---------------------------------------------------------------- volume by time of day\n// The closing auction is not comparable to a normal minute, so relative volume\n// is measured against the SAME minute on previous days. With no history the\n// baseline is null and callers fall back to the session average.\nfunction volumeBaseline(daysRows) {\n  var sum = {}, cnt = {}, days = 0;\n  (daysRows || []).forEach(function (rows) {\n    if (!rows || !rows.length) return;\n    days++;\n    rows.forEach(function (r) { sum[r.time] = (sum[r.time] || 0) + r.volume; cnt[r.time] = (cnt[r.time] || 0) + 1; });\n  });\n  if (days < 2) return null;\n  var by = {};\n  Object.keys(sum).forEach(function (t) { by[t] = sum[t] / cnt[t]; });\n  return { byTime: by, days: days };\n}\nfunction volxTod(bar, baseline) {\n  if (!baseline || !baseline.byTime[bar.time]) return null;\n  var b = baseline.byTime[bar.time];\n  return b > 0 ? Math.round(bar.volume / b * 100) / 100 : null;\n}\n\n// ---------------------------------------------------------------- daily layer\n// daysRows: array of one day's 1-minute rows, oldest first, today last.\n// A US regular session is 390 one-minute bars, 09:30 to 15:59, plus the 16:00\n// auction print. Anything materially short of that is a session still being\n// collected or one that was fetched mid-day — and aggregating it produces a\n// daily candle that is simply wrong, with no outward sign.\n//\n// Measured: a WMT session truncated at 13:20 reported a high of 106.44 when the\n// real high was 106.78, and that number reached the screen as \"yesterday's\n// high\". A partial session is now marked and excluded from anything that claims\n// to be a completed day.\nvar SESSION_BARS = 390, MIN_COVERAGE = 0.85;\n// Counting bars alone is the wrong test. A thinly traded name has no print in\n// some minutes and the feed omits them, so a COMPLETE session can arrive with\n// 343 bars — PGR, BLK and HON were all refused that way. What actually\n// distinguishes a complete session is that it SPANS the session: it starts at\n// the open and runs to the close. A day truncated at 13:20 fails that however\n// many bars it has.\nfunction sessionSpans(rows) {\n  if (!rows || !rows.length) return false;\n  var times = rows.map(function (r) { return r.time; }).sort();\n  return times[0] <= '09:35' && times[times.length - 1] >= '15:55';\n}\nfunction sessionCoverage(rows) {\n  if (!rows || !rows.length) return 0;\n  var regular = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n  return regular.length / SESSION_BARS;\n}\nfunction aggregateSession(rows) {\n  var o = rows[0].open, c = rows[rows.length - 1].close;\n  var h = -Infinity, l = Infinity, v = 0;\n  rows.forEach(function (r) { h = Math.max(h, r.high); l = Math.min(l, r.low); v += r.volume; });\n  var cov = sessionCoverage(rows), spans = sessionSpans(rows);\n  return { date: rows[0].date, open: o, high: h, low: l, close: c, volume: v, range: h - l,\n    closeStrength: h > l ? (c - l) / (h - l) : 0.5,\n    bars: rows.length, coverage: Math.round(cov * 1000) / 1000, spans: spans,\n    complete: spans && cov >= MIN_COVERAGE,\n    hasAuction: rows.some(function (r) { return r.time === '16:00'; }),\n    source: 'aggregated' };\n}\n\nfunction dailyContext(daysRows, opts) {\n  var o2 = opts || {};\n  var days = (daysRows || []).filter(function (r) { return r && r.length; });\n  if (!days.length) return null;\n  var bars = days.map(aggregateSession);\n  // An authoritative daily candle, when the caller has one, replaces the\n  // aggregation for that date entirely. Never a mix of the two.\n  if (o2.authoritative) {\n    var auth = {};\n    (o2.authoritative || []).forEach(function (b) { auth[b.date] = b; });\n    bars = bars.map(function (b) {\n      var a = auth[b.date];\n      if (!a) return b;\n      return { date: b.date, open: a.open, high: a.high, low: a.low, close: a.close,\n        volume: a.volume, range: a.high - a.low,\n        closeStrength: a.high > a.low ? (a.close - a.low) / (a.high - a.low) : 0.5,\n        bars: b.bars, coverage: b.coverage, complete: true, hasAuction: b.hasAuction,\n        source: 'authoritative' };\n    });\n  }\n  // Which of these IS today? Before the open there is no session for today at\n  // all, and treating the newest stored row as \"today\" pushed every previous-day\n  // value one session further back than its label claimed. The caller passes the\n  // current trading date; without one the old positional rule stands, but the\n  // result says which rule was used.\n  var todayDate = o2.todayDate || null;\n  var hasToday = todayDate ? bars.some(function (b) { return b.date === todayDate; }) : true;\n  var todayIdx = todayDate\n    ? (hasToday ? bars.map(function (b) { return b.date; }).indexOf(todayDate) : bars.length)\n    : bars.length - 1;\n  var today = bars[todayIdx] || bars[bars.length - 1] || null;\n  // \"Previous\" must mean the previous COMPLETED session. A partial day is not a\n  // day, and silently using one is how a truncated high became yesterday's high.\n  var prior = bars.slice(0, todayIdx).filter(function (b) { return b.complete; });\n  var excluded = bars.slice(0, todayIdx).filter(function (b) { return !b.complete; });\n  var atrDaily = prior.length ? prior.reduce(function (s, b) { return s + b.range; }, 0) / prior.length : today.range;\n  // Daily trend over the prior sessions: higher closes and higher lows.\n  var trend = 'RANGE', why = 'אין רצף יומי';\n  if (prior.length >= 3) {\n    var last3 = prior.slice(-3);\n    var upCloses = last3.filter(function (b, i) { return i === 0 || b.close > last3[i - 1].close; }).length;\n    var upLows = last3.filter(function (b, i) { return i === 0 || b.low > last3[i - 1].low; }).length;\n    if (upCloses >= 3 && upLows >= 2) { trend = 'UP'; why = 'סגירות ושפלים עולים'; }\n    else if (upCloses <= 1 && upLows <= 1) { trend = 'DOWN'; why = 'סגירות ושפלים יורדים'; }\n    else why = 'מעורב';\n  }\n  var prev = prior.length ? prior[prior.length - 1] : null;\n  // Multi-day levels: prior highs/lows that price reacted to more than once.\n  var levels = [];\n  prior.forEach(function (b) { levels.push({ price: b.high, why: 'גבוה ' + b.date }, { price: b.low, why: 'נמוך ' + b.date }); });\n  var gap = prev ? today.open - prev.close : 0;\n  return { days: bars.length, priorDays: prior.length, trend: trend, trendWhy: why,\n    incomplete: excluded.map(function (b) { return { date: b.date, bars: b.bars,\n      coverage: Math.round(b.coverage * 100) + '%' }; }),\n    hasToday: hasToday, todayDate: todayDate,\n    previousDate: prior.length ? prior[prior.length - 1].date : null,\n    dateRule: todayDate ? 'previous = newest COMPLETE session before ' + todayDate\n      : 'previous = the row before the last (no trading date supplied)',\n    sources: bars.map(function (b) { return { date: b.date, source: b.source, complete: b.complete }; }),\n    prevHigh: prev ? prev.high : null, prevLow: prev ? prev.low : null, prevClose: prev ? prev.close : null,\n    atrDaily: atrDaily, levels: levels, gap: gap, today: today,\n    closeStrength: prev ? prev.closeStrength : null };\n}\n\n\n\n// ---------------------------------------------------------------- level state\n// The current condition of a level, decided from where price is now and what it\n// did on its way here. A support that was lost and then regained reads as\n// reclaimed — never as \"breaking\", which is what it did ten candles ago.\nvar LEVEL_TEXT = {\n  approaching: 'מתקרב', testing: 'נבחנת', broken: 'נשברה', reclaimed: 'נכבשה מחדש',\n  reclaiming: 'נשברה וחוזרת מעליה', held: 'נשמרת', rejected: 'נדחתה', lost: 'אבדה', far: 'רחוקה'\n};\nfunction levelState(A, level, isSupport, look) {\n  if (!A || !A.state || level == null) return null;\n  var bars = A.bars, n = bars.length, b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  var w = bars.slice(Math.max(0, n - (look || 20)));\n  var d = (b.close - level) / atr;                       // + = price above the level\n  var near = w.filter(function (x) { return x.low <= level + 0.25 * atr && x.high >= level - 0.25 * atr; });\n  var closedBelow = w.filter(function (x) { return x.close < level - 0.15 * atr; }).length;\n  var closedAbove = w.filter(function (x) { return x.close > level + 0.15 * atr; }).length;\n  var lastTwoBelow = n >= 2 && bars[n - 1].close < level - 0.1 * atr && bars[n - 2].close < level - 0.1 * atr;\n  var lastTwoAbove = n >= 2 && bars[n - 1].close > level + 0.1 * atr && bars[n - 2].close > level + 0.1 * atr;\n  var recoveries = w.filter(function (x) { return x.low < level && x.close > level; }).length;\n  // Bars whose LOW went through a support, or whose HIGH went through a\n  // resistance, without closing beyond it: pierced, not merely approached.\n  var pierced = isSupport\n    ? w.filter(function (x) { return x.low < level - 0.02 * atr; }).length\n    : w.filter(function (x) { return x.high > level + 0.02 * atr; }).length;\n  var rejections = w.filter(function (x) { return x.high > level && x.close < level; }).length;\n\n  var state;\n  if (isSupport) {\n    // \"Held\" must mean the level was never given up. Requiring only two\n    // recoveries let a support that CLOSED below five times in a row still read\n    // as held: GOOGL closed 337.31, 337.31, 337.17, 337.32, 337.275 under\n    // 337.57 and came back at 337.75, which is a break and a reclaim, not a\n    // level that was defended.\n    if (lastTwoBelow) state = 'broken';\n    else if (closedBelow >= 2 && lastTwoAbove) state = 'reclaimed';\n    else if (closedBelow >= 2 && d > 0) state = 'reclaiming';\n    // Closes are not the whole story. GOOGL's 338.51 was traded through on four\n    // separate bars — lows of 338.4233, 338.4096, 338.43, 338.42 — and still\n    // read as held because none of them CLOSED below. A level price went\n    // through is not a level that was defended.\n    else if (closedBelow === 0 && pierced === 0 && recoveries >= 2 && d > 0) state = 'held';\n    else if (closedBelow === 0 && pierced >= 2 && d > 0) state = 'reclaiming';\n    else if (Math.abs(d) <= 0.25) state = 'testing';\n    else if (Math.abs(d) <= 1.0) state = 'approaching';\n    else state = 'far';\n  } else {\n    if (lastTwoAbove) state = 'broken';                  // resistance broken = price accepted above\n    // \"Lost\" means the level stopped functioning. A resistance with price\n    // sitting UNDER it is still a resistance: it was rejected, not lost.\n    else if (closedAbove >= 2 && lastTwoBelow && d >= 0) state = 'lost';\n    else if (closedAbove >= 2 && lastTwoBelow) state = 'rejected';\n    else if (rejections >= 1 && d < 0) state = 'rejected';\n    else if (Math.abs(d) <= 0.25) state = 'testing';\n    else if (Math.abs(d) <= 1.0) state = 'approaching';\n    else state = 'far';\n  }\n  return { price: level, state: state, text: LEVEL_TEXT[state], distanceR: d,\n    touches: near.length, recoveries: recoveries, rejections: rejections, isSupport: !!isSupport };\n}\n\n// ---------------------------------------------------------------- buying / selling pressure\n//\n// IMPORTANT: this bar feed carries no order book and no trade-by-trade tape.\n// There is no bid/ask, no Level 2, no per-trade aggressor flag. What follows is\n// pressure INFERRED FROM CANDLES: where each minute closed inside its own range,\n// weighted by that minute's volume. Every result is labelled `source: 'candles'`\n// so the UI can say so and never imply it read the book.\n//\n//   buyVolume  = volume * (close - low)  / (high - low)\n//   sellVolume = volume * (high - close) / (high - low)\n//\n// which is the standard close-location value applied to volume. A minute that\n// closes on its high counts fully as buying, on its low fully as selling.\nfunction pressure(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, bars = A.bars, n = bars.length;\n  if (!n) return null;\n  var WIN = c.window || 15, RECENT = c.recent || 5;\n  var b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n\n  var split = function (list) {\n    var buy = 0, sell = 0;\n    list.forEach(function (x) {\n      var rng = x.high - x.low;\n      // A zero-range minute has no information about who won it.\n      if (rng <= 0 || !x.volume) return;\n      var clv = (x.close - x.low) / rng;\n      buy += x.volume * clv; sell += x.volume * (1 - clv);\n    });\n    var tot = buy + sell;\n    return { buy: buy, sell: sell, total: tot, buyPct: tot > 0 ? buy / tot * 100 : 50 };\n  };\n\n  // The closing auction is excluded: it is one print, not a minute of trading.\n  var usable = bars.filter(function (x) { return !x.auction; });\n  var win = usable.slice(-WIN), recent = usable.slice(-RECENT), prior = usable.slice(-(WIN), -RECENT);\n  if (win.length < 3) return null;\n\n  var W = split(win), R = split(recent), P = prior.length >= 3 ? split(prior) : null;\n  var buyPct = Math.round(W.buyPct), sellPct = 100 - buyPct;\n\n  // Strengthening / weakening is measured per side, on volume actually traded,\n  // not on the percentage alone: a side can gain share while doing less.\n  var perBar = function (s, list) { return list.length ? s / list.length : 0; };\n  var dBuy = P ? perBar(R.buy, recent) - perBar(P.buy, prior) : 0;\n  var dSell = P ? perBar(R.sell, recent) - perBar(P.sell, prior) : 0;\n  var scale = P ? Math.max(perBar(P.buy, prior) + perBar(P.sell, prior), 1) : 1;\n  // A side is only strengthening if it is doing MORE and taking MORE of the\n  // flow. Without the share test a volume surge would read as both sides\n  // strengthening at once, which tells the trader nothing.\n  var dShare = P ? R.buyPct - P.buyPct : 0;\n  var trendOf = function (d, shareDelta) {\n    var rel = d / scale;\n    if (rel >= 0.15 && shareDelta > -3) return 'מתחזקים';\n    if (rel <= -0.15 || shareDelta < -8) return 'נחלשים';\n    return 'ללא שינוי';\n  };\n  var buyersTrend = trendOf(dBuy, dShare), sellersTrend = trendOf(dSell, -dShare);\n\n  // Is the nearby level being defended or absorbed?\n  var T = c.tactical || tactical(A);\n  // Level condition comes from levelState(), which is anchored to the current\n  // price. Volume only adds the \"absorbed\" nuance on top of it.\n  var levelVerdict = function (level, isSupport) {\n    if (!level) return null;\n    var ls = levelState(A, level.price, isSupport);\n    if (!ls) return null;\n    var near = usable.filter(function (x) {\n      return x.low <= level.price + 0.3 * atr && x.high >= level.price - 0.3 * atr;\n    }).slice(-12);\n    var vol = near.length ? near.reduce(function (s, x) { return s + x.volume; }, 0) / near.length : 0;\n    var avgVol = usable.reduce(function (s, x) { return s + x.volume; }, 0) / usable.length;\n    var heavy = avgVol > 0 && vol / avgVol >= 1.2;\n    var parked = near.filter(function (x) { return Math.abs(x.close - level.price) <= 0.2 * atr; }).length;\n    var absorbed = heavy && parked >= 3 && (ls.state === 'testing' || ls.state === 'approaching');\n    return { price: level.price, state: ls.state, verdict: absorbed ? 'נבלעת' : ls.text,\n      distanceR: ls.distanceR, touches: near.length, heavy: heavy,\n      held: ls.recoveries, through: isSupport ? (ls.state === 'broken' ? 2 : 0) : (ls.state === 'broken' ? 2 : 0) };\n  };\n  var sup = levelVerdict(T.support, true), res = levelVerdict(T.resistance, false);\n\n  // Does the flow agree with the setup on the table?\n  var plan = c.plan || null;\n  var bullishSetup = plan && ['WAITING_FOR_ZONE', 'IN_ZONE', 'READY_PARTIAL', 'WAITING_FOR_CONFIRMATION', 'READY_ADD', 'ACTIVE', 'TAKE_PROFIT_AREA'].indexOf(plan.state) >= 0;\n  var side = buyPct >= 58 ? 'buyers' : buyPct <= 42 ? 'sellers' : 'balanced';\n  // Who is winning now and which way that is heading are two different facts.\n  // \"Buyers 64%\" plus \"buyers weakening\" is not support for a long — it is a\n  // lead that is being given back, and the wording has to say so.\n  // When BOTH sides are fading, the buyers' edge may be widening but their\n  // pressure is not strengthening. Saying it is contradicts the two trend\n  // labels printed directly above it.\n  // Who leads and how each side is moving are separate facts, and the sentence\n  // must be built from BOTH. Saying \"buyers strengthening\" because the SELLERS\n  // faded, while the buyers' own trend reads unchanged, contradicts the two\n  // labels printed directly above it.\n  var UP = 'מתחזקים', DOWN = 'נחלשים', SAME = 'ללא שינוי';\n  var leaderTrend = side === 'sellers' ? sellersTrend : buyersTrend;\n  var otherTrend = side === 'sellers' ? buyersTrend : sellersTrend;\n  // 'own'      the leading side is itself doing more\n  // 'relative' the leader is unchanged or fading, but the other side fades faster\n  // 'fading'   the leading side is giving its lead back\n  var direction = leaderTrend === UP ? 'own'\n    : leaderTrend === DOWN ? (otherTrend === DOWN ? 'relative' : 'fading')\n    : otherTrend === DOWN ? 'relative'\n    : otherTrend === UP ? 'fading' : 'steady';\n\n  var leadWord = side === 'sellers' ? 'המוכרים' : 'הקונים';\n  var conclusion;\n  if (side === 'balanced') {\n    conclusion = buyersTrend === UP && sellersTrend !== UP ? 'הכוחות שקולים, הקונים משתפרים'\n      : sellersTrend === UP && buyersTrend !== UP ? 'הכוחות שקולים, המוכרים משתפרים'\n      : 'הכוחות שקולים';\n  } else if (direction === 'own') {\n    conclusion = leadWord + ' מובילים והלחץ שלהם מתחזק';\n  } else if (direction === 'relative') {\n    // The leader is not stronger. The gap widened because the other side eased.\n    conclusion = leadWord + ' מובילים; היתרון היחסי שלהם משתפר משום ש'\n      + (side === 'sellers' ? 'הקונים' : 'המוכרים') + ' נחלשים'\n      + (leaderTrend === DOWN ? ', אך גם הלחץ שלהם עצמו נחלש' : '');\n  } else if (direction === 'fading') {\n    conclusion = leadWord + ' עדיין מובילים, אבל הלחץ שלהם נחלש';\n  } else {\n    conclusion = leadWord + ' מובילים, ללא שינוי בעוצמה';\n  }\n  var agreement = !bullishSetup ? 'לא רלוונטי'\n    : side === 'buyers' ? (direction === 'own' ? 'תומך'\n        : direction === 'fading' ? 'תומך אך נחלש'\n        : direction === 'relative' ? 'תומך במידה' : 'תומך')\n    : side === 'sellers' ? 'סותר'\n    : (buyersTrend === 'נחלשים' ? 'נוטה נגד' : 'ניטרלי');\n\n  // Net contribution to the up-side, in the same units the probability model uses.\n  var tilt = 0;\n  if (side === 'buyers') tilt += 1; else if (side === 'sellers') tilt -= 1;\n  if (buyersTrend === 'מתחזקים') tilt += 1;\n  if (buyersTrend === 'נחלשים') tilt -= 1;\n  if (sellersTrend === 'מתחזקים') tilt -= 1;\n  if (sellersTrend === 'נחלשים') tilt += 1;\n  if (sup && (sup.state === 'held' || sup.state === 'reclaimed')) tilt += 1;\n  if (sup && (sup.state === 'broken' || sup.verdict === 'נבלעת')) tilt -= 1;\n  if (res && res.state === 'broken') tilt += 1;\n  if (res && (res.state === 'rejected' || res.verdict === 'נבלעת')) tilt -= 1;\n\n  return {\n    source: 'candles',                       // never 'orderbook' — we have none\n    hasOrderBook: false, hasTape: false,\n    buyPct: buyPct, sellPct: sellPct,\n    side: side, buyersTrend: buyersTrend, sellersTrend: sellersTrend,\n    support: sup, resistance: res, agreement: agreement, tilt: tilt,\n    direction: direction, conclusion: conclusion,\n    window: win.length, recentWindow: recent.length,\n    summary: 'קונים ' + buyPct + '% · מוכרים ' + sellPct + '%',\n    trendSummary: 'קונים ' + buyersTrend + ' · מוכרים ' + sellersTrend\n  };\n}\n\n// ---------------------------------------------------------------- features\n// A compact description of \"situations like this one\", used both to bucket\n// history and to score the model fallback.\nfunction features(A, ctx, upper, lower) {\n  var S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var vx = (ctx && ctx.baseline) ? volxTod(b, ctx.baseline) : null;\n  if (vx == null) vx = b.volx;\n  var mom = momentum(A);\n  return {\n    structure: S.announced || S.trend,\n    vwap: b.aboveVwap ? 'above' : 'below',\n    ema: b.align,\n    momentum: mom.label,\n    vol: vx >= 1.5 ? 'high' : vx >= 0.8 ? 'normal' : 'low',\n    daily: ctx && ctx.daily ? ctx.daily.trend : 'NA',\n    dUp: Math.round(((upper - b.close) / atr) * 2) / 2,\n    dDown: Math.round(((b.close - lower) / atr) * 2) / 2,\n    tod: b.time < '10:30' ? 'open' : b.time < '14:30' ? 'mid' : 'late',\n    flow: (function () { var p = (ctx && ctx.pressure) || pressure(A, {}); return p ? p.side : 'NA'; })()\n  };\n}\nfunction bucketKey(f, coarse) {\n  return coarse\n    ? [f.structure, f.vwap, f.daily].join('|')\n    : [f.structure, f.vwap, f.ema, f.momentum, f.vol, f.daily, f.dUp, f.dDown].join('|');\n}\n\n// ---------------------------------------------------------------- calibration\n// Replays the loaded historical days and records, for each situation, whether\n// price rose by the required distance before falling by the other one, within\n// the horizon. These are real observed outcomes from the symbol's own history —\n// when there are too few of them the caller must not show a percentage.\nfunction calibrate(daysRows, opts) {\n  var o = opts || {}, horizon = o.horizon || 60, step = o.step || 2;\n  var table = {}, coarse = {}, total = 0;\n  (daysRows || []).forEach(function (rows) {\n    if (!rows || rows.length < 120) return;\n    var A = analyze(rows, o.engine || { K: 3 });\n    if (!A) return;\n    var bars = A.bars, n = bars.length;\n    for (var i = 60; i < n - horizon; i += step) {\n      var b = bars[i], atr = b.atr20 || b.avgRange || 1;\n      // sample a spread of target distances so the table can answer different asks\n      [0.5, 1, 1.5, 2].forEach(function (du) {\n        [0.5, 1, 1.5, 2].forEach(function (dd) {\n          var up = b.close + du * atr, dn = b.close - dd * atr, hit = null;\n          for (var k = i + 1; k <= i + horizon && k < n; k++) {\n            if (bars[k].high >= up) { hit = 'up'; break; }\n            if (bars[k].low <= dn) { hit = 'down'; break; }\n          }\n          if (!hit) return;\n          var sub = { state: A.states[i], bar: b };\n          var f = features({ state: A.states[i], bars: bars.slice(0, i + 1), swings: A.swings, states: A.states.slice(0, i + 1) },\n            { daily: o.daily, baseline: o.baseline }, up, dn);\n          [bucketKey(f, false), 'C:' + bucketKey(f, true) + '|' + f.dUp + '|' + f.dDown].forEach(function (key, idx) {\n            var t = idx === 0 ? table : coarse;\n            t[key] = t[key] || { n: 0, up: 0 };\n            t[key].n++; if (hit === 'up') t[key].up++;\n          });\n          total++;\n        });\n      });\n    }\n  });\n  return { table: table, coarse: coarse, total: total };\n}\n\n// ---------------------------------------------------------------- probability\n// Answers ONE defined question: does price reach `upper` before `lower`, within\n// `horizonMin` minutes. Empirical when the symbol's own history has enough\n// comparable cases; otherwise a transparent weighted model, reported as a bias\n// with a confidence score rather than a fake percentage.\n// Pull an estimate toward 50 in proportion to how much the model actually\n// knows. Full confidence (>=70) keeps the number as computed.\nfunction shrink(pct, confidence) {\n  var f = Math.max(0, Math.min(1, confidence / 70));\n  return Math.round(50 + (pct - 50) * f);\n}\n\n// How much of the session that SHOULD have happened by now do we actually hold?\n// A window that starts at 11:02 on a day that opened at 09:30 is not a session:\n// its open is not the open, its VWAP is not the VWAP, and its volume is a\n// fraction. Everything derived from the session is refused until this passes.\nfunction sessionCompleteness(rows, opts) {\n  var o = opts || {};\n  var out = { firstBar: null, lastBar: null, receivedMinutes: 0, expectedMinutes: null,\n    coveragePct: null, fromOpen: false, complete: false };\n  if (!rows || !rows.length) return out;\n  var reg = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n  if (!reg.length) return out;\n  var times = reg.map(function (r) { return r.time; }).sort();\n  out.firstBar = times[0];\n  out.lastBar = times[times.length - 1];\n  var seen = {}; reg.forEach(function (r) { seen[r.time] = 1; });\n  out.receivedMinutes = Object.keys(seen).length;\n  // Minutes that should exist between the open and the newest bar we hold.\n  var toMin = function (t) { var p = t.split(':'); return (+p[0]) * 60 + (+p[1]); };\n  out.expectedMinutes = Math.max(1, toMin(out.lastBar) - toMin('09:30') + 1);\n  out.coveragePct = Math.round(out.receivedMinutes / out.expectedMinutes * 100);\n  out.fromOpen = out.firstBar <= '09:35';\n  out.missingFromOpen = out.fromOpen ? 0 : toMin(out.firstBar) - toMin('09:30');\n  // Holes AFTER the first bar are a separate failure and were not reported at\n  // all: 49 bars scattered between 11:02 and 13:44 is not the same thing as a\n  // clean window starting late. Both numbers, always.\n  out.spanMinutes = toMin(out.lastBar) - toMin(out.firstBar) + 1;\n  out.missingInside = Math.max(0, out.spanMinutes - out.receivedMinutes);\n  out.missingTotal = out.missingFromOpen + out.missingInside;\n  out.contiguous = out.missingInside === 0;\n  // Both conditions: it must start at the open AND have most of the minutes in\n  // between. Either alone lets a hole through.\n  out.complete = out.fromOpen && out.coveragePct >= (o.minCoverage || 85);\n  return out;\n}\n\nfunction pathProbability(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = c.tactical || tactical(A);\n  var upper = c.upper != null ? c.upper : (T.resistance ? T.resistance.price : b.close + atr);\n  var lower = c.lower != null ? c.lower : (T.support ? T.support.price : b.close - atr);\n  var horizon = c.horizonMin || 60;\n  var f = features(A, c, upper, lower);\n  var out = { upper: upper, lower: lower, horizonMin: horizon, features: f, why: [] };\n\n  // A probability is only meaningful if the two levels are far enough apart to\n  // mean something for this instrument. Asking whether price reaches 146.63\n  // before 146.62 — one cent, a fraction of a normal minute's range — produces\n  // a confident-looking number about nothing.\n  // The question only means \"up or down\" when the two levels BRACKET the price.\n  // With both below it, \"reaches 187.34 before 187.27\" asks which small decline\n  // happens first — and reporting that as UP 78% reads as a claim the stock will\n  // rise. It also hands the model a free +6 for \"the upper level is closer\",\n  // which is pure geometry: three cents against ten.\n  var px = b.close;\n  out.price = px;\n  out.bracketed = lower < px && px < upper;\n  // \"Reaches 338.66 before 338.63\" with price at 338.61 asks whether the far\n  // level is touched before the near one on the SAME side, which cannot happen\n  // on a continuous path — 338.63 is passed on the way. The bracket test below\n  // catches it, but say plainly why rather than reporting it as a coincidence.\n  out.samSideOrdering = (upper > px && lower > px && lower < upper)\n    || (upper < px && lower < px && lower > upper);\n  if (!out.bracketed) {\n    out.up = null; out.down = null; out.confidence = 0;\n    out.notDirectional = true;\n    out.side = upper <= px ? 'both_below' : 'both_above';\n    if (out.samSideOrdering)\n      out.why.push('הרמות באותו צד והרחוקה נמצאת אחרי הקרובה — לא ניתן להגיע לאחת לפני השנייה בדרך רציפה');\n    out.why.push(upper <= px\n      ? 'שתי הרמות מתחת למחיר — השאלה אינה עלייה מול ירידה אלא איזו ירידה מגיעה קודם, ולכן לא מוצגת כהסתברות כיוון'\n      : 'שתי הרמות מעל המחיר — השאלה אינה עלייה מול ירידה, ולכן לא מוצגת כהסתברות כיוון');\n    return out;\n  }\n\n  var band = Math.abs(upper - lower);\n  var MIN_BAND_ATR = 0.25;\n  if (atr > 0 && band < MIN_BAND_ATR * atr) {\n    out.up = null; out.down = null; out.confidence = 0; out.meaningless = true;\n    out.band = band; out.minBand = MIN_BAND_ATR * atr;\n    out.why.push('הרמות קרובות מדי זו לזו (' + (Math.round(band * 100) / 100) +\n      ') ביחס לתנודה הרגילה — הסתברות כאן חסרת משמעות');\n    return out;\n  }\n\n  // 1. empirical\n  if (c.calibration) {\n    var exact = c.calibration.table[bucketKey(f, false)];\n    var coarse = c.calibration.coarse['C:' + bucketKey(f, true) + '|' + f.dUp + '|' + f.dDown];\n    var hit = (exact && exact.n >= 30) ? exact : (coarse && coarse.n >= 30) ? coarse : null;\n    if (hit) {\n      out.source = 'empirical'; out.n = hit.n;\n      out.up = Math.round(hit.up / hit.n * 100);\n      out.confidence = Math.min(90, 40 + Math.round(Math.min(hit.n, 400) / 8));\n      out.raw = out.up;\n      out.up = shrink(out.raw, out.confidence);\n      out.down = 100 - out.up;\n      out.lowConfidence = out.confidence < 50;\n      out.why.push(hit.n + ' מקרים דומים בהיסטוריה של הנייר');\n      return out;\n    }\n    out.sampled = (exact ? exact.n : 0) + (coarse ? coarse.n : 0);\n  }\n\n  // 2. transparent model. Each layer moves the odds; none of it is invented,\n  // and the result is labelled as a model bias, not an observed frequency.\n  var score = 0, add = function (pts, txt) { if (!pts) return; score += pts; out.why.push((pts > 0 ? '+' : '') + pts + ' ' + txt); };\n  add(f.structure === 'UP' ? 2 : f.structure === 'DOWN' ? -2 : 0, 'מבנה תוך-יומי ' + f.structure);\n  add(f.daily === 'UP' ? 2 : f.daily === 'DOWN' ? -2 : 0, 'מגמה רב-יומית ' + f.daily);\n  add(f.vwap === 'above' ? 1 : -1, 'מחיר ' + (f.vwap === 'above' ? 'מעל' : 'מתחת') + ' VWAP');\n  add(f.ema === 'bull' ? 1 : f.ema === 'bear' ? -1 : 0, 'EMA ' + f.ema);\n  add(f.momentum === 'PUSHING' || f.momentum === 'RECOVERY' ? 1 : f.momentum === 'SELLING' ? -1 : 0, 'מומנטום ' + f.momentum);\n  add(c.market === 'Bullish' ? 1 : c.market === 'Bearish' ? -1 : 0, 'שוק ' + (c.market || 'לא ידוע'));\n  var pres = c.pressure || pressure(A, { tactical: T });\n  if (pres) {\n    out.pressure = pres;\n    add(Math.max(-3, Math.min(3, pres.tilt)), 'לחץ קונים/מוכרים ' + pres.buyPct + '/' + pres.sellPct);\n  }\n  // Distance asymmetry: the nearer level is simply likelier to be reached.\n  var dU = (upper - b.close) / atr, dD = (b.close - lower) / atr;\n  var geom = dU + dD > 0 ? (dD - dU) / (dU + dD) : 0;\n  add(Math.round(geom * 3), 'הרמה ' + (geom > 0 ? 'העליונה' : 'התחתונה') + ' קרובה יותר');\n  var pct = Math.round(100 / (1 + Math.exp(-score / 3.2)));\n  out.source = 'model';\n  out.confidence = Math.max(20, Math.min(55, 25 + Math.abs(score) * 3));\n  out.raw = Math.max(10, Math.min(90, pct));\n  // A number like 87% next to \"confidence 43\" reads as certainty the model does\n  // not have. The displayed probability is pulled toward 50 in proportion to\n  // the confidence, so the two can never tell different stories.\n  out.up = shrink(out.raw, out.confidence);\n  out.down = 100 - out.up;\n  out.lowConfidence = out.confidence < 50;\n  out.bias = out.up >= 60 ? 'נטייה שורית' : out.up <= 40 ? 'נטייה דובית' : 'ללא נטייה ברורה';\n  return out;\n}\n\n// ---------------------------------------------------------------- what now\n// Plain language. No HH/HL/VWAP/R in the user-facing strings.\nvar ACTIONS = {\n  DO_NOT_BUY: 'לא לקנות',\n  START_WATCHING: 'להתחיל לעקוב',\n  WAIT_FOR_CONFIRMATION: 'לחכות לאישור',\n  ENTRY_AVAILABLE: 'אפשר להיכנס',\n  HOLD: 'להחזיק',\n  TAKE_PARTIAL: 'לממש חלק',\n  MOVE_STOP: 'להעלות סטופ',\n  EXIT: 'לצאת',\n  SETUP_CANCELLED: 'ה-setup בוטל',\n  SESSION_CLOSED: 'המסחר הסתיים',\n  DATA_STALE: 'נתונים לא עדכניים — לא לסחור',\n  WATCH_ONLY: 'לא לסחור — רק לעקוב',\n  DO_NOT_CHASE: 'לא לרדוף — להמתין לפולבק'\n};\nvar PLAN_TO_ACTION = {\n  NO_SETUP: 'DO_NOT_BUY',\n  WAITING_FOR_ZONE: 'START_WATCHING',\n  IN_ZONE: 'WAIT_FOR_CONFIRMATION',\n  READY_PARTIAL: 'ENTRY_AVAILABLE',\n  WAITING_FOR_CONFIRMATION: 'WAIT_FOR_CONFIRMATION',\n  READY_ADD: 'ENTRY_AVAILABLE',\n  ACTIVE: 'HOLD',\n  TAKE_PROFIT_AREA: 'TAKE_PARTIAL',\n  DO_NOT_CHASE: 'DO_NOT_CHASE',\n  FAILED: 'SETUP_CANCELLED'\n};\nfunction n2(x) { return x == null ? null : (Math.round(x * 100) / 100).toFixed(2); }\nfunction scenarioLabelFor(P, watch) {\n  if (!P || !P.kind) return 'ללא setup';\n  if (P.kind === 'breakout') return 'פריצה מעל ' + n2(watch);\n  return P.zone ? 'חזרה לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) : 'פולבק';\n}\n\nfunction whatNow(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = c.tactical || tactical(A);\n  var P = c.plan || executionPlan(A, { label: c.market || 'Neutral' });\n  var ended = c.sessionEnded;\n\n  var watch = T.resistance ? T.resistance.price : (P && P.zone ? P.zone[1] : b.dayHigh);\n  var hasZone = !!(P && P.zone && P.entry != null && P.addAbove != null);\n  var lower = T.support ? T.support.price : (P && P.invalidation != null ? P.invalidation : b.dayLow);\n  var prob = c.probability || pathProbability(A, { tactical: T, upper: watch, lower: lower,\n    horizonMin: 60, daily: c.daily, baseline: c.baseline, calibration: c.calibration, market: c.market });\n\n  // ---- one active scenario only. The plan already picked pullback OR\n  // breakout; whatNow must never narrate the other one as a live instruction.\n  var scenario = !P || !P.kind ? { kind: 'none', label: 'אין setup פעיל' }\n    : P.kind === 'breakout' ? { kind: 'breakout', label: 'פריצה מעל ' + n2(watch) }\n    : { kind: 'pullback', label: 'חזרה לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) };\n\n  // ---- no-edge gate. When the model's own numbers say there is nothing here,\n  // it must not offer an entry \"if X happens\".\n  var edge = c.edge || null;\n  var noEdge = !!(edge && edge.noEdge);\n  var stale = !!c.stale;\n  // The model's own cancellation rule, applied to the model's own price. If the\n  // last close is already through the level that voids the idea, the idea is\n  // void — it cannot still read START_WATCHING.\n  var cancelAt = (c.levels && c.levels.tacticalStop != null) ? c.levels.tacticalStop\n    : (P && P.invalidation != null ? P.invalidation : null);\n  var alreadyCancelled = !ended && cancelAt != null && b.close != null && b.close < cancelAt;\n  // Staleness outranks the plan. A setup derived from bars that stopped hours\n  // ago is a description of a market that has already moved.\n  var actionKey = ended ? 'SESSION_CLOSED'\n    : stale ? 'DATA_STALE'\n    : alreadyCancelled ? 'SETUP_CANCELLED'\n    : noEdge ? 'WATCH_ONLY'\n    : (PLAN_TO_ACTION[P ? P.state : 'NO_SETUP'] || 'DO_NOT_BUY');\n  var W = { action: actionKey, actionText: ACTIONS[actionKey], sessionEnded: !!ended,\n    stale: stale, cancelled: alreadyCancelled, price: b.close, watch: watch,\n    // A probability computed from stale bars is not a probability about now.\n    // A probability is a claim about the next hour. After the close there is no\n    // next hour, and on stale bars it is a claim about a market that has moved.\n    probability: (stale || ended || (prob && (prob.meaningless || prob.notDirectional))) ? null : prob,\n    probabilityRaw: prob || null,\n    plan: P, scenario: (stale || alreadyCancelled) ? { kind: 'none', label: 'אין setup פעיל' } : scenario,\n    noEdge: noEdge, edge: edge, up: [], down: [], why: [] };\n\n  // The one line that matters\n  if (stale) W.next = 'הנרות האחרונים ישנים — אין כאן איתות חי. הרמות למטה הן לעיון בלבד.';\n  else if (alreadyCancelled) W.next = 'המחיר כבר מתחת ל-' + n2(cancelAt) + ' — התרחיש בוטל. לחכות למבנה חדש.';\n  else if (ended) W.next = 'המסחר הסתיים — הרמות למטה הן לקראת המסחר הבא, לא להוראה עכשיו';\n  else if (noEdge) W.next = 'אין כרגע יתרון מספיק לכניסה. לעקוב אם המחיר מגיע ל-' + n2(watch) + '.';\n  else if (P && P.state === 'FAILED') W.next = 'אין כניסה. צריך מצב חדש לפני שמסתכלים שוב.';\n  else if (P && (P.state === 'READY_PARTIAL' || P.state === 'READY_ADD')) W.next = 'המחיר במקום שתכננו — ' + P.headline;\n  else W.next = 'לשים לב אם המחיר מגיע ל-' + n2(watch);\n\n  // If it goes up\n  var above = T.above.filter(function (l) { return l.price > b.close + 0.05 * atr; });\n  // The narrative must quote the SAME targets the levels block shows. Deriving\n  // them separately here is how the text came to say 'second target 146.66'\n  // while the levels said 146.73.\n  var canon = c.levels || null;\n  var t1 = (canon && canon.target1 != null) ? canon.target1\n    : (above[1] ? above[1].price : watch + 1.2 * atr);\n  var t2 = (canon && canon.target2 != null) ? canon.target2\n    : (above[2] && above[2].price >= t1 + 0.2 * atr ? above[2].price : t1 + 1.5 * atr);\n  // Two targets that print as the same number are not two targets. On quiet or\n  // low-priced instruments the rounded levels collided.\n  // Compare what will actually be PRINTED. Two levels 0.004 apart are distinct\n  // numbers that render as the same string, which is what the reader sees.\n  if (t1 != null && t2 != null && n2(t1) === n2(t2)) t2 = t1 + Math.max(0.01, 1.0 * atr);\n  if (stale) {\n    // Reference only. The last idea is described in the past tense, never as\n    // something to act on.\n    W.up.push('התרחיש האחרון שנרשם: ' + scenarioLabelFor(P, watch) + ' — לעיון בלבד');\n    W.up.push('אין הוראת כניסה על נתונים ישנים');\n  } else if (noEdge && !ended) {\n    // What would have to change before any entry is even discussed.\n    W.up.push('אם המחיר עולה מעל ' + n2(watch) + ' ונשאר שם — ייבחן מחדש');\n    W.up.push('כרגע אין הוראת כניסה');\n  } else if (!ended) {\n    if (P && P.state === 'READY_PARTIAL' && hasZone) {\n      W.up.push('אם המחיר נשאר מעל ' + n2(P.zone[0]) + ' — אפשר להיכנס בחלק מהסכום סביב ' + n2(P.entry));\n      W.up.push('אישור חזק יותר מעל ' + n2(P.addAbove) + ' (המערכת אינה יודעת אם יש פוזיציה)');\n    } else if (scenario.kind === 'breakout' && P.entry != null) {\n      W.up.push('אם עובר את ' + n2(watch) + ' ונשאר מעל — אפשר להיכנס סביב ' + n2(P.entry));\n    } else if (scenario.kind === 'pullback' && hasZone) {\n      W.up.push('אם המחיר יורד לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) + ' ונבלם — כניסה חלקית ב-' + n2(P.entry));\n      W.up.push('אישור חזק יותר מעל ' + n2(P.addAbove));\n    } else {\n      W.up.push('אם עולה מעל ' + n2(watch) + ' ונשאר שם — להתחיל לעקוב מקרוב');\n    }\n    if (!noEdge) { W.up.push('יעד ראשון ' + n2(t1) + ' — שם לממש חלק'); W.up.push('יעד שני ' + n2(t2)); }\n  } else {\n    W.up.push('הרמה החשובה למעלה הייתה ' + n2(watch));\n  }\n\n  // If it goes down\n  var below = T.below.filter(function (l) { return l.price < b.close - 0.05 * atr; });\n  var s1 = T.support ? T.support.price : (below[0] ? below[0].price : b.close - atr);\n  var s2 = below.find(function (l) { return l.price < s1 - 0.1 * atr; });\n  var s3 = P && P.invalidation != null ? P.invalidation : (s2 ? s2.price - 0.5 * atr : s1 - atr);\n  if (stale) {\n    // The DOWN block describes the downside, in the past tense. It must not\n    // repeat the UP block's lines — that duplication reached the screen.\n    W.down.push('התמיכה האחרונה שנרשמה: ' + n2(s1) + ' — לעיון בלבד');\n    W.down.push('אין רמת ביטול פעילה על נתונים ישנים');\n  } else if (noEdge && !ended) {\n    W.down.push('אם יורד ל-' + n2(s1) + ' — לבדוק אם קונים נכנסים שם');\n    W.down.push('מתחת ' + n2(s3) + ' — התרחיש החיובי יורד מהפרק');\n  } else if (!ended) {\n    W.down.push('אם יורד ל-' + n2(s1) + ' — לבדוק אם יש קונים שעוצרים את הירידה');\n    if (s2) W.down.push('אם שובר את ' + n2(s2.price) + ' — התרחיש החיובי נחלש');\n    W.down.push('אם יורד מתחת ' + n2(s3) + ' — לבטל, לא להיכנס');\n  } else {\n    W.down.push('התמיכה החשובה הייתה ' + n2(s1));\n  }\n\n  // Why, in plain words\n  var d = c.daily;\n  if (d) W.why.push('מגמה של כמה ימים: ' + (d.trend === 'UP' ? 'עולה' : d.trend === 'DOWN' ? 'יורדת' : 'ללא כיוון'));\n  W.why.push('היום: ' + (S.trend === 'UP' ? 'עולה' : S.trend === 'DOWN' ? 'יורד' : 'ללא כיוון'));\n  var mom = momentum(A);\n  W.why.push('בדקות האחרונות: ' + ({ PUSHING: 'לוחץ למעלה', PULLBACK: 'נסוג', RECOVERY: 'מתאושש', SELLING: 'נמכר', FLAT: 'שקט' }[mom.label]));\n  W.why.push(b.aboveVwap ? 'מעל ממוצע היום' : 'מתחת לממוצע היום');\n  var vx = c.baseline ? volxTod(b, c.baseline) : null;\n  if (vx != null) W.why.push('מחזור מול אותה דקה בימים קודמים: ' + vx.toFixed(1) + '×');\n  var pres = c.pressure || pressure(A, { tactical: T, plan: P });\n  if (pres) {\n    W.pressure = pres;\n    W.why.push('בדקות האחרונות ' + (pres.side === 'buyers' ? 'הקונים חזקים יותר' : pres.side === 'sellers' ? 'המוכרים חזקים יותר' : 'הכוחות שקולים')\n      + ' (' + pres.buyPct + '/' + pres.sellPct + ')');\n    if (pres.support && pres.support.state !== 'far')\n      W.why.push('התמיכה ב-' + n2(pres.support.price) + ' ' + pres.support.verdict);\n    if (pres.resistance && pres.resistance.state !== 'far')\n      W.why.push('ההתנגדות ב-' + n2(pres.resistance.price) + ' ' + pres.resistance.verdict);\n  }\n  // Identical lines are never informative twice, whichever branch produced them.\n  var uniq = function (list) { var seen = {}; return list.filter(function (x) {\n    if (seen[x]) return false; seen[x] = 1; return true; }); };\n  W.up = uniq(W.up); W.down = uniq(W.down); W.why = uniq(W.why);\n  return W;\n}\n\n\n// ---------------------------------------------------------------- ticker state\n//\n// ONE snapshot per symbol per refresh. Every UI surface — the radar row and the\n// detail sheet — renders from this object, so a score can never differ between\n// them. Nothing downstream recalculates.\n//\n// Level roles are explicit and each level has exactly one job:\n//   watch                 the price we are waiting for\n//   entry                 where a position would actually be opened\n//   target1 / target2     profit areas, always above entry for a long\n//   tacticalInvalidation  cancels the immediate entry idea\n//   hardStop              structure is broken; the whole idea is off\n//   probUpper / probLower the two boundaries the probability question uses\nvar STATE_VERSION = 4;\n\nfunction buildTickerState(symbol, A, ctx) {\n  var c = ctx || {};\n  var now = c.now || Date.now();\n  if (!A || !A.state) {\n    return { symbol: symbol, state_version: STATE_VERSION, calculated_at: now, data_through: null,\n      valid: false, violations: [{ code: 'NO_DATA', severity: 'block', text: 'אין נתונים' }],\n      status: 'NO DATA', action: 'DO_NOT_BUY', actionText: ACTIONS.DO_NOT_BUY };\n    }\n  var S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = tactical(A);\n  var plan = executionPlan(A, { label: c.market || 'Neutral' });\n  var pres = pressure(A, { tactical: T, plan: plan });\n\n  // --- levels, derived once\n  var lv = { watch: null, entry: null, target1: null, target2: null,\n    tacticalInvalidation: null, hardStop: null, probUpper: null, probLower: null };\n  if (plan && plan.kind) {\n    lv.watch = plan.kind === 'breakout' ? plan.zone[0] : plan.zone[1];\n    lv.entry = plan.entry;\n    lv.tacticalInvalidation = plan.kind === 'breakout' ? plan.zone[0] - 0.2 * atr : plan.zone[0];\n    lv.hardStop = plan.invalidation;\n  } else {\n    lv.watch = T.resistance ? T.resistance.price : b.dayHigh;\n    lv.tacticalInvalidation = T.support ? T.support.price : b.dayLow;\n    lv.hardStop = lv.tacticalInvalidation - 0.5 * atr;\n  }\n  // A long is cancelled on the way DOWN, so the level hit first must be the\n  // entry cancellation and the structural one must sit BELOW it. Reported the\n  // other way round — entry at 187.27, structure at 187.28 — the whole setup\n  // died before its own entry rule could ever fire.\n  if (lv.tacticalInvalidation != null && lv.hardStop != null && lv.hardStop >= lv.tacticalInvalidation) {\n    lv.hardStop = lv.tacticalInvalidation - Math.max(0.02, 0.3 * atr);\n    lv.hardStopAdjusted = true;\n  }\n  // Targets are always resolved ABOVE the entry. A level at or below the entry\n  // can be the watch level or resistance, but never target 1.\n  // With no entry planned, targets are measured from the WATCH level, not from\n  // the current price — otherwise the first level above price is both the thing\n  // we are waiting for and the first target, which is meaningless.\n  // The add trigger and target 1 cannot be the same price. One says \"commit\n  // more here\", the other says \"take some off here\", and printing both against\n  // 187.47 is an instruction to do two opposite things at once.\n  var addAt = plan && plan.addAbove != null ? plan.addAbove : null;\n  var anchor = lv.entry != null ? lv.entry : (lv.watch != null ? lv.watch : b.close);\n  var aboveEntry = T.above.filter(function (x) { return x.price >= anchor + 0.3 * atr; })\n    .sort(function (x, y) { return x.price - y.price; });\n  lv.target1 = aboveEntry[0] ? aboveEntry[0].price : anchor + 1.0 * atr;\n  // A target that lands on the add trigger is pushed to the next level above it,\n  // or one ATR beyond — the trigger keeps its role, the target moves.\n  if (addAt != null && Math.abs(lv.target1 - addAt) < 0.005) {\n    var beyond = aboveEntry.filter(function (x) { return x.price > addAt + 0.005; });\n    lv.target1 = beyond[0] ? beyond[0].price : addAt + Math.max(0.02, 0.5 * atr);\n    lv.target1MovedFromAdd = true;\n  }\n  lv.target2 = aboveEntry[1] && aboveEntry[1].price >= lv.target1 + 0.3 * atr ? aboveEntry[1].price : lv.target1 + 1.0 * atr;\n  lv.probUpper = lv.watch;\n  lv.probLower = lv.tacticalInvalidation;\n\n  var prob = pathProbability(A, { tactical: T, upper: lv.probUpper, lower: lv.probLower, horizonMin: 60,\n    daily: c.daily, baseline: c.baseline, calibration: c.calibration, market: c.market, pressure: pres });\n\n  // ---- edge check. The recommendation has to agree with the model's own\n  // numbers: a low score, a coin-flip probability, low confidence and balanced\n  // flow together mean there is nothing to act on, whatever the plan says.\n  var row0 = radarRow(symbol, A, c.marketCtx || { label: c.market || 'Neutral' }, c.freshness);\n  var score0 = row0.bl ? row0.bl.confidence : 0;\n  var weak = [];\n  if (score0 <= 2) weak.push('ציון setup נמוך');\n  if (prob && prob.meaningless) weak.push('הרמות קרובות מדי למדידה');\n  if (prob && prob.notDirectional) weak.push('הרמות אינן מקיפות את המחיר — אין הסתברות כיוון');\n  if (prob && prob.up != null && Math.abs(prob.up - 50) <= 8) weak.push('הסיכויים שקולים');\n  if (prob && prob.confidence < 50) weak.push('ביטחון נמוך');\n  if (pres && pres.side === 'balanced') weak.push('קונים ומוכרים מאוזנים');\n  var structureFlat = (row0.structure === 'RANGE') && (row0.momentum === 'FLAT');\n  if (structureFlat) weak.push('אין מבנה ואין מומנטום');\n  var edge = { noEdge: weak.length >= 4, reasons: weak, score: score0,\n    probUp: prob.up, confidence: prob.confidence, flow: pres ? pres.side : 'NA' };\n\n  // ---- staleness is a hard gate, not a label.\n  // Bars that stopped arriving hours ago describe a market that has moved on.\n  // Showing an entry, a probability or a buyer/seller split from them reads as\n  // live guidance about a price that no longer exists, so none of it survives.\n  // On a one-minute feed, four minutes of lag is long enough for price to have\n  // left the entry zone entirely. An entry offered on data that old is an\n  // instruction to trade a price that may no longer exist.\n  var ENTRY_MAX_AGE = 180;                       // three minutes\n  var STALE_SECONDS = 300;                       // five minutes of missing bars\n  var ageSec = (c.staleSeconds != null) ? c.staleSeconds\n    : (b.unix != null && c.nowUnix != null ? c.nowUnix - b.unix : null);\n  var isStale = c.sessionEnded ? false\n    : (c.freshness === 'STALE' || (ageSec != null && ageSec > STALE_SECONDS));\n  // Not stale enough to freeze the whole state, but too old to act on.\n  var tooOldToEnter = !c.sessionEnded && ageSec != null && ageSec > ENTRY_MAX_AGE;\n\n  // And the session itself must be present. A window that starts at 11:02 on a\n  // day that opened at 09:30 has no session open, no session VWAP and a\n  // fraction of the volume — every figure derived from it is wrong while\n  // looking entirely clean.\n  // Measure the rows that were LOADED, not the ones analysis kept. A no-trade\n  // minute is deliberately excluded from the analysis and is still a minute we\n  // hold — counting the filtered set turned a complete session into \"186 of 335\n  // with 57 holes\".\n  var cover = sessionCompleteness((A && (A.rawBars || A.bars)) || [], {});\n  var sessionIncomplete = !c.sessionEnded && !isStale && A && A.bars && A.bars.length > 0\n    && !cover.complete;\n\n  var W = whatNow(A, { tactical: T, plan: plan, market: c.market, daily: c.daily,\n    baseline: c.baseline, calibration: c.calibration, pressure: pres, probability: prob,\n    sessionEnded: c.sessionEnded, levels: lv, edge: edge, stale: isStale });\n\n  // Nothing actionable may remain on the state when there is no edge, so the\n  // technical block underneath cannot contradict the headline.\n  if (edge.noEdge && !c.sessionEnded) {\n    lv.entry = null; lv.target1 = null; lv.target2 = null;\n  }\n  // A closed session is a reference view, not an order. The levels stay visible\n  // as context, but nothing actionable survives — otherwise the headline says\n  // the market is shut while an entry price sits underneath it.\n  if (c.sessionEnded) { lv.entry = null; lv.target1 = null; lv.target2 = null; }\n  if (isStale) { lv.entry = null; lv.target1 = null; lv.target2 = null; }\n  // Too old to act on, or built from part of a session: the levels stay as\n  // context but no entry is offered, and the headline says which it is.\n  // Staleness is the stronger statement and must not be overwritten by the\n  // weaker one: data that is five minutes old is not merely \"delayed\".\n  if (!isStale && !c.sessionEnded && (tooOldToEnter || sessionIncomplete)) {\n    lv.entry = null; lv.target1 = null; lv.target2 = null;\n    W.action = 'WAIT';\n    W.actionText = sessionIncomplete\n      ? 'סשן חלקי — לא לסחור'\n      : 'נתונים באיחור — להמתין לנר טרי';\n    W.next = sessionIncomplete\n      ? ('נטענו ' + cover.receivedMinutes + ' דקות מתוך ' + cover.expectedMinutes\n         + ' — חסרות ' + cover.missingFromOpen + ' מהפתיחה ועוד ' + cover.missingInside\n         + ' בתוך החלון, אז הפתיחה, ה-VWAP, המבנה והמחזור אינם של הסשן')\n      : ('הנר האחרון בן ' + Math.round(ageSec) + ' שניות; בפיד של דקה זה מספיק כדי שהמחיר יעזוב את האזור');\n    // The WHAT-IF lines are built before this gate and still carried \"you can\n    // enter around 338.68\" with targets underneath a headline that says not to\n    // trade. Rewrite them as observations: the levels are worth naming, the\n    // instruction is not.\n    var obs = function (list) {\n      return (list || []).map(function (x) {\n        return String(x)\n          .replace(/אפשר להיכנס/g, 'רמה לצפייה')\n          .replace(/כניסה חלקית/g, 'אזור לצפייה')\n          .replace(/לממש/g, 'רמת יעד לשעבר')\n          .replace(/להוסיף/g, 'רמת אישור');\n      }).filter(function (x) { return !/להיכנס|לקנות|לממש חלק/.test(x); });\n    };\n    W.up = obs(W.up); W.down = obs(W.down);\n    W.up.unshift(sessionIncomplete\n      ? 'הרמות מוצגות לצפייה בלבד — הסדרה חסרה ' + cover.missingTotal + ' דקות'\n      : 'הרמות מוצגות לצפייה בלבד — הנתונים אינם טריים מספיק לפעולה');\n  }\n  var row = row0;\n\n  var st = {\n    symbol: symbol, state_version: STATE_VERSION, calculated_at: now,\n    data_through: { time: b.time, date: b.date || c.date || null, unix: b.unix != null ? b.unix : null },\n    price: b.close, atr: atr,\n    status: row.status, score: row.bl ? row.bl.confidence : 0,\n    action: W.action, actionText: W.actionText,\n    structure: row.structure, momentum: row.momentum,\n    coverage: cover, sessionIncomplete: sessionIncomplete, tooOldToEnter: tooOldToEnter,\n    levels: lv, plan: plan, tactical: T, pressure: pres, probability: W.probability,\n    probabilityRaw: W.probabilityRaw || null, whatNow: W,\n    scenario: W.scenario, edge: edge, noEdge: edge.noEdge,\n    levelStates: { support: T.support ? levelState(A, T.support.price, true) : null,\n                   resistance: T.resistance ? levelState(A, T.resistance.price, false) : null },\n    row: row, sessionEnded: !!c.sessionEnded, freshness: c.freshness || 'LIVE',\n    stale: isStale, data_age_seconds: ageSec\n  };\n  // Keep the row pointing at the same snapshot so nothing can drift.\n  row.score = st.score; row.state = st; row.pressure = pres;\n  if (edge.noEdge && !c.sessionEnded) {\n    row.status = row.status === 'AVOID' ? 'AVOID' : 'WATCH';\n    row.why = 'אין יתרון — רק מעקב';\n    st.status = row.status; st.action = 'WATCH_ONLY'; st.actionText = ACTIONS.WATCH_ONLY;\n  }\n  // The row is what the board shows, so it must carry the same verdict.\n  if (isStale) {\n    row.status = 'AVOID';\n    row.why = 'נתונים לא עדכניים — לא לסחור';\n    st.status = 'AVOID';\n    st.action = 'DATA_STALE'; st.actionText = ACTIONS.DATA_STALE;\n    st.probability = null;\n    st.pressure = pres ? Object.assign({}, pres, { current: false }) : pres;\n  }\n  var v = validateState(st);\n  st.violations = v.violations; st.valid = v.valid;\n  if (!v.valid) {\n    st.status = 'AVOID';\n    st.action = 'DO_NOT_BUY';\n    st.actionText = 'המתן — מצב המודל לא עקבי';\n    st.whatNow.actionText = st.actionText;\n    st.whatNow.next = 'הנתונים סותרים את עצמם, לכן לא מוצגת הוראת מסחר. יחושב מחדש בעדכון הבא.';\n    st.whatNow.up = []; st.whatNow.down = [];\n    // Nothing actionable may survive a blocked state.\n    st.levels = Object.assign({}, st.levels, { entry: null, target1: null, target2: null });\n  }\n  return st;\n}\n\n// ---------------------------------------------------------------- validation\n// Runs before anything is shown. A blocking violation replaces the trade\n// instruction with an explicit \"state inconsistent\" message rather than\n// letting a wrong instruction reach the screen.\nfunction validateState(st) {\n  var v = [], add = function (code, severity, text) { v.push({ code: code, severity: severity, text: text }); };\n  var lv = st.levels, p = st.price, plan = st.plan, atr = st.atr;\n  var actionable = ['ENTRY_AVAILABLE', 'HOLD', 'TAKE_PARTIAL'].indexOf(st.action) >= 0\n    || ['READY', 'ACTIVE'].indexOf(st.status) >= 0;\n\n  if (lv.entry != null) {\n    if (lv.target1 != null && lv.target1 <= lv.entry) add('TARGET_BELOW_ENTRY', 'block', 'יעד ראשון ' + n2(lv.target1) + ' אינו מעל הכניסה ' + n2(lv.entry));\n    if (lv.target2 != null && lv.target1 != null && lv.target2 <= lv.target1) add('TARGET2_BELOW_TARGET1', 'block', 'יעד שני אינו מעל יעד ראשון');\n    if (lv.hardStop != null && lv.hardStop >= lv.entry) add('STOP_ABOVE_ENTRY', 'block', 'הסטופ ' + n2(lv.hardStop) + ' אינו מתחת לכניסה ' + n2(lv.entry));\n    if (lv.tacticalInvalidation != null && lv.hardStop != null && lv.hardStop > lv.tacticalInvalidation)\n      add('STOP_ORDER', 'warn', 'הסטופ המבני אינו מתחת לביטול הטקטי');\n    // an entry the price has already left behind must not read as available\n    if (actionable && p > lv.entry + 0.6 * atr) add('ENTRY_BEHIND_PRICE', 'block', 'המחיר כבר רחוק מעל אזור הכניסה');\n  }\n  if (lv.probUpper != null && lv.probLower != null && lv.probUpper <= lv.probLower)\n    add('PROB_BOUNDS', 'block', 'גבולות ההסתברות אינם בסדר הנכון');\n  if (lv.watch != null && lv.target1 != null && Math.abs(lv.watch - lv.target1) < 1e-9)\n    add('LEVEL_DOUBLE_ROLE', 'block', 'אותה רמה משמשת גם כרמת מעקב וגם כיעד');\n\n  // descriptive text must agree with where price is\n  var pf = st.pressure;\n  if (pf && pf.support && pf.support.verdict === 'נשברת' && p > pf.support.price + 0.05 * st.atr)\n    add('TEXT_CONTRADICTS_PRICE', 'block', 'נטען שהתמיכה נשברת בזמן שהמחיר מעליה');\n  if (pf && pf.resistance && pf.resistance.verdict === 'נפרצת' && p < pf.resistance.price - 0.05 * st.atr)\n    add('TEXT_CONTRADICTS_PRICE', 'block', 'נטען שההתנגדות נפרצת בזמן שהמחיר מתחתיה');\n\n  // the headline and the technical block must describe the same situation\n  var passive = ['DO_NOT_BUY', 'WATCH_ONLY', 'SESSION_CLOSED', 'SETUP_CANCELLED'].indexOf(st.action) >= 0;\n  if (passive && lv.entry != null) add('ACTION_VS_ENTRY', 'block', 'ההוראה היא להמתין בזמן שמוצגת רמת כניסה');\n  if (passive && st.whatNow && st.whatNow.up.some(function (s) { return /אפשר להיכנס|כניסה חלקית ב-/.test(s); }))\n    add('ACTION_VS_INSTRUCTION', 'block', 'ההוראה היא להמתין בזמן שמוצגת הוראת כניסה');\n  if (st.noEdge && lv.target1 != null) add('NO_EDGE_TARGETS', 'block', 'אין יתרון אבל מוצגים יעדים');\n  // exactly one entry scenario may be narrated at a time\n  if (st.whatNow) {\n    var entryLines = st.whatNow.up.filter(function (s) { return /אפשר להיכנס|כניסה חלקית ב-/.test(s); });\n    var mentionsBoth = entryLines.some(function (s) { return /עובר את/.test(s); })\n      && entryLines.some(function (s) { return /יורד לאזור/.test(s); });\n    if (mentionsBoth) add('TWO_SCENARIOS', 'block', 'מוצגים שני תרחישי כניסה במקביל');\n  }\n  // a level description must match its current state\n  if (st.levelStates && st.levelStates.support && st.pressure && st.pressure.support) {\n    if (st.pressure.support.verdict === 'נשברה' && st.levelStates.support.state !== 'broken')\n      add('LEVEL_TEXT_STALE', 'block', 'תיאור התמיכה אינו תואם את מצבה הנוכחי');\n  }\n\n  // status vs plan\n  if (st.status === 'ACTIVE' && plan && plan.state === 'FAILED') add('STATUS_VS_PLAN', 'block', 'הסטטוס פעיל בזמן שה-setup בוטל');\n  if (st.status === 'READY' && st.action === 'DO_NOT_BUY') add('ACTION_VS_STATUS', 'block', 'סטטוס מוכן מול הוראה לא לקנות');\n\n  // pressure conclusion vs its own numbers\n  if (pf && pf.agreement === 'תומך' && pf.direction === 'deteriorating')\n    add('PRESSURE_CONCLUSION', 'block', 'נטען שהלחץ תומך בזמן שהוא נחלש');\n  if (pf && pf.agreement === 'תומך' && pf.buyPct < 50)\n    add('PRESSURE_CONCLUSION', 'block', 'נטען שהקונים תומכים בזמן שהם במיעוט');\n\n  // probability vs confidence\n  var pr = st.probability;\n  // `pr.up` is null when the probability was suppressed, and `null <= 20` is\n  // true in JavaScript — which made a *withheld* probability look like an\n  // extreme one and invalidated the whole state.\n  if (pr && pr.up != null && pr.confidence < 50 && (pr.up >= 80 || pr.up <= 20))\n    add('PROB_CONFIDENCE', 'block', 'הסתברות קיצונית עם ביטחון נמוך');\n  if (pr && pr.upper != null && lv.probUpper != null && Math.abs(pr.upper - lv.probUpper) > 1e-9)\n    add('PROB_STALE_LEVELS', 'block', 'ההסתברות חושבה מול רמות אחרות מאלה שמוצגות');\n\n  // ---- staleness. A state built from bars that stopped arriving is not a\n  // valid live state, whatever else it says.\n  if (st.stale && st.action !== 'DATA_STALE' && st.action !== 'SESSION_CLOSED')\n    add('STALE_BUT_ACTIONABLE', 'block', 'הנתונים ישנים אך המסך מציג הוראה חיה');\n  if (st.stale && lv && (lv.entry != null || lv.target1 != null))\n    add('STALE_WITH_LEVELS', 'block', 'הנתונים ישנים אך מוצגת רמת כניסה');\n  if (st.stale && pr && pr.up != null)\n    add('STALE_WITH_PROBABILITY', 'block', 'הנתונים ישנים אך מוצגת הסתברות כאילו היא נוכחית');\n\n  // ---- a setup whose cancellation level the price has already crossed\n  var cancelLv = (lv && lv.tacticalStop != null) ? lv.tacticalStop\n    : (st.plan && st.plan.invalidation != null ? st.plan.invalidation : null);\n  var LIVE_ACTIONS = ['START_WATCHING', 'WAIT_FOR_CONFIRMATION', 'ENTRY_AVAILABLE', 'HOLD', 'TAKE_PARTIAL'];\n  if (!st.sessionEnded && cancelLv != null && st.price != null && st.price < cancelLv\n      && LIVE_ACTIONS.indexOf(st.action) >= 0)\n    add('CANCELLED_BUT_ACTIVE', 'block', 'המחיר כבר מתחת לרמת הביטול אך התרחיש מוצג כפעיל');\n\n  // ---- the narrative targets must be the levels block's targets\n  if (st.whatNow && Array.isArray(st.whatNow.up) && lv && lv.target1 != null && lv.target2 != null) {\n    var txt = st.whatNow.up.join(' ');\n    var m1 = txt.match(/\\u05d9\\u05e2\\u05d3 \\u05e8\\u05d0\\u05e9\\u05d5\\u05df ([\\d.]+)/);\n    var m2 = txt.match(/\\u05d9\\u05e2\\u05d3 \\u05e9\\u05e0\\u05d9 ([\\d.]+)/);\n    var near = function (a, b2) { return Math.abs(Number(a) - Number(b2)) < 0.005; };\n    if (m1 && !near(m1[1], lv.target1)) add('TARGET1_TEXT_MISMATCH', 'block', 'יעד ראשון בטקסט שונה מהיעד ברמות');\n    if (m2 && !near(m2[1], lv.target2)) add('TARGET2_TEXT_MISMATCH', 'block', 'יעד שני בטקסט שונה מהיעד ברמות');\n  }\n\n  return { valid: !v.some(function (x) { return x.severity === 'block'; }), violations: v };\n}\n\n\n// ---------------------------------------------------------------- analysis pack\n//\n// Everything another trader (or another model) needs to re-do the analysis from\n// scratch: the raw data AND the decision state, from ONE snapshot. Any field the\n// system does not have says NOT AVAILABLE rather than being quietly dropped.\nvar NA = 'NOT AVAILABLE';\nfunction nz(v, d) { return v == null || v === '' || (typeof v === 'number' && !isFinite(v)) ? NA : (d != null && typeof v === 'number' ? v.toFixed(d) : String(v)); }\n\n// Buckets belong to the exchange clock: 09:30, 09:35, ... 11:00, 11:05. The\n// grouping was already correct, but the LABEL came from the first bar that\n// happened to fall inside — so a window starting at 11:02 produced a bar called\n// \"11:02\", which is not a five-minute timeframe at all. A bucket missing some\n// of its minutes is marked rather than silently presented as whole.\nfunction aggregate(rows, minutes) {\n  var out = [], bucket = null;\n  var label = function (m) {\n    var h = Math.floor(m / 60), mm = m % 60;\n    return String(h).padStart(2, '0') + ':' + String(mm).padStart(2, '0');\n  };\n  (rows || []).forEach(function (r) {\n    var m = parseInt(r.time.slice(0, 2), 10) * 60 + parseInt(r.time.slice(3), 10);\n    var key = Math.floor(m / minutes);\n    if (!bucket || bucket.key !== key) {\n      if (bucket) out.push(bucket);\n      bucket = { key: key, time: label(key * minutes), firstBar: r.time, minutes: 1,\n        open: r.open, high: r.high, low: r.low, close: r.close, volume: r.volume, date: r.date };\n    } else {\n      bucket.high = Math.max(bucket.high, r.high); bucket.low = Math.min(bucket.low, r.low);\n      bucket.close = r.close; bucket.volume += r.volume; bucket.minutes++;\n    }\n  });\n  if (bucket) out.push(bucket);\n  out.forEach(function (b) { b.partial = b.minutes < minutes; });\n  return out;\n}\nfunction candleLine(sym, r, avgVol) {\n  var range = r.high - r.low, pct = function (x) { return range > 0 ? Math.round(x / range * 1000) / 10 : 0; };\n  var dir = r.close > r.open ? 'BULL' : r.close < r.open ? 'BEAR' : 'FLAT';\n  return [sym, r.date || '', r.time, r.open, r.high, r.low, r.close, r.volume, dir,\n    pct(Math.abs(r.close - r.open)), pct(r.high - Math.max(r.open, r.close)), pct(Math.min(r.open, r.close) - r.low),\n    Math.round(range * 10000) / 10000, avgVol > 0 ? Math.round(r.volume / avgVol * 100) / 100 : 0].join(',');\n}\nvar CANDLE_HEADER = 'symbol,date,time,open,high,low,close,volume,dir,body_pct,upper_wick_pct,lower_wick_pct,range,vol_x';\n\nfunction analysisPack(ctx) {\n  var c = ctx || {}, st = c.snap;\n  var L = [], add = function (s) { L.push(s == null ? '' : s); };\n  var head = function (t) { add(''); add('===== ' + t + ' ====='); };\n  if (!st) return 'NO SNAPSHOT — ' + NA;\n  var A = c.analysis, b = st.price != null ? st : null;\n  var bar = A && A.state ? A.state.bar : null;\n  var lv = st.levels || {}, P = st.plan, W = st.whatNow, pr = st.probability, pf = st.pressure;\n  var rows = c.rows || [], atr = st.atr;\n\n  add('FULL ANALYSIS PACK · ' + st.symbol);\n  add('Generated from one atomic snapshot: state_version ' + st.state_version + ', calculated_at ' + new Date(st.calculated_at).toISOString());\n\n  head('1. TIME & DATA STATE');\n  add('Symbol: ' + st.symbol);\n  add('Date (ET): ' + nz(st.data_through && st.data_through.date || c.date));\n  add('Last closed candle (ET): ' + nz(st.data_through && st.data_through.time));\n  add('Current price (last close): ' + nz(st.price, 2));\n  add('Quote timestamp: ' + nz(c.quoteTime) + '   (the feed is 1-minute bars; there is no separate quote stream)');\n  add('Data age / stale seconds: ' + nz(c.staleSeconds));\n  add('Freshness: ' + nz(st.freshness));\n  add('Session: ' + nz(c.session) + '   (PRE / REGULAR / AFTER / CLOSED)');\n  add('Data source: Yahoo Finance 1-minute bars via Cloudflare Worker');\n\n  head('2. MODEL DECISION');\n  add('Status: ' + nz(st.status));\n  add('Primary action: ' + nz(st.actionText) + ' [' + nz(st.action) + ']');\n  add('Setup Score: ' + nz(st.score) + '/10');\n  add('Active setup type: ' + (st.stale ? 'none (data stale)'\n    : (st.scenario ? st.scenario.kind + ' — ' + st.scenario.label : NA)));\n  add('Setup state: ' + (st.stale ? 'FROZEN (last known: ' + (P ? P.state : NA) + ') — reference only'\n    : (P ? P.state : NA)));\n  add('Reason: ' + (W ? nz(W.next) : NA));\n  add('Model state valid: ' + (st.valid ? 'yes' : 'NO — ' + (st.violations || []).map(function (v) { return v.code; }).join(', ')));\n  if (st.noEdge) add('No-edge reasons: ' + ((st.edge && st.edge.reasons) || []).join(' · '));\n\n  head('3. PRICE LEVELS AND THEIR ROLES');\n  // While frozen, every level is a record of what WAS, not a price to act on.\n  // The labels say so, because a reader skimming the pack reads the label.\n  var FROZEN = !!st.stale;\n  var role = function (name, val, note) {\n    var label = FROZEN ? 'LAST KNOWN ' + name : name;\n    var n = FROZEN ? (note ? note + ' — REFERENCE ONLY' : 'REFERENCE ONLY') : note;\n    add(label + ': ' + (val == null ? NA : val.toFixed(2) + (atr ? '  (' + fmtR((val - st.price) / atr) + ')' : '')) + (n ? '  — ' + n : ''));\n  };\n  if (FROZEN) add('NOTE: data is stale. Everything in this section is the last recorded state, not a current instruction.');\n  role('Watch level', lv.watch, FROZEN ? 'the price that was being waited for' : 'the price being waited for');\n  role('Entry zone low', P && P.zone ? P.zone[0] : null);\n  role('Entry zone high', P && P.zone ? P.zone[1] : null);\n  role('Planned entry', lv.entry);\n  role('Breakout / reclaim trigger', P && P.kind === 'breakout' ? lv.watch : (st.plan && st.plan.addAbove != null ? st.plan.addAbove : null), FROZEN ? 'confirmed an add while the setup was live'\n      : (lv.entry == null) ? 'a level to watch — there is no position to add to'\n      : 'confirms an add');\n  role('Tactical support', st.tactical && st.tactical.support ? st.tactical.support.price : null, st.tactical && st.tactical.support ? st.tactical.support.why : '');\n  role('Tactical resistance', st.tactical && st.tactical.resistance ? st.tactical.resistance.price : null, st.tactical && st.tactical.resistance ? st.tactical.resistance.why : '');\n  role('Entry cancellation (tactical)', lv.tacticalInvalidation);\n  role('Setup invalidation / structural stop', lv.hardStop);\n  role('Stop', lv.hardStop, 'same as structural invalidation; no separate money stop is computed');\n  role('Target 1', lv.target1);\n  role('Target 2', lv.target2);\n  add('Target 3: ' + NA);\n  role('Reclaim level (broken and being retested)', st.plan && st.plan.state !== 'FAILED' && A && A.state ? A.state.reclaim : null);\n  add('');\n  add('Level states (approaching / testing / held / broken / reclaimed / rejected / lost / far):');\n  ['support', 'resistance'].forEach(function (k) {\n    var s = st.levelStates && st.levelStates[k];\n    add('  ' + k + ': ' + (s ? s.price.toFixed(2) + ' — ' + s.state + ' (' + LEVEL_TEXT[s.state] + '), ' + fmtR(s.distanceR) + ', touches ' + s.touches + ', recoveries ' + s.recoveries + ', rejections ' + s.rejections : NA));\n  });\n\n  head('4. WHAT-IF' + (FROZEN ? ' — FROZEN, REFERENCE ONLY' : ''));\n  add('NOW: ' + (W ? W.actionText + ' — ' + W.next : NA));\n  add('IF UP:');\n  ((W && W.up) || []).forEach(function (s) { add('  - ' + s); });\n  if (!W || !W.up.length) add('  ' + NA);\n  add('IF DOWN:');\n  ((W && W.down) || []).forEach(function (s) { add('  - ' + s); });\n  if (!W || !W.down.length) add('  ' + NA);\n  // The same rule as FROZEN, for every state where no entry is planned. I fixed\n  // this for stale data and left the identical contradiction standing when the\n  // session is incomplete: \"Planned entry: NOT AVAILABLE\" above \"Confirms\n  // entry: partial entry on the pullback\" tells you not to trade and how to\n  // trade in the same breath.\n  var NO_ENTRY = !FROZEN && (lv.entry == null);\n  var noEntryWhy = st.sessionIncomplete ? 'the session is incomplete'\n    : st.tooOldToEnter ? 'the data is too old to act on'\n    : st.sessionEnded ? 'the session is closed'\n    : 'no entry is planned';\n  if (NO_ENTRY) {\n    add('Confirms entry: ' + NA + ' — ' + noEntryWhy);\n    add('Cancels entry: ' + NA + ' — there is no entry to cancel');\n    add('Cancels the whole setup: ' + NA + ' — ' + noEntryWhy);\n    add('After entry: ' + NA + ' — ' + noEntryWhy);\n    add('At target 1: ' + NA + ' — ' + noEntryWhy);\n    add('Confirmation strengthens above: ' + NA + ' — ' + noEntryWhy);\n    add('Exit when: ' + NA + ' — ' + noEntryWhy);\n    add('The levels above are reference only: with no entry there is nothing to add to, take off, or exit.');\n  } else if (FROZEN) {\n    // None of these may be phrased as something to do. They are the last\n    // recorded rules of a setup that is no longer live.\n    add('Confirms entry: ' + NA + ' — setup frozen (data stale)');\n    add('After entry: ' + NA + ' — setup frozen (data stale)');\n    add('At target 1: ' + NA + ' — setup frozen (data stale)');\n    add('Confirmation strengthens above: ' + NA + ' — setup frozen (data stale)');\n    add('Exit when: ' + NA + ' — setup frozen (data stale)');\n    add('LAST KNOWN entry cancellation: ' + (lv.tacticalInvalidation != null ? lv.tacticalInvalidation.toFixed(2) + '  — REFERENCE ONLY' : NA));\n    add('LAST KNOWN setup invalidation: ' + (lv.hardStop != null ? lv.hardStop.toFixed(2) + '  — REFERENCE ONLY' : NA));\n    add('These are the rules the setup had when the data stopped. They are not active.');\n  } else {\n  add('Confirms entry: ' + (P ? nz(P.ifConfirmed) : NA));\n  add('Cancels entry: ' + (lv.tacticalInvalidation != null ? 'close below ' + lv.tacticalInvalidation.toFixed(2) : NA));\n  add('Cancels the whole setup: ' + (lv.hardStop != null ? 'close below ' + lv.hardStop.toFixed(2) : NA));\n  add('After entry: ' + (P ? nz(P.nextStep) : NA));\n  add('At target 1: ' + (lv.target1 != null ? 'take partial at ' + lv.target1.toFixed(2) : NA));\n  add('Confirmation strengthens above: ' + (P && P.addAbove != null ? P.addAbove.toFixed(2) + ' (two closes); position state unknown to the system' : NA));\n  add('Exit when: ' + (lv.hardStop != null ? 'close below ' + lv.hardStop.toFixed(2) + ', or at target' : NA));\n  }\n\n  head('4b. SESSION COVERAGE');\n  var cv = st.coverage || {};\n  add('First regular bar: ' + (cv.firstBar || NA));\n  add('Last regular bar: ' + (cv.lastBar || NA));\n  add('Expected minutes: ' + (cv.expectedMinutes != null ? cv.expectedMinutes : NA));\n  add('Received unique minutes: ' + (cv.receivedMinutes != null ? cv.receivedMinutes : NA));\n  add('Coverage: ' + (cv.coveragePct != null ? cv.coveragePct + '%' : NA));\n  add('Starts at the open: ' + (cv.fromOpen ? 'yes' : 'NO — missing ' + (cv.missingFromOpen || '?') + ' minutes from 09:30'));\n  add('Holes inside the loaded window: ' + (cv.missingInside\n    ? cv.missingInside + ' minutes missing between ' + cv.firstBar + ' and ' + cv.lastBar\n      + ' — the series is not continuous, so structure, momentum and the buyer/seller split describe a broken sequence'\n    : 'none'));\n  add('Total missing minutes: ' + (cv.missingTotal != null ? cv.missingTotal : NA));\n  add('Session values usable: ' + (cv.complete ? 'yes' : 'NO — open, gap, VWAP, EMA, relative volume and any entry are derived from a partial window and are not the session\\'s'));\n\n  head('5. PROBABILITY');\n  // A suppressed probability must still say WHAT was asked and WHY there is no\n  // number. \"NOT AVAILABLE\" on its own leaves the reader unable to tell a stale\n  // feed from a question that was never answerable.\n  if (!pr) {\n    var praw = (st && st.probabilityRaw) || null;\n    add('UP: ' + NA + '   DOWN: ' + NA);\n    add('Event measured: ' + (praw && praw.upper != null\n      ? 'price reaches ' + praw.upper.toFixed(2) + ' before ' + praw.lower.toFixed(2) : NA));\n    add('Horizon: ' + (praw ? praw.horizonMin + ' minutes' : NA));\n    add('Confidence: ' + NA + ' — no probability is being claimed');\n    add('Source: ' + NA);\n    // Only the reason for the SUPPRESSION, never the half-computed scoring\n    // inputs — those describe a number we have decided not to trust.\n    var suppressWhy = (praw && (praw.notDirectional || praw.meaningless))\n      ? praw.why.join(' | ')\n      : (st && st.stale) ? 'the data is stale'\n      : (st && st.coverage && !st.coverage.complete) ? 'the session is incomplete — the inputs are not the session\\'s'\n      : 'not answerable in this state';\n    add('Why: ' + suppressWhy);\n    add('Scoring inputs: ' + NA + ' — withheld with the probability they produced');\n  } else {\n    add('UP: ' + pr.up + '%   DOWN: ' + pr.down + '%');\n    add('Event measured: price reaches ' + pr.upper.toFixed(2) + ' before ' + pr.lower.toFixed(2));\n    add('Horizon: ' + pr.horizonMin + ' minutes');\n    add('Confidence: ' + pr.confidence + '/100' + (pr.lowConfidence ? '  (LOW — displayed % is pulled toward 50)' : ''));\n    add('Source: ' + (pr.source === 'empirical' ? 'empirical, ' + pr.n + ' comparable cases in this symbol\\'s own loaded history'\n      : 'MODEL ESTIMATE — not a measured frequency' + (pr.raw != null ? ' (raw ' + pr.raw + '%, shrunk for confidence)' : '')));\n    add('Why: ' + pr.why.join(' | '));\n    if (st.coverage && !st.coverage.complete)\n      add('NOTE: these inputs come from a partial window and the probability above should not be relied on.');\n  }\n\n  head('6. BUYERS / SELLERS (inferred from candles)');\n  if (!pf) add(NA); else {\n    add('Buyers: ' + pf.buyPct + '%   Sellers: ' + pf.sellPct + '%   (window ' + pf.window + ' bars)');\n    add('Buyers: ' + pf.buyersTrend + '   Sellers: ' + pf.sellersTrend + '   Direction: ' + pf.direction);\n    add('Conclusion: ' + pf.conclusion);\n    add('Agreement with the setup: ' + pf.agreement);\n    add('METHOD: close location inside each bar weighted by that bar\\'s volume. NOT order flow. hasOrderBook=' + pf.hasOrderBook + ' hasTape=' + pf.hasTape);\n  }\n\n  head('7. ORDER BOOK / LEVEL 2');\n  var bk = c.book;\n  if (!bk || bk.error || !bk.summary || bk.summary.bid_pct == null) {\n    add(NA + ' — the Cboe book did not return data for this symbol at this time');\n  } else {\n    var s = bk.summary;\n    add('PARTIAL BOOK — CBOE ONLY');\n    add('Venues returning data: ' + (s.venues_ok.join(', ') || NA) + '   Not answering: ' + (s.venues_failed.join(', ') || 'none'));\n    add('Missing from this book: ARCA, NYSE, Nasdaq and all other venues. This is NOT the NBBO and NOT consolidated depth.');\n    add('Book timestamp: ' + nz(bk.fetched_at));\n    add('Best bid: ' + nz(s.best_bid, 2) + '   Best ask: ' + nz(s.best_ask, 2) + '   Spread: ' + nz(s.spread, 4));\n    add('Total bid depth: ' + s.bid_shares + ' shares   Total ask depth: ' + s.ask_shares + ' shares');\n    add('Imbalance: ' + s.bid_pct + '% bid / ' + s.ask_pct + '% ask  (net ' + s.imbalance + ')');\n    add('Levels available: ' + s.depth_levels + ' per side per venue (' + NA + ' beyond 5 — the public viewer publishes 5)');\n    add('');\n    add('venue,side,level,price,shares');\n    (bk.venues || []).forEach(function (v) {\n      if (v.error) { add(v.venue + ',ERROR,,,' + v.error); return; }\n      (v.asks || []).forEach(function (l, i) { add(v.venue + ',ask,' + (i + 1) + ',' + l.price + ',' + l.shares); });\n      (v.bids || []).forEach(function (l, i) { add(v.venue + ',bid,' + (i + 1) + ',' + l.price + ',' + l.shares); });\n    });\n    add('');\n    add('Depth change over the last 30-60s: ' + (c.bookPrev ? 'bid ' + (s.bid_shares - c.bookPrev.bid_shares) + ', ask ' + (s.ask_shares - c.bookPrev.ask_shares) : NA + ' (no earlier snapshot held)'));\n  }\n\n  head('8. TAPE / TIME & SALES');\n  add('TAPE NOT AVAILABLE — this feed carries 1-minute bars only. No per-trade prints, no at-bid/at-ask flags, no aggressive volume split.');\n\n  head('9. STRUCTURE & MOMENTUM');\n  // Structure is a claim about a SEQUENCE. With minutes missing in the middle\n  // the sequence is not the market's, so the reading is qualified rather than\n  // stated — a swing \"confirmed\" across a hole may never have happened.\n  var BROKEN = (st.coverage && st.coverage.contiguous === false)\n    ? '  [SERIES NOT CONTINUOUS — ' + st.coverage.missingInside + ' minutes missing inside the window; this reading is not reliable]'\n    : '';\n  add('Main structure: ' + nz(st.structure) + (A && A.state ? '  (' + A.state.reason + ')' : '') + BROKEN);\n  add('Announced trend: ' + (A && A.state ? nz(A.state.announced) : NA) + BROKEN);\n  add('Short momentum: ' + nz(st.momentum) + (st.row && st.row.mom ? '   move over last 5 bars: ' + st.row.mom.net.toFixed(2) + 'R' : ''));\n  add('Recent swings (K=3 confirmed):');\n  var sw = A ? A.swings.filter(function (w) { return w.confirmedAt <= A.bars.length - 1; }).slice(-10) : [];\n  if (!sw.length) add('  ' + NA);\n  sw.forEach(function (w) { add('  ' + w.label + '  ' + w.price.toFixed(2) + '  @' + w.time + (w.outside ? '  (outside bar — not counted as structure)' : '')); });\n\n  head('10. INDICATORS');\n  // These are all session-scoped. Computed over a partial window they are not\n  // what their names say, so every line carries the warning rather than the\n  // reader being expected to remember a note from six sections earlier.\n  var PART = (st.coverage && !st.coverage.complete) ? '  [PARTIAL WINDOW — NOT A SESSION INDICATOR]' : '';\n  if (PART) add('NOTE: the session is incomplete — every figure below describes the loaded window only.');\n  if (!bar) add(NA); else {\n    add('VWAP: ' + bar.vwap.toFixed(2) + PART + '   price is ' + (bar.aboveVwap ? 'ABOVE' : 'BELOW') + '   distance: ' + (st.price - bar.vwap).toFixed(2) + ' (' + fmtR((st.price - bar.vwap) / atr) + ')');\n    add('EMA9: ' + bar.ema9.toFixed(2) + PART + '   EMA20: ' + bar.ema20.toFixed(2) + '   state: ' + bar.align +\n      '   separation: ' + Math.abs(bar.ema9 - bar.ema20).toFixed(3) + ' (minimum for a call: ' + (0.15 * atr).toFixed(3) + ')');\n    add('Average candle range (20 bars): ' + atr.toFixed(4) + '   = 1R throughout this pack');\n    add('Relative volume (vs session average): ' + bar.volx.toFixed(2) + 'x' + PART + (bar.auction ? '  [CLOSING AUCTION BAR — excluded from scoring]' : ''));\n    add('Time-of-day normalised volume: ' + (c.baseline ? (volxTod(bar, c.baseline) != null ? volxTod(bar, c.baseline).toFixed(2) + 'x vs the same minute on ' + c.baseline.days + ' prior days' : NA) : NA + ' (no prior days loaded)'));\n    var dl = function (name, v) { return v == null ? name + ': ' + NA : name + ': ' + (st.price - v).toFixed(2) + ' (' + fmtR((st.price - v) / atr) + ')'; };\n    add('Distance from levels — ' + dl('support', st.tactical && st.tactical.support ? st.tactical.support.price : null) +\n      '   ' + dl('resistance', st.tactical && st.tactical.resistance ? st.tactical.resistance.price : null));\n  }\n\n  head('11. MARKET CONTEXT');\n  var mk = c.marketCtx;\n  if (!mk || !mk.parts || !mk.parts.length) add(NA); else {\n    add('Regime: ' + mk.label);\n    mk.parts.forEach(function (p) {\n      add('  ' + p.symbol + ': ' + p.close.toFixed(2) + '   VWAP ' + p.vwap.toFixed(2) + ' (' + (p.aboveVwap ? 'above' : 'below') + ')   EMA ' + p.align + '   trend ' + p.trend);\n    });\n    add('Sector ETF: ' + (c.sectorEtf || NA));\n    add('Does the market support this symbol: ' + (mk.label === 'Bullish' ? 'supports a long' : mk.label === 'Bearish' ? 'contradicts a long' : 'neutral'));\n  }\n\n  head('12. MULTI-DAY CONTEXT');\n  if (st.coverage && !st.coverage.complete)\n    add(\"NOTE: today's row is built from a PARTIAL session (\" + (st.coverage.coveragePct || 0)\n      + '% of the minutes since the open) — it is not a daily candle.');\n  var d = c.daily;\n  if (!d) add(NA + ' — no prior sessions loaded'); else {\n    add('Sessions loaded: ' + d.days + ' (prior: ' + d.priorDays + ')');\n    // The daily trend is computed over COMPLETED sessions only — today's row is\n    // excluded by dailyContext. Say so, because a reader looking at a partial\n    // row whose low is already below yesterday's will otherwise read the trend\n    // as a claim about it.\n    add('Daily trend: ' + d.trend + ' (' + d.trendWhy + ')'\n      + '  [computed over the ' + (d.priorDays || 0) + ' COMPLETED sessions before today'\n      + ((st.coverage && !st.coverage.complete) ? \"; today's partial row is excluded\" : '') + ']');\n    add('Previous day: high ' + nz(d.prevHigh, 2) + '  low ' + nz(d.prevLow, 2) + '  close ' + nz(d.prevClose, 2));\n    add('Gap today vs previous close: ' + ((st.coverage && !st.coverage.complete)\n      ? NA + ' — the session open is missing, so there is nothing to measure the gap from'\n      : nz(d.gap, 2)));\n    add('Average daily range: ' + nz(d.atrDaily, 2));\n    add('');\n    add('date,open,high,low,close,volume');\n    (c.dailyBars || []).forEach(function (x) { add([x.date, x.open, x.high, x.low, x.close, x.volume].join(',')); });\n    if (!(c.dailyBars || []).length) add(NA);\n    var hi = (c.dailyBars || []).reduce(function (m, x) { return Math.max(m, x.high); }, -Infinity);\n    var lo = (c.dailyBars || []).reduce(function (m, x) { return Math.min(m, x.low); }, Infinity);\n    add('');\n    add('Multi-day high/low: ' + (isFinite(hi) ? hi.toFixed(2) + ' / ' + lo.toFixed(2) : NA));\n  }\n\n  head('13. EVENTS');\n  var evs = A ? A.events.slice(-40).reverse() : [];\n  if (!evs.length) add(NA);\n  evs.forEach(function (s2) {\n    var parts = s2.events.map(function (e) {\n      return e.type + (e.level != null ? ' @' + e.level.toFixed(2) : '') + (e.confirmed === true ? ' [confirmed]' : e.confirmed === false ? ' [unconfirmed]' : '') + (e.side ? ' ' + e.side : '');\n    });\n    if (parts.length) add(s2.time + '  ' + s2.close.toFixed(2) + '  ' + parts.join(' · ') + '  (score ' + s2.score + ')');\n  });\n\n  head('14. POSITION');\n  var pos = c.position;\n  if (!pos) {\n    add('Position: NOT AVAILABLE — this application does not track positions, orders or account state.');\n    add('Quantity / average entry / P&L / current stop / invested / remaining / partials taken: ' + NA);\n  } else {\n    add('Position: ' + nz(pos.state));\n    add('Quantity: ' + nz(pos.qty) + '   Average entry: ' + nz(pos.avg, 2));\n    add('P&L: ' + nz(pos.pnl, 2) + ' (' + nz(pos.pnlPct, 2) + '%)   Current stop: ' + nz(pos.stop, 2));\n    add('Invested: ' + nz(pos.invested) + '   Remaining planned: ' + nz(pos.remaining) + '   Partials taken: ' + nz(pos.partials));\n  }\n\n  head('15. CANDLES — LAST 50 ONE-MINUTE (closed only)');\n  var avgVol = rows.length ? rows.reduce(function (a2, x) { return a2 + x.volume; }, 0) / rows.length : 0;\n  add(CANDLE_HEADER);\n  rows.slice(-50).forEach(function (r) { add(candleLine(st.symbol, r, avgVol)); });\n  if (!rows.length) add(NA);\n\n  head('16. CANDLES — LAST 20 FIVE-MINUTE (aggregated from 1-minute)');\n  var f5 = aggregate(rows, 5).slice(-20);\n  add(CANDLE_HEADER);\n  var avg5 = f5.length ? f5.reduce(function (a2, x) { return a2 + x.volume; }, 0) / f5.length : 0;\n  f5.forEach(function (r) { add(candleLine(st.symbol, r, avg5)); });\n  if (!f5.length) add(NA);\n\n  head('17. CANDLES — LAST 10 DAILY');\n  add('symbol,date,open,high,low,close,volume');\n  (c.dailyBars || []).slice(-10).forEach(function (x) { add([st.symbol, x.date, x.open, x.high, x.low, x.close, x.volume].join(',')); });\n  if (!(c.dailyBars || []).length) add(NA + ' — no prior sessions loaded');\n\n  head('END OF PACK');\n  add('Fields marked ' + NA + ' are genuinely absent from this data feed, not omitted.');\n  return L.join('\\n');\n}\n\n\n// Candidate scoring — \"is this stock worth my attention today?\"\n//\n// THREE SEPARATE QUESTIONS, and this file must never blur them:\n//\n//   Candidate Score  is this worth WATCHING today?      0-100, from history\n//   Live Interest    is it still worth live attention?  0-100, from the session\n//   Setup Score      is a trade developing RIGHT NOW?   0-10, engine.cjs\n//\n// A stock can be a strong candidate with no setup at all: a quiet coil under a\n// multi-day high is exactly that. Equally, a live symbol can hold a setup and\n// have stopped being interesting, because the range died.\n//\n// Everything here is computed from CLOSED sessions. Nothing in this file may\n// produce a live instruction, and yesterday's execution state (ACTIVE, FAILED,\n// WAITING, DO_NOT_CHASE) is deliberately not carried in: structure and levels\n// survive a session boundary, decisions do not.\n\n\n// ---------------------------------------------------------------- daily bars\n// One bar per session out of per-minute rows.\n// A US regular session is 390 one-minute bars. A set materially shorter than\n// that is a session still being collected, or one fetched mid-day — and\n// aggregating it yields a daily candle that is wrong with no outward sign.\nvar SESSION_BARS = 390, MIN_COVERAGE = 0.85;\n// Counting bars alone is the wrong test. A thinly traded name has no print in\n// some minutes and the feed omits them, so a COMPLETE session can arrive with\n// 343 bars — PGR, BLK and HON were all refused that way. What actually\n// distinguishes a complete session is that it SPANS the session: it starts at\n// the open and runs to the close. A day truncated at 13:20 fails that however\n// many bars it has.\nfunction sessionSpans(rows) {\n  if (!rows || !rows.length) return false;\n  var times = rows.map(function (r) { return r.time; }).sort();\n  return times[0] <= '09:35' && times[times.length - 1] >= '15:55';\n}\nfunction toDaily(daysRows, opts) {\n  var o2 = opts || {};\n  var auth = {};\n  (o2.authoritative || []).forEach(function (b) { auth[b.date] = b; });\n  var out = (daysRows || []).filter(function (r) { return r && r.length; }).map(function (rows) {\n    var h = -Infinity, l = Infinity, v = 0;\n    rows.forEach(function (r) { h = Math.max(h, r.high); l = Math.min(l, r.low); v += r.volume; });\n    var o = rows[0].open, c = rows[rows.length - 1].close;\n    var regular = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n    var cov = regular.length / SESSION_BARS;\n    var spans = sessionSpans(rows);\n    var a = auth[rows[0].date];\n    if (a) return { date: a.date, open: a.open, high: a.high, low: a.low, close: a.close,\n      volume: a.volume, range: a.high - a.low,\n      closePos: a.high > a.low ? (a.close - a.low) / (a.high - a.low) : 0.5,\n      bars: rows.length, coverage: Math.round(cov * 1000) / 1000, complete: true, source: 'authoritative' };\n    return { date: rows[0].date, open: o, high: h, low: l, close: c, volume: v,\n      range: h - l, closePos: h > l ? (c - l) / (h - l) : 0.5, bars: rows.length,\n      coverage: Math.round(cov * 1000) / 1000, spans: spans,\n      complete: spans && cov >= MIN_COVERAGE, source: 'aggregated' };\n  });\n  // An incomplete session is dropped rather than scored: every component here\n  // reads high, low, close or volume, and all four are wrong on a partial day.\n  return out.filter(function (b) { return b.complete; });\n}\n\n// ---------------------------------------------------------------- components\n// Each returns { points, max, why } so the score can always be explained. A\n// number with no reasons attached is not usable for a decision.\n\n// Multi-day direction: higher closes AND higher lows, or the mirror.\nfunction trendComponent(daily) {\n  var out = { points: 0, max: 25, why: null, label: 'RANGE' };\n  if (daily.length < 4) { out.why = null; return out; }\n  var w = daily.slice(-4);\n  var upC = 0, upL = 0, dnC = 0, dnL = 0;\n  for (var i = 1; i < w.length; i++) {\n    if (w[i].close > w[i - 1].close) upC++; else if (w[i].close < w[i - 1].close) dnC++;\n    if (w[i].low > w[i - 1].low) upL++; else if (w[i].low < w[i - 1].low) dnL++;\n  }\n  if (upC >= 2 && upL >= 2) { out.points = 12 + Math.min(13, (upC + upL) * 3); out.label = 'UP';\n    out.why = 'מגמה רב-יומית עולה — ' + upC + ' סגירות ו-' + upL + ' שפלים עולים'; }\n  else if (dnC >= 2 && dnL >= 2) { out.points = 10 + Math.min(10, (dnC + dnL) * 2); out.label = 'DOWN';\n    out.why = 'מגמה רב-יומית יורדת — ' + dnC + ' סגירות ו-' + dnL + ' שיאים יורדים'; }\n  else { out.points = 4; out.label = 'RANGE'; out.why = 'ללא כיוון רב-יומי ברור'; }\n  return out;\n}\n\n// Enough daily movement to be worth watching at all. A name whose whole day is\n// 0.4% cannot pay for the attention, however clean its structure.\nfunction rangeComponent(daily) {\n  var out = { points: 0, max: 15, why: null, atrPct: null };\n  if (!daily.length) return out;\n  var w = daily.slice(-5);\n  var avg = w.reduce(function (s, b) { return s + b.range; }, 0) / w.length;\n  var px = w[w.length - 1].close || 1;\n  var pct = avg / px * 100;\n  out.atrPct = Math.round(pct * 100) / 100;\n  // This is the average over five sessions, not today's range. Calling it\n  // \"daily range\" made it look wrong against a single day's high-low.\n  var last = w[w.length - 1];\n  out.lastRangePct = px > 0 ? Math.round(last.range / px * 10000) / 100 : null;\n  out.window = w.length;\n  if (pct >= 3) { out.points = 15; out.why = 'טווח ממוצע רחב (' + out.window + ' ימים) — ' + out.atrPct + '% מהמחיר'; }\n  else if (pct >= 1.8) { out.points = 12; out.why = 'טווח ממוצע טוב (' + out.window + ' ימים) — ' + out.atrPct + '%'; }\n  else if (pct >= 1.0) { out.points = 7; out.why = 'טווח ממוצע בינוני (' + out.window + ' ימים) — ' + out.atrPct + '%'; }\n  else { out.points = 1; out.why = 'טווח ממוצע צר (' + out.window + ' ימים) — ' + out.atrPct + '%, קשה להוציא ממנו מהלך'; }\n  return out;\n}\n\n// A contraction into a decision point is the classic pre-move condition: the\n// last two sessions narrower than the ones before them.\nfunction coilComponent(daily, atrPct, relVol) {\n  var out = { points: 0, max: 12, why: null, coiled: false };\n  if (daily.length < 5) return out;\n  // A dead stock is narrow and quiet too. Contraction only means something when\n  // the name has enough daily range to matter and the volume has not collapsed\n  // with it — otherwise this rewards exactly the stocks worth ignoring.\n  if (atrPct != null && atrPct < 1.0) { out.why = null; return out; }\n  if (relVol != null && relVol < 0.6) { out.why = null; return out; }\n  var recent = daily.slice(-2), prior = daily.slice(-5, -2);\n  var r = recent.reduce(function (s, b) { return s + b.range; }, 0) / recent.length;\n  var p = prior.reduce(function (s, b) { return s + b.range; }, 0) / prior.length;\n  if (!(p > 0)) return out;\n  var ratio = r / p;\n  if (ratio <= 0.6) { out.points = 12; out.coiled = true; out.why = 'התכווצות טווח חדה לפני הפתיחה — ' + Math.round(ratio * 100) + '% מהממוצע'; }\n  else if (ratio <= 0.8) { out.points = 8; out.coiled = true; out.why = 'התכווצות טווח — ' + Math.round(ratio * 100) + '% מהממוצע'; }\n  return out;\n}\n\n// Where the last session closed inside its own range. A close on the high into\n// a multi-day high is the setup that most often continues.\nfunction closeComponent(daily) {\n  var out = { points: 0, max: 10, why: null };\n  if (!daily.length) return out;\n  var last = daily[daily.length - 1];\n  if (last.closePos >= 0.85) { out.points = 10; out.why = 'סגר בשיא היום — ' + Math.round(last.closePos * 100) + '% מהטווח'; }\n  else if (last.closePos >= 0.7) { out.points = 7; out.why = 'סגר בחלק העליון של היום'; }\n  else if (last.closePos <= 0.15) { out.points = 6; out.why = 'סגר בשפל היום — חולשה שמעניינת לשורט או לריבאונד'; }\n  else if (last.closePos <= 0.3) { out.points = 4; out.why = 'סגר בחלק התחתון של היום'; }\n  return out;\n}\n\n// Volume behind the last session, against its own recent average.\nfunction volumeComponent(daily) {\n  var out = { points: 0, max: 13, why: null, relVol: null };\n  if (daily.length < 3) return out;\n  var last = daily[daily.length - 1], prior = daily.slice(-6, -1);\n  if (!prior.length) return out;\n  var avg = prior.reduce(function (s, b) { return s + b.volume; }, 0) / prior.length;\n  if (!(avg > 0)) return out;\n  var rel = last.volume / avg;\n  out.relVol = Math.round(rel * 100) / 100;\n  if (rel >= 1.8) { out.points = 13; out.why = 'מחזור חריג בסשן האחרון — ×' + out.relVol; }\n  else if (rel >= 1.3) { out.points = 10; out.why = 'מחזור מוגבר — ×' + out.relVol; }\n  else if (rel >= 0.9) { out.points = 5; out.why = 'מחזור רגיל — ×' + out.relVol; }\n  else { out.points = 2; out.why = 'מחזור דל — ×' + out.relVol + ', עניין נמוך'; }\n  return out;\n}\n\n// Sitting just under a multi-day high (or over a low) is a decision point: the\n// session opens with something to resolve.\nfunction levelComponent(daily) {\n  var out = { points: 0, max: 25, why: null, levels: [] };\n  if (daily.length < 2) return out;\n  var last = daily[daily.length - 1], prior = daily.slice(0, -1);\n  var px = last.close, atr = daily.slice(-5).reduce(function (s, b) { return s + b.range; }, 0) / Math.min(5, daily.length);\n  if (!(atr > 0)) return out;\n\n  var hi = Math.max.apply(null, prior.map(function (b) { return b.high; }));\n  var lo = Math.min.apply(null, prior.map(function (b) { return b.low; }));\n  var pd = prior[prior.length - 1];\n\n  // Each level carries the session it came from. \"Yesterday\" meant 09-02 here\n  // and 09-03 in the range figure on the same card — the reader had no way to\n  // know which.\n  var pdDate = pd.date ? ' ' + pd.date.slice(5).replace('-', '/') : '';\n  out.levels = [\n    { name: 'גבוה רב-יומי', price: hi, date: null },\n    { name: 'גבוה סשן קודם' + pdDate, price: pd.high, date: pd.date },\n    { name: 'סגירת סשן קודם' + pdDate, price: pd.close, date: pd.date },\n    { name: 'נמוך סשן קודם' + pdDate, price: pd.low, date: pd.date },\n    { name: 'נמוך רב-יומי', price: lo, date: null }\n  ].filter(function (x) { return isFinite(x.price); });\n  out.priorDate = pd.date || null;\n\n  // The same guard: \"right under the multi-day high\" is meaningless when the\n  // multi-day range is a fraction of a percent.\n  var spanPct = (hi - lo) / (px || 1) * 100;\n  if (spanPct < 1.0) { out.points = 2; out.why = 'הטווח הרב-יומי צר מדי מכדי שרמה תהיה נקודת הכרעה'; return out; }\n  var dHi = (hi - px) / atr, dLo = (px - lo) / atr;\n  // WHAT THE LAST SESSION DID AT THE LEVEL, not just where it closed relative\n  // to it. A day that traded THROUGH the multi-day high and closed back under\n  // it is a rejected breakout — the opposite of a coil waiting to resolve — and\n  // scoring both as \"right under the level, decision point\" gave a failed\n  // breakout the maximum available points. Measured: AAPL reached 330.81 above\n  // a 328.40 level and closed 328.21; NVDA reached 230.40 above 229.26 and\n  // closed 228.45. Both were being ranked as pre-breakout.\n  var touchedHi = last.high >= hi - 0.05 * atr;\n  var brokeHi = last.high > hi;\n  var heldHi = px > hi;\n  var brokeLo = last.low < lo, heldLo = px < lo;\n  out.event = heldHi ? 'breakout_held'\n    : brokeHi ? 'breakout_failed'\n    : touchedHi ? 'testing_high'\n    : heldLo ? 'breakdown_held'\n    : brokeLo ? 'breakdown_recovered'\n    : dHi <= 0.35 ? 'approaching_high'\n    : dLo <= 0.35 ? 'approaching_low' : 'mid_range';\n\n  if (out.event === 'breakout_held') { out.points = 22;\n    out.why = 'פרץ את הגבוה הרב-יומי וסגר מעליו — פריצה שצריכה אישור'; }\n  else if (out.event === 'breakout_failed') { out.points = 6;\n    // Name the LEVEL and the price reached separately. Putting last.high in the\n    // parentheses read as though 83.60 were the multi-day high, while the level\n    // printed two lines below said 83.12 — the same card contradicting itself.\n    out.why = 'ניסה לפרוץ את הגבוה הרב-יומי ' + hi.toFixed(2)\n      + ', הגיע ל-' + last.high.toFixed(2) + ' ונסגר ב-' + px.toFixed(2) + ' מתחתיו — פריצה שנדחתה'; }\n  else if (out.event === 'testing_high') { out.points = 20;\n    out.why = 'נגע בגבוה הרב-יומי בלי לפרוץ — נבחן'; }\n  else if (out.event === 'approaching_high') { out.points = 25;\n    out.why = 'ממש מתחת לגבוה הרב-יומי, בלי ניסיון פריצה — נקודת הכרעה בפתיחה'; }\n  else if (dHi > 0.35 && dHi <= 1) { out.points = 18; out.why = 'קרוב לגבוה הרב-יומי'; }\n  else if (out.event === 'breakdown_held') { out.points = 16;\n    out.why = 'סגר מתחת לנמוך הרב-יומי — שבירה'; }\n  else if (out.event === 'breakdown_recovered') { out.points = 14;\n    out.why = 'שבר את הנמוך הרב-יומי ' + lo.toFixed(2)\n      + ', ירד עד ' + last.low.toFixed(2) + ' וחזר מעליו — שבירה שנדחתה'; }\n  else if (out.event === 'approaching_low') { out.points = 17;\n    out.why = 'ממש מעל הנמוך הרב-יומי — נקודת הכרעה'; }\n  else { out.points = 5; out.why = 'באמצע הטווח הרב-יומי'; }\n  return out;\n}\n\n// ---------------------------------------------------------------- the score\n//\n// candidateScore(daysRows) -> { score, reasons, structure, levels, ... }\n//\n// daysRows: an array of sessions, oldest first, each an array of 1-minute rows.\n// Every session here is CLOSED. Nothing about a live setup enters this.\nfunction candidateScore(daysRows, opts) {\n  var o = opts || {};\n  var all = (daysRows || []).filter(function (r) { return r && r.length; });\n  var daily = toDaily(all, o);\n  var dropped = all.length - daily.length;\n  if (!daily.length) {\n    return { score: null, reasons: [], structure: null, levels: [], sessions: 0,\n      incomplete_sessions: dropped,\n      note: dropped ? 'הסשנים הקיימים אינם שלמים — לא ניתן לחשב' : 'אין נתונים היסטוריים' };\n  }\n  var range = rangeComponent(daily), volume = volumeComponent(daily);\n  var parts = {\n    trend: trendComponent(daily),\n    range: range,\n    coil: coilComponent(daily, range.atrPct, volume.relVol),\n    close: closeComponent(daily),\n    volume: volume,\n    level: levelComponent(daily)\n  };\n  var got = 0, max = 0;\n  Object.keys(parts).forEach(function (k) { got += parts[k].points; max += parts[k].max; });\n  // Normalised so a symbol with only three sessions is not punished for the\n  // components that need five — its max is smaller, not its score.\n  var score = Math.round(got / max * 100);\n  // A stock whose daily range cannot pay for a trade is not a candidate, no\n  // matter how tidy its structure looks. This is a ceiling, not a deduction:\n  // the components stay honest and the verdict is capped.\n  var capped = null;\n  if (range.atrPct != null && range.atrPct < 0.8 && score > 30) { capped = score; score = 30; }\n  else if (range.atrPct != null && range.atrPct < 1.2 && score > 55) { capped = score; score = 55; }\n  var capWindow = range.window || 5;\n\n  // Reasons, strongest first, so the screen can show the top two.\n  var reasons = Object.keys(parts)\n    .map(function (k) { return { key: k, points: parts[k].points, text: parts[k].why }; })\n    .filter(function (x) { return x.text; })\n    .sort(function (a, b) { return b.points - a.points; });\n\n  var last = daily[daily.length - 1];\n  return {\n    score: score, reasons: reasons, parts: parts,\n    cappedFrom: capped,\n    capReason: capped != null ? 'הציון הוגבל: טווח ממוצע ' + capWindow + ' ימים של '\n      + range.atrPct + '% קטן מכדי להצדיק מעקב' : null,\n    structure: parts.trend.label,\n    structureWhy: parts.trend.why,\n    levels: parts.level.levels,\n    atrPct: parts.range.atrPct, avgRangeWindow: parts.range.window,\n    lastRangePct: parts.range.lastRangePct,\n    levelEvent: parts.level.event, priorDate: parts.level.priorDate,\n    relVol: parts.volume.relVol, coiled: parts.coil.coiled,\n    sessions: daily.length, incomplete_sessions: dropped,\n    incompleteNote: dropped ? dropped + ' סשנים לא שלמים לא נכללו בחישוב' : null,\n    dataSource: daily.every(function (b) { return b.source === 'authoritative'; }) ? 'authoritative'\n      : daily.some(function (b) { return b.source === 'authoritative'; }) ? 'mixed' : 'aggregated',\n    lastClose: last.close, lastDate: last.date, closePos: Math.round(last.closePos * 100),\n    daily: daily\n  };\n}\n\n// Is the interest improving or fading? The same score computed one session\n// earlier, compared with today's. Without at least three sessions there is\n// nothing to compare and the honest answer is that we do not know.\nfunction candidateTrend(daysRows) {\n  var rows = (daysRows || []).filter(function (r) { return r && r.length; });\n  if (rows.length < 3) return { direction: 'unknown', delta: null, text: 'אין מספיק ימים להשוואה' };\n  var now = candidateScore(rows), before = candidateScore(rows.slice(0, -1));\n  if (now.score == null || before.score == null) return { direction: 'unknown', delta: null, text: 'אין מספיק ימים להשוואה' };\n  var d = now.score - before.score;\n  return {\n    direction: d >= 6 ? 'improving' : d <= -6 ? 'fading' : 'steady',\n    delta: d,\n    text: d >= 6 ? 'העניין מתחזק (+' + d + ')' : d <= -6 ? 'העניין נחלש (' + d + ')' : 'ללא שינוי מהותי'\n  };\n}\n\n// ---------------------------------------------------------------- live interest\n//\n// For symbols already on the watchlist, during the session: is this still worth\n// the attention it is taking? This is NOT the setup score — a symbol can hold a\n// valid setup and still have gone quiet, and a symbol with no setup can be the\n// most alive thing on the screen.\nfunction liveInterest(A, ctx) {\n  var c = ctx || {};\n  if (!A || !A.state) return { score: null, reasons: [], verdict: 'אין נתונים' };\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length;\n  var atr = b.atr20 || b.avgRange || 1;\n  var reasons = [], score = 0;\n\n  // Movement over the last half hour, judged BOTH against the symbol's own\n  // average bar and against its price. Measuring only in ATR units made a dead\n  // stock look active: when the average bar is a cent, six cents is \"6R\" and\n  // nothing at all has happened.\n  var w = bars.slice(-30);\n  var hi = Math.max.apply(null, w.map(function (x) { return x.high; }));\n  var lo = Math.min.apply(null, w.map(function (x) { return x.low; }));\n  var swing = (hi - lo) / atr;\n  var swingPct = b.close > 0 ? (hi - lo) / b.close * 100 : 0;\n  var moving = swingPct >= 0.25;                  // a quarter percent in 30 min\n  if (swing >= 6 && moving) { score += 30; reasons.push('תנועה חזקה בחצי השעה האחרונה'); }\n  else if (swing >= 3 && moving) { score += 20; reasons.push('תנועה סבירה בחצי השעה האחרונה'); }\n  else if (swing >= 1.5 && moving) { score += 10; reasons.push('תנועה מוגבלת'); }\n  else { reasons.push('כמעט ללא תנועה בחצי השעה האחרונה (' + (Math.round(swingPct * 100) / 100) + '%)'); }\n\n  // is volume still arriving\n  var recentVol = w.reduce(function (s, x) { return s + x.volume; }, 0) / Math.max(1, w.length);\n  var dayVol = bars.reduce(function (s, x) { return s + x.volume; }, 0) / Math.max(1, n);\n  var rel = dayVol > 0 ? recentVol / dayVol : 0;\n  if (rel >= 1.3) { score += 25; reasons.push('מחזור מתגבר (×' + (Math.round(rel * 100) / 100) + ')'); }\n  else if (rel >= 0.8) { score += 15; reasons.push('מחזור יציב'); }\n  else { score += 3; reasons.push('המחזור דועך (×' + (Math.round(rel * 100) / 100) + ')'); }\n\n  // proximity to a level that matters\n  var T = c.tactical || tactical(A);\n  var near = [T.support, T.resistance].filter(Boolean).map(function (l) { return Math.abs(l.r); });\n  var d = near.length ? Math.min.apply(null, near) : Infinity;\n  if (d <= 0.5) { score += 25; reasons.push('צמוד לרמה משמעותית'); }\n  else if (d <= 1.5) { score += 15; reasons.push('קרוב לרמה'); }\n  else if (isFinite(d)) { score += 5; reasons.push('רחוק מרמות'); }\n\n  // a structure worth following\n  var st = S.announced || S.trend;\n  if (st === 'UP' || st === 'DOWN') { score += 20; reasons.push('מבנה תוך-יומי מוכרז (' + st + ')'); }\n  else { score += 5; reasons.push('ללא מבנה תוך-יומי'); }\n\n  // Nothing else can rescue a symbol that is not moving: structure and levels\n  // are only worth watching on something that travels.\n  if (!moving) { score = Math.min(score, 30); reasons.push('אין תנועה — לא מצדיק תשומת לב חיה'); }\n  score = Math.max(0, Math.min(100, score));\n  return {\n    score: score, reasons: reasons, movingPct: Math.round(swingPct * 100) / 100,\n    verdict: score >= 60 ? 'שווה תשומת לב חיה' : score >= 35 ? 'עניין בינוני' : 'כבר לא מעניין',\n    quiet: score < 35, swingR: Math.round(swing * 10) / 10, volRel: Math.round(rel * 100) / 100\n  };\n}\n\n// ---------------------------------------------------------------- suggestions\n//\n// Recommends only. Nothing here moves a symbol on its own — the trader decides,\n// and a screen that silently reshuffled the watchlist would be worse than one\n// that said nothing.\n//\n//   watch: [{ symbol, candidate, live }]\n//   scan:  [{ symbol, candidate, trend }]\nfunction suggestions(watch, scan, opts) {\n  var o = opts || {};\n  var PROMOTE_AT = o.promoteAt == null ? 60 : o.promoteAt;\n  var DEMOTE_AT = o.demoteAt == null ? 35 : o.demoteAt;\n\n  var weakest = (watch || []).filter(function (x) {\n    return x.live && x.live.score != null && x.live.score < DEMOTE_AT;\n  }).sort(function (a, b) { return a.live.score - b.live.score; });\n\n  var promote = (scan || []).filter(function (x) {\n    if (!x.candidate || x.candidate.score == null) return false;\n    if (x.candidate.score < PROMOTE_AT) return false;\n    // Improving matters as much as high: a score that is high and fading is\n    // yesterday's story.\n    return !x.trend || x.trend.direction !== 'fading';\n  }).sort(function (a, b) {\n    var d = b.candidate.score - a.candidate.score;\n    if (d) return d;\n    return (b.trend && b.trend.delta || 0) - (a.trend && a.trend.delta || 0);\n  });\n\n  var room = Math.max(0, (o.max == null ? 40 : o.max) - (watch || []).length);\n  return {\n    promote: promote.slice(0, 8).map(function (x, i) {\n      // Only pair a promotion with a removal when the list is actually full.\n      var swap = (!room && weakest[i]) ? weakest[i] : null;\n      return {\n        symbol: x.symbol, score: x.candidate.score,\n        why: (x.candidate.reasons || []).slice(0, 2).map(function (r) { return r.text; }),\n        direction: x.trend ? x.trend.direction : 'unknown',\n        directionText: x.trend ? x.trend.text : null,\n        insteadOf: swap ? swap.symbol : null,\n        insteadOfWhy: swap ? swap.live.verdict + ' (' + swap.live.score + ')' : null\n      };\n    }),\n    demote: weakest.slice(0, 8).map(function (x) {\n      return { symbol: x.symbol, score: x.live.score, why: x.live.reasons.slice(0, 2),\n        verdict: x.live.verdict };\n    }),\n    room: room,\n    note: room ? 'יש מקום — הוספה לא מחייבת הסרה' : 'המעקב מלא: הוספה מחייבת הסרה'\n  };\n}\n\n\n// ============================================================================\n// BUY DECISION CARD — an additional view. Nothing here is read by any other\n// surface, and this file reads the state the application has already built.\n//\n// The premise that shapes everything: we do NOT know the price the trader is\n// looking at in his broker. So the card must never say \"if it reaches X, buy\" —\n// that is a limit order, and it assumes we can see the market. It says instead:\n// given the price ON YOUR SCREEN RIGHT NOW, here is the decision.\n//\n// Four states only. BUY, WAIT, RECHECK, NO SETUP. RECHECK is not a refusal —\n// it means the snapshot no longer covers the price being seen, and the answer\n// has to be recomputed from newer candles. A lower price may be a better\n// entry; a higher one may be a breakout. Neither is knowable from here.\n// ============================================================================\n\nfunction buyCard(st, A, ctx) {\n  var c = ctx || {};\n  var NA = '—';\n  var n2 = function (x) { return x == null ? NA : (+x).toFixed(2); };\n\n  var out = {\n    symbol: st && st.symbol || (c.symbol || NA),\n    snapshotTime: c.snapshotTime || null,\n    lastBarTime: null, lastClose: null,\n    coverage: null, decision: 'NO SETUP', quality: NA, confidence: NA,\n    map: [], validity: null, reasons: [], levels: [], blocked: null\n  };\n  if (!st || !A || !A.state) { out.blocked = 'אין ניתוח זמין'; return out; }\n\n  var bars = (A.rawBars && A.rawBars.length) ? A.rawBars : A.bars;\n  var last = bars && bars.length ? bars[bars.length - 1] : null;\n  var b = A.state.bar;\n  var atr = (b && (b.atr20 || b.avgRange)) || 0;\n  out.lastBarTime = last ? last.time : null;\n  out.lastClose = last ? last.close : null;\n  out.coverage = st.coverage || null;\n\n  // ---- data safety comes before anything else -----------------------------\n  // An unusable session cannot produce a BUY, whatever the setup looks like.\n  var cov = st.coverage || {};\n  if (st.stale) out.blocked = 'נתונים ישנים — לא ניתן להחליט';\n  else if (st.sessionIncomplete)\n    out.blocked = 'סשן חלקי (' + (cov.receivedMinutes || 0) + '/' + (cov.expectedMinutes || 0)\n      + ' דקות) — לא ניתן להחליט';\n  else if (st.tooOldToEnter) out.blocked = 'הנר האחרון ישן מדי להחלטה';\n  else if (!last) out.blocked = 'אין נרות';\n  if (out.blocked) {\n    out.decision = 'RECHECK';\n    out.reasons.push(out.blocked);\n    return out;\n  }\n\n  // ---- what the existing analysis already decided --------------------------\n  var P = st.plan || null;\n  var lv = st.levels || {};\n  var px = out.lastClose;\n\n  // The BUY range is the zone the existing plan already identified, not a\n  // percentage invented around the last price. Without a plan there is no zone\n  // and therefore no buy.\n  var zone = (P && P.zone && P.zone.length === 2) ? P.zone.slice() : null;\n  var hasEdge = !!(P && zone && !st.noEdge);\n\n  // Validity comes from STRUCTURE, not from a band drawn around the last price.\n  // 616.42-617.08 around 616.75 is arbitrary symmetry dressed up as analysis:\n  // it says nothing about where the reading actually stops holding. The real\n  // boundaries are the levels the setup depends on — the zone, the\n  // invalidation, the nearest support and resistance. Where none of those\n  // exist, there is no validity range and none is invented.\n  var T = st.tactical || null;\n  var bounds = [];\n  var addBound = function (v, why) {\n    if (v == null || !isFinite(v)) return;\n    bounds.push({ price: +(+v).toFixed(2), why: why });\n  };\n  if (T && T.support) addBound(T.support.price, 'תמיכה');\n  if (T && T.resistance) addBound(T.resistance.price, 'התנגדות');\n  if (lv.tacticalInvalidation != null) addBound(lv.tacticalInvalidation, 'ביטול');\n  if (lv.watch != null) addBound(lv.watch, 'רמת מעקב');\n  if (P && P.zone && P.zone.length === 2) { addBound(P.zone[0], 'אזור'); addBound(P.zone[1], 'אזור'); }\n\n  var below = bounds.filter(function (x) { return x.price < px; }).sort(function (a, b2) { return b2.price - a.price; })[0];\n  var above = bounds.filter(function (x) { return x.price > px; }).sort(function (a, b2) { return a.price - b2.price; })[0];\n  var vLow = below ? below.price : null;\n  var vHigh = above ? above.price : null;\n  out.validity = (vLow != null && vHigh != null)\n    ? { low: vLow, high: vHigh, lowWhy: below.why, highWhy: above.why } : null;\n\n  // ---- the decision --------------------------------------------------------\n  if (!hasEdge) {\n    // NO SETUP is its own answer, not a weak version of one. There is nothing\n    // whose validity could be mapped, so there is no price map, no validity\n    // range and no confidence — confidence in a setup that does not exist is a\n    // number about nothing. And it is never RECHECK: RECHECK means an ACTIVE\n    // setup no longer covers the price, which presupposes a setup.\n    out.decision = 'NO SETUP';\n    out.reasons.push(st.why || 'אין setup קנייה כרגע');\n    out.map = [];\n    out.validity = null;\n    out.quality = 'אין';\n    out.confidence = NA;\n    out.hasMap = false;\n  } else {\n    var zLow = Math.min(zone[0], zone[1]), zHigh = Math.max(zone[0], zone[1]);\n    // Does the buy zone overlap the band this snapshot can speak about?\n    var overlaps = zHigh >= vLow && zLow <= vHigh;\n    out.decision = overlaps ? 'BUY' : 'WAIT';\n    if (!overlaps) out.reasons.push('אזור הקנייה (' + n2(zLow) + '–' + n2(zHigh)\n      + ') מחוץ לטווח שהכרטיס יכול לדבר עליו');\n  }\n\n  // ---- the price decision map ---------------------------------------------\n  // Only where there IS an active setup, and only where structure gives real\n  // boundaries. Read as: the number you are looking at in your broker.\n  var rows = [];\n  var canMap = hasEdge && out.validity != null;\n  out.hasMap = canMap;\n  if (canMap) {\n  if (out.decision === 'BUY') {\n    var zl = Math.max(vLow, Math.min(zone[0], zone[1]));\n    var zh = Math.min(vHigh, Math.max(zone[0], zone[1]));\n    rows.push({ from: zl, to: zh, decision: 'BUY', text: 'קונה' });\n    if (zh < vHigh) rows.push({ from: +(zh + 0.01).toFixed(2), to: vHigh, decision: 'WAIT', text: 'לא קונה' });\n    if (zl > vLow) rows.push({ from: vLow, to: +(zl - 0.01).toFixed(2), decision: 'WAIT', text: 'לא קונה' });\n  } else if (out.decision === 'WAIT' || out.decision === 'NO SETUP') {\n    rows.push({ from: vLow, to: vHigh, decision: out.decision === 'WAIT' ? 'WAIT' : 'NO SETUP',\n      text: out.decision === 'WAIT' ? 'לא קונה' : 'אין setup' });\n  }\n  rows.sort(function (x, y) { return y.from - x.from; });\n  rows.push({ above: vHigh, decision: 'RECHECK', text: 'לבדוק מחדש' });\n  rows.push({ below: vLow, decision: 'RECHECK', text: 'לבדוק מחדש' });\n  }\n  out.map = rows;\n\n  // ---- supporting readings, all taken from what already exists -------------\n  if (out.decision !== 'NO SETUP') {\n    out.quality = out.decision === 'BUY' ? 'תקין' : 'חלש';\n    out.confidence = (st.score != null)\n      ? (st.score >= 7 ? 'גבוה' : st.score >= 4 ? 'בינוני' : 'נמוך') : NA;\n  }\n\n  out.structure = st.structure || (A.state.announced || A.state.trend) || NA;\n  out.momentum = st.momentum || NA;\n  out.vwap = b ? b.vwap : null;\n  out.aboveVwap = b ? !!b.aboveVwap : null;\n  out.ema9 = b ? b.ema9 : null;\n  out.ema20 = b ? b.ema20 : null;\n  out.market = c.market || NA;\n\n  var pf = st.pressure || null;\n  if (pf) {\n    // The pressure object exposes buyPct/sellPct. I read buyers/sellers and got\n    // undefined, so the card showed \"buyers weakening\" with no numbers beside\n    // it — the exact thing the rule forbids.\n    out.buyersPct = pf.buyPct != null ? pf.buyPct : pf.buyers;\n    out.sellersPct = pf.sellPct != null ? pf.sellPct : pf.sellers;\n    out.buyersTrend = pf.buyersTrend; out.sellersTrend = pf.sellersTrend;\n  }\n\n  // Nearby levels that actually bear on a buy decision.\n  if (T) {\n    if (T.support) out.levels.push({ name: 'תמיכה קרובה', price: T.support.price });\n    if (T.resistance) out.levels.push({ name: 'התנגדות קרובה', price: T.resistance.price });\n  }\n  if (lv.watch != null) out.levels.push({ name: 'רמת מעקב', price: lv.watch });\n\n  // ---- a real explanation, not a label ------------------------------------\n  // \"אין setup קנייה כרגע\" restates the verdict. The reader needs to know WHY\n  // from the same facts printed above: where the price finished, what the\n  // structure is doing, and which side is gaining.\n  out.why = (function () {\n    var parts = [];\n    if (px != null) parts.push('המחיר ' + (c.closed ? 'סיים ב-' : 'עומד על ') + px.toFixed(2));\n    var mom = { PUSHING: 'בדחיפה', PULLBACK: 'בתוך pullback', RECOVERY: 'בהתאוששות',\n      SELLING: 'תחת מכירות', FLAT: 'ללא מומנטום' }[out.momentum];\n    var strc = { UP: 'מבנה עולה', DOWN: 'מבנה יורד', RANGE: 'בטווח' }[out.structure];\n    if (strc) parts.push(strc);\n    if (mom) parts.push(mom);\n    if (pf && pf.buyersTrend && pf.sellersTrend)\n      parts.push('הקונים ' + pf.buyersTrend + ' והמוכרים ' + pf.sellersTrend);\n    var lead = parts.join(', ') + '.';\n    var verdict = out.decision === 'NO SETUP' ? ' כרגע אין setup קנייה מספיק ברור.'\n      : out.decision === 'WAIT' ? ' התנאים לא מצדיקים קנייה במחיר הנוכחי.'\n      : out.decision === 'BUY' ? ' התנאים מצדיקים קנייה בטווח המסומן.'\n      : ' צריך נרות חדשים כדי להחליט.';\n    return lead + verdict;\n  })();\n\n  // What the trader is actually asking. When the market is shut this is not a\n  // rejection — there is simply nothing to act on.\n  out.buyNow = c.closed ? '— המסחר סגור'\n    : out.decision === 'BUY' ? 'כן, בטווח המסומן'\n    : out.decision === 'WAIT' ? 'לא'\n    : out.decision === 'RECHECK' ? '— צריך נרות חדשים'\n    : '— אין setup';\n\n  // A live price map only means something while a live price exists.\n  if (c.closed) { out.hasMap = false; out.map = []; out.validity = null; }\n  out.marketClosed = !!c.closed;\n\n  // A probability is shown only if it measures THIS decision. The path model\n  // answers \"which of two levels is touched first\", which is a different\n  // question, so it is deliberately not carried over.\n  out.probability = null;\n  out.probabilityNote = 'הסתברות לא מוצגת: המודל הקיים מודד אירוע אחר';\n\n  if (!out.reasons.length && st.why) out.reasons.push(st.why);\n  return out;\n}\n\n// The same card as plain text. Built from the card object rather than scraped\n// from the DOM, so what is copied is exactly what was decided — a copy that can\n// drift from the screen is worse than none.\nfunction buyCardText(card, opts) {\n  var o = opts || {};\n  var NA = '—';\n  var n2 = function (x, d) { return x == null ? NA : (+x).toFixed(d == null ? 2 : d); };\n  var L = [];\n  var DEC = { BUY: 'BUY — אפשר לקנות', WAIT: 'WAIT — לא קונה כרגע',\n    RECHECK: 'RECHECK — לבדוק מחדש', 'NO SETUP': 'NO SETUP — אין setup' };\n\n  L.push(card.symbol + (card.marketClosed\n    ? ' · סגירה' + (card.snapshotTime ? ' ' + card.snapshotTime : '')\n    : (card.snapshotTime ? ' · תמונת מצב ' + card.snapshotTime : '')));\n  var cov = card.coverage || {};\n  L.push('נר אחרון: ' + n2(card.lastClose, 3)\n    + (cov.receivedMinutes != null ? ' · ' + cov.receivedMinutes + '/' + cov.expectedMinutes\n      + ' נרות ' + (cov.complete ? 'תקין' : 'חסר') : ''));\n  L.push('');\n  L.push(DEC[card.decision] || card.decision);\n  if (card.blocked) L.push(card.blocked);\n  L.push('');\n  L.push('מצב קנייה: ' + card.quality);\n  L.push('ביטחון: ' + card.confidence);\n\n  if (card.hasMap && card.validity) {\n    L.push('');\n    L.push(o.live ? 'מחיר Trading שלך עכשיו' : 'מפת מחיר');\n    (card.map || []).forEach(function (r) {\n      var range = r.above != null ? ('מעל ' + n2(r.above))\n        : r.below != null ? ('מתחת ' + n2(r.below))\n        : (n2(r.from) + '–' + n2(r.to));\n      L.push('  ' + range + '  ->  ' + r.decision + ' — ' + r.text);\n    });\n    L.push('');\n    L.push('תקפות הכרטיס: ' + n2(card.validity.low) + '–' + n2(card.validity.high)\n      + ' (' + card.validity.lowWhy + ' / ' + card.validity.highWhy + ')');\n    L.push('מחוץ לטווח -> RECHECK עם הנרות החדשים.');\n    L.push('מחיר נמוך יותר עשוי ליצור setup טוב יותר, גבוה יותר עשוי ליצור פריצה.');\n  }\n\n  L.push('');\n  L.push('למה אין BUY');\n  if (card.blocked) L.push('  ' + card.blocked);\n  L.push('  ' + (card.why || (card.reasons || []).join(' ')));\n\n  L.push('');\n  L.push('נתונים');\n  L.push('  מבנה: ' + card.structure);\n  L.push('  מומנטום: ' + card.momentum);\n  L.push('  ' + (card.vwap == null ? 'VWAP: ' + NA\n    : (card.aboveVwap ? 'מחיר מעל VWAP ' : 'מחיר מתחת VWAP ') + n2(card.vwap)));\n  L.push('  EMA9 / EMA20: ' + n2(card.ema9) + ' / ' + n2(card.ema20));\n  L.push('  שוק: ' + card.market);\n\n  if (card.buyersPct != null) {\n    L.push('');\n    L.push('קונים / מוכרים: ' + card.buyersPct + '% / ' + card.sellersPct + '%');\n    var dir = [];\n    if (card.buyersTrend) dir.push('קונים ' + card.buyersTrend);\n    if (card.sellersTrend) dir.push('מוכרים ' + card.sellersTrend);\n    if (dir.length) L.push('כיוון הכוח: ' + dir.join(' · '));\n  }\n\n  if ((card.levels || []).length) {\n    L.push('');\n    L.push('רמות');\n    card.levels.forEach(function (l) { L.push('  ' + l.name + ': ' + n2(l.price)); });\n  }\n\n  L.push('');\n  L.push('החלטה');\n  L.push('  BUY NOW: ' + card.buyNow);\n  if (!card.hasMap) L.push('  מפת מחיר: '\n    + (card.marketClosed ? 'לא מוצגת כשהמסחר סגור' : 'לא מוצגת — אין setup'));\n  L.push('');\n  L.push(card.probabilityNote);\n  return L.join('\\n');\n}\n\n\n</script>\n<script>\n(function(){\n  var qs=function(s){return document.querySelector(s)};\n  var MARKET=['SPY','QQQ'];\n  var SECTOR={NVDA:'SMH',AVGO:'SMH',AMD:'SMH',MU:'SMH',AAPL:'XLK',MSFT:'XLK',GOOGL:'XLC',META:'XLC',AMZN:'XLY',TSLA:'XLY',JPM:'XLF','BRK-B':'XLF'};\n  var SENS={sensitive:{K:3,minMove:1.0,runs:2},balanced:{K:3,minMove:1.5,runs:3},quiet:{K:3,minMove:2.0,runs:3}};\n  var STATUS_TXT={QUIET:'שקט',WATCH:'מעקב',CLOSE:'קרוב',READY:'מוכן',ACTIVE:'פעיל',AVOID:'להימנע','NO DATA':'אין נתונים'};\n  var MOM_TXT={PUSHING:'דוחף',PULLBACK:'נסיגה',RECOVERY:'התאוששות',SELLING:'מכירות',FLAT:'שטוח'};\n  var STRUCT_TXT={UP:'עולה',DOWN:'יורד',RANGE:'טווח'};\n  // The row leads with the plain action so the list answers \"what do I do\"\n  // without opening anything.\n  var PLAIN={NO_SETUP:'לא לקנות · ',WAITING_FOR_ZONE:'לעקוב · ',IN_ZONE:'לחכות לאישור · ',READY_PARTIAL:'אפשר להיכנס · ',\n    WAITING_FOR_CONFIRMATION:'לחכות לאישור · ',READY_ADD:'אפשר להוסיף · ',ACTIVE:'להחזיק · ',\n    TAKE_PROFIT_AREA:'לממש חלק · ',DO_NOT_CHASE:'לא לרדוף · ',FAILED:'בוטל · '};\n\n  // store: symbol -> { rows (raw candles by date), analysis, row, days }\n  var store={}, symbols=[], marketA={}, filterStatus=null, openSym=null, feedMode='IMPORTANT', menuOpen=false;\n  var alertState={}, alertLog=[], alertsOn=true, soundOn=false, loading=false, tick=0, rangeOpen=false, sessionEnded=false;\n  var lastBoardUnix=0, boardDate=null, mktSyms=[], quotaFrozen=false, trackedSymbols=[], lastGoodDate=null;\n  var opts=function(){return SENS[qs('#sens').value]||SENS.balanced};\n\n  function bust(u){return u+(u.indexOf('?')>0?'&':'?')+'ts='+Date.now()}\n\n  // The session is kept in the browser so a page refresh resumes where it left\n  // off instead of re-downloading every bar. Without this, F5 every few seconds\n  // costs 20 requests and ~7,800 database rows each time.\n  var CACHE_KEY='bars-cache-v1';\n  function cacheLoad(){\n    try{ var raw=localStorage.getItem(CACHE_KEY); if(!raw)return null;\n      var c=JSON.parse(raw);\n      return (c&&c.date)?c:null;\n    }catch(e){return null}\n  }\n  function cacheSave(date,bySym){\n    try{ localStorage.setItem(CACHE_KEY,JSON.stringify({date:date,saved:Date.now(),bySym:bySym})); }\n    catch(e){ try{localStorage.removeItem(CACHE_KEY)}catch(e2){} }   // quota or private mode\n  }\n  // 429 and 5xx are transient: a rate cap that resets in a minute, or a\n  // deploy swapping over. Retrying beats showing the trader a dead screen.\n  function j(u,tries){\n    var left=tries==null?2:tries;\n    return fetch(bust(u),{cache:'no-store'}).then(function(r){\n      if(r.status===404)return r.json();\n      if(!r.ok){\n        if((r.status===429||r.status>=500)&&left>0)\n          return new Promise(function(res){setTimeout(res,r.status===429?4000:1500)}).then(function(){return j(u,left-1)});\n        throw new Error(r.status);\n      }\n      return r.json();\n    });\n  }\n  function fmtK(v){return v>=1e6?(v/1e6).toFixed(1)+'M':v>=1e3?Math.round(v/1e3)+'K':String(v)}\n  function toast(t){var el=qs('#toast');el.textContent=t;el.hidden=false;clearTimeout(el._t);el._t=setTimeout(function(){el.hidden=true},1800)}\n\n  // The trading date that SHOULD have data right now. Before the open, that is\n  // the previous weekday; from the open onwards it is today. Weekends fall back\n  // to Friday. Holidays are not modelled — the worst case is that a symbol is\n  // called stale on a day the market never opened, which is the safe direction.\n  function expectedSessionDate(){\n    var p=new Intl.DateTimeFormat('en-CA',{timeZone:'America/New_York',year:'numeric',month:'2-digit',\n      day:'2-digit',weekday:'short',hour:'2-digit',minute:'2-digit',hour12:false})\n      .formatToParts(new Date()).reduce(function(a,x){a[x.type]=x.value;return a},{});\n    var d=new Date(p.year+'-'+p.month+'-'+p.day+'T12:00:00Z');\n    var beforeOpen=(p.hour+':'+p.minute)<'09:30';\n    var step=function(){ d.setUTCDate(d.getUTCDate()-1); };\n    if(beforeOpen)step();\n    for(var i=0;i<7;i++){\n      var wd=d.getUTCDay();                       // 0 Sun .. 6 Sat\n      if(wd!==0&&wd!==6)break;\n      step();\n    }\n    return d.toISOString().slice(0,10);\n  }\n\n  function freshness(d){\n    if(!d||!d.rows||!d.rows.length)return 'NO DATA';\n    var last=d.rows[d.rows.length-1];\n    // A session that ended is only \"SESSION ENDED\" if it is the session we are\n    // actually in. A complete day from yesterday is stale, however tidily it\n    // finished — otherwise the 2 Sep close reads as the 3 Sep close and every\n    // level, score and market read is a day out of date.\n    var expected=expectedSessionDate();\n    if(last.date&&last.date<expected)return 'STALE';\n    if(last.time>='15:59')return 'SESSION ENDED';\n    var s=d.stale_seconds;\n    if(s==null)return 'STALE';\n    return s<=180?'LIVE':s<=600?'DELAYED':'STALE';\n  }\n\n  // ---- loading. One request per symbol; market ETFs fetched once and reused.\n  // The first load pulls the session; every refresh after it asks only for bars\n  // newer than the last one held. A full day is ~390 database rows, and the\n  // free tier allows 5 million reads a day — polling 13 symbols in full every\n  // minute would burn through that before lunch.\n  function loadSymbol(sym){\n    var st=store[sym]||(store[sym]={days:{}});\n    var have=st.rows&&st.rows.length&&st.date;\n    var last=have?st.rows[st.rows.length-1]:null;\n    var u='/day/'+sym+'?format=json'+(have&&last&&last.unix?'&since='+last.unix:'');\n    return j(u).then(function(d){\n      st.stale=d.stale_seconds; st.fresh=freshness(d.rows&&d.rows.length?d:{rows:st.rows||[],stale_seconds:d.stale_seconds});\n      if(d.incremental&&st.date===d.date){\n        // append only what is new; never re-download what we already have\n        var known={}; (st.rows||[]).forEach(function(r){known[r.unix]=1});\n        (d.rows||[]).forEach(function(r){ if(!known[r.unix]) st.rows.push(r); });\n        st.rows.sort(function(a,b){return a.unix-b.unix});\n      } else {\n        st.date=d.date; st.rows=d.rows||[];\n      }\n      st.days[st.date]=st.rows;\n      st.A=st.rows.length?analyze(st.rows,opts()):null;\n      return st;\n    },function(){ if(!st.rows||!st.rows.length){st.fresh='NO DATA';st.A=null;} return st; });\n  }\n  function mkLabel(sym){return marketCtxFor(sym).label}\n  // 'Market: Bullish' derived from yesterday's SPY is a statement about\n  // yesterday. If the benchmarks are not on the current session, the regime is\n  // unavailable and must not feed scoring.\n  function benchmarksStale(sym){\n    var list=MARKET.concat(SECTOR[sym]?[SECTOR[sym]]:[]);\n    var seen=0,stale=0;\n    list.forEach(function(s){\n      var b=store[s]; if(!b)return;\n      seen++;\n      if(b.fresh==='STALE'||b.fresh==='NO DATA')stale++;\n    });\n    return seen===0||stale>0;\n  }\n  function marketCtxFor(sym){\n    if(benchmarksStale(sym))\n      return {label:'Unavailable',stale:true,parts:[],reason:'נתוני המדדים אינם מהסשן הנוכחי'};\n    var list=MARKET.concat(SECTOR[sym]?[SECTOR[sym]]:[]);\n    return marketContext(list.map(function(s){return marketA[s]||null}));\n  }\n  // One place builds the snapshot, so every caller gets the same object.\n  function buildSnap(s,now){\n    var st=store[s]; if(!st)return null;\n    // The measured age of the newest bar goes in, so the state builder can\n    // refuse to give live guidance off data that stopped arriving.\n    st.snap=buildTickerState(s,st.A,{market:mkLabel(s),marketCtx:marketCtxFor(s),freshness:st.fresh,\n      daily:st.daily,baseline:st.baseline,calibration:st.calibration,staleSeconds:st.stale,\n      sessionEnded:st.fresh==='STALE'?false:(sessionEnded||st.fresh==='SESSION ENDED'),\n      now:now||Date.now(),date:st.date});\n    // The row is part of the snapshot, never a separate calculation.\n    st.row=st.snap.row||{symbol:s,status:st.snap.status,why:'אין נתונים',score:0,price:null,\n      structure:'RANGE',momentum:'FLAT',nearestR:Infinity,freshness:st.fresh||'NO DATA',urgency:9};\n    st.row.state=st.snap; st.row.status=st.snap.status; st.row.score=st.snap.score||0;\n    if(!st.snap.valid&&st.snap.violations&&st.snap.violations[0]&&st.snap.violations[0].code!=='NO_DATA'){\n      st.row.why='מצב המודל לא עקבי'; st.row.status='AVOID';\n    }\n    return st.snap;\n  }\n  function recompute(){\n    sessionEnded=symbols.length>0&&symbols.every(function(s){return !store[s]||store[s].fresh==='SESSION ENDED'||store[s].fresh==='NO DATA'});\n    // Any symbol whose newest bar predates the current session is stale, and a\n    // stale symbol can never be a \"close\" candidate.\n    symbols.forEach(function(s){\n      var b=store[s]; if(!b||!b.row)return;\n      if(b.fresh==='STALE'&&b.row.status!=='NO DATA'){\n        b.row.status='AVOID';\n        b.row.why='נתונים מסשן קודם — לא לסחור';\n      }\n    });\n    // \"The session ended\" at 05:30 is wrong: it has not started. Name the phase.\n    var ph=marketPhase(), sess=qs('#sess');\n    var showBanner=sessionEnded||ph==='pre'||ph==='weekend'||ph==='post';\n    sess.hidden=!showBanner;\n    if(showBanner){\n      var shown=boardDate?(' · מוצגים נתוני '+boardDate.slice(5).replace('-','/')):'';\n      sess.textContent=ph==='pre'?('המסחר טרם נפתח'+shown+' — לא איתות חי')\n        :ph==='weekend'?('סוף שבוע'+shown+' — לא איתות חי')\n        :('המסחר הסתיים'+shown+' — התצוגה היא מצב בסגירה, לא איתות חי');\n    }\n    var now=Date.now();\n    symbols.forEach(function(s){\n      // A symbol the board returned nothing for still gets a row, so it shows\n      // as \"no data\" instead of silently disappearing from the list.\n      var st=store[s]||(store[s]={days:{},rows:[],fresh:'NO DATA'});\n      if(!st.rows||!st.rows.length){st.fresh=st.fresh||'NO DATA';st.A=null}\n      if(st.rows&&st.rows.length)st.A=analyze(st.rows,opts());\n      // One snapshot per symbol per refresh. The row and the detail sheet both\n      // read this object, so a score can never differ between them.\n      buildSnap(s,now);\n    });\n    render();\n  }\n\n  function refresh(){ return loadAllThenRepair()\n    .then(function(){ if(openSym&&!sessionEnded)loadBook(openSym);\n      // The buy card rides the existing refresh: no second polling loop.\n      if(openSym)(buyOpen?drawBuy():drawDetail()); }); }\n  // One request for every symbol. `since` is the newest bar we already hold,\n  // so a refresh only carries what changed.\n  // Which symbols the board covers. In scan mode the whole universe is\n  // requested; the server can only reach ~40 archive symbols per call, so the\n  // ones it names in not_fetched are asked for in follow-up calls until none\n  // are left.\n  function scope(){ return 'tracked' }\n  // The session that should have data now. Before the open this is yesterday,\n  // so the board asks for a day that exists instead of an empty today.\n  function marketPhase(){\n    var p=new Intl.DateTimeFormat('en-US',{timeZone:'America/New_York',weekday:'short',\n      hour:'2-digit',minute:'2-digit',hour12:false}).formatToParts(new Date())\n      .reduce(function(a,x){a[x.type]=x.value;return a},{});\n    var hm=p.hour+':'+p.minute;\n    if(p.weekday==='Sat'||p.weekday==='Sun')return 'weekend';\n    if(hm<'09:30')return 'pre';\n    if(hm<'16:00')return 'open';\n    return 'post';\n  }\n  // The data checks itself and reports in the header. Every failure found by\n  // hand today was invisible to the unit tests, because those check the code\n  // against assumptions — these check properties that cannot be true whatever\n  // the assumptions were.\n  function runAudit(){\n    return j('/audit').then(function(d){\n      var el=qs('#audit'); if(!el)return;\n      el.hidden=false;\n      var n=(d.findings||[]).length;\n      el.className='audit '+(d.verdict==='CRITICAL'?'bad':d.verdict==='HIGH'?'warn':'ok');\n      el.textContent=d.verdict==='OK'?'נתונים תקינים':(n+' בעיות בנתונים');\n      el.onclick=function(){\n        var box=qs('#auditbox'); if(!box)return;\n        box.hidden=!box.hidden;\n        box.innerHTML=n?('<b>בדיקות שנכשלו על הנתונים עצמם</b>'+(d.findings||[]).map(function(f){\n          return '<div class=\"f\"><b>'+f.symbol+'</b> · '+f.check+'<br>'+f.detail+'</div>'}).join('')\n          +'<div class=\"f\" style=\"color:var(--muted)\">אלה תכונות שלא יכולות להיות נכונות — אל תסחור עד שהן נקיות.</div>')\n          :'<b>כל הבדיקות עברו על הנתונים שנשמרו בפועל.</b>';\n      };\n    },function(){});\n  }\n  // The board is incremental: it returns what is NEW since the cursor. If the\n  // cursor was restored from a cache that never held the open — a tab opened at\n  // 11:02, a cleared store, a rollover — the first 92 minutes are never asked\n  // for again and the session is permanently short. Detect that and fetch the\n  // whole day once for that symbol.\n  var repaired={};\n  function repairFromOpen(){\n    var date=boardDate; if(!date)return Promise.resolve();\n    // Iterate what is actually IN the store, not the display list. The display\n    // list is the tracked symbols minus the market strip, so benchmarks were\n    // repaired and the twenty symbols on screen were not — the exact opposite\n    // of what was needed.\n    var need=Object.keys(store).filter(function(s){\n      var st=store[s]; if(!st||!st.rows||!st.rows.length)return false;\n      if(repaired[s+':'+date])return false;\n      return st.rows[0].time>'09:35';\n    }).slice(0,8);\n    if(!need.length)return Promise.resolve();\n    return need.reduce(function(p,s){\n      return p.then(function(){\n        repaired[s+':'+date]=1;\n        return j('/day/'+s+'/'+date+'?format=json').then(function(d){\n          var st=store[s]; if(!st||!d.rows||!d.rows.length)return;\n          var seen=st.seen=st.seen||{};\n          d.rows.forEach(function(r){\n            if(!r.date||!r.unix)return;\n            var k=r.date+':'+r.unix;\n            if(seen[k])return;\n            seen[k]=1; st.rows.push(r);\n          });\n          st.rows=sessionRows(st,st.date||date);\n          st.days[st.date||date]=st.rows;\n          st.A=st.rows.length?analyze(st.rows,opts()):null;\n        },function(){});\n      });\n    },Promise.resolve());\n  }\n  function loadBoard(){\n    var have=lastBoardUnix||0;\n    // Outside the session, ask for the most recent day that actually has bars.\n    var phase=marketPhase();\n    var forDate=(phase==='open'||have)?null:(lastGoodDate||null);\n    var url='/board?'+(have?'since='+have+'&':'')+(forDate?'date='+forDate+'&':'')\n      +(scope()==='universe'?'universe=1&':'');\n    url=url.replace(/[?&]$/,'');\n    return j(url).then(function(d){\n      if(scope()==='universe'&&d.not_fetched&&d.not_fetched.length){\n        // walk the remainder, one batch per request\n        var rest=d.not_fetched.slice();\n        var next=function(){\n          if(!rest.length)return Promise.resolve(d);\n          var batch=rest.splice(0,40);\n          return j('/board?date='+d.date+'&symbols='+batch.join(',')+(have?'&since='+have:'')).then(function(more){\n            (more.rows||[]).forEach(function(r){d.rows.push(r)});\n            d.count=(d.count||0)+(more.count||0);\n            if(more.last_bar_unix)d.last_bar_unix=Math.max(d.last_bar_unix||0,more.last_bar_unix);\n            // a follow-up call defers too; put that back on the queue\n            (more.not_fetched||[]).forEach(function(s){ if(batch.indexOf(s)>=0)rest.push(s) });\n            return next();\n          });\n        };\n        return next().then(function(){return absorb(d)});\n      }\n      return absorb(d);\n    });\n  }\n  function absorb(d){\n    return Promise.resolve(d).then(function(d){\n      if(d.from_snapshot){toast('מכסת המסד נגמרה — מוצג העותק השמור מ-'+String(d.snapshot_saved_at||'').slice(11,16));}\n      else if(d.count===0&&quotaFrozen){toast('אין נרות חדשים: מכסת הכתיבות נגמרה. מוצג המצב האחרון שנשמר.');}\n      else if(d.frozen){toast('מכסת המסד נגמרה ואין עותק שמור');return d}\n      boardDate=d.date;\n      // Nothing for the requested day and we asked for today: find the most\n      // recent day that does have bars and show that, labelled.\n      if((d.count===0)&&!lastBoardUnix&&!lastGoodDate){\n        return j('/days/'+(d.symbols&&d.symbols[0]||'SPY')).then(function(dd){\n          var days=(dd.days||[]).map(function(x){return x.date}).filter(function(x){return x<d.date});\n          if(!days.length)return d;\n          lastGoodDate=days[0];\n          return loadBoard();\n        },function(){ return d; });\n      }\n      // In scan mode the board's own symbol list is the truth: it carries the\n      // universe. In tracked mode it is what D1 tracks.\n      if(scope()==='universe'&&d.symbols&&d.symbols.length){\n        symbols=d.symbols.filter(function(s){return MARKET.indexOf(s)<0});\n      } else if(trackedSymbols.length){ symbols=trackedSymbols.slice(); }\n      // New symbols in the board that are not yet in the cache need a full load.\n      // Without this, lastBoardUnix skips their entire history on the first call.\n      var newSyms=(d.symbols||[]).filter(function(s){return !store[s]||!store[s].rows||!store[s].rows.length});\n      if(newSyms.length&&lastBoardUnix>0){\n        newSyms.forEach(function(s){store[s]=null});   // will be filled from the rows below\n        lastBoardUnix=0;                               // re-fetch from the beginning next time\n      }\n      (d.rows||[]).forEach(function(r){\n        var st=store[r.symbol]||(store[r.symbol]={days:{}});\n        // File by the bar's own date, not the date the board was asked for, and\n        // key the dedup ledger on date+timestamp so a rollover cannot let the\n        // same minute in twice under a different day.\n        if(!r.date||!r.unix)return;\n        if(st.date!==r.date){st.date=r.date;st.rows=[];st.days[r.date]=st.rows}\n        st.rows=st.rows||[];\n        if(!st.seen)st.seen={};\n        var key=r.date+':'+r.unix;\n        if(st.seen[key])return;\n        st.seen[key]=1; st.rows.push(r);\n      });\n      if(d.last_bar_unix)lastBoardUnix=d.last_bar_unix;\n      symbols.forEach(function(s){\n        var st=store[s]; if(!st||!st.rows)return;\n        // Filter to one session — but NEVER let the filter empty a symbol that\n        // had bars. If it returns nothing, the date we filtered on is wrong,\n        // not the data, and wiping the rows blanks the symbol on screen.\n        var oneSession=sessionRows(st,st.date);\n        if(oneSession.length){ st.rows=oneSession; st.days[st.date]=st.rows; }\n        else if(st.rows&&st.rows.length){\n          // fall back to the date the bars themselves carry\n          var realDate=st.rows[st.rows.length-1].date;\n          if(realDate&&realDate!==st.date){\n            st.date=realDate;\n            var byOwn=sessionRows(st,realDate);\n            if(byOwn.length){ st.rows=byOwn; st.days[realDate]=st.rows; }\n          }\n        }\n        st.fresh=freshness({rows:st.rows,stale_seconds:Math.max(0,Math.floor(Date.now()/1000)-((st.rows.length?st.rows[st.rows.length-1].unix:0)+60))});\n        // A bar's stamp is its START; it is complete a minute later.\n        st.stale=st.rows.length?Math.max(0,Math.floor(Date.now()/1000)-(st.rows[st.rows.length-1].unix+60)):null;\n        st.A=st.rows.length?analyze(st.rows,opts()):null;\n      });\n      return d;\n    });\n  }\n  // Every completed load repairs a short session, wherever the load came from.\n  function loadAllThenRepair(){ return loadAll().then(function(){return repairFromOpen()}).then(function(){recompute();render()}); }\n  function loadAll(){\n    if(loading)return Promise.resolve();       // never overlap two refreshes\n    loading=true;\n    qs('#prog').textContent='טוען…';\n    return j('/').then(function(h){\n      symbols=(h.tracked||[]).filter(function(s){return MARKET.indexOf(s)<0});\n      trackedSymbols=symbols.slice();\n      var mkt=MARKET.concat(['SMH','XLK','XLC','XLY','XLF'].filter(function(e){return symbols.some(function(s){return SECTOR[s]===e})}));\n      mktSyms=mkt;\n      return Promise.all(mkt.map(function(s){\n        return loadSymbol(s).then(function(stx){\n          marketA[s]=stx.A||null; if(marketA[s])marketA[s].symbol=s;\n        },function(){marketA[s]=null});\n      }));\n    }).then(function(){\n      return loadBoard();\n    }).then(function(){\n      mktSyms.forEach(function(s){ var st=store[s]; if(st&&st.A)marketA[s]=st.A, marketA[s].symbol=s; });\n      qs('#prog').textContent='';loading=false;recompute();fireAlerts();\n      // persist for the next page load\n      var bySym={}; symbols.concat(mktSyms).forEach(function(s){ var st=store[s]; if(st&&st.rows&&st.rows.length)bySym[s]=st.rows; });\n      cacheSave(boardDate,bySym);\n      j('/usage').then(drawQuota,function(){});\n    },\n      function(e){loading=false;throw e});\n  }\n\n  // ---- market strip + counts\n  function drawFrozenBanner(){\n    var el=qs('#frozen'); if(!el)return;\n    if(!quotaFrozen){el.hidden=true;return}\n    el.hidden=false;\n    var now=new Date(), left=(23-now.getUTCHours())+' שע׳ ו-'+(60-now.getUTCMinutes())+' דק׳';\n    el.innerHTML='<b>מכסת מסד הנתונים נגמרה — אין נתונים חיים</b>'+\n      '<div>מה שמוצג הוא המצב האחרון שנשמר, לא איתות חי. זה לא סוף מסחר.</div>'+\n      '<div>המכסה מתאפסת בחצות UTC — בעוד '+left+'.</div>';\n  }\n  function drawQuota(u){\n    var el=qs('#quota'); if(!u||u.error){el.hidden=true;return}\n    el.hidden=false;\n    var TIER={normal:['',''],warn:['color:var(--s-watch);font-weight:600',' — מתקרב'],\n      frugal:['color:var(--s-close);font-weight:600',' — מצב חיסכון, רענון מואט'],\n      frozen:['color:var(--down);font-weight:700',' — מוקפא']};\n    var t=TIER[u.tier]||TIER.normal;\n    // Reads and writes fail differently and the reason matters: a spent write\n    // budget means no NEW bars today, while reading history still works.\n    var why='';\n    if(u.write_tier==='frozen'&&u.read_tier!=='frozen')\n      why=' — מכסת הכתיבות נגמרה: אין נרות חדשים היום, היסטוריה נקראת כרגיל';\n    else if(u.read_tier==='frozen')\n      why=' — מכסת הקריאות נגמרה, מוגש מהעותק השמור';\n    t=[t[0],why||t[1]];\n    quotaFrozen=(u.write_tier==='frozen');\n    // A percentage is not an answer. The only thing worth showing is whether\n    // the data will keep arriving, and what to do if not.\n    var worst=Math.max(u.read_pct||0,u.write_pct||0);\n    var msg=(u.read_tier==='frozen'||u.write_tier==='frozen')\n        ? 'המכסה נגמרה — אין נרות חדשים היום. חוזר לעבוד ב-03:00.'\n      : worst>=85 ? 'המכסה כמעט נגמרה — אולי ייעצר היום. מתאפס ב-03:00.'\n      : worst>=60 ? 'המכסה בסדר להיום.'\n      : 'המכסה בסדר.';\n    el.innerHTML='<span style=\"'+t[0]+'\">'+msg+'</span>';\n    // In the low tiers the page stops polling hard so it cannot make things worse.\n    if(u.tier==='frugal'&&parseInt(qs('#every').value,10)<120)qs('#every').value='120';\n    if(u.tier==='frozen')qs('#every').value='0';\n  }\n  function drawHead(rows){\n    var strip=MARKET.concat(Object.keys(SECTOR).map(function(k){return SECTOR[k]}).filter(function(e,i,a){return a.indexOf(e)===i&&marketA[e]}));\n    strip=strip.filter(function(e,i,a){return a.indexOf(e)===i});\n    // The benchmark chips must not show a VWAP state from an older session\n    // either — that is the same claim in miniature.\n    var benchStale=MARKET.some(function(s){var b=store[s];return !b||b.fresh==='STALE'||b.fresh==='NO DATA'});\n    var mk=benchStale?{label:'Unavailable',parts:[]}:marketContext(MARKET.map(function(s){return marketA[s]||null}));\n    var html=strip.map(function(s){var A=marketA[s],b0=store[s];\n      if(!A||!b0||b0.fresh==='STALE'||b0.fresh==='NO DATA')return '<span class=\"chip\">'+s+' —</span>';\n      var b=A.state.bar;return '<span class=\"chip '+(b.aboveVwap?'up':'down')+'\">'+s+' <b>'+(b.aboveVwap?'↑':'↓')+'VWAP</b></span>'}).join('');\n    var mkTxt=mk.label==='Bullish'?'שורי':mk.label==='Bearish'?'דובי':mk.label==='Neutral'?'ניטרלי':'לא זמין — נתונים לא מהסשן הנוכחי';\n    html+='<span class=\"chip\">שוק: <b>'+mkTxt+'</b></span>';\n    qs('#strip').innerHTML=html;\n    var order=['READY','CLOSE','ACTIVE','WATCH','QUIET','AVOID'];\n    qs('#counts').innerHTML=order.map(function(k){\n      var n=rows.filter(function(r){return r.status===k}).length;\n      return '<button type=\"button\" class=\"cnt bg-'+k+(filterStatus===k?' sel':'')+'\" data-k=\"'+k+'\" data-on=\"'+n+'\">'+STATUS_TXT[k]+' '+n+'</button>';\n    }).join('');\n    [].forEach.call(document.querySelectorAll('.cnt'),function(b){b.onclick=function(){filterStatus=filterStatus===b.dataset.k?null:b.dataset.k;render()}});\n  }\n\n  // ---- sparkline: last 45 bars + tactical levels\n  function spark(st,row){\n    var bars=(st.A?st.A.bars:[]).slice(-45); if(bars.length<2)return '';\n    var W=74,H=26,lo=Infinity,hi=-Infinity;\n    bars.forEach(function(b){lo=Math.min(lo,b.low);hi=Math.max(hi,b.high)});\n    var lv=[row&&row.support?row.support.price:null,row&&row.resistance?row.resistance.price:null];\n    lv.forEach(function(p){if(p!=null){lo=Math.min(lo,p);hi=Math.max(hi,p)}});\n    var sp=(hi-lo)||1, y=function(v){return 2+(hi-v)/sp*(H-4)}, x=function(i){return i/(bars.length-1)*(W-3)+1.5};\n    var d=bars.map(function(b,i){return (i?'L':'M')+x(i).toFixed(1)+' '+y(b.close).toFixed(1)}).join(' ');\n    var out='<svg class=\"spark\" viewBox=\"0 0 '+W+' '+H+'\" aria-hidden=\"true\">';\n    if(lv[0]!=null)out+='<line class=\"lvl sup\" x1=\"0\" x2=\"'+W+'\" y1=\"'+y(lv[0]).toFixed(1)+'\" y2=\"'+y(lv[0]).toFixed(1)+'\"/>';\n    if(lv[1]!=null)out+='<line class=\"lvl res\" x1=\"0\" x2=\"'+W+'\" y1=\"'+y(lv[1]).toFixed(1)+'\" y2=\"'+y(lv[1]).toFixed(1)+'\"/>';\n    out+='<path class=\"line\" d=\"'+d+'\"/><circle class=\"dot\" cx=\"'+x(bars.length-1).toFixed(1)+'\" cy=\"'+y(bars[bars.length-1].close).toFixed(1)+'\" r=\"1.8\"/></svg>';\n    return out;\n  }\n\n  function render(){\n    var rows=symbols.map(function(s){return store[s]&&store[s].row}).filter(Boolean);\n    drawHead(rows);\n    var list=sortRadar(rows,qs('#sort').value);\n    if(filterStatus)list=list.filter(function(r){return r.status===filterStatus});\n    if(!list.length){qs('#rows').innerHTML='<div class=\"empty\">אין מניות להצגה.</div>';return}\n    qs('#rows').innerHTML=list.map(function(r){\n      var st=store[r.symbol];\n      // The sign of R flipped meaning between panels. Say the direction in\n      // words instead: a support BELOW price is \"above the support\".\n      var absR=function(x){var a=Math.abs(x);return a<0.05?'על הרמה':(a<1?a.toFixed(2):a.toFixed(1))+'R'};\n      var loc=r.nearestR===Infinity?'אמצע טווח':\n        (r.support&&Math.abs(r.support.r)<=Math.abs(r.resistance?r.resistance.r:99))\n          ?absR(r.support.r)+' מעל התמיכה'\n        :r.resistance?absR(r.resistance.r)+' מתחת להתנגדות':'אמצע טווח';\n      var stale=r.freshness==='STALE'||r.freshness==='NO DATA';\n      // With no data there is nothing to describe. Printing \"range / flat /\n      // mid-range / 0-10\" against an empty symbol looks like a real reading.\n      var noData=r.freshness==='NO DATA'||!r.price;\n      if(noData) return '<div class=\"row NODATA stale\" data-s=\"'+r.symbol+'\" tabindex=\"0\" role=\"button\">'+\n        '<div><div class=\"sym\">'+r.symbol+'</div><div class=\"px num\">—</div></div>'+\n        '<div class=\"mid\"><span class=\"st bg-NODATA\">אין נתונים</span>'+\n          '<div class=\"why\">לא התקבלו נרות עבור הסימבול הזה</div>'+\n          '<div class=\"meta\"><span>מבנה <b>—</b></span><span>מומנטום <b>—</b></span>'+\n          '<span>רמות —</span><span>נפח <b>—</b></span></div></div>'+\n        '<div class=\"rt\"><div class=\"score\">—</div></div></div>';\n      return '<div class=\"row '+r.status.replace(' ','')+(stale?' stale':'')+'\" data-s=\"'+r.symbol+'\" tabindex=\"0\" role=\"button\">'+\n        '<div><div class=\"sym\">'+r.symbol+'</div><div class=\"px num\">'+(r.price?r.price.toFixed(2):'—')+'</div></div>'+\n        '<div class=\"mid\"><span class=\"st bg-'+r.status.replace(' ','')+'\">'+STATUS_TXT[r.status]+'</span>'+\n          '<div class=\"why\">'+(r.state&&r.state.stale?'⚠ נתונים ישנים · ':quotaFrozen?'עותק שמור · ':r.sessionEnded?'מצב בסגירה · ':'')+(PLAIN[r.plan&&r.plan.state]||'')+r.why+'</div>'+\n          '<div class=\"meta\"><span>מבנה <b>'+(STRUCT_TXT[r.structure]||r.structure)+'</b></span><span>מומנטום <b>'+(MOM_TXT[r.momentum]||r.momentum)+'</b></span>'+\n          '<span>'+loc+'</span><span>נפח <b class=\"num\">'+(r.volx?r.volx.toFixed(1)+'×':'—')+'</b>'+\n          (r.auction?' <small style=\"opacity:.8\">סגירה — לא נספר</small>':'')+'</span>'+\n          (stale?'<span style=\"color:var(--down)\">נתונים ישנים</span>':'')+'</div>'+\n        (r.pressure&&!(r.state&&r.state.stale)?'<div class=\"fl\"><span class=\"'+(r.pressure.buyPct>=58?'up':r.pressure.buyPct<=42?'dn':'')+'\">קונים <b>'+r.pressure.buyPct+'%</b> · מוכרים <b>'+r.pressure.sellPct+'%</b></span>'+\n          (r.pressure.buyersTrend!=='ללא שינוי'?' · קונים '+r.pressure.buyersTrend:'')+'</div>':'')+\n        '</div>'+\n        '<div class=\"rt\"><div class=\"score\">'+r.score+'<small>/10</small></div>'+spark(st,r)+'</div></div>';\n    }).join('');\n    [].forEach.call(document.querySelectorAll('.row'),function(el){\n      el.onclick=function(){openDetail(el.dataset.s)};\n      el.onkeydown=function(e){if(e.key==='Enter'||e.key===' '){e.preventDefault();openDetail(el.dataset.s)}};\n    });\n  }\n\n  // ---- detail panel\n  var EV={\n    strong_breakout:['פריצה חזקה','g'],breakout:['פריצה','g'],testing:['בדיקת התנגדות','y'],rejected:['פריצה נדחתה','r'],\n    failed:['פריצה כשלה','r'],retest_hold:['בדיקה חוזרת החזיקה','g'],support_break:['שבירת תמיכה','r'],\n    structure_break:['שבירת מבנה','r'],hh_confirmed:['HH אושר','g'],hl_confirmed:['HL אושר','g'],\n    lh_confirmed:['LH אושר','r'],ll_confirmed:['LL אושר','r'],volume_spike:['קפיצת נפח','b'],\n    momentum:['נר מומנטום','b'],trend_change:['שינוי מגמה','o'],outside_bar:['נר outside — לא מבנה','n'],\n    level_reclaim:['רמה נכבשה מחדש','g'],vwap_reclaim:['VWAP נכבש מחדש','g'],vwap_loss:['VWAP אבד','r']\n  };\n  var IMPORTANT={strong_breakout:1,breakout:1,failed:1,rejected:1,support_break:1,structure_break:1,\n    hh_confirmed:1,hl_confirmed:1,lh_confirmed:1,ll_confirmed:1,trend_change:1,retest_hold:1,level_reclaim:1};\n  function evLine(e){\n    var m=EV[e.type]; if(!m)return null;\n    var s=m[0]; if(e.level!=null)s+=' '+e.level.toFixed(2);\n    if(e.side)s+=e.side==='bull'?' (שורית)':' (דובית)';\n    if(e.type==='trend_change')s+=' '+(STRUCT_TXT[e.from]||e.from)+' → '+(STRUCT_TXT[e.to]||e.to);\n    if(e.confirmed===true)s+=' · אושר'; else if(e.confirmed===false)s+=' · לא אושר'; else if(e.confirmed===null)s+=' · ממתין';\n    if(e.wick!=null)s+=' · פתיל '+e.wick+'%';\n    if(e.volx!=null)s+=' ×'+e.volx.toFixed(2);\n    return {text:s,cls:m[1]};\n  }\n  function openDetail(sym){\n    // Opening a different symbol starts on the detail view, not on whichever\n    // tab happened to be showing for the previous one.\n    buyOpen=false;\n    openSym=sym; menuOpen=false;\n    var st=store[sym]; if(st){st.viewDate=st.viewDate||st.date;}\n    qs('#sheet').hidden=false; document.body.style.overflow='hidden';\n    drawDetail();\n    // Which historical days exist is only needed once a symbol is opened.\n    if(st&&!st.dates)j('/days/'+sym).then(function(d){\n      st.dates=(d.days||[]).map(function(x){return x.date});\n      if(openSym===sym)drawDetail();\n      loadHistory(sym);\n    },function(){});\n    else if(st&&!st.hist)loadHistory(sym);\n  }\n  // ONE place that turns everything loaded for a symbol into clean sessions:\n  // grouped by each bar's own date, deduplicated, sorted. Every consumer — the\n  // daily context, the gap, VWAP, the pack — must go through this, because a\n  // set that mixes two sessions produces a VWAP below the session low and a gap\n  // measured from the wrong day's open.\n  // A bar identifies itself by date and timestamp; when a source omits the\n  // timestamp the time-of-day serves. Dropping bars for a missing field would\n  // lose real data silently, which is the failure mode this whole exercise is\n  // about.\n  function barKey(x){ return x.date+':'+(x.unix!=null?x.unix:x.time); }\n  function byTime(a,b){\n    if(a.unix!=null&&b.unix!=null)return a.unix-b.unix;\n    return a.time<b.time?-1:a.time>b.time?1:0;\n  }\n  function sessionsOf(st){\n    var byDay={},seen={};\n    var pools=[st.rows||[]].concat(st.hist||[]);\n    Object.keys(st.days||{}).forEach(function(d){pools.push(st.days[d]||[])});\n    pools.forEach(function(r){\n      (r||[]).forEach(function(x){\n        if(!x||!x.date||(x.unix==null&&!x.time))return;\n        var k=barKey(x);\n        if(seen[k])return;\n        seen[k]=1;\n        (byDay[x.date]=byDay[x.date]||[]).push(x);\n      });\n    });\n    return Object.keys(byDay).sort().map(function(d){\n      return byDay[d].sort(byTime);\n    });\n  }\n  // The bars of ONE session, never a mixture. analyze() computes VWAP over\n  // whatever it is given, so a contaminated array yields a VWAP outside the\n  // session's own range.\n  function sessionRows(st,date){\n    var out=[],seen={};\n    var pools=[st.rows||[]].concat(st.hist||[]);\n    Object.keys(st.days||{}).forEach(function(d){pools.push(st.days[d]||[])});\n    pools.forEach(function(r){(r||[]).forEach(function(x){\n      if(!x||x.date!==date||(x.unix==null&&!x.time))return;\n      var k=x.unix!=null?x.unix:x.time;\n      if(seen[k])return;\n      seen[k]=1; out.push(x);\n    })});\n    return out.sort(byTime);\n  }\n\n  // Multi-day layer: the five most recent sessions, fetched once per symbol\n  // when its detail is first opened. Auto-refresh never re-fetches them.\n  function loadHistory(sym){\n    var st=store[sym]; if(!st||st.histLoading||st.hist)return Promise.resolve();\n    var want=(st.dates||[]).slice(0,6).filter(function(d){return d!==st.date}).slice(0,5).sort();\n    if(!want.length){st.hist=[];return Promise.resolve()}\n    st.histLoading=true;\n    return Promise.all(want.map(function(d){\n      if(st.days[d])return Promise.resolve(st.days[d]);\n      return j('/day/'+sym+'/'+d+'?format=json').then(function(x){st.days[d]=x.rows||[];return st.days[d]},function(){return null});\n    })).then(function(list){\n      st.hist=list.filter(function(r){return r&&r.length});\n      st.baseline=volumeBaseline(st.hist);\n      // Sessions grouped by their own date, and the current trading date named\n      // explicitly — dailyContext otherwise treats the LAST ARRAY ROW as today,\n      // which is how the gap came to be measured from 09/02's open.\n      st.daily=dailyContext(sessionsOf(st),{todayDate:st.date});\n      st.calibration=calibrate(st.hist,{});\n      st.histLoading=false;\n      buildSnap(sym);                       // the daily layer is part of the state\n      if(openSym===sym)drawDetail();\n    },function(){st.histLoading=false});\n  }\n  // Real depth, fetched only for the symbol on screen. Cboe venues only.\n  function loadBook(sym){\n    var st=store[sym]; if(!st||st.bookLoading)return Promise.resolve();\n    st.bookLoading=true;\n    return j('/book/'+sym).then(function(d){\n      if(st.book&&st.book.summary)st.bookPrev=st.book.summary;   // for the 30-60s depth delta\n      st.book=d;st.bookLoading=false;if(openSym===sym)drawDetail()},\n      function(){st.book={error:true};st.bookLoading=false});\n  }\n  function loadDay(sym,date){\n    var st=store[sym];\n    if(st.days[date]){st.viewDate=date;drawDetail();return Promise.resolve()}\n    return j('/day/'+sym+'/'+date+'?format=json').then(function(d){st.days[date]=d.rows||[];st.viewDate=date;drawDetail()},\n      function(){toast('לא הצלחתי לטעון '+date)});\n  }\n  function closeDetail(){qs('#sheet').hidden=true;document.body.style.overflow='';openSym=null}\n\n  function levelRow(label,L,unit){\n    if(!L)return '<div class=\"l\"><span>'+label+'</span><b>—</b><i></i></div>';\n    var rr=L.r!=null?L.r:(L.dist?L.dist.atr:null);\n    return '<div class=\"l\"><span>'+label+'</span><b class=\"num\">'+L.price.toFixed(2)+'</b><i>'+(rr!=null?fmtR(rr)+' · ':'')+(L.why||L.reason||'')+'</i></div>';\n  }\n  function drawDetail(){\n    // The buy card is a view of the same symbol; a refresh must not throw the\n    // trader back to the full screen.\n    if(buyOpen)return drawBuy();\n    var st=store[openSym], p=qs('#panel');\n    var vd=st&&(st.viewDate||st.date), hist=st&&vd&&vd!==st.date;\n    var A=st&&(hist?(st.days[vd]&&st.days[vd].length?analyze(st.days[vd],opts()):null):st.A);\n    var r=st&&(hist?(A?radarRow(openSym,A,marketCtxFor(openSym),'SESSION ENDED'):null):st.row);\n    if(!A||!r){p.innerHTML='<div class=\"grab\"></div><div class=\"empty\"><b>'+openSym+'</b><br>אין נתונים.</div>'+backBtn();bind();return}\n    var b=A.state.bar, T=r.tactical, BL=r.bl, first=A.bars[0], curRows=hist?st.days[vd]:(st.rows||[]);\n    var chg=(b.close-first.open)/first.open*100;\n    var mk=marketCtxFor(openSym);\n    var mkTxt=mk.label==='Bullish'?'שורי':mk.label==='Bearish'?'דובי':mk.label==='Neutral'?'ניטרלי':'לא זמין';\n    var frTxt={LIVE:'חי',DELAYED:'מתעכב',STALE:'ישן','SESSION ENDED':'סשן הסתיים','NO DATA':'אין נתונים'}[r.freshness]||r.freshness;\n    var P=r.plan;\n    var planHtml=P&&P.state!=='NO_SETUP'?\n      '<div class=\"plan '+P.state+'\"><div class=\"act\"><b>'+P.action+'</b><span class=\"st2\">'+P.state+'</span></div>'+\n      '<div class=\"hl\">'+P.headline+(P.levelWhy?' · '+P.levelWhy:'')+'</div>'+\n      (P.waitPrice?'<div class=\"wait\"><small>'+(P.state==='DO_NOT_CHASE'?'מחכים לפולבק':P.state==='READY_ADD'||P.state==='WAITING_FOR_CONFIRMATION'?'מחיר האישור':P.state==='ACTIVE'||P.state==='TAKE_PROFIT_AREA'?'יעד':'מחיר שמחכים לו')+'</small><b class=\"num\">'+P.waitPrice+'</b></div>':'')+\n      '<div class=\"steps\">'+\n        '<div><span>השלב הבא</span><span style=\"opacity:1\">'+(P.nextStep||'—')+'</span></div>'+\n        (P.ifConfirmed?'<div><span>אם מאושר</span><span style=\"opacity:1\">'+P.ifConfirmed+'</span></div>':'')+\n        (P.ifFailed?'<div><span>אם נכשל</span><span style=\"opacity:1\">'+P.ifFailed+'</span></div>':'')+\n        (P.invalidation!=null?'<div><span>ביטול</span><span style=\"opacity:1\" class=\"num\">'+P.invalidation.toFixed(2)+'</span></div>':'')+\n        (P.target!=null?'<div><span>יעד</span><span style=\"opacity:1\" class=\"num\">'+P.target.toFixed(2)+'</span></div>':'')+\n      '</div></div>'\n      :'<div class=\"plan NO_SETUP\"><div class=\"act\"><b>המתן</b></div><div class=\"hl\">'+(P?P.reason:'אין נתונים')+'</div></div>';\n    var snap=hist?buildTickerState(openSym,A,{market:mkLabel(openSym),marketCtx:marketCtxFor(openSym),\n      freshness:'SESSION ENDED',daily:st.daily,baseline:st.baseline,calibration:st.calibration,\n      sessionEnded:true,now:Date.now(),date:vd})\n      :((st.snap&&st.snap.sessionEnded===(sessionEnded||st.fresh==='SESSION ENDED'))?st.snap:buildSnap(openSym));\n    var W=snap.whatNow, pr=W.probability, LV=snap.levels;\n    var badge=snap.valid?'':'<div style=\"background:var(--s-avoid);color:#fff;padding:9px 14px;font-size:13px;font-weight:600\">'+\n      'מצב המודל לא עקבי — לא מוצגת הוראת מסחר<div style=\"font-weight:400;opacity:.9;font-size:12px;margin-top:3px\">'+\n      snap.violations.filter(function(x){return x.severity==='block'}).map(function(x){return x.text}).join(' · ')+'</div></div>';\n    var roles=snap.valid&&LV?'<div class=\"lvls\">'+\n      [['רמת מעקב',LV.watch,'המחיר שמחכים לו'],['כניסה',LV.entry,'שם נכנסים בפועל'],\n       ['יעד ראשון',LV.target1,'אזור מימוש ראשון'],['יעד שני',LV.target2,''],\n       ['ביטול הכניסה',LV.tacticalInvalidation,'מבטל את רעיון הכניסה עכשיו'],\n       ['שבירת מבנה',LV.hardStop,'הרעיון כולו יורד מהפרק']]\n      .filter(function(x){return x[1]!=null})\n      .map(function(x){return '<div class=\"l\"><span>'+x[0]+'</span><b class=\"num\">'+x[1].toFixed(2)+'</b><i>'+\n        fmtR((x[1]-snap.price)/snap.atr)+(x[2]?' · '+x[2]:'')+'</i></div>'}).join('')+'</div>':'';\n    var whatNowHtml=badge+'<div class=\"wn '+W.action+'\"><div class=\"top2\"><div class=\"a\">'+W.actionText+'</div><div class=\"n\">'+W.next+'</div>'+(W.scenario&&W.scenario.kind!=='none'&&!snap.noEdge&&!snap.sessionEnded?'<div class=\"scen\">התרחיש הפעיל: '+W.scenario.label+'</div>':'')+(snap.noEdge?'<div class=\"scen\">'+((snap.edge&&snap.edge.reasons)||[]).join(' · ')+'</div>':'')+'</div>'+\n      (pr?'<div class=\"odds\"><div class=\"bar2\"><div class=\"u\" style=\"width:'+pr.up+'%\">'+(pr.up>=18?'למעלה '+pr.up+'%':'')+'</div>'+\n        '<div class=\"d\" style=\"width:'+pr.down+'%\">'+(pr.down>=18?'למטה '+pr.down+'%':'')+'</div></div>'+\n        '<div class=\"q\">מה זה אומר: הסיכוי שהמחיר יגיע ל-<b>'+pr.upper.toFixed(2)+'</b> (רמת המעקב) לפני <b>'+pr.lower.toFixed(2)+'</b> (ביטול הכניסה) בשעה הקרובה.<br>'+\n        (pr.lowConfidence?'<b style=\"color:var(--down)\">ביטחון נמוך — מעט ראיות</b><br>':'')+\n        (pr.source==='empirical'?'מבוסס על <b>'+pr.n+'</b> מקרים דומים בהיסטוריה של הנייר · ביטחון '+pr.confidence+'/100'\n          :'<b>'+pr.bias+'</b> — הערכת מודל, לא סטטיסטיקה מדודה · ביטחון '+pr.confidence+'/100'+\n           (st.histLoading?' · טוען היסטוריה…':st.hist&&!st.hist.length?' · אין ימים קודמים':''))+'</div></div>'\n        :(function(){\n          // A suppressed probability must still occupy its place and say what\n          // was asked and why there is no number. Vanishing silently reads as\n          // though the question was never posed.\n          var raw=snap.probabilityRaw;\n          if(!raw)return '';\n          return '<div class=\"odds\"><div class=\"q\"><b>אין הסתברות מוצגת</b><br>'+\n            (raw.upper!=null?'השאלה שנבדקה: האם המחיר מגיע ל-<b>'+raw.upper.toFixed(2)+'</b> לפני <b>'+raw.lower.toFixed(2)+'</b>.<br>':'')+\n            ((raw.why||[])[0]||'לא ניתן לענות על השאלה במצב הנוכחי')+'</div></div>';\n        })())+\n      (function(){ var pf=W.pressure; if(!pf)return '';\n        var agree=pf.agreement==='תומך'?'<b style=\"color:var(--up)\">תומך בתרחיש</b>':pf.agreement==='סותר'?'<b style=\"color:var(--down)\">סותר את התרחיש</b>':pf.agreement;\n        var lvl=[];\n        if(pf.support&&pf.support.state!=='far')lvl.push('התמיכה ב-'+pf.support.price.toFixed(2)+' <b>'+pf.support.verdict+'</b>');\n        if(pf.resistance&&pf.resistance.state!=='far')lvl.push('ההתנגדות ב-'+pf.resistance.price.toFixed(2)+' <b>'+pf.resistance.verdict+'</b>');\n        var concl=pf.conclusion?'<br><b>'+pf.conclusion+'</b>':'';\n        return '<div class=\"flow\"><div class=\"fb\"><div class=\"b\" style=\"width:'+pf.buyPct+'%\">'+(pf.buyPct>=18?'קונים '+pf.buyPct+'%':'')+'</div>'+\n          '<div class=\"s\" style=\"width:'+pf.sellPct+'%\">'+(pf.sellPct>=18?'מוכרים '+pf.sellPct+'%':'')+'</div></div>'+\n          '<div class=\"ft\">קונים <b>'+pf.buyersTrend+'</b> · מוכרים <b>'+pf.sellersTrend+'</b>'+\n          (pf.agreement!=='לא רלוונטי'?' · '+agree:'')+\n          concl+(lvl.length?'<br>'+lvl.join(' · '):'')+'</div>'+\n          '<div class=\"src\">נגזר מהנרות (סגירה בתוך הטווח × מחזור). אין כאן ספר פקודות או נתוני עסקאות בודדות.</div></div>';\n      })()+\n      (function(){ var bk=st.book;\n        if(!bk||bk.error)return st.bookLoading?'<div class=\"book\"><div class=\"meta2\">טוען ספר פקודות…</div></div>':'';\n        var s=bk.summary; if(!s||s.bid_pct==null)return '<div class=\"book\"><div class=\"meta2\">ספר הפקודות לא זמין כרגע'+\n          (s&&s.venues_failed&&s.venues_failed.length?' ('+s.venues_failed.join(', ')+')':'')+'</div></div>';\n        var v=(bk.venues||[]).filter(function(x){return !x.error});\n        // merge the five levels of every venue into one ladder\n        var agg=function(side,desc){ var m={};\n          v.forEach(function(x){(x[side]||[]).forEach(function(l){m[l.price.toFixed(2)]=(m[l.price.toFixed(2)]||0)+l.shares})});\n          return Object.keys(m).map(function(p){return {price:+p,shares:m[p]}})\n            .sort(function(a,b){return desc?b.price-a.price:a.price-b.price}).slice(0,5); };\n        var bids=agg('bids',true), asks=agg('asks',false);\n        var rows='<div class=\"lv3\"><span class=\"h bq\">קנייה</span><span class=\"h px2\">מחיר</span><span class=\"h aq\">מכירה</span>';\n        for(var i=0;i<5;i++){ var b2=bids[i],a2=asks[i];\n          if(a2)rows+='<span class=\"bq\"></span><span class=\"px2\">'+a2.price.toFixed(2)+'</span><span class=\"aq\">'+a2.shares.toLocaleString()+'</span>'; }\n        for(var i2=0;i2<5;i2++){ var b3=bids[i2];\n          if(b3)rows+='<span class=\"bq\">'+b3.shares.toLocaleString()+'</span><span class=\"px2\">'+b3.price.toFixed(2)+'</span><span class=\"aq\"></span>'; }\n        rows+='</div>';\n        return '<div class=\"book\"><div class=\"bb\"><div class=\"b\" style=\"width:'+s.bid_pct+'%\">'+(s.bid_pct>=18?'קנייה '+s.bid_pct+'%':'')+'</div>'+\n          '<div class=\"a\" style=\"width:'+s.ask_pct+'%\">'+(s.ask_pct>=18?'מכירה '+s.ask_pct+'%':'')+'</div></div>'+rows+\n          '<div class=\"meta2\">עומק: '+s.bid_shares.toLocaleString()+' מניות בקנייה מול '+s.ask_shares.toLocaleString()+' במכירה'+\n          (s.best_bid!=null?' · '+s.best_bid.toFixed(2)+' / '+s.best_ask.toFixed(2)+' (מרווח '+s.spread.toFixed(2)+')':'')+\n          '<br>מסחרים: '+s.venues_ok.join(', ')+(s.venues_failed.length?' · לא ענו: '+s.venues_failed.join(', '):'')+'</div>'+\n          '<div class=\"warn\">ספר אמיתי, אבל של Cboe בלבד — ARCA, NYSE ונאסד\"ק אינם כאן. זו לא תמונת השוק המלאה ולא NBBO.</div></div>';\n      })()+\n      '<div class=\"paths\"><div class=\"pth u\"><h4>אם עולה</h4>'+W.up.map(function(x){return '<div>'+x+'</div>'}).join('')+'</div>'+\n      '<div class=\"pth d\"><h4>אם יורד</h4>'+W.down.map(function(x){return '<div>'+x+'</div>'}).join('')+'</div></div>'+\n      '<div class=\"why2\">למה: '+W.why.join(' · ')+'</div></div>'+roles;\n    var h='<div class=\"grab\"></div>'+\n      '<div class=\"phead\"><span class=\"s\">'+openSym+'</span><span class=\"p num\">'+b.close.toFixed(2)+'</span>'+\n        '<span class=\"chg num '+(chg>=0?'up':'down')+'\">'+(chg>=0?'+':'')+chg.toFixed(2)+'%</span>'+\n        '<span class=\"spacer\"></span><select id=\"pdate\" style=\"background:var(--well);border:1px solid var(--rule);border-radius:8px;padding:4px 7px;font-size:12px\">'+\n          (st.dates||[st.date]).map(function(d){return '<option value=\"'+d+'\"'+(d===st.viewDate?' selected':'')+'>'+d.slice(5).replace('-','/')+(st.days[d]?'':' ↓')+'</option>'}).join('')+\n        '</select><span style=\"font-size:12px;color:var(--muted)\">'+b.time+'</span></div>'+\n      '<div class=\"pstat\"><span class=\"st bg-'+r.status.replace(' ','')+'\">'+STATUS_TXT[r.status]+'</span>'+\n        '<span class=\"sc\">ציון Setup <b>'+snap.score+'</b>/10</span>'+\n        '<span class=\"fr\" style=\"opacity:.8\">נתונים עד '+snap.data_through.time+'</span>'+\n        '<span class=\"fr '+r.freshness.replace(' ','')+'\"><i></i>'+frTxt+'</span></div>'+\n      whatNowHtml+\n      '<details class=\"raw\"><summary>פירוט טכני</summary>'+planHtml+\n      '<div class=\"two\"><div class=\"card\"><small>מבנה ראשי</small><b>'+(STRUCT_TXT[r.structure]||r.structure)+'</b><span>'+A.state.reason+'</span></div>'+\n        '<div class=\"card\"><small>מומנטום קצר</small><b>'+(MOM_TXT[r.momentum]||r.momentum)+'</b><span class=\"num\">'+r.mom.net.toFixed(1)+'R ב-5 נרות</span></div></div>'+\n      '<div class=\"lvls\">'+\n        levelRow('התנגדות טקטית',T.resistance)+\n        levelRow('תמיכה טקטית',T.support)+\n        levelRow('טריגר',BL.trigger)+\n        levelRow('ביטול',BL.invalidation)+\n        (BL.reclaim?levelRow('רמה שנשברה',{price:BL.reclaim.price,r:BL.reclaim.dist.atr,why:'reclaim'}):'')+\n      '</div>'+\n      rangeBar(b,T)+\n      '<div class=\"sec\"><span>VWAP <b class=\"num\">'+b.vwap.toFixed(2)+'</b> '+(b.aboveVwap?'✅':'❌')+'</span>'+\n        '<span>EMA9 <b class=\"num\">'+b.ema9.toFixed(2)+'</b> / EMA20 <b class=\"num\">'+b.ema20.toFixed(2)+'</b> '+(b.align==='bull'?'שורי ✅':b.align==='bear'?'דובי ❌':'ניטרלי')+'</span>'+\n        '<span>טווח ממוצע <b class=\"num\">'+(b.atr20||0).toFixed(2)+'</b></span><span>שוק <b>'+mkTxt+'</b></span>'+\n        '<span>60 נרות <b class=\"num\">'+(A.state.sup?A.state.sup.price.toFixed(2):'—')+' / '+(A.state.res?A.state.res.price.toFixed(2):'—')+'</b></span>'+\n        '<span>יום <b class=\"num\">'+b.dayLow.toFixed(2)+' / '+b.dayHigh.toFixed(2)+'</b></span>'+\n        '<span>נפח <b class=\"num\">'+b.volx.toFixed(2)+'×</b> ('+fmtK(b.volume)+')</span></div>'+\n      '</details>'+\n      '<div class=\"acts\"><button type=\"button\" id=\"untrack\" class=\"ghost\" style=\"color:var(--down)\">הסר מהמעקב</button>'+\n        '<button type=\"button\" id=\"copyState\">העתק חבילת ניתוח מלאה</button>'+\n        '<button type=\"button\" class=\"ghost\" id=\"copyShort\">סיכום קצר</button>'+\n        '<button type=\"button\" class=\"ghost\" id=\"copyMenuBtn\">העתקת נרות ▾</button>'+\n        '<button type=\"button\" class=\"ghost\" id=\"openFull\">מסך מלא</button>'+\n        '<button type=\"button\" class=\"ghost\" id=\"openBuy\">כרטיס קנייה</button>'+\n        '<button type=\"button\" class=\"ghost\" id=\"close\">סגור</button></div>'+\n      copyMenu(st,curRows)+rangePicker(st)+\n      '<div class=\"feed\"><div class=\"fmodes\">'+\n        ['LIVE','IMPORTANT','ALL'].map(function(m){return '<button type=\"button\" data-m=\"'+m+'\" class=\"'+(feedMode===m?'on':'')+'\">'+(m==='LIVE'?'אחרונים':m==='IMPORTANT'?'חשובים':'הכל')+'</button>'}).join('')+\n        '</div>'+feed(A)+'</div>';\n    p.innerHTML=h; p.scrollTop=0; bind();\n  }\n  // ---- BUY DECISION CARD ---------------------------------------------------\n  // An additional view. It reads the state the page has already built and\n  // writes to nothing but its own container.\n  var buyOpen=false;\n  function buyDecisionHtml(){\n    var st=store[openSym]; if(!st||!st.snap)return '<div class=\"empty\">אין נתונים</div>';\n    var vd=st.viewDate||st.date;\n    var live=marketPhase()==='open';\n    var card=buyCard(st.snap,st.A,{symbol:openSym,closed:!live,\n      market:live?mkLabel(openSym):'סגור',\n      snapshotTime:(st.rows&&st.rows.length?st.rows[st.rows.length-1].time:null)});\n    var DEC={BUY:['🟢','BUY — אפשר לקנות','buy'],WAIT:['🔴','WAIT — לא קונה כרגע','wait'],\n      RECHECK:['🔄','RECHECK — לבדוק מחדש','recheck'],'NO SETUP':['⚪','NO SETUP — אין setup','nosetup']};\n    var d=(DEC[card.decision]||DEC['NO SETUP']).slice();\n    if(card.marketClosed&&card.decision==='NO SETUP')d[1]='NO SETUP — אין setup קנייה בסגירה';\n    var cov=card.coverage||{};\n    var rowTxt=function(r){\n      var range=r.above!=null?('מעל '+r.above.toFixed(2))\n        :r.below!=null?('מתחת '+r.below.toFixed(2))\n        :(r.from.toFixed(2)+'–'+r.to.toFixed(2));\n      var m=DEC[r.decision]||DEC['NO SETUP'];\n      return '<div class=\"bcrow '+m[2]+'\"><span class=\"bcr\">'+range+'</span>'+\n        '<span class=\"bcd\">'+m[0]+' '+r.decision+' — '+r.text+'</span></div>';\n    };\n    // Symbol first and large, then when the snapshot was taken, then the\n    // decision. The trader should not have to read a line of prose to find out\n    // which stock this is.\n    return '<div class=\"buycard\">'+\n      '<div class=\"bchead\"><b>'+card.symbol+'</b>'+\n        (card.marketClosed\n          ? 'סגירת '+(vd?vd.slice(5).replace('-','/'):'')+(card.snapshotTime?' · '+card.snapshotTime:'')\n          : (card.snapshotTime?'תמונת מצב '+card.snapshotTime:''))+'</div>'+\n      '<div class=\"bcsub\">נר אחרון '+(card.lastClose!=null?card.lastClose.toFixed(3):'—')+\n        (cov.receivedMinutes!=null?' · '+cov.receivedMinutes+'/'+cov.expectedMinutes+' נרות '+(cov.complete?'✓':'✗'):'')+'</div>'+\n      '<div class=\"bcverdict '+d[2]+'\">'+d[0]+' '+d[1]+'</div>'+\n      (card.blocked?'<div class=\"bcblock\">'+card.blocked+'</div>':'')+\n      '<div class=\"bcmeta\"><span>מצב קנייה <b>'+card.quality+'</b></span>'+\n        '<span>ביטחון <b>'+card.confidence+'</b></span></div>'+\n      // No map when there is no setup: there is nothing whose validity could be\n      // mapped. And after the close this is a snapshot, not a live signal, so\n      // it is not labelled as the price you are watching right now.\n      (card.hasMap?\n        '<div class=\"bcsec\">'+(marketPhase()==='open'?'מחיר Trading שלך עכשיו':'מפת מחיר')+'</div>'+\n        '<div class=\"bcmap\">'+card.map.map(rowTxt).join('')+'</div>'+\n        '<div class=\"bcvalid\">תקפות הכרטיס: '+\n          card.validity.low.toFixed(2)+'–'+card.validity.high.toFixed(2)+\n          ' <small>('+card.validity.lowWhy+' / '+card.validity.highWhy+')</small>'+\n          '<br><small>מחוץ לטווח → RECHECK עם הנרות החדשים. מחיר נמוך יותר עשוי ליצור setup טוב יותר, גבוה יותר עשוי ליצור פריצה — צריך נרות חדשים כדי לדעת.</small></div>'\n        :'')+\n      '<div class=\"bcsec\">למה אין BUY</div><div class=\"bcwhy\">'+\n        (card.blocked?card.blocked+'<br>':'')+(card.why||card.reasons.join('<br>'))+'</div>'+\n      '<div class=\"bcsec\">נתונים</div>'+\n      '<div class=\"bctech\">'+\n        '<span>מבנה<b>'+card.structure+'</b></span>'+\n        '<span>מומנטום<b>'+card.momentum+'</b></span>'+\n        '<span>'+(card.vwap==null?'VWAP<b>—</b>'\n          :(card.aboveVwap?'מחיר מעל VWAP':'מחיר מתחת VWAP')+'<b>'+card.vwap.toFixed(2)+'</b>')+'</span>'+\n        '<span>EMA9 / EMA20<b>'+(card.ema9!=null?card.ema9.toFixed(2):'—')+' / '+(card.ema20!=null?card.ema20.toFixed(2):'—')+'</b></span>'+\n        '<span>שוק<b>'+card.market+'</b></span>'+\n      '</div>'+\n      (card.buyersPct!=null?'<div class=\"bcsec\">קונים / מוכרים</div>'+\n        '<div class=\"bctech\"><span>יחס<b>'+card.buyersPct+'% / '+card.sellersPct+'%</b></span>'+\n        ((card.buyersTrend||card.sellersTrend)?'<span>כיוון הכוח<b>'+\n          [card.buyersTrend?'קונים '+card.buyersTrend:null,\n           card.sellersTrend?'מוכרים '+card.sellersTrend:null].filter(Boolean).join(' · ')+'</b></span>':'')+\n        '</div>':'')+\n      (card.levels.length?'<div class=\"bcsec\">רמות</div><div class=\"bctech\">'+\n        card.levels.map(function(l){return '<span>'+l.name+'<b>'+l.price.toFixed(2)+'</b></span>'}).join('')+'</div>':'')+\n      '<div class=\"bcsec\">החלטה</div>'+\n      '<div class=\"bctech\"><span>BUY NOW<b>'+card.buyNow+'</b></span>'+\n        (!card.hasMap?'<span>מפת מחיר<b>'+(card.marketClosed?'לא מוצגת כשהמסחר סגור':'לא מוצגת — אין setup')+'</b></span>':'')+\n      '</div>'+\n      '<div class=\"bcnote\">'+card.probabilityNote+'</div>'+\n      '<div class=\"acts\"><button type=\"button\" id=\"buyCopy\">העתק כרטיס</button>'+\n        '<button type=\"button\" class=\"ghost\" id=\"buyBack\">חזרה</button></div>'+\n    '</div>';\n  }\n  function drawBuy(){\n    var p=qs('#panel'); if(!p)return;\n    p.innerHTML=buyDecisionHtml(); p.scrollTop=0;\n    var bb=qs('#buyBack'); if(bb)bb.onclick=function(){buyOpen=false;drawDetail()};\n    // Copy the card as text, built from the same object the screen rendered so\n    // the two can never disagree.\n    var bcp=qs('#buyCopy'); if(bcp)bcp.onclick=function(){\n      var st=store[openSym]; if(!st||!st.snap)return;\n      var card=buyCard(st.snap,st.A,{symbol:openSym,market:mkLabel(openSym),\n        snapshotTime:(st.rows&&st.rows.length?st.rows[st.rows.length-1].time:null)});\n      var txt=buyCardText(card,{live:marketPhase()==='open'});\n      var done=function(){toast('הכרטיס הועתק')};\n      (navigator.clipboard?navigator.clipboard.writeText(txt):Promise.reject())\n        .then(done,function(){prompt('העתק:',txt)});\n    };\n  }\n\n  function backBtn(){return '<div class=\"acts\"><button type=\"button\" id=\"close\">סגור</button></div>'}\n\n  // #12 price position between the nearest levels\n  function rangeBar(b,T){\n    var lo=T.support?T.support.price:b.dayLow, hi=T.resistance?T.resistance.price:b.dayHigh;\n    if(!(hi>lo))return '';\n    var pos=Math.max(0,Math.min(1,(b.close-lo)/(hi-lo)));\n    var W=100,x=6+pos*(W-12);\n    return '<div class=\"bar\"><svg viewBox=\"0 0 100 26\" preserveAspectRatio=\"none\">'+\n      '<line x1=\"6\" x2=\"94\" y1=\"14\" y2=\"14\" stroke=\"var(--rule)\" stroke-width=\"1.5\"/>'+\n      '<line x1=\"6\" x2=\"6\" y1=\"9\" y2=\"19\" stroke=\"var(--up)\" stroke-width=\"1.5\"/>'+\n      '<line x1=\"94\" x2=\"94\" y1=\"9\" y2=\"19\" stroke=\"var(--down)\" stroke-width=\"1.5\"/>'+\n      '<circle cx=\"'+x.toFixed(1)+'\" cy=\"14\" r=\"3.2\" fill=\"var(--ink)\"/>'+\n      '<text class=\"t\" x=\"6\" y=\"25\" text-anchor=\"start\">'+lo.toFixed(2)+'</text>'+\n      '<text class=\"t\" x=\"94\" y=\"25\" text-anchor=\"end\">'+hi.toFixed(2)+'</text>'+\n      '<text class=\"now\" x=\"'+x.toFixed(1)+'\" y=\"7\" text-anchor=\"middle\">'+b.close.toFixed(2)+'</text>'+\n      '</svg></div>';\n  }\n\n  function feed(A){\n    var states=A.events.slice().reverse(), out=[];\n    states.forEach(function(s){\n      var lines=s.events.map(evLine).filter(Boolean);\n      if(feedMode==='IMPORTANT')lines=lines.filter(function(l,i){return IMPORTANT[s.events[i]&&s.events[i].type]});\n      if(!lines.length)return;                       // never render a bare score row\n      out.push({time:s.time,close:s.close,lines:lines});\n    });\n    if(feedMode==='LIVE')out=out.slice(0,8);\n    if(!out.length)return '<div class=\"empty\">אין אירועים להצגה במצב הזה.</div>';\n    return out.map(function(o){\n      return '<div class=\"ev '+o.lines[0].cls+'\"><span class=\"t\">'+o.time+'</span><span>'+o.lines.map(function(l){return l.text}).join(' · ')+'</span><span class=\"num\">'+o.close.toFixed(2)+'</span></div>';\n    }).join('');\n  }\n\n  // ---- copy\n  var CSV='symbol,date,time,open,high,low,close,volume,dir,body_pct,upper_wick_pct,lower_wick_pct,range,vol_x';\n  function csvRows(sym,rows){\n    // vol_x is normalised over the day the candles belong to, so a partial copy\n    // reports the same value the full day would.\n    var byDate={}; rows.forEach(function(r){(byDate[r.date]=byDate[r.date]||[]).push(r)});\n    var avg={}; Object.keys(byDate).forEach(function(d){var a=(store[sym].days[d]||byDate[d]);avg[d]=a.reduce(function(s,x){return s+x.volume},0)/a.length});\n    return rows.map(function(r){\n      var range=r.high-r.low, pct=function(x){return range>0?Math.round(x/range*1000)/10:0};\n      var dir=r.close>r.open?'BULL':r.close<r.open?'BEAR':'FLAT';\n      return [sym,r.date,r.time,r.open,r.high,r.low,r.close,r.volume,dir,\n        pct(Math.abs(r.close-r.open)),pct(r.high-Math.max(r.open,r.close)),pct(Math.min(r.open,r.close)-r.low),\n        Math.round(range*10000)/10000,avg[r.date]>0?Math.round(r.volume/avg[r.date]*100)/100:0].join(',');\n    });\n  }\n  function loadedDays(st){return Object.keys(st.days).sort().reverse()}\n  function allLoadedRows(st){var out=[];loadedDays(st).forEach(function(d){out=out.concat(st.days[d])});return out}\n  function copyMenu(st,curRows){\n    var n=(curRows||st.rows||[]).length, days=loadedDays(st), total=allLoadedRows(st).length;\n    var item=function(v,label,cnt){return '<button type=\"button\" data-copy=\"'+v+'\">'+label+(cnt!=null?' <small>'+cnt+' נרות</small>':'')+'</button>'};\n    return '<div class=\"menu\" id=\"copyMenu\" hidden>'+\n      [5,10,15,20,50].map(function(k){return item(String(k),k+' נרות אחרונים',Math.min(k,n))}).join('')+\n      item('today','כל נרות היום המוצג',n)+\n      item('all','כל הימים הטעונים — '+days.length,total)+\n      item('range','טווח תאריכים…')+\n      item('pack','חבילת ניתוח מלאה')+\n      item('state20','מצב + 20 נרות')+\n      item('state50','מצב + 50 נרות')+\n      item('stateall','מצב + כל נרות היום')+\n      '</div>';\n  }\n  function rangePicker(st){\n    var all=(st.dates||[st.date]).slice().sort();\n    return '<div class=\"range\" id=\"rangeBox\"'+(rangeOpen?'':' hidden')+'>'+\n      '<div class=\"r2\"><div><label>מתאריך</label><select id=\"rFrom\">'+all.map(function(d){return '<option>'+d+'</option>'}).join('')+'</select></div>'+\n      '<div><label>עד תאריך</label><select id=\"rTo\">'+all.map(function(d,i){return '<option'+(i===all.length-1?' selected':'')+'>'+d+'</option>'}).join('')+'</select></div></div>'+\n      '<div class=\"sum\" id=\"rSum\">בחר טווח</div><button type=\"button\" class=\"go\" id=\"rGo\">העתק</button></div>';\n  }\n  // Fetches every trading day in the range that is not already in memory.\n  // Days with no data (weekends, holidays) simply do not exist in /days and\n  // are skipped — nothing is fabricated.\n  function fetchRange(sym,from,to){\n    var st=store[sym];\n    var days=(st.dates||[]).filter(function(d){return d>=from&&d<=to}).sort();\n    var missing=days.filter(function(d){return !st.days[d]});\n    qs('#rSum').textContent='טוען '+missing.length+' ימים…';\n    return Promise.all(missing.map(function(d){\n      return j('/day/'+sym+'/'+d+'?format=json').then(function(x){st.days[d]=x.rows||[]},function(){st.days[d]=null});\n    })).then(function(){\n      var ok=days.filter(function(d){return st.days[d]&&st.days[d].length});\n      var failed=days.filter(function(d){return !st.days[d]||!st.days[d].length});\n      return {days:ok,failed:failed,count:ok.reduce(function(a,d){return a+st.days[d].length},0)};\n    });\n  }\n  // The pack is built from the SAME snapshot on screen, so what a reader\n  // validates is exactly what the trader saw.\n  function fullPack(){\n    var st=store[openSym], vd=st.viewDate||st.date, hist=vd!==st.date;\n    // One session, deduplicated, in order. A duplicated minute doubled both the\n    // 1-minute list and every 5-minute bucket built from it.\n    var rows=sessionRows(st,vd);\n    var A=hist?analyze(rows,opts()):st.A;\n    var snap=hist?buildTickerState(openSym,A,{market:mkLabel(openSym),marketCtx:marketCtxFor(openSym),\n      freshness:'SESSION ENDED',daily:st.daily,baseline:st.baseline,calibration:st.calibration,\n      sessionEnded:true,now:Date.now(),date:vd}):st.snap;\n    // Group by the BAR'S OWN DATE, never by array position. Dating a bucket\n    // from r[0].date and summing everything in it meant one array holding two\n    // sessions produced a row labelled with the first date and carrying the\n    // volume of both — the 09/02 row that read 8,047,020 was 09/02's real\n    // 5,343,181 plus every 09/04 bar that had leaked into the same array.\n    // Duplicates are removed by date+timestamp for the same reason.\n    var dailyBars=sessionsOf(st).map(function(r){\n      var h2=-Infinity,l2=Infinity,v2=0;\n      r.forEach(function(x){h2=Math.max(h2,x.high);l2=Math.min(l2,x.low);v2+=x.volume});\n      return {date:r[0].date,open:r[0].open,high:h2,low:l2,close:r[r.length-1].close,volume:v2,bars:r.length};\n    });\n    var last=rows.length?rows[rows.length-1]:null;\n    // The session is where the CLOCK is, not where the last bar happens to sit.\n    // Using the bar's time reported REGULAR at 16:29 because the newest bar was\n    // stamped 11:19.\n    var nowET=new Intl.DateTimeFormat('en-US',{timeZone:'America/New_York',weekday:'short',\n      hour:'2-digit',minute:'2-digit',hour12:false}).formatToParts(new Date())\n      .reduce(function(a,x){a[x.type]=x.value;return a},{});\n    var hm=nowET.hour+':'+nowET.minute;\n    var weekend=(nowET.weekday==='Sat'||nowET.weekday==='Sun');\n    var session=hist?'CLOSED':(st.fresh==='SESSION ENDED'||weekend?'CLOSED'\n      :hm<'04:00'?'CLOSED':hm<'09:30'?'PRE':hm<'16:00'?'REGULAR':hm<'20:00'?'AFTER':'CLOSED');\n    return analysisPack({snap:snap,analysis:A,rows:rows,book:hist?null:st.book,daily:st.daily,\n      dailyBars:dailyBars,baseline:st.baseline,marketCtx:marketCtxFor(openSym),sectorEtf:SECTOR[openSym]||null,\n      staleSeconds:hist?null:st.stale,session:session,date:vd,\n      quoteTime:last?vd+' '+last.time+' ET (bar close)':null,bookPrev:st.bookPrev||null});\n  }\n  function stateText(){\n    var st=store[openSym], vd=st.viewDate||st.date, hist=vd!==st.date;\n    var A=hist?analyze(st.days[vd],opts()):st.A;\n    var r=hist?radarRow(openSym,A,marketCtxFor(openSym),'SESSION ENDED'):st.row;\n    var b=A.state.bar, T=r.tactical, BL=r.bl, mk=marketCtxFor(openSym);\n    var sw=A.swings.filter(function(w){return w.confirmedAt<=A.bars.length-1}).slice(-8);\n    var lv=function(L){return L?L.price.toFixed(2)+' ('+((L.r!=null?L.r:L.dist.atr)>=0?'+':'')+(L.r!=null?L.r:L.dist.atr).toFixed(1)+'R) — '+(L.why||L.reason||''):'—'};\n    return [\n      openSym+' '+vd+' '+b.time+' ET  ('+r.freshness+(!hist&&st.stale!=null?', '+st.stale+'s old':'')+')',\n      'PRICE: '+b.close.toFixed(2)+'  last closed candle O'+b.open.toFixed(2)+' H'+b.high.toFixed(2)+' L'+b.low.toFixed(2)+' C'+b.close.toFixed(2)+' V'+b.volume+' ('+b.volx.toFixed(2)+'x)',\n      'STATUS: '+r.status+' — '+r.why+'   SETUP SCORE: '+r.score+'/10',\n      'MAIN STRUCTURE: '+r.structure+' ('+A.state.reason+')   SHORT MOMENTUM: '+r.momentum+' ('+r.mom.net.toFixed(1)+'R/5 bars)',\n      'TACTICAL RESISTANCE: '+lv(T.resistance),\n      'TACTICAL SUPPORT: '+lv(T.support),\n      'TRIGGER: '+lv(BL.trigger)+'   INVALIDATION: '+lv(BL.invalidation),\n      'RECLAIM LEVEL: '+(BL.reclaim?BL.reclaim.price.toFixed(2):'—'),\n      '60-BAR: '+(A.state.sup?A.state.sup.price.toFixed(2):'—')+' / '+(A.state.res?A.state.res.price.toFixed(2):'—')+'   DAY: '+b.dayLow.toFixed(2)+' / '+b.dayHigh.toFixed(2),\n      'VWAP: '+b.vwap.toFixed(2)+(b.aboveVwap?' above':' below')+'   EMA9 '+b.ema9.toFixed(2)+' / EMA20 '+b.ema20.toFixed(2)+' ('+b.align+')   AVG RANGE '+(b.atr20||0).toFixed(2),\n      'MARKET: '+mk.label+(mk.parts.length?' ('+mk.parts.map(function(p){return p.symbol+(p.aboveVwap?'>':'<')+'VWAP '+p.align}).join(', ')+')':''),\n      'SWINGS (last 8): '+(sw.length?sw.map(function(w){return w.label+' '+w.price.toFixed(2)+'@'+w.time}).join(', '):'none'),\n      'RECENT EVENTS: '+(function(){var out=[];A.events.slice().reverse().forEach(function(s){if(out.length>=6)return;\n        var l=s.events.map(evLine).filter(function(x,i){return x&&IMPORTANT[s.events[i].type]});\n        if(l.length)out.push(s.time+' '+l.map(function(x){return x.text}).join(' · '))});return out.length?out.join(' | '):'none'})(),\n      'Source: '+location.origin+'/day/'+openSym+'/'+vd+'?ts='+Date.now()\n    ].join('\\n');\n  }\n  function copyPayload(kind){\n    var st=store[openSym], vd=st.viewDate||st.date, today=st.days[vd]||st.rows||[];\n    var head=function(rows){return [CSV].concat(csvRows(openSym,rows)).join('\\n')};\n    if(kind==='today')return head(today);\n    if(kind==='all'){\n      return loadedDays(st).map(function(d){return '===== '+d+' =====\\n'+[CSV].concat(csvRows(openSym,st.days[d])).join('\\n')}).join('\\n\\n');\n    }\n    if(kind==='pack')return fullPack();\n    if(kind==='state20')return stateText()+'\\n\\n'+head(today.slice(-20));\n    if(kind==='state50')return stateText()+'\\n\\n'+head(today.slice(-50));\n    if(kind==='stateall')return stateText()+'\\n\\n'+head(today);\n    var k=parseInt(kind,10);\n    return head(today.slice(-k));\n  }\n  function doCopy(text,label){\n    var done=function(){toast(label+' הועתקו')};\n    if(navigator.clipboard&&navigator.clipboard.writeText)navigator.clipboard.writeText(text).then(done,function(){prompt('העתק:',text)});\n    else prompt('העתק:',text);\n  }\n\n  function bind(){\n    var c=qs('#close'); if(c)c.onclick=closeDetail;\n    // Un-tracking stops the live pull for this symbol. Its bars stay in D1\n    // until the trim and in the archive for good, so nothing is lost.\n    var ob=qs('#openBuy'); if(ob)ob.onclick=function(){buyOpen=true;drawBuy()};\n    var ut=qs('#untrack'); if(ut)ut.onclick=function(){\n      var s=openSym;\n      if(!confirm(s+' יוסר מהמעקב החי. ההיסטוריה נשארת בארכיון. להמשיך?'))return;\n      j('/watch/remove/'+s).then(function(d){\n        if(d.error){toast(d.error);return}\n        symbols=symbols.filter(function(x){return x!==s}); delete store[s];\n        closeDetail(); render(); toast(s+' הוסר מהמעקב');\n      },function(e){toast('לא הצלחתי להסיר: '+e)});\n    };\n    var of=qs('#openFull'); if(of)of.onclick=function(){var s=store[openSym];location.href='/view/'+openSym+'/'+(s.viewDate||s.date||'')};\n    var pd=qs('#pdate'); if(pd)pd.onchange=function(){loadDay(openSym,pd.value)};\n    var cs=qs('#copyState'); if(cs)cs.onclick=function(){var t=fullPack();doCopy(t,'חבילת ניתוח מלאה ('+t.split('\\n').length+' שורות)')};\n    var cshort=qs('#copyShort'); if(cshort)cshort.onclick=function(){doCopy(stateText(),'סיכום קצר')};\n    var cm=qs('#copyMenuBtn'); if(cm)cm.onclick=function(){menuOpen=!menuOpen;qs('#copyMenu').hidden=!menuOpen};\n    var menu=qs('#copyMenu'); if(menu){menu.hidden=!menuOpen;\n      [].forEach.call(menu.querySelectorAll('[data-copy]'),function(btn){btn.onclick=function(){\n        if(btn.dataset.copy==='range'){rangeOpen=true;qs('#rangeBox').hidden=false;menuOpen=false;qs('#copyMenu').hidden=true;bindRange();return}\n        var txt=copyPayload(btn.dataset.copy);\n        var lines=txt.split('\\n').filter(function(l){return l&&l.indexOf('=====')!==0&&l!==CSV&&l.indexOf(',')>0}).length;\n        doCopy(txt,lines+' שורות')}});}\n    [].forEach.call(document.querySelectorAll('.fmodes button'),function(btn){btn.onclick=function(){feedMode=btn.dataset.m;drawDetail()}});\n    if(rangeOpen)bindRange();\n  }\n\n\n  // ---- alerts. Identity = symbol + execution state + the level it refers to,\n  // so the same condition never fires twice while it persists.\n  var PRIORITY={READY_PARTIAL:'high',READY_ADD:'high',FAILED:'bad',TAKE_PROFIT_AREA:'med',DO_NOT_CHASE:'med',ACTIVE:'med'};\n  var ALERT_TXT={\n    READY_PARTIAL:function(r){return['כניסה חלקית',r.plan.waitPrice+' · האזור מוחזק']},\n    READY_ADD:function(r){return['אישור להוספה','חזר מעל '+r.plan.addAbove.toFixed(2)]},\n    FAILED:function(r){return['setup בוטל',(r.plan.invalidation!=null?r.plan.invalidation.toFixed(2)+' נשבר':'הרמה נשברה')]},\n    TAKE_PROFIT_AREA:function(r){return['מתקרב ליעד',r.plan.target?r.plan.target.toFixed(2):'']},\n    DO_NOT_CHASE:function(r){return['המחיר התרחק מהכניסה','המתן לפולבק '+r.plan.waitPrice]},\n    ACTIVE:function(r){return['מהלך פעיל',r.plan.target?'יעד '+r.plan.target.toFixed(2):'']}\n  };\n  function alertKey(r){\n    var P=r.plan; if(!P)return null;\n    var lvl=P.kind==='pullback'?(P.entry||0):(P.zone?P.zone[0]:0);\n    return r.symbol+'|'+P.state+'|'+lvl.toFixed(2);\n  }\n  function fireAlerts(){\n    if(sessionEnded)return;                       // no live alerts after the close\n    symbols.forEach(function(s){\n      var r=store[s]&&store[s].row; if(!r||!r.plan)return;\n      if(r.freshness==='STALE'||r.freshness==='NO DATA'||r.freshness==='SESSION ENDED')return;\n      var txt=ALERT_TXT[r.plan.state]; if(!txt)return;\n      var key=alertKey(r);\n      if(alertState[s]===key)return;              // same condition, already announced\n      alertState[s]=key;\n      var body=txt(r), pri=PRIORITY[r.plan.state]||'med';\n      alertLog.unshift({time:new Date().toTimeString().slice(0,5),symbol:s,title:body[0],sub:body[1],pri:pri});\n      alertLog=alertLog.slice(0,20);\n      if(alertsOn)showAlert(s,body[0],body[1],pri);\n      if(soundOn&&pri==='high')beep();\n      drawAlertLog();\n    });\n  }\n  function showAlert(sym,title,sub,pri){\n    var host=qs('#alerts'), el=document.createElement('div');\n    el.className='al '+pri;\n    el.innerHTML='<span class=\"x\">×</span><b>'+sym+' — '+title+'</b><div>'+(sub||'')+'</div>';\n    el.onclick=function(ev){ if(ev&&ev.target&&ev.target.className==='x'){el.remove();return} openDetail(sym); el.remove(); };\n    host.appendChild(el);\n    setTimeout(function(){el.remove()},9000);\n    // At most two on screen at once; the rest stay in the log behind the bell.\n    while(host.children.length>2)host.removeChild(host.firstChild);\n  }\n  function beep(){ try{var C=window.AudioContext||window.webkitAudioContext;if(!C)return;var c=new C(),o=c.createOscillator(),g=c.createGain();\n    o.connect(g);g.connect(c.destination);o.frequency.value=880;g.gain.value=0.05;o.start();setTimeout(function(){o.stop();c.close()},140)}catch(e){} }\n  function drawAlertLog(){\n    var el=qs('#alertLog');\n    el.innerHTML='<button type=\"button\" data-x=\"1\" style=\"font-weight:600\">התראות '+\n      '<small>פופאפ '+(alertsOn?'פעיל':'כבוי')+' · צליל '+(soundOn?'פעיל':'כבוי')+'</small></button>'+\n      (alertLog.length?alertLog.map(function(a,i){return '<button type=\"button\" data-i=\"'+i+'\">'+a.time+' · <b>'+a.symbol+'</b> '+a.title+' <small>'+(a.sub||'')+'</small></button>'}).join('')\n        :'<button type=\"button\" disabled>עוד לא היו התראות</button>');\n    [].forEach.call(el.querySelectorAll('[data-i]'),function(b){b.onclick=function(){openDetail(alertLog[+b.dataset.i].symbol)}});\n    var head=el.querySelector('[data-x]'); if(head)head.onclick=function(){alertsOn=!alertsOn;if(!alertsOn)soundOn=false;else soundOn=!soundOn?soundOn:soundOn;drawAlertLog()};\n  }\n  qs('#alertsBtn').onclick=function(){var el=qs('#alertLog');el.hidden=!el.hidden;drawAlertLog()};\n\n  function bindRange(){\n    var box=qs('#rangeBox'); if(!box)return;\n    var upd=function(){\n      var f=qs('#rFrom').value,t2=qs('#rTo').value;\n      if(f>t2){qs('#rSum').textContent='טווח לא תקין';return}\n      var st=store[openSym];\n      var days=(st.dates||[]).filter(function(d){return d>=f&&d<=t2});\n      var known=days.filter(function(d){return st.days[d]});\n      var cnt=known.reduce(function(a,d){return a+(st.days[d]?st.days[d].length:0)},0);\n      qs('#rSum').textContent=f+' → '+t2+' · '+days.length+' ימי מסחר'+(known.length<days.length?' ('+(days.length-known.length)+' ייטענו בהעתקה)':' · '+cnt.toLocaleString()+' נרות');\n    };\n    qs('#rFrom').onchange=upd; qs('#rTo').onchange=upd; upd();\n    qs('#rGo').onclick=function(){\n      var f=qs('#rFrom').value,t2=qs('#rTo').value;\n      if(f>t2){toast('טווח לא תקין');return}\n      fetchRange(openSym,f,t2).then(function(res){\n        if(!res.days.length){qs('#rSum').textContent='אין נתונים בטווח הזה';return}\n        var txt=res.days.map(function(d){return '===== '+d+' =====\\n'+[CSV].concat(csvRows(openSym,store[openSym].days[d])).join('\\n')}).join('\\n\\n');\n        var note=res.failed.length?' · '+res.failed.length+' ימים לא נטענו':'';\n        qs('#rSum').textContent=f+' → '+t2+' · '+res.days.length+' ימים · '+res.count.toLocaleString()+' נרות'+note;\n        if(res.count>200000)toast('ייצוא גדול — '+res.count.toLocaleString()+' נרות');\n        doCopy(txt,res.count.toLocaleString()+' נרות מ-'+res.days.length+' ימים'+note);\n      });\n    };\n  }\n\n  qs('#sheet').onclick=function(e){if(e.target===qs('#sheet'))closeDetail()};\n  document.addEventListener('keydown',function(e){if(e.key==='Escape'&&openSym)closeDetail()});\n  qs('#sort').onchange=render;\n  qs('#sens').onchange=function(){recompute();if(openSym)drawDetail()};\n\n  // The audit is a diagnostic, not part of the trading loop. Once every ten\n  // minutes, never on the refresh path: bars arriving on time matters more than\n  // a verdict arriving promptly.\n  // Never automatic. Measured: 31 runs cost 1.32 million reads, a quarter of\n  // the daily budget and more than the whole trading system. The bootstrap call\n  // is kept because the UI tests hook onto its exact text, but it does nothing.\n  var lastAudit=0;\n  function auditIfDue(){ return Promise.resolve(); }\n  // The badge starts as an invitation, not a verdict: running the check is the\n  // expensive part, so it happens when asked and not before.\n  (function(){\n    var el=qs('#audit'); if(!el)return;\n    el.hidden=false; el.className='audit'; el.textContent='בדוק נתונים';\n    el.onclick=function(){ el.textContent='בודק…'; runAudit(); };\n  })();\n  // NOT on a timer. Measured: 31 runs cost 1.32 million reads — a quarter of the\n  // daily budget, more than the entire trading system. A diagnostic that costs\n  // more than the thing it diagnoses is not a diagnostic. It runs only when you\n  // tap the badge.\n  // A short session is repaired shortly after the first paint, and then by the\n  // refresh loop. Kept off the bootstrap call itself, whose exact text the UI\n  // tests hook into.\n  setTimeout(function(){ repairFromOpen().then(function(){recompute();render()}); },1500);\n  qs('#reload').onclick=function(){loadAll().then(function(){if(openSym)drawDetail();toast('עודכן')});runAudit()};\n  setInterval(function(){var d=new Date();qs('#clock').textContent=d.toTimeString().slice(0,5)},1000);\n\n  // Auto-refresh: one timer for the whole page, never overlapping requests,\n  // and it stops once the session is over.\n  setInterval(function(){\n    var every=parseInt(qs('#every').value,10)||0;\n    if(!every||document.hidden||sessionEnded){qs('#prog').textContent=loading?'מרענן…':'';return}\n    tick++;\n    var left=every-(tick%every);\n    qs('#prog').textContent=loading?'מרענן…':'עוד '+left+'ש';\n    if(tick%every===0&&!loading)refresh();\n  },1000);\n  // While the market is open the collector must not depend on the refresh\n  // interval the trader happens to have chosen, nor on the tab being focused\n  // for a whole cycle. One quiet request a minute keeps bars arriving.\n  setInterval(function(){\n    if(sessionEnded)return;\n    if(marketPhase()!=='open')return;\n    // /tick reads no rows. The old keepalive called /board with no cursor,\n    // which is a full day for every symbol, every minute.\n    j('/tick').catch(function(){});\n  },60000);\n  qs('#every').onchange=function(){tick=0};\n\n  // Restore whatever the last page load already had, so the screen paints\n  // immediately and the first request only asks for what is new.\n  (function restore(){\n    var c=cacheLoad(); if(!c)return;\n    boardDate=c.date;\n    Object.keys(c.bySym||{}).forEach(function(s){\n      var rows=c.bySym[s]; if(!rows||!rows.length)return;\n      var st=store[s]||(store[s]={days:{}});\n      st.date=c.date; st.rows=rows; st.days[c.date]=rows; st.seen={};\n      rows.forEach(function(r){st.seen[r.date+':'+r.unix]=1});\n      lastBoardUnix=Math.max(lastBoardUnix,rows[rows.length-1].unix||0);\n    });\n  })();\n\n  // Kept as loadAll().catch(...) because the UI tests attach their hook to this\n  // exact call; the audit runs from inside the success path instead.\n  // NOTE: the UI tests attach their hook to the literal text of this call, so\n  // its shape must not change; the extra work goes inside the callback.\n  loadAll().then(function(){return auditIfDue()}).catch(function(e){qs('#rows').innerHTML='<div class=\"empty\"><b>שגיאת טעינה</b><br>'+String(e)+'</div>'});\n})();\n</script>\n</body>\n</html>\n";
export const DB_HTML = "<!doctype html>\n<html lang=\"he\" dir=\"rtl\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width,initial-scale=1,viewport-fit=cover\">\n<title>מה יש במסד הנתונים</title>\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link href=\"https://fonts.googleapis.com/css2?family=Heebo:wght@400;600;800&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{--paper:#F2F4F7;--ink:#1B2430;--muted:#5B6673;--rule:#D6DBE2;--well:#FFFFFF;\n    --ok:#0F766E;--warn:#B45309;--bad:#B42318;--flat:#8A94A0}\n  *{box-sizing:border-box}\n  html,body{margin:0;background:var(--paper);color:var(--ink);font-family:Heebo,system-ui,sans-serif;font-variant-numeric:tabular-nums;max-width:100%;overflow-x:hidden}\n  body{padding-bottom:calc(28px + env(safe-area-inset-bottom))}\n  .num{direction:ltr;unicode-bidi:isolate}\n  button,select{font:inherit}\n  header{position:sticky;top:0;z-index:10;background:var(--paper);border-bottom:1px solid var(--rule);padding:12px 16px 10px}\n  .hrow{display:flex;align-items:center;gap:10px}\n  h1{margin:0;font-size:19px;font-weight:800}\n  .spacer{flex:1}\n  .btn{background:var(--well);border:1px solid var(--rule);border-radius:9px;padding:6px 11px;font-size:13px;color:var(--ink)}\n  .sub{font-size:12px;color:var(--muted);margin-top:5px}\n\n  .cards{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;padding:12px 16px 0}\n  .card{background:var(--well);border:1px solid var(--rule);border-radius:12px;padding:10px 12px}\n  .card small{display:block;font-size:11.5px;color:var(--muted)}\n  .card b{font-size:21px;font-weight:800;display:block;line-height:1.25}\n  .card span{font-size:11.5px;color:var(--muted)}\n\n  .quota{margin:12px 16px 0;background:var(--well);border:1px solid var(--rule);border-radius:12px;padding:11px 12px}\n  .quota .lbl{display:flex;justify-content:space-between;font-size:12px;color:var(--muted);margin-bottom:4px}\n  .quota .lbl b{color:var(--ink)}\n  .meter{height:9px;background:var(--rule);border-radius:5px;overflow:hidden;margin-bottom:9px}\n  .meter i{display:block;height:100%;background:var(--ok)}\n  .meter.warn i{background:var(--warn)} .meter.bad i{background:var(--bad)}\n\n  h2{font-size:13px;font-weight:800;color:var(--muted);margin:18px 16px 6px;letter-spacing:.03em}\n  table{width:100%;border-collapse:collapse;font-size:13px}\n  th{position:sticky;top:0;background:var(--paper);color:var(--muted);font-size:11.5px;font-weight:600;\n     text-align:right;padding:7px 8px;border-bottom:1px solid var(--rule);white-space:nowrap}\n  td{padding:8px;border-bottom:1px solid var(--rule);text-align:right;white-space:nowrap}\n  th:first-child,td:first-child{padding-right:16px;text-align:right;font-weight:600}\n  th:last-child,td:last-child{padding-left:16px}\n  tr.clickable{cursor:pointer}\n  tr.clickable:active{background:#EEF1F5}\n  .lv{display:inline-block;font-size:11px;font-weight:700;color:#fff;border-radius:6px;padding:1px 7px}\n  .lv-error,.lv-quota{background:var(--bad)} .lv-warn{background:var(--warn)}\n  .lv-info{background:var(--flat)} .lv-ok{background:var(--ok)}\n  .pill{display:inline-block;font-size:11px;font-weight:700;color:#fff;border-radius:6px;padding:1px 7px}\n  .p-ok{background:var(--ok)} .p-warn{background:var(--warn)} .p-bad{background:var(--bad)} .p-flat{background:var(--flat)}\n  .empty{padding:26px 16px;text-align:center;color:var(--muted);line-height:1.7}\n  .err{margin:12px 16px;background:#F6DAD6;border:1px solid #E4B4AD;border-radius:12px;padding:12px;font-size:13px;line-height:1.6}\n  .err b{display:block;margin-bottom:4px}\n  .note{padding:14px 16px 0;font-size:11.5px;color:var(--muted);line-height:1.6}\n  .wrap{overflow-x:auto}\n  details{margin:0 16px}\n  details summary{cursor:pointer;font-size:13px;color:var(--muted);padding:9px 0}\n  pre{background:var(--well);border:1px solid var(--rule);border-radius:10px;padding:10px;font-size:11.5px;\n      overflow:auto;direction:ltr;line-height:1.5;margin:0 0 10px}\n  @media(min-width:720px){ .cards{grid-template-columns:repeat(4,1fr)} table{max-width:1000px;margin:0 auto} }\n</style>\n</head>\n<body>\n<header>\n  <div class=\"hrow\">\n    <h1>מה יש במסד הנתונים</h1>\n    <div class=\"spacer\"></div>\n    <button class=\"btn\" id=\"reload\" type=\"button\">רענן</button>\n  </div>\n  <div class=\"sub\" id=\"sub\">טוען…</div>\n</header>\n\n<div id=\"err\"></div>\n<div class=\"cards\" id=\"cards\"></div>\n<div class=\"quota\" id=\"quota\"></div>\n\n<h2>לפי סימבול</h2>\n<div class=\"wrap\"><table id=\"syms\"><tbody><tr><td class=\"empty\">טוען…</td></tr></tbody></table></div>\n\n<h2 id=\"daysTitle\" hidden>ימים שמורים</h2>\n<div class=\"wrap\"><table id=\"days\" hidden></table></div>\n\n<h2>יומן מערכת · KV, מחוץ ל-D1</h2>\n<div class=\"wrap\"><table id=\"log\"><tbody><tr><td class=\"empty\">טוען…</td></tr></tbody></table></div>\n\n<h2>ריצות אחרונות</h2>\n<div class=\"wrap\"><table id=\"runs\"></table></div>\n\n<details>\n  <summary>מבנה הטבלאות</summary>\n  <pre id=\"schema\"></pre>\n</details>\n\n<div class=\"note\" id=\"note\"></div>\n\n<script>\n(function(){\n  var qs=function(s){return document.querySelector(s)};\n  var bust=function(u){return u+(u.indexOf('?')>0?'&':'?')+'ts='+Date.now()};\n  var j=function(u){return fetch(bust(u),{cache:'no-store'}).then(function(r){\n    return r.json().then(function(d){ if(!r.ok&&!d.symbols)throw d; return d; }); })};\n  var n=function(v){return v==null?'—':Number(v).toLocaleString()};\n  var openSym=null;\n\n  function ago(sec){\n    if(sec==null)return 'מעולם';\n    if(sec<90)return 'לפני '+sec+' שנ׳';\n    var m=Math.round(sec/60); if(m<90)return 'לפני '+m+' דק׳';\n    var h=Math.round(m/60); if(h<48)return 'לפני '+h+' שע׳';\n    return 'לפני '+Math.round(h/24)+' ימים';\n  }\n  function when(ts){ return ts?new Date(ts*1000).toISOString().replace('T',' ').slice(0,16)+'Z':'—'; }\n\n  function render(d){\n    qs('#err').innerHTML='';\n    var syms=d.symbols||[], withData=syms.filter(function(s){return s.bars>0});\n    var days=syms.reduce(function(a,s){return a+s.days},0);\n    var revs=syms.reduce(function(a,s){return a+(s.revisions||0)},0);\n    var errs=syms.filter(function(s){return s.last_error});\n    qs('#sub').textContent='נכון ל-'+d.time.replace('T',' ').slice(0,19)+'Z · יום מסחר '+d.today_et;\n\n    // storage estimate: a bar row is roughly 60 bytes with its index\n    var mb=(d.total_bars*60/1048576);\n    qs('#cards').innerHTML=\n      '<div class=\"card\"><small>נרות שמורים</small><b class=\"num\">'+n(d.total_bars)+'</b><span>שורות בטבלת bars</span></div>'+\n      '<div class=\"card\"><small>סימבולים</small><b class=\"num\">'+withData.length+' / '+syms.length+'</b><span>עם נתונים / במעקב</span></div>'+\n      '<div class=\"card\"><small>ימי מסחר</small><b class=\"num\">'+n(days)+'</b><span>צירופי סימבול-יום</span></div>'+\n      '<div class=\"card\"><small>נפח משוער</small><b class=\"num\">'+(mb<1?(mb*1024).toFixed(0)+' KB':mb.toFixed(1)+' MB')+'</b><span>מתוך 5GB · '+(mb/5120*100).toFixed(2)+'%</span></div>';\n\n    var u=d.usage||{};\n    if(u.read_pct!=null){\n      var cls=function(p){return p>=80?'bad':p>=60?'warn':''};\n      qs('#quota').innerHTML=\n        '<div class=\"lbl\"><span>קריאות היום</span><b class=\"num\">'+n(u.reads)+' / '+n(u.read_limit)+' ('+u.read_pct+'%)</b></div>'+\n        '<div class=\"meter '+cls(u.read_pct)+'\"><i style=\"width:'+Math.min(100,u.read_pct)+'%\"></i></div>'+\n        '<div class=\"lbl\"><span>כתיבות היום</span><b class=\"num\">'+n(u.writes)+' / '+n(u.write_limit)+' ('+u.write_pct+'%)</b></div>'+\n        '<div class=\"meter '+cls(u.write_pct)+'\"><i style=\"width:'+Math.min(100,u.write_pct)+'%\"></i></div>'+\n        '<div class=\"lbl\" style=\"margin:0\"><span>מתאפס בחצות UTC</span><span>'+(u.day||'')+'</span></div>';\n    } else qs('#quota').innerHTML='<div class=\"lbl\"><span>מכסה</span><b>לא זמין</b></div>';\n\n    // symbols\n    var head='<thead><tr><th>סימבול</th><th>נרות</th><th>ימים</th><th>נר אחרון</th><th>נמשך</th><th>תיקונים</th><th>מצב</th></tr></thead>';\n    var body=syms.map(function(s){\n      var state=s.last_error?'<span class=\"pill p-bad\">שגיאה</span>'\n        :s.bars===0?'<span class=\"pill p-flat\">ריק</span>'\n        :s.data_stale?'<span class=\"pill p-warn\">ישן</span>':'<span class=\"pill p-ok\">עדכני</span>';\n      return '<tr class=\"clickable\" data-s=\"'+s.symbol+'\"><td>'+s.symbol+'</td><td class=\"num\">'+n(s.bars)+'</td>'+\n        '<td class=\"num\">'+n(s.days)+'</td><td class=\"num\">'+ago(s.stale_seconds)+'</td>'+\n        '<td class=\"num\">'+ago(s.last_fetch_at?Math.floor(Date.now()/1000)-s.last_fetch_at:null)+'</td>'+\n        '<td class=\"num\">'+n(s.revisions)+'</td><td>'+state+'</td></tr>'+\n        (s.last_error?'<tr><td colspan=\"7\" style=\"color:var(--bad);font-size:12px;white-space:normal\">'+s.last_error+'</td></tr>':'');\n    }).join('');\n    qs('#syms').innerHTML=head+'<tbody>'+(body||'<tr><td class=\"empty\">אין סימבולים</td></tr>')+'</tbody>';\n    [].forEach.call(document.querySelectorAll('#syms tr[data-s]'),function(tr){\n      tr.onclick=function(){ loadDays(tr.dataset.s); };\n    });\n\n    // runs\n    var rh='<thead><tr><th>מתי</th><th>סוג</th><th>סטטוס</th><th>סימבולים</th><th>נכתבו</th><th>משך</th></tr></thead>';\n    var rb=(d.recent_runs||[]).map(function(r){\n      var cls=r.status==='ok'?'p-ok':r.status==='partial'?'p-warn':r.status==='running'?'p-flat':'p-bad';\n      var dur=r.finished_at&&r.started_at?(r.finished_at-r.started_at)+'ש':'—';\n      return '<tr><td class=\"num\">'+when(r.started_at)+'</td><td>'+r.kind+'</td>'+\n        '<td><span class=\"pill '+cls+'\">'+r.status+'</span></td><td class=\"num\">'+r.symbols+'</td>'+\n        '<td class=\"num\">'+n(r.rows_written)+'</td><td class=\"num\">'+dur+'</td></tr>'+\n        (r.errors?'<tr><td colspan=\"6\" style=\"color:var(--bad);font-size:12px;white-space:normal\">'+r.errors+'</td></tr>':'');\n    }).join('');\n    qs('#runs').innerHTML=rh+'<tbody>'+(rb||'<tr><td class=\"empty\">עוד לא רצה אף ריצה</td></tr>')+'</tbody>';\n\n    qs('#note').innerHTML='כל המספרים כאן מגיעים מטבלאות המונים (days, symbols, runs, usage). '+\n      'טבלת הנרות עצמה לא נסרקת, אחרת הצפייה בעמוד הזה הייתה שורפת את המכסה שהוא בא להציג. '+\n      'הנפח הוא הערכה לפי כ-60 בייט לשורה, לא מדידה.';\n  }\n\n  function loadDays(sym){\n    openSym=sym;\n    qs('#daysTitle').hidden=false; qs('#days').hidden=false;\n    qs('#daysTitle').textContent='ימים שמורים · '+sym;\n    qs('#days').innerHTML='<tbody><tr><td class=\"empty\">טוען…</td></tr></tbody>';\n    j('/days/'+sym).then(function(d){\n      var rows=(d.days||[]).map(function(x){\n        var full=x.bars>=380?'p-ok':x.bars>=100?'p-warn':'p-flat';\n        return '<tr><td class=\"num\">'+x.date+'</td><td class=\"num\">'+n(x.bars)+'</td>'+\n          '<td class=\"num\">'+(x.first||'—')+'–'+(x.last||'—')+'</td>'+\n          '<td class=\"num\">'+n(x.revisions)+'</td>'+\n          '<td><span class=\"pill '+full+'\">'+(x.bars>=380?'יום מלא':x.bars>=100?'חלקי':'מעט')+'</span></td></tr>';\n      }).join('');\n      qs('#days').innerHTML='<thead><tr><th>תאריך</th><th>נרות</th><th>טווח שעות</th><th>תיקונים</th><th></th></tr></thead>'+\n        '<tbody>'+(rows||'<tr><td class=\"empty\">אין ימים שמורים ל-'+sym+'</td></tr>')+'</tbody>';\n      qs('#daysTitle').scrollIntoView({behavior:'smooth',block:'start'});\n    },function(e){ qs('#days').innerHTML='<tbody><tr><td class=\"empty\">לא הצלחתי לטעון: '+(e&&e.message||e)+'</td></tr></tbody>'; });\n  }\n\n  function fail(e){\n    var msg=(e&&e.message)||String(e);\n    var quota=/exceeded|limit/i.test(msg);\n    qs('#sub').textContent='לא הצלחתי לקרוא את מסד הנתונים';\n    qs('#err').innerHTML='<div class=\"err\"><b>'+(quota?'המכסה היומית של D1 נגמרה':'שגיאה')+'</b>'+msg+\n      (quota?'<br><br>הנתונים השמורים לא נפגעו. המכסה מתאפסת בחצות UTC, ואז העמוד הזה יעבוד.'+\n        '<br>יומן המערכת למטה יושב ב-KV ולא ב-D1, ולכן הוא ממשיך לעבוד גם עכשיו.':'')+'</div>';\n    qs('#cards').innerHTML=''; qs('#quota').innerHTML='';\n    qs('#syms').innerHTML='<tbody><tr><td class=\"empty\">—</td></tr></tbody>';\n    qs('#runs').innerHTML='<tbody><tr><td class=\"empty\">—</td></tr></tbody>';\n  }\n\n  function loadLog(){\n    // Fetched separately and on purpose: this is the one panel that must still\n    // render when the database is the thing that is broken.\n    return j('/log?days=7').then(function(d){\n      if(!d.available){\n        qs('#log').innerHTML='<tbody><tr><td class=\"empty\">היומן לא מוגדר.<br>'+\n          'צריך לקשור KV בשם <b>LOG</b> ב-Settings → Bindings של ה-Worker.<br>'+\n          '<span style=\"font-size:11.5px\">('+(d.reason||'')+')</span></td></tr></tbody>';\n        return;\n      }\n      var rows=(d.entries||[]).slice(0,60).map(function(e){\n        var cls='lv-'+(e.level||'info');\n        var extra=e.extra?Object.keys(e.extra).map(function(k){\n          var v=e.extra[k]; return k+'='+(Array.isArray(v)?v.join(' / '):v);\n        }).join(' · '):'';\n        return '<tr><td class=\"num\">'+e.t.replace('T',' ').slice(5,19)+'</td>'+\n          '<td><span class=\"lv '+cls+'\">'+(e.level||'info')+'</span></td>'+\n          '<td>'+(e.code||'')+'</td>'+\n          '<td style=\"white-space:normal;text-align:right\">'+e.message+\n          (extra?'<div style=\"font-size:11px;color:var(--muted);margin-top:2px\">'+extra+'</div>':'')+'</td></tr>';\n      }).join('');\n      qs('#log').innerHTML='<thead><tr><th>מתי (UTC)</th><th>רמה</th><th>קוד</th><th>מה קרה</th></tr></thead>'+\n        '<tbody>'+(rows||'<tr><td class=\"empty\">אין רשומות בשבעת הימים האחרונים</td></tr>')+'</tbody>';\n    },function(e){\n      qs('#log').innerHTML='<tbody><tr><td class=\"empty\">לא הצלחתי לקרוא את היומן: '+((e&&e.message)||e)+'</td></tr></tbody>';\n    });\n  }\n  function load(){\n    qs('#sub').textContent='טוען…';\n    loadLog();                       // independent of the call below\n    return j('/status').then(render,fail);\n  }\n  qs('#reload').onclick=function(){ load().then(function(){ if(openSym)loadDays(openSym); }); };\n  qs('#schema').textContent=\n    'bars     symbol, unix, date, time, open, high, low, close, volume,\\n'+\n    '         first_seen, updated_at, revisions        PK(symbol, unix)\\n'+\n    '         index: (symbol, date)\\n\\n'+\n    'days     symbol, date, bars, revisions, first, last   PK(symbol, date)\\n'+\n    '         per-day counters, so nothing has to scan bars\\n\\n'+\n    'symbols  symbol, added_at, last_fetch_at, last_bar_unix,\\n'+\n    '         last_error, last_backfill_at              PK(symbol)\\n\\n'+\n    'runs     id, started_at, finished_at, kind, status, symbols,\\n'+\n    '         rows_written, errors                      PK(id)\\n\\n'+\n    'meta     key, value                                PK(key)\\n'+\n    '         holds schema_version\\n\\n'+\n    'usage    day, reads, writes, queries               PK(day)\\n'+\n    '         daily D1 row accounting';\n  load();\n})();\n</script>\n</body>\n</html>\n";
export const DATA_HTML = "<!doctype html>\n<html lang=\"he\" dir=\"rtl\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width,initial-scale=1,viewport-fit=cover\">\n<title>קריאת מסד הנתונים</title>\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link href=\"https://fonts.googleapis.com/css2?family=Heebo:wght@400;600;800&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{--paper:#F2F4F7;--ink:#1B2430;--muted:#5B6673;--rule:#D6DBE2;--well:#FFFFFF;\n    --up:#0F766E;--down:#B42318;--warn:#B45309;--flat:#8A94A0}\n  *{box-sizing:border-box}\n  html,body{margin:0;background:var(--paper);color:var(--ink);font-family:Heebo,system-ui,sans-serif;font-variant-numeric:tabular-nums;max-width:100%;overflow-x:hidden}\n  body{padding-bottom:calc(30px + env(safe-area-inset-bottom))}\n  .num{direction:ltr;unicode-bidi:isolate}\n  button,select,input{font:inherit}\n  button:focus-visible,select:focus-visible,input:focus-visible{outline:2px solid var(--ink);outline-offset:2px}\n\n  header{position:sticky;top:0;z-index:20;background:var(--paper);border-bottom:1px solid var(--rule)}\n  .hrow{display:flex;align-items:center;gap:10px;padding:12px 16px 8px}\n  h1{margin:0;font-size:18px;font-weight:800}\n  .spacer{flex:1}\n  .btn{background:var(--well);border:1px solid var(--rule);border-radius:9px;padding:6px 11px;font-size:13px;color:var(--ink)}\n  .btn.on{background:var(--ink);color:var(--paper);border-color:var(--ink)}\n  .tabs{display:flex;gap:6px;padding:0 16px 8px;overflow-x:auto;scrollbar-width:none}\n  .tabs::-webkit-scrollbar{display:none}\n  .ctrl{display:flex;gap:8px;align-items:center;padding:0 16px 10px;flex-wrap:wrap}\n  select,input[type=search]{background:var(--well);border:1px solid var(--rule);border-radius:9px;padding:7px 10px;font-size:13px;color:var(--ink)}\n  input[type=search]{min-width:150px;flex:1}\n  .cost{font-size:11.5px;color:var(--muted);padding:0 16px 8px}\n  .cost b{color:var(--ink)}\n  .cost.hi b{color:var(--down)}\n\n  .wrap{overflow-x:auto;-webkit-overflow-scrolling:touch}\n  table{border-collapse:collapse;font-size:12.5px;min-width:100%}\n  th{position:sticky;top:0;background:var(--paper);color:var(--muted);font-size:11px;font-weight:600;\n     text-align:left;padding:7px 9px;border-bottom:1px solid var(--rule);white-space:nowrap;direction:ltr}\n  td{padding:6px 9px;border-bottom:1px solid var(--rule);white-space:nowrap;direction:ltr;text-align:left}\n  tbody tr:nth-child(even){background:rgba(0,0,0,.015)}\n  td.up{color:var(--up)} td.down{color:var(--down)}\n  td.hi{font-weight:700}\n  .empty{padding:26px 16px;text-align:center;color:var(--muted);line-height:1.7}\n  .err{margin:12px 16px;background:#F6DAD6;border:1px solid #E4B4AD;border-radius:12px;padding:12px;font-size:13px;line-height:1.6}\n  .foot{display:flex;gap:8px;align-items:center;padding:12px 16px;flex-wrap:wrap}\n  .note{padding:0 16px 12px;font-size:11.5px;color:var(--muted);line-height:1.6}\n  .toast{position:fixed;bottom:20px;right:50%;transform:translateX(50%);background:var(--ink);color:var(--paper);\n    padding:9px 16px;border-radius:10px;font-size:13px;z-index:60}\n  .toast[hidden]{display:none}\n  @media(min-width:720px){ .wrap{max-width:1100px;margin:0 auto} }\n</style>\n</head>\n<body>\n<header>\n  <div class=\"hrow\">\n    <h1>קריאת מסד הנתונים</h1>\n    <div class=\"spacer\"></div>\n    <button class=\"btn\" id=\"reload\" type=\"button\">רענן</button>\n  </div>\n  <div class=\"tabs\" id=\"tabs\"></div>\n  <div class=\"ctrl\" id=\"ctrl\"></div>\n  <div class=\"cost\" id=\"cost\"></div>\n</header>\n\n<div id=\"err\"></div>\n<div class=\"wrap\"><table id=\"grid\"><tbody><tr><td class=\"empty\">טוען…</td></tr></tbody></table></div>\n<div class=\"foot\" id=\"foot\"></div>\n<div class=\"note\" id=\"note\"></div>\n<div class=\"toast\" id=\"toast\" hidden></div>\n\n<script>\n(function(){\n  var qs=function(s){return document.querySelector(s)};\n  var view='bars', sym=null, date=null, filter='', offset=0, LIMIT=200;\n  var rows=[], cols=[], lastCost=null, tables=[], symbols=[], dates=[];\n\n  function bust(u){return u+(u.indexOf('?')>0?'&':'?')+'ts='+Date.now()}\n  function fetchJ(u){\n    return fetch(bust(u),{cache:'no-store'}).then(function(r){\n      // The service reports what every request cost, so browsing can show it.\n      lastCost={reads:+(r.headers.get('X-Rows-Read')||0),queries:+(r.headers.get('X-Queries')||0),\n        top:r.headers.get('X-Top-Query')||null};\n      return r.json().then(function(d){ if(!r.ok&&d&&d.error)throw new Error(d.message||d.error); return d; });\n    });\n  }\n  function toast(t){var e=qs('#toast');e.textContent=t;e.hidden=false;clearTimeout(e._t);e._t=setTimeout(function(){e.hidden=true},1800)}\n  function esc(v){return v==null?'':String(v).replace(/[<>&]/g,function(c){return {'<':'&lt;','>':'&gt;','&':'&amp;'}[c]})}\n\n  function drawTabs(){\n    var list=[['bars','נרות']].concat(tables.map(function(t){return [t,t]}));\n    qs('#tabs').innerHTML=list.map(function(x){\n      return '<button class=\"btn '+(view===x[0]?'on':'')+'\" data-v=\"'+x[0]+'\">'+x[1]+'</button>';\n    }).join('');\n    [].forEach.call(document.querySelectorAll('#tabs .btn'),function(b){\n      b.onclick=function(){ view=b.dataset.v; offset=0; filter=''; draw(); load(); };\n    });\n  }\n\n  function drawCtrl(){\n    var c=qs('#ctrl');\n    if(view==='bars'){\n      c.innerHTML='<select id=\"sym\">'+symbols.map(function(s){return '<option'+(s===sym?' selected':'')+'>'+s+'</option>'}).join('')+'</select>'+\n        '<select id=\"date\">'+dates.map(function(d){return '<option value=\"'+d.date+'\"'+(d.date===date?' selected':'')+'>'+d.date+' · '+d.bars+'</option>'}).join('')+'</select>'+\n        '<input type=\"search\" id=\"q\" placeholder=\"סינון (שעה, מחיר…)\" value=\"'+esc(filter)+'\">';\n      qs('#sym').onchange=function(){ sym=qs('#sym').value; date=null; offset=0; loadDates().then(load); };\n      qs('#date').onchange=function(){ date=qs('#date').value; offset=0; load(); };\n    } else {\n      c.innerHTML='<input type=\"search\" id=\"q\" placeholder=\"סינון\" value=\"'+esc(filter)+'\">'+\n        '<span style=\"font-size:12px;color:var(--muted)\">טבלה: '+view+'</span>';\n    }\n    var q=qs('#q'); if(q)q.oninput=function(){ filter=q.value.trim().toLowerCase(); draw(); };\n  }\n\n  function drawCost(){\n    if(!lastCost){qs('#cost').textContent='';return}\n    var el=qs('#cost'); var hi=lastCost.reads>2000;\n    el.className='cost'+(hi?' hi':'');\n    el.innerHTML='הבקשה האחרונה קראה <b>'+lastCost.reads.toLocaleString()+'</b> שורות ב-'+lastCost.queries+' שאילתות'+\n      (hi?' — יקר':'')+(lastCost.top?'<br>'+esc(lastCost.top):'');\n  }\n\n  function draw(){\n    drawTabs(); drawCtrl(); drawCost();\n    var list=rows;\n    if(filter){\n      list=rows.filter(function(r){\n        return cols.some(function(c){ return String(r[c]==null?'':r[c]).toLowerCase().indexOf(filter)>=0; });\n      });\n    }\n    if(!list.length){\n      qs('#grid').innerHTML='<tbody><tr><td class=\"empty\">'+(rows.length?'אין שורות שתואמות לסינון':'אין שורות')+'</td></tr></tbody>';\n    } else {\n      var head='<thead><tr>'+cols.map(function(c){return '<th>'+c+'</th>'}).join('')+'</tr></thead>';\n      var body=list.map(function(r){\n        return '<tr>'+cols.map(function(c){\n          var v=r[c], cls='';\n          if(c==='close'&&r.open!=null)cls=v>r.open?'up':v<r.open?'down':'';\n          if(c==='revisions'&&v>0)cls='hi';\n          return '<td class=\"'+cls+'\">'+esc(v)+'</td>';\n        }).join('')+'</tr>';\n      }).join('');\n      qs('#grid').innerHTML=head+'<tbody>'+body+'</tbody>';\n    }\n    var f=qs('#foot');\n    f.innerHTML='<span style=\"font-size:12.5px;color:var(--muted)\">'+list.length+' מתוך '+rows.length+' שורות'+\n      (view!=='bars'?' · מתחיל ב-'+offset:'')+'</span><div class=\"spacer\"></div>'+\n      (view!=='bars'?'<button class=\"btn\" id=\"prev\" '+(offset<=0?'disabled':'')+'>הקודם</button>'+\n        '<button class=\"btn\" id=\"next\" '+(rows.length<LIMIT?'disabled':'')+'>הבא</button>':'')+\n      '<button class=\"btn\" id=\"copy\">העתק CSV</button>'+\n      (view==='bars'&&sym?'<button class=\"btn\" id=\"dl\">הורד קובץ</button>':'');\n    var p=qs('#prev'); if(p)p.onclick=function(){ offset=Math.max(0,offset-LIMIT); load(); };\n    var n=qs('#next'); if(n)n.onclick=function(){ offset+=LIMIT; load(); };\n    qs('#copy').onclick=function(){\n      var txt=[cols.join(',')].concat(list.map(function(r){return cols.map(function(c){return r[c]==null?'':r[c]}).join(',')})).join('\\n');\n      (navigator.clipboard?navigator.clipboard.writeText(txt):Promise.reject()).then(\n        function(){toast(list.length+' שורות הועתקו')},function(){prompt('העתק:',txt)});\n    };\n    var d=qs('#dl'); if(d)d.onclick=function(){ location.href='/export/'+sym+(date?'/'+date:''); };\n    qs('#note').textContent = view==='bars'\n      ? 'הנרות נקראים לפי סימבול ותאריך, דרך האינדקס — קריאה של יום שלם עולה כמספר הנרות שבו, לא יותר.'\n      : 'הטבלאות האלה קטנות. טבלת הנרות לא נפתחת כאן ישירות כדי שדפדוף לא יוכל לסרוק אותה.';\n  }\n\n  function fail(e){\n    var msg=(e&&e.message)||String(e);\n    qs('#err').innerHTML='<div class=\"err\"><b>לא הצלחתי לקרוא</b><br>'+esc(msg)+\n      (/limit|exceeded/i.test(msg)?'<br><br>המכסה היומית נגמרה. הנתונים לא נפגעו; היא מתאפסת בחצות UTC.':'')+'</div>';\n    qs('#grid').innerHTML='<tbody><tr><td class=\"empty\">—</td></tr></tbody>';\n  }\n\n  function loadDates(){\n    if(!sym)return Promise.resolve();\n    return fetchJ('/days/'+sym).then(function(d){\n      dates=(d.days||[]);\n      if(!date||!dates.some(function(x){return x.date===date}))date=dates.length?dates[0].date:null;\n    });\n  }\n  function load(){\n    qs('#err').innerHTML='';\n    var p;\n    if(view==='bars'){\n      if(!sym||!date){ rows=[]; cols=[]; draw(); return Promise.resolve(); }\n      p=fetchJ('/day/'+sym+'/'+date+'?format=json').then(function(d){\n        rows=d.rows||[];\n        cols=rows.length?Object.keys(rows[0]):[];\n      });\n    } else {\n      p=fetchJ('/table/'+view+'?limit='+LIMIT+'&offset='+offset).then(function(d){\n        rows=d.rows||[]; cols=d.columns&&d.columns.length?d.columns:(rows.length?Object.keys(rows[0]):[]);\n      });\n    }\n    return p.then(draw,fail);\n  }\n\n  qs('#reload').onclick=function(){ load(); };\n\n  // start: what tables exist, what symbols exist\n  Promise.all([\n    fetchJ('/table').then(function(d){ tables=d.tables||[] },function(){ tables=[] }),\n    fetchJ('/').then(function(h){ symbols=(h.tracked||[]).slice(); sym=symbols[0]||null; })\n  ]).then(loadDates).then(load).catch(fail);\n})();\n</script>\n</body>\n</html>\n";
export const SCAN_HTML = "<!doctype html>\n<html lang=\"he\" dir=\"rtl\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width,initial-scale=1,viewport-fit=cover\">\n<title>סריקה · 100</title>\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link href=\"https://fonts.googleapis.com/css2?family=Heebo:wght@400;600;800&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{--paper:#F2F4F7;--ink:#1B2430;--muted:#5B6673;--rule:#D6DBE2;--well:#FFFFFF;\n    --up:#0F766E;--down:#B42318;--flat:#8A94A0;\n    --s-quiet:#8A94A0;--s-watch:#B7791F;--s-close:#C2410C;--s-ready:#0F766E;--s-active:#1D4ED8;--s-avoid:#B42318;--s-none:#98A2B3}\n  *{box-sizing:border-box}\n  html,body{margin:0;background:var(--paper);color:var(--ink);font-family:Heebo,system-ui,sans-serif;font-variant-numeric:tabular-nums;max-width:100%;overflow-x:hidden}\n  body{padding-bottom:calc(24px + env(safe-area-inset-bottom))}\n  .num{direction:ltr;unicode-bidi:isolate}\n  button,select{font:inherit}\n  header{position:sticky;top:0;z-index:20;background:var(--paper);border-bottom:1px solid var(--rule)}\n  .hrow{display:flex;align-items:center;gap:10px;padding:12px 16px 8px}\n  h1{margin:0;font-size:19px;font-weight:800}\n  .spacer{flex:1}\n  .build{font-size:10px;color:var(--muted);direction:ltr;unicode-bidi:isolate;opacity:.8}\n  .mini-btn{background:var(--well);border:1px solid var(--rule);border-radius:9px;padding:6px 10px;font-size:13px;color:var(--ink);text-decoration:none}\n  .note{padding:0 16px 8px;font-size:12px;color:var(--muted);line-height:1.5}\n  .note b{color:var(--ink)}\n  .ctrl{display:flex;align-items:center;gap:8px;padding:0 16px 10px;font-size:13px;color:var(--muted);flex-wrap:wrap}\n  .ctrl select{max-width:46vw}\n  .counts{-webkit-overflow-scrolling:touch}\n  .ctrl select{background:var(--well);border:1px solid var(--rule);border-radius:9px;padding:6px 9px;font-size:13px;color:var(--ink)}\n  .counts{display:flex;gap:6px;overflow-x:auto;padding:0 16px 10px;scrollbar-width:none}\n  .cnt{border-radius:8px;padding:4px 9px;font-size:12px;font-weight:600;color:#fff;white-space:nowrap;border:none}\n  .cnt[data-on=\"0\"]{opacity:.35} .cnt.sel{outline:2px solid var(--ink);outline-offset:1px}\n  .bg-QUIET{background:var(--s-quiet)} .bg-WATCH{background:var(--s-watch)} .bg-CLOSE{background:var(--s-close)}\n  .bg-READY{background:var(--s-ready)} .bg-ACTIVE{background:var(--s-active)} .bg-AVOID{background:var(--s-avoid)} .bg-NODATA{background:var(--s-none)}\n  .rows{padding:6px 10px 0}\n  /* Three columns with a hard minimum-width of zero on the middle one. Without\n     min-width:0 a grid track refuses to shrink below its content, so the long\n     reason text pushed the score and the buttons out of the card. */\n  .row{display:grid;grid-template-columns:62px minmax(0,1fr) 62px;gap:6px 10px;background:var(--well);\n    border:1px solid var(--rule);border-left:4px solid var(--s-none);border-radius:12px;\n    padding:10px 12px;margin-bottom:7px;align-items:start;overflow:hidden}\n  .row>*{min-width:0}\n  .row .sym{font-weight:800;font-size:15px;overflow:hidden;text-overflow:ellipsis}\n  .row .px{font-size:12.5px;color:var(--muted);font-weight:600;direction:ltr;unicode-bidi:isolate}\n  .row .st{display:inline-block;font-size:11px;font-weight:800;color:#fff;border-radius:6px;padding:2px 7px}\n  /* The reason wraps to two lines instead of being cut mid-word: it is the\n     whole point of the row, and an ellipsis hid the second half of every one. */\n  .row .why{font-size:12.5px;margin:0 0 3px;line-height:1.45;overflow:hidden;\n    display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical}\n  .row .meta{font-size:11px;color:var(--muted);margin-top:2px;display:flex;gap:4px 9px;flex-wrap:wrap;align-items:baseline}\n  .row .meta b{color:var(--ink);font-weight:600}\n  .row .score{font-size:20px;font-weight:800;line-height:1;text-align:center}\n  .row .score small{font-size:9.5px;color:var(--muted);font-weight:600}\n  .QUIET{border-left-color:var(--s-quiet)} .WATCH{border-left-color:var(--s-watch)} .CLOSE{border-left-color:var(--s-close)}\n  .READY{border-left-color:var(--s-ready)} .ACTIVE{border-left-color:var(--s-active)} .AVOID{border-left-color:var(--s-avoid)}\n  .row.stale{opacity:.7}\n  /* The right-hand column is a stack, not a line: score, then state, then the\n     two actions, each full width so nothing spills sideways. */\n  .row .rt{display:flex;flex-direction:column;align-items:stretch;gap:4px}\n  .track{background:var(--ink);color:var(--paper);border:none;border-radius:8px;padding:4px 2px;\n    font-size:11px;font-weight:600;width:100%;white-space:nowrap}\n  .track:disabled{opacity:.5}\n  .live{display:block;text-align:center;font-size:11px;font-weight:700;color:var(--up)}\n  .hist{margin:0;background:var(--well);border:1px solid var(--rule);border-radius:8px;padding:4px 2px;\n    font-size:10.5px;color:var(--muted);width:100%;white-space:nowrap}\n  .empty{padding:22px 16px;text-align:center;color:var(--muted);line-height:1.6}\n  .prog{font-size:12px;color:var(--muted)}\n  .sheet{position:fixed;inset:0;z-index:40;background:rgba(20,26,34,.45);display:flex;align-items:flex-end}\n  .sheet[hidden]{display:none}\n  .hpanel{background:var(--paper);width:100%;max-height:88vh;overflow:auto;border-radius:18px 18px 0 0;padding-bottom:calc(20px + env(safe-area-inset-bottom))}\n  .hhead{display:flex;align-items:center;gap:10px;padding:14px 16px 8px;border-bottom:1px solid var(--rule);position:sticky;top:0;background:var(--paper)}\n  .hhead b{font-size:19px;font-weight:800}\n  .hsum{font-size:12.5px;color:var(--muted)}\n  .hhead button{background:var(--well);border:1px solid var(--rule);border-radius:9px;padding:6px 11px;font-size:13px}\n  .hdays{padding:6px 16px 0}\n  .hd{display:grid;grid-template-columns:1fr auto auto auto;gap:10px;align-items:center;padding:9px 0;border-bottom:1px solid var(--rule);font-size:13.5px}\n  .hd b{font-weight:700;color:var(--muted);font-size:12.5px}\n  .hcopy,.hdl{background:var(--well);border:1px solid var(--rule);border-radius:8px;padding:5px 10px;font-size:12px;color:var(--ink);text-decoration:none}\n  .hall{display:flex;gap:8px;padding:14px 16px}\n  .hall button{background:var(--ink);color:var(--paper);border:none;border-radius:10px;padding:9px 13px;font-size:13px;font-weight:600}\n  .sugg{margin:8px 10px 0;background:var(--well);border:1px solid var(--rule);border-radius:12px;padding:10px 12px;overflow:hidden}\n  .sugg[hidden]{display:none}\n  .sh{font-size:12.5px;font-weight:800;color:var(--muted);margin:4px 0 6px;letter-spacing:.02em}\n  .sg{border-top:1px solid var(--rule);padding:8px 0}\n  .sg:first-of-type{border-top:none}\n  .sgtop{display:flex;align-items:baseline;gap:8px;flex-wrap:wrap}\n  .sgtop b{font-size:15px;font-weight:800}\n  .sgs{font-size:12.5px;color:var(--muted);font-weight:700}\n  .sgw{font-size:12.5px;color:var(--ink);margin-top:2px;line-height:1.5}\n  .sgsw{font-size:12px;color:var(--s-close);margin-top:3px}\n  .sgnone{font-size:12.5px;color:var(--muted);padding:4px 0}\n  .sgnote{font-size:11.5px;color:var(--muted);margin-top:8px;padding-top:7px;border-top:1px solid var(--rule)}\n  .sg .track{margin-top:7px;width:auto;padding:6px 14px;font-size:12px}\n  .dir{font-weight:700} .dir.up{color:var(--up)} .dir.dn{color:var(--down)}\n  .lvls2{font-size:11px;color:var(--muted);margin-top:3px;line-height:1.5;\n    overflow:hidden;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical}\n  .dir{white-space:nowrap}\n  .ev{white-space:nowrap;font-weight:700;padding:1px 6px;border-radius:5px;background:var(--rule);color:var(--ink)}\n  .ev.up{background:#D7EFEC;color:var(--up)} .ev.dn{background:#F6DAD6;color:var(--down)}\n  .cap{font-size:11.5px;color:var(--s-close);margin-top:3px}\n  .toast{position:fixed;bottom:24px;right:50%;transform:translateX(50%);background:var(--ink);color:var(--paper);padding:9px 16px;border-radius:10px;font-size:13px;z-index:60}\n  .toast[hidden]{display:none}\n  @media(min-width:720px){\n    .rows,.sugg{max-width:900px;margin-left:auto;margin-right:auto}\n    .row{grid-template-columns:90px minmax(0,1fr) 96px}\n    .row .why{-webkit-line-clamp:1}\n    .track,.hist{font-size:12px;padding:5px 6px}\n  }\n</style>\n</head>\n<body>\n<header>\n  <div class=\"hrow\">\n    <h1 id=\"title\">סריקה</h1>\n    <div class=\"spacer\"></div>\n    <a class=\"mini-btn\" href=\"/radar\">מעקב · 20</a>\n    <button class=\"mini-btn\" id=\"reload\" type=\"button\">רענן</button>\n    <span class=\"build\" title=\"גרסת העמוד\">v138  (2026-09-05 16:01Z)</span>\n  </div>\n  <div class=\"note\">כל המניות בארכיון. הנתונים מפגרים עד שעה אחרי השוק, לכן <b>זו סריקה לזיהוי מועמדים, לא מסך מסחר</b>. מי שמעניין — עוברים אליו במעקב.</div>\n  <div class=\"ctrl\">\n    <span>מיון</span>\n    <select id=\"sort\">\n      <option value=\"score\" selected>ציון מועמדות</option>\n      <option value=\"direction\">מתחזקים</option>\n      <option value=\"range\">טווח יומי</option>\n      <option value=\"volume\">מחזור</option>\n      <option value=\"symbol\">סימבול</option>\n    </select>\n    <select id=\"date\" aria-label=\"תאריך\"></select>\n    <div class=\"spacer\"></div>\n    <button class=\"mini-btn\" id=\"fill\" type=\"button\" hidden></button>\n    <span class=\"prog\" id=\"prog\"></span>\n  </div>\n  <div class=\"note\" id=\"when\"></div>\n  <div class=\"note\" id=\"fillmsg\" hidden style=\"color:var(--s-close)\"></div>\n  <div class=\"counts\" id=\"counts\"></div>\n</header>\n<div class=\"sugg\" id=\"sugg\" hidden></div>\n<div class=\"rows\" id=\"rows\"><div class=\"empty\">טוען…</div></div>\n<div class=\"sheet\" id=\"sheet\" hidden><div class=\"hpanel\" id=\"hpanel\"></div></div>\n<div class=\"toast\" id=\"toast\" hidden></div>\n\n<script>\n// bars-vault structure engine — pure function over one day's 1-minute bars.\n// Inlined into view.html by build-view.mjs; unit-tested directly in node.\n//\n// analyze(rows, { K: 3 }) -> { bars, swings, state, events }\n//   rows: [{ time, open, high, low, close, volume }] in chronological order.\n//\n// Swing confirmation is K bars on each side (default 3, per the owner's\n// \"K=3, no exceptions\" rule; the spec that inspired this used 2). A swing is\n// only usable from the bar that confirms it, so nothing here looks ahead:\n// the state at bar i is computed from bars 0..i.\nfunction analyze(allRows, opts) {\n  var K = (opts && opts.K) || 3;\n  // A flat zero-volume bar is an end-of-session filler, not market activity.\n  // It must not touch momentum, wicks, EMA, volume or scoring.\n  //\n  // ONE EXCEPTION: the 16:00 bar. The closing auction prints there and the feed\n  // often reports it flat with no volume attached, so the old rule threw away\n  // the OFFICIAL CLOSE and left 15:59 standing in for it. Measured on WMT: the\n  // only session whose close matched the provider's daily candle was the one\n  // whose last bar was 16:00, and dropping that bar moved 108.42 to 108.45.\n  var isAuction = function (r) { return r.time === '16:00'; };\n  var rows = allRows.filter(function (r) {\n    return isAuction(r) || !(r.volume === 0 && r.high === r.low);\n  });\n  var n = rows.length, skipped = allRows.length - n;\n  if (!n) return null;\n\n  // ---- per-bar derived values (causal: volume average is of bars 0..i)\n  var bars = [], volSum = 0, rangeSum = 0, dayHigh = -Infinity, dayLow = Infinity;\n  var pvSum = 0, ema9 = null, ema20 = null, k9 = 2 / 10, k20 = 2 / 21, belowVwapRun = 0;\n  for (var i = 0; i < n; i++) {\n    var r = rows[i], range = r.high - r.low, body = Math.abs(r.close - r.open);\n    volSum += r.volume; rangeSum += range;\n    dayHigh = Math.max(dayHigh, r.high); dayLow = Math.min(dayLow, r.low);\n    // VWAP from the open, EMA9/EMA20 on closes, 20-bar average range.\n    pvSum += (r.high + r.low + r.close) / 3 * r.volume;\n    var vwap = volSum > 0 ? pvSum / volSum : r.close;\n    ema9 = ema9 == null ? r.close : r.close * k9 + ema9 * (1 - k9);\n    ema20 = ema20 == null ? r.close : r.close * k20 + ema20 * (1 - k20);\n    var w20 = rows.slice(Math.max(0, i - 19), i + 1), atr20 = w20.reduce(function (s, x) { return s + (x.high - x.low); }, 0) / w20.length;\n    // (atr20 is needed by the EMA test below, so it is computed first)\n    var aboveVwap = r.close > vwap, vwapReclaim = aboveVwap && belowVwapRun >= 3, vwapLoss = !aboveVwap && belowVwapRun === 0 && i > 0 && bars[i - 1].aboveVwap;\n    belowVwapRun = aboveVwap ? 0 : belowVwapRun + 1;\n    // EMA is only bullish/bearish when the two are meaningfully apart (0.15x the\n    // average range) AND the fast one is moving that way. Otherwise: neutral.\n    var prevEma9 = i > 0 ? bars[i - 1].ema9 : ema9;\n    var sep = Math.abs(ema9 - ema20), minSep = 0.15 * (atr20 || range || 1);\n    // The closing auction prints a huge single-minute volume. It is marked so\n    // scoring and attention can ignore it; it is still shown as information.\n    var auction = r.time >= '15:55';\n    var align = 'neutral';\n    if (sep >= minSep) {\n      if (r.close > ema9 && ema9 > ema20 && ema9 > prevEma9) align = 'bull';\n      else if (r.close < ema9 && ema9 < ema20 && ema9 < prevEma9) align = 'bear';\n    }\n    var pct = function (x) { return range > 0 ? Math.round(x / range * 1000) / 10 : 0; };\n    bars.push({\n      i: i, time: r.time, open: r.open, high: r.high, low: r.low, close: r.close, volume: r.volume,\n      range: range, body: pct(body), uw: pct(r.high - Math.max(r.open, r.close)), lw: pct(Math.min(r.open, r.close) - r.low),\n      dir: r.close > r.open ? 'up' : r.close < r.open ? 'down' : 'flat',\n      volx: volSum > 0 ? Math.round(r.volume / (volSum / (i + 1)) * 100) / 100 : 0,\n      avgRange: rangeSum / (i + 1),\n      closePos: range > 0 ? (r.close - r.low) / range : 0.5,      // 1 = closed at high\n      vwap: vwap, ema9: ema9, ema20: ema20, atr20: atr20, aboveVwap: aboveVwap, vwapReclaim: vwapReclaim, vwapLoss: vwapLoss, align: align, auction: auction,\n      newDayHigh: r.high >= dayHigh && (i === 0 || r.high > Math.max.apply(null, rows.slice(0, i).map(function (x) { return x.high; }))),\n      dayHigh: dayHigh, dayLow: dayLow\n    });\n  }\n\n  // ---- swings, confirmed K bars later\n  var swings = [];\n  for (var s = K; s < n - K; s++) {\n    var hiL = true, loL = true, hiR = true, loR = true;\n    for (var k = 1; k <= K; k++) {\n      if (!(rows[s].high > rows[s - k].high)) hiL = false;\n      if (!(rows[s].high >= rows[s + k].high)) hiR = false;\n      if (!(rows[s].low < rows[s - k].low)) loL = false;\n      if (!(rows[s].low <= rows[s + k].low)) loR = false;\n    }\n    if (hiL && hiR) swings.push({ i: s, time: rows[s].time, kind: 'H', price: rows[s].high, confirmedAt: s + K });\n    if (loL && loR) swings.push({ i: s, time: rows[s].time, kind: 'L', price: rows[s].low, confirmedAt: s + K });\n  }\n  swings.sort(function (a, b) { return a.i - b.i; });\n  // An outside / expansion bar that is both a swing high and a swing low gives\n  // no order information (which came first?), so it is a level but never structure.\n  var byBar = {};\n  swings.forEach(function (w) { byBar[w.i] = (byBar[w.i] || 0) + 1; });\n  swings.forEach(function (w) { if (byBar[w.i] > 1) w.outside = true; });\n  // A swing only earns HH/LH/HL/LL if it moved at least MIN_MOVE x the average\n  // bar range past the previous swing of its kind; smaller wiggles are EQ and\n  // count as levels but never as structure.\n  var MIN_MOVE = (opts && opts.minMove) || 1.5, prevH = null, prevL = null;\n  swings.forEach(function (w) {\n    if (w.outside) { w.label = 'OUT'; return; }\n    var tol = MIN_MOVE * bars[w.i].avgRange;\n    if (w.kind === 'H') { w.label = !prevH ? 'H' : w.price > prevH.price + tol ? 'HH' : w.price < prevH.price - tol ? 'LH' : 'EQ'; if (w.label !== 'EQ') prevH = w; }\n    else { w.label = !prevL ? 'L' : w.price > prevL.price + tol ? 'HL' : w.price < prevL.price - tol ? 'LL' : 'EQ'; if (w.label !== 'EQ') prevL = w; }\n  });\n\n  // ---- sequential walk: levels, trend, events\n  //\n  // Levels for breakout/support events are WINDOW extremes, not the nearest\n  // micro-swing: the highest high / lowest low of the last 60 bars, excluding\n  // the most recent 5 (a high made a minute ago is not resistance yet).\n  // Swings still drive trend, HL/LL structure, and the \"structure break\" event.\n  var WIN = 60, AGE = 5, COOLDOWN = 30, MARGIN = (opts && opts.margin) || 0.15, RUNS = (opts && opts.runs) || 3, TEST_RANGE = 0.75;\n  var events = [], prevState = null, lastBreakout = null, states = [], fired = {}, trendRun = 0, runTrend = null, announced = null, broken = null;\n  var cooled = function (type, b) { return fired[type] == null || b - fired[type] > COOLDOWN; };\n  var mark = function (type, b) { fired[type] = b; };\n\n  for (var b = 0; b < n; b++) {\n    var bar = bars[b], next = bars[b + 1] || null, margin = MARGIN * bar.avgRange;\n    var conf = swings.filter(function (w) { return w.confirmedAt <= b; });\n    var structural = function (w) { return w.label !== 'EQ' && w.label !== 'OUT'; };\n    var SH = conf.filter(function (w) { return w.kind === 'H' && structural(w); }), SL = conf.filter(function (w) { return w.kind === 'L' && structural(w); });\n    var lastSH = SH[SH.length - 1] || null, lastSL = SL[SL.length - 1] || null;\n\n    var wEnd = b - AGE, wStart = Math.max(0, b - WIN), res = null, sup = null, reclaimed = null;\n    if (wEnd >= wStart && wEnd >= 0) {\n      var w = rows.slice(wStart, wEnd + 1), hiIdx = 0, loIdx = 0;\n      for (var q = 1; q < w.length; q++) { if (w[q].high > w[hiIdx].high) hiIdx = q; if (w[q].low < w[loIdx].low) loIdx = q; }\n      if (w[hiIdx].high > bar.close) res = { price: w[hiIdx].high, time: w[hiIdx].time, kind: 'window' };\n      if (w[loIdx].low < bar.close) sup = { price: w[loIdx].low, time: w[loIdx].time, kind: 'window' };\n    }\n    // After a confirmed support break the broken level is a reclaim level, not\n    // support; support is the lowest low made since the break.\n    if (broken) {\n      if (bar.low < broken.low) { broken.low = bar.low; broken.lowTime = bar.time; }\n      if (bar.close > broken.level + MARGIN * bar.atr20) broken.above++; else broken.above = 0;\n      if (broken.above >= 2) { reclaimed = broken.level; broken = null; } else if (b - broken.i > 60) broken = null;\n    }\n    if (broken && broken.low < bar.close) sup = { price: broken.low, time: broken.lowTime, kind: 'postbreak' };\n    if (broken && sup && sup.kind !== 'postbreak' && sup.price >= broken.level - 1e-9) sup = broken.low < bar.close ? { price: broken.low, time: broken.lowTime, kind: 'postbreak' } : null;\n\n    var trend = 'RANGE', reason = 'אין רצף';\n    if (lastSH && lastSL && lastSH.label !== 'H' && lastSL.label !== 'L') {\n      if (lastSH.label === 'HH' && lastSL.label === 'HL') { trend = 'UP'; reason = 'HH + HL'; }\n      else if (lastSH.label === 'LH' && lastSL.label === 'LL') { trend = 'DOWN'; reason = 'LH + LL'; }\n      else reason = lastSH.label + ' + ' + lastSL.label;\n    } else if (!lastSH || !lastSL) reason = 'עדיין אין מספיק swings';\n\n    // ---- events, judged against the PREVIOUS bar's levels (no look-ahead on levels;\n    // the only forward look is the 2-bar confirmation, which is reported as such).\n    var pres = prevState ? prevState.res : null, psup = prevState ? prevState.sup : null;\n    var pTrend = prevState ? prevState.trend : null, pSL = prevState ? prevState.lastSL : null, pSH = prevState ? prevState.lastSH : null;\n    var ev = [], score = 0;\n\n    if (pres && bar.high > pres.price && bar.close > pres.price + margin && cooled('breakout', b)) {\n      var confirmed = next ? next.close > pres.price : null;\n      var strong = bar.body > 65 && bar.volx > 1.2;\n      score += confirmed ? 5 : 2;\n      ev.push({ type: strong && confirmed ? 'strong_breakout' : 'breakout', level: pres.price, confirmed: confirmed, strong: strong });\n      lastBreakout = { i: b, level: pres.price, confirmed: confirmed, dayHigh: pres.price >= prevState.bar.dayHigh - 1e-9 };\n      if (confirmed) mark('breakout', b);\n    } else if (pres && bar.high > pres.price && bar.close < pres.price && bar.uw >= 50 && cooled('rejected', b)) {\n      ev.push({ type: 'rejected', level: pres.price, wick: bar.uw }); mark('rejected', b);\n    } else if (pres && !ev.length && bar.close <= pres.price && pres.price - bar.close <= TEST_RANGE * bar.atr20) {\n      ev.push({ type: 'testing', level: pres.price });\n    }\n    if (lastBreakout && b > lastBreakout.i && b - lastBreakout.i <= 10) {\n      if (bar.close < lastBreakout.level) { score -= 3; ev.push({ type: 'failed', level: lastBreakout.level, confirmed: lastBreakout.confirmed, dayHigh: lastBreakout.dayHigh }); lastBreakout = null; }\n      else if (bar.low <= lastBreakout.level + margin && bar.close >= lastBreakout.level && cooled('retest_hold', b)) { ev.push({ type: 'retest_hold', level: lastBreakout.level }); mark('retest_hold', b); }\n    }\n    if (psup && bar.low < psup.price && bar.close < psup.price - margin && cooled('support_break', b)) {\n      var confS = next ? next.close < psup.price : null;\n      score -= 2; ev.push({ type: 'support_break', level: psup.price, confirmed: confS });\n      if (confS) { mark('support_break', b); broken = { level: psup.price, i: b, low: bar.low, lowTime: bar.time, above: 0 }; }\n    }\n    // Structure break: the level the trend rests on gives way.\n    // Only an announced trend (two agreeing swings) can be \"broken\".\n    var pAnnounced = prevState ? prevState.announced : null;\n    if (pAnnounced === 'UP' && pSL && bar.close < pSL.price - margin && cooled('structure_break', b)) {\n      score -= 2; ev.push({ type: 'structure_break', level: pSL.price, side: 'bear', confirmed: next ? next.close < pSL.price : null }); mark('structure_break', b);\n    } else if (pAnnounced === 'DOWN' && pSH && bar.close > pSH.price + margin && cooled('structure_break', b)) {\n      score += 2; ev.push({ type: 'structure_break', level: pSH.price, side: 'bull', confirmed: next ? next.close > pSH.price : null }); mark('structure_break', b);\n    }\n    conf.forEach(function (w) {\n      if (w.confirmedAt !== b || w.label === 'EQ') return;\n      if (w.label === 'OUT') { if (w.kind === 'H') ev.push({ type: 'outside_bar', level: w.price, at: w.time }); return; }\n      ev.push({ type: (w.label.toLowerCase()) + '_confirmed', level: w.price, at: w.time });\n    });\n    if (bar.volx >= 1.5 && !bar.auction) { score += 2; ev.push({ type: 'volume_spike', volx: bar.volx }); }\n    else if (bar.volx >= 1.5 && bar.auction) ev.push({ type: 'auction_volume', volx: bar.volx });\n    var momentum = bar.body >= 75 && bar.range > 1.5 * bar.avgRange;\n    if (momentum) ev.push({ type: 'momentum', body: bar.body, range: bar.range });\n    if (reclaimed != null) ev.push({ type: 'level_reclaim', level: reclaimed });\n    if (bar.vwapReclaim) ev.push({ type: 'vwap_reclaim', level: bar.vwap });\n    if (bar.vwapLoss) ev.push({ type: 'vwap_loss', level: bar.vwap });\n    if (bar.body >= 70) score += 1;\n    if (bar.closePos >= 0.8) score += 1;\n    if (bar.newDayHigh) score += 1;\n    if (bar.uw > 55) score -= 2;\n    // Trend change is announced on the SECOND structural swing that agrees\n    // (HH+HL is the trend; the next HH or HL is the confirmation).\n    var structConfirmedNow = conf.some(function (w) { return w.confirmedAt === b && structural(w); });\n    if (structConfirmedNow) {\n      if (trend === runTrend) trendRun++; else { runTrend = trend; trendRun = 1; }\n      if (trendRun === RUNS && (trend === 'UP' || trend === 'DOWN') && cooled('trend_change', b)) {\n        ev.push({ type: 'trend_change', from: pTrend === trend ? 'RANGE' : pTrend, to: trend }); mark('trend_change', b); announced = trend;\n      }\n    }\n    if (announced && trend !== announced) announced = null;\n\n    var attempt = (pres && Math.abs(pres.price - bar.close) <= TEST_RANGE * bar.atr20) || (lastBreakout && b - lastBreakout.i <= 1);\n    var nearLow = sup ? bar.close - sup.price : bar.close - bar.dayLow;\n    var bounce = (trend === 'DOWN' || broken) && bar.dir === 'up' && nearLow <= 1.5 * bar.atr20;\n    var state = attempt ? 'ATTEMPT' : bounce ? 'BOUNCE' : trend;\n    var tier = score >= 7 ? 'CHECK' : score >= 5 ? 'IMPORTANT' : score >= 3 ? 'WATCH' : 'NONE';\n    // Ask = worth a fresh look: only day-extreme levels, only confirmed.\n    var pDayHigh = prevState ? prevState.bar.dayHigh : Infinity, pDayLow = prevState ? prevState.bar.dayLow : -Infinity;\n    var ask = ev.some(function (e) {\n      if (e.type === 'strong_breakout' || e.type === 'breakout') return e.confirmed === true && e.level >= pDayHigh - 1e-9;\n      if (e.type === 'rejected') return e.level >= pDayHigh - 1e-9;\n      if (e.type === 'failed') return e.confirmed === true && e.dayHigh === true;\n      if (e.type === 'support_break') return e.confirmed === true && e.level <= pDayLow + 1e-9;\n      if (e.type === 'structure_break') return e.confirmed === true;\n      if (e.type === 'trend_change') return true;\n      return false;\n    }) || (bar.volx >= 1.5 && !bar.auction && momentum);\n    var loggable = ask || tier !== 'NONE' || ev.some(function (e) {\n      return e.type === 'failed' || e.type === 'rejected' || e.type === 'support_break' || e.type === 'structure_break' || e.type === 'trend_change' || e.type === 'retest_hold' || /breakout/.test(e.type);\n    });\n\n    var st = { i: b, time: bar.time, close: bar.close, trend: trend, reason: reason, state: state, res: res, sup: sup,\n      lastSH: lastSH, lastSL: lastSL, announced: announced, reclaim: broken ? broken.level : null, score: score, tier: tier, ask: ask, events: ev, bar: bar };\n    states.push(st);\n    if (loggable) events.push(st);\n    prevState = st;\n  }\n\n  // The UNFILTERED rows too. Coverage is a question about the session that was\n  // loaded, not about the subset the analysis chose to use: a no-trade minute\n  // is dropped here on purpose, and counting the remainder made a complete\n  // 335-bar session report as 186 with holes that were never there.\n  return { bars: bars, rawBars: allRows, swings: swings, states: states, state: states[n - 1],\n    events: events, skippedBars: skipped };\n}\n\n// ---------------------------------------------------------------- radar layer\n//\n// Short-term momentum, kept separate from structure on purpose: a confirmed\n// downtrend can still be bouncing.\n//   PUSHING   pressing higher with the structure\n//   PULLBACK  easing back inside an up structure\n//   RECOVERY  bouncing off the lows against a down structure\n//   SELLING   pressing lower\n//   FLAT      no net movement worth naming\nfunction momentum(A, look) {\n  if (!A || !A.state) return { label: 'FLAT', net: 0 };\n  var L = look || 5, bars = A.bars, n = bars.length, w = bars.slice(Math.max(0, n - L));\n  var b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  var net = (b.close - w[0].open) / atr;                       // in range units\n  var ups = w.filter(function (x) { return x.dir === 'up'; }).length;\n  var struct = A.state.announced || A.state.trend;\n  var lowRun = Math.min.apply(null, w.map(function (x) { return x.low; }));\n  var highRun = Math.max.apply(null, w.map(function (x) { return x.high; }));\n  var offLow = (b.close - lowRun) / atr, offHigh = (highRun - b.close) / atr;\n  var label = 'FLAT';\n  if (net >= 0.8 && ups >= L / 2) label = struct === 'DOWN' ? 'RECOVERY' : 'PUSHING';\n  else if (net <= -0.8 && ups <= L / 2) label = struct === 'UP' ? 'PULLBACK' : 'SELLING';\n  else if (offLow >= 1 && offHigh <= 0.5) label = struct === 'DOWN' ? 'RECOVERY' : 'PUSHING';\n  else if (offHigh >= 1 && offLow <= 0.5) label = struct === 'UP' ? 'PULLBACK' : 'SELLING';\n  return { label: label, net: net, offLow: offLow, offHigh: offHigh };\n}\n\n// Tactical levels: what is actually within reach, preferring levels that price\n// has interacted with more than once. Everything is measured in R = one average\n// 1-minute range, so \"0.3R away\" means the same thing on any symbol.\nfunction tactical(A, maxR) {\n  if (!A || !A.state) return { support: null, resistance: null, above: [], below: [], atr: 1 };\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length, atr = b.atr20 || b.avgRange || 1, MAX = (maxR || 3) * atr;\n  var cand = [];\n  var add = function (price, why, weight, kind) {\n    if (price == null || !isFinite(price)) return;\n    cand.push({ price: price, why: why, weight: weight, kind: kind });\n  };\n\n  // A prior HIGH only becomes support once price broke above it and came back\n  // to it without losing it — resistance-turned-support needs evidence, not\n  // just \"price happens to be above it now\". Same, inverted, for prior lows.\n  var acceptedAsSupport = function (level, fromIdx) {\n    var closedAbove = 0, retest = false;\n    for (var i = fromIdx; i < n; i++) {\n      if (bars[i].close > level) closedAbove++;\n      if (closedAbove >= 2 && bars[i].low <= level + 0.25 * atr && bars[i].close >= level) retest = true;\n    }\n    return closedAbove >= 3 && retest;\n  };\n  var acceptedAsResistance = function (level, fromIdx) {\n    var closedBelow = 0, retest = false;\n    for (var i = fromIdx; i < n; i++) {\n      if (bars[i].close < level) closedBelow++;\n      if (closedBelow >= 2 && bars[i].high >= level - 0.25 * atr && bars[i].close <= level) retest = true;\n    }\n    return closedBelow >= 3 && retest;\n  };\n\n  A.swings.forEach(function (w) {\n    if (w.confirmedAt > n - 1) return;\n    var age = n - 1 - w.i; if (age > 120) return;\n    var base = (w.label === 'OUT' || w.label === 'EQ') ? 2 : 3;\n    var recency = age <= 30 ? 2 : age <= 60 ? 1 : 0;\n    if (w.kind === 'H') {\n      if (w.price > b.close) add(w.price, 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time, base + recency, 'res');\n      else if (acceptedAsSupport(w.price, w.i)) add(w.price, 'התנגדות שהפכה לתמיכה ' + w.time, base + recency + 1, 'sup');\n    } else {\n      if (w.price < b.close) add(w.price, 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time, base + recency, 'sup');\n      else if (acceptedAsResistance(w.price, w.i)) add(w.price, 'תמיכה שהפכה להתנגדות ' + w.time, base + recency + 1, 'res');\n    }\n  });\n  if (S.res && S.res.price > b.close) add(S.res.price, 'גבוה 60 נרות', 3, 'res');\n  if (S.sup && S.sup.price < b.close) add(S.sup.price, S.sup.kind === 'postbreak' ? 'הנמוך מאז השבירה' : 'נמוך 60 נרות', S.sup.kind === 'postbreak' ? 4 : 3, S.sup.kind === 'postbreak' ? 'sup' : 'sup');\n  if (b.dayHigh > b.close) add(b.dayHigh, 'גבוה היום', 3, 'res');\n  if (b.dayLow < b.close) add(b.dayLow, 'נמוך היום', 3, 'sup');\n  if (S.reclaim != null) add(S.reclaim, 'רמה שנשברה', 4, S.reclaim > b.close ? 'res' : 'sup');\n  add(b.vwap, 'VWAP', 2, b.vwap > b.close ? 'res' : 'sup');\n  cand[cand.length - 1].soft = true;   // VWAP is context, never a setup level on its own\n\n  // touches and rejections add weight; distance never does.\n  cand.forEach(function (c) {\n    var touch = 0, reject = 0;\n    bars.forEach(function (x) {\n      if (x.low <= c.price + 0.25 * atr && x.high >= c.price - 0.25 * atr) {\n        touch++;\n        if (c.kind === 'res' && x.high > c.price && x.close < c.price) reject++;\n        if (c.kind === 'sup' && x.low < c.price && x.close > c.price) reject++;\n      }\n    });\n    c.touches = touch; c.rejects = reject;\n    c.weight += Math.min(3, Math.floor(touch / 4)) + Math.min(2, reject);\n    c.r = (c.price - b.close) / atr;\n  });\n\n  // Price sitting exactly on a level is the important case, so the side test\n  // allows a small epsilon rather than requiring strictly above/below.\n  var eps = 0.05 * atr;\n  var side = function (want) {\n    return cand.filter(function (c) { return c.kind === want && (want === 'res' ? c.price > b.close - eps : c.price < b.close + eps); })\n      .filter(function (c) { return Math.abs(c.price - b.close) <= MAX; });\n  };\n  // Strongest level wins; distance is only the tie-breaker, and a much closer\n  // level of near-equal strength is preferred.\n  var pick = function (want) {\n    var list = side(want).sort(function (x, y) { return (y.weight - x.weight) || (Math.abs(x.r) - Math.abs(y.r)); });\n    if (!list.length) return null;\n    var best = list[0];\n    list.slice(1, 4).forEach(function (c) { if (c.weight >= best.weight - 1 && Math.abs(c.r) < Math.abs(best.r) - 0.3) best = c; });\n    return best;\n  };\n  var all = function (want) {\n    return side(want).sort(function (x, y) { return want === 'res' ? x.price - y.price : y.price - x.price; });\n  };\n  return { support: pick('sup'), resistance: pick('res'), above: all('res'), below: all('sup'), atr: atr, candidates: cand };\n}\n\n// Distance in R, kept readable at small values. Below 0.05R it is \"on the level\".\nfunction fmtR(r) {\n  var a = Math.abs(r);\n  if (a < 0.05) return 'על הרמה';\n  return (r >= 0 ? '+' : '-') + (a < 1 ? a.toFixed(2) : a.toFixed(1)) + 'R';\n}\n\n// A support is only \"holding\" if price is not driving through it. Deterministic:\n// a big bearish body closing near its low, meaningfully below the level, or two\n// consecutive closes below it.\nfunction aggressiveBreak(A, level) {\n  var bars = A.bars, n = bars.length, b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  if (b.close < level - 0.5 * atr) return true;\n  if (n >= 2 && bars[n - 2].close < level && b.close < level) return true;\n  if (b.dir === 'down' && b.body >= 60 && b.closePos <= 0.25 && b.close < level - 0.15 * atr) return true;\n  return false;\n}\n\n// ---- execution plan: a small deterministic state machine.\n// States: WAITING_FOR_ZONE, IN_ZONE, READY_PARTIAL, WAITING_FOR_CONFIRMATION,\n//         READY_ADD, ACTIVE, TAKE_PROFIT_AREA, DO_NOT_CHASE, FAILED, NO_SETUP\nvar PLAN_TEXT = {\n  WAITING_FOR_ZONE: ['המתן', 'מחכים לכניסה לאזור'],\n  IN_ZONE: ['המתן', 'בתוך האזור — צריך בלימה'],\n  READY_PARTIAL: ['כניסה חלקית', 'האזור מוחזק'],\n  WAITING_FOR_CONFIRMATION: ['החזק', 'מחכים לאישור להוספה'],\n  READY_ADD: ['הוסף', 'האישור התקבל'],\n  ACTIVE: ['החזק', 'מהלך פעיל'],\n  TAKE_PROFIT_AREA: ['מימוש', 'אזור מימוש ראשון'],\n  DO_NOT_CHASE: ['לא לרדוף', 'המחיר התרחק מהכניסה'],\n  FAILED: ['להימנע', 'ה-setup בוטל'],\n  NO_SETUP: ['המתן', 'אין setup']\n};\nfunction executionPlan(A, market) {\n  if (!A || !A.state) return null;\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length, atr = b.atr20 || b.avgRange || 1;\n  var T = tactical(A), BL = bottomLine(A, market), mom = momentum(A);\n  var P = { state: 'NO_SETUP', kind: null, zone: null, entry: null, addAbove: null,\n    invalidation: null, target: null, atr: atr, price: b.close };\n\n  var sup = T.support, res = T.resistance;\n  var bearish = S.announced === 'DOWN' && mom.label !== 'RECOVERY';\n  // A setup needs a real structural level and a tape with some range in it.\n  // A dead-flat chop whose whole day spans one average bar is not a setup,\n  // however close price sits to \"a level\".\n  var dayRangeR = (b.dayHigh - b.dayLow) / atr;\n  var hasTape = dayRangeR >= 2;\n  // Whether the level is actually holding is decided by the state machine\n  // below (holding / aggressiveBreak), not by momentum: the first green bar\n  // off support is exactly the partial-entry moment, and a 5-bar momentum\n  // reading still says SELLING there.\n  var pullbackOk = !bearish && !!sup && !sup.soft && hasTape && sup.weight >= 5;\n  var breakoutOk = !bearish && !!res && !res.soft && hasTape &&\n    (S.trend !== 'DOWN' || mom.label === 'RECOVERY') && (BL.confidence >= 3 || mom.label === 'PUSHING' || S.trend === 'UP');\n\n  // Which setup is in play: the nearer opportunity wins.\n  var dSup = sup ? Math.abs(b.close - sup.price) / atr : Infinity;\n  var dRes = res ? Math.abs(res.price - b.close) / atr : Infinity;\n  if (pullbackOk && dSup <= 3 && (dSup <= dRes || !breakoutOk)) {\n    P.kind = 'pullback';\n    P.zone = [sup.price - 0.15 * atr, sup.price + 0.30 * atr];\n    P.entry = sup.price + 0.05 * atr;\n    var nextUp = T.above.filter(function (c) { return c.price > P.zone[1] + 0.1 * atr; })[0];\n    P.addAbove = nextUp ? nextUp.price : sup.price + 0.7 * atr;\n    var lowerLevels = T.below.filter(function (c) { return c.price < sup.price - 0.05 * atr; });\n    P.invalidation = lowerLevels.length ? Math.max(lowerLevels[0].price, sup.price - 0.9 * atr) : sup.price - 0.45 * atr;\n    P.target = res ? res.price : b.dayHigh;\n    P.levelWhy = sup.why;\n  } else if (breakoutOk && dRes <= 3) {\n    P.kind = 'breakout';\n    P.zone = [res.price, res.price + 0.20 * atr];\n    P.entry = res.price + 0.10 * atr;\n    P.addAbove = res.price + 0.5 * atr;\n    P.invalidation = Math.max(res.price - 0.6 * atr, sup ? sup.price : res.price - 0.9 * atr);\n    var beyond = T.above.filter(function (c) { return c.price > res.price + 0.2 * atr; })[0];\n    P.target = beyond ? beyond.price : res.price + 1.5 * atr;\n    P.levelWhy = res.why;\n  }\n\n  // A setup that just broke must still read FAILED, even though the level it\n  // rested on is now above price and no longer offered as support.\n  // Losing the last structural low by a clear margin is a broken setup even if\n  // the 60-bar window never registered a support_break (the window low can sit\n  // far below the level the setup actually rested on).\n  var lostLevel = !!(S.lastSL && b.close < S.lastSL.price - 0.3 * atr && (n - 1 - S.lastSL.i) <= 25);\n  var justBroke = lostLevel || A.states.slice(-6).some(function (s) {\n    return s.events.some(function (e) {\n      return (e.type === 'support_break' && e.confirmed === true)\n        || (e.type === 'structure_break' && e.side === 'bear' && e.confirmed === true)\n        || (e.type === 'failed');\n    });\n  });\n  if (!P.kind) {\n    if (justBroke) { P.state = 'FAILED'; P.invalidation = null; return decorate(P); }\n    P.state = 'NO_SETUP'; P.reason = bearish ? 'מבנה דובי — אין setup לונג' : 'אין רמה קרובה';\n    return decorate(P);\n  }\n\n  var brokeInvalid = b.close < P.invalidation || lostLevel;\n  var agg = aggressiveBreak(A, P.kind === 'pullback' ? sup.price : P.invalidation);\n  var inZone = b.close >= P.zone[0] && b.close <= P.zone[1];\n  // \"Holding\" = the last two bars did not lose the level. This, not momentum,\n  // is what turns being in the zone into a partial entry.\n  var lvl = P.kind === 'pullback' ? sup.price : P.zone[0];\n  var holding = P.kind !== 'pullback' || (bars.slice(-2).every(function (x) { return x.low >= lvl - 0.35 * atr; }) && b.close >= lvl - 0.1 * atr);\n  var aboveAdd = b.close > P.addAbove;\n  var confirmedAdd = aboveAdd && n >= 2 && bars[n - 2].close > P.addAbove;\n  var nearTarget = P.target && (P.target - b.close) / atr <= 0.3;\n  var extended = b.close > P.entry + 0.5 * atr;\n\n  if (brokeInvalid || (agg && !inZone && b.close < P.zone[0])) P.state = 'FAILED';\n  else if (agg && inZone) P.state = 'FAILED';\n  else if (nearTarget && (aboveAdd || P.kind === 'breakout')) P.state = 'TAKE_PROFIT_AREA';\n  else if (confirmedAdd) P.state = extended && !nearTarget ? 'ACTIVE' : 'READY_ADD';\n  else if (aboveAdd) P.state = 'WAITING_FOR_CONFIRMATION';\n  else if (inZone && holding) P.state = 'READY_PARTIAL';\n  else if (inZone) P.state = 'IN_ZONE';\n  else if (extended) P.state = 'DO_NOT_CHASE';\n  else P.state = 'WAITING_FOR_ZONE';\n\n  return decorate(P);\n\n  function decorate(P) {\n    var t = PLAN_TEXT[P.state] || PLAN_TEXT.NO_SETUP;\n    P.action = t[0]; P.headline = t[1];\n    var z = P.zone ? P.zone.map(function (x) { return x.toFixed(2); }).join('–') : null;\n    P.waitPrice = null; P.nextStep = null; P.ifConfirmed = null; P.ifFailed = null; P.short = null;\n    if (P.state === 'NO_SETUP') { P.short = P.reason; return P; }\n    switch (P.state) {\n      case 'WAITING_FOR_ZONE':\n        P.waitPrice = z;\n        P.nextStep = P.kind === 'pullback' ? 'ירידה לאזור ' + z + ' בלי שבירה אגרסיבית' : 'סגירה מעל ' + P.zone[0].toFixed(2);\n        P.ifConfirmed = 'כניסה חלקית ב-' + P.entry.toFixed(2);\n        P.ifFailed = 'שבירה מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'מחכים ל-' + (P.kind === 'pullback' ? z : P.zone[0].toFixed(2));\n        break;\n      case 'IN_ZONE':\n        P.waitPrice = z;\n        P.nextStep = 'בתוך האזור — צריך בלימה לפני כניסה';\n        P.ifConfirmed = 'בלימה מעל ' + P.zone[0].toFixed(2) + ' — כניסה חלקית';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'באזור ' + z + ' — צריך בלימה';\n        break;\n      case 'READY_PARTIAL':\n        P.waitPrice = z;\n        P.nextStep = 'האזור מוחזק — כניסה חלקית ב-' + P.entry.toFixed(2);\n        P.ifConfirmed = 'חזרה מעל ' + P.addAbove.toFixed(2) + ' — הוספה';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'כניסה חלקית ' + z;\n        break;\n      case 'WAITING_FOR_CONFIRMATION':\n        P.waitPrice = P.addAbove.toFixed(2);\n        P.nextStep = 'נר סגירה נוסף מעל ' + P.addAbove.toFixed(2);\n        P.ifConfirmed = 'הוספה';\n        P.ifFailed = 'חזרה מתחת ' + P.zone[0].toFixed(2) + ' — להחזיק בלבד';\n        P.short = 'אישור מעל ' + P.addAbove.toFixed(2);\n        break;\n      case 'READY_ADD':\n        P.waitPrice = P.addAbove.toFixed(2);\n        P.nextStep = 'הוספה מעל ' + P.addAbove.toFixed(2);\n        P.ifConfirmed = 'יעד ' + (P.target ? P.target.toFixed(2) : '—');\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'הוסף מעל ' + P.addAbove.toFixed(2);\n        break;\n      case 'ACTIVE':\n        P.waitPrice = P.target ? P.target.toFixed(2) : null;\n        P.nextStep = 'מהלך פעיל לעבר ' + (P.target ? P.target.toFixed(2) : 'היעד');\n        P.ifConfirmed = 'מימוש חלקי באזור היעד';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'יעד הבא ' + (P.target ? P.target.toFixed(2) : '—');\n        break;\n      case 'TAKE_PROFIT_AREA':\n        P.waitPrice = P.target.toFixed(2);\n        P.nextStep = 'אזור מימוש ' + P.target.toFixed(2);\n        P.ifConfirmed = 'מימוש חלקי';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'מימוש ' + P.target.toFixed(2);\n        break;\n      case 'DO_NOT_CHASE':\n        P.pullbackZone = [P.entry, P.entry + 0.35 * atr];\n        P.waitPrice = P.pullbackZone.map(function (x) { return x.toFixed(2); }).join('–');\n        P.nextStep = 'פולבק ל-' + P.waitPrice;\n        P.ifConfirmed = 'כניסה חלקית בפולבק';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'לא לרדוף · פולבק ' + P.waitPrice;\n        break;\n      case 'FAILED':\n        P.waitPrice = null;\n        if (P.invalidation == null) { P.nextStep = 'הרמה נשברה — אין כניסה'; P.ifConfirmed = 'setup חדש נדרש'; P.short = 'setup נכשל'; return P; }\n        P.nextStep = 'אין כניסה — הרמה נשברה';\n        P.ifConfirmed = 'setup חדש נדרש';\n        P.ifFailed = null;\n        P.short = 'setup נכשל';\n        break;\n    }\n    return P;\n  }\n}\n\n// One radar row. Status follows the execution state — the single source of\n// truth — so the row can never say CLOSE while the plan says there is nothing\n// to do.\nfunction radarRow(symbol, A, market, freshness) {\n  if (!A || !A.state) return { symbol: symbol, status: 'NO DATA', score: 0, why: 'אין נתונים', nearestR: Infinity, freshness: freshness || 'NO DATA' };\n  var S = A.state, b = S.bar, T = tactical(A), mom = momentum(A), BL = bottomLine(A, market), P = executionPlan(A, market);\n  var near = [T.support, T.resistance].filter(Boolean).map(function (l) { return Math.abs(l.r); });\n  var nearest = near.length ? Math.min.apply(null, near) : Infinity;\n  var ended = freshness === 'SESSION ENDED';\n\n  var status, why = P && P.short ? P.short : 'אין setup';\n  var st = P ? P.state : 'NO_SETUP';\n  if (freshness === 'NO DATA') { status = 'NO DATA'; why = 'אין נתונים'; }\n  else if (st === 'FAILED' || (S.announced === 'DOWN' && st === 'NO_SETUP')) { status = 'AVOID'; why = st === 'FAILED' ? 'setup נכשל' : 'מגמת ירידה מוכרזת'; }\n  else if (st === 'ACTIVE' || st === 'TAKE_PROFIT_AREA') status = 'ACTIVE';\n  else if (st === 'READY_PARTIAL' || st === 'READY_ADD') status = 'READY';\n  // CLOSE needs proximity AND a reason to look: a live plan step, not distance.\n  else if (st === 'WAITING_FOR_CONFIRMATION' || st === 'DO_NOT_CHASE') status = 'CLOSE';\n  else if (st === 'WAITING_FOR_ZONE' && nearest <= 1.0 && (BL.confidence >= 3 || mom.label === 'RECOVERY' || mom.label === 'PUSHING' || (b.volx >= 1.3 && !b.auction))) status = 'CLOSE';\n  else if (st === 'WAITING_FOR_ZONE') status = nearest <= 2 || BL.setup ? 'WATCH' : 'QUIET';\n  else status = 'QUIET';\n\n  // Rank by execution urgency first (spec 31), then status, then distance.\n  var URGENCY = { READY_PARTIAL: 0, READY_ADD: 1, TAKE_PROFIT_AREA: 2, ACTIVE: 3, WAITING_FOR_CONFIRMATION: 4, DO_NOT_CHASE: 5, WAITING_FOR_ZONE: 6, IN_ZONE: 6, NO_SETUP: 8, FAILED: 9 };\n  return {\n    symbol: symbol, status: status, why: why, score: BL.confidence, price: b.close,\n    structure: S.announced || S.trend, momentum: mom.label, nearestR: nearest,\n    support: T.support, resistance: T.resistance, volx: b.volx, auction: b.auction,\n    vwapAbove: b.aboveVwap, align: b.align, atr: T.atr, freshness: freshness || 'LIVE',\n    bl: BL, tactical: T, mom: mom, plan: P, action: P ? P.action : 'המתן',\n    pressure: (typeof pressure === 'function') ? pressure(A, { tactical: T, plan: P }) : null,\n    urgency: URGENCY[st] == null ? 7 : URGENCY[st], sessionEnded: ended\n  };\n}\n\nvar STATUS_ORDER = ['READY', 'CLOSE', 'ACTIVE', 'WATCH', 'QUIET', 'AVOID', 'NO DATA'];\nfunction sortRadar(list, mode) {\n  var arr = list.slice();\n  if (mode === 'symbol') return arr.sort(function (a, b) { return a.symbol < b.symbol ? -1 : 1; });\n  if (mode === 'score') return arr.sort(function (a, b) { return b.score - a.score || a.nearestR - b.nearestR; });\n  if (mode === 'volume') return arr.sort(function (a, b) { return (b.volx || 0) - (a.volx || 0); });\n  if (mode === 'distance') return arr.sort(function (a, b) { return a.nearestR - b.nearestR; });\n  return arr.sort(function (a, b) {\n    return ((a.urgency == null ? 7 : a.urgency) - (b.urgency == null ? 7 : b.urgency))\n      || (STATUS_ORDER.indexOf(a.status) - STATUS_ORDER.indexOf(b.status))\n      || (b.score - a.score) || (a.nearestR - b.nearestR);\n  });\n}\n\n// Market context from the analyses of SPY / QQQ (and optionally a sector ETF).\n// Bullish needs VWAP and EMA alignment to agree across the index ETFs.\nfunction marketContext(list) {\n  var have = list.filter(function (x) { return x && x.state; });\n  if (!have.length) return { label: 'Unavailable', score: 0, parts: [] };\n  var score = 0, parts = [];\n  have.forEach(function (x) {\n    var b = x.state.bar, s = 0;\n    s += b.aboveVwap ? 1 : -1;\n    s += b.align === 'bull' ? 1 : b.align === 'bear' ? -1 : 0;\n    if (x.state.announced === 'UP') s += 1; else if (x.state.announced === 'DOWN') s -= 1;\n    score += s; parts.push({ symbol: x.symbol, score: s, aboveVwap: b.aboveVwap, align: b.align, trend: x.state.trend, close: b.close, vwap: b.vwap });\n  });\n  var per = score / have.length;\n  return { label: per >= 1.5 ? 'Bullish' : per <= -1.5 ? 'Bearish' : 'Neutral', score: score, parts: parts };\n}\n\n// Bottom line: structure first, indicators only move confidence.\n// Never emits LONG without a confirmed structural event.\nfunction bottomLine(A, market) {\n  if (!A || !A.state) return null;\n  var S = A.state, b = S.bar, n = A.states.length, atr = b.atr20 || b.avgRange || 1;\n  var recent = function (type, within, pred) {\n    for (var i = n - 1; i >= Math.max(0, n - 1 - within); i--) {\n      var hit = A.states[i].events.find(function (e) { return e.type === type && (!pred || pred(e)); });\n      if (hit) return { e: hit, ago: n - 1 - i };\n    }\n    return null;\n  };\n  var conf = function (e) { return e.confirmed === true; };\n  var breakout = recent('breakout', 3, conf) || recent('strong_breakout', 3, conf);\n  var bullBreak = recent('structure_break', 3, function (e) { return e.side === 'bull' && e.confirmed === true; });\n  var retest = recent('retest_hold', 5);\n  var rejected = recent('rejected', 5), failed = recent('failed', 5);\n  var bearBreak = recent('structure_break', 10, function (e) { return e.side === 'bear' && e.confirmed === true; });\n  var supBreak = recent('support_break', 10, conf);\n  var vwapReclaim = recent('vwap_reclaim', 3);\n\n  // Levels above: every confirmed swing high of the last 90 bars, the 60-bar\n  // high, the day high and any reclaim level — ascending, merged if closer\n  // than a quarter range. First = watch, second = trigger, third = strong.\n  var above = [], below = [];\n  A.swings.forEach(function (w) {\n    if (w.kind !== 'H' || w.confirmedAt > n - 1 || n - 1 - w.i > 90 || w.price <= b.close) return;\n    above.push({ price: w.price, reason: 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time });\n  });\n  if (S.res) above.push({ price: S.res.price, reason: 'גבוה 60 נרות' + (S.res.time ? ' ' + S.res.time : '') });\n  if (b.dayHigh > b.close) above.push({ price: b.dayHigh, reason: 'גבוה היום' });\n  if (S.reclaim != null && S.reclaim > b.close) above.push({ price: S.reclaim, reason: 'רמה שנשברה — reclaim' });\n  if (S.sup) below.push({ price: S.sup.price, reason: S.sup.kind === 'postbreak' ? 'הנמוך מאז השבירה' : 'נמוך 60 נרות' + (S.sup.time ? ' ' + S.sup.time : '') });\n  if (S.lastSL && S.lastSL.price < b.close && !(S.reclaim != null && S.lastSL.price >= S.reclaim)) below.push({ price: S.lastSL.price, reason: 'swing ' + S.lastSL.label + ' ' + S.lastSL.time });\n  if (b.dayLow < b.close) below.push({ price: b.dayLow, reason: 'נמוך היום' });\n  var merge = function (arr, asc) {\n    arr.sort(function (x, y) { return asc ? x.price - y.price : y.price - x.price; });\n    var out = [];\n    arr.forEach(function (x) { var last = out[out.length - 1]; if (last && Math.abs(x.price - last.price) <= 0.25 * atr) { if (!/swing/.test(last.reason) && /swing/.test(x.reason)) last.reason = x.reason; return; } out.push(x); });\n    return out;\n  };\n  above = merge(above, true); below = merge(below, false);\n  var watch = above[0] || null, trigger = above[1] || null, strong = above[2] || null, invalid = below[0] || null;\n  var dist = function (lv) { return lv ? { pts: lv.price - b.close, atr: (lv.price - b.close) / atr } : null; };\n\n  // Structural setup?\n  var trendUp = S.trend === 'UP', announcedUp = S.announced === 'UP', announcedDown = S.announced === 'DOWN';\n  var setup = trendUp || !!breakout || !!bullBreak || !!retest;\n\n  // Confidence\n  var c = 0, why = [];\n  if (breakout || bullBreak) { c += 3; why.push('+3 פריצה/שבירה מבנית מאושרת'); }\n  if (trendUp) { c += 2; why.push('+2 HH/HL'); }\n  if (b.aboveVwap) { c += 1; why.push('+1 מעל VWAP'); } else { c -= 2; why.push('-2 מתחת VWAP'); }\n  if (b.align === 'bull') { c += 1; why.push('+1 EMA שורי'); } else if (b.align === 'bear') { c -= 1; why.push('-1 EMA דובי'); }\n  var volOk = b.volx >= 1.2 || (breakout && A.states[n - 1 - breakout.ago].bar.volx >= 1.2);\n  if (volOk) { c += 1; why.push('+1 נפח'); }\n  if (market && market.label === 'Bullish') { c += 1; why.push('+1 שוק'); } else if (market && market.label === 'Bearish') { c -= 2; why.push('-2 שוק נגד'); }\n  if (retest) { c += 1; why.push('+1 בדיקה חוזרת'); }\n  if (rejected) { c -= 2; why.push('-2 דחייה'); }\n  if (bearBreak || supBreak || failed) { c -= 3; why.push('-3 ביטול/שבירה'); }\n  c = Math.max(0, Math.min(10, c));\n\n  // Action\n  var action = 'WAIT', reason = 'אין setup מבני';\n  if (announcedDown || bearBreak || supBreak) { action = 'AVOID'; reason = announcedDown ? 'מגמת ירידה מוכרזת' : 'תמיכה/מבנה נשברו'; }\n  else if ((breakout || bullBreak) && c >= 6) { action = 'LONG'; reason = (breakout ? 'פריצה' : 'שבירת מבנה שורית') + ' מאושרת · ביטחון ' + c; }\n  else if (breakout || bullBreak) { action = 'LONG WATCH'; reason = 'פריצה מאושרת אבל ביטחון ' + c + '/10'; }\n  else if (setup && watch && (watch.price - b.close) <= 1.0 * atr) { action = 'LONG WATCH'; reason = 'מגמה עולה, ' + ((watch.price - b.close) / atr).toFixed(1) + '× טווח מהרמה הראשונה'; }\n  else if (setup && vwapReclaim) { action = 'LONG WATCH'; reason = 'VWAP נכבש מחדש בתוך מגמה עולה'; }\n  else if (setup) { action = 'WAIT'; reason = 'מגמה עולה, רחוק מהטריגר'; }\n\n  return {\n    action: action, reason: reason, confidence: c, why: why,\n    watch: watch ? { price: watch.price, reason: watch.reason, dist: dist(watch) } : null,\n    trigger: trigger ? { price: trigger.price, reason: trigger.reason, dist: dist(trigger) } : null,\n    strong: strong ? { price: strong.price, reason: strong.reason, dist: dist(strong) } : null,\n    reclaim: S.reclaim != null ? { price: S.reclaim, dist: dist({ price: S.reclaim }) } : null,\n    invalidation: invalid ? { price: invalid.price, reason: invalid.reason, dist: dist(invalid) } : null,\n    vwap: b.vwap, aboveVwap: b.aboveVwap, ema9: b.ema9, ema20: b.ema20, align: b.align, atr: atr,\n    market: market ? market.label : 'Unavailable', setup: setup\n  };\n}\n\n// Multi-day layer, time-of-day volume, path probability and the plain-language\n// \"what now\" block. Kept in its own file so engine.cjs stays the intraday core.\n\n// ---------------------------------------------------------------- volume by time of day\n// The closing auction is not comparable to a normal minute, so relative volume\n// is measured against the SAME minute on previous days. With no history the\n// baseline is null and callers fall back to the session average.\nfunction volumeBaseline(daysRows) {\n  var sum = {}, cnt = {}, days = 0;\n  (daysRows || []).forEach(function (rows) {\n    if (!rows || !rows.length) return;\n    days++;\n    rows.forEach(function (r) { sum[r.time] = (sum[r.time] || 0) + r.volume; cnt[r.time] = (cnt[r.time] || 0) + 1; });\n  });\n  if (days < 2) return null;\n  var by = {};\n  Object.keys(sum).forEach(function (t) { by[t] = sum[t] / cnt[t]; });\n  return { byTime: by, days: days };\n}\nfunction volxTod(bar, baseline) {\n  if (!baseline || !baseline.byTime[bar.time]) return null;\n  var b = baseline.byTime[bar.time];\n  return b > 0 ? Math.round(bar.volume / b * 100) / 100 : null;\n}\n\n// ---------------------------------------------------------------- daily layer\n// daysRows: array of one day's 1-minute rows, oldest first, today last.\n// A US regular session is 390 one-minute bars, 09:30 to 15:59, plus the 16:00\n// auction print. Anything materially short of that is a session still being\n// collected or one that was fetched mid-day — and aggregating it produces a\n// daily candle that is simply wrong, with no outward sign.\n//\n// Measured: a WMT session truncated at 13:20 reported a high of 106.44 when the\n// real high was 106.78, and that number reached the screen as \"yesterday's\n// high\". A partial session is now marked and excluded from anything that claims\n// to be a completed day.\nvar SESSION_BARS = 390, MIN_COVERAGE = 0.85;\n// Counting bars alone is the wrong test. A thinly traded name has no print in\n// some minutes and the feed omits them, so a COMPLETE session can arrive with\n// 343 bars — PGR, BLK and HON were all refused that way. What actually\n// distinguishes a complete session is that it SPANS the session: it starts at\n// the open and runs to the close. A day truncated at 13:20 fails that however\n// many bars it has.\nfunction sessionSpans(rows) {\n  if (!rows || !rows.length) return false;\n  var times = rows.map(function (r) { return r.time; }).sort();\n  return times[0] <= '09:35' && times[times.length - 1] >= '15:55';\n}\nfunction sessionCoverage(rows) {\n  if (!rows || !rows.length) return 0;\n  var regular = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n  return regular.length / SESSION_BARS;\n}\nfunction aggregateSession(rows) {\n  var o = rows[0].open, c = rows[rows.length - 1].close;\n  var h = -Infinity, l = Infinity, v = 0;\n  rows.forEach(function (r) { h = Math.max(h, r.high); l = Math.min(l, r.low); v += r.volume; });\n  var cov = sessionCoverage(rows), spans = sessionSpans(rows);\n  return { date: rows[0].date, open: o, high: h, low: l, close: c, volume: v, range: h - l,\n    closeStrength: h > l ? (c - l) / (h - l) : 0.5,\n    bars: rows.length, coverage: Math.round(cov * 1000) / 1000, spans: spans,\n    complete: spans && cov >= MIN_COVERAGE,\n    hasAuction: rows.some(function (r) { return r.time === '16:00'; }),\n    source: 'aggregated' };\n}\n\nfunction dailyContext(daysRows, opts) {\n  var o2 = opts || {};\n  var days = (daysRows || []).filter(function (r) { return r && r.length; });\n  if (!days.length) return null;\n  var bars = days.map(aggregateSession);\n  // An authoritative daily candle, when the caller has one, replaces the\n  // aggregation for that date entirely. Never a mix of the two.\n  if (o2.authoritative) {\n    var auth = {};\n    (o2.authoritative || []).forEach(function (b) { auth[b.date] = b; });\n    bars = bars.map(function (b) {\n      var a = auth[b.date];\n      if (!a) return b;\n      return { date: b.date, open: a.open, high: a.high, low: a.low, close: a.close,\n        volume: a.volume, range: a.high - a.low,\n        closeStrength: a.high > a.low ? (a.close - a.low) / (a.high - a.low) : 0.5,\n        bars: b.bars, coverage: b.coverage, complete: true, hasAuction: b.hasAuction,\n        source: 'authoritative' };\n    });\n  }\n  // Which of these IS today? Before the open there is no session for today at\n  // all, and treating the newest stored row as \"today\" pushed every previous-day\n  // value one session further back than its label claimed. The caller passes the\n  // current trading date; without one the old positional rule stands, but the\n  // result says which rule was used.\n  var todayDate = o2.todayDate || null;\n  var hasToday = todayDate ? bars.some(function (b) { return b.date === todayDate; }) : true;\n  var todayIdx = todayDate\n    ? (hasToday ? bars.map(function (b) { return b.date; }).indexOf(todayDate) : bars.length)\n    : bars.length - 1;\n  var today = bars[todayIdx] || bars[bars.length - 1] || null;\n  // \"Previous\" must mean the previous COMPLETED session. A partial day is not a\n  // day, and silently using one is how a truncated high became yesterday's high.\n  var prior = bars.slice(0, todayIdx).filter(function (b) { return b.complete; });\n  var excluded = bars.slice(0, todayIdx).filter(function (b) { return !b.complete; });\n  var atrDaily = prior.length ? prior.reduce(function (s, b) { return s + b.range; }, 0) / prior.length : today.range;\n  // Daily trend over the prior sessions: higher closes and higher lows.\n  var trend = 'RANGE', why = 'אין רצף יומי';\n  if (prior.length >= 3) {\n    var last3 = prior.slice(-3);\n    var upCloses = last3.filter(function (b, i) { return i === 0 || b.close > last3[i - 1].close; }).length;\n    var upLows = last3.filter(function (b, i) { return i === 0 || b.low > last3[i - 1].low; }).length;\n    if (upCloses >= 3 && upLows >= 2) { trend = 'UP'; why = 'סגירות ושפלים עולים'; }\n    else if (upCloses <= 1 && upLows <= 1) { trend = 'DOWN'; why = 'סגירות ושפלים יורדים'; }\n    else why = 'מעורב';\n  }\n  var prev = prior.length ? prior[prior.length - 1] : null;\n  // Multi-day levels: prior highs/lows that price reacted to more than once.\n  var levels = [];\n  prior.forEach(function (b) { levels.push({ price: b.high, why: 'גבוה ' + b.date }, { price: b.low, why: 'נמוך ' + b.date }); });\n  var gap = prev ? today.open - prev.close : 0;\n  return { days: bars.length, priorDays: prior.length, trend: trend, trendWhy: why,\n    incomplete: excluded.map(function (b) { return { date: b.date, bars: b.bars,\n      coverage: Math.round(b.coverage * 100) + '%' }; }),\n    hasToday: hasToday, todayDate: todayDate,\n    previousDate: prior.length ? prior[prior.length - 1].date : null,\n    dateRule: todayDate ? 'previous = newest COMPLETE session before ' + todayDate\n      : 'previous = the row before the last (no trading date supplied)',\n    sources: bars.map(function (b) { return { date: b.date, source: b.source, complete: b.complete }; }),\n    prevHigh: prev ? prev.high : null, prevLow: prev ? prev.low : null, prevClose: prev ? prev.close : null,\n    atrDaily: atrDaily, levels: levels, gap: gap, today: today,\n    closeStrength: prev ? prev.closeStrength : null };\n}\n\n\n\n// ---------------------------------------------------------------- level state\n// The current condition of a level, decided from where price is now and what it\n// did on its way here. A support that was lost and then regained reads as\n// reclaimed — never as \"breaking\", which is what it did ten candles ago.\nvar LEVEL_TEXT = {\n  approaching: 'מתקרב', testing: 'נבחנת', broken: 'נשברה', reclaimed: 'נכבשה מחדש',\n  reclaiming: 'נשברה וחוזרת מעליה', held: 'נשמרת', rejected: 'נדחתה', lost: 'אבדה', far: 'רחוקה'\n};\nfunction levelState(A, level, isSupport, look) {\n  if (!A || !A.state || level == null) return null;\n  var bars = A.bars, n = bars.length, b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  var w = bars.slice(Math.max(0, n - (look || 20)));\n  var d = (b.close - level) / atr;                       // + = price above the level\n  var near = w.filter(function (x) { return x.low <= level + 0.25 * atr && x.high >= level - 0.25 * atr; });\n  var closedBelow = w.filter(function (x) { return x.close < level - 0.15 * atr; }).length;\n  var closedAbove = w.filter(function (x) { return x.close > level + 0.15 * atr; }).length;\n  var lastTwoBelow = n >= 2 && bars[n - 1].close < level - 0.1 * atr && bars[n - 2].close < level - 0.1 * atr;\n  var lastTwoAbove = n >= 2 && bars[n - 1].close > level + 0.1 * atr && bars[n - 2].close > level + 0.1 * atr;\n  var recoveries = w.filter(function (x) { return x.low < level && x.close > level; }).length;\n  // Bars whose LOW went through a support, or whose HIGH went through a\n  // resistance, without closing beyond it: pierced, not merely approached.\n  var pierced = isSupport\n    ? w.filter(function (x) { return x.low < level - 0.02 * atr; }).length\n    : w.filter(function (x) { return x.high > level + 0.02 * atr; }).length;\n  var rejections = w.filter(function (x) { return x.high > level && x.close < level; }).length;\n\n  var state;\n  if (isSupport) {\n    // \"Held\" must mean the level was never given up. Requiring only two\n    // recoveries let a support that CLOSED below five times in a row still read\n    // as held: GOOGL closed 337.31, 337.31, 337.17, 337.32, 337.275 under\n    // 337.57 and came back at 337.75, which is a break and a reclaim, not a\n    // level that was defended.\n    if (lastTwoBelow) state = 'broken';\n    else if (closedBelow >= 2 && lastTwoAbove) state = 'reclaimed';\n    else if (closedBelow >= 2 && d > 0) state = 'reclaiming';\n    // Closes are not the whole story. GOOGL's 338.51 was traded through on four\n    // separate bars — lows of 338.4233, 338.4096, 338.43, 338.42 — and still\n    // read as held because none of them CLOSED below. A level price went\n    // through is not a level that was defended.\n    else if (closedBelow === 0 && pierced === 0 && recoveries >= 2 && d > 0) state = 'held';\n    else if (closedBelow === 0 && pierced >= 2 && d > 0) state = 'reclaiming';\n    else if (Math.abs(d) <= 0.25) state = 'testing';\n    else if (Math.abs(d) <= 1.0) state = 'approaching';\n    else state = 'far';\n  } else {\n    if (lastTwoAbove) state = 'broken';                  // resistance broken = price accepted above\n    // \"Lost\" means the level stopped functioning. A resistance with price\n    // sitting UNDER it is still a resistance: it was rejected, not lost.\n    else if (closedAbove >= 2 && lastTwoBelow && d >= 0) state = 'lost';\n    else if (closedAbove >= 2 && lastTwoBelow) state = 'rejected';\n    else if (rejections >= 1 && d < 0) state = 'rejected';\n    else if (Math.abs(d) <= 0.25) state = 'testing';\n    else if (Math.abs(d) <= 1.0) state = 'approaching';\n    else state = 'far';\n  }\n  return { price: level, state: state, text: LEVEL_TEXT[state], distanceR: d,\n    touches: near.length, recoveries: recoveries, rejections: rejections, isSupport: !!isSupport };\n}\n\n// ---------------------------------------------------------------- buying / selling pressure\n//\n// IMPORTANT: this bar feed carries no order book and no trade-by-trade tape.\n// There is no bid/ask, no Level 2, no per-trade aggressor flag. What follows is\n// pressure INFERRED FROM CANDLES: where each minute closed inside its own range,\n// weighted by that minute's volume. Every result is labelled `source: 'candles'`\n// so the UI can say so and never imply it read the book.\n//\n//   buyVolume  = volume * (close - low)  / (high - low)\n//   sellVolume = volume * (high - close) / (high - low)\n//\n// which is the standard close-location value applied to volume. A minute that\n// closes on its high counts fully as buying, on its low fully as selling.\nfunction pressure(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, bars = A.bars, n = bars.length;\n  if (!n) return null;\n  var WIN = c.window || 15, RECENT = c.recent || 5;\n  var b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n\n  var split = function (list) {\n    var buy = 0, sell = 0;\n    list.forEach(function (x) {\n      var rng = x.high - x.low;\n      // A zero-range minute has no information about who won it.\n      if (rng <= 0 || !x.volume) return;\n      var clv = (x.close - x.low) / rng;\n      buy += x.volume * clv; sell += x.volume * (1 - clv);\n    });\n    var tot = buy + sell;\n    return { buy: buy, sell: sell, total: tot, buyPct: tot > 0 ? buy / tot * 100 : 50 };\n  };\n\n  // The closing auction is excluded: it is one print, not a minute of trading.\n  var usable = bars.filter(function (x) { return !x.auction; });\n  var win = usable.slice(-WIN), recent = usable.slice(-RECENT), prior = usable.slice(-(WIN), -RECENT);\n  if (win.length < 3) return null;\n\n  var W = split(win), R = split(recent), P = prior.length >= 3 ? split(prior) : null;\n  var buyPct = Math.round(W.buyPct), sellPct = 100 - buyPct;\n\n  // Strengthening / weakening is measured per side, on volume actually traded,\n  // not on the percentage alone: a side can gain share while doing less.\n  var perBar = function (s, list) { return list.length ? s / list.length : 0; };\n  var dBuy = P ? perBar(R.buy, recent) - perBar(P.buy, prior) : 0;\n  var dSell = P ? perBar(R.sell, recent) - perBar(P.sell, prior) : 0;\n  var scale = P ? Math.max(perBar(P.buy, prior) + perBar(P.sell, prior), 1) : 1;\n  // A side is only strengthening if it is doing MORE and taking MORE of the\n  // flow. Without the share test a volume surge would read as both sides\n  // strengthening at once, which tells the trader nothing.\n  var dShare = P ? R.buyPct - P.buyPct : 0;\n  var trendOf = function (d, shareDelta) {\n    var rel = d / scale;\n    if (rel >= 0.15 && shareDelta > -3) return 'מתחזקים';\n    if (rel <= -0.15 || shareDelta < -8) return 'נחלשים';\n    return 'ללא שינוי';\n  };\n  var buyersTrend = trendOf(dBuy, dShare), sellersTrend = trendOf(dSell, -dShare);\n\n  // Is the nearby level being defended or absorbed?\n  var T = c.tactical || tactical(A);\n  // Level condition comes from levelState(), which is anchored to the current\n  // price. Volume only adds the \"absorbed\" nuance on top of it.\n  var levelVerdict = function (level, isSupport) {\n    if (!level) return null;\n    var ls = levelState(A, level.price, isSupport);\n    if (!ls) return null;\n    var near = usable.filter(function (x) {\n      return x.low <= level.price + 0.3 * atr && x.high >= level.price - 0.3 * atr;\n    }).slice(-12);\n    var vol = near.length ? near.reduce(function (s, x) { return s + x.volume; }, 0) / near.length : 0;\n    var avgVol = usable.reduce(function (s, x) { return s + x.volume; }, 0) / usable.length;\n    var heavy = avgVol > 0 && vol / avgVol >= 1.2;\n    var parked = near.filter(function (x) { return Math.abs(x.close - level.price) <= 0.2 * atr; }).length;\n    var absorbed = heavy && parked >= 3 && (ls.state === 'testing' || ls.state === 'approaching');\n    return { price: level.price, state: ls.state, verdict: absorbed ? 'נבלעת' : ls.text,\n      distanceR: ls.distanceR, touches: near.length, heavy: heavy,\n      held: ls.recoveries, through: isSupport ? (ls.state === 'broken' ? 2 : 0) : (ls.state === 'broken' ? 2 : 0) };\n  };\n  var sup = levelVerdict(T.support, true), res = levelVerdict(T.resistance, false);\n\n  // Does the flow agree with the setup on the table?\n  var plan = c.plan || null;\n  var bullishSetup = plan && ['WAITING_FOR_ZONE', 'IN_ZONE', 'READY_PARTIAL', 'WAITING_FOR_CONFIRMATION', 'READY_ADD', 'ACTIVE', 'TAKE_PROFIT_AREA'].indexOf(plan.state) >= 0;\n  var side = buyPct >= 58 ? 'buyers' : buyPct <= 42 ? 'sellers' : 'balanced';\n  // Who is winning now and which way that is heading are two different facts.\n  // \"Buyers 64%\" plus \"buyers weakening\" is not support for a long — it is a\n  // lead that is being given back, and the wording has to say so.\n  // When BOTH sides are fading, the buyers' edge may be widening but their\n  // pressure is not strengthening. Saying it is contradicts the two trend\n  // labels printed directly above it.\n  // Who leads and how each side is moving are separate facts, and the sentence\n  // must be built from BOTH. Saying \"buyers strengthening\" because the SELLERS\n  // faded, while the buyers' own trend reads unchanged, contradicts the two\n  // labels printed directly above it.\n  var UP = 'מתחזקים', DOWN = 'נחלשים', SAME = 'ללא שינוי';\n  var leaderTrend = side === 'sellers' ? sellersTrend : buyersTrend;\n  var otherTrend = side === 'sellers' ? buyersTrend : sellersTrend;\n  // 'own'      the leading side is itself doing more\n  // 'relative' the leader is unchanged or fading, but the other side fades faster\n  // 'fading'   the leading side is giving its lead back\n  var direction = leaderTrend === UP ? 'own'\n    : leaderTrend === DOWN ? (otherTrend === DOWN ? 'relative' : 'fading')\n    : otherTrend === DOWN ? 'relative'\n    : otherTrend === UP ? 'fading' : 'steady';\n\n  var leadWord = side === 'sellers' ? 'המוכרים' : 'הקונים';\n  var conclusion;\n  if (side === 'balanced') {\n    conclusion = buyersTrend === UP && sellersTrend !== UP ? 'הכוחות שקולים, הקונים משתפרים'\n      : sellersTrend === UP && buyersTrend !== UP ? 'הכוחות שקולים, המוכרים משתפרים'\n      : 'הכוחות שקולים';\n  } else if (direction === 'own') {\n    conclusion = leadWord + ' מובילים והלחץ שלהם מתחזק';\n  } else if (direction === 'relative') {\n    // The leader is not stronger. The gap widened because the other side eased.\n    conclusion = leadWord + ' מובילים; היתרון היחסי שלהם משתפר משום ש'\n      + (side === 'sellers' ? 'הקונים' : 'המוכרים') + ' נחלשים'\n      + (leaderTrend === DOWN ? ', אך גם הלחץ שלהם עצמו נחלש' : '');\n  } else if (direction === 'fading') {\n    conclusion = leadWord + ' עדיין מובילים, אבל הלחץ שלהם נחלש';\n  } else {\n    conclusion = leadWord + ' מובילים, ללא שינוי בעוצמה';\n  }\n  var agreement = !bullishSetup ? 'לא רלוונטי'\n    : side === 'buyers' ? (direction === 'own' ? 'תומך'\n        : direction === 'fading' ? 'תומך אך נחלש'\n        : direction === 'relative' ? 'תומך במידה' : 'תומך')\n    : side === 'sellers' ? 'סותר'\n    : (buyersTrend === 'נחלשים' ? 'נוטה נגד' : 'ניטרלי');\n\n  // Net contribution to the up-side, in the same units the probability model uses.\n  var tilt = 0;\n  if (side === 'buyers') tilt += 1; else if (side === 'sellers') tilt -= 1;\n  if (buyersTrend === 'מתחזקים') tilt += 1;\n  if (buyersTrend === 'נחלשים') tilt -= 1;\n  if (sellersTrend === 'מתחזקים') tilt -= 1;\n  if (sellersTrend === 'נחלשים') tilt += 1;\n  if (sup && (sup.state === 'held' || sup.state === 'reclaimed')) tilt += 1;\n  if (sup && (sup.state === 'broken' || sup.verdict === 'נבלעת')) tilt -= 1;\n  if (res && res.state === 'broken') tilt += 1;\n  if (res && (res.state === 'rejected' || res.verdict === 'נבלעת')) tilt -= 1;\n\n  return {\n    source: 'candles',                       // never 'orderbook' — we have none\n    hasOrderBook: false, hasTape: false,\n    buyPct: buyPct, sellPct: sellPct,\n    side: side, buyersTrend: buyersTrend, sellersTrend: sellersTrend,\n    support: sup, resistance: res, agreement: agreement, tilt: tilt,\n    direction: direction, conclusion: conclusion,\n    window: win.length, recentWindow: recent.length,\n    summary: 'קונים ' + buyPct + '% · מוכרים ' + sellPct + '%',\n    trendSummary: 'קונים ' + buyersTrend + ' · מוכרים ' + sellersTrend\n  };\n}\n\n// ---------------------------------------------------------------- features\n// A compact description of \"situations like this one\", used both to bucket\n// history and to score the model fallback.\nfunction features(A, ctx, upper, lower) {\n  var S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var vx = (ctx && ctx.baseline) ? volxTod(b, ctx.baseline) : null;\n  if (vx == null) vx = b.volx;\n  var mom = momentum(A);\n  return {\n    structure: S.announced || S.trend,\n    vwap: b.aboveVwap ? 'above' : 'below',\n    ema: b.align,\n    momentum: mom.label,\n    vol: vx >= 1.5 ? 'high' : vx >= 0.8 ? 'normal' : 'low',\n    daily: ctx && ctx.daily ? ctx.daily.trend : 'NA',\n    dUp: Math.round(((upper - b.close) / atr) * 2) / 2,\n    dDown: Math.round(((b.close - lower) / atr) * 2) / 2,\n    tod: b.time < '10:30' ? 'open' : b.time < '14:30' ? 'mid' : 'late',\n    flow: (function () { var p = (ctx && ctx.pressure) || pressure(A, {}); return p ? p.side : 'NA'; })()\n  };\n}\nfunction bucketKey(f, coarse) {\n  return coarse\n    ? [f.structure, f.vwap, f.daily].join('|')\n    : [f.structure, f.vwap, f.ema, f.momentum, f.vol, f.daily, f.dUp, f.dDown].join('|');\n}\n\n// ---------------------------------------------------------------- calibration\n// Replays the loaded historical days and records, for each situation, whether\n// price rose by the required distance before falling by the other one, within\n// the horizon. These are real observed outcomes from the symbol's own history —\n// when there are too few of them the caller must not show a percentage.\nfunction calibrate(daysRows, opts) {\n  var o = opts || {}, horizon = o.horizon || 60, step = o.step || 2;\n  var table = {}, coarse = {}, total = 0;\n  (daysRows || []).forEach(function (rows) {\n    if (!rows || rows.length < 120) return;\n    var A = analyze(rows, o.engine || { K: 3 });\n    if (!A) return;\n    var bars = A.bars, n = bars.length;\n    for (var i = 60; i < n - horizon; i += step) {\n      var b = bars[i], atr = b.atr20 || b.avgRange || 1;\n      // sample a spread of target distances so the table can answer different asks\n      [0.5, 1, 1.5, 2].forEach(function (du) {\n        [0.5, 1, 1.5, 2].forEach(function (dd) {\n          var up = b.close + du * atr, dn = b.close - dd * atr, hit = null;\n          for (var k = i + 1; k <= i + horizon && k < n; k++) {\n            if (bars[k].high >= up) { hit = 'up'; break; }\n            if (bars[k].low <= dn) { hit = 'down'; break; }\n          }\n          if (!hit) return;\n          var sub = { state: A.states[i], bar: b };\n          var f = features({ state: A.states[i], bars: bars.slice(0, i + 1), swings: A.swings, states: A.states.slice(0, i + 1) },\n            { daily: o.daily, baseline: o.baseline }, up, dn);\n          [bucketKey(f, false), 'C:' + bucketKey(f, true) + '|' + f.dUp + '|' + f.dDown].forEach(function (key, idx) {\n            var t = idx === 0 ? table : coarse;\n            t[key] = t[key] || { n: 0, up: 0 };\n            t[key].n++; if (hit === 'up') t[key].up++;\n          });\n          total++;\n        });\n      });\n    }\n  });\n  return { table: table, coarse: coarse, total: total };\n}\n\n// ---------------------------------------------------------------- probability\n// Answers ONE defined question: does price reach `upper` before `lower`, within\n// `horizonMin` minutes. Empirical when the symbol's own history has enough\n// comparable cases; otherwise a transparent weighted model, reported as a bias\n// with a confidence score rather than a fake percentage.\n// Pull an estimate toward 50 in proportion to how much the model actually\n// knows. Full confidence (>=70) keeps the number as computed.\nfunction shrink(pct, confidence) {\n  var f = Math.max(0, Math.min(1, confidence / 70));\n  return Math.round(50 + (pct - 50) * f);\n}\n\n// How much of the session that SHOULD have happened by now do we actually hold?\n// A window that starts at 11:02 on a day that opened at 09:30 is not a session:\n// its open is not the open, its VWAP is not the VWAP, and its volume is a\n// fraction. Everything derived from the session is refused until this passes.\nfunction sessionCompleteness(rows, opts) {\n  var o = opts || {};\n  var out = { firstBar: null, lastBar: null, receivedMinutes: 0, expectedMinutes: null,\n    coveragePct: null, fromOpen: false, complete: false };\n  if (!rows || !rows.length) return out;\n  var reg = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n  if (!reg.length) return out;\n  var times = reg.map(function (r) { return r.time; }).sort();\n  out.firstBar = times[0];\n  out.lastBar = times[times.length - 1];\n  var seen = {}; reg.forEach(function (r) { seen[r.time] = 1; });\n  out.receivedMinutes = Object.keys(seen).length;\n  // Minutes that should exist between the open and the newest bar we hold.\n  var toMin = function (t) { var p = t.split(':'); return (+p[0]) * 60 + (+p[1]); };\n  out.expectedMinutes = Math.max(1, toMin(out.lastBar) - toMin('09:30') + 1);\n  out.coveragePct = Math.round(out.receivedMinutes / out.expectedMinutes * 100);\n  out.fromOpen = out.firstBar <= '09:35';\n  out.missingFromOpen = out.fromOpen ? 0 : toMin(out.firstBar) - toMin('09:30');\n  // Holes AFTER the first bar are a separate failure and were not reported at\n  // all: 49 bars scattered between 11:02 and 13:44 is not the same thing as a\n  // clean window starting late. Both numbers, always.\n  out.spanMinutes = toMin(out.lastBar) - toMin(out.firstBar) + 1;\n  out.missingInside = Math.max(0, out.spanMinutes - out.receivedMinutes);\n  out.missingTotal = out.missingFromOpen + out.missingInside;\n  out.contiguous = out.missingInside === 0;\n  // Both conditions: it must start at the open AND have most of the minutes in\n  // between. Either alone lets a hole through.\n  out.complete = out.fromOpen && out.coveragePct >= (o.minCoverage || 85);\n  return out;\n}\n\nfunction pathProbability(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = c.tactical || tactical(A);\n  var upper = c.upper != null ? c.upper : (T.resistance ? T.resistance.price : b.close + atr);\n  var lower = c.lower != null ? c.lower : (T.support ? T.support.price : b.close - atr);\n  var horizon = c.horizonMin || 60;\n  var f = features(A, c, upper, lower);\n  var out = { upper: upper, lower: lower, horizonMin: horizon, features: f, why: [] };\n\n  // A probability is only meaningful if the two levels are far enough apart to\n  // mean something for this instrument. Asking whether price reaches 146.63\n  // before 146.62 — one cent, a fraction of a normal minute's range — produces\n  // a confident-looking number about nothing.\n  // The question only means \"up or down\" when the two levels BRACKET the price.\n  // With both below it, \"reaches 187.34 before 187.27\" asks which small decline\n  // happens first — and reporting that as UP 78% reads as a claim the stock will\n  // rise. It also hands the model a free +6 for \"the upper level is closer\",\n  // which is pure geometry: three cents against ten.\n  var px = b.close;\n  out.price = px;\n  out.bracketed = lower < px && px < upper;\n  // \"Reaches 338.66 before 338.63\" with price at 338.61 asks whether the far\n  // level is touched before the near one on the SAME side, which cannot happen\n  // on a continuous path — 338.63 is passed on the way. The bracket test below\n  // catches it, but say plainly why rather than reporting it as a coincidence.\n  out.samSideOrdering = (upper > px && lower > px && lower < upper)\n    || (upper < px && lower < px && lower > upper);\n  if (!out.bracketed) {\n    out.up = null; out.down = null; out.confidence = 0;\n    out.notDirectional = true;\n    out.side = upper <= px ? 'both_below' : 'both_above';\n    if (out.samSideOrdering)\n      out.why.push('הרמות באותו צד והרחוקה נמצאת אחרי הקרובה — לא ניתן להגיע לאחת לפני השנייה בדרך רציפה');\n    out.why.push(upper <= px\n      ? 'שתי הרמות מתחת למחיר — השאלה אינה עלייה מול ירידה אלא איזו ירידה מגיעה קודם, ולכן לא מוצגת כהסתברות כיוון'\n      : 'שתי הרמות מעל המחיר — השאלה אינה עלייה מול ירידה, ולכן לא מוצגת כהסתברות כיוון');\n    return out;\n  }\n\n  var band = Math.abs(upper - lower);\n  var MIN_BAND_ATR = 0.25;\n  if (atr > 0 && band < MIN_BAND_ATR * atr) {\n    out.up = null; out.down = null; out.confidence = 0; out.meaningless = true;\n    out.band = band; out.minBand = MIN_BAND_ATR * atr;\n    out.why.push('הרמות קרובות מדי זו לזו (' + (Math.round(band * 100) / 100) +\n      ') ביחס לתנודה הרגילה — הסתברות כאן חסרת משמעות');\n    return out;\n  }\n\n  // 1. empirical\n  if (c.calibration) {\n    var exact = c.calibration.table[bucketKey(f, false)];\n    var coarse = c.calibration.coarse['C:' + bucketKey(f, true) + '|' + f.dUp + '|' + f.dDown];\n    var hit = (exact && exact.n >= 30) ? exact : (coarse && coarse.n >= 30) ? coarse : null;\n    if (hit) {\n      out.source = 'empirical'; out.n = hit.n;\n      out.up = Math.round(hit.up / hit.n * 100);\n      out.confidence = Math.min(90, 40 + Math.round(Math.min(hit.n, 400) / 8));\n      out.raw = out.up;\n      out.up = shrink(out.raw, out.confidence);\n      out.down = 100 - out.up;\n      out.lowConfidence = out.confidence < 50;\n      out.why.push(hit.n + ' מקרים דומים בהיסטוריה של הנייר');\n      return out;\n    }\n    out.sampled = (exact ? exact.n : 0) + (coarse ? coarse.n : 0);\n  }\n\n  // 2. transparent model. Each layer moves the odds; none of it is invented,\n  // and the result is labelled as a model bias, not an observed frequency.\n  var score = 0, add = function (pts, txt) { if (!pts) return; score += pts; out.why.push((pts > 0 ? '+' : '') + pts + ' ' + txt); };\n  add(f.structure === 'UP' ? 2 : f.structure === 'DOWN' ? -2 : 0, 'מבנה תוך-יומי ' + f.structure);\n  add(f.daily === 'UP' ? 2 : f.daily === 'DOWN' ? -2 : 0, 'מגמה רב-יומית ' + f.daily);\n  add(f.vwap === 'above' ? 1 : -1, 'מחיר ' + (f.vwap === 'above' ? 'מעל' : 'מתחת') + ' VWAP');\n  add(f.ema === 'bull' ? 1 : f.ema === 'bear' ? -1 : 0, 'EMA ' + f.ema);\n  add(f.momentum === 'PUSHING' || f.momentum === 'RECOVERY' ? 1 : f.momentum === 'SELLING' ? -1 : 0, 'מומנטום ' + f.momentum);\n  add(c.market === 'Bullish' ? 1 : c.market === 'Bearish' ? -1 : 0, 'שוק ' + (c.market || 'לא ידוע'));\n  var pres = c.pressure || pressure(A, { tactical: T });\n  if (pres) {\n    out.pressure = pres;\n    add(Math.max(-3, Math.min(3, pres.tilt)), 'לחץ קונים/מוכרים ' + pres.buyPct + '/' + pres.sellPct);\n  }\n  // Distance asymmetry: the nearer level is simply likelier to be reached.\n  var dU = (upper - b.close) / atr, dD = (b.close - lower) / atr;\n  var geom = dU + dD > 0 ? (dD - dU) / (dU + dD) : 0;\n  add(Math.round(geom * 3), 'הרמה ' + (geom > 0 ? 'העליונה' : 'התחתונה') + ' קרובה יותר');\n  var pct = Math.round(100 / (1 + Math.exp(-score / 3.2)));\n  out.source = 'model';\n  out.confidence = Math.max(20, Math.min(55, 25 + Math.abs(score) * 3));\n  out.raw = Math.max(10, Math.min(90, pct));\n  // A number like 87% next to \"confidence 43\" reads as certainty the model does\n  // not have. The displayed probability is pulled toward 50 in proportion to\n  // the confidence, so the two can never tell different stories.\n  out.up = shrink(out.raw, out.confidence);\n  out.down = 100 - out.up;\n  out.lowConfidence = out.confidence < 50;\n  out.bias = out.up >= 60 ? 'נטייה שורית' : out.up <= 40 ? 'נטייה דובית' : 'ללא נטייה ברורה';\n  return out;\n}\n\n// ---------------------------------------------------------------- what now\n// Plain language. No HH/HL/VWAP/R in the user-facing strings.\nvar ACTIONS = {\n  DO_NOT_BUY: 'לא לקנות',\n  START_WATCHING: 'להתחיל לעקוב',\n  WAIT_FOR_CONFIRMATION: 'לחכות לאישור',\n  ENTRY_AVAILABLE: 'אפשר להיכנס',\n  HOLD: 'להחזיק',\n  TAKE_PARTIAL: 'לממש חלק',\n  MOVE_STOP: 'להעלות סטופ',\n  EXIT: 'לצאת',\n  SETUP_CANCELLED: 'ה-setup בוטל',\n  SESSION_CLOSED: 'המסחר הסתיים',\n  DATA_STALE: 'נתונים לא עדכניים — לא לסחור',\n  WATCH_ONLY: 'לא לסחור — רק לעקוב',\n  DO_NOT_CHASE: 'לא לרדוף — להמתין לפולבק'\n};\nvar PLAN_TO_ACTION = {\n  NO_SETUP: 'DO_NOT_BUY',\n  WAITING_FOR_ZONE: 'START_WATCHING',\n  IN_ZONE: 'WAIT_FOR_CONFIRMATION',\n  READY_PARTIAL: 'ENTRY_AVAILABLE',\n  WAITING_FOR_CONFIRMATION: 'WAIT_FOR_CONFIRMATION',\n  READY_ADD: 'ENTRY_AVAILABLE',\n  ACTIVE: 'HOLD',\n  TAKE_PROFIT_AREA: 'TAKE_PARTIAL',\n  DO_NOT_CHASE: 'DO_NOT_CHASE',\n  FAILED: 'SETUP_CANCELLED'\n};\nfunction n2(x) { return x == null ? null : (Math.round(x * 100) / 100).toFixed(2); }\nfunction scenarioLabelFor(P, watch) {\n  if (!P || !P.kind) return 'ללא setup';\n  if (P.kind === 'breakout') return 'פריצה מעל ' + n2(watch);\n  return P.zone ? 'חזרה לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) : 'פולבק';\n}\n\nfunction whatNow(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = c.tactical || tactical(A);\n  var P = c.plan || executionPlan(A, { label: c.market || 'Neutral' });\n  var ended = c.sessionEnded;\n\n  var watch = T.resistance ? T.resistance.price : (P && P.zone ? P.zone[1] : b.dayHigh);\n  var hasZone = !!(P && P.zone && P.entry != null && P.addAbove != null);\n  var lower = T.support ? T.support.price : (P && P.invalidation != null ? P.invalidation : b.dayLow);\n  var prob = c.probability || pathProbability(A, { tactical: T, upper: watch, lower: lower,\n    horizonMin: 60, daily: c.daily, baseline: c.baseline, calibration: c.calibration, market: c.market });\n\n  // ---- one active scenario only. The plan already picked pullback OR\n  // breakout; whatNow must never narrate the other one as a live instruction.\n  var scenario = !P || !P.kind ? { kind: 'none', label: 'אין setup פעיל' }\n    : P.kind === 'breakout' ? { kind: 'breakout', label: 'פריצה מעל ' + n2(watch) }\n    : { kind: 'pullback', label: 'חזרה לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) };\n\n  // ---- no-edge gate. When the model's own numbers say there is nothing here,\n  // it must not offer an entry \"if X happens\".\n  var edge = c.edge || null;\n  var noEdge = !!(edge && edge.noEdge);\n  var stale = !!c.stale;\n  // The model's own cancellation rule, applied to the model's own price. If the\n  // last close is already through the level that voids the idea, the idea is\n  // void — it cannot still read START_WATCHING.\n  var cancelAt = (c.levels && c.levels.tacticalStop != null) ? c.levels.tacticalStop\n    : (P && P.invalidation != null ? P.invalidation : null);\n  var alreadyCancelled = !ended && cancelAt != null && b.close != null && b.close < cancelAt;\n  // Staleness outranks the plan. A setup derived from bars that stopped hours\n  // ago is a description of a market that has already moved.\n  var actionKey = ended ? 'SESSION_CLOSED'\n    : stale ? 'DATA_STALE'\n    : alreadyCancelled ? 'SETUP_CANCELLED'\n    : noEdge ? 'WATCH_ONLY'\n    : (PLAN_TO_ACTION[P ? P.state : 'NO_SETUP'] || 'DO_NOT_BUY');\n  var W = { action: actionKey, actionText: ACTIONS[actionKey], sessionEnded: !!ended,\n    stale: stale, cancelled: alreadyCancelled, price: b.close, watch: watch,\n    // A probability computed from stale bars is not a probability about now.\n    // A probability is a claim about the next hour. After the close there is no\n    // next hour, and on stale bars it is a claim about a market that has moved.\n    probability: (stale || ended || (prob && (prob.meaningless || prob.notDirectional))) ? null : prob,\n    probabilityRaw: prob || null,\n    plan: P, scenario: (stale || alreadyCancelled) ? { kind: 'none', label: 'אין setup פעיל' } : scenario,\n    noEdge: noEdge, edge: edge, up: [], down: [], why: [] };\n\n  // The one line that matters\n  if (stale) W.next = 'הנרות האחרונים ישנים — אין כאן איתות חי. הרמות למטה הן לעיון בלבד.';\n  else if (alreadyCancelled) W.next = 'המחיר כבר מתחת ל-' + n2(cancelAt) + ' — התרחיש בוטל. לחכות למבנה חדש.';\n  else if (ended) W.next = 'המסחר הסתיים — הרמות למטה הן לקראת המסחר הבא, לא להוראה עכשיו';\n  else if (noEdge) W.next = 'אין כרגע יתרון מספיק לכניסה. לעקוב אם המחיר מגיע ל-' + n2(watch) + '.';\n  else if (P && P.state === 'FAILED') W.next = 'אין כניסה. צריך מצב חדש לפני שמסתכלים שוב.';\n  else if (P && (P.state === 'READY_PARTIAL' || P.state === 'READY_ADD')) W.next = 'המחיר במקום שתכננו — ' + P.headline;\n  else W.next = 'לשים לב אם המחיר מגיע ל-' + n2(watch);\n\n  // If it goes up\n  var above = T.above.filter(function (l) { return l.price > b.close + 0.05 * atr; });\n  // The narrative must quote the SAME targets the levels block shows. Deriving\n  // them separately here is how the text came to say 'second target 146.66'\n  // while the levels said 146.73.\n  var canon = c.levels || null;\n  var t1 = (canon && canon.target1 != null) ? canon.target1\n    : (above[1] ? above[1].price : watch + 1.2 * atr);\n  var t2 = (canon && canon.target2 != null) ? canon.target2\n    : (above[2] && above[2].price >= t1 + 0.2 * atr ? above[2].price : t1 + 1.5 * atr);\n  // Two targets that print as the same number are not two targets. On quiet or\n  // low-priced instruments the rounded levels collided.\n  // Compare what will actually be PRINTED. Two levels 0.004 apart are distinct\n  // numbers that render as the same string, which is what the reader sees.\n  if (t1 != null && t2 != null && n2(t1) === n2(t2)) t2 = t1 + Math.max(0.01, 1.0 * atr);\n  if (stale) {\n    // Reference only. The last idea is described in the past tense, never as\n    // something to act on.\n    W.up.push('התרחיש האחרון שנרשם: ' + scenarioLabelFor(P, watch) + ' — לעיון בלבד');\n    W.up.push('אין הוראת כניסה על נתונים ישנים');\n  } else if (noEdge && !ended) {\n    // What would have to change before any entry is even discussed.\n    W.up.push('אם המחיר עולה מעל ' + n2(watch) + ' ונשאר שם — ייבחן מחדש');\n    W.up.push('כרגע אין הוראת כניסה');\n  } else if (!ended) {\n    if (P && P.state === 'READY_PARTIAL' && hasZone) {\n      W.up.push('אם המחיר נשאר מעל ' + n2(P.zone[0]) + ' — אפשר להיכנס בחלק מהסכום סביב ' + n2(P.entry));\n      W.up.push('אישור חזק יותר מעל ' + n2(P.addAbove) + ' (המערכת אינה יודעת אם יש פוזיציה)');\n    } else if (scenario.kind === 'breakout' && P.entry != null) {\n      W.up.push('אם עובר את ' + n2(watch) + ' ונשאר מעל — אפשר להיכנס סביב ' + n2(P.entry));\n    } else if (scenario.kind === 'pullback' && hasZone) {\n      W.up.push('אם המחיר יורד לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) + ' ונבלם — כניסה חלקית ב-' + n2(P.entry));\n      W.up.push('אישור חזק יותר מעל ' + n2(P.addAbove));\n    } else {\n      W.up.push('אם עולה מעל ' + n2(watch) + ' ונשאר שם — להתחיל לעקוב מקרוב');\n    }\n    if (!noEdge) { W.up.push('יעד ראשון ' + n2(t1) + ' — שם לממש חלק'); W.up.push('יעד שני ' + n2(t2)); }\n  } else {\n    W.up.push('הרמה החשובה למעלה הייתה ' + n2(watch));\n  }\n\n  // If it goes down\n  var below = T.below.filter(function (l) { return l.price < b.close - 0.05 * atr; });\n  var s1 = T.support ? T.support.price : (below[0] ? below[0].price : b.close - atr);\n  var s2 = below.find(function (l) { return l.price < s1 - 0.1 * atr; });\n  var s3 = P && P.invalidation != null ? P.invalidation : (s2 ? s2.price - 0.5 * atr : s1 - atr);\n  if (stale) {\n    // The DOWN block describes the downside, in the past tense. It must not\n    // repeat the UP block's lines — that duplication reached the screen.\n    W.down.push('התמיכה האחרונה שנרשמה: ' + n2(s1) + ' — לעיון בלבד');\n    W.down.push('אין רמת ביטול פעילה על נתונים ישנים');\n  } else if (noEdge && !ended) {\n    W.down.push('אם יורד ל-' + n2(s1) + ' — לבדוק אם קונים נכנסים שם');\n    W.down.push('מתחת ' + n2(s3) + ' — התרחיש החיובי יורד מהפרק');\n  } else if (!ended) {\n    W.down.push('אם יורד ל-' + n2(s1) + ' — לבדוק אם יש קונים שעוצרים את הירידה');\n    if (s2) W.down.push('אם שובר את ' + n2(s2.price) + ' — התרחיש החיובי נחלש');\n    W.down.push('אם יורד מתחת ' + n2(s3) + ' — לבטל, לא להיכנס');\n  } else {\n    W.down.push('התמיכה החשובה הייתה ' + n2(s1));\n  }\n\n  // Why, in plain words\n  var d = c.daily;\n  if (d) W.why.push('מגמה של כמה ימים: ' + (d.trend === 'UP' ? 'עולה' : d.trend === 'DOWN' ? 'יורדת' : 'ללא כיוון'));\n  W.why.push('היום: ' + (S.trend === 'UP' ? 'עולה' : S.trend === 'DOWN' ? 'יורד' : 'ללא כיוון'));\n  var mom = momentum(A);\n  W.why.push('בדקות האחרונות: ' + ({ PUSHING: 'לוחץ למעלה', PULLBACK: 'נסוג', RECOVERY: 'מתאושש', SELLING: 'נמכר', FLAT: 'שקט' }[mom.label]));\n  W.why.push(b.aboveVwap ? 'מעל ממוצע היום' : 'מתחת לממוצע היום');\n  var vx = c.baseline ? volxTod(b, c.baseline) : null;\n  if (vx != null) W.why.push('מחזור מול אותה דקה בימים קודמים: ' + vx.toFixed(1) + '×');\n  var pres = c.pressure || pressure(A, { tactical: T, plan: P });\n  if (pres) {\n    W.pressure = pres;\n    W.why.push('בדקות האחרונות ' + (pres.side === 'buyers' ? 'הקונים חזקים יותר' : pres.side === 'sellers' ? 'המוכרים חזקים יותר' : 'הכוחות שקולים')\n      + ' (' + pres.buyPct + '/' + pres.sellPct + ')');\n    if (pres.support && pres.support.state !== 'far')\n      W.why.push('התמיכה ב-' + n2(pres.support.price) + ' ' + pres.support.verdict);\n    if (pres.resistance && pres.resistance.state !== 'far')\n      W.why.push('ההתנגדות ב-' + n2(pres.resistance.price) + ' ' + pres.resistance.verdict);\n  }\n  // Identical lines are never informative twice, whichever branch produced them.\n  var uniq = function (list) { var seen = {}; return list.filter(function (x) {\n    if (seen[x]) return false; seen[x] = 1; return true; }); };\n  W.up = uniq(W.up); W.down = uniq(W.down); W.why = uniq(W.why);\n  return W;\n}\n\n\n// ---------------------------------------------------------------- ticker state\n//\n// ONE snapshot per symbol per refresh. Every UI surface — the radar row and the\n// detail sheet — renders from this object, so a score can never differ between\n// them. Nothing downstream recalculates.\n//\n// Level roles are explicit and each level has exactly one job:\n//   watch                 the price we are waiting for\n//   entry                 where a position would actually be opened\n//   target1 / target2     profit areas, always above entry for a long\n//   tacticalInvalidation  cancels the immediate entry idea\n//   hardStop              structure is broken; the whole idea is off\n//   probUpper / probLower the two boundaries the probability question uses\nvar STATE_VERSION = 4;\n\nfunction buildTickerState(symbol, A, ctx) {\n  var c = ctx || {};\n  var now = c.now || Date.now();\n  if (!A || !A.state) {\n    return { symbol: symbol, state_version: STATE_VERSION, calculated_at: now, data_through: null,\n      valid: false, violations: [{ code: 'NO_DATA', severity: 'block', text: 'אין נתונים' }],\n      status: 'NO DATA', action: 'DO_NOT_BUY', actionText: ACTIONS.DO_NOT_BUY };\n    }\n  var S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = tactical(A);\n  var plan = executionPlan(A, { label: c.market || 'Neutral' });\n  var pres = pressure(A, { tactical: T, plan: plan });\n\n  // --- levels, derived once\n  var lv = { watch: null, entry: null, target1: null, target2: null,\n    tacticalInvalidation: null, hardStop: null, probUpper: null, probLower: null };\n  if (plan && plan.kind) {\n    lv.watch = plan.kind === 'breakout' ? plan.zone[0] : plan.zone[1];\n    lv.entry = plan.entry;\n    lv.tacticalInvalidation = plan.kind === 'breakout' ? plan.zone[0] - 0.2 * atr : plan.zone[0];\n    lv.hardStop = plan.invalidation;\n  } else {\n    lv.watch = T.resistance ? T.resistance.price : b.dayHigh;\n    lv.tacticalInvalidation = T.support ? T.support.price : b.dayLow;\n    lv.hardStop = lv.tacticalInvalidation - 0.5 * atr;\n  }\n  // A long is cancelled on the way DOWN, so the level hit first must be the\n  // entry cancellation and the structural one must sit BELOW it. Reported the\n  // other way round — entry at 187.27, structure at 187.28 — the whole setup\n  // died before its own entry rule could ever fire.\n  if (lv.tacticalInvalidation != null && lv.hardStop != null && lv.hardStop >= lv.tacticalInvalidation) {\n    lv.hardStop = lv.tacticalInvalidation - Math.max(0.02, 0.3 * atr);\n    lv.hardStopAdjusted = true;\n  }\n  // Targets are always resolved ABOVE the entry. A level at or below the entry\n  // can be the watch level or resistance, but never target 1.\n  // With no entry planned, targets are measured from the WATCH level, not from\n  // the current price — otherwise the first level above price is both the thing\n  // we are waiting for and the first target, which is meaningless.\n  // The add trigger and target 1 cannot be the same price. One says \"commit\n  // more here\", the other says \"take some off here\", and printing both against\n  // 187.47 is an instruction to do two opposite things at once.\n  var addAt = plan && plan.addAbove != null ? plan.addAbove : null;\n  var anchor = lv.entry != null ? lv.entry : (lv.watch != null ? lv.watch : b.close);\n  var aboveEntry = T.above.filter(function (x) { return x.price >= anchor + 0.3 * atr; })\n    .sort(function (x, y) { return x.price - y.price; });\n  lv.target1 = aboveEntry[0] ? aboveEntry[0].price : anchor + 1.0 * atr;\n  // A target that lands on the add trigger is pushed to the next level above it,\n  // or one ATR beyond — the trigger keeps its role, the target moves.\n  if (addAt != null && Math.abs(lv.target1 - addAt) < 0.005) {\n    var beyond = aboveEntry.filter(function (x) { return x.price > addAt + 0.005; });\n    lv.target1 = beyond[0] ? beyond[0].price : addAt + Math.max(0.02, 0.5 * atr);\n    lv.target1MovedFromAdd = true;\n  }\n  lv.target2 = aboveEntry[1] && aboveEntry[1].price >= lv.target1 + 0.3 * atr ? aboveEntry[1].price : lv.target1 + 1.0 * atr;\n  lv.probUpper = lv.watch;\n  lv.probLower = lv.tacticalInvalidation;\n\n  var prob = pathProbability(A, { tactical: T, upper: lv.probUpper, lower: lv.probLower, horizonMin: 60,\n    daily: c.daily, baseline: c.baseline, calibration: c.calibration, market: c.market, pressure: pres });\n\n  // ---- edge check. The recommendation has to agree with the model's own\n  // numbers: a low score, a coin-flip probability, low confidence and balanced\n  // flow together mean there is nothing to act on, whatever the plan says.\n  var row0 = radarRow(symbol, A, c.marketCtx || { label: c.market || 'Neutral' }, c.freshness);\n  var score0 = row0.bl ? row0.bl.confidence : 0;\n  var weak = [];\n  if (score0 <= 2) weak.push('ציון setup נמוך');\n  if (prob && prob.meaningless) weak.push('הרמות קרובות מדי למדידה');\n  if (prob && prob.notDirectional) weak.push('הרמות אינן מקיפות את המחיר — אין הסתברות כיוון');\n  if (prob && prob.up != null && Math.abs(prob.up - 50) <= 8) weak.push('הסיכויים שקולים');\n  if (prob && prob.confidence < 50) weak.push('ביטחון נמוך');\n  if (pres && pres.side === 'balanced') weak.push('קונים ומוכרים מאוזנים');\n  var structureFlat = (row0.structure === 'RANGE') && (row0.momentum === 'FLAT');\n  if (structureFlat) weak.push('אין מבנה ואין מומנטום');\n  var edge = { noEdge: weak.length >= 4, reasons: weak, score: score0,\n    probUp: prob.up, confidence: prob.confidence, flow: pres ? pres.side : 'NA' };\n\n  // ---- staleness is a hard gate, not a label.\n  // Bars that stopped arriving hours ago describe a market that has moved on.\n  // Showing an entry, a probability or a buyer/seller split from them reads as\n  // live guidance about a price that no longer exists, so none of it survives.\n  // On a one-minute feed, four minutes of lag is long enough for price to have\n  // left the entry zone entirely. An entry offered on data that old is an\n  // instruction to trade a price that may no longer exist.\n  var ENTRY_MAX_AGE = 180;                       // three minutes\n  var STALE_SECONDS = 300;                       // five minutes of missing bars\n  var ageSec = (c.staleSeconds != null) ? c.staleSeconds\n    : (b.unix != null && c.nowUnix != null ? c.nowUnix - b.unix : null);\n  var isStale = c.sessionEnded ? false\n    : (c.freshness === 'STALE' || (ageSec != null && ageSec > STALE_SECONDS));\n  // Not stale enough to freeze the whole state, but too old to act on.\n  var tooOldToEnter = !c.sessionEnded && ageSec != null && ageSec > ENTRY_MAX_AGE;\n\n  // And the session itself must be present. A window that starts at 11:02 on a\n  // day that opened at 09:30 has no session open, no session VWAP and a\n  // fraction of the volume — every figure derived from it is wrong while\n  // looking entirely clean.\n  // Measure the rows that were LOADED, not the ones analysis kept. A no-trade\n  // minute is deliberately excluded from the analysis and is still a minute we\n  // hold — counting the filtered set turned a complete session into \"186 of 335\n  // with 57 holes\".\n  var cover = sessionCompleteness((A && (A.rawBars || A.bars)) || [], {});\n  var sessionIncomplete = !c.sessionEnded && !isStale && A && A.bars && A.bars.length > 0\n    && !cover.complete;\n\n  var W = whatNow(A, { tactical: T, plan: plan, market: c.market, daily: c.daily,\n    baseline: c.baseline, calibration: c.calibration, pressure: pres, probability: prob,\n    sessionEnded: c.sessionEnded, levels: lv, edge: edge, stale: isStale });\n\n  // Nothing actionable may remain on the state when there is no edge, so the\n  // technical block underneath cannot contradict the headline.\n  if (edge.noEdge && !c.sessionEnded) {\n    lv.entry = null; lv.target1 = null; lv.target2 = null;\n  }\n  // A closed session is a reference view, not an order. The levels stay visible\n  // as context, but nothing actionable survives — otherwise the headline says\n  // the market is shut while an entry price sits underneath it.\n  if (c.sessionEnded) { lv.entry = null; lv.target1 = null; lv.target2 = null; }\n  if (isStale) { lv.entry = null; lv.target1 = null; lv.target2 = null; }\n  // Too old to act on, or built from part of a session: the levels stay as\n  // context but no entry is offered, and the headline says which it is.\n  // Staleness is the stronger statement and must not be overwritten by the\n  // weaker one: data that is five minutes old is not merely \"delayed\".\n  if (!isStale && !c.sessionEnded && (tooOldToEnter || sessionIncomplete)) {\n    lv.entry = null; lv.target1 = null; lv.target2 = null;\n    W.action = 'WAIT';\n    W.actionText = sessionIncomplete\n      ? 'סשן חלקי — לא לסחור'\n      : 'נתונים באיחור — להמתין לנר טרי';\n    W.next = sessionIncomplete\n      ? ('נטענו ' + cover.receivedMinutes + ' דקות מתוך ' + cover.expectedMinutes\n         + ' — חסרות ' + cover.missingFromOpen + ' מהפתיחה ועוד ' + cover.missingInside\n         + ' בתוך החלון, אז הפתיחה, ה-VWAP, המבנה והמחזור אינם של הסשן')\n      : ('הנר האחרון בן ' + Math.round(ageSec) + ' שניות; בפיד של דקה זה מספיק כדי שהמחיר יעזוב את האזור');\n    // The WHAT-IF lines are built before this gate and still carried \"you can\n    // enter around 338.68\" with targets underneath a headline that says not to\n    // trade. Rewrite them as observations: the levels are worth naming, the\n    // instruction is not.\n    var obs = function (list) {\n      return (list || []).map(function (x) {\n        return String(x)\n          .replace(/אפשר להיכנס/g, 'רמה לצפייה')\n          .replace(/כניסה חלקית/g, 'אזור לצפייה')\n          .replace(/לממש/g, 'רמת יעד לשעבר')\n          .replace(/להוסיף/g, 'רמת אישור');\n      }).filter(function (x) { return !/להיכנס|לקנות|לממש חלק/.test(x); });\n    };\n    W.up = obs(W.up); W.down = obs(W.down);\n    W.up.unshift(sessionIncomplete\n      ? 'הרמות מוצגות לצפייה בלבד — הסדרה חסרה ' + cover.missingTotal + ' דקות'\n      : 'הרמות מוצגות לצפייה בלבד — הנתונים אינם טריים מספיק לפעולה');\n  }\n  var row = row0;\n\n  var st = {\n    symbol: symbol, state_version: STATE_VERSION, calculated_at: now,\n    data_through: { time: b.time, date: b.date || c.date || null, unix: b.unix != null ? b.unix : null },\n    price: b.close, atr: atr,\n    status: row.status, score: row.bl ? row.bl.confidence : 0,\n    action: W.action, actionText: W.actionText,\n    structure: row.structure, momentum: row.momentum,\n    coverage: cover, sessionIncomplete: sessionIncomplete, tooOldToEnter: tooOldToEnter,\n    levels: lv, plan: plan, tactical: T, pressure: pres, probability: W.probability,\n    probabilityRaw: W.probabilityRaw || null, whatNow: W,\n    scenario: W.scenario, edge: edge, noEdge: edge.noEdge,\n    levelStates: { support: T.support ? levelState(A, T.support.price, true) : null,\n                   resistance: T.resistance ? levelState(A, T.resistance.price, false) : null },\n    row: row, sessionEnded: !!c.sessionEnded, freshness: c.freshness || 'LIVE',\n    stale: isStale, data_age_seconds: ageSec\n  };\n  // Keep the row pointing at the same snapshot so nothing can drift.\n  row.score = st.score; row.state = st; row.pressure = pres;\n  if (edge.noEdge && !c.sessionEnded) {\n    row.status = row.status === 'AVOID' ? 'AVOID' : 'WATCH';\n    row.why = 'אין יתרון — רק מעקב';\n    st.status = row.status; st.action = 'WATCH_ONLY'; st.actionText = ACTIONS.WATCH_ONLY;\n  }\n  // The row is what the board shows, so it must carry the same verdict.\n  if (isStale) {\n    row.status = 'AVOID';\n    row.why = 'נתונים לא עדכניים — לא לסחור';\n    st.status = 'AVOID';\n    st.action = 'DATA_STALE'; st.actionText = ACTIONS.DATA_STALE;\n    st.probability = null;\n    st.pressure = pres ? Object.assign({}, pres, { current: false }) : pres;\n  }\n  var v = validateState(st);\n  st.violations = v.violations; st.valid = v.valid;\n  if (!v.valid) {\n    st.status = 'AVOID';\n    st.action = 'DO_NOT_BUY';\n    st.actionText = 'המתן — מצב המודל לא עקבי';\n    st.whatNow.actionText = st.actionText;\n    st.whatNow.next = 'הנתונים סותרים את עצמם, לכן לא מוצגת הוראת מסחר. יחושב מחדש בעדכון הבא.';\n    st.whatNow.up = []; st.whatNow.down = [];\n    // Nothing actionable may survive a blocked state.\n    st.levels = Object.assign({}, st.levels, { entry: null, target1: null, target2: null });\n  }\n  return st;\n}\n\n// ---------------------------------------------------------------- validation\n// Runs before anything is shown. A blocking violation replaces the trade\n// instruction with an explicit \"state inconsistent\" message rather than\n// letting a wrong instruction reach the screen.\nfunction validateState(st) {\n  var v = [], add = function (code, severity, text) { v.push({ code: code, severity: severity, text: text }); };\n  var lv = st.levels, p = st.price, plan = st.plan, atr = st.atr;\n  var actionable = ['ENTRY_AVAILABLE', 'HOLD', 'TAKE_PARTIAL'].indexOf(st.action) >= 0\n    || ['READY', 'ACTIVE'].indexOf(st.status) >= 0;\n\n  if (lv.entry != null) {\n    if (lv.target1 != null && lv.target1 <= lv.entry) add('TARGET_BELOW_ENTRY', 'block', 'יעד ראשון ' + n2(lv.target1) + ' אינו מעל הכניסה ' + n2(lv.entry));\n    if (lv.target2 != null && lv.target1 != null && lv.target2 <= lv.target1) add('TARGET2_BELOW_TARGET1', 'block', 'יעד שני אינו מעל יעד ראשון');\n    if (lv.hardStop != null && lv.hardStop >= lv.entry) add('STOP_ABOVE_ENTRY', 'block', 'הסטופ ' + n2(lv.hardStop) + ' אינו מתחת לכניסה ' + n2(lv.entry));\n    if (lv.tacticalInvalidation != null && lv.hardStop != null && lv.hardStop > lv.tacticalInvalidation)\n      add('STOP_ORDER', 'warn', 'הסטופ המבני אינו מתחת לביטול הטקטי');\n    // an entry the price has already left behind must not read as available\n    if (actionable && p > lv.entry + 0.6 * atr) add('ENTRY_BEHIND_PRICE', 'block', 'המחיר כבר רחוק מעל אזור הכניסה');\n  }\n  if (lv.probUpper != null && lv.probLower != null && lv.probUpper <= lv.probLower)\n    add('PROB_BOUNDS', 'block', 'גבולות ההסתברות אינם בסדר הנכון');\n  if (lv.watch != null && lv.target1 != null && Math.abs(lv.watch - lv.target1) < 1e-9)\n    add('LEVEL_DOUBLE_ROLE', 'block', 'אותה רמה משמשת גם כרמת מעקב וגם כיעד');\n\n  // descriptive text must agree with where price is\n  var pf = st.pressure;\n  if (pf && pf.support && pf.support.verdict === 'נשברת' && p > pf.support.price + 0.05 * st.atr)\n    add('TEXT_CONTRADICTS_PRICE', 'block', 'נטען שהתמיכה נשברת בזמן שהמחיר מעליה');\n  if (pf && pf.resistance && pf.resistance.verdict === 'נפרצת' && p < pf.resistance.price - 0.05 * st.atr)\n    add('TEXT_CONTRADICTS_PRICE', 'block', 'נטען שההתנגדות נפרצת בזמן שהמחיר מתחתיה');\n\n  // the headline and the technical block must describe the same situation\n  var passive = ['DO_NOT_BUY', 'WATCH_ONLY', 'SESSION_CLOSED', 'SETUP_CANCELLED'].indexOf(st.action) >= 0;\n  if (passive && lv.entry != null) add('ACTION_VS_ENTRY', 'block', 'ההוראה היא להמתין בזמן שמוצגת רמת כניסה');\n  if (passive && st.whatNow && st.whatNow.up.some(function (s) { return /אפשר להיכנס|כניסה חלקית ב-/.test(s); }))\n    add('ACTION_VS_INSTRUCTION', 'block', 'ההוראה היא להמתין בזמן שמוצגת הוראת כניסה');\n  if (st.noEdge && lv.target1 != null) add('NO_EDGE_TARGETS', 'block', 'אין יתרון אבל מוצגים יעדים');\n  // exactly one entry scenario may be narrated at a time\n  if (st.whatNow) {\n    var entryLines = st.whatNow.up.filter(function (s) { return /אפשר להיכנס|כניסה חלקית ב-/.test(s); });\n    var mentionsBoth = entryLines.some(function (s) { return /עובר את/.test(s); })\n      && entryLines.some(function (s) { return /יורד לאזור/.test(s); });\n    if (mentionsBoth) add('TWO_SCENARIOS', 'block', 'מוצגים שני תרחישי כניסה במקביל');\n  }\n  // a level description must match its current state\n  if (st.levelStates && st.levelStates.support && st.pressure && st.pressure.support) {\n    if (st.pressure.support.verdict === 'נשברה' && st.levelStates.support.state !== 'broken')\n      add('LEVEL_TEXT_STALE', 'block', 'תיאור התמיכה אינו תואם את מצבה הנוכחי');\n  }\n\n  // status vs plan\n  if (st.status === 'ACTIVE' && plan && plan.state === 'FAILED') add('STATUS_VS_PLAN', 'block', 'הסטטוס פעיל בזמן שה-setup בוטל');\n  if (st.status === 'READY' && st.action === 'DO_NOT_BUY') add('ACTION_VS_STATUS', 'block', 'סטטוס מוכן מול הוראה לא לקנות');\n\n  // pressure conclusion vs its own numbers\n  if (pf && pf.agreement === 'תומך' && pf.direction === 'deteriorating')\n    add('PRESSURE_CONCLUSION', 'block', 'נטען שהלחץ תומך בזמן שהוא נחלש');\n  if (pf && pf.agreement === 'תומך' && pf.buyPct < 50)\n    add('PRESSURE_CONCLUSION', 'block', 'נטען שהקונים תומכים בזמן שהם במיעוט');\n\n  // probability vs confidence\n  var pr = st.probability;\n  // `pr.up` is null when the probability was suppressed, and `null <= 20` is\n  // true in JavaScript — which made a *withheld* probability look like an\n  // extreme one and invalidated the whole state.\n  if (pr && pr.up != null && pr.confidence < 50 && (pr.up >= 80 || pr.up <= 20))\n    add('PROB_CONFIDENCE', 'block', 'הסתברות קיצונית עם ביטחון נמוך');\n  if (pr && pr.upper != null && lv.probUpper != null && Math.abs(pr.upper - lv.probUpper) > 1e-9)\n    add('PROB_STALE_LEVELS', 'block', 'ההסתברות חושבה מול רמות אחרות מאלה שמוצגות');\n\n  // ---- staleness. A state built from bars that stopped arriving is not a\n  // valid live state, whatever else it says.\n  if (st.stale && st.action !== 'DATA_STALE' && st.action !== 'SESSION_CLOSED')\n    add('STALE_BUT_ACTIONABLE', 'block', 'הנתונים ישנים אך המסך מציג הוראה חיה');\n  if (st.stale && lv && (lv.entry != null || lv.target1 != null))\n    add('STALE_WITH_LEVELS', 'block', 'הנתונים ישנים אך מוצגת רמת כניסה');\n  if (st.stale && pr && pr.up != null)\n    add('STALE_WITH_PROBABILITY', 'block', 'הנתונים ישנים אך מוצגת הסתברות כאילו היא נוכחית');\n\n  // ---- a setup whose cancellation level the price has already crossed\n  var cancelLv = (lv && lv.tacticalStop != null) ? lv.tacticalStop\n    : (st.plan && st.plan.invalidation != null ? st.plan.invalidation : null);\n  var LIVE_ACTIONS = ['START_WATCHING', 'WAIT_FOR_CONFIRMATION', 'ENTRY_AVAILABLE', 'HOLD', 'TAKE_PARTIAL'];\n  if (!st.sessionEnded && cancelLv != null && st.price != null && st.price < cancelLv\n      && LIVE_ACTIONS.indexOf(st.action) >= 0)\n    add('CANCELLED_BUT_ACTIVE', 'block', 'המחיר כבר מתחת לרמת הביטול אך התרחיש מוצג כפעיל');\n\n  // ---- the narrative targets must be the levels block's targets\n  if (st.whatNow && Array.isArray(st.whatNow.up) && lv && lv.target1 != null && lv.target2 != null) {\n    var txt = st.whatNow.up.join(' ');\n    var m1 = txt.match(/\\u05d9\\u05e2\\u05d3 \\u05e8\\u05d0\\u05e9\\u05d5\\u05df ([\\d.]+)/);\n    var m2 = txt.match(/\\u05d9\\u05e2\\u05d3 \\u05e9\\u05e0\\u05d9 ([\\d.]+)/);\n    var near = function (a, b2) { return Math.abs(Number(a) - Number(b2)) < 0.005; };\n    if (m1 && !near(m1[1], lv.target1)) add('TARGET1_TEXT_MISMATCH', 'block', 'יעד ראשון בטקסט שונה מהיעד ברמות');\n    if (m2 && !near(m2[1], lv.target2)) add('TARGET2_TEXT_MISMATCH', 'block', 'יעד שני בטקסט שונה מהיעד ברמות');\n  }\n\n  return { valid: !v.some(function (x) { return x.severity === 'block'; }), violations: v };\n}\n\n\n// ---------------------------------------------------------------- analysis pack\n//\n// Everything another trader (or another model) needs to re-do the analysis from\n// scratch: the raw data AND the decision state, from ONE snapshot. Any field the\n// system does not have says NOT AVAILABLE rather than being quietly dropped.\nvar NA = 'NOT AVAILABLE';\nfunction nz(v, d) { return v == null || v === '' || (typeof v === 'number' && !isFinite(v)) ? NA : (d != null && typeof v === 'number' ? v.toFixed(d) : String(v)); }\n\n// Buckets belong to the exchange clock: 09:30, 09:35, ... 11:00, 11:05. The\n// grouping was already correct, but the LABEL came from the first bar that\n// happened to fall inside — so a window starting at 11:02 produced a bar called\n// \"11:02\", which is not a five-minute timeframe at all. A bucket missing some\n// of its minutes is marked rather than silently presented as whole.\nfunction aggregate(rows, minutes) {\n  var out = [], bucket = null;\n  var label = function (m) {\n    var h = Math.floor(m / 60), mm = m % 60;\n    return String(h).padStart(2, '0') + ':' + String(mm).padStart(2, '0');\n  };\n  (rows || []).forEach(function (r) {\n    var m = parseInt(r.time.slice(0, 2), 10) * 60 + parseInt(r.time.slice(3), 10);\n    var key = Math.floor(m / minutes);\n    if (!bucket || bucket.key !== key) {\n      if (bucket) out.push(bucket);\n      bucket = { key: key, time: label(key * minutes), firstBar: r.time, minutes: 1,\n        open: r.open, high: r.high, low: r.low, close: r.close, volume: r.volume, date: r.date };\n    } else {\n      bucket.high = Math.max(bucket.high, r.high); bucket.low = Math.min(bucket.low, r.low);\n      bucket.close = r.close; bucket.volume += r.volume; bucket.minutes++;\n    }\n  });\n  if (bucket) out.push(bucket);\n  out.forEach(function (b) { b.partial = b.minutes < minutes; });\n  return out;\n}\nfunction candleLine(sym, r, avgVol) {\n  var range = r.high - r.low, pct = function (x) { return range > 0 ? Math.round(x / range * 1000) / 10 : 0; };\n  var dir = r.close > r.open ? 'BULL' : r.close < r.open ? 'BEAR' : 'FLAT';\n  return [sym, r.date || '', r.time, r.open, r.high, r.low, r.close, r.volume, dir,\n    pct(Math.abs(r.close - r.open)), pct(r.high - Math.max(r.open, r.close)), pct(Math.min(r.open, r.close) - r.low),\n    Math.round(range * 10000) / 10000, avgVol > 0 ? Math.round(r.volume / avgVol * 100) / 100 : 0].join(',');\n}\nvar CANDLE_HEADER = 'symbol,date,time,open,high,low,close,volume,dir,body_pct,upper_wick_pct,lower_wick_pct,range,vol_x';\n\nfunction analysisPack(ctx) {\n  var c = ctx || {}, st = c.snap;\n  var L = [], add = function (s) { L.push(s == null ? '' : s); };\n  var head = function (t) { add(''); add('===== ' + t + ' ====='); };\n  if (!st) return 'NO SNAPSHOT — ' + NA;\n  var A = c.analysis, b = st.price != null ? st : null;\n  var bar = A && A.state ? A.state.bar : null;\n  var lv = st.levels || {}, P = st.plan, W = st.whatNow, pr = st.probability, pf = st.pressure;\n  var rows = c.rows || [], atr = st.atr;\n\n  add('FULL ANALYSIS PACK · ' + st.symbol);\n  add('Generated from one atomic snapshot: state_version ' + st.state_version + ', calculated_at ' + new Date(st.calculated_at).toISOString());\n\n  head('1. TIME & DATA STATE');\n  add('Symbol: ' + st.symbol);\n  add('Date (ET): ' + nz(st.data_through && st.data_through.date || c.date));\n  add('Last closed candle (ET): ' + nz(st.data_through && st.data_through.time));\n  add('Current price (last close): ' + nz(st.price, 2));\n  add('Quote timestamp: ' + nz(c.quoteTime) + '   (the feed is 1-minute bars; there is no separate quote stream)');\n  add('Data age / stale seconds: ' + nz(c.staleSeconds));\n  add('Freshness: ' + nz(st.freshness));\n  add('Session: ' + nz(c.session) + '   (PRE / REGULAR / AFTER / CLOSED)');\n  add('Data source: Yahoo Finance 1-minute bars via Cloudflare Worker');\n\n  head('2. MODEL DECISION');\n  add('Status: ' + nz(st.status));\n  add('Primary action: ' + nz(st.actionText) + ' [' + nz(st.action) + ']');\n  add('Setup Score: ' + nz(st.score) + '/10');\n  add('Active setup type: ' + (st.stale ? 'none (data stale)'\n    : (st.scenario ? st.scenario.kind + ' — ' + st.scenario.label : NA)));\n  add('Setup state: ' + (st.stale ? 'FROZEN (last known: ' + (P ? P.state : NA) + ') — reference only'\n    : (P ? P.state : NA)));\n  add('Reason: ' + (W ? nz(W.next) : NA));\n  add('Model state valid: ' + (st.valid ? 'yes' : 'NO — ' + (st.violations || []).map(function (v) { return v.code; }).join(', ')));\n  if (st.noEdge) add('No-edge reasons: ' + ((st.edge && st.edge.reasons) || []).join(' · '));\n\n  head('3. PRICE LEVELS AND THEIR ROLES');\n  // While frozen, every level is a record of what WAS, not a price to act on.\n  // The labels say so, because a reader skimming the pack reads the label.\n  var FROZEN = !!st.stale;\n  var role = function (name, val, note) {\n    var label = FROZEN ? 'LAST KNOWN ' + name : name;\n    var n = FROZEN ? (note ? note + ' — REFERENCE ONLY' : 'REFERENCE ONLY') : note;\n    add(label + ': ' + (val == null ? NA : val.toFixed(2) + (atr ? '  (' + fmtR((val - st.price) / atr) + ')' : '')) + (n ? '  — ' + n : ''));\n  };\n  if (FROZEN) add('NOTE: data is stale. Everything in this section is the last recorded state, not a current instruction.');\n  role('Watch level', lv.watch, FROZEN ? 'the price that was being waited for' : 'the price being waited for');\n  role('Entry zone low', P && P.zone ? P.zone[0] : null);\n  role('Entry zone high', P && P.zone ? P.zone[1] : null);\n  role('Planned entry', lv.entry);\n  role('Breakout / reclaim trigger', P && P.kind === 'breakout' ? lv.watch : (st.plan && st.plan.addAbove != null ? st.plan.addAbove : null), FROZEN ? 'confirmed an add while the setup was live'\n      : (lv.entry == null) ? 'a level to watch — there is no position to add to'\n      : 'confirms an add');\n  role('Tactical support', st.tactical && st.tactical.support ? st.tactical.support.price : null, st.tactical && st.tactical.support ? st.tactical.support.why : '');\n  role('Tactical resistance', st.tactical && st.tactical.resistance ? st.tactical.resistance.price : null, st.tactical && st.tactical.resistance ? st.tactical.resistance.why : '');\n  role('Entry cancellation (tactical)', lv.tacticalInvalidation);\n  role('Setup invalidation / structural stop', lv.hardStop);\n  role('Stop', lv.hardStop, 'same as structural invalidation; no separate money stop is computed');\n  role('Target 1', lv.target1);\n  role('Target 2', lv.target2);\n  add('Target 3: ' + NA);\n  role('Reclaim level (broken and being retested)', st.plan && st.plan.state !== 'FAILED' && A && A.state ? A.state.reclaim : null);\n  add('');\n  add('Level states (approaching / testing / held / broken / reclaimed / rejected / lost / far):');\n  ['support', 'resistance'].forEach(function (k) {\n    var s = st.levelStates && st.levelStates[k];\n    add('  ' + k + ': ' + (s ? s.price.toFixed(2) + ' — ' + s.state + ' (' + LEVEL_TEXT[s.state] + '), ' + fmtR(s.distanceR) + ', touches ' + s.touches + ', recoveries ' + s.recoveries + ', rejections ' + s.rejections : NA));\n  });\n\n  head('4. WHAT-IF' + (FROZEN ? ' — FROZEN, REFERENCE ONLY' : ''));\n  add('NOW: ' + (W ? W.actionText + ' — ' + W.next : NA));\n  add('IF UP:');\n  ((W && W.up) || []).forEach(function (s) { add('  - ' + s); });\n  if (!W || !W.up.length) add('  ' + NA);\n  add('IF DOWN:');\n  ((W && W.down) || []).forEach(function (s) { add('  - ' + s); });\n  if (!W || !W.down.length) add('  ' + NA);\n  // The same rule as FROZEN, for every state where no entry is planned. I fixed\n  // this for stale data and left the identical contradiction standing when the\n  // session is incomplete: \"Planned entry: NOT AVAILABLE\" above \"Confirms\n  // entry: partial entry on the pullback\" tells you not to trade and how to\n  // trade in the same breath.\n  var NO_ENTRY = !FROZEN && (lv.entry == null);\n  var noEntryWhy = st.sessionIncomplete ? 'the session is incomplete'\n    : st.tooOldToEnter ? 'the data is too old to act on'\n    : st.sessionEnded ? 'the session is closed'\n    : 'no entry is planned';\n  if (NO_ENTRY) {\n    add('Confirms entry: ' + NA + ' — ' + noEntryWhy);\n    add('Cancels entry: ' + NA + ' — there is no entry to cancel');\n    add('Cancels the whole setup: ' + NA + ' — ' + noEntryWhy);\n    add('After entry: ' + NA + ' — ' + noEntryWhy);\n    add('At target 1: ' + NA + ' — ' + noEntryWhy);\n    add('Confirmation strengthens above: ' + NA + ' — ' + noEntryWhy);\n    add('Exit when: ' + NA + ' — ' + noEntryWhy);\n    add('The levels above are reference only: with no entry there is nothing to add to, take off, or exit.');\n  } else if (FROZEN) {\n    // None of these may be phrased as something to do. They are the last\n    // recorded rules of a setup that is no longer live.\n    add('Confirms entry: ' + NA + ' — setup frozen (data stale)');\n    add('After entry: ' + NA + ' — setup frozen (data stale)');\n    add('At target 1: ' + NA + ' — setup frozen (data stale)');\n    add('Confirmation strengthens above: ' + NA + ' — setup frozen (data stale)');\n    add('Exit when: ' + NA + ' — setup frozen (data stale)');\n    add('LAST KNOWN entry cancellation: ' + (lv.tacticalInvalidation != null ? lv.tacticalInvalidation.toFixed(2) + '  — REFERENCE ONLY' : NA));\n    add('LAST KNOWN setup invalidation: ' + (lv.hardStop != null ? lv.hardStop.toFixed(2) + '  — REFERENCE ONLY' : NA));\n    add('These are the rules the setup had when the data stopped. They are not active.');\n  } else {\n  add('Confirms entry: ' + (P ? nz(P.ifConfirmed) : NA));\n  add('Cancels entry: ' + (lv.tacticalInvalidation != null ? 'close below ' + lv.tacticalInvalidation.toFixed(2) : NA));\n  add('Cancels the whole setup: ' + (lv.hardStop != null ? 'close below ' + lv.hardStop.toFixed(2) : NA));\n  add('After entry: ' + (P ? nz(P.nextStep) : NA));\n  add('At target 1: ' + (lv.target1 != null ? 'take partial at ' + lv.target1.toFixed(2) : NA));\n  add('Confirmation strengthens above: ' + (P && P.addAbove != null ? P.addAbove.toFixed(2) + ' (two closes); position state unknown to the system' : NA));\n  add('Exit when: ' + (lv.hardStop != null ? 'close below ' + lv.hardStop.toFixed(2) + ', or at target' : NA));\n  }\n\n  head('4b. SESSION COVERAGE');\n  var cv = st.coverage || {};\n  add('First regular bar: ' + (cv.firstBar || NA));\n  add('Last regular bar: ' + (cv.lastBar || NA));\n  add('Expected minutes: ' + (cv.expectedMinutes != null ? cv.expectedMinutes : NA));\n  add('Received unique minutes: ' + (cv.receivedMinutes != null ? cv.receivedMinutes : NA));\n  add('Coverage: ' + (cv.coveragePct != null ? cv.coveragePct + '%' : NA));\n  add('Starts at the open: ' + (cv.fromOpen ? 'yes' : 'NO — missing ' + (cv.missingFromOpen || '?') + ' minutes from 09:30'));\n  add('Holes inside the loaded window: ' + (cv.missingInside\n    ? cv.missingInside + ' minutes missing between ' + cv.firstBar + ' and ' + cv.lastBar\n      + ' — the series is not continuous, so structure, momentum and the buyer/seller split describe a broken sequence'\n    : 'none'));\n  add('Total missing minutes: ' + (cv.missingTotal != null ? cv.missingTotal : NA));\n  add('Session values usable: ' + (cv.complete ? 'yes' : 'NO — open, gap, VWAP, EMA, relative volume and any entry are derived from a partial window and are not the session\\'s'));\n\n  head('5. PROBABILITY');\n  // A suppressed probability must still say WHAT was asked and WHY there is no\n  // number. \"NOT AVAILABLE\" on its own leaves the reader unable to tell a stale\n  // feed from a question that was never answerable.\n  if (!pr) {\n    var praw = (st && st.probabilityRaw) || null;\n    add('UP: ' + NA + '   DOWN: ' + NA);\n    add('Event measured: ' + (praw && praw.upper != null\n      ? 'price reaches ' + praw.upper.toFixed(2) + ' before ' + praw.lower.toFixed(2) : NA));\n    add('Horizon: ' + (praw ? praw.horizonMin + ' minutes' : NA));\n    add('Confidence: ' + NA + ' — no probability is being claimed');\n    add('Source: ' + NA);\n    // Only the reason for the SUPPRESSION, never the half-computed scoring\n    // inputs — those describe a number we have decided not to trust.\n    var suppressWhy = (praw && (praw.notDirectional || praw.meaningless))\n      ? praw.why.join(' | ')\n      : (st && st.stale) ? 'the data is stale'\n      : (st && st.coverage && !st.coverage.complete) ? 'the session is incomplete — the inputs are not the session\\'s'\n      : 'not answerable in this state';\n    add('Why: ' + suppressWhy);\n    add('Scoring inputs: ' + NA + ' — withheld with the probability they produced');\n  } else {\n    add('UP: ' + pr.up + '%   DOWN: ' + pr.down + '%');\n    add('Event measured: price reaches ' + pr.upper.toFixed(2) + ' before ' + pr.lower.toFixed(2));\n    add('Horizon: ' + pr.horizonMin + ' minutes');\n    add('Confidence: ' + pr.confidence + '/100' + (pr.lowConfidence ? '  (LOW — displayed % is pulled toward 50)' : ''));\n    add('Source: ' + (pr.source === 'empirical' ? 'empirical, ' + pr.n + ' comparable cases in this symbol\\'s own loaded history'\n      : 'MODEL ESTIMATE — not a measured frequency' + (pr.raw != null ? ' (raw ' + pr.raw + '%, shrunk for confidence)' : '')));\n    add('Why: ' + pr.why.join(' | '));\n    if (st.coverage && !st.coverage.complete)\n      add('NOTE: these inputs come from a partial window and the probability above should not be relied on.');\n  }\n\n  head('6. BUYERS / SELLERS (inferred from candles)');\n  if (!pf) add(NA); else {\n    add('Buyers: ' + pf.buyPct + '%   Sellers: ' + pf.sellPct + '%   (window ' + pf.window + ' bars)');\n    add('Buyers: ' + pf.buyersTrend + '   Sellers: ' + pf.sellersTrend + '   Direction: ' + pf.direction);\n    add('Conclusion: ' + pf.conclusion);\n    add('Agreement with the setup: ' + pf.agreement);\n    add('METHOD: close location inside each bar weighted by that bar\\'s volume. NOT order flow. hasOrderBook=' + pf.hasOrderBook + ' hasTape=' + pf.hasTape);\n  }\n\n  head('7. ORDER BOOK / LEVEL 2');\n  var bk = c.book;\n  if (!bk || bk.error || !bk.summary || bk.summary.bid_pct == null) {\n    add(NA + ' — the Cboe book did not return data for this symbol at this time');\n  } else {\n    var s = bk.summary;\n    add('PARTIAL BOOK — CBOE ONLY');\n    add('Venues returning data: ' + (s.venues_ok.join(', ') || NA) + '   Not answering: ' + (s.venues_failed.join(', ') || 'none'));\n    add('Missing from this book: ARCA, NYSE, Nasdaq and all other venues. This is NOT the NBBO and NOT consolidated depth.');\n    add('Book timestamp: ' + nz(bk.fetched_at));\n    add('Best bid: ' + nz(s.best_bid, 2) + '   Best ask: ' + nz(s.best_ask, 2) + '   Spread: ' + nz(s.spread, 4));\n    add('Total bid depth: ' + s.bid_shares + ' shares   Total ask depth: ' + s.ask_shares + ' shares');\n    add('Imbalance: ' + s.bid_pct + '% bid / ' + s.ask_pct + '% ask  (net ' + s.imbalance + ')');\n    add('Levels available: ' + s.depth_levels + ' per side per venue (' + NA + ' beyond 5 — the public viewer publishes 5)');\n    add('');\n    add('venue,side,level,price,shares');\n    (bk.venues || []).forEach(function (v) {\n      if (v.error) { add(v.venue + ',ERROR,,,' + v.error); return; }\n      (v.asks || []).forEach(function (l, i) { add(v.venue + ',ask,' + (i + 1) + ',' + l.price + ',' + l.shares); });\n      (v.bids || []).forEach(function (l, i) { add(v.venue + ',bid,' + (i + 1) + ',' + l.price + ',' + l.shares); });\n    });\n    add('');\n    add('Depth change over the last 30-60s: ' + (c.bookPrev ? 'bid ' + (s.bid_shares - c.bookPrev.bid_shares) + ', ask ' + (s.ask_shares - c.bookPrev.ask_shares) : NA + ' (no earlier snapshot held)'));\n  }\n\n  head('8. TAPE / TIME & SALES');\n  add('TAPE NOT AVAILABLE — this feed carries 1-minute bars only. No per-trade prints, no at-bid/at-ask flags, no aggressive volume split.');\n\n  head('9. STRUCTURE & MOMENTUM');\n  // Structure is a claim about a SEQUENCE. With minutes missing in the middle\n  // the sequence is not the market's, so the reading is qualified rather than\n  // stated — a swing \"confirmed\" across a hole may never have happened.\n  var BROKEN = (st.coverage && st.coverage.contiguous === false)\n    ? '  [SERIES NOT CONTINUOUS — ' + st.coverage.missingInside + ' minutes missing inside the window; this reading is not reliable]'\n    : '';\n  add('Main structure: ' + nz(st.structure) + (A && A.state ? '  (' + A.state.reason + ')' : '') + BROKEN);\n  add('Announced trend: ' + (A && A.state ? nz(A.state.announced) : NA) + BROKEN);\n  add('Short momentum: ' + nz(st.momentum) + (st.row && st.row.mom ? '   move over last 5 bars: ' + st.row.mom.net.toFixed(2) + 'R' : ''));\n  add('Recent swings (K=3 confirmed):');\n  var sw = A ? A.swings.filter(function (w) { return w.confirmedAt <= A.bars.length - 1; }).slice(-10) : [];\n  if (!sw.length) add('  ' + NA);\n  sw.forEach(function (w) { add('  ' + w.label + '  ' + w.price.toFixed(2) + '  @' + w.time + (w.outside ? '  (outside bar — not counted as structure)' : '')); });\n\n  head('10. INDICATORS');\n  // These are all session-scoped. Computed over a partial window they are not\n  // what their names say, so every line carries the warning rather than the\n  // reader being expected to remember a note from six sections earlier.\n  var PART = (st.coverage && !st.coverage.complete) ? '  [PARTIAL WINDOW — NOT A SESSION INDICATOR]' : '';\n  if (PART) add('NOTE: the session is incomplete — every figure below describes the loaded window only.');\n  if (!bar) add(NA); else {\n    add('VWAP: ' + bar.vwap.toFixed(2) + PART + '   price is ' + (bar.aboveVwap ? 'ABOVE' : 'BELOW') + '   distance: ' + (st.price - bar.vwap).toFixed(2) + ' (' + fmtR((st.price - bar.vwap) / atr) + ')');\n    add('EMA9: ' + bar.ema9.toFixed(2) + PART + '   EMA20: ' + bar.ema20.toFixed(2) + '   state: ' + bar.align +\n      '   separation: ' + Math.abs(bar.ema9 - bar.ema20).toFixed(3) + ' (minimum for a call: ' + (0.15 * atr).toFixed(3) + ')');\n    add('Average candle range (20 bars): ' + atr.toFixed(4) + '   = 1R throughout this pack');\n    add('Relative volume (vs session average): ' + bar.volx.toFixed(2) + 'x' + PART + (bar.auction ? '  [CLOSING AUCTION BAR — excluded from scoring]' : ''));\n    add('Time-of-day normalised volume: ' + (c.baseline ? (volxTod(bar, c.baseline) != null ? volxTod(bar, c.baseline).toFixed(2) + 'x vs the same minute on ' + c.baseline.days + ' prior days' : NA) : NA + ' (no prior days loaded)'));\n    var dl = function (name, v) { return v == null ? name + ': ' + NA : name + ': ' + (st.price - v).toFixed(2) + ' (' + fmtR((st.price - v) / atr) + ')'; };\n    add('Distance from levels — ' + dl('support', st.tactical && st.tactical.support ? st.tactical.support.price : null) +\n      '   ' + dl('resistance', st.tactical && st.tactical.resistance ? st.tactical.resistance.price : null));\n  }\n\n  head('11. MARKET CONTEXT');\n  var mk = c.marketCtx;\n  if (!mk || !mk.parts || !mk.parts.length) add(NA); else {\n    add('Regime: ' + mk.label);\n    mk.parts.forEach(function (p) {\n      add('  ' + p.symbol + ': ' + p.close.toFixed(2) + '   VWAP ' + p.vwap.toFixed(2) + ' (' + (p.aboveVwap ? 'above' : 'below') + ')   EMA ' + p.align + '   trend ' + p.trend);\n    });\n    add('Sector ETF: ' + (c.sectorEtf || NA));\n    add('Does the market support this symbol: ' + (mk.label === 'Bullish' ? 'supports a long' : mk.label === 'Bearish' ? 'contradicts a long' : 'neutral'));\n  }\n\n  head('12. MULTI-DAY CONTEXT');\n  if (st.coverage && !st.coverage.complete)\n    add(\"NOTE: today's row is built from a PARTIAL session (\" + (st.coverage.coveragePct || 0)\n      + '% of the minutes since the open) — it is not a daily candle.');\n  var d = c.daily;\n  if (!d) add(NA + ' — no prior sessions loaded'); else {\n    add('Sessions loaded: ' + d.days + ' (prior: ' + d.priorDays + ')');\n    // The daily trend is computed over COMPLETED sessions only — today's row is\n    // excluded by dailyContext. Say so, because a reader looking at a partial\n    // row whose low is already below yesterday's will otherwise read the trend\n    // as a claim about it.\n    add('Daily trend: ' + d.trend + ' (' + d.trendWhy + ')'\n      + '  [computed over the ' + (d.priorDays || 0) + ' COMPLETED sessions before today'\n      + ((st.coverage && !st.coverage.complete) ? \"; today's partial row is excluded\" : '') + ']');\n    add('Previous day: high ' + nz(d.prevHigh, 2) + '  low ' + nz(d.prevLow, 2) + '  close ' + nz(d.prevClose, 2));\n    add('Gap today vs previous close: ' + ((st.coverage && !st.coverage.complete)\n      ? NA + ' — the session open is missing, so there is nothing to measure the gap from'\n      : nz(d.gap, 2)));\n    add('Average daily range: ' + nz(d.atrDaily, 2));\n    add('');\n    add('date,open,high,low,close,volume');\n    (c.dailyBars || []).forEach(function (x) { add([x.date, x.open, x.high, x.low, x.close, x.volume].join(',')); });\n    if (!(c.dailyBars || []).length) add(NA);\n    var hi = (c.dailyBars || []).reduce(function (m, x) { return Math.max(m, x.high); }, -Infinity);\n    var lo = (c.dailyBars || []).reduce(function (m, x) { return Math.min(m, x.low); }, Infinity);\n    add('');\n    add('Multi-day high/low: ' + (isFinite(hi) ? hi.toFixed(2) + ' / ' + lo.toFixed(2) : NA));\n  }\n\n  head('13. EVENTS');\n  var evs = A ? A.events.slice(-40).reverse() : [];\n  if (!evs.length) add(NA);\n  evs.forEach(function (s2) {\n    var parts = s2.events.map(function (e) {\n      return e.type + (e.level != null ? ' @' + e.level.toFixed(2) : '') + (e.confirmed === true ? ' [confirmed]' : e.confirmed === false ? ' [unconfirmed]' : '') + (e.side ? ' ' + e.side : '');\n    });\n    if (parts.length) add(s2.time + '  ' + s2.close.toFixed(2) + '  ' + parts.join(' · ') + '  (score ' + s2.score + ')');\n  });\n\n  head('14. POSITION');\n  var pos = c.position;\n  if (!pos) {\n    add('Position: NOT AVAILABLE — this application does not track positions, orders or account state.');\n    add('Quantity / average entry / P&L / current stop / invested / remaining / partials taken: ' + NA);\n  } else {\n    add('Position: ' + nz(pos.state));\n    add('Quantity: ' + nz(pos.qty) + '   Average entry: ' + nz(pos.avg, 2));\n    add('P&L: ' + nz(pos.pnl, 2) + ' (' + nz(pos.pnlPct, 2) + '%)   Current stop: ' + nz(pos.stop, 2));\n    add('Invested: ' + nz(pos.invested) + '   Remaining planned: ' + nz(pos.remaining) + '   Partials taken: ' + nz(pos.partials));\n  }\n\n  head('15. CANDLES — LAST 50 ONE-MINUTE (closed only)');\n  var avgVol = rows.length ? rows.reduce(function (a2, x) { return a2 + x.volume; }, 0) / rows.length : 0;\n  add(CANDLE_HEADER);\n  rows.slice(-50).forEach(function (r) { add(candleLine(st.symbol, r, avgVol)); });\n  if (!rows.length) add(NA);\n\n  head('16. CANDLES — LAST 20 FIVE-MINUTE (aggregated from 1-minute)');\n  var f5 = aggregate(rows, 5).slice(-20);\n  add(CANDLE_HEADER);\n  var avg5 = f5.length ? f5.reduce(function (a2, x) { return a2 + x.volume; }, 0) / f5.length : 0;\n  f5.forEach(function (r) { add(candleLine(st.symbol, r, avg5)); });\n  if (!f5.length) add(NA);\n\n  head('17. CANDLES — LAST 10 DAILY');\n  add('symbol,date,open,high,low,close,volume');\n  (c.dailyBars || []).slice(-10).forEach(function (x) { add([st.symbol, x.date, x.open, x.high, x.low, x.close, x.volume].join(',')); });\n  if (!(c.dailyBars || []).length) add(NA + ' — no prior sessions loaded');\n\n  head('END OF PACK');\n  add('Fields marked ' + NA + ' are genuinely absent from this data feed, not omitted.');\n  return L.join('\\n');\n}\n\n\n// Candidate scoring — \"is this stock worth my attention today?\"\n//\n// THREE SEPARATE QUESTIONS, and this file must never blur them:\n//\n//   Candidate Score  is this worth WATCHING today?      0-100, from history\n//   Live Interest    is it still worth live attention?  0-100, from the session\n//   Setup Score      is a trade developing RIGHT NOW?   0-10, engine.cjs\n//\n// A stock can be a strong candidate with no setup at all: a quiet coil under a\n// multi-day high is exactly that. Equally, a live symbol can hold a setup and\n// have stopped being interesting, because the range died.\n//\n// Everything here is computed from CLOSED sessions. Nothing in this file may\n// produce a live instruction, and yesterday's execution state (ACTIVE, FAILED,\n// WAITING, DO_NOT_CHASE) is deliberately not carried in: structure and levels\n// survive a session boundary, decisions do not.\n\n\n// ---------------------------------------------------------------- daily bars\n// One bar per session out of per-minute rows.\n// A US regular session is 390 one-minute bars. A set materially shorter than\n// that is a session still being collected, or one fetched mid-day — and\n// aggregating it yields a daily candle that is wrong with no outward sign.\nvar SESSION_BARS = 390, MIN_COVERAGE = 0.85;\n// Counting bars alone is the wrong test. A thinly traded name has no print in\n// some minutes and the feed omits them, so a COMPLETE session can arrive with\n// 343 bars — PGR, BLK and HON were all refused that way. What actually\n// distinguishes a complete session is that it SPANS the session: it starts at\n// the open and runs to the close. A day truncated at 13:20 fails that however\n// many bars it has.\nfunction sessionSpans(rows) {\n  if (!rows || !rows.length) return false;\n  var times = rows.map(function (r) { return r.time; }).sort();\n  return times[0] <= '09:35' && times[times.length - 1] >= '15:55';\n}\nfunction toDaily(daysRows, opts) {\n  var o2 = opts || {};\n  var auth = {};\n  (o2.authoritative || []).forEach(function (b) { auth[b.date] = b; });\n  var out = (daysRows || []).filter(function (r) { return r && r.length; }).map(function (rows) {\n    var h = -Infinity, l = Infinity, v = 0;\n    rows.forEach(function (r) { h = Math.max(h, r.high); l = Math.min(l, r.low); v += r.volume; });\n    var o = rows[0].open, c = rows[rows.length - 1].close;\n    var regular = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n    var cov = regular.length / SESSION_BARS;\n    var spans = sessionSpans(rows);\n    var a = auth[rows[0].date];\n    if (a) return { date: a.date, open: a.open, high: a.high, low: a.low, close: a.close,\n      volume: a.volume, range: a.high - a.low,\n      closePos: a.high > a.low ? (a.close - a.low) / (a.high - a.low) : 0.5,\n      bars: rows.length, coverage: Math.round(cov * 1000) / 1000, complete: true, source: 'authoritative' };\n    return { date: rows[0].date, open: o, high: h, low: l, close: c, volume: v,\n      range: h - l, closePos: h > l ? (c - l) / (h - l) : 0.5, bars: rows.length,\n      coverage: Math.round(cov * 1000) / 1000, spans: spans,\n      complete: spans && cov >= MIN_COVERAGE, source: 'aggregated' };\n  });\n  // An incomplete session is dropped rather than scored: every component here\n  // reads high, low, close or volume, and all four are wrong on a partial day.\n  return out.filter(function (b) { return b.complete; });\n}\n\n// ---------------------------------------------------------------- components\n// Each returns { points, max, why } so the score can always be explained. A\n// number with no reasons attached is not usable for a decision.\n\n// Multi-day direction: higher closes AND higher lows, or the mirror.\nfunction trendComponent(daily) {\n  var out = { points: 0, max: 25, why: null, label: 'RANGE' };\n  if (daily.length < 4) { out.why = null; return out; }\n  var w = daily.slice(-4);\n  var upC = 0, upL = 0, dnC = 0, dnL = 0;\n  for (var i = 1; i < w.length; i++) {\n    if (w[i].close > w[i - 1].close) upC++; else if (w[i].close < w[i - 1].close) dnC++;\n    if (w[i].low > w[i - 1].low) upL++; else if (w[i].low < w[i - 1].low) dnL++;\n  }\n  if (upC >= 2 && upL >= 2) { out.points = 12 + Math.min(13, (upC + upL) * 3); out.label = 'UP';\n    out.why = 'מגמה רב-יומית עולה — ' + upC + ' סגירות ו-' + upL + ' שפלים עולים'; }\n  else if (dnC >= 2 && dnL >= 2) { out.points = 10 + Math.min(10, (dnC + dnL) * 2); out.label = 'DOWN';\n    out.why = 'מגמה רב-יומית יורדת — ' + dnC + ' סגירות ו-' + dnL + ' שיאים יורדים'; }\n  else { out.points = 4; out.label = 'RANGE'; out.why = 'ללא כיוון רב-יומי ברור'; }\n  return out;\n}\n\n// Enough daily movement to be worth watching at all. A name whose whole day is\n// 0.4% cannot pay for the attention, however clean its structure.\nfunction rangeComponent(daily) {\n  var out = { points: 0, max: 15, why: null, atrPct: null };\n  if (!daily.length) return out;\n  var w = daily.slice(-5);\n  var avg = w.reduce(function (s, b) { return s + b.range; }, 0) / w.length;\n  var px = w[w.length - 1].close || 1;\n  var pct = avg / px * 100;\n  out.atrPct = Math.round(pct * 100) / 100;\n  // This is the average over five sessions, not today's range. Calling it\n  // \"daily range\" made it look wrong against a single day's high-low.\n  var last = w[w.length - 1];\n  out.lastRangePct = px > 0 ? Math.round(last.range / px * 10000) / 100 : null;\n  out.window = w.length;\n  if (pct >= 3) { out.points = 15; out.why = 'טווח ממוצע רחב (' + out.window + ' ימים) — ' + out.atrPct + '% מהמחיר'; }\n  else if (pct >= 1.8) { out.points = 12; out.why = 'טווח ממוצע טוב (' + out.window + ' ימים) — ' + out.atrPct + '%'; }\n  else if (pct >= 1.0) { out.points = 7; out.why = 'טווח ממוצע בינוני (' + out.window + ' ימים) — ' + out.atrPct + '%'; }\n  else { out.points = 1; out.why = 'טווח ממוצע צר (' + out.window + ' ימים) — ' + out.atrPct + '%, קשה להוציא ממנו מהלך'; }\n  return out;\n}\n\n// A contraction into a decision point is the classic pre-move condition: the\n// last two sessions narrower than the ones before them.\nfunction coilComponent(daily, atrPct, relVol) {\n  var out = { points: 0, max: 12, why: null, coiled: false };\n  if (daily.length < 5) return out;\n  // A dead stock is narrow and quiet too. Contraction only means something when\n  // the name has enough daily range to matter and the volume has not collapsed\n  // with it — otherwise this rewards exactly the stocks worth ignoring.\n  if (atrPct != null && atrPct < 1.0) { out.why = null; return out; }\n  if (relVol != null && relVol < 0.6) { out.why = null; return out; }\n  var recent = daily.slice(-2), prior = daily.slice(-5, -2);\n  var r = recent.reduce(function (s, b) { return s + b.range; }, 0) / recent.length;\n  var p = prior.reduce(function (s, b) { return s + b.range; }, 0) / prior.length;\n  if (!(p > 0)) return out;\n  var ratio = r / p;\n  if (ratio <= 0.6) { out.points = 12; out.coiled = true; out.why = 'התכווצות טווח חדה לפני הפתיחה — ' + Math.round(ratio * 100) + '% מהממוצע'; }\n  else if (ratio <= 0.8) { out.points = 8; out.coiled = true; out.why = 'התכווצות טווח — ' + Math.round(ratio * 100) + '% מהממוצע'; }\n  return out;\n}\n\n// Where the last session closed inside its own range. A close on the high into\n// a multi-day high is the setup that most often continues.\nfunction closeComponent(daily) {\n  var out = { points: 0, max: 10, why: null };\n  if (!daily.length) return out;\n  var last = daily[daily.length - 1];\n  if (last.closePos >= 0.85) { out.points = 10; out.why = 'סגר בשיא היום — ' + Math.round(last.closePos * 100) + '% מהטווח'; }\n  else if (last.closePos >= 0.7) { out.points = 7; out.why = 'סגר בחלק העליון של היום'; }\n  else if (last.closePos <= 0.15) { out.points = 6; out.why = 'סגר בשפל היום — חולשה שמעניינת לשורט או לריבאונד'; }\n  else if (last.closePos <= 0.3) { out.points = 4; out.why = 'סגר בחלק התחתון של היום'; }\n  return out;\n}\n\n// Volume behind the last session, against its own recent average.\nfunction volumeComponent(daily) {\n  var out = { points: 0, max: 13, why: null, relVol: null };\n  if (daily.length < 3) return out;\n  var last = daily[daily.length - 1], prior = daily.slice(-6, -1);\n  if (!prior.length) return out;\n  var avg = prior.reduce(function (s, b) { return s + b.volume; }, 0) / prior.length;\n  if (!(avg > 0)) return out;\n  var rel = last.volume / avg;\n  out.relVol = Math.round(rel * 100) / 100;\n  if (rel >= 1.8) { out.points = 13; out.why = 'מחזור חריג בסשן האחרון — ×' + out.relVol; }\n  else if (rel >= 1.3) { out.points = 10; out.why = 'מחזור מוגבר — ×' + out.relVol; }\n  else if (rel >= 0.9) { out.points = 5; out.why = 'מחזור רגיל — ×' + out.relVol; }\n  else { out.points = 2; out.why = 'מחזור דל — ×' + out.relVol + ', עניין נמוך'; }\n  return out;\n}\n\n// Sitting just under a multi-day high (or over a low) is a decision point: the\n// session opens with something to resolve.\nfunction levelComponent(daily) {\n  var out = { points: 0, max: 25, why: null, levels: [] };\n  if (daily.length < 2) return out;\n  var last = daily[daily.length - 1], prior = daily.slice(0, -1);\n  var px = last.close, atr = daily.slice(-5).reduce(function (s, b) { return s + b.range; }, 0) / Math.min(5, daily.length);\n  if (!(atr > 0)) return out;\n\n  var hi = Math.max.apply(null, prior.map(function (b) { return b.high; }));\n  var lo = Math.min.apply(null, prior.map(function (b) { return b.low; }));\n  var pd = prior[prior.length - 1];\n\n  // Each level carries the session it came from. \"Yesterday\" meant 09-02 here\n  // and 09-03 in the range figure on the same card — the reader had no way to\n  // know which.\n  var pdDate = pd.date ? ' ' + pd.date.slice(5).replace('-', '/') : '';\n  out.levels = [\n    { name: 'גבוה רב-יומי', price: hi, date: null },\n    { name: 'גבוה סשן קודם' + pdDate, price: pd.high, date: pd.date },\n    { name: 'סגירת סשן קודם' + pdDate, price: pd.close, date: pd.date },\n    { name: 'נמוך סשן קודם' + pdDate, price: pd.low, date: pd.date },\n    { name: 'נמוך רב-יומי', price: lo, date: null }\n  ].filter(function (x) { return isFinite(x.price); });\n  out.priorDate = pd.date || null;\n\n  // The same guard: \"right under the multi-day high\" is meaningless when the\n  // multi-day range is a fraction of a percent.\n  var spanPct = (hi - lo) / (px || 1) * 100;\n  if (spanPct < 1.0) { out.points = 2; out.why = 'הטווח הרב-יומי צר מדי מכדי שרמה תהיה נקודת הכרעה'; return out; }\n  var dHi = (hi - px) / atr, dLo = (px - lo) / atr;\n  // WHAT THE LAST SESSION DID AT THE LEVEL, not just where it closed relative\n  // to it. A day that traded THROUGH the multi-day high and closed back under\n  // it is a rejected breakout — the opposite of a coil waiting to resolve — and\n  // scoring both as \"right under the level, decision point\" gave a failed\n  // breakout the maximum available points. Measured: AAPL reached 330.81 above\n  // a 328.40 level and closed 328.21; NVDA reached 230.40 above 229.26 and\n  // closed 228.45. Both were being ranked as pre-breakout.\n  var touchedHi = last.high >= hi - 0.05 * atr;\n  var brokeHi = last.high > hi;\n  var heldHi = px > hi;\n  var brokeLo = last.low < lo, heldLo = px < lo;\n  out.event = heldHi ? 'breakout_held'\n    : brokeHi ? 'breakout_failed'\n    : touchedHi ? 'testing_high'\n    : heldLo ? 'breakdown_held'\n    : brokeLo ? 'breakdown_recovered'\n    : dHi <= 0.35 ? 'approaching_high'\n    : dLo <= 0.35 ? 'approaching_low' : 'mid_range';\n\n  if (out.event === 'breakout_held') { out.points = 22;\n    out.why = 'פרץ את הגבוה הרב-יומי וסגר מעליו — פריצה שצריכה אישור'; }\n  else if (out.event === 'breakout_failed') { out.points = 6;\n    // Name the LEVEL and the price reached separately. Putting last.high in the\n    // parentheses read as though 83.60 were the multi-day high, while the level\n    // printed two lines below said 83.12 — the same card contradicting itself.\n    out.why = 'ניסה לפרוץ את הגבוה הרב-יומי ' + hi.toFixed(2)\n      + ', הגיע ל-' + last.high.toFixed(2) + ' ונסגר ב-' + px.toFixed(2) + ' מתחתיו — פריצה שנדחתה'; }\n  else if (out.event === 'testing_high') { out.points = 20;\n    out.why = 'נגע בגבוה הרב-יומי בלי לפרוץ — נבחן'; }\n  else if (out.event === 'approaching_high') { out.points = 25;\n    out.why = 'ממש מתחת לגבוה הרב-יומי, בלי ניסיון פריצה — נקודת הכרעה בפתיחה'; }\n  else if (dHi > 0.35 && dHi <= 1) { out.points = 18; out.why = 'קרוב לגבוה הרב-יומי'; }\n  else if (out.event === 'breakdown_held') { out.points = 16;\n    out.why = 'סגר מתחת לנמוך הרב-יומי — שבירה'; }\n  else if (out.event === 'breakdown_recovered') { out.points = 14;\n    out.why = 'שבר את הנמוך הרב-יומי ' + lo.toFixed(2)\n      + ', ירד עד ' + last.low.toFixed(2) + ' וחזר מעליו — שבירה שנדחתה'; }\n  else if (out.event === 'approaching_low') { out.points = 17;\n    out.why = 'ממש מעל הנמוך הרב-יומי — נקודת הכרעה'; }\n  else { out.points = 5; out.why = 'באמצע הטווח הרב-יומי'; }\n  return out;\n}\n\n// ---------------------------------------------------------------- the score\n//\n// candidateScore(daysRows) -> { score, reasons, structure, levels, ... }\n//\n// daysRows: an array of sessions, oldest first, each an array of 1-minute rows.\n// Every session here is CLOSED. Nothing about a live setup enters this.\nfunction candidateScore(daysRows, opts) {\n  var o = opts || {};\n  var all = (daysRows || []).filter(function (r) { return r && r.length; });\n  var daily = toDaily(all, o);\n  var dropped = all.length - daily.length;\n  if (!daily.length) {\n    return { score: null, reasons: [], structure: null, levels: [], sessions: 0,\n      incomplete_sessions: dropped,\n      note: dropped ? 'הסשנים הקיימים אינם שלמים — לא ניתן לחשב' : 'אין נתונים היסטוריים' };\n  }\n  var range = rangeComponent(daily), volume = volumeComponent(daily);\n  var parts = {\n    trend: trendComponent(daily),\n    range: range,\n    coil: coilComponent(daily, range.atrPct, volume.relVol),\n    close: closeComponent(daily),\n    volume: volume,\n    level: levelComponent(daily)\n  };\n  var got = 0, max = 0;\n  Object.keys(parts).forEach(function (k) { got += parts[k].points; max += parts[k].max; });\n  // Normalised so a symbol with only three sessions is not punished for the\n  // components that need five — its max is smaller, not its score.\n  var score = Math.round(got / max * 100);\n  // A stock whose daily range cannot pay for a trade is not a candidate, no\n  // matter how tidy its structure looks. This is a ceiling, not a deduction:\n  // the components stay honest and the verdict is capped.\n  var capped = null;\n  if (range.atrPct != null && range.atrPct < 0.8 && score > 30) { capped = score; score = 30; }\n  else if (range.atrPct != null && range.atrPct < 1.2 && score > 55) { capped = score; score = 55; }\n  var capWindow = range.window || 5;\n\n  // Reasons, strongest first, so the screen can show the top two.\n  var reasons = Object.keys(parts)\n    .map(function (k) { return { key: k, points: parts[k].points, text: parts[k].why }; })\n    .filter(function (x) { return x.text; })\n    .sort(function (a, b) { return b.points - a.points; });\n\n  var last = daily[daily.length - 1];\n  return {\n    score: score, reasons: reasons, parts: parts,\n    cappedFrom: capped,\n    capReason: capped != null ? 'הציון הוגבל: טווח ממוצע ' + capWindow + ' ימים של '\n      + range.atrPct + '% קטן מכדי להצדיק מעקב' : null,\n    structure: parts.trend.label,\n    structureWhy: parts.trend.why,\n    levels: parts.level.levels,\n    atrPct: parts.range.atrPct, avgRangeWindow: parts.range.window,\n    lastRangePct: parts.range.lastRangePct,\n    levelEvent: parts.level.event, priorDate: parts.level.priorDate,\n    relVol: parts.volume.relVol, coiled: parts.coil.coiled,\n    sessions: daily.length, incomplete_sessions: dropped,\n    incompleteNote: dropped ? dropped + ' סשנים לא שלמים לא נכללו בחישוב' : null,\n    dataSource: daily.every(function (b) { return b.source === 'authoritative'; }) ? 'authoritative'\n      : daily.some(function (b) { return b.source === 'authoritative'; }) ? 'mixed' : 'aggregated',\n    lastClose: last.close, lastDate: last.date, closePos: Math.round(last.closePos * 100),\n    daily: daily\n  };\n}\n\n// Is the interest improving or fading? The same score computed one session\n// earlier, compared with today's. Without at least three sessions there is\n// nothing to compare and the honest answer is that we do not know.\nfunction candidateTrend(daysRows) {\n  var rows = (daysRows || []).filter(function (r) { return r && r.length; });\n  if (rows.length < 3) return { direction: 'unknown', delta: null, text: 'אין מספיק ימים להשוואה' };\n  var now = candidateScore(rows), before = candidateScore(rows.slice(0, -1));\n  if (now.score == null || before.score == null) return { direction: 'unknown', delta: null, text: 'אין מספיק ימים להשוואה' };\n  var d = now.score - before.score;\n  return {\n    direction: d >= 6 ? 'improving' : d <= -6 ? 'fading' : 'steady',\n    delta: d,\n    text: d >= 6 ? 'העניין מתחזק (+' + d + ')' : d <= -6 ? 'העניין נחלש (' + d + ')' : 'ללא שינוי מהותי'\n  };\n}\n\n// ---------------------------------------------------------------- live interest\n//\n// For symbols already on the watchlist, during the session: is this still worth\n// the attention it is taking? This is NOT the setup score — a symbol can hold a\n// valid setup and still have gone quiet, and a symbol with no setup can be the\n// most alive thing on the screen.\nfunction liveInterest(A, ctx) {\n  var c = ctx || {};\n  if (!A || !A.state) return { score: null, reasons: [], verdict: 'אין נתונים' };\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length;\n  var atr = b.atr20 || b.avgRange || 1;\n  var reasons = [], score = 0;\n\n  // Movement over the last half hour, judged BOTH against the symbol's own\n  // average bar and against its price. Measuring only in ATR units made a dead\n  // stock look active: when the average bar is a cent, six cents is \"6R\" and\n  // nothing at all has happened.\n  var w = bars.slice(-30);\n  var hi = Math.max.apply(null, w.map(function (x) { return x.high; }));\n  var lo = Math.min.apply(null, w.map(function (x) { return x.low; }));\n  var swing = (hi - lo) / atr;\n  var swingPct = b.close > 0 ? (hi - lo) / b.close * 100 : 0;\n  var moving = swingPct >= 0.25;                  // a quarter percent in 30 min\n  if (swing >= 6 && moving) { score += 30; reasons.push('תנועה חזקה בחצי השעה האחרונה'); }\n  else if (swing >= 3 && moving) { score += 20; reasons.push('תנועה סבירה בחצי השעה האחרונה'); }\n  else if (swing >= 1.5 && moving) { score += 10; reasons.push('תנועה מוגבלת'); }\n  else { reasons.push('כמעט ללא תנועה בחצי השעה האחרונה (' + (Math.round(swingPct * 100) / 100) + '%)'); }\n\n  // is volume still arriving\n  var recentVol = w.reduce(function (s, x) { return s + x.volume; }, 0) / Math.max(1, w.length);\n  var dayVol = bars.reduce(function (s, x) { return s + x.volume; }, 0) / Math.max(1, n);\n  var rel = dayVol > 0 ? recentVol / dayVol : 0;\n  if (rel >= 1.3) { score += 25; reasons.push('מחזור מתגבר (×' + (Math.round(rel * 100) / 100) + ')'); }\n  else if (rel >= 0.8) { score += 15; reasons.push('מחזור יציב'); }\n  else { score += 3; reasons.push('המחזור דועך (×' + (Math.round(rel * 100) / 100) + ')'); }\n\n  // proximity to a level that matters\n  var T = c.tactical || tactical(A);\n  var near = [T.support, T.resistance].filter(Boolean).map(function (l) { return Math.abs(l.r); });\n  var d = near.length ? Math.min.apply(null, near) : Infinity;\n  if (d <= 0.5) { score += 25; reasons.push('צמוד לרמה משמעותית'); }\n  else if (d <= 1.5) { score += 15; reasons.push('קרוב לרמה'); }\n  else if (isFinite(d)) { score += 5; reasons.push('רחוק מרמות'); }\n\n  // a structure worth following\n  var st = S.announced || S.trend;\n  if (st === 'UP' || st === 'DOWN') { score += 20; reasons.push('מבנה תוך-יומי מוכרז (' + st + ')'); }\n  else { score += 5; reasons.push('ללא מבנה תוך-יומי'); }\n\n  // Nothing else can rescue a symbol that is not moving: structure and levels\n  // are only worth watching on something that travels.\n  if (!moving) { score = Math.min(score, 30); reasons.push('אין תנועה — לא מצדיק תשומת לב חיה'); }\n  score = Math.max(0, Math.min(100, score));\n  return {\n    score: score, reasons: reasons, movingPct: Math.round(swingPct * 100) / 100,\n    verdict: score >= 60 ? 'שווה תשומת לב חיה' : score >= 35 ? 'עניין בינוני' : 'כבר לא מעניין',\n    quiet: score < 35, swingR: Math.round(swing * 10) / 10, volRel: Math.round(rel * 100) / 100\n  };\n}\n\n// ---------------------------------------------------------------- suggestions\n//\n// Recommends only. Nothing here moves a symbol on its own — the trader decides,\n// and a screen that silently reshuffled the watchlist would be worse than one\n// that said nothing.\n//\n//   watch: [{ symbol, candidate, live }]\n//   scan:  [{ symbol, candidate, trend }]\nfunction suggestions(watch, scan, opts) {\n  var o = opts || {};\n  var PROMOTE_AT = o.promoteAt == null ? 60 : o.promoteAt;\n  var DEMOTE_AT = o.demoteAt == null ? 35 : o.demoteAt;\n\n  var weakest = (watch || []).filter(function (x) {\n    return x.live && x.live.score != null && x.live.score < DEMOTE_AT;\n  }).sort(function (a, b) { return a.live.score - b.live.score; });\n\n  var promote = (scan || []).filter(function (x) {\n    if (!x.candidate || x.candidate.score == null) return false;\n    if (x.candidate.score < PROMOTE_AT) return false;\n    // Improving matters as much as high: a score that is high and fading is\n    // yesterday's story.\n    return !x.trend || x.trend.direction !== 'fading';\n  }).sort(function (a, b) {\n    var d = b.candidate.score - a.candidate.score;\n    if (d) return d;\n    return (b.trend && b.trend.delta || 0) - (a.trend && a.trend.delta || 0);\n  });\n\n  var room = Math.max(0, (o.max == null ? 40 : o.max) - (watch || []).length);\n  return {\n    promote: promote.slice(0, 8).map(function (x, i) {\n      // Only pair a promotion with a removal when the list is actually full.\n      var swap = (!room && weakest[i]) ? weakest[i] : null;\n      return {\n        symbol: x.symbol, score: x.candidate.score,\n        why: (x.candidate.reasons || []).slice(0, 2).map(function (r) { return r.text; }),\n        direction: x.trend ? x.trend.direction : 'unknown',\n        directionText: x.trend ? x.trend.text : null,\n        insteadOf: swap ? swap.symbol : null,\n        insteadOfWhy: swap ? swap.live.verdict + ' (' + swap.live.score + ')' : null\n      };\n    }),\n    demote: weakest.slice(0, 8).map(function (x) {\n      return { symbol: x.symbol, score: x.live.score, why: x.live.reasons.slice(0, 2),\n        verdict: x.live.verdict };\n    }),\n    room: room,\n    note: room ? 'יש מקום — הוספה לא מחייבת הסרה' : 'המעקב מלא: הוספה מחייבת הסרה'\n  };\n}\n\n\n// ============================================================================\n// BUY DECISION CARD — an additional view. Nothing here is read by any other\n// surface, and this file reads the state the application has already built.\n//\n// The premise that shapes everything: we do NOT know the price the trader is\n// looking at in his broker. So the card must never say \"if it reaches X, buy\" —\n// that is a limit order, and it assumes we can see the market. It says instead:\n// given the price ON YOUR SCREEN RIGHT NOW, here is the decision.\n//\n// Four states only. BUY, WAIT, RECHECK, NO SETUP. RECHECK is not a refusal —\n// it means the snapshot no longer covers the price being seen, and the answer\n// has to be recomputed from newer candles. A lower price may be a better\n// entry; a higher one may be a breakout. Neither is knowable from here.\n// ============================================================================\n\nfunction buyCard(st, A, ctx) {\n  var c = ctx || {};\n  var NA = '—';\n  var n2 = function (x) { return x == null ? NA : (+x).toFixed(2); };\n\n  var out = {\n    symbol: st && st.symbol || (c.symbol || NA),\n    snapshotTime: c.snapshotTime || null,\n    lastBarTime: null, lastClose: null,\n    coverage: null, decision: 'NO SETUP', quality: NA, confidence: NA,\n    map: [], validity: null, reasons: [], levels: [], blocked: null\n  };\n  if (!st || !A || !A.state) { out.blocked = 'אין ניתוח זמין'; return out; }\n\n  var bars = (A.rawBars && A.rawBars.length) ? A.rawBars : A.bars;\n  var last = bars && bars.length ? bars[bars.length - 1] : null;\n  var b = A.state.bar;\n  var atr = (b && (b.atr20 || b.avgRange)) || 0;\n  out.lastBarTime = last ? last.time : null;\n  out.lastClose = last ? last.close : null;\n  out.coverage = st.coverage || null;\n\n  // ---- data safety comes before anything else -----------------------------\n  // An unusable session cannot produce a BUY, whatever the setup looks like.\n  var cov = st.coverage || {};\n  if (st.stale) out.blocked = 'נתונים ישנים — לא ניתן להחליט';\n  else if (st.sessionIncomplete)\n    out.blocked = 'סשן חלקי (' + (cov.receivedMinutes || 0) + '/' + (cov.expectedMinutes || 0)\n      + ' דקות) — לא ניתן להחליט';\n  else if (st.tooOldToEnter) out.blocked = 'הנר האחרון ישן מדי להחלטה';\n  else if (!last) out.blocked = 'אין נרות';\n  if (out.blocked) {\n    out.decision = 'RECHECK';\n    out.reasons.push(out.blocked);\n    return out;\n  }\n\n  // ---- what the existing analysis already decided --------------------------\n  var P = st.plan || null;\n  var lv = st.levels || {};\n  var px = out.lastClose;\n\n  // The BUY range is the zone the existing plan already identified, not a\n  // percentage invented around the last price. Without a plan there is no zone\n  // and therefore no buy.\n  var zone = (P && P.zone && P.zone.length === 2) ? P.zone.slice() : null;\n  var hasEdge = !!(P && zone && !st.noEdge);\n\n  // Validity comes from STRUCTURE, not from a band drawn around the last price.\n  // 616.42-617.08 around 616.75 is arbitrary symmetry dressed up as analysis:\n  // it says nothing about where the reading actually stops holding. The real\n  // boundaries are the levels the setup depends on — the zone, the\n  // invalidation, the nearest support and resistance. Where none of those\n  // exist, there is no validity range and none is invented.\n  var T = st.tactical || null;\n  var bounds = [];\n  var addBound = function (v, why) {\n    if (v == null || !isFinite(v)) return;\n    bounds.push({ price: +(+v).toFixed(2), why: why });\n  };\n  if (T && T.support) addBound(T.support.price, 'תמיכה');\n  if (T && T.resistance) addBound(T.resistance.price, 'התנגדות');\n  if (lv.tacticalInvalidation != null) addBound(lv.tacticalInvalidation, 'ביטול');\n  if (lv.watch != null) addBound(lv.watch, 'רמת מעקב');\n  if (P && P.zone && P.zone.length === 2) { addBound(P.zone[0], 'אזור'); addBound(P.zone[1], 'אזור'); }\n\n  var below = bounds.filter(function (x) { return x.price < px; }).sort(function (a, b2) { return b2.price - a.price; })[0];\n  var above = bounds.filter(function (x) { return x.price > px; }).sort(function (a, b2) { return a.price - b2.price; })[0];\n  var vLow = below ? below.price : null;\n  var vHigh = above ? above.price : null;\n  out.validity = (vLow != null && vHigh != null)\n    ? { low: vLow, high: vHigh, lowWhy: below.why, highWhy: above.why } : null;\n\n  // ---- the decision --------------------------------------------------------\n  if (!hasEdge) {\n    // NO SETUP is its own answer, not a weak version of one. There is nothing\n    // whose validity could be mapped, so there is no price map, no validity\n    // range and no confidence — confidence in a setup that does not exist is a\n    // number about nothing. And it is never RECHECK: RECHECK means an ACTIVE\n    // setup no longer covers the price, which presupposes a setup.\n    out.decision = 'NO SETUP';\n    out.reasons.push(st.why || 'אין setup קנייה כרגע');\n    out.map = [];\n    out.validity = null;\n    out.quality = 'אין';\n    out.confidence = NA;\n    out.hasMap = false;\n  } else {\n    var zLow = Math.min(zone[0], zone[1]), zHigh = Math.max(zone[0], zone[1]);\n    // Does the buy zone overlap the band this snapshot can speak about?\n    var overlaps = zHigh >= vLow && zLow <= vHigh;\n    out.decision = overlaps ? 'BUY' : 'WAIT';\n    if (!overlaps) out.reasons.push('אזור הקנייה (' + n2(zLow) + '–' + n2(zHigh)\n      + ') מחוץ לטווח שהכרטיס יכול לדבר עליו');\n  }\n\n  // ---- the price decision map ---------------------------------------------\n  // Only where there IS an active setup, and only where structure gives real\n  // boundaries. Read as: the number you are looking at in your broker.\n  var rows = [];\n  var canMap = hasEdge && out.validity != null;\n  out.hasMap = canMap;\n  if (canMap) {\n  if (out.decision === 'BUY') {\n    var zl = Math.max(vLow, Math.min(zone[0], zone[1]));\n    var zh = Math.min(vHigh, Math.max(zone[0], zone[1]));\n    rows.push({ from: zl, to: zh, decision: 'BUY', text: 'קונה' });\n    if (zh < vHigh) rows.push({ from: +(zh + 0.01).toFixed(2), to: vHigh, decision: 'WAIT', text: 'לא קונה' });\n    if (zl > vLow) rows.push({ from: vLow, to: +(zl - 0.01).toFixed(2), decision: 'WAIT', text: 'לא קונה' });\n  } else if (out.decision === 'WAIT' || out.decision === 'NO SETUP') {\n    rows.push({ from: vLow, to: vHigh, decision: out.decision === 'WAIT' ? 'WAIT' : 'NO SETUP',\n      text: out.decision === 'WAIT' ? 'לא קונה' : 'אין setup' });\n  }\n  rows.sort(function (x, y) { return y.from - x.from; });\n  rows.push({ above: vHigh, decision: 'RECHECK', text: 'לבדוק מחדש' });\n  rows.push({ below: vLow, decision: 'RECHECK', text: 'לבדוק מחדש' });\n  }\n  out.map = rows;\n\n  // ---- supporting readings, all taken from what already exists -------------\n  if (out.decision !== 'NO SETUP') {\n    out.quality = out.decision === 'BUY' ? 'תקין' : 'חלש';\n    out.confidence = (st.score != null)\n      ? (st.score >= 7 ? 'גבוה' : st.score >= 4 ? 'בינוני' : 'נמוך') : NA;\n  }\n\n  out.structure = st.structure || (A.state.announced || A.state.trend) || NA;\n  out.momentum = st.momentum || NA;\n  out.vwap = b ? b.vwap : null;\n  out.aboveVwap = b ? !!b.aboveVwap : null;\n  out.ema9 = b ? b.ema9 : null;\n  out.ema20 = b ? b.ema20 : null;\n  out.market = c.market || NA;\n\n  var pf = st.pressure || null;\n  if (pf) {\n    // The pressure object exposes buyPct/sellPct. I read buyers/sellers and got\n    // undefined, so the card showed \"buyers weakening\" with no numbers beside\n    // it — the exact thing the rule forbids.\n    out.buyersPct = pf.buyPct != null ? pf.buyPct : pf.buyers;\n    out.sellersPct = pf.sellPct != null ? pf.sellPct : pf.sellers;\n    out.buyersTrend = pf.buyersTrend; out.sellersTrend = pf.sellersTrend;\n  }\n\n  // Nearby levels that actually bear on a buy decision.\n  if (T) {\n    if (T.support) out.levels.push({ name: 'תמיכה קרובה', price: T.support.price });\n    if (T.resistance) out.levels.push({ name: 'התנגדות קרובה', price: T.resistance.price });\n  }\n  if (lv.watch != null) out.levels.push({ name: 'רמת מעקב', price: lv.watch });\n\n  // ---- a real explanation, not a label ------------------------------------\n  // \"אין setup קנייה כרגע\" restates the verdict. The reader needs to know WHY\n  // from the same facts printed above: where the price finished, what the\n  // structure is doing, and which side is gaining.\n  out.why = (function () {\n    var parts = [];\n    if (px != null) parts.push('המחיר ' + (c.closed ? 'סיים ב-' : 'עומד על ') + px.toFixed(2));\n    var mom = { PUSHING: 'בדחיפה', PULLBACK: 'בתוך pullback', RECOVERY: 'בהתאוששות',\n      SELLING: 'תחת מכירות', FLAT: 'ללא מומנטום' }[out.momentum];\n    var strc = { UP: 'מבנה עולה', DOWN: 'מבנה יורד', RANGE: 'בטווח' }[out.structure];\n    if (strc) parts.push(strc);\n    if (mom) parts.push(mom);\n    if (pf && pf.buyersTrend && pf.sellersTrend)\n      parts.push('הקונים ' + pf.buyersTrend + ' והמוכרים ' + pf.sellersTrend);\n    var lead = parts.join(', ') + '.';\n    var verdict = out.decision === 'NO SETUP' ? ' כרגע אין setup קנייה מספיק ברור.'\n      : out.decision === 'WAIT' ? ' התנאים לא מצדיקים קנייה במחיר הנוכחי.'\n      : out.decision === 'BUY' ? ' התנאים מצדיקים קנייה בטווח המסומן.'\n      : ' צריך נרות חדשים כדי להחליט.';\n    return lead + verdict;\n  })();\n\n  // What the trader is actually asking. When the market is shut this is not a\n  // rejection — there is simply nothing to act on.\n  out.buyNow = c.closed ? '— המסחר סגור'\n    : out.decision === 'BUY' ? 'כן, בטווח המסומן'\n    : out.decision === 'WAIT' ? 'לא'\n    : out.decision === 'RECHECK' ? '— צריך נרות חדשים'\n    : '— אין setup';\n\n  // A live price map only means something while a live price exists.\n  if (c.closed) { out.hasMap = false; out.map = []; out.validity = null; }\n  out.marketClosed = !!c.closed;\n\n  // A probability is shown only if it measures THIS decision. The path model\n  // answers \"which of two levels is touched first\", which is a different\n  // question, so it is deliberately not carried over.\n  out.probability = null;\n  out.probabilityNote = 'הסתברות לא מוצגת: המודל הקיים מודד אירוע אחר';\n\n  if (!out.reasons.length && st.why) out.reasons.push(st.why);\n  return out;\n}\n\n// The same card as plain text. Built from the card object rather than scraped\n// from the DOM, so what is copied is exactly what was decided — a copy that can\n// drift from the screen is worse than none.\nfunction buyCardText(card, opts) {\n  var o = opts || {};\n  var NA = '—';\n  var n2 = function (x, d) { return x == null ? NA : (+x).toFixed(d == null ? 2 : d); };\n  var L = [];\n  var DEC = { BUY: 'BUY — אפשר לקנות', WAIT: 'WAIT — לא קונה כרגע',\n    RECHECK: 'RECHECK — לבדוק מחדש', 'NO SETUP': 'NO SETUP — אין setup' };\n\n  L.push(card.symbol + (card.marketClosed\n    ? ' · סגירה' + (card.snapshotTime ? ' ' + card.snapshotTime : '')\n    : (card.snapshotTime ? ' · תמונת מצב ' + card.snapshotTime : '')));\n  var cov = card.coverage || {};\n  L.push('נר אחרון: ' + n2(card.lastClose, 3)\n    + (cov.receivedMinutes != null ? ' · ' + cov.receivedMinutes + '/' + cov.expectedMinutes\n      + ' נרות ' + (cov.complete ? 'תקין' : 'חסר') : ''));\n  L.push('');\n  L.push(DEC[card.decision] || card.decision);\n  if (card.blocked) L.push(card.blocked);\n  L.push('');\n  L.push('מצב קנייה: ' + card.quality);\n  L.push('ביטחון: ' + card.confidence);\n\n  if (card.hasMap && card.validity) {\n    L.push('');\n    L.push(o.live ? 'מחיר Trading שלך עכשיו' : 'מפת מחיר');\n    (card.map || []).forEach(function (r) {\n      var range = r.above != null ? ('מעל ' + n2(r.above))\n        : r.below != null ? ('מתחת ' + n2(r.below))\n        : (n2(r.from) + '–' + n2(r.to));\n      L.push('  ' + range + '  ->  ' + r.decision + ' — ' + r.text);\n    });\n    L.push('');\n    L.push('תקפות הכרטיס: ' + n2(card.validity.low) + '–' + n2(card.validity.high)\n      + ' (' + card.validity.lowWhy + ' / ' + card.validity.highWhy + ')');\n    L.push('מחוץ לטווח -> RECHECK עם הנרות החדשים.');\n    L.push('מחיר נמוך יותר עשוי ליצור setup טוב יותר, גבוה יותר עשוי ליצור פריצה.');\n  }\n\n  L.push('');\n  L.push('למה אין BUY');\n  if (card.blocked) L.push('  ' + card.blocked);\n  L.push('  ' + (card.why || (card.reasons || []).join(' ')));\n\n  L.push('');\n  L.push('נתונים');\n  L.push('  מבנה: ' + card.structure);\n  L.push('  מומנטום: ' + card.momentum);\n  L.push('  ' + (card.vwap == null ? 'VWAP: ' + NA\n    : (card.aboveVwap ? 'מחיר מעל VWAP ' : 'מחיר מתחת VWAP ') + n2(card.vwap)));\n  L.push('  EMA9 / EMA20: ' + n2(card.ema9) + ' / ' + n2(card.ema20));\n  L.push('  שוק: ' + card.market);\n\n  if (card.buyersPct != null) {\n    L.push('');\n    L.push('קונים / מוכרים: ' + card.buyersPct + '% / ' + card.sellersPct + '%');\n    var dir = [];\n    if (card.buyersTrend) dir.push('קונים ' + card.buyersTrend);\n    if (card.sellersTrend) dir.push('מוכרים ' + card.sellersTrend);\n    if (dir.length) L.push('כיוון הכוח: ' + dir.join(' · '));\n  }\n\n  if ((card.levels || []).length) {\n    L.push('');\n    L.push('רמות');\n    card.levels.forEach(function (l) { L.push('  ' + l.name + ': ' + n2(l.price)); });\n  }\n\n  L.push('');\n  L.push('החלטה');\n  L.push('  BUY NOW: ' + card.buyNow);\n  if (!card.hasMap) L.push('  מפת מחיר: '\n    + (card.marketClosed ? 'לא מוצגת כשהמסחר סגור' : 'לא מוצגת — אין setup'));\n  L.push('');\n  L.push(card.probabilityNote);\n  return L.join('\\n');\n}\n\n\n</script>\n<script>\n(function(){\n  var qs=function(s){return document.querySelector(s)};\n  // No setup vocabulary lives on this page. The bands describe CANDIDACY, and\n  // the CSS class names are reused only for their colours.\n  var MOM_TXT={PUSHING:'דוחף',PULLBACK:'נסיגה',RECOVERY:'התאוששות',SELLING:'מכירות',FLAT:'שטוח'};\n  var STRUCT_TXT={UP:'עולה',DOWN:'יורד',RANGE:'טווח'};\n  // A scan describes where a symbol IS. It never says what to do — even when\n  // the archive happens to be fresh — because that is the live radar's job and\n  // the two must not be confusable.\n  var OBSERVE={NO_SETUP:'אין setup',WAITING_FOR_ZONE:'מתקרב לאזור',IN_ZONE:'בתוך האזור',\n    READY_PARTIAL:'האזור מוחזק',WAITING_FOR_CONFIRMATION:'מעל רמת האישור',READY_ADD:'אישור התקבל',\n    ACTIVE:'מהלך פעיל',TAKE_PROFIT_AREA:'באזור היעד',DO_NOT_CHASE:'התרחק מהכניסה',FAILED:'setup נכשל'};\n  function observe(r){\n    var P=r.plan; if(!P)return r.why||'';\n    var base=OBSERVE[P.state]||P.state;\n    var z=P.zone?P.zone.map(function(x){return x.toFixed(2)}).join('–'):null;\n    if(P.state==='WAITING_FOR_ZONE'||P.state==='IN_ZONE'||P.state==='READY_PARTIAL')return base+(z?' '+z:'');\n    if(P.state==='WAITING_FOR_CONFIRMATION'||P.state==='READY_ADD')return base+(P.addAbove!=null?' '+P.addAbove.toFixed(2):'');\n    if(P.state==='TAKE_PROFIT_AREA'||P.state==='ACTIVE')return base+(P.target!=null?' '+P.target.toFixed(2):'');\n    return base;\n  }\n  var store={}, symbols=[], filterStatus=null, loading=false, viewDate=null, shownDate=null, dates=[], loadedDays={}, collecting=[], coverage={}, probes=0;\n\n  function bust(u){return u+(u.indexOf('?')>0?'&':'?')+'ts='+Date.now()}\n  // 429 and 5xx are transient: a rate cap that resets in a minute, or a\n  // deploy swapping over. Retrying beats showing the trader a dead screen.\n  function j(u,tries){\n    var left=tries==null?2:tries;\n    return fetch(bust(u),{cache:'no-store'}).then(function(r){\n      if(r.status===404)return r.json();\n      if(!r.ok){\n        if((r.status===429||r.status>=500)&&left>0)\n          return new Promise(function(res){setTimeout(res,r.status===429?4000:1500)}).then(function(){return j(u,left-1)});\n        throw new Error(r.status);\n      }\n      return r.json();\n    });\n  }\n  function toast(t){var el=qs('#toast');el.textContent=t;el.hidden=false;clearTimeout(el._t);el._t=setTimeout(function(){el.hidden=true},1800)}\n\n  // The trading date that should have data now (same rule as the radar).\n  function expectedSessionDate(){\n    var p=new Intl.DateTimeFormat('en-CA',{timeZone:'America/New_York',year:'numeric',month:'2-digit',\n      day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false}).formatToParts(new Date())\n      .reduce(function(a,x){a[x.type]=x.value;return a},{});\n    var d=new Date(p.year+'-'+p.month+'-'+p.day+'T12:00:00Z');\n    if((p.hour+':'+p.minute)<'09:30')d.setUTCDate(d.getUTCDate()-1);\n    while(d.getUTCDay()===0||d.getUTCDay()===6)d.setUTCDate(d.getUTCDate()-1);\n    return d.toISOString().slice(0,10);\n  }\n  function freshness(rows,date){\n    if(!rows||!rows.length)return 'NO DATA';\n    var last=rows[rows.length-1];\n    if(date<expectedSessionDate())return 'STALE';\n    if(last.time>='15:59')return 'SESSION ENDED';\n    // The bar's stamp is its START; it completes a minute later.\n    var age=Math.max(0,Math.floor(Date.now()/1000)-((last.unix||0)+60));\n    return age<=180?'LIVE':age<=600?'DELAYED':'STALE';\n  }\n\n  // One call for the board, then the remainder the server could not reach in\n  // batches of 40, until nothing is left.\n  function loadUniverse(date){\n    var q='/board?universe=1&date='+date;\n    return j(q).then(function(d){\n      var rest=(d.not_fetched||[]).slice(), total=(d.symbols||[]).length, got=total-rest.length;\n      qs('#prog').textContent=got+'/'+total;\n      // A follow-up call has the SAME subrequest ceiling as the first, so it\n      // also defers what it could not reach. Ignoring that left most of the\n      // universe unfetched while the page reported it as missing.\n      var guard=0;\n      var next=function(){\n        if(!rest.length||guard++>30)return Promise.resolve(d);\n        var batch=rest.splice(0,40);\n        return j('/board?date='+date+'&symbols='+batch.join(',')).then(function(more){\n          (more.rows||[]).forEach(function(r){d.rows.push(r)});\n          // whatever this call deferred goes back on the queue\n          var again=(more.not_fetched||[]).filter(function(s){return batch.indexOf(s)>=0});\n          again.forEach(function(s){rest.push(s)});\n          got+=(batch.length-again.length);\n          qs('#prog').textContent=got+'/'+total;\n          return next();\n        },function(){ return next(); });\n      };\n      return next();\n    });\n  }\n\n  // The latest session the archive actually holds. Before the open, that is\n  // yesterday — asking for today and printing \"no data\" beside a symbol with\n  // five days of history was the wrong answer to the wrong question.\n  // Show the latest session that is actually COVERED. The moment the intraday\n  // walk writes its first symbol of a new day, that day exists in the archive —\n  // and picking it would make the other 99 read as missing every time the page\n  // opened, which is what made this look like data that kept disappearing.\n  function latestArchivedDate(){\n    return j('/archive/dates').then(function(d){\n      dates=(d.dates||[]);\n      collecting=(d.collecting||[]);\n      coverage=d.coverage||{}; probes=d.readable||0;\n      return d.latest_covered||dates[0]||expectedSessionDate();\n    },function(){ return expectedSessionDate(); });\n  }\n  // The candidate score needs several closed sessions, not one. The board gives\n  // the day on screen; the rest come from the archive, once, in one call per\n  // day rather than one per symbol.\n  // One request per symbol, cached in D1 on the server side, so this is cheap\n  // after the first call of the day. Only for what is on screen.\n  var dailyBars={};\n  // Forty sequential requests behind the first paint is a page that shows\n  // \"loading\" for a minute and stops entirely if one of them hangs. These run\n  // AFTER the rows are on screen, a few at a time, each with its own timeout,\n  // and the screen re-renders as they land.\n  function withTimeout(p,ms){\n    return new Promise(function(res,rej){\n      var done=false;\n      var t=setTimeout(function(){ if(!done){done=true;rej(new Error('timeout'))} },ms||6000);\n      p.then(function(v){ if(!done){done=true;clearTimeout(t);res(v)} },\n             function(e){ if(!done){done=true;clearTimeout(t);rej(e)} });\n    });\n  }\n  function loadDailyFor(syms){\n    var need=syms.filter(function(s){return !dailyBars[s]});\n    if(!need.length)return Promise.resolve();\n    var i=0;\n    var worker=function(){\n      if(i>=need.length)return Promise.resolve();\n      var s=need[i++];\n      return withTimeout(j('/daily/'+s+'?days=30'),6000)\n        .then(function(d){ dailyBars[s]=d.bars||[]; },function(){ dailyBars[s]=[]; })\n        .then(worker);\n    };\n    // three at a time: fast enough to finish in seconds, gentle enough not to\n    // trip the per-minute rate cap\n    return Promise.all([worker(),worker(),worker()]);\n  }\n  // Prior sessions are CLOSED — they cannot change. Re-pulling four full days\n  // of the whole universe on every load was reading roughly 20,000 archive rows\n  // a time and is the bulk of the day's read budget. Keep them locally.\n  function cacheKey(d){ return 'scan:'+d }\n  function cachedDay(d){\n    try{ var s=localStorage.getItem(cacheKey(d)); return s?JSON.parse(s):null; }catch(e){ return null }\n  }\n  function cacheDay(d,rows){\n    try{ localStorage.setItem(cacheKey(d),JSON.stringify(rows)); }catch(e){ /* quota: skip */ }\n  }\n  function loadPriorDays(date){\n    var want=dates.filter(function(d){return d<date}).slice(0,4);\n    // Serve what we already hold before asking the server for anything.\n    want.forEach(function(d){\n      if(loadedDays[d])return;\n      var cached=cachedDay(d);\n      if(cached&&cached.length){\n        cached.forEach(function(r){\n          if(!r.date||!r.unix)return;\n          var st=store[r.symbol]||(store[r.symbol]={rows:[],days:{}});\n          st.days=st.days||{};\n          var seen=st.seen=st.seen||{}, k=r.date+':'+r.unix;\n          if(seen[k])return;\n          seen[k]=1;\n          (st.days[r.date]=st.days[r.date]||[]).push(r);\n        });\n        loadedDays[d]=1;\n      }\n    });\n    var need=want.filter(function(d){return !loadedDays[d]});\n    if(!need.length)return Promise.resolve();\n    // A call serves only what its subrequest ceiling allows and names the rest.\n    // loadUniverse walks that remainder; this did not, so most symbols came back\n    // with a single session and no history — and a symbol with no history has no\n    // candidate score, which the screen then reported as \"no data\".\n    var absorb=function(bd){\n      (bd.rows||[]).forEach(function(r){\n        if(!r.date||!r.unix)return;\n        var st=store[r.symbol]||(store[r.symbol]={rows:[],days:{}});\n        st.days=st.days||{};\n        var seen=st.seen=st.seen||{}, key=r.date+':'+r.unix;\n        if(seen[key])return;\n        seen[key]=1;\n        (st.days[r.date]=st.days[r.date]||[]).push(r);\n      });\n    };\n    return need.reduce(function(p,d){\n      return p.then(function(){\n        return j('/board?universe=1&date='+d).then(function(bd){\n          absorb(bd);\n          var rest=(bd.not_fetched||[]).slice(), guard=0;\n          var more=function(){\n            if(!rest.length||guard++>30)return Promise.resolve();\n            var batch=rest.splice(0,40);\n            return j('/board?date='+d+'&symbols='+batch.join(',')).then(function(m2){\n              absorb(m2);\n              (m2.not_fetched||[]).forEach(function(s){ if(batch.indexOf(s)>=0)rest.push(s) });\n              return more();\n            },function(){ return more() });\n          };\n          return more().then(function(){\n            loadedDays[d]=1;\n            // Keep only what is needed to score: closed sessions never change.\n            var all=[];\n            Object.keys(store).forEach(function(s){ (store[s].days&&store[s].days[d]||[]).forEach(function(r){all.push(r)}) });\n            if(all.length)cacheDay(d,all);\n          });\n        },function(){});\n      });\n    },Promise.resolve());\n  }\n  function loadAll(){\n    if(loading)return Promise.resolve();\n    loading=true;\n    qs('#prog').textContent='טוען…';\n    var pick=viewDate?Promise.resolve(viewDate):latestArchivedDate();\n    return pick.then(function(date){\n      shownDate=date;\n      return loadUniverse(date);\n    }).then(function(d){\n      // Clear BEFORE loading the prior days, not after: resetting the store\n      // afterwards threw away every session the multi-day score depends on,\n      // which is why no levels ever reached the screen.\n      symbols=(d.symbols||[]).slice();\n      store={}; loadedDays={};   // and with it every dedup ledger\n      (d.rows||[]).forEach(function(r){\n        var st=store[r.symbol]||(store[r.symbol]={rows:[],days:{}});\n        st.rows.push(r);\n      });\n      return loadPriorDays(shownDate).then(function(){return d});\n    }).then(function(d){\n      var date=shownDate;\n      // The scanner asks ONE question: is this worth attention today? It does\n      // not run the execution engine, because a setup state derived from a\n      // closed session is not a state — it is yesterday's decision, and\n      // carrying it forward is exactly what we forbade.\n      symbols.forEach(function(s){\n        var st=store[s]||(store[s]={rows:[]});\n        st.rows.sort(function(a,b){return a.unix-b.unix});\n        st.days=st.days||{};\n        // Same rule for the day on screen: file each bar under its own date.\n        if(st.rows.length){\n          var seen2=st.seen=st.seen||{};\n          st.rows.forEach(function(r){\n            if(!r.date||!r.unix)return;\n            var key=r.date+':'+r.unix;\n            if(seen2[key])return;\n            seen2[key]=1;\n            (st.days[r.date]=st.days[r.date]||[]).push(r);\n          });\n        }\n        var sessions=Object.keys(st.days).sort().map(function(d){\n          var rows=st.days[d];\n          rows.sort(function(a,b){return a.unix-b.unix});\n          // A bucket may only contain bars of its own date. Anything else is a\n          // pipeline fault, not data — drop it rather than aggregate it.\n          return rows.filter(function(r){return r.date===d});\n        }).filter(function(rows){return rows.length});\n        // Previous-day values come from the provider's own daily candles when we\n        // have them: those carry the auction prints and consolidated volume,\n        // which minute bars do not. Aggregation is the fallback, and an\n        // incomplete session is dropped rather than scored.\n        st.cand=sessions.length?candidateScore(sessions,{authoritative:dailyBars[s]||null}):{score:null,reasons:[],levels:[]};\n        st.trend=sessions.length>=3?candidateTrend(sessions):{direction:'unknown',delta:null,text:'נדרשים עוד ימים להשוואה'};\n      });\n      loading=false;\n      // The header counted symbols with BARS while the bands counted symbols with\n      // a SCORE, and the coverage gate made those two different numbers — so the\n      // page reported 108 with data beside bands totalling 33. One measure now.\n      var scored=symbols.filter(function(s){return store[s]&&store[s].cand&&store[s].cand.score!=null}).length;\n      var withData=scored;\n      var exp=expectedSessionDate();\n      qs('#prog').textContent=scored+'/'+symbols.length+' מדורגות';\n      // Only symbols with NO bars at all are a gap. One that has bars but too few\n      // to score is a short session, which filling again will not repair.\n      var noBars=symbols.filter(function(s){return !store[s]||!store[s].rows.length}).length;\n      var gap=noBars, fb=qs('#fill');\n      // A day still being collected is not a gap. Offering to fill it by hand\n      // meant re-pulling the whole universe every time the page opened.\n      var partial=collecting.indexOf(date)>=0;\n      fb.hidden=(gap===0||partial); fb.disabled=false;\n      if(!fb.hidden)fb.textContent='מלא '+gap+' חסרות';\n      if(partial)setFill('הסשן הזה עדיין נאסף אוטומטית — אין צורך למלא ידנית');\n      // Say plainly which session is on screen, and whether it is the current one.\n      var midway=collecting.filter(function(d){return d>date});\n      // \"The current session\" at 12:39Z on 09/04, with the US market shut and\n      // 09/03 on screen, is simply false.\n      var openNow=(function(){\n        var p=new Intl.DateTimeFormat('en-US',{timeZone:'America/New_York',weekday:'short',\n          hour:'2-digit',minute:'2-digit',hour12:false}).formatToParts(new Date())\n          .reduce(function(a,x){a[x.type]=x.value;return a},{});\n        var hm=p.hour+':'+p.minute;\n        return p.weekday!=='Sat'&&p.weekday!=='Sun'&&hm>='09:30'&&hm<'16:00';\n      })();\n      qs('#when').innerHTML=((date===exp&&openNow)\n        ?'נתונים מהסשן הפעיל · '+date.slice(5).replace('-','/')\n        :'<b>נתונים מהסשן האחרון שהושלם · '+date.slice(5).replace('-','/')+'</b>')\n        +(midway.length?' · <span style=\"color:var(--s-close)\">'+midway[0].slice(5).replace('-','/')+' עדיין נאסף</span>':'');\n      drawDates();\n      var ttl=qs('#title'); if(ttl)ttl.textContent='סריקה · '+symbols.length;\n      render();\n      // Official daily candles refine the levels; they must never hold up the\n      // first paint. Fetch them behind the screen and re-render when they land.\n      var top=list40();\n      loadDailyFor(top).then(function(){\n        top.forEach(function(s){\n          var st=store[s]; if(!st||!st.days)return;\n          var sessions=Object.keys(st.days).sort().map(function(d2){\n            return (st.days[d2]||[]).filter(function(r){return r.date===d2});\n          }).filter(function(rows){return rows.length});\n          if(sessions.length)st.cand=candidateScore(sessions,{authoritative:dailyBars[s]||null});\n        });\n        render();\n      });\n    },function(e){loading=false;qs('#rows').innerHTML='<div class=\"empty\"><b>שגיאת טעינה</b><br>'+String(e)+'</div>'});\n  }\n\n  function render(){\n    var rows=symbols.map(function(s){return {symbol:s}});\n    var bandOf=function(s){var c=(store[s]||{}).cand||{};\n      return c.score==null?'NODATA':c.score>=65?'READY':c.score>=50?'CLOSE':c.score>=35?'WATCH':'QUIET'};\n    var BAND_TXT={READY:'מועמד חזק',CLOSE:'מועמד',WATCH:'שולי',QUIET:'לא מעניין',NODATA:'לא מדורג'};\n    ['READY','CLOSE','WATCH','QUIET','NODATA'].forEach(function(){});\n    qs('#counts').innerHTML=['READY','CLOSE','WATCH','QUIET','NODATA'].map(function(k){\n      var n=rows.filter(function(r){return bandOf(r.symbol)===k}).length;\n      return '<button type=\"button\" class=\"cnt bg-'+k+(filterStatus===k?' sel':'')+'\" data-k=\"'+k+'\" data-on=\"'+n+'\">'+BAND_TXT[k]+' '+n+'</button>';\n    }).join('');\n    [].forEach.call(document.querySelectorAll('.cnt'),function(b){b.onclick=function(){filterStatus=filterStatus===b.dataset.k?null:b.dataset.k;render()}});\n    var mode=qs('#sort').value;\n    var list=rows.slice().sort(function(a,b){\n      var ca=(store[a.symbol]||{}).cand||{}, cb=(store[b.symbol]||{}).cand||{};\n      var ta=(store[a.symbol]||{}).trend||{}, tb=(store[b.symbol]||{}).trend||{};\n      if(mode==='symbol')return a.symbol<b.symbol?-1:1;\n      if(mode==='direction')return (tb.delta||0)-(ta.delta||0)||((cb.score||0)-(ca.score||0));\n      if(mode==='range')return (cb.atrPct||0)-(ca.atrPct||0);\n      if(mode==='volume')return (cb.relVol||0)-(ca.relVol||0);\n      return (cb.score==null?-1:cb.score)-(ca.score==null?-1:ca.score);\n    });\n    if(filterStatus)list=list.filter(function(r){return bandOf(r.symbol)===filterStatus});\n    drawSuggestions();\n    if(!list.length){qs('#rows').innerHTML='<div class=\"empty\">אין מניות להצגה.</div>';return}\n    qs('#rows').innerHTML=list.map(function(r){\n      var st=store[r.symbol]||{}, c=st.cand||{}, tr=st.trend||{};\n      if(c.score==null){\n        // \"No data\" was covering three different situations, and the difference\n        // decides what to do about it: refill, wait for the collector, or wait\n        // for more sessions to accumulate.\n        var nBars=(st.rows||[]).length;\n        var sess=Object.keys(st.days||{}).length;\n        var shortest=Object.keys(st.days||{}).map(function(d2){return (st.days[d2]||[]).length});\n        var why=nBars===0?'לא בארכיון — נסה למלא'\n          :c.incomplete_sessions?('סשן קטוע — לא מגיע לסגירה ('+Math.max.apply(null,shortest.concat([0]))+' נרות) — ייתוקן בלילה')\n          :sess<2?'רק סשן אחד — נדרשים עוד ימים לדירוג'\n          :'אין מספיק היסטוריה לדירוג';\n        return '<div class=\"row NODATA stale\"><div><div class=\"sym\">'+r.symbol+'</div><div class=\"px\">—</div></div>'+\n        '<div><span class=\"st bg-NODATA\">לא מדורג</span><div class=\"why\">'+why+'</div></div>'+\n        '<div class=\"rt\"><div class=\"score\">—</div>'+\n        (live[r.symbol]?'<span class=\"live\">חי</span>':'<button type=\"button\" class=\"track\" data-s=\"'+r.symbol+'\">למעקב</button>')+\n        '<button type=\"button\" class=\"hist\" data-s=\"'+r.symbol+'\">היסטוריה</button></div></div>';\n      }\n      var band=c.score>=65?'READY':c.score>=50?'CLOSE':c.score>=35?'WATCH':'QUIET';\n      var arrow=tr.direction==='improving'?'<span class=\"dir up\">▲ מתחזק</span>'\n        :tr.direction==='fading'?'<span class=\"dir dn\">▼ נחלש</span>':'';\n      // What the last session did AT the level changes the whole reading, so it\n      // gets its own mark rather than being buried in the reason text.\n      var EV={breakout_held:['פרץ והחזיק','up'],breakout_failed:['פריצה נדחתה','dn'],\n        testing_high:['נבחן ברמה',''],approaching_high:['לפני הכרעה',''],\n        breakdown_held:['שבר למטה','dn'],breakdown_recovered:['שבירה נדחתה','up'],\n        approaching_low:['מעל התמיכה',''],mid_range:['','']};\n      var ev=EV[c.levelEvent]||['',''];\n      var evTag=ev[0]?'<span class=\"ev '+ev[1]+'\">'+ev[0]+'</span>':'';\n      var why=(c.reasons||[]).slice(0,2).map(function(x){return x.text}).join(' · ');\n      var lv=(c.levels||[]).slice(0,3).map(function(x){return x.name+' '+x.price.toFixed(2)}).join(' · ');\n      return '<div class=\"row '+band+'\" data-s=\"'+r.symbol+'\">'+\n        '<div><div class=\"sym\">'+r.symbol+'</div><div class=\"px num\">'+(c.lastClose!=null?c.lastClose.toFixed(2):'—')+'</div></div>'+\n        '<div><div class=\"why\">'+(why||'—')+'</div>'+\n        '<div class=\"meta\"><span>מבנה <b>'+(STRUCT_TXT[c.structure]||c.structure||'—')+'</b></span>'+\n        (c.atrPct!=null?'<span>טווח ממוצע '+(c.avgRangeWindow||5)+'ימ <b>'+c.atrPct+'%</b></span>':'')+\n        (c.lastRangePct!=null?'<span>טווח סשן '+(c.lastDate?c.lastDate.slice(5).replace('-','/'):'')+' <b>'+c.lastRangePct+'%</b></span>':'')+\n        (c.relVol!=null?'<span>מחזור <b>×'+c.relVol+'</b></span>':'')+\n        evTag+arrow+'</div>'+\n        (lv?'<div class=\"lvls2\">רמות למעקב: '+lv+'</div>':'')+\n        (c.capReason?'<div class=\"cap\">'+c.capReason+'</div>':'')+\n        (c.incompleteNote?'<div class=\"cap\">'+c.incompleteNote+'</div>':'')+\n        (c.dataSource==='aggregated'?'<div class=\"cap\">מקור: צבירת נרות דקה — לא נר יומי רשמי</div>':'')+'</div>'+\n        '<div class=\"rt\"><div class=\"score\">'+c.score+'<small>/100</small></div>'+\n        (live[r.symbol]?'<span class=\"live\">חי</span>':'<button type=\"button\" class=\"track\" data-s=\"'+r.symbol+'\">למעקב</button>')+\n        '<button type=\"button\" class=\"hist\" data-s=\"'+r.symbol+'\">היסטוריה</button></div></div>';\n    }).join('');\n    [].forEach.call(document.querySelectorAll('.track'),function(b){b.onclick=function(e){e.stopPropagation();track(b.dataset.s,b)}});\n    [].forEach.call(document.querySelectorAll('.hist'),function(b){b.onclick=function(e){e.stopPropagation();history(b.dataset.s)}});\n  }\n  // Add to the live set. The server pulls the session immediately, so the\n  // radar shows it on its next refresh rather than after the next cron.\n  var live={};\n  function loadWatch(){\n    return j('/watch').then(function(d){ live={}; (d.tracked||[]).forEach(function(s){live[s]=1}); watchRoom=d.room; },function(){});\n  }\n  // Live Interest is a question about the CURRENT session, so it needs today's\n  // bars from D1 — not the archive. Only for symbols actually on the watchlist:\n  // that is the whole point of the split.\n  function loadLiveInterest(){\n    var syms=Object.keys(live);\n    if(!syms.length)return Promise.resolve();\n    return j('/board').then(function(d){\n      var by={};\n      (d.rows||[]).forEach(function(r){ (by[r.symbol]=by[r.symbol]||[]).push(r) });\n      syms.forEach(function(s){\n        var rows=by[s];\n        var st=store[s]||(store[s]={rows:[],days:{}});\n        if(!rows||rows.length<20){ st.liveInterest=null; return; }\n        rows.sort(function(a,b){return a.unix-b.unix});\n        var A=analyze(rows,{K:3,minMove:1.5,runs:3});\n        st.liveInterest=A?liveInterest(A,{}):null;\n      });\n    },function(){});\n  }\n  var watchRoom=null;\n  function track(sym,btn){\n    if(btn){btn.disabled=true;btn.textContent='…'}\n    return j('/watch/add/'+sym).then(function(d){\n      if(d.error){ toast(d.error==='watchlist full'?'המעקב מלא ('+d.max+'). הסר מניה ברדאר קודם.':d.error); if(btn){btn.disabled=false;btn.textContent='למעקב'} return; }\n      live[sym]=1; toast(sym+' נוסף למעקב — יופיע ברדאר ברענון הבא'); render();\n    },function(e){ toast('לא הצלחתי להוסיף: '+e); if(btn){btn.disabled=false;btn.textContent='למעקב'} });\n  }\n\n  // The date picker lists what the ARCHIVE holds, newest first.\n  function drawDates(){\n    var ds=dates.slice();\n    if(!ds.length)ds=[shownDate||expectedSessionDate()];\n    qs('#date').innerHTML=ds.map(function(x){return '<option value=\"'+x+'\"'+(x===shownDate?' selected':'')+'>'+x.slice(5).replace('-','/')+'</option>'}).join('');\n  }\n\n  // Every day the archive holds for one symbol: counts, and a way to take the\n  // raw candles away — copy for pasting, or the CSV file.\n  function history(sym){\n    var sheet=qs('#sheet'), panel=qs('#hpanel');\n    sheet.hidden=false;\n    panel.innerHTML='<div class=\"hhead\"><b>'+sym+'</b><button type=\"button\" id=\"hclose\">סגור</button></div><div class=\"empty\">טוען…</div>';\n    qs('#hclose').onclick=function(){sheet.hidden=true};\n    j('/archive/check/'+sym).then(function(d){\n      var days=(d.detail||[]).slice().reverse();\n      panel.innerHTML='<div class=\"hhead\"><b>'+sym+'</b><span class=\"hsum\">'+d.days+' ימים · '+d.bars.toLocaleString()+' נרות</span>'+\n        '<div class=\"spacer\"></div><button type=\"button\" id=\"hclose\">סגור</button></div>'+\n        (days.length?'<div class=\"hdays\">'+days.map(function(x){\n          return '<div class=\"hd\"><span>'+x.date+'</span><b class=\"num\">'+x.bars+'</b>'+\n            '<button type=\"button\" class=\"hcopy\" data-s=\"'+sym+'\" data-d=\"'+x.date+'\">העתק</button>'+\n            '<a class=\"hdl\" href=\"/export/'+sym+'/'+x.date+'\">קובץ</a></div>';\n        }).join('')+'</div>':'<div class=\"empty\">אין ימים בארכיון עבור '+sym+'</div>')+\n        '<div class=\"hall\"><button type=\"button\" id=\"copyAll\" data-s=\"'+sym+'\">העתק את כל הימים</button>'+\n        '<a class=\"hdl\" href=\"/export/'+sym+'\">הורד הכל כקובץ</a></div>';\n      qs('#hclose').onclick=function(){sheet.hidden=true};\n      [].forEach.call(document.querySelectorAll('.hcopy'),function(b){b.onclick=function(){copyDay(b.dataset.s,b.dataset.d,b)}});\n      var ca=qs('#copyAll'); if(ca)ca.onclick=function(){copyDay(ca.dataset.s,null,ca)};\n    },function(e){ panel.innerHTML='<div class=\"hhead\"><b>'+sym+'</b><button type=\"button\" id=\"hclose\">סגור</button></div>'+\n      '<div class=\"empty\">לא הצלחתי לקרוא: '+e+'</div>'; qs('#hclose').onclick=function(){sheet.hidden=true}; });\n  }\n  var CSV='symbol,date,time,open,high,low,close,volume';\n  function copyDay(sym,date,btn){\n    var label=btn?btn.textContent:''; if(btn){btn.disabled=true;btn.textContent='…'}\n    // The same endpoint the download link uses, so copy and file can never\n    // disagree about where the history came from.\n    var u='/export/'+sym+(date?'/'+date:'');\n    return fetch(bust(u),{cache:'no-store'}).then(function(r){return r.text()}).then(function(txt){\n      var rows=txt.trim().split('\\n').slice(1);\n      var done=function(){toast(rows.length.toLocaleString()+' נרות הועתקו')};\n      (navigator.clipboard?navigator.clipboard.writeText(txt):Promise.reject()).then(done,function(){prompt('העתק:',txt)});\n      if(btn){btn.disabled=false;btn.textContent=label}\n    },function(e){ toast('לא הצלחתי: '+e); if(btn){btn.disabled=false;btn.textContent=label} });\n  }\n\n  // Filling the gaps should be one button, not seven URLs pasted by hand. The\n  // page knows which symbols have nothing for this day, so it asks for exactly\n  // those, ten at a time, and reports progress.\n  function fillMissing(){\n    var btn=qs('#fill');\n    try{\n      var missing=symbols.filter(function(s){return !store[s]||!store[s].rows.length});\n      if(!missing.length){setFill('כל המניות כבר בארכיון');return Promise.resolve()}\n      btn.disabled=true;\n      var total=missing.length, done=0, failed=[], lastErr=null;\n      // Each batch is a real upstream pull of ~20 seconds, so the button has to\n      // say what is happening — a page that looks frozen is indistinguishable\n      // from one that is doing nothing.\n      var step=function(){\n        if(!missing.length)return Promise.resolve();\n        var batch=missing.splice(0,10);\n        btn.textContent='ממלא '+done+'/'+total+'…';\n        setFill('מושך '+batch.join(', ')+' מיאהו — כ-20 שניות לקבוצה');\n        return j('/archive/fill/'+batch.join(',')).then(function(d){\n          if(d&&d.error){ lastErr=d.error; failed=failed.concat(batch); }\n          else {\n            (d.filled||[]).forEach(function(x){ if(x.error){failed.push(x.symbol);lastErr=x.error} });\n            (d.skipped||[]).forEach(function(s){ missing.push(s); });\n          }\n          done+=batch.length;\n          return step();\n        },function(e){ lastErr=String(e); failed=failed.concat(batch); done+=batch.length; return step(); });\n      };\n      return step().then(function(){\n        btn.disabled=false;\n        setFill(failed.length?('נכשלו '+failed.length+': '+failed.slice(0,4).join(', ')+(lastErr?' — '+lastErr:'')):'הושלם — טוען מחדש');\n        return loadAll();\n      },function(e){ btn.disabled=false; setFill('נכשל: '+String(e)); });\n    }catch(e){ if(btn)btn.disabled=false; setFill('נכשל: '+String(e&&e.message||e)); return Promise.resolve(); }\n  }\n  // A visible line, not a toast that vanishes in two seconds while a two-minute\n  // job runs behind it.\n  function setFill(msg){ var el=qs('#fillmsg'); if(el){el.hidden=!msg;el.textContent=msg||''} }\n  qs('#fill').onclick=fillMissing;\n\n  // The point of the scanner: surface what changed, so the 100 names do not have\n  // to be read one by one during the session.\n  // The names actually worth refining: whatever ranks highest right now.\n  function list40(){\n    return symbols.filter(function(s){return store[s]&&store[s].cand&&store[s].cand.score!=null})\n      .sort(function(a,b){return store[b].cand.score-store[a].cand.score}).slice(0,40);\n  }\n  function drawSuggestions(){\n    var el=qs('#sugg'); if(!el)return;\n    var scanList=symbols.filter(function(s){return !live[s]}).map(function(s){\n      return {symbol:s,candidate:(store[s]||{}).cand,trend:(store[s]||{}).trend}});\n    var watchList=Object.keys(live).map(function(s){\n      return {symbol:s,candidate:(store[s]||{}).cand,live:(store[s]||{}).liveInterest||null}})\n      .filter(function(x){return x.live});\n    var g=suggestions(watchList,scanList,{max:40});\n    if(!g.promote.length&&!g.demote.length){el.hidden=true;return}\n    el.hidden=false;\n    el.innerHTML='<div class=\"sh\">מועמדים חדשים למעקב</div>'+\n      (g.promote.length?g.promote.slice(0,5).map(function(p){\n        return '<div class=\"sg\"><div class=\"sgtop\"><b>'+p.symbol+'</b><span class=\"sgs\">'+p.score+'/100</span>'+\n          (p.direction==='improving'?'<span class=\"dir up\">▲ מתחזק</span>':'')+'</div>'+\n          '<div class=\"sgw\">'+p.why.join(' · ')+'</div>'+\n          (p.directionText?'<div class=\"sgw\">'+p.directionText+'</div>':'')+\n          (p.insteadOf?'<div class=\"sgsw\">המעקב מלא — שקול להסיר <b>'+p.insteadOf+'</b> ('+p.insteadOfWhy+')</div>':'')+\n          '<button type=\"button\" class=\"track\" data-s=\"'+p.symbol+'\">הוסף למעקב</button></div>';\n      }).join(''):'<div class=\"sgnone\">אין מועמדים חדשים כרגע</div>')+\n      (g.demote.length?'<div class=\"sh\">כבר לא מעניינים במעקב</div>'+g.demote.slice(0,5).map(function(x){\n        return '<div class=\"sg\"><div class=\"sgtop\"><b>'+x.symbol+'</b><span class=\"sgs\">'+x.score+'/100</span></div>'+\n          '<div class=\"sgw\">'+x.why.join(' · ')+'</div>'+\n          '<div class=\"sgsw\">שקול להסיר — ההחלטה שלך</div></div>';\n      }).join(''):'')+\n      '<div class=\"sgnote\">המערכת ממליצה בלבד. שום מניה לא נכנסת או יוצאת מהמעקב מעצמה.</div>';\n    [].forEach.call(el.querySelectorAll('.track'),function(b){b.onclick=function(){track(b.dataset.s,b)}});\n  }\n\n  qs('#sort').onchange=render;\n  qs('#date').onchange=function(){viewDate=qs('#date').value;loadAll()};\n  qs('#reload').onclick=function(){loadWatch().then(loadAll).then(function(){return loadLiveInterest()}).then(function(){render();toast('עודכן')})};\n  // The archive moves on a ~50 minute cycle, so polling harder than every five\n  // minutes buys nothing.\n  // The archive moves on a ~50 minute cycle, but live interest is about the\n  // current session and has to keep up with it.\n  setInterval(function(){ if(!document.hidden)loadAll(); },5*60*1000);\n  setInterval(function(){ if(!document.hidden)loadLiveInterest().then(render); },60*1000);\n\n  loadWatch().then(loadAll).then(function(){return loadLiveInterest()}).then(render);\n})();\n</script>\n</body>\n</html>\n";
export const BARS_HTML = "<!doctype html>\n<html lang=\"he\" dir=\"rtl\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width,initial-scale=1,viewport-fit=cover\">\n<title>נרות שמורים</title>\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link href=\"https://fonts.googleapis.com/css2?family=Heebo:wght@400;600;800&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{--paper:#F2F4F7;--ink:#1B2430;--muted:#5B6673;--rule:#D6DBE2;--well:#FFFFFF;--up:#0F766E;--down:#B42318;--flat:#8A94A0}\n  *{box-sizing:border-box}\n  html,body{margin:0;background:var(--paper);color:var(--ink);font-family:Heebo,system-ui,sans-serif;font-variant-numeric:tabular-nums;-webkit-text-size-adjust:100%;max-width:100%;overflow-x:hidden}\n  body{padding-bottom:calc(24px + env(safe-area-inset-bottom))}\n  header{position:sticky;top:0;z-index:5;background:var(--paper);border-bottom:1px solid var(--rule)}\n  .top{display:flex;align-items:center;gap:8px;padding:12px 14px 8px;flex-wrap:wrap}\n  .top h1{font-size:18px;font-weight:800;margin:0}\n  .spacer{flex:1}\n  .build{font-size:10px;color:var(--muted);direction:ltr;unicode-bidi:isolate}\n  a.back{font-size:13px;color:var(--ink);text-decoration:none;background:var(--well);border:1px solid var(--rule);border-radius:9px;padding:6px 10px}\n  .pick{display:flex;gap:8px;padding:0 14px 10px;flex-wrap:wrap;align-items:center}\n  select,input{font:inherit;font-size:15px;background:var(--well);color:var(--ink);border:1px solid var(--rule);border-radius:10px;padding:8px 10px;min-width:0}\n  select{max-width:46vw}\n  input{width:120px}\n  .sum{padding:0 14px 10px;font-size:12.5px;color:var(--muted);line-height:1.6}\n  .sum b{color:var(--ink);font-weight:600;direction:ltr;unicode-bidi:isolate}\n  .days{display:flex;gap:6px;overflow-x:auto;padding:0 14px 10px;scrollbar-width:none}\n  .days::-webkit-scrollbar{display:none}\n  .day{flex:none;background:var(--well);border:1px solid var(--rule);border-radius:9px;padding:6px 10px;font-size:13px;color:var(--ink);white-space:nowrap}\n  .day.on{background:var(--ink);color:var(--paper);border-color:var(--ink)}\n  .day small{display:block;font-size:10.5px;color:var(--muted);text-align:center}\n  .day.on small{color:var(--paper);opacity:.8}\n  .day.short{border-color:#E4B4AD}\n  table{width:100%;border-collapse:collapse;direction:ltr;font-size:13px}\n  th{position:sticky;top:0;background:var(--paper);color:var(--muted);font-weight:600;text-align:right;padding:8px 6px;border-bottom:1px solid var(--rule);font-size:11.5px}\n  td{padding:6px;text-align:right;border-bottom:1px solid var(--rule);white-space:nowrap}\n  th:first-child,td:first-child{text-align:left;padding-left:14px;font-weight:600}\n  th:last-child,td:last-child{padding-right:14px}\n  tr.up td.c{color:var(--up)} tr.down td.c{color:var(--down)}\n  tr.quiet td{color:var(--muted)}\n  tr.gap td{background:#FBEAE7}\n  .msg{padding:40px 16px;text-align:center;color:var(--muted);line-height:1.6}\n  .msg b{color:var(--ink)}\n  .acts{display:flex;gap:8px;padding:10px 14px;flex-wrap:wrap}\n  button{font:inherit;font-size:13px;background:var(--well);border:1px solid var(--rule);color:var(--ink);border-radius:10px;padding:7px 12px}\n  button.dark{background:var(--ink);color:var(--paper);border-color:var(--ink)}\n  .range{margin:0 14px 10px;background:var(--well);border:1px solid var(--rule);border-radius:12px;padding:12px}\n  .range[hidden]{display:none}\n  .range label{font-size:12.5px;color:var(--muted);display:block}\n  .range .r2{display:grid;grid-template-columns:1fr 1fr;gap:8px}\n  .range input[type=date]{width:100%;margin-top:3px}\n  .range .est{font-size:13px;line-height:1.6;margin-top:10px;padding:9px 11px;background:var(--paper);border-radius:9px}\n  .range .est b{font-weight:800}\n  .range .est.warn{background:#FBEED0;color:#8A5B12} .range .est.bad{background:#F6DAD6;color:var(--down)}\n  .range .r3{display:flex;gap:8px;margin-top:10px}\n  .prog{margin:0 14px 10px;padding:9px 12px;background:var(--well);border:1px solid var(--rule);border-radius:10px;font-size:13px}\n  .prog[hidden]{display:none}\n  .tabs{display:flex;gap:6px;padding:0 14px 10px}\n  .tab{font:inherit;font-size:13.5px;background:none;border:1px solid var(--rule);color:var(--muted);\n    border-radius:9px;padding:7px 13px}\n  .tab.on{background:var(--ink);color:var(--paper);border-color:var(--ink);font-weight:700}\n  tr.part td{background:#FBEED0}\n  td.pos{color:var(--up)} td.neg{color:var(--down)}\n  th.s{cursor:pointer;user-select:none;white-space:nowrap}\n  th.s:hover{color:var(--ink)}\n  th.s.on{color:var(--ink);font-weight:800}\n  th.s .ar{font-size:9px;margin-right:3px;opacity:.8}\n  .srcp{font-size:10px;font-weight:700;color:var(--muted);border:1px solid var(--rule);border-radius:4px;padding:0 4px}\n  .toast{position:fixed;bottom:24px;right:50%;transform:translateX(50%);background:var(--ink);color:var(--paper);padding:9px 16px;border-radius:10px;font-size:13px;z-index:60}\n  .toast[hidden]{display:none}\n  @media(min-width:720px){ .wrap{max-width:900px;margin:0 auto} }\n</style>\n</head>\n<body>\n<div class=\"wrap\">\n<header>\n  <div class=\"top\">\n    <h1>נרות שמורים</h1>\n    <div class=\"spacer\"></div>\n    <a class=\"back\" href=\"/radar\">רדאר</a>\n    <span class=\"build\">v138  (2026-09-05 16:01Z)</span>\n  </div>\n  <div class=\"tabs\">\n    <button type=\"button\" class=\"tab on\" id=\"tabDay\">נרות דקה</button>\n    <button type=\"button\" class=\"tab\" id=\"tabDaily\">מחירים יומיים</button>\n  </div>\n  <div class=\"pick\" id=\"pickMin\">\n    <select id=\"sym\" aria-label=\"סימבול\"><option>טוען…</option></select>\n    <input id=\"find\" placeholder=\"חפש סימבול\" autocomplete=\"off\" autocapitalize=\"characters\">\n    <span id=\"count\" style=\"font-size:12.5px;color:var(--muted)\"></span>\n  </div>\n  <div class=\"days\" id=\"days\"></div>\n  <div class=\"sum\" id=\"sum\"></div>\n  <div class=\"pick\" id=\"pickDaily\" hidden>\n    <input id=\"dSym\" placeholder=\"סנן סימבול\" autocomplete=\"off\" style=\"width:110px;text-transform:uppercase\">\n    <input id=\"dFrom\" type=\"date\" style=\"width:140px\">\n    <input id=\"dTo\" type=\"date\" style=\"width:140px\">\n    <button type=\"button\" id=\"dGo\" class=\"dark\">הצג</button>\n    <span id=\"dCount\" style=\"font-size:12.5px;color:var(--muted)\"></span>\n  </div>\n</header>\n<div class=\"acts\">\n  <button type=\"button\" id=\"copy\" class=\"dark\">העתק CSV</button>\n  <button type=\"button\" id=\"order\">ישן למעלה</button>\n  <button type=\"button\" id=\"dl\">הורד יום</button>\n  <button type=\"button\" id=\"dlAll\">הורד כל ההיסטוריה של הסימבול</button>\n  <button type=\"button\" id=\"dlRange\">הורד הכל לטווח תאריכים</button>\n</div>\n<div class=\"range\" id=\"range\" hidden>\n  <div class=\"r2\">\n    <label>מתאריך<input type=\"date\" id=\"rFrom\"></label>\n    <label>עד תאריך<input type=\"date\" id=\"rTo\"></label>\n  </div>\n  <label style=\"margin-top:8px\"><input type=\"checkbox\" id=\"rTracked\"> רק מניות במעקב חי (במקום כל הסימבולים)</label>\n  <div class=\"est\" id=\"rEst\"></div>\n  <div class=\"r3\">\n    <button type=\"button\" id=\"rGo\" class=\"dark\">בדוק גודל</button>\n    <button type=\"button\" id=\"rCancel\">ביטול</button>\n  </div>\n</div>\n<div class=\"prog\" id=\"prog\" hidden></div>\n<div id=\"tbl\"><div class=\"msg\">בחר סימבול</div></div>\n<div id=\"dailyWrap\" hidden>\n  <div class=\"acts\">\n    <button type=\"button\" id=\"dCopy\" class=\"dark\">העתק CSV</button>\n    <button type=\"button\" id=\"dDl\">הורד CSV</button>\n    <button type=\"button\" id=\"dCmp\">הצג השוואה לספק</button>\n  </div>\n  <div id=\"dTbl\"><div class=\"msg\">לחץ \"הצג\"</div></div>\n</div>\n</div>\n<div class=\"toast\" id=\"toast\" hidden></div>\n\n<script>\n(function(){\n  var qs=function(s){return document.querySelector(s)};\n  var SYM='', DATE='', rows=[], days=[], newestFirst=false, all=[];\n  var mSort='time', mDir=-1;\n  function j(u){return fetch(u+(u.indexOf('?')>0?'&':'?')+'ts='+Date.now(),{cache:'no-store'}).then(function(r){return r.json()})}\n  function toast(m){var t=qs('#toast');t.textContent=m;t.hidden=false;clearTimeout(toast.h);toast.h=setTimeout(function(){t.hidden=true},1600)}\n  function fmtK(v){return v>=1e6?(v/1e6).toFixed(2)+'M':v>=1e3?Math.round(v/1e3)+'K':String(v)}\n\n  // 1. every symbol that exists anywhere\n  function loadIndex(){\n    return j('/bars/index').then(function(d){\n      all=d.symbols||[]; tracked=d.tracked||[];\n      qs('#count').textContent=all.length+' סימבולים · '+(d.tracked||[]).length+' במעקב חי';\n      drawSymbols(all);\n      var want=(location.hash||'').replace('#','').toUpperCase();\n      SYM=all.indexOf(want)>=0?want:(all[0]||'');\n      qs('#sym').value=SYM;\n      if(SYM)return loadDays();\n    });\n  }\n  function drawSymbols(list){\n    qs('#sym').innerHTML=list.map(function(s){return '<option'+(s===SYM?' selected':'')+'>'+s+'</option>'}).join('')||'<option>אין</option>';\n  }\n  // 2. which days this symbol has, and how complete each is\n  function loadDays(){\n    qs('#days').innerHTML='';qs('#sum').textContent='טוען ימים…';\n    return j('/days/'+SYM).then(function(d){\n      days=(d.days||[]);\n      if(!days.length){qs('#sum').textContent='אין ימים שמורים ל-'+SYM;qs('#tbl').innerHTML='<div class=\"msg\"><b>אין נרות ל-'+SYM+'</b></div>';return}\n      DATE=days[0].date;\n      drawDays(); return loadDay();\n    });\n  }\n  function drawDays(){\n    qs('#days').innerHTML=days.map(function(d){\n      var n=d.bars||0, short=n>0&&n<370;\n      return '<button type=\"button\" class=\"day'+(d.date===DATE?' on':'')+(short?' short':'')+'\" data-d=\"'+d.date+'\">'+\n        d.date.slice(5).replace('-','/')+'<small>'+n+' נרות'+(short?' ✗':'')+'</small></button>';\n    }).join('');\n    qs('#days').onclick=function(e){var b=e.target.closest('.day');if(!b)return;DATE=b.dataset.d;drawDays();loadDay()};\n  }\n  // 3. the candles of one day. /day merges D1 and the archive itself.\n  function loadDay(){\n    qs('#tbl').innerHTML='<div class=\"msg\">טוען '+SYM+' · '+DATE+'…</div>';\n    return j('/day/'+SYM+'/'+DATE+'?format=json').then(function(d){\n      rows=(d.rows||[]).slice().sort(function(a,b){return a.unix-b.unix});\n      history.replaceState(null,'','/bars#'+SYM);\n      render(d);\n    },function(e){qs('#tbl').innerHTML='<div class=\"msg\"><b>שגיאה</b><br>'+String(e)+'</div>'});\n  }\n  function render(d){\n    if(!rows.length){qs('#sum').textContent='אין נרות ל-'+SYM+' ב-'+DATE;qs('#tbl').innerHTML='<div class=\"msg\"><b>ריק</b></div>';return}\n    var first=rows[0], last=rows[rows.length-1];\n    var hi=Math.max.apply(null,rows.map(function(r){return r.high})), lo=Math.min.apply(null,rows.map(function(r){return r.low}));\n    var vol=rows.reduce(function(a,r){return a+r.volume},0);\n    var quiet=rows.filter(function(r){return r.volume===0&&r.high===r.low}).length;\n    // holes: consecutive bars more than a minute apart, inside the session\n    var holes=0; for(var i=1;i<rows.length;i++){ if(rows[i].unix-rows[i-1].unix>60) holes+=Math.round((rows[i].unix-rows[i-1].unix)/60)-1; }\n    qs('#sum').innerHTML='<b>'+SYM+'</b> · '+DATE+' · <b>'+rows.length+'</b> נרות · '+first.time+'–'+last.time+\n      ' · פתיחה <b>'+first.open.toFixed(2)+'</b> גבוה <b>'+hi.toFixed(2)+'</b> נמוך <b>'+lo.toFixed(2)+'</b> סגירה <b>'+last.close.toFixed(2)+'</b>'+\n      ' · נפח <b>'+fmtK(vol)+'</b>'+(quiet?' · '+quiet+' דקות ללא עסקה':'')+(holes?' · <span style=\"color:var(--down)\">'+holes+' דקות חסרות</span>':'')+\n      (d.source?' · מקור: '+d.source:'');\n    var MCOL={time:{t:'שעה',v:function(r){return r.time}},open:{t:'פתיחה',v:function(r){return r.open}},\n      high:{t:'גבוה',v:function(r){return r.high}},low:{t:'נמוך',v:function(r){return r.low}},\n      close:{t:'סגירה',v:function(r){return r.close}},volume:{t:'נפח',v:function(r){return r.volume}}};\n    // Sorting by time keeps the existing newest/oldest toggle as its direction,\n    // so the two controls agree instead of fighting each other.\n    var order;\n    if(mSort==='time'){ order=newestFirst?rows.slice().reverse():rows.slice(); }\n    else {\n      var col=MCOL[mSort];\n      order=rows.slice().sort(function(a,b){\n        var x=col.v(a),y=col.v(b);\n        if(x==null&&y==null)return 0; if(x==null)return 1; if(y==null)return -1;\n        return typeof x==='number'?(x-y)*mDir:String(x).localeCompare(String(y))*mDir;\n      });\n    }\n    var mth=function(k){\n      var on=mSort===k, arrow=k==='time'?(newestFirst?'▼':'▲'):(mDir<0?'▼':'▲');\n      return '<th class=\"s'+(on?' on':'')+'\" data-c=\"'+k+'\">'+MCOL[k].t+(on?'<span class=\"ar\">'+arrow+'</span>':'')+'</th>';\n    };\n    var h='<table><thead><tr>'+['time','open','high','low','close','volume'].map(mth).join('')+'</tr></thead><tbody>';\n    order.forEach(function(r,i){\n      var idx=rows.indexOf(r);\n      var gap=idx>0&&rows[idx].unix-rows[idx-1].unix>60;\n      var cls=(r.close>r.open?'up':r.close<r.open?'down':'flat')+((r.volume===0&&r.high===r.low)?' quiet':'')+(gap?' gap':'');\n      h+='<tr class=\"'+cls+'\"><td>'+r.time+'</td><td>'+r.open.toFixed(2)+'</td><td>'+r.high.toFixed(2)+'</td><td>'+r.low.toFixed(2)+'</td><td class=\"c\">'+r.close.toFixed(2)+'</td><td>'+fmtK(r.volume)+'</td></tr>';\n    });\n    qs('#tbl').innerHTML=h+'</tbody></table>';\n    var mh=qs('#tbl').querySelector('thead');\n    if(mh)mh.onclick=function(e){\n      var c=e.target.closest('.s'); if(!c)return;\n      var k=c.dataset.c;\n      if(k==='time'){ mSort='time'; newestFirst=!newestFirst;\n        qs('#order').textContent=newestFirst?'חדש למעלה':'ישן למעלה'; }\n      else if(mSort===k)mDir=-mDir;\n      else {mSort=k;mDir=-1}\n      render({});\n    };\n  }\n  function csv(){\n    return ['symbol,date,time,open,high,low,close,volume'].concat(rows.map(function(r){\n      return [SYM,DATE,r.time,r.open,r.high,r.low,r.close,r.volume].join(',');\n    })).join('\\n');\n  }\n  qs('#sym').onchange=function(){SYM=this.value;loadDays()};\n  qs('#find').oninput=function(){\n    var q=this.value.trim().toUpperCase();\n    var list=q?all.filter(function(s){return s.indexOf(q)===0}):all;\n    drawSymbols(list);\n    if(list.length&&list.indexOf(SYM)<0){SYM=list[0];qs('#sym').value=SYM;loadDays()}\n  };\n  qs('#order').onclick=function(){newestFirst=!newestFirst;mSort='time';\n    this.textContent=newestFirst?'חדש למעלה':'ישן למעלה';render({})};\n  qs('#copy').onclick=function(){\n    if(!rows.length)return;\n    var t=csv();\n    (navigator.clipboard?navigator.clipboard.writeText(t):Promise.reject()).then(function(){toast('הועתק '+rows.length+' נרות')},function(){prompt('העתק:',t)});\n  };\n  qs('#dl').onclick=function(){\n    if(!rows.length)return;\n    var blob=new Blob([csv()],{type:'text/csv'}), a=document.createElement('a');\n    a.href=URL.createObjectURL(blob); a.download=SYM+'_'+DATE+'.csv'; a.click();\n  };\n  // ---- bulk downloads. The count comes BEFORE any download, and above a\n  // threshold the download needs a second tap. Excel stops at 1,048,576 rows;\n  // a phone browser struggles well before that.\n  var EXCEL_MAX=1048576, HEAVY=300000;\n  var tracked=[];\n  function tradingDaysBetween(a,b){var n=0;for(var d=new Date(a+'T12:00:00Z');d<=new Date(b+'T12:00:00Z');d.setUTCDate(d.getUTCDate()+1)){var w=d.getUTCDay();if(w!==0&&w!==6)n++}return n}\n  function sizeVerdict(rows){\n    if(rows>EXCEL_MAX)return {cls:'bad',text:'<b>'+rows.toLocaleString()+' שורות</b> — יותר ממה ש-Excel יכול לפתוח ('+EXCEL_MAX.toLocaleString()+'). אפשר להוריד, אבל תצטרך כלי אחר לקרוא את זה.'};\n    if(rows>HEAVY)return {cls:'warn',text:'<b>'+rows.toLocaleString()+' שורות</b> — כבד לטלפון. ההורדה עלולה להיות איטית או להיכשל.'};\n    return {cls:'',text:'<b>'+rows.toLocaleString()+' שורות</b> — בסדר להורדה.'};\n  }\n  function fetchCsv(sym,from,to){\n    var u='/bars/export/'+sym+(from?'?from='+from:'')+(to?(from?'&':'?')+'to='+to:'');\n    return fetch(u,{cache:'no-store'}).then(function(r){return r.text()}).then(function(t){\n      var lines=t.split('\\n'); return lines.slice(1).filter(Boolean);   // drop the header\n    });\n  }\n  function saveCsv(name,lines){\n    var blob=new Blob(['symbol,date,time,open,high,low,close,volume\\n'+lines.join('\\n')],{type:'text/csv'});\n    var a=document.createElement('a'); a.href=URL.createObjectURL(blob); a.download=name; a.click();\n    setTimeout(function(){URL.revokeObjectURL(a.href)},5000);\n  }\n  function progress(msg){var p=qs('#prog');p.hidden=!msg;p.textContent=msg||''}\n\n  // one symbol, every day it has\n  qs('#dlAll').onclick=function(){\n    if(!SYM||!days.length){toast('אין ימים');return}\n    var est=days.reduce(function(s,d){return s+(d.bars||0)},0);\n    var v=sizeVerdict(est);\n    if(!confirm(SYM+' · '+days.length+' ימים · '+v.text.replace(/<[^>]+>/g,'')+'\\n\\nלהוריד?'))return;\n    progress('מוריד '+SYM+'…');\n    fetchCsv(SYM,days[days.length-1].date,days[0].date).then(function(lines){\n      progress(''); saveCsv(SYM+'_all_'+days[days.length-1].date+'_'+days[0].date+'.csv',lines);\n      toast('הורדו '+lines.length.toLocaleString()+' שורות');\n    },function(e){progress('');toast('נכשל: '+e)});\n  };\n\n  // every symbol, a date range\n  qs('#dlRange').onclick=function(){\n    var r=qs('#range'); r.hidden=!r.hidden; qs('#rEst').textContent=''; qs('#rEst').className='est';\n    if(!qs('#rTo').value){var t=new Date();qs('#rTo').value=t.toISOString().slice(0,10);\n      var f=new Date(t.getTime()-7*86400000);qs('#rFrom').value=f.toISOString().slice(0,10)}\n    qs('#rGo').textContent='בדוק גודל'; qs('#rGo').dataset.ready='';\n  };\n  qs('#rCancel').onclick=function(){qs('#range').hidden=true};\n  qs('#rGo').onclick=function(){\n    var from=qs('#rFrom').value,to=qs('#rTo').value;\n    if(!from||!to||from>to){toast('טווח תאריכים לא תקין');return}\n    var syms=qs('#rTracked').checked?tracked:all;\n    if(!syms.length){toast('אין סימבולים');return}\n    var est=qs('#rEst'), go=qs('#rGo');\n    // second tap: the count was shown and accepted\n    if(go.dataset.ready==='1'){ return downloadRange(syms,from,to); }\n    est.className='est'; est.textContent='בודק…';\n    j('/bars/count?symbols='+syms.join(',')+'&from='+from+'&to='+to).then(function(c){\n      var rows=c.estimate_total||0, v=sizeVerdict(rows);\n      est.className='est '+v.cls;\n      est.innerHTML=syms.length+' סימבולים · '+c.trading_days_in_range+' ימי מסחר בטווח<br>'+v.text+\n        '<br><small>'+(c.d1_rows_exact?'מדויק מ-D1: '+c.d1_rows_exact.toLocaleString()+' · ':'')+'הערכה לארכיון: '+c.archive_rows_estimate.toLocaleString()+' (חסם עליון)</small>';\n      go.textContent=rows>EXCEL_MAX?'הורד בכל זאת':'הורד עכשיו'; go.dataset.ready='1';\n    },function(e){est.className='est bad';est.textContent='לא הצלחתי לספור: '+e});\n  };\n  function downloadRange(syms,from,to){\n    var acc=[], i=0, failed=[];\n    qs('#rGo').disabled=true;\n    var step=function(){\n      if(i>=syms.length){\n        qs('#rGo').disabled=false; progress('');\n        if(!acc.length){toast('לא נמצאו נרות בטווח');return}\n        saveCsv('all_'+from+'_'+to+'.csv',acc);\n        toast('הורדו '+acc.length.toLocaleString()+' שורות מ-'+(syms.length-failed.length)+' סימבולים'+(failed.length?' · נכשלו: '+failed.join(','):''));\n        return;\n      }\n      var s=syms[i++];\n      progress('מוריד '+s+' ('+i+'/'+syms.length+') · '+acc.length.toLocaleString()+' שורות עד כה');\n      fetchCsv(s,from,to).then(function(lines){lines.forEach(function(l){acc.push(l)});step()},function(){failed.push(s);step()});\n    };\n    step();\n  }\n\n  // ---- daily prices: one row per symbol per day -------------------------\n  var dRows=[], showCmp=false;\n  // Sorting is a VIEW over the loaded rows: the fetch is untouched, so a sort\n  // never costs a request and never changes what was loaded.\n  var dSort='date', dDir=-1;\n  var DCOL={\n    symbol:{t:'סימבול',v:function(r){return r.symbol}},\n    date:{t:'תאריך',v:function(r){return r.date}},\n    open:{t:'פתיחה',v:function(r){return r.open}},\n    high:{t:'גבוה',v:function(r){return r.high}},\n    low:{t:'נמוך',v:function(r){return r.low}},\n    close:{t:'סגירה',v:function(r){return r.close}},\n    chg:{t:'שינוי',v:function(r){return (r.open!=null&&r.close!=null)?(r.close-r.open)/r.open*100:null}},\n    volume:{t:'נפח',v:function(r){return r.volume}},\n    bars:{t:'נרות',v:function(r){return r.bars||0}},\n    pclose:{t:'ספק סגירה',v:function(r){return r.provider?r.provider.close:null}},\n    gap:{t:'פער',v:function(r){return (r.provider&&r.close!=null)?r.provider.close-r.close:null}}\n  };\n  function sortedDaily(){\n    var col=DCOL[dSort]; if(!col)return dRows.slice();\n    return dRows.slice().sort(function(a,b){\n      var x=col.v(a), y=col.v(b);\n      // Missing values sink to the bottom in either direction. A null is not a\n      // small number, and sorting by change should not put unknowns on top.\n      if(x==null&&y==null)return 0;\n      if(x==null)return 1;\n      if(y==null)return -1;\n      if(typeof x==='number'&&typeof y==='number')return (x-y)*dDir;\n      return String(x).localeCompare(String(y))*dDir;\n    });\n  }\n  function th(key){\n    var on=dSort===key;\n    return '<th class=\"s'+(on?' on':'')+'\" data-c=\"'+key+'\">'+DCOL[key].t+\n      (on?'<span class=\"ar\">'+(dDir<0?'▼':'▲')+'</span>':'')+'</th>';\n  }\n  function switchTab(daily){\n    qs('#tabDay').className='tab'+(daily?'':' on');\n    qs('#tabDaily').className='tab'+(daily?' on':'');\n    ['#pickMin','#days','#sum','#tbl'].forEach(function(s){var e=qs(s);if(e)e.hidden=daily});\n    document.querySelectorAll('.acts')[0].hidden=daily;\n    qs('#pickDaily').hidden=!daily;\n    qs('#dailyWrap').hidden=!daily;\n  }\n  qs('#tabDay').onclick=function(){switchTab(false)};\n  qs('#tabDaily').onclick=function(){\n    switchTab(true);\n    if(!qs('#dTo').value){\n      var t=new Date(); qs('#dTo').value=t.toISOString().slice(0,10);\n      qs('#dFrom').value=new Date(t.getTime()-30*86400000).toISOString().slice(0,10);\n    }\n    if(!dRows.length)loadDaily();\n  };\n  function loadDaily(){\n    var q=[], s=(qs('#dSym').value||'').trim().toUpperCase(),\n        f=qs('#dFrom').value, t=qs('#dTo').value;\n    if(s)q.push('symbols='+s);\n    if(f)q.push('from='+f);\n    if(t)q.push('to='+t);\n    qs('#dTbl').innerHTML='<div class=\"msg\">טוען…</div>';\n    return j('/bars/daily'+(q.length?'?'+q.join('&'):'')).then(function(d){\n      dRows=d.rows||[]; drawDaily();\n    },function(e){qs('#dTbl').innerHTML='<div class=\"msg\">שגיאה: '+e+'</div>'});\n  }\n  function drawDaily(){\n    if(!dRows.length){qs('#dTbl').innerHTML='<div class=\"msg\"><b>אין נתונים בטווח</b></div>';qs('#dCount').textContent='';return}\n    var syms={}; dRows.forEach(function(r){syms[r.symbol]=1});\n    qs('#dCount').textContent=dRows.length+' שורות · '+Object.keys(syms).length+' סימבולים';\n    var h='<table><thead><tr>'+\n      ['symbol','date','open','high','low','close','chg','volume','bars'].map(th).join('')+\n      (showCmp?th('pclose')+th('gap'):'')+'</tr></thead><tbody>';\n    sortedDaily().forEach(function(r){\n      var chg=(r.open!=null&&r.close!=null)?(r.close-r.open)/r.open*100:null;\n      // A day we did not collect in full is marked: its open, high, low and\n      // volume describe the minutes we have, not the session.\n      var part=r.source==='minutes'&&r.complete===false;\n      h+='<tr class=\"'+(part?'part':'')+'\">'+\n        '<td>'+r.symbol+(r.source==='provider'?' <span class=\"srcp\">ספק</span>':'')+'</td>'+\n        '<td>'+r.date+'</td>'+\n        '<td>'+(r.open!=null?r.open.toFixed(2):'—')+'</td>'+\n        '<td>'+(r.high!=null?r.high.toFixed(2):'—')+'</td>'+\n        '<td>'+(r.low!=null?r.low.toFixed(2):'—')+'</td>'+\n        '<td class=\"p\">'+(r.close!=null?r.close.toFixed(2):'—')+'</td>'+\n        '<td class=\"'+(chg>0?'pos':chg<0?'neg':'')+'\">'+(chg==null?'—':(chg>=0?'+':'')+chg.toFixed(2)+'%')+'</td>'+\n        '<td>'+(r.volume!=null?fmtK(r.volume):'—')+'</td>'+\n        '<td>'+(r.bars||0)+(part?' ✗':'')+'</td>'+\n        (showCmp?'<td>'+(r.provider?r.provider.close.toFixed(2):'—')+'</td>'+\n          '<td class=\"'+(r.provider&&r.close!=null&&Math.abs(r.provider.close-r.close)>0.02?'neg':'')+'\">'+\n          (r.provider&&r.close!=null?(r.provider.close-r.close).toFixed(2):'—')+'</td>':'')+\n        '</tr>';\n    });\n    qs('#dTbl').innerHTML=h+'</tbody></table>'+\n      '<div style=\"padding:9px 12px;font-size:11.5px;color:var(--muted);line-height:1.6\">'+\n      'שורה צהובה = יום שלא נאסף במלואו, כלומר הפתיחה/הגבוה/הנמוך/הנפח מתארים את הדקות שיש לנו ולא את הסשן.<br>'+\n      'שורה עם תג \"ספק\" = נר יומי מהספק, בלי נרות דקה אצלנו. \"הצג השוואה לספק\" מציג את הסגירה שלו לצד שלנו.<br>'+\n      'לחיצה על כותרת עמודה ממיינת לפיה; לחיצה שנייה הופכת את הכיוון. ערכים חסרים תמיד בתחתית.</div>';\n    var head=qs('#dTbl').querySelector('thead');\n    if(head)head.onclick=function(e){\n      var c=e.target.closest('.s'); if(!c)return;\n      var k=c.dataset.c;\n      // Same column flips direction; a new column starts descending for numbers\n      // and ascending for text, which is what each is normally wanted as.\n      if(dSort===k)dDir=-dDir;\n      else {dSort=k;dDir=(k==='symbol'||k==='date')?(k==='date'?-1:1):-1}\n      drawDaily();\n    };\n  }\n  function dailyCsv(){\n    return ['symbol,date,open,high,low,close,volume,bars,source'].concat(dRows.map(function(r){\n      return [r.symbol,r.date,r.open,r.high,r.low,r.close,r.volume,r.bars,r.source].join(',');\n    })).join('\\n');\n  }\n  qs('#dGo').onclick=loadDaily;\n  qs('#dSym').onchange=loadDaily;\n  qs('#dCmp').onclick=function(){showCmp=!showCmp;this.textContent=showCmp?'הסתר השוואה':'הצג השוואה לספק';drawDaily()};\n  qs('#dCopy').onclick=function(){\n    if(!dRows.length)return; var t=dailyCsv();\n    (navigator.clipboard?navigator.clipboard.writeText(t):Promise.reject())\n      .then(function(){toast('הועתקו '+dRows.length+' שורות')},function(){prompt('העתק:',t)});\n  };\n  qs('#dDl').onclick=function(){\n    if(!dRows.length)return;\n    var b=new Blob([dailyCsv()],{type:'text/csv'}),a=document.createElement('a');\n    a.href=URL.createObjectURL(b); a.download='daily_'+(qs('#dFrom').value||'all')+'_'+(qs('#dTo').value||'all')+'.csv'; a.click();\n  };\n\n  loadIndex().catch(function(e){qs('#tbl').innerHTML='<div class=\"msg\"><b>לא הצלחתי לטעון.</b><br>'+String(e)+'</div>'});\n})();\n</script>\n</body>\n</html>\n";
export const REPLAY_HTML = "<!doctype html>\n<html lang=\"he\" dir=\"rtl\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width,initial-scale=1,viewport-fit=cover\">\n<title>Scanner Replay Lab</title>\n<link rel=\"preconnect\" href=\"https://fonts.googleapis.com\">\n<link href=\"https://fonts.googleapis.com/css2?family=Heebo:wght@400;600;800&display=swap\" rel=\"stylesheet\">\n<style>\n  :root{--paper:#F2F4F7;--ink:#1B2430;--muted:#5B6673;--rule:#D6DBE2;--well:#FFFFFF;\n    --up:#0F766E;--down:#B42318;--flat:#8A94A0;\n    --s-quiet:#8A94A0;--s-watch:#B7791F;--s-close:#C2410C;--s-ready:#0F766E;--s-active:#1D4ED8;--s-avoid:#B42318}\n  *{box-sizing:border-box}\n  html,body{margin:0;background:var(--paper);color:var(--ink);font-family:Heebo,system-ui,sans-serif;\n    font-variant-numeric:tabular-nums;-webkit-text-size-adjust:100%;max-width:100%;overflow-x:hidden}\n  body{padding-bottom:calc(24px + env(safe-area-inset-bottom))}\n  .wrap{max-width:1000px;margin:0 auto}\n  header{position:sticky;top:0;z-index:5;background:var(--paper);border-bottom:1px solid var(--rule)}\n  .top{display:flex;align-items:center;gap:8px;padding:12px 14px 8px;flex-wrap:wrap}\n  .top h1{font-size:17px;font-weight:800;margin:0}\n  .lab{font-size:10.5px;font-weight:800;background:var(--s-active);color:#fff;border-radius:6px;padding:2px 7px}\n  .eng,.par{font-size:10.5px;font-weight:800;border-radius:6px;padding:2px 7px}\n  .eng.ok{background:#D7EFEC;color:var(--up)} .eng.no{background:#FBEED0;color:#8A5B12}\n  .par.no{background:#F6DAD6;color:var(--down)} .par.yes{background:#D7EFEC;color:var(--up)}\n  .parity{margin:8px 14px;padding:11px 13px;background:#F6DAD6;border:1px solid #E4B4AD;\n    border-radius:11px;font-size:12.5px;line-height:1.65;color:var(--ink)}\n  .parity b{font-weight:800;color:var(--down)}\n  .parity.good{background:#E6F4F1;border-color:#B7DCD5} .parity.good b{color:var(--up)}\n  .parity th{background:none;font-size:10.5px;padding:3px 6px;position:static;border-bottom:1px solid var(--rule)}\n  .parity td.ok{color:var(--up);font-weight:700} .parity td.rec{color:#8A5B12;font-weight:700}\n  .parity td.bad{color:var(--down);font-weight:700}\n  .parity td.n{color:var(--muted);font-size:11px;white-space:normal}\n  .parity td.na{color:var(--muted);font-weight:600}\n  .warn2{margin-top:8px;padding-top:7px;border-top:1px solid var(--rule);font-size:11.5px;color:var(--muted)}\n  .parity table{margin-top:8px;font-size:11.5px;direction:rtl}\n  .parity td{padding:3px 6px;border:none;text-align:right}\n  .parity td.bad{color:var(--down);font-weight:700}\n  .spacer{flex:1}\n  .build{font-size:10px;color:var(--muted);direction:ltr;unicode-bidi:isolate}\n  .pick{display:flex;gap:8px;padding:0 14px 10px;flex-wrap:wrap;align-items:center}\n  select,input,button{font:inherit;font-size:14px;background:var(--well);color:var(--ink);\n    border:1px solid var(--rule);border-radius:10px;padding:8px 10px;min-width:0}\n  input#sym{width:96px;text-transform:uppercase}\n  button.dark{background:var(--ink);color:var(--paper);border-color:var(--ink);font-weight:600}\n  button:disabled{opacity:.45}\n  .ctl{display:flex;gap:6px;padding:0 14px 10px;flex-wrap:wrap;align-items:center}\n  .ctl button{padding:7px 11px;font-size:13px}\n  .at{font-size:13px;color:var(--muted);padding:0 14px 8px}\n  .at b{color:var(--ink);font-weight:800;direction:ltr;unicode-bidi:isolate}\n\n  .sum{display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:8px;padding:10px 14px}\n  .box{background:var(--well);border:1px solid var(--rule);border-radius:11px;padding:9px 11px}\n  .box small{display:block;font-size:11px;color:var(--muted);margin-bottom:2px}\n  .box b{font-size:18px;font-weight:800;direction:ltr;unicode-bidi:isolate}\n  .box span{font-size:11.5px;color:var(--muted)}\n  .box.good b{color:var(--up)} .box.bad b{color:var(--down)}\n\n  .chart{margin:8px 14px;background:var(--well);border:1px solid var(--rule);border-radius:12px;padding:8px}\n  .chart svg{display:block;width:100%;height:auto}\n  .wick{stroke-width:1} .body{stroke:none}\n  .c-up{fill:var(--up);stroke:var(--up)} .c-dn{fill:var(--down);stroke:var(--down)} .c-fl{fill:var(--flat);stroke:var(--flat)}\n  .grid{stroke:var(--rule);stroke-width:1} .gtxt{fill:var(--muted);font-size:9px;font-family:inherit}\n  .vwapL{fill:none;stroke:#7C3AED;stroke-width:1;stroke-dasharray:3 3}\n  .mk{cursor:pointer} .mk text{font-size:8.5px;fill:#fff;font-weight:700}\n  .cur{stroke:var(--ink);stroke-width:1;stroke-dasharray:3 3;opacity:.7}\n\n  .filt{display:flex;gap:6px;margin:0 14px 8px;flex-wrap:wrap}\n  .filt[hidden]{display:none}\n  .filt button{padding:5px 10px;font-size:12.5px;border-radius:8px;opacity:.5}\n  .filt button.on{opacity:1;font-weight:800;color:#fff;border-color:transparent}\n  .filt button.on.f-QUIET{background:var(--s-quiet)} .filt button.on.f-WATCH{background:var(--s-watch)}\n  .filt button.on.f-CLOSE{background:var(--s-close)} .filt button.on.f-READY{background:var(--s-ready)}\n  .filt button.on.f-ACTIVE{background:var(--s-active)} .filt button.on.f-AVOID{background:var(--s-avoid)}\n  .filt button.on.f-ALL{background:var(--ink)}\n  .filt .n{font-weight:400;opacity:.75}\n  h2{font-size:13px;font-weight:800;color:var(--muted);margin:16px 14px 6px;letter-spacing:.03em}\n  .tw{margin:0 14px;overflow-x:auto;background:var(--well);border:1px solid var(--rule);border-radius:11px}\n  table{width:100%;border-collapse:collapse;font-size:12.5px;direction:ltr}\n  th{background:var(--well);color:var(--muted);font-weight:600;text-align:right;padding:7px 8px;\n    border-bottom:1px solid var(--rule);font-size:11px;white-space:nowrap;position:sticky;top:0}\n  td{padding:6px 8px;text-align:right;border-bottom:1px solid var(--rule);white-space:nowrap}\n  th:first-child,td:first-child{text-align:left;font-weight:600}\n  tr.weak td{background:#FBEAE7}\n  tr.best td{background:#E6F4F1}\n  tr.unmeas td{opacity:.55}\n  td.p{font-weight:700} td.g{color:var(--up)} td.r{color:var(--down)}\n  .st{display:inline-block;font-size:10.5px;font-weight:800;color:#fff;border-radius:5px;padding:1px 6px}\n  .bg-QUIET{background:var(--s-quiet)} .bg-WATCH{background:var(--s-watch)} .bg-CLOSE{background:var(--s-close)}\n  .bg-READY{background:var(--s-ready)} .bg-ACTIVE{background:var(--s-active)} .bg-AVOID{background:var(--s-avoid)}\n  .bgNODATA{background:var(--flat)}\n  .msg{padding:34px 16px;text-align:center;color:var(--muted);line-height:1.7}\n  .msg b{color:var(--ink)}\n  .note{margin:10px 14px 0;padding:9px 11px;background:var(--well);border:1px solid var(--rule);\n    border-radius:10px;font-size:11.5px;color:var(--muted);line-height:1.6}\n  .cfg{display:flex;gap:8px;align-items:center;padding:0 14px 10px;font-size:12.5px;color:var(--muted);flex-wrap:wrap}\n  .cfg input{width:70px;padding:5px 7px;font-size:13px}\n  .toast{position:fixed;bottom:24px;right:50%;transform:translateX(50%);background:var(--ink);color:var(--paper);\n    padding:9px 16px;border-radius:10px;font-size:13px;z-index:60}\n  .toast[hidden]{display:none}\n  .pop{margin:8px 14px;background:var(--ink);color:var(--paper);border-radius:11px;padding:11px 13px;font-size:13px;line-height:1.65}\n  .pop[hidden]{display:none}\n  .pop b{font-weight:800}\n</style>\n</head>\n<body>\n<div class=\"wrap\">\n<header>\n  <div class=\"top\">\n    <h1>Scanner Replay Lab</h1><span class=\"lab\">QA</span>\n    <span class=\"eng\" id=\"eng\"></span><span class=\"eng\" id=\"path\"></span>\n    <span class=\"eng\" id=\"inp\"></span><span class=\"par\" id=\"par\"></span>\n    <div class=\"spacer\"></div>\n    <span class=\"build\">v138  (2026-09-05 16:01Z)</span>\n  </div>\n  <div class=\"pick\">\n    <input id=\"sym\" value=\"NVDA\" placeholder=\"סימבול\" autocomplete=\"off\">\n    <select id=\"date\"><option>—</option></select>\n    <button type=\"button\" id=\"load\" class=\"dark\">טען יום</button>\n    <button type=\"button\" id=\"full\" class=\"dark\">נתח יום מלא</button>\n    <span id=\"loaded\" style=\"font-size:12.5px;color:var(--muted)\"></span>\n  </div>\n  <div class=\"ctl\" id=\"ctl\" hidden>\n    <button type=\"button\" id=\"start\">▶ הרץ</button>\n    <button type=\"button\" id=\"pause\">⏸ עצור</button>\n    <button type=\"button\" id=\"prev\">◀ נר</button>\n    <button type=\"button\" id=\"next\">נר ▶</button>\n    <button type=\"button\" id=\"restart\">↺ מהתחלה</button>\n    <select id=\"speed\">\n      <option value=\"1\">1x</option><option value=\"5\" selected>5x</option>\n      <option value=\"10\">10x</option><option value=\"50\">50x</option>\n    </select>\n    <input id=\"jump\" placeholder=\"קפוץ ל-10:30\" style=\"width:120px\">\n  </div>\n  <div class=\"at\" id=\"at\"></div>\n  <div class=\"cfg\">\n    <span>סף התראה חלשה</span><input id=\"thr\" type=\"number\" step=\"0.05\" value=\"0.30\"><span>% תוך</span>\n    <input id=\"win\" type=\"number\" step=\"5\" value=\"30\"><span>דקות</span>\n    <span style=\"opacity:.7\">· משנה רק את הסיווג, לא את הסורק</span>\n  </div>\n</header>\n\n<div class=\"parity\" id=\"parity\"></div>\n<div class=\"pop\" id=\"pop\" hidden></div>\n<div class=\"sum\" id=\"sum\"></div>\n<div class=\"chart\" id=\"chart\" hidden><svg id=\"svg\" viewBox=\"0 0 800 300\" preserveAspectRatio=\"none\"></svg></div>\n<h2 id=\"hEvents\" hidden>שינויי מצב</h2>\n<div class=\"filt\" id=\"filt\" hidden></div>\n<div class=\"tw\" id=\"events\" hidden></div>\n<h2 id=\"hAlerts\" hidden>ביצועי התראות</h2>\n<div class=\"tw\" id=\"alerts\" hidden></div>\n<div class=\"note\" id=\"note\" hidden></div>\n<div id=\"empty\" class=\"msg\"><b>בחר סימבול ויום</b><br>הכלי מריץ את הסורק הקיים על נרות היסטוריים, נר אחר נר,<br>ומודד מה קרה למחיר אחרי כל התראה.</div>\n</div>\n<div class=\"toast\" id=\"toast\" hidden></div>\n\n<script>\n// bars-vault structure engine — pure function over one day's 1-minute bars.\n// Inlined into view.html by build-view.mjs; unit-tested directly in node.\n//\n// analyze(rows, { K: 3 }) -> { bars, swings, state, events }\n//   rows: [{ time, open, high, low, close, volume }] in chronological order.\n//\n// Swing confirmation is K bars on each side (default 3, per the owner's\n// \"K=3, no exceptions\" rule; the spec that inspired this used 2). A swing is\n// only usable from the bar that confirms it, so nothing here looks ahead:\n// the state at bar i is computed from bars 0..i.\nfunction analyze(allRows, opts) {\n  var K = (opts && opts.K) || 3;\n  // A flat zero-volume bar is an end-of-session filler, not market activity.\n  // It must not touch momentum, wicks, EMA, volume or scoring.\n  //\n  // ONE EXCEPTION: the 16:00 bar. The closing auction prints there and the feed\n  // often reports it flat with no volume attached, so the old rule threw away\n  // the OFFICIAL CLOSE and left 15:59 standing in for it. Measured on WMT: the\n  // only session whose close matched the provider's daily candle was the one\n  // whose last bar was 16:00, and dropping that bar moved 108.42 to 108.45.\n  var isAuction = function (r) { return r.time === '16:00'; };\n  var rows = allRows.filter(function (r) {\n    return isAuction(r) || !(r.volume === 0 && r.high === r.low);\n  });\n  var n = rows.length, skipped = allRows.length - n;\n  if (!n) return null;\n\n  // ---- per-bar derived values (causal: volume average is of bars 0..i)\n  var bars = [], volSum = 0, rangeSum = 0, dayHigh = -Infinity, dayLow = Infinity;\n  var pvSum = 0, ema9 = null, ema20 = null, k9 = 2 / 10, k20 = 2 / 21, belowVwapRun = 0;\n  for (var i = 0; i < n; i++) {\n    var r = rows[i], range = r.high - r.low, body = Math.abs(r.close - r.open);\n    volSum += r.volume; rangeSum += range;\n    dayHigh = Math.max(dayHigh, r.high); dayLow = Math.min(dayLow, r.low);\n    // VWAP from the open, EMA9/EMA20 on closes, 20-bar average range.\n    pvSum += (r.high + r.low + r.close) / 3 * r.volume;\n    var vwap = volSum > 0 ? pvSum / volSum : r.close;\n    ema9 = ema9 == null ? r.close : r.close * k9 + ema9 * (1 - k9);\n    ema20 = ema20 == null ? r.close : r.close * k20 + ema20 * (1 - k20);\n    var w20 = rows.slice(Math.max(0, i - 19), i + 1), atr20 = w20.reduce(function (s, x) { return s + (x.high - x.low); }, 0) / w20.length;\n    // (atr20 is needed by the EMA test below, so it is computed first)\n    var aboveVwap = r.close > vwap, vwapReclaim = aboveVwap && belowVwapRun >= 3, vwapLoss = !aboveVwap && belowVwapRun === 0 && i > 0 && bars[i - 1].aboveVwap;\n    belowVwapRun = aboveVwap ? 0 : belowVwapRun + 1;\n    // EMA is only bullish/bearish when the two are meaningfully apart (0.15x the\n    // average range) AND the fast one is moving that way. Otherwise: neutral.\n    var prevEma9 = i > 0 ? bars[i - 1].ema9 : ema9;\n    var sep = Math.abs(ema9 - ema20), minSep = 0.15 * (atr20 || range || 1);\n    // The closing auction prints a huge single-minute volume. It is marked so\n    // scoring and attention can ignore it; it is still shown as information.\n    var auction = r.time >= '15:55';\n    var align = 'neutral';\n    if (sep >= minSep) {\n      if (r.close > ema9 && ema9 > ema20 && ema9 > prevEma9) align = 'bull';\n      else if (r.close < ema9 && ema9 < ema20 && ema9 < prevEma9) align = 'bear';\n    }\n    var pct = function (x) { return range > 0 ? Math.round(x / range * 1000) / 10 : 0; };\n    bars.push({\n      i: i, time: r.time, open: r.open, high: r.high, low: r.low, close: r.close, volume: r.volume,\n      range: range, body: pct(body), uw: pct(r.high - Math.max(r.open, r.close)), lw: pct(Math.min(r.open, r.close) - r.low),\n      dir: r.close > r.open ? 'up' : r.close < r.open ? 'down' : 'flat',\n      volx: volSum > 0 ? Math.round(r.volume / (volSum / (i + 1)) * 100) / 100 : 0,\n      avgRange: rangeSum / (i + 1),\n      closePos: range > 0 ? (r.close - r.low) / range : 0.5,      // 1 = closed at high\n      vwap: vwap, ema9: ema9, ema20: ema20, atr20: atr20, aboveVwap: aboveVwap, vwapReclaim: vwapReclaim, vwapLoss: vwapLoss, align: align, auction: auction,\n      newDayHigh: r.high >= dayHigh && (i === 0 || r.high > Math.max.apply(null, rows.slice(0, i).map(function (x) { return x.high; }))),\n      dayHigh: dayHigh, dayLow: dayLow\n    });\n  }\n\n  // ---- swings, confirmed K bars later\n  var swings = [];\n  for (var s = K; s < n - K; s++) {\n    var hiL = true, loL = true, hiR = true, loR = true;\n    for (var k = 1; k <= K; k++) {\n      if (!(rows[s].high > rows[s - k].high)) hiL = false;\n      if (!(rows[s].high >= rows[s + k].high)) hiR = false;\n      if (!(rows[s].low < rows[s - k].low)) loL = false;\n      if (!(rows[s].low <= rows[s + k].low)) loR = false;\n    }\n    if (hiL && hiR) swings.push({ i: s, time: rows[s].time, kind: 'H', price: rows[s].high, confirmedAt: s + K });\n    if (loL && loR) swings.push({ i: s, time: rows[s].time, kind: 'L', price: rows[s].low, confirmedAt: s + K });\n  }\n  swings.sort(function (a, b) { return a.i - b.i; });\n  // An outside / expansion bar that is both a swing high and a swing low gives\n  // no order information (which came first?), so it is a level but never structure.\n  var byBar = {};\n  swings.forEach(function (w) { byBar[w.i] = (byBar[w.i] || 0) + 1; });\n  swings.forEach(function (w) { if (byBar[w.i] > 1) w.outside = true; });\n  // A swing only earns HH/LH/HL/LL if it moved at least MIN_MOVE x the average\n  // bar range past the previous swing of its kind; smaller wiggles are EQ and\n  // count as levels but never as structure.\n  var MIN_MOVE = (opts && opts.minMove) || 1.5, prevH = null, prevL = null;\n  swings.forEach(function (w) {\n    if (w.outside) { w.label = 'OUT'; return; }\n    var tol = MIN_MOVE * bars[w.i].avgRange;\n    if (w.kind === 'H') { w.label = !prevH ? 'H' : w.price > prevH.price + tol ? 'HH' : w.price < prevH.price - tol ? 'LH' : 'EQ'; if (w.label !== 'EQ') prevH = w; }\n    else { w.label = !prevL ? 'L' : w.price > prevL.price + tol ? 'HL' : w.price < prevL.price - tol ? 'LL' : 'EQ'; if (w.label !== 'EQ') prevL = w; }\n  });\n\n  // ---- sequential walk: levels, trend, events\n  //\n  // Levels for breakout/support events are WINDOW extremes, not the nearest\n  // micro-swing: the highest high / lowest low of the last 60 bars, excluding\n  // the most recent 5 (a high made a minute ago is not resistance yet).\n  // Swings still drive trend, HL/LL structure, and the \"structure break\" event.\n  var WIN = 60, AGE = 5, COOLDOWN = 30, MARGIN = (opts && opts.margin) || 0.15, RUNS = (opts && opts.runs) || 3, TEST_RANGE = 0.75;\n  var events = [], prevState = null, lastBreakout = null, states = [], fired = {}, trendRun = 0, runTrend = null, announced = null, broken = null;\n  var cooled = function (type, b) { return fired[type] == null || b - fired[type] > COOLDOWN; };\n  var mark = function (type, b) { fired[type] = b; };\n\n  for (var b = 0; b < n; b++) {\n    var bar = bars[b], next = bars[b + 1] || null, margin = MARGIN * bar.avgRange;\n    var conf = swings.filter(function (w) { return w.confirmedAt <= b; });\n    var structural = function (w) { return w.label !== 'EQ' && w.label !== 'OUT'; };\n    var SH = conf.filter(function (w) { return w.kind === 'H' && structural(w); }), SL = conf.filter(function (w) { return w.kind === 'L' && structural(w); });\n    var lastSH = SH[SH.length - 1] || null, lastSL = SL[SL.length - 1] || null;\n\n    var wEnd = b - AGE, wStart = Math.max(0, b - WIN), res = null, sup = null, reclaimed = null;\n    if (wEnd >= wStart && wEnd >= 0) {\n      var w = rows.slice(wStart, wEnd + 1), hiIdx = 0, loIdx = 0;\n      for (var q = 1; q < w.length; q++) { if (w[q].high > w[hiIdx].high) hiIdx = q; if (w[q].low < w[loIdx].low) loIdx = q; }\n      if (w[hiIdx].high > bar.close) res = { price: w[hiIdx].high, time: w[hiIdx].time, kind: 'window' };\n      if (w[loIdx].low < bar.close) sup = { price: w[loIdx].low, time: w[loIdx].time, kind: 'window' };\n    }\n    // After a confirmed support break the broken level is a reclaim level, not\n    // support; support is the lowest low made since the break.\n    if (broken) {\n      if (bar.low < broken.low) { broken.low = bar.low; broken.lowTime = bar.time; }\n      if (bar.close > broken.level + MARGIN * bar.atr20) broken.above++; else broken.above = 0;\n      if (broken.above >= 2) { reclaimed = broken.level; broken = null; } else if (b - broken.i > 60) broken = null;\n    }\n    if (broken && broken.low < bar.close) sup = { price: broken.low, time: broken.lowTime, kind: 'postbreak' };\n    if (broken && sup && sup.kind !== 'postbreak' && sup.price >= broken.level - 1e-9) sup = broken.low < bar.close ? { price: broken.low, time: broken.lowTime, kind: 'postbreak' } : null;\n\n    var trend = 'RANGE', reason = 'אין רצף';\n    if (lastSH && lastSL && lastSH.label !== 'H' && lastSL.label !== 'L') {\n      if (lastSH.label === 'HH' && lastSL.label === 'HL') { trend = 'UP'; reason = 'HH + HL'; }\n      else if (lastSH.label === 'LH' && lastSL.label === 'LL') { trend = 'DOWN'; reason = 'LH + LL'; }\n      else reason = lastSH.label + ' + ' + lastSL.label;\n    } else if (!lastSH || !lastSL) reason = 'עדיין אין מספיק swings';\n\n    // ---- events, judged against the PREVIOUS bar's levels (no look-ahead on levels;\n    // the only forward look is the 2-bar confirmation, which is reported as such).\n    var pres = prevState ? prevState.res : null, psup = prevState ? prevState.sup : null;\n    var pTrend = prevState ? prevState.trend : null, pSL = prevState ? prevState.lastSL : null, pSH = prevState ? prevState.lastSH : null;\n    var ev = [], score = 0;\n\n    if (pres && bar.high > pres.price && bar.close > pres.price + margin && cooled('breakout', b)) {\n      var confirmed = next ? next.close > pres.price : null;\n      var strong = bar.body > 65 && bar.volx > 1.2;\n      score += confirmed ? 5 : 2;\n      ev.push({ type: strong && confirmed ? 'strong_breakout' : 'breakout', level: pres.price, confirmed: confirmed, strong: strong });\n      lastBreakout = { i: b, level: pres.price, confirmed: confirmed, dayHigh: pres.price >= prevState.bar.dayHigh - 1e-9 };\n      if (confirmed) mark('breakout', b);\n    } else if (pres && bar.high > pres.price && bar.close < pres.price && bar.uw >= 50 && cooled('rejected', b)) {\n      ev.push({ type: 'rejected', level: pres.price, wick: bar.uw }); mark('rejected', b);\n    } else if (pres && !ev.length && bar.close <= pres.price && pres.price - bar.close <= TEST_RANGE * bar.atr20) {\n      ev.push({ type: 'testing', level: pres.price });\n    }\n    if (lastBreakout && b > lastBreakout.i && b - lastBreakout.i <= 10) {\n      if (bar.close < lastBreakout.level) { score -= 3; ev.push({ type: 'failed', level: lastBreakout.level, confirmed: lastBreakout.confirmed, dayHigh: lastBreakout.dayHigh }); lastBreakout = null; }\n      else if (bar.low <= lastBreakout.level + margin && bar.close >= lastBreakout.level && cooled('retest_hold', b)) { ev.push({ type: 'retest_hold', level: lastBreakout.level }); mark('retest_hold', b); }\n    }\n    if (psup && bar.low < psup.price && bar.close < psup.price - margin && cooled('support_break', b)) {\n      var confS = next ? next.close < psup.price : null;\n      score -= 2; ev.push({ type: 'support_break', level: psup.price, confirmed: confS });\n      if (confS) { mark('support_break', b); broken = { level: psup.price, i: b, low: bar.low, lowTime: bar.time, above: 0 }; }\n    }\n    // Structure break: the level the trend rests on gives way.\n    // Only an announced trend (two agreeing swings) can be \"broken\".\n    var pAnnounced = prevState ? prevState.announced : null;\n    if (pAnnounced === 'UP' && pSL && bar.close < pSL.price - margin && cooled('structure_break', b)) {\n      score -= 2; ev.push({ type: 'structure_break', level: pSL.price, side: 'bear', confirmed: next ? next.close < pSL.price : null }); mark('structure_break', b);\n    } else if (pAnnounced === 'DOWN' && pSH && bar.close > pSH.price + margin && cooled('structure_break', b)) {\n      score += 2; ev.push({ type: 'structure_break', level: pSH.price, side: 'bull', confirmed: next ? next.close > pSH.price : null }); mark('structure_break', b);\n    }\n    conf.forEach(function (w) {\n      if (w.confirmedAt !== b || w.label === 'EQ') return;\n      if (w.label === 'OUT') { if (w.kind === 'H') ev.push({ type: 'outside_bar', level: w.price, at: w.time }); return; }\n      ev.push({ type: (w.label.toLowerCase()) + '_confirmed', level: w.price, at: w.time });\n    });\n    if (bar.volx >= 1.5 && !bar.auction) { score += 2; ev.push({ type: 'volume_spike', volx: bar.volx }); }\n    else if (bar.volx >= 1.5 && bar.auction) ev.push({ type: 'auction_volume', volx: bar.volx });\n    var momentum = bar.body >= 75 && bar.range > 1.5 * bar.avgRange;\n    if (momentum) ev.push({ type: 'momentum', body: bar.body, range: bar.range });\n    if (reclaimed != null) ev.push({ type: 'level_reclaim', level: reclaimed });\n    if (bar.vwapReclaim) ev.push({ type: 'vwap_reclaim', level: bar.vwap });\n    if (bar.vwapLoss) ev.push({ type: 'vwap_loss', level: bar.vwap });\n    if (bar.body >= 70) score += 1;\n    if (bar.closePos >= 0.8) score += 1;\n    if (bar.newDayHigh) score += 1;\n    if (bar.uw > 55) score -= 2;\n    // Trend change is announced on the SECOND structural swing that agrees\n    // (HH+HL is the trend; the next HH or HL is the confirmation).\n    var structConfirmedNow = conf.some(function (w) { return w.confirmedAt === b && structural(w); });\n    if (structConfirmedNow) {\n      if (trend === runTrend) trendRun++; else { runTrend = trend; trendRun = 1; }\n      if (trendRun === RUNS && (trend === 'UP' || trend === 'DOWN') && cooled('trend_change', b)) {\n        ev.push({ type: 'trend_change', from: pTrend === trend ? 'RANGE' : pTrend, to: trend }); mark('trend_change', b); announced = trend;\n      }\n    }\n    if (announced && trend !== announced) announced = null;\n\n    var attempt = (pres && Math.abs(pres.price - bar.close) <= TEST_RANGE * bar.atr20) || (lastBreakout && b - lastBreakout.i <= 1);\n    var nearLow = sup ? bar.close - sup.price : bar.close - bar.dayLow;\n    var bounce = (trend === 'DOWN' || broken) && bar.dir === 'up' && nearLow <= 1.5 * bar.atr20;\n    var state = attempt ? 'ATTEMPT' : bounce ? 'BOUNCE' : trend;\n    var tier = score >= 7 ? 'CHECK' : score >= 5 ? 'IMPORTANT' : score >= 3 ? 'WATCH' : 'NONE';\n    // Ask = worth a fresh look: only day-extreme levels, only confirmed.\n    var pDayHigh = prevState ? prevState.bar.dayHigh : Infinity, pDayLow = prevState ? prevState.bar.dayLow : -Infinity;\n    var ask = ev.some(function (e) {\n      if (e.type === 'strong_breakout' || e.type === 'breakout') return e.confirmed === true && e.level >= pDayHigh - 1e-9;\n      if (e.type === 'rejected') return e.level >= pDayHigh - 1e-9;\n      if (e.type === 'failed') return e.confirmed === true && e.dayHigh === true;\n      if (e.type === 'support_break') return e.confirmed === true && e.level <= pDayLow + 1e-9;\n      if (e.type === 'structure_break') return e.confirmed === true;\n      if (e.type === 'trend_change') return true;\n      return false;\n    }) || (bar.volx >= 1.5 && !bar.auction && momentum);\n    var loggable = ask || tier !== 'NONE' || ev.some(function (e) {\n      return e.type === 'failed' || e.type === 'rejected' || e.type === 'support_break' || e.type === 'structure_break' || e.type === 'trend_change' || e.type === 'retest_hold' || /breakout/.test(e.type);\n    });\n\n    var st = { i: b, time: bar.time, close: bar.close, trend: trend, reason: reason, state: state, res: res, sup: sup,\n      lastSH: lastSH, lastSL: lastSL, announced: announced, reclaim: broken ? broken.level : null, score: score, tier: tier, ask: ask, events: ev, bar: bar };\n    states.push(st);\n    if (loggable) events.push(st);\n    prevState = st;\n  }\n\n  // The UNFILTERED rows too. Coverage is a question about the session that was\n  // loaded, not about the subset the analysis chose to use: a no-trade minute\n  // is dropped here on purpose, and counting the remainder made a complete\n  // 335-bar session report as 186 with holes that were never there.\n  return { bars: bars, rawBars: allRows, swings: swings, states: states, state: states[n - 1],\n    events: events, skippedBars: skipped };\n}\n\n// ---------------------------------------------------------------- radar layer\n//\n// Short-term momentum, kept separate from structure on purpose: a confirmed\n// downtrend can still be bouncing.\n//   PUSHING   pressing higher with the structure\n//   PULLBACK  easing back inside an up structure\n//   RECOVERY  bouncing off the lows against a down structure\n//   SELLING   pressing lower\n//   FLAT      no net movement worth naming\nfunction momentum(A, look) {\n  if (!A || !A.state) return { label: 'FLAT', net: 0 };\n  var L = look || 5, bars = A.bars, n = bars.length, w = bars.slice(Math.max(0, n - L));\n  var b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  var net = (b.close - w[0].open) / atr;                       // in range units\n  var ups = w.filter(function (x) { return x.dir === 'up'; }).length;\n  var struct = A.state.announced || A.state.trend;\n  var lowRun = Math.min.apply(null, w.map(function (x) { return x.low; }));\n  var highRun = Math.max.apply(null, w.map(function (x) { return x.high; }));\n  var offLow = (b.close - lowRun) / atr, offHigh = (highRun - b.close) / atr;\n  var label = 'FLAT';\n  if (net >= 0.8 && ups >= L / 2) label = struct === 'DOWN' ? 'RECOVERY' : 'PUSHING';\n  else if (net <= -0.8 && ups <= L / 2) label = struct === 'UP' ? 'PULLBACK' : 'SELLING';\n  else if (offLow >= 1 && offHigh <= 0.5) label = struct === 'DOWN' ? 'RECOVERY' : 'PUSHING';\n  else if (offHigh >= 1 && offLow <= 0.5) label = struct === 'UP' ? 'PULLBACK' : 'SELLING';\n  return { label: label, net: net, offLow: offLow, offHigh: offHigh };\n}\n\n// Tactical levels: what is actually within reach, preferring levels that price\n// has interacted with more than once. Everything is measured in R = one average\n// 1-minute range, so \"0.3R away\" means the same thing on any symbol.\nfunction tactical(A, maxR) {\n  if (!A || !A.state) return { support: null, resistance: null, above: [], below: [], atr: 1 };\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length, atr = b.atr20 || b.avgRange || 1, MAX = (maxR || 3) * atr;\n  var cand = [];\n  var add = function (price, why, weight, kind) {\n    if (price == null || !isFinite(price)) return;\n    cand.push({ price: price, why: why, weight: weight, kind: kind });\n  };\n\n  // A prior HIGH only becomes support once price broke above it and came back\n  // to it without losing it — resistance-turned-support needs evidence, not\n  // just \"price happens to be above it now\". Same, inverted, for prior lows.\n  var acceptedAsSupport = function (level, fromIdx) {\n    var closedAbove = 0, retest = false;\n    for (var i = fromIdx; i < n; i++) {\n      if (bars[i].close > level) closedAbove++;\n      if (closedAbove >= 2 && bars[i].low <= level + 0.25 * atr && bars[i].close >= level) retest = true;\n    }\n    return closedAbove >= 3 && retest;\n  };\n  var acceptedAsResistance = function (level, fromIdx) {\n    var closedBelow = 0, retest = false;\n    for (var i = fromIdx; i < n; i++) {\n      if (bars[i].close < level) closedBelow++;\n      if (closedBelow >= 2 && bars[i].high >= level - 0.25 * atr && bars[i].close <= level) retest = true;\n    }\n    return closedBelow >= 3 && retest;\n  };\n\n  A.swings.forEach(function (w) {\n    if (w.confirmedAt > n - 1) return;\n    var age = n - 1 - w.i; if (age > 120) return;\n    var base = (w.label === 'OUT' || w.label === 'EQ') ? 2 : 3;\n    var recency = age <= 30 ? 2 : age <= 60 ? 1 : 0;\n    if (w.kind === 'H') {\n      if (w.price > b.close) add(w.price, 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time, base + recency, 'res');\n      else if (acceptedAsSupport(w.price, w.i)) add(w.price, 'התנגדות שהפכה לתמיכה ' + w.time, base + recency + 1, 'sup');\n    } else {\n      if (w.price < b.close) add(w.price, 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time, base + recency, 'sup');\n      else if (acceptedAsResistance(w.price, w.i)) add(w.price, 'תמיכה שהפכה להתנגדות ' + w.time, base + recency + 1, 'res');\n    }\n  });\n  if (S.res && S.res.price > b.close) add(S.res.price, 'גבוה 60 נרות', 3, 'res');\n  if (S.sup && S.sup.price < b.close) add(S.sup.price, S.sup.kind === 'postbreak' ? 'הנמוך מאז השבירה' : 'נמוך 60 נרות', S.sup.kind === 'postbreak' ? 4 : 3, S.sup.kind === 'postbreak' ? 'sup' : 'sup');\n  if (b.dayHigh > b.close) add(b.dayHigh, 'גבוה היום', 3, 'res');\n  if (b.dayLow < b.close) add(b.dayLow, 'נמוך היום', 3, 'sup');\n  if (S.reclaim != null) add(S.reclaim, 'רמה שנשברה', 4, S.reclaim > b.close ? 'res' : 'sup');\n  add(b.vwap, 'VWAP', 2, b.vwap > b.close ? 'res' : 'sup');\n  cand[cand.length - 1].soft = true;   // VWAP is context, never a setup level on its own\n\n  // touches and rejections add weight; distance never does.\n  cand.forEach(function (c) {\n    var touch = 0, reject = 0;\n    bars.forEach(function (x) {\n      if (x.low <= c.price + 0.25 * atr && x.high >= c.price - 0.25 * atr) {\n        touch++;\n        if (c.kind === 'res' && x.high > c.price && x.close < c.price) reject++;\n        if (c.kind === 'sup' && x.low < c.price && x.close > c.price) reject++;\n      }\n    });\n    c.touches = touch; c.rejects = reject;\n    c.weight += Math.min(3, Math.floor(touch / 4)) + Math.min(2, reject);\n    c.r = (c.price - b.close) / atr;\n  });\n\n  // Price sitting exactly on a level is the important case, so the side test\n  // allows a small epsilon rather than requiring strictly above/below.\n  var eps = 0.05 * atr;\n  var side = function (want) {\n    return cand.filter(function (c) { return c.kind === want && (want === 'res' ? c.price > b.close - eps : c.price < b.close + eps); })\n      .filter(function (c) { return Math.abs(c.price - b.close) <= MAX; });\n  };\n  // Strongest level wins; distance is only the tie-breaker, and a much closer\n  // level of near-equal strength is preferred.\n  var pick = function (want) {\n    var list = side(want).sort(function (x, y) { return (y.weight - x.weight) || (Math.abs(x.r) - Math.abs(y.r)); });\n    if (!list.length) return null;\n    var best = list[0];\n    list.slice(1, 4).forEach(function (c) { if (c.weight >= best.weight - 1 && Math.abs(c.r) < Math.abs(best.r) - 0.3) best = c; });\n    return best;\n  };\n  var all = function (want) {\n    return side(want).sort(function (x, y) { return want === 'res' ? x.price - y.price : y.price - x.price; });\n  };\n  return { support: pick('sup'), resistance: pick('res'), above: all('res'), below: all('sup'), atr: atr, candidates: cand };\n}\n\n// Distance in R, kept readable at small values. Below 0.05R it is \"on the level\".\nfunction fmtR(r) {\n  var a = Math.abs(r);\n  if (a < 0.05) return 'על הרמה';\n  return (r >= 0 ? '+' : '-') + (a < 1 ? a.toFixed(2) : a.toFixed(1)) + 'R';\n}\n\n// A support is only \"holding\" if price is not driving through it. Deterministic:\n// a big bearish body closing near its low, meaningfully below the level, or two\n// consecutive closes below it.\nfunction aggressiveBreak(A, level) {\n  var bars = A.bars, n = bars.length, b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  if (b.close < level - 0.5 * atr) return true;\n  if (n >= 2 && bars[n - 2].close < level && b.close < level) return true;\n  if (b.dir === 'down' && b.body >= 60 && b.closePos <= 0.25 && b.close < level - 0.15 * atr) return true;\n  return false;\n}\n\n// ---- execution plan: a small deterministic state machine.\n// States: WAITING_FOR_ZONE, IN_ZONE, READY_PARTIAL, WAITING_FOR_CONFIRMATION,\n//         READY_ADD, ACTIVE, TAKE_PROFIT_AREA, DO_NOT_CHASE, FAILED, NO_SETUP\nvar PLAN_TEXT = {\n  WAITING_FOR_ZONE: ['המתן', 'מחכים לכניסה לאזור'],\n  IN_ZONE: ['המתן', 'בתוך האזור — צריך בלימה'],\n  READY_PARTIAL: ['כניסה חלקית', 'האזור מוחזק'],\n  WAITING_FOR_CONFIRMATION: ['החזק', 'מחכים לאישור להוספה'],\n  READY_ADD: ['הוסף', 'האישור התקבל'],\n  ACTIVE: ['החזק', 'מהלך פעיל'],\n  TAKE_PROFIT_AREA: ['מימוש', 'אזור מימוש ראשון'],\n  DO_NOT_CHASE: ['לא לרדוף', 'המחיר התרחק מהכניסה'],\n  FAILED: ['להימנע', 'ה-setup בוטל'],\n  NO_SETUP: ['המתן', 'אין setup']\n};\nfunction executionPlan(A, market) {\n  if (!A || !A.state) return null;\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length, atr = b.atr20 || b.avgRange || 1;\n  var T = tactical(A), BL = bottomLine(A, market), mom = momentum(A);\n  var P = { state: 'NO_SETUP', kind: null, zone: null, entry: null, addAbove: null,\n    invalidation: null, target: null, atr: atr, price: b.close };\n\n  var sup = T.support, res = T.resistance;\n  var bearish = S.announced === 'DOWN' && mom.label !== 'RECOVERY';\n  // A setup needs a real structural level and a tape with some range in it.\n  // A dead-flat chop whose whole day spans one average bar is not a setup,\n  // however close price sits to \"a level\".\n  var dayRangeR = (b.dayHigh - b.dayLow) / atr;\n  var hasTape = dayRangeR >= 2;\n  // Whether the level is actually holding is decided by the state machine\n  // below (holding / aggressiveBreak), not by momentum: the first green bar\n  // off support is exactly the partial-entry moment, and a 5-bar momentum\n  // reading still says SELLING there.\n  var pullbackOk = !bearish && !!sup && !sup.soft && hasTape && sup.weight >= 5;\n  var breakoutOk = !bearish && !!res && !res.soft && hasTape &&\n    (S.trend !== 'DOWN' || mom.label === 'RECOVERY') && (BL.confidence >= 3 || mom.label === 'PUSHING' || S.trend === 'UP');\n\n  // Which setup is in play: the nearer opportunity wins.\n  var dSup = sup ? Math.abs(b.close - sup.price) / atr : Infinity;\n  var dRes = res ? Math.abs(res.price - b.close) / atr : Infinity;\n  if (pullbackOk && dSup <= 3 && (dSup <= dRes || !breakoutOk)) {\n    P.kind = 'pullback';\n    P.zone = [sup.price - 0.15 * atr, sup.price + 0.30 * atr];\n    P.entry = sup.price + 0.05 * atr;\n    var nextUp = T.above.filter(function (c) { return c.price > P.zone[1] + 0.1 * atr; })[0];\n    P.addAbove = nextUp ? nextUp.price : sup.price + 0.7 * atr;\n    var lowerLevels = T.below.filter(function (c) { return c.price < sup.price - 0.05 * atr; });\n    P.invalidation = lowerLevels.length ? Math.max(lowerLevels[0].price, sup.price - 0.9 * atr) : sup.price - 0.45 * atr;\n    P.target = res ? res.price : b.dayHigh;\n    P.levelWhy = sup.why;\n  } else if (breakoutOk && dRes <= 3) {\n    P.kind = 'breakout';\n    P.zone = [res.price, res.price + 0.20 * atr];\n    P.entry = res.price + 0.10 * atr;\n    P.addAbove = res.price + 0.5 * atr;\n    P.invalidation = Math.max(res.price - 0.6 * atr, sup ? sup.price : res.price - 0.9 * atr);\n    var beyond = T.above.filter(function (c) { return c.price > res.price + 0.2 * atr; })[0];\n    P.target = beyond ? beyond.price : res.price + 1.5 * atr;\n    P.levelWhy = res.why;\n  }\n\n  // A setup that just broke must still read FAILED, even though the level it\n  // rested on is now above price and no longer offered as support.\n  // Losing the last structural low by a clear margin is a broken setup even if\n  // the 60-bar window never registered a support_break (the window low can sit\n  // far below the level the setup actually rested on).\n  var lostLevel = !!(S.lastSL && b.close < S.lastSL.price - 0.3 * atr && (n - 1 - S.lastSL.i) <= 25);\n  var justBroke = lostLevel || A.states.slice(-6).some(function (s) {\n    return s.events.some(function (e) {\n      return (e.type === 'support_break' && e.confirmed === true)\n        || (e.type === 'structure_break' && e.side === 'bear' && e.confirmed === true)\n        || (e.type === 'failed');\n    });\n  });\n  if (!P.kind) {\n    if (justBroke) { P.state = 'FAILED'; P.invalidation = null; return decorate(P); }\n    P.state = 'NO_SETUP'; P.reason = bearish ? 'מבנה דובי — אין setup לונג' : 'אין רמה קרובה';\n    return decorate(P);\n  }\n\n  var brokeInvalid = b.close < P.invalidation || lostLevel;\n  var agg = aggressiveBreak(A, P.kind === 'pullback' ? sup.price : P.invalidation);\n  var inZone = b.close >= P.zone[0] && b.close <= P.zone[1];\n  // \"Holding\" = the last two bars did not lose the level. This, not momentum,\n  // is what turns being in the zone into a partial entry.\n  var lvl = P.kind === 'pullback' ? sup.price : P.zone[0];\n  var holding = P.kind !== 'pullback' || (bars.slice(-2).every(function (x) { return x.low >= lvl - 0.35 * atr; }) && b.close >= lvl - 0.1 * atr);\n  var aboveAdd = b.close > P.addAbove;\n  var confirmedAdd = aboveAdd && n >= 2 && bars[n - 2].close > P.addAbove;\n  var nearTarget = P.target && (P.target - b.close) / atr <= 0.3;\n  var extended = b.close > P.entry + 0.5 * atr;\n\n  if (brokeInvalid || (agg && !inZone && b.close < P.zone[0])) P.state = 'FAILED';\n  else if (agg && inZone) P.state = 'FAILED';\n  else if (nearTarget && (aboveAdd || P.kind === 'breakout')) P.state = 'TAKE_PROFIT_AREA';\n  else if (confirmedAdd) P.state = extended && !nearTarget ? 'ACTIVE' : 'READY_ADD';\n  else if (aboveAdd) P.state = 'WAITING_FOR_CONFIRMATION';\n  else if (inZone && holding) P.state = 'READY_PARTIAL';\n  else if (inZone) P.state = 'IN_ZONE';\n  else if (extended) P.state = 'DO_NOT_CHASE';\n  else P.state = 'WAITING_FOR_ZONE';\n\n  return decorate(P);\n\n  function decorate(P) {\n    var t = PLAN_TEXT[P.state] || PLAN_TEXT.NO_SETUP;\n    P.action = t[0]; P.headline = t[1];\n    var z = P.zone ? P.zone.map(function (x) { return x.toFixed(2); }).join('–') : null;\n    P.waitPrice = null; P.nextStep = null; P.ifConfirmed = null; P.ifFailed = null; P.short = null;\n    if (P.state === 'NO_SETUP') { P.short = P.reason; return P; }\n    switch (P.state) {\n      case 'WAITING_FOR_ZONE':\n        P.waitPrice = z;\n        P.nextStep = P.kind === 'pullback' ? 'ירידה לאזור ' + z + ' בלי שבירה אגרסיבית' : 'סגירה מעל ' + P.zone[0].toFixed(2);\n        P.ifConfirmed = 'כניסה חלקית ב-' + P.entry.toFixed(2);\n        P.ifFailed = 'שבירה מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'מחכים ל-' + (P.kind === 'pullback' ? z : P.zone[0].toFixed(2));\n        break;\n      case 'IN_ZONE':\n        P.waitPrice = z;\n        P.nextStep = 'בתוך האזור — צריך בלימה לפני כניסה';\n        P.ifConfirmed = 'בלימה מעל ' + P.zone[0].toFixed(2) + ' — כניסה חלקית';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'באזור ' + z + ' — צריך בלימה';\n        break;\n      case 'READY_PARTIAL':\n        P.waitPrice = z;\n        P.nextStep = 'האזור מוחזק — כניסה חלקית ב-' + P.entry.toFixed(2);\n        P.ifConfirmed = 'חזרה מעל ' + P.addAbove.toFixed(2) + ' — הוספה';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'כניסה חלקית ' + z;\n        break;\n      case 'WAITING_FOR_CONFIRMATION':\n        P.waitPrice = P.addAbove.toFixed(2);\n        P.nextStep = 'נר סגירה נוסף מעל ' + P.addAbove.toFixed(2);\n        P.ifConfirmed = 'הוספה';\n        P.ifFailed = 'חזרה מתחת ' + P.zone[0].toFixed(2) + ' — להחזיק בלבד';\n        P.short = 'אישור מעל ' + P.addAbove.toFixed(2);\n        break;\n      case 'READY_ADD':\n        P.waitPrice = P.addAbove.toFixed(2);\n        P.nextStep = 'הוספה מעל ' + P.addAbove.toFixed(2);\n        P.ifConfirmed = 'יעד ' + (P.target ? P.target.toFixed(2) : '—');\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'הוסף מעל ' + P.addAbove.toFixed(2);\n        break;\n      case 'ACTIVE':\n        P.waitPrice = P.target ? P.target.toFixed(2) : null;\n        P.nextStep = 'מהלך פעיל לעבר ' + (P.target ? P.target.toFixed(2) : 'היעד');\n        P.ifConfirmed = 'מימוש חלקי באזור היעד';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'יעד הבא ' + (P.target ? P.target.toFixed(2) : '—');\n        break;\n      case 'TAKE_PROFIT_AREA':\n        P.waitPrice = P.target.toFixed(2);\n        P.nextStep = 'אזור מימוש ' + P.target.toFixed(2);\n        P.ifConfirmed = 'מימוש חלקי';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — יציאה';\n        P.short = 'מימוש ' + P.target.toFixed(2);\n        break;\n      case 'DO_NOT_CHASE':\n        P.pullbackZone = [P.entry, P.entry + 0.35 * atr];\n        P.waitPrice = P.pullbackZone.map(function (x) { return x.toFixed(2); }).join('–');\n        P.nextStep = 'פולבק ל-' + P.waitPrice;\n        P.ifConfirmed = 'כניסה חלקית בפולבק';\n        P.ifFailed = 'מתחת ' + P.invalidation.toFixed(2) + ' — ביטול';\n        P.short = 'לא לרדוף · פולבק ' + P.waitPrice;\n        break;\n      case 'FAILED':\n        P.waitPrice = null;\n        if (P.invalidation == null) { P.nextStep = 'הרמה נשברה — אין כניסה'; P.ifConfirmed = 'setup חדש נדרש'; P.short = 'setup נכשל'; return P; }\n        P.nextStep = 'אין כניסה — הרמה נשברה';\n        P.ifConfirmed = 'setup חדש נדרש';\n        P.ifFailed = null;\n        P.short = 'setup נכשל';\n        break;\n    }\n    return P;\n  }\n}\n\n// One radar row. Status follows the execution state — the single source of\n// truth — so the row can never say CLOSE while the plan says there is nothing\n// to do.\nfunction radarRow(symbol, A, market, freshness) {\n  if (!A || !A.state) return { symbol: symbol, status: 'NO DATA', score: 0, why: 'אין נתונים', nearestR: Infinity, freshness: freshness || 'NO DATA' };\n  var S = A.state, b = S.bar, T = tactical(A), mom = momentum(A), BL = bottomLine(A, market), P = executionPlan(A, market);\n  var near = [T.support, T.resistance].filter(Boolean).map(function (l) { return Math.abs(l.r); });\n  var nearest = near.length ? Math.min.apply(null, near) : Infinity;\n  var ended = freshness === 'SESSION ENDED';\n\n  var status, why = P && P.short ? P.short : 'אין setup';\n  var st = P ? P.state : 'NO_SETUP';\n  if (freshness === 'NO DATA') { status = 'NO DATA'; why = 'אין נתונים'; }\n  else if (st === 'FAILED' || (S.announced === 'DOWN' && st === 'NO_SETUP')) { status = 'AVOID'; why = st === 'FAILED' ? 'setup נכשל' : 'מגמת ירידה מוכרזת'; }\n  else if (st === 'ACTIVE' || st === 'TAKE_PROFIT_AREA') status = 'ACTIVE';\n  else if (st === 'READY_PARTIAL' || st === 'READY_ADD') status = 'READY';\n  // CLOSE needs proximity AND a reason to look: a live plan step, not distance.\n  else if (st === 'WAITING_FOR_CONFIRMATION' || st === 'DO_NOT_CHASE') status = 'CLOSE';\n  else if (st === 'WAITING_FOR_ZONE' && nearest <= 1.0 && (BL.confidence >= 3 || mom.label === 'RECOVERY' || mom.label === 'PUSHING' || (b.volx >= 1.3 && !b.auction))) status = 'CLOSE';\n  else if (st === 'WAITING_FOR_ZONE') status = nearest <= 2 || BL.setup ? 'WATCH' : 'QUIET';\n  else status = 'QUIET';\n\n  // Rank by execution urgency first (spec 31), then status, then distance.\n  var URGENCY = { READY_PARTIAL: 0, READY_ADD: 1, TAKE_PROFIT_AREA: 2, ACTIVE: 3, WAITING_FOR_CONFIRMATION: 4, DO_NOT_CHASE: 5, WAITING_FOR_ZONE: 6, IN_ZONE: 6, NO_SETUP: 8, FAILED: 9 };\n  return {\n    symbol: symbol, status: status, why: why, score: BL.confidence, price: b.close,\n    structure: S.announced || S.trend, momentum: mom.label, nearestR: nearest,\n    support: T.support, resistance: T.resistance, volx: b.volx, auction: b.auction,\n    vwapAbove: b.aboveVwap, align: b.align, atr: T.atr, freshness: freshness || 'LIVE',\n    bl: BL, tactical: T, mom: mom, plan: P, action: P ? P.action : 'המתן',\n    pressure: (typeof pressure === 'function') ? pressure(A, { tactical: T, plan: P }) : null,\n    urgency: URGENCY[st] == null ? 7 : URGENCY[st], sessionEnded: ended\n  };\n}\n\nvar STATUS_ORDER = ['READY', 'CLOSE', 'ACTIVE', 'WATCH', 'QUIET', 'AVOID', 'NO DATA'];\nfunction sortRadar(list, mode) {\n  var arr = list.slice();\n  if (mode === 'symbol') return arr.sort(function (a, b) { return a.symbol < b.symbol ? -1 : 1; });\n  if (mode === 'score') return arr.sort(function (a, b) { return b.score - a.score || a.nearestR - b.nearestR; });\n  if (mode === 'volume') return arr.sort(function (a, b) { return (b.volx || 0) - (a.volx || 0); });\n  if (mode === 'distance') return arr.sort(function (a, b) { return a.nearestR - b.nearestR; });\n  return arr.sort(function (a, b) {\n    return ((a.urgency == null ? 7 : a.urgency) - (b.urgency == null ? 7 : b.urgency))\n      || (STATUS_ORDER.indexOf(a.status) - STATUS_ORDER.indexOf(b.status))\n      || (b.score - a.score) || (a.nearestR - b.nearestR);\n  });\n}\n\n// Market context from the analyses of SPY / QQQ (and optionally a sector ETF).\n// Bullish needs VWAP and EMA alignment to agree across the index ETFs.\nfunction marketContext(list) {\n  var have = list.filter(function (x) { return x && x.state; });\n  if (!have.length) return { label: 'Unavailable', score: 0, parts: [] };\n  var score = 0, parts = [];\n  have.forEach(function (x) {\n    var b = x.state.bar, s = 0;\n    s += b.aboveVwap ? 1 : -1;\n    s += b.align === 'bull' ? 1 : b.align === 'bear' ? -1 : 0;\n    if (x.state.announced === 'UP') s += 1; else if (x.state.announced === 'DOWN') s -= 1;\n    score += s; parts.push({ symbol: x.symbol, score: s, aboveVwap: b.aboveVwap, align: b.align, trend: x.state.trend, close: b.close, vwap: b.vwap });\n  });\n  var per = score / have.length;\n  return { label: per >= 1.5 ? 'Bullish' : per <= -1.5 ? 'Bearish' : 'Neutral', score: score, parts: parts };\n}\n\n// Bottom line: structure first, indicators only move confidence.\n// Never emits LONG without a confirmed structural event.\nfunction bottomLine(A, market) {\n  if (!A || !A.state) return null;\n  var S = A.state, b = S.bar, n = A.states.length, atr = b.atr20 || b.avgRange || 1;\n  var recent = function (type, within, pred) {\n    for (var i = n - 1; i >= Math.max(0, n - 1 - within); i--) {\n      var hit = A.states[i].events.find(function (e) { return e.type === type && (!pred || pred(e)); });\n      if (hit) return { e: hit, ago: n - 1 - i };\n    }\n    return null;\n  };\n  var conf = function (e) { return e.confirmed === true; };\n  var breakout = recent('breakout', 3, conf) || recent('strong_breakout', 3, conf);\n  var bullBreak = recent('structure_break', 3, function (e) { return e.side === 'bull' && e.confirmed === true; });\n  var retest = recent('retest_hold', 5);\n  var rejected = recent('rejected', 5), failed = recent('failed', 5);\n  var bearBreak = recent('structure_break', 10, function (e) { return e.side === 'bear' && e.confirmed === true; });\n  var supBreak = recent('support_break', 10, conf);\n  var vwapReclaim = recent('vwap_reclaim', 3);\n\n  // Levels above: every confirmed swing high of the last 90 bars, the 60-bar\n  // high, the day high and any reclaim level — ascending, merged if closer\n  // than a quarter range. First = watch, second = trigger, third = strong.\n  var above = [], below = [];\n  A.swings.forEach(function (w) {\n    if (w.kind !== 'H' || w.confirmedAt > n - 1 || n - 1 - w.i > 90 || w.price <= b.close) return;\n    above.push({ price: w.price, reason: 'swing ' + (w.label === 'OUT' ? 'outside' : w.label) + ' ' + w.time });\n  });\n  if (S.res) above.push({ price: S.res.price, reason: 'גבוה 60 נרות' + (S.res.time ? ' ' + S.res.time : '') });\n  if (b.dayHigh > b.close) above.push({ price: b.dayHigh, reason: 'גבוה היום' });\n  if (S.reclaim != null && S.reclaim > b.close) above.push({ price: S.reclaim, reason: 'רמה שנשברה — reclaim' });\n  if (S.sup) below.push({ price: S.sup.price, reason: S.sup.kind === 'postbreak' ? 'הנמוך מאז השבירה' : 'נמוך 60 נרות' + (S.sup.time ? ' ' + S.sup.time : '') });\n  if (S.lastSL && S.lastSL.price < b.close && !(S.reclaim != null && S.lastSL.price >= S.reclaim)) below.push({ price: S.lastSL.price, reason: 'swing ' + S.lastSL.label + ' ' + S.lastSL.time });\n  if (b.dayLow < b.close) below.push({ price: b.dayLow, reason: 'נמוך היום' });\n  var merge = function (arr, asc) {\n    arr.sort(function (x, y) { return asc ? x.price - y.price : y.price - x.price; });\n    var out = [];\n    arr.forEach(function (x) { var last = out[out.length - 1]; if (last && Math.abs(x.price - last.price) <= 0.25 * atr) { if (!/swing/.test(last.reason) && /swing/.test(x.reason)) last.reason = x.reason; return; } out.push(x); });\n    return out;\n  };\n  above = merge(above, true); below = merge(below, false);\n  var watch = above[0] || null, trigger = above[1] || null, strong = above[2] || null, invalid = below[0] || null;\n  var dist = function (lv) { return lv ? { pts: lv.price - b.close, atr: (lv.price - b.close) / atr } : null; };\n\n  // Structural setup?\n  var trendUp = S.trend === 'UP', announcedUp = S.announced === 'UP', announcedDown = S.announced === 'DOWN';\n  var setup = trendUp || !!breakout || !!bullBreak || !!retest;\n\n  // Confidence\n  var c = 0, why = [];\n  if (breakout || bullBreak) { c += 3; why.push('+3 פריצה/שבירה מבנית מאושרת'); }\n  if (trendUp) { c += 2; why.push('+2 HH/HL'); }\n  if (b.aboveVwap) { c += 1; why.push('+1 מעל VWAP'); } else { c -= 2; why.push('-2 מתחת VWAP'); }\n  if (b.align === 'bull') { c += 1; why.push('+1 EMA שורי'); } else if (b.align === 'bear') { c -= 1; why.push('-1 EMA דובי'); }\n  var volOk = b.volx >= 1.2 || (breakout && A.states[n - 1 - breakout.ago].bar.volx >= 1.2);\n  if (volOk) { c += 1; why.push('+1 נפח'); }\n  if (market && market.label === 'Bullish') { c += 1; why.push('+1 שוק'); } else if (market && market.label === 'Bearish') { c -= 2; why.push('-2 שוק נגד'); }\n  if (retest) { c += 1; why.push('+1 בדיקה חוזרת'); }\n  if (rejected) { c -= 2; why.push('-2 דחייה'); }\n  if (bearBreak || supBreak || failed) { c -= 3; why.push('-3 ביטול/שבירה'); }\n  c = Math.max(0, Math.min(10, c));\n\n  // Action\n  var action = 'WAIT', reason = 'אין setup מבני';\n  if (announcedDown || bearBreak || supBreak) { action = 'AVOID'; reason = announcedDown ? 'מגמת ירידה מוכרזת' : 'תמיכה/מבנה נשברו'; }\n  else if ((breakout || bullBreak) && c >= 6) { action = 'LONG'; reason = (breakout ? 'פריצה' : 'שבירת מבנה שורית') + ' מאושרת · ביטחון ' + c; }\n  else if (breakout || bullBreak) { action = 'LONG WATCH'; reason = 'פריצה מאושרת אבל ביטחון ' + c + '/10'; }\n  else if (setup && watch && (watch.price - b.close) <= 1.0 * atr) { action = 'LONG WATCH'; reason = 'מגמה עולה, ' + ((watch.price - b.close) / atr).toFixed(1) + '× טווח מהרמה הראשונה'; }\n  else if (setup && vwapReclaim) { action = 'LONG WATCH'; reason = 'VWAP נכבש מחדש בתוך מגמה עולה'; }\n  else if (setup) { action = 'WAIT'; reason = 'מגמה עולה, רחוק מהטריגר'; }\n\n  return {\n    action: action, reason: reason, confidence: c, why: why,\n    watch: watch ? { price: watch.price, reason: watch.reason, dist: dist(watch) } : null,\n    trigger: trigger ? { price: trigger.price, reason: trigger.reason, dist: dist(trigger) } : null,\n    strong: strong ? { price: strong.price, reason: strong.reason, dist: dist(strong) } : null,\n    reclaim: S.reclaim != null ? { price: S.reclaim, dist: dist({ price: S.reclaim }) } : null,\n    invalidation: invalid ? { price: invalid.price, reason: invalid.reason, dist: dist(invalid) } : null,\n    vwap: b.vwap, aboveVwap: b.aboveVwap, ema9: b.ema9, ema20: b.ema20, align: b.align, atr: atr,\n    market: market ? market.label : 'Unavailable', setup: setup\n  };\n}\n\n// Multi-day layer, time-of-day volume, path probability and the plain-language\n// \"what now\" block. Kept in its own file so engine.cjs stays the intraday core.\n\n// ---------------------------------------------------------------- volume by time of day\n// The closing auction is not comparable to a normal minute, so relative volume\n// is measured against the SAME minute on previous days. With no history the\n// baseline is null and callers fall back to the session average.\nfunction volumeBaseline(daysRows) {\n  var sum = {}, cnt = {}, days = 0;\n  (daysRows || []).forEach(function (rows) {\n    if (!rows || !rows.length) return;\n    days++;\n    rows.forEach(function (r) { sum[r.time] = (sum[r.time] || 0) + r.volume; cnt[r.time] = (cnt[r.time] || 0) + 1; });\n  });\n  if (days < 2) return null;\n  var by = {};\n  Object.keys(sum).forEach(function (t) { by[t] = sum[t] / cnt[t]; });\n  return { byTime: by, days: days };\n}\nfunction volxTod(bar, baseline) {\n  if (!baseline || !baseline.byTime[bar.time]) return null;\n  var b = baseline.byTime[bar.time];\n  return b > 0 ? Math.round(bar.volume / b * 100) / 100 : null;\n}\n\n// ---------------------------------------------------------------- daily layer\n// daysRows: array of one day's 1-minute rows, oldest first, today last.\n// A US regular session is 390 one-minute bars, 09:30 to 15:59, plus the 16:00\n// auction print. Anything materially short of that is a session still being\n// collected or one that was fetched mid-day — and aggregating it produces a\n// daily candle that is simply wrong, with no outward sign.\n//\n// Measured: a WMT session truncated at 13:20 reported a high of 106.44 when the\n// real high was 106.78, and that number reached the screen as \"yesterday's\n// high\". A partial session is now marked and excluded from anything that claims\n// to be a completed day.\nvar SESSION_BARS = 390, MIN_COVERAGE = 0.85;\n// Counting bars alone is the wrong test. A thinly traded name has no print in\n// some minutes and the feed omits them, so a COMPLETE session can arrive with\n// 343 bars — PGR, BLK and HON were all refused that way. What actually\n// distinguishes a complete session is that it SPANS the session: it starts at\n// the open and runs to the close. A day truncated at 13:20 fails that however\n// many bars it has.\nfunction sessionSpans(rows) {\n  if (!rows || !rows.length) return false;\n  var times = rows.map(function (r) { return r.time; }).sort();\n  return times[0] <= '09:35' && times[times.length - 1] >= '15:55';\n}\nfunction sessionCoverage(rows) {\n  if (!rows || !rows.length) return 0;\n  var regular = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n  return regular.length / SESSION_BARS;\n}\nfunction aggregateSession(rows) {\n  var o = rows[0].open, c = rows[rows.length - 1].close;\n  var h = -Infinity, l = Infinity, v = 0;\n  rows.forEach(function (r) { h = Math.max(h, r.high); l = Math.min(l, r.low); v += r.volume; });\n  var cov = sessionCoverage(rows), spans = sessionSpans(rows);\n  return { date: rows[0].date, open: o, high: h, low: l, close: c, volume: v, range: h - l,\n    closeStrength: h > l ? (c - l) / (h - l) : 0.5,\n    bars: rows.length, coverage: Math.round(cov * 1000) / 1000, spans: spans,\n    complete: spans && cov >= MIN_COVERAGE,\n    hasAuction: rows.some(function (r) { return r.time === '16:00'; }),\n    source: 'aggregated' };\n}\n\nfunction dailyContext(daysRows, opts) {\n  var o2 = opts || {};\n  var days = (daysRows || []).filter(function (r) { return r && r.length; });\n  if (!days.length) return null;\n  var bars = days.map(aggregateSession);\n  // An authoritative daily candle, when the caller has one, replaces the\n  // aggregation for that date entirely. Never a mix of the two.\n  if (o2.authoritative) {\n    var auth = {};\n    (o2.authoritative || []).forEach(function (b) { auth[b.date] = b; });\n    bars = bars.map(function (b) {\n      var a = auth[b.date];\n      if (!a) return b;\n      return { date: b.date, open: a.open, high: a.high, low: a.low, close: a.close,\n        volume: a.volume, range: a.high - a.low,\n        closeStrength: a.high > a.low ? (a.close - a.low) / (a.high - a.low) : 0.5,\n        bars: b.bars, coverage: b.coverage, complete: true, hasAuction: b.hasAuction,\n        source: 'authoritative' };\n    });\n  }\n  // Which of these IS today? Before the open there is no session for today at\n  // all, and treating the newest stored row as \"today\" pushed every previous-day\n  // value one session further back than its label claimed. The caller passes the\n  // current trading date; without one the old positional rule stands, but the\n  // result says which rule was used.\n  var todayDate = o2.todayDate || null;\n  var hasToday = todayDate ? bars.some(function (b) { return b.date === todayDate; }) : true;\n  var todayIdx = todayDate\n    ? (hasToday ? bars.map(function (b) { return b.date; }).indexOf(todayDate) : bars.length)\n    : bars.length - 1;\n  var today = bars[todayIdx] || bars[bars.length - 1] || null;\n  // \"Previous\" must mean the previous COMPLETED session. A partial day is not a\n  // day, and silently using one is how a truncated high became yesterday's high.\n  var prior = bars.slice(0, todayIdx).filter(function (b) { return b.complete; });\n  var excluded = bars.slice(0, todayIdx).filter(function (b) { return !b.complete; });\n  var atrDaily = prior.length ? prior.reduce(function (s, b) { return s + b.range; }, 0) / prior.length : today.range;\n  // Daily trend over the prior sessions: higher closes and higher lows.\n  var trend = 'RANGE', why = 'אין רצף יומי';\n  if (prior.length >= 3) {\n    var last3 = prior.slice(-3);\n    var upCloses = last3.filter(function (b, i) { return i === 0 || b.close > last3[i - 1].close; }).length;\n    var upLows = last3.filter(function (b, i) { return i === 0 || b.low > last3[i - 1].low; }).length;\n    if (upCloses >= 3 && upLows >= 2) { trend = 'UP'; why = 'סגירות ושפלים עולים'; }\n    else if (upCloses <= 1 && upLows <= 1) { trend = 'DOWN'; why = 'סגירות ושפלים יורדים'; }\n    else why = 'מעורב';\n  }\n  var prev = prior.length ? prior[prior.length - 1] : null;\n  // Multi-day levels: prior highs/lows that price reacted to more than once.\n  var levels = [];\n  prior.forEach(function (b) { levels.push({ price: b.high, why: 'גבוה ' + b.date }, { price: b.low, why: 'נמוך ' + b.date }); });\n  var gap = prev ? today.open - prev.close : 0;\n  return { days: bars.length, priorDays: prior.length, trend: trend, trendWhy: why,\n    incomplete: excluded.map(function (b) { return { date: b.date, bars: b.bars,\n      coverage: Math.round(b.coverage * 100) + '%' }; }),\n    hasToday: hasToday, todayDate: todayDate,\n    previousDate: prior.length ? prior[prior.length - 1].date : null,\n    dateRule: todayDate ? 'previous = newest COMPLETE session before ' + todayDate\n      : 'previous = the row before the last (no trading date supplied)',\n    sources: bars.map(function (b) { return { date: b.date, source: b.source, complete: b.complete }; }),\n    prevHigh: prev ? prev.high : null, prevLow: prev ? prev.low : null, prevClose: prev ? prev.close : null,\n    atrDaily: atrDaily, levels: levels, gap: gap, today: today,\n    closeStrength: prev ? prev.closeStrength : null };\n}\n\n\n\n// ---------------------------------------------------------------- level state\n// The current condition of a level, decided from where price is now and what it\n// did on its way here. A support that was lost and then regained reads as\n// reclaimed — never as \"breaking\", which is what it did ten candles ago.\nvar LEVEL_TEXT = {\n  approaching: 'מתקרב', testing: 'נבחנת', broken: 'נשברה', reclaimed: 'נכבשה מחדש',\n  reclaiming: 'נשברה וחוזרת מעליה', held: 'נשמרת', rejected: 'נדחתה', lost: 'אבדה', far: 'רחוקה'\n};\nfunction levelState(A, level, isSupport, look) {\n  if (!A || !A.state || level == null) return null;\n  var bars = A.bars, n = bars.length, b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n  var w = bars.slice(Math.max(0, n - (look || 20)));\n  var d = (b.close - level) / atr;                       // + = price above the level\n  var near = w.filter(function (x) { return x.low <= level + 0.25 * atr && x.high >= level - 0.25 * atr; });\n  var closedBelow = w.filter(function (x) { return x.close < level - 0.15 * atr; }).length;\n  var closedAbove = w.filter(function (x) { return x.close > level + 0.15 * atr; }).length;\n  var lastTwoBelow = n >= 2 && bars[n - 1].close < level - 0.1 * atr && bars[n - 2].close < level - 0.1 * atr;\n  var lastTwoAbove = n >= 2 && bars[n - 1].close > level + 0.1 * atr && bars[n - 2].close > level + 0.1 * atr;\n  var recoveries = w.filter(function (x) { return x.low < level && x.close > level; }).length;\n  // Bars whose LOW went through a support, or whose HIGH went through a\n  // resistance, without closing beyond it: pierced, not merely approached.\n  var pierced = isSupport\n    ? w.filter(function (x) { return x.low < level - 0.02 * atr; }).length\n    : w.filter(function (x) { return x.high > level + 0.02 * atr; }).length;\n  var rejections = w.filter(function (x) { return x.high > level && x.close < level; }).length;\n\n  var state;\n  if (isSupport) {\n    // \"Held\" must mean the level was never given up. Requiring only two\n    // recoveries let a support that CLOSED below five times in a row still read\n    // as held: GOOGL closed 337.31, 337.31, 337.17, 337.32, 337.275 under\n    // 337.57 and came back at 337.75, which is a break and a reclaim, not a\n    // level that was defended.\n    if (lastTwoBelow) state = 'broken';\n    else if (closedBelow >= 2 && lastTwoAbove) state = 'reclaimed';\n    else if (closedBelow >= 2 && d > 0) state = 'reclaiming';\n    // Closes are not the whole story. GOOGL's 338.51 was traded through on four\n    // separate bars — lows of 338.4233, 338.4096, 338.43, 338.42 — and still\n    // read as held because none of them CLOSED below. A level price went\n    // through is not a level that was defended.\n    else if (closedBelow === 0 && pierced === 0 && recoveries >= 2 && d > 0) state = 'held';\n    else if (closedBelow === 0 && pierced >= 2 && d > 0) state = 'reclaiming';\n    else if (Math.abs(d) <= 0.25) state = 'testing';\n    else if (Math.abs(d) <= 1.0) state = 'approaching';\n    else state = 'far';\n  } else {\n    if (lastTwoAbove) state = 'broken';                  // resistance broken = price accepted above\n    // \"Lost\" means the level stopped functioning. A resistance with price\n    // sitting UNDER it is still a resistance: it was rejected, not lost.\n    else if (closedAbove >= 2 && lastTwoBelow && d >= 0) state = 'lost';\n    else if (closedAbove >= 2 && lastTwoBelow) state = 'rejected';\n    else if (rejections >= 1 && d < 0) state = 'rejected';\n    else if (Math.abs(d) <= 0.25) state = 'testing';\n    else if (Math.abs(d) <= 1.0) state = 'approaching';\n    else state = 'far';\n  }\n  return { price: level, state: state, text: LEVEL_TEXT[state], distanceR: d,\n    touches: near.length, recoveries: recoveries, rejections: rejections, isSupport: !!isSupport };\n}\n\n// ---------------------------------------------------------------- buying / selling pressure\n//\n// IMPORTANT: this bar feed carries no order book and no trade-by-trade tape.\n// There is no bid/ask, no Level 2, no per-trade aggressor flag. What follows is\n// pressure INFERRED FROM CANDLES: where each minute closed inside its own range,\n// weighted by that minute's volume. Every result is labelled `source: 'candles'`\n// so the UI can say so and never imply it read the book.\n//\n//   buyVolume  = volume * (close - low)  / (high - low)\n//   sellVolume = volume * (high - close) / (high - low)\n//\n// which is the standard close-location value applied to volume. A minute that\n// closes on its high counts fully as buying, on its low fully as selling.\nfunction pressure(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, bars = A.bars, n = bars.length;\n  if (!n) return null;\n  var WIN = c.window || 15, RECENT = c.recent || 5;\n  var b = bars[n - 1], atr = b.atr20 || b.avgRange || 1;\n\n  var split = function (list) {\n    var buy = 0, sell = 0;\n    list.forEach(function (x) {\n      var rng = x.high - x.low;\n      // A zero-range minute has no information about who won it.\n      if (rng <= 0 || !x.volume) return;\n      var clv = (x.close - x.low) / rng;\n      buy += x.volume * clv; sell += x.volume * (1 - clv);\n    });\n    var tot = buy + sell;\n    return { buy: buy, sell: sell, total: tot, buyPct: tot > 0 ? buy / tot * 100 : 50 };\n  };\n\n  // The closing auction is excluded: it is one print, not a minute of trading.\n  var usable = bars.filter(function (x) { return !x.auction; });\n  var win = usable.slice(-WIN), recent = usable.slice(-RECENT), prior = usable.slice(-(WIN), -RECENT);\n  if (win.length < 3) return null;\n\n  var W = split(win), R = split(recent), P = prior.length >= 3 ? split(prior) : null;\n  var buyPct = Math.round(W.buyPct), sellPct = 100 - buyPct;\n\n  // Strengthening / weakening is measured per side, on volume actually traded,\n  // not on the percentage alone: a side can gain share while doing less.\n  var perBar = function (s, list) { return list.length ? s / list.length : 0; };\n  var dBuy = P ? perBar(R.buy, recent) - perBar(P.buy, prior) : 0;\n  var dSell = P ? perBar(R.sell, recent) - perBar(P.sell, prior) : 0;\n  var scale = P ? Math.max(perBar(P.buy, prior) + perBar(P.sell, prior), 1) : 1;\n  // A side is only strengthening if it is doing MORE and taking MORE of the\n  // flow. Without the share test a volume surge would read as both sides\n  // strengthening at once, which tells the trader nothing.\n  var dShare = P ? R.buyPct - P.buyPct : 0;\n  var trendOf = function (d, shareDelta) {\n    var rel = d / scale;\n    if (rel >= 0.15 && shareDelta > -3) return 'מתחזקים';\n    if (rel <= -0.15 || shareDelta < -8) return 'נחלשים';\n    return 'ללא שינוי';\n  };\n  var buyersTrend = trendOf(dBuy, dShare), sellersTrend = trendOf(dSell, -dShare);\n\n  // Is the nearby level being defended or absorbed?\n  var T = c.tactical || tactical(A);\n  // Level condition comes from levelState(), which is anchored to the current\n  // price. Volume only adds the \"absorbed\" nuance on top of it.\n  var levelVerdict = function (level, isSupport) {\n    if (!level) return null;\n    var ls = levelState(A, level.price, isSupport);\n    if (!ls) return null;\n    var near = usable.filter(function (x) {\n      return x.low <= level.price + 0.3 * atr && x.high >= level.price - 0.3 * atr;\n    }).slice(-12);\n    var vol = near.length ? near.reduce(function (s, x) { return s + x.volume; }, 0) / near.length : 0;\n    var avgVol = usable.reduce(function (s, x) { return s + x.volume; }, 0) / usable.length;\n    var heavy = avgVol > 0 && vol / avgVol >= 1.2;\n    var parked = near.filter(function (x) { return Math.abs(x.close - level.price) <= 0.2 * atr; }).length;\n    var absorbed = heavy && parked >= 3 && (ls.state === 'testing' || ls.state === 'approaching');\n    return { price: level.price, state: ls.state, verdict: absorbed ? 'נבלעת' : ls.text,\n      distanceR: ls.distanceR, touches: near.length, heavy: heavy,\n      held: ls.recoveries, through: isSupport ? (ls.state === 'broken' ? 2 : 0) : (ls.state === 'broken' ? 2 : 0) };\n  };\n  var sup = levelVerdict(T.support, true), res = levelVerdict(T.resistance, false);\n\n  // Does the flow agree with the setup on the table?\n  var plan = c.plan || null;\n  var bullishSetup = plan && ['WAITING_FOR_ZONE', 'IN_ZONE', 'READY_PARTIAL', 'WAITING_FOR_CONFIRMATION', 'READY_ADD', 'ACTIVE', 'TAKE_PROFIT_AREA'].indexOf(plan.state) >= 0;\n  var side = buyPct >= 58 ? 'buyers' : buyPct <= 42 ? 'sellers' : 'balanced';\n  // Who is winning now and which way that is heading are two different facts.\n  // \"Buyers 64%\" plus \"buyers weakening\" is not support for a long — it is a\n  // lead that is being given back, and the wording has to say so.\n  // When BOTH sides are fading, the buyers' edge may be widening but their\n  // pressure is not strengthening. Saying it is contradicts the two trend\n  // labels printed directly above it.\n  // Who leads and how each side is moving are separate facts, and the sentence\n  // must be built from BOTH. Saying \"buyers strengthening\" because the SELLERS\n  // faded, while the buyers' own trend reads unchanged, contradicts the two\n  // labels printed directly above it.\n  var UP = 'מתחזקים', DOWN = 'נחלשים', SAME = 'ללא שינוי';\n  var leaderTrend = side === 'sellers' ? sellersTrend : buyersTrend;\n  var otherTrend = side === 'sellers' ? buyersTrend : sellersTrend;\n  // 'own'      the leading side is itself doing more\n  // 'relative' the leader is unchanged or fading, but the other side fades faster\n  // 'fading'   the leading side is giving its lead back\n  var direction = leaderTrend === UP ? 'own'\n    : leaderTrend === DOWN ? (otherTrend === DOWN ? 'relative' : 'fading')\n    : otherTrend === DOWN ? 'relative'\n    : otherTrend === UP ? 'fading' : 'steady';\n\n  var leadWord = side === 'sellers' ? 'המוכרים' : 'הקונים';\n  var conclusion;\n  if (side === 'balanced') {\n    conclusion = buyersTrend === UP && sellersTrend !== UP ? 'הכוחות שקולים, הקונים משתפרים'\n      : sellersTrend === UP && buyersTrend !== UP ? 'הכוחות שקולים, המוכרים משתפרים'\n      : 'הכוחות שקולים';\n  } else if (direction === 'own') {\n    conclusion = leadWord + ' מובילים והלחץ שלהם מתחזק';\n  } else if (direction === 'relative') {\n    // The leader is not stronger. The gap widened because the other side eased.\n    conclusion = leadWord + ' מובילים; היתרון היחסי שלהם משתפר משום ש'\n      + (side === 'sellers' ? 'הקונים' : 'המוכרים') + ' נחלשים'\n      + (leaderTrend === DOWN ? ', אך גם הלחץ שלהם עצמו נחלש' : '');\n  } else if (direction === 'fading') {\n    conclusion = leadWord + ' עדיין מובילים, אבל הלחץ שלהם נחלש';\n  } else {\n    conclusion = leadWord + ' מובילים, ללא שינוי בעוצמה';\n  }\n  var agreement = !bullishSetup ? 'לא רלוונטי'\n    : side === 'buyers' ? (direction === 'own' ? 'תומך'\n        : direction === 'fading' ? 'תומך אך נחלש'\n        : direction === 'relative' ? 'תומך במידה' : 'תומך')\n    : side === 'sellers' ? 'סותר'\n    : (buyersTrend === 'נחלשים' ? 'נוטה נגד' : 'ניטרלי');\n\n  // Net contribution to the up-side, in the same units the probability model uses.\n  var tilt = 0;\n  if (side === 'buyers') tilt += 1; else if (side === 'sellers') tilt -= 1;\n  if (buyersTrend === 'מתחזקים') tilt += 1;\n  if (buyersTrend === 'נחלשים') tilt -= 1;\n  if (sellersTrend === 'מתחזקים') tilt -= 1;\n  if (sellersTrend === 'נחלשים') tilt += 1;\n  if (sup && (sup.state === 'held' || sup.state === 'reclaimed')) tilt += 1;\n  if (sup && (sup.state === 'broken' || sup.verdict === 'נבלעת')) tilt -= 1;\n  if (res && res.state === 'broken') tilt += 1;\n  if (res && (res.state === 'rejected' || res.verdict === 'נבלעת')) tilt -= 1;\n\n  return {\n    source: 'candles',                       // never 'orderbook' — we have none\n    hasOrderBook: false, hasTape: false,\n    buyPct: buyPct, sellPct: sellPct,\n    side: side, buyersTrend: buyersTrend, sellersTrend: sellersTrend,\n    support: sup, resistance: res, agreement: agreement, tilt: tilt,\n    direction: direction, conclusion: conclusion,\n    window: win.length, recentWindow: recent.length,\n    summary: 'קונים ' + buyPct + '% · מוכרים ' + sellPct + '%',\n    trendSummary: 'קונים ' + buyersTrend + ' · מוכרים ' + sellersTrend\n  };\n}\n\n// ---------------------------------------------------------------- features\n// A compact description of \"situations like this one\", used both to bucket\n// history and to score the model fallback.\nfunction features(A, ctx, upper, lower) {\n  var S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var vx = (ctx && ctx.baseline) ? volxTod(b, ctx.baseline) : null;\n  if (vx == null) vx = b.volx;\n  var mom = momentum(A);\n  return {\n    structure: S.announced || S.trend,\n    vwap: b.aboveVwap ? 'above' : 'below',\n    ema: b.align,\n    momentum: mom.label,\n    vol: vx >= 1.5 ? 'high' : vx >= 0.8 ? 'normal' : 'low',\n    daily: ctx && ctx.daily ? ctx.daily.trend : 'NA',\n    dUp: Math.round(((upper - b.close) / atr) * 2) / 2,\n    dDown: Math.round(((b.close - lower) / atr) * 2) / 2,\n    tod: b.time < '10:30' ? 'open' : b.time < '14:30' ? 'mid' : 'late',\n    flow: (function () { var p = (ctx && ctx.pressure) || pressure(A, {}); return p ? p.side : 'NA'; })()\n  };\n}\nfunction bucketKey(f, coarse) {\n  return coarse\n    ? [f.structure, f.vwap, f.daily].join('|')\n    : [f.structure, f.vwap, f.ema, f.momentum, f.vol, f.daily, f.dUp, f.dDown].join('|');\n}\n\n// ---------------------------------------------------------------- calibration\n// Replays the loaded historical days and records, for each situation, whether\n// price rose by the required distance before falling by the other one, within\n// the horizon. These are real observed outcomes from the symbol's own history —\n// when there are too few of them the caller must not show a percentage.\nfunction calibrate(daysRows, opts) {\n  var o = opts || {}, horizon = o.horizon || 60, step = o.step || 2;\n  var table = {}, coarse = {}, total = 0;\n  (daysRows || []).forEach(function (rows) {\n    if (!rows || rows.length < 120) return;\n    var A = analyze(rows, o.engine || { K: 3 });\n    if (!A) return;\n    var bars = A.bars, n = bars.length;\n    for (var i = 60; i < n - horizon; i += step) {\n      var b = bars[i], atr = b.atr20 || b.avgRange || 1;\n      // sample a spread of target distances so the table can answer different asks\n      [0.5, 1, 1.5, 2].forEach(function (du) {\n        [0.5, 1, 1.5, 2].forEach(function (dd) {\n          var up = b.close + du * atr, dn = b.close - dd * atr, hit = null;\n          for (var k = i + 1; k <= i + horizon && k < n; k++) {\n            if (bars[k].high >= up) { hit = 'up'; break; }\n            if (bars[k].low <= dn) { hit = 'down'; break; }\n          }\n          if (!hit) return;\n          var sub = { state: A.states[i], bar: b };\n          var f = features({ state: A.states[i], bars: bars.slice(0, i + 1), swings: A.swings, states: A.states.slice(0, i + 1) },\n            { daily: o.daily, baseline: o.baseline }, up, dn);\n          [bucketKey(f, false), 'C:' + bucketKey(f, true) + '|' + f.dUp + '|' + f.dDown].forEach(function (key, idx) {\n            var t = idx === 0 ? table : coarse;\n            t[key] = t[key] || { n: 0, up: 0 };\n            t[key].n++; if (hit === 'up') t[key].up++;\n          });\n          total++;\n        });\n      });\n    }\n  });\n  return { table: table, coarse: coarse, total: total };\n}\n\n// ---------------------------------------------------------------- probability\n// Answers ONE defined question: does price reach `upper` before `lower`, within\n// `horizonMin` minutes. Empirical when the symbol's own history has enough\n// comparable cases; otherwise a transparent weighted model, reported as a bias\n// with a confidence score rather than a fake percentage.\n// Pull an estimate toward 50 in proportion to how much the model actually\n// knows. Full confidence (>=70) keeps the number as computed.\nfunction shrink(pct, confidence) {\n  var f = Math.max(0, Math.min(1, confidence / 70));\n  return Math.round(50 + (pct - 50) * f);\n}\n\n// How much of the session that SHOULD have happened by now do we actually hold?\n// A window that starts at 11:02 on a day that opened at 09:30 is not a session:\n// its open is not the open, its VWAP is not the VWAP, and its volume is a\n// fraction. Everything derived from the session is refused until this passes.\nfunction sessionCompleteness(rows, opts) {\n  var o = opts || {};\n  var out = { firstBar: null, lastBar: null, receivedMinutes: 0, expectedMinutes: null,\n    coveragePct: null, fromOpen: false, complete: false };\n  if (!rows || !rows.length) return out;\n  var reg = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n  if (!reg.length) return out;\n  var times = reg.map(function (r) { return r.time; }).sort();\n  out.firstBar = times[0];\n  out.lastBar = times[times.length - 1];\n  var seen = {}; reg.forEach(function (r) { seen[r.time] = 1; });\n  out.receivedMinutes = Object.keys(seen).length;\n  // Minutes that should exist between the open and the newest bar we hold.\n  var toMin = function (t) { var p = t.split(':'); return (+p[0]) * 60 + (+p[1]); };\n  out.expectedMinutes = Math.max(1, toMin(out.lastBar) - toMin('09:30') + 1);\n  out.coveragePct = Math.round(out.receivedMinutes / out.expectedMinutes * 100);\n  out.fromOpen = out.firstBar <= '09:35';\n  out.missingFromOpen = out.fromOpen ? 0 : toMin(out.firstBar) - toMin('09:30');\n  // Holes AFTER the first bar are a separate failure and were not reported at\n  // all: 49 bars scattered between 11:02 and 13:44 is not the same thing as a\n  // clean window starting late. Both numbers, always.\n  out.spanMinutes = toMin(out.lastBar) - toMin(out.firstBar) + 1;\n  out.missingInside = Math.max(0, out.spanMinutes - out.receivedMinutes);\n  out.missingTotal = out.missingFromOpen + out.missingInside;\n  out.contiguous = out.missingInside === 0;\n  // Both conditions: it must start at the open AND have most of the minutes in\n  // between. Either alone lets a hole through.\n  out.complete = out.fromOpen && out.coveragePct >= (o.minCoverage || 85);\n  return out;\n}\n\nfunction pathProbability(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = c.tactical || tactical(A);\n  var upper = c.upper != null ? c.upper : (T.resistance ? T.resistance.price : b.close + atr);\n  var lower = c.lower != null ? c.lower : (T.support ? T.support.price : b.close - atr);\n  var horizon = c.horizonMin || 60;\n  var f = features(A, c, upper, lower);\n  var out = { upper: upper, lower: lower, horizonMin: horizon, features: f, why: [] };\n\n  // A probability is only meaningful if the two levels are far enough apart to\n  // mean something for this instrument. Asking whether price reaches 146.63\n  // before 146.62 — one cent, a fraction of a normal minute's range — produces\n  // a confident-looking number about nothing.\n  // The question only means \"up or down\" when the two levels BRACKET the price.\n  // With both below it, \"reaches 187.34 before 187.27\" asks which small decline\n  // happens first — and reporting that as UP 78% reads as a claim the stock will\n  // rise. It also hands the model a free +6 for \"the upper level is closer\",\n  // which is pure geometry: three cents against ten.\n  var px = b.close;\n  out.price = px;\n  out.bracketed = lower < px && px < upper;\n  // \"Reaches 338.66 before 338.63\" with price at 338.61 asks whether the far\n  // level is touched before the near one on the SAME side, which cannot happen\n  // on a continuous path — 338.63 is passed on the way. The bracket test below\n  // catches it, but say plainly why rather than reporting it as a coincidence.\n  out.samSideOrdering = (upper > px && lower > px && lower < upper)\n    || (upper < px && lower < px && lower > upper);\n  if (!out.bracketed) {\n    out.up = null; out.down = null; out.confidence = 0;\n    out.notDirectional = true;\n    out.side = upper <= px ? 'both_below' : 'both_above';\n    if (out.samSideOrdering)\n      out.why.push('הרמות באותו צד והרחוקה נמצאת אחרי הקרובה — לא ניתן להגיע לאחת לפני השנייה בדרך רציפה');\n    out.why.push(upper <= px\n      ? 'שתי הרמות מתחת למחיר — השאלה אינה עלייה מול ירידה אלא איזו ירידה מגיעה קודם, ולכן לא מוצגת כהסתברות כיוון'\n      : 'שתי הרמות מעל המחיר — השאלה אינה עלייה מול ירידה, ולכן לא מוצגת כהסתברות כיוון');\n    return out;\n  }\n\n  var band = Math.abs(upper - lower);\n  var MIN_BAND_ATR = 0.25;\n  if (atr > 0 && band < MIN_BAND_ATR * atr) {\n    out.up = null; out.down = null; out.confidence = 0; out.meaningless = true;\n    out.band = band; out.minBand = MIN_BAND_ATR * atr;\n    out.why.push('הרמות קרובות מדי זו לזו (' + (Math.round(band * 100) / 100) +\n      ') ביחס לתנודה הרגילה — הסתברות כאן חסרת משמעות');\n    return out;\n  }\n\n  // 1. empirical\n  if (c.calibration) {\n    var exact = c.calibration.table[bucketKey(f, false)];\n    var coarse = c.calibration.coarse['C:' + bucketKey(f, true) + '|' + f.dUp + '|' + f.dDown];\n    var hit = (exact && exact.n >= 30) ? exact : (coarse && coarse.n >= 30) ? coarse : null;\n    if (hit) {\n      out.source = 'empirical'; out.n = hit.n;\n      out.up = Math.round(hit.up / hit.n * 100);\n      out.confidence = Math.min(90, 40 + Math.round(Math.min(hit.n, 400) / 8));\n      out.raw = out.up;\n      out.up = shrink(out.raw, out.confidence);\n      out.down = 100 - out.up;\n      out.lowConfidence = out.confidence < 50;\n      out.why.push(hit.n + ' מקרים דומים בהיסטוריה של הנייר');\n      return out;\n    }\n    out.sampled = (exact ? exact.n : 0) + (coarse ? coarse.n : 0);\n  }\n\n  // 2. transparent model. Each layer moves the odds; none of it is invented,\n  // and the result is labelled as a model bias, not an observed frequency.\n  var score = 0, add = function (pts, txt) { if (!pts) return; score += pts; out.why.push((pts > 0 ? '+' : '') + pts + ' ' + txt); };\n  add(f.structure === 'UP' ? 2 : f.structure === 'DOWN' ? -2 : 0, 'מבנה תוך-יומי ' + f.structure);\n  add(f.daily === 'UP' ? 2 : f.daily === 'DOWN' ? -2 : 0, 'מגמה רב-יומית ' + f.daily);\n  add(f.vwap === 'above' ? 1 : -1, 'מחיר ' + (f.vwap === 'above' ? 'מעל' : 'מתחת') + ' VWAP');\n  add(f.ema === 'bull' ? 1 : f.ema === 'bear' ? -1 : 0, 'EMA ' + f.ema);\n  add(f.momentum === 'PUSHING' || f.momentum === 'RECOVERY' ? 1 : f.momentum === 'SELLING' ? -1 : 0, 'מומנטום ' + f.momentum);\n  add(c.market === 'Bullish' ? 1 : c.market === 'Bearish' ? -1 : 0, 'שוק ' + (c.market || 'לא ידוע'));\n  var pres = c.pressure || pressure(A, { tactical: T });\n  if (pres) {\n    out.pressure = pres;\n    add(Math.max(-3, Math.min(3, pres.tilt)), 'לחץ קונים/מוכרים ' + pres.buyPct + '/' + pres.sellPct);\n  }\n  // Distance asymmetry: the nearer level is simply likelier to be reached.\n  var dU = (upper - b.close) / atr, dD = (b.close - lower) / atr;\n  var geom = dU + dD > 0 ? (dD - dU) / (dU + dD) : 0;\n  add(Math.round(geom * 3), 'הרמה ' + (geom > 0 ? 'העליונה' : 'התחתונה') + ' קרובה יותר');\n  var pct = Math.round(100 / (1 + Math.exp(-score / 3.2)));\n  out.source = 'model';\n  out.confidence = Math.max(20, Math.min(55, 25 + Math.abs(score) * 3));\n  out.raw = Math.max(10, Math.min(90, pct));\n  // A number like 87% next to \"confidence 43\" reads as certainty the model does\n  // not have. The displayed probability is pulled toward 50 in proportion to\n  // the confidence, so the two can never tell different stories.\n  out.up = shrink(out.raw, out.confidence);\n  out.down = 100 - out.up;\n  out.lowConfidence = out.confidence < 50;\n  out.bias = out.up >= 60 ? 'נטייה שורית' : out.up <= 40 ? 'נטייה דובית' : 'ללא נטייה ברורה';\n  return out;\n}\n\n// ---------------------------------------------------------------- what now\n// Plain language. No HH/HL/VWAP/R in the user-facing strings.\nvar ACTIONS = {\n  DO_NOT_BUY: 'לא לקנות',\n  START_WATCHING: 'להתחיל לעקוב',\n  WAIT_FOR_CONFIRMATION: 'לחכות לאישור',\n  ENTRY_AVAILABLE: 'אפשר להיכנס',\n  HOLD: 'להחזיק',\n  TAKE_PARTIAL: 'לממש חלק',\n  MOVE_STOP: 'להעלות סטופ',\n  EXIT: 'לצאת',\n  SETUP_CANCELLED: 'ה-setup בוטל',\n  SESSION_CLOSED: 'המסחר הסתיים',\n  DATA_STALE: 'נתונים לא עדכניים — לא לסחור',\n  WATCH_ONLY: 'לא לסחור — רק לעקוב',\n  DO_NOT_CHASE: 'לא לרדוף — להמתין לפולבק'\n};\nvar PLAN_TO_ACTION = {\n  NO_SETUP: 'DO_NOT_BUY',\n  WAITING_FOR_ZONE: 'START_WATCHING',\n  IN_ZONE: 'WAIT_FOR_CONFIRMATION',\n  READY_PARTIAL: 'ENTRY_AVAILABLE',\n  WAITING_FOR_CONFIRMATION: 'WAIT_FOR_CONFIRMATION',\n  READY_ADD: 'ENTRY_AVAILABLE',\n  ACTIVE: 'HOLD',\n  TAKE_PROFIT_AREA: 'TAKE_PARTIAL',\n  DO_NOT_CHASE: 'DO_NOT_CHASE',\n  FAILED: 'SETUP_CANCELLED'\n};\nfunction n2(x) { return x == null ? null : (Math.round(x * 100) / 100).toFixed(2); }\nfunction scenarioLabelFor(P, watch) {\n  if (!P || !P.kind) return 'ללא setup';\n  if (P.kind === 'breakout') return 'פריצה מעל ' + n2(watch);\n  return P.zone ? 'חזרה לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) : 'פולבק';\n}\n\nfunction whatNow(A, ctx) {\n  if (!A || !A.state) return null;\n  var c = ctx || {}, S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = c.tactical || tactical(A);\n  var P = c.plan || executionPlan(A, { label: c.market || 'Neutral' });\n  var ended = c.sessionEnded;\n\n  var watch = T.resistance ? T.resistance.price : (P && P.zone ? P.zone[1] : b.dayHigh);\n  var hasZone = !!(P && P.zone && P.entry != null && P.addAbove != null);\n  var lower = T.support ? T.support.price : (P && P.invalidation != null ? P.invalidation : b.dayLow);\n  var prob = c.probability || pathProbability(A, { tactical: T, upper: watch, lower: lower,\n    horizonMin: 60, daily: c.daily, baseline: c.baseline, calibration: c.calibration, market: c.market });\n\n  // ---- one active scenario only. The plan already picked pullback OR\n  // breakout; whatNow must never narrate the other one as a live instruction.\n  var scenario = !P || !P.kind ? { kind: 'none', label: 'אין setup פעיל' }\n    : P.kind === 'breakout' ? { kind: 'breakout', label: 'פריצה מעל ' + n2(watch) }\n    : { kind: 'pullback', label: 'חזרה לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) };\n\n  // ---- no-edge gate. When the model's own numbers say there is nothing here,\n  // it must not offer an entry \"if X happens\".\n  var edge = c.edge || null;\n  var noEdge = !!(edge && edge.noEdge);\n  var stale = !!c.stale;\n  // The model's own cancellation rule, applied to the model's own price. If the\n  // last close is already through the level that voids the idea, the idea is\n  // void — it cannot still read START_WATCHING.\n  var cancelAt = (c.levels && c.levels.tacticalStop != null) ? c.levels.tacticalStop\n    : (P && P.invalidation != null ? P.invalidation : null);\n  var alreadyCancelled = !ended && cancelAt != null && b.close != null && b.close < cancelAt;\n  // Staleness outranks the plan. A setup derived from bars that stopped hours\n  // ago is a description of a market that has already moved.\n  var actionKey = ended ? 'SESSION_CLOSED'\n    : stale ? 'DATA_STALE'\n    : alreadyCancelled ? 'SETUP_CANCELLED'\n    : noEdge ? 'WATCH_ONLY'\n    : (PLAN_TO_ACTION[P ? P.state : 'NO_SETUP'] || 'DO_NOT_BUY');\n  var W = { action: actionKey, actionText: ACTIONS[actionKey], sessionEnded: !!ended,\n    stale: stale, cancelled: alreadyCancelled, price: b.close, watch: watch,\n    // A probability computed from stale bars is not a probability about now.\n    // A probability is a claim about the next hour. After the close there is no\n    // next hour, and on stale bars it is a claim about a market that has moved.\n    probability: (stale || ended || (prob && (prob.meaningless || prob.notDirectional))) ? null : prob,\n    probabilityRaw: prob || null,\n    plan: P, scenario: (stale || alreadyCancelled) ? { kind: 'none', label: 'אין setup פעיל' } : scenario,\n    noEdge: noEdge, edge: edge, up: [], down: [], why: [] };\n\n  // The one line that matters\n  if (stale) W.next = 'הנרות האחרונים ישנים — אין כאן איתות חי. הרמות למטה הן לעיון בלבד.';\n  else if (alreadyCancelled) W.next = 'המחיר כבר מתחת ל-' + n2(cancelAt) + ' — התרחיש בוטל. לחכות למבנה חדש.';\n  else if (ended) W.next = 'המסחר הסתיים — הרמות למטה הן לקראת המסחר הבא, לא להוראה עכשיו';\n  else if (noEdge) W.next = 'אין כרגע יתרון מספיק לכניסה. לעקוב אם המחיר מגיע ל-' + n2(watch) + '.';\n  else if (P && P.state === 'FAILED') W.next = 'אין כניסה. צריך מצב חדש לפני שמסתכלים שוב.';\n  else if (P && (P.state === 'READY_PARTIAL' || P.state === 'READY_ADD')) W.next = 'המחיר במקום שתכננו — ' + P.headline;\n  else W.next = 'לשים לב אם המחיר מגיע ל-' + n2(watch);\n\n  // If it goes up\n  var above = T.above.filter(function (l) { return l.price > b.close + 0.05 * atr; });\n  // The narrative must quote the SAME targets the levels block shows. Deriving\n  // them separately here is how the text came to say 'second target 146.66'\n  // while the levels said 146.73.\n  var canon = c.levels || null;\n  var t1 = (canon && canon.target1 != null) ? canon.target1\n    : (above[1] ? above[1].price : watch + 1.2 * atr);\n  var t2 = (canon && canon.target2 != null) ? canon.target2\n    : (above[2] && above[2].price >= t1 + 0.2 * atr ? above[2].price : t1 + 1.5 * atr);\n  // Two targets that print as the same number are not two targets. On quiet or\n  // low-priced instruments the rounded levels collided.\n  // Compare what will actually be PRINTED. Two levels 0.004 apart are distinct\n  // numbers that render as the same string, which is what the reader sees.\n  if (t1 != null && t2 != null && n2(t1) === n2(t2)) t2 = t1 + Math.max(0.01, 1.0 * atr);\n  if (stale) {\n    // Reference only. The last idea is described in the past tense, never as\n    // something to act on.\n    W.up.push('התרחיש האחרון שנרשם: ' + scenarioLabelFor(P, watch) + ' — לעיון בלבד');\n    W.up.push('אין הוראת כניסה על נתונים ישנים');\n  } else if (noEdge && !ended) {\n    // What would have to change before any entry is even discussed.\n    W.up.push('אם המחיר עולה מעל ' + n2(watch) + ' ונשאר שם — ייבחן מחדש');\n    W.up.push('כרגע אין הוראת כניסה');\n  } else if (!ended) {\n    if (P && P.state === 'READY_PARTIAL' && hasZone) {\n      W.up.push('אם המחיר נשאר מעל ' + n2(P.zone[0]) + ' — אפשר להיכנס בחלק מהסכום סביב ' + n2(P.entry));\n      W.up.push('אישור חזק יותר מעל ' + n2(P.addAbove) + ' (המערכת אינה יודעת אם יש פוזיציה)');\n    } else if (scenario.kind === 'breakout' && P.entry != null) {\n      W.up.push('אם עובר את ' + n2(watch) + ' ונשאר מעל — אפשר להיכנס סביב ' + n2(P.entry));\n    } else if (scenario.kind === 'pullback' && hasZone) {\n      W.up.push('אם המחיר יורד לאזור ' + n2(P.zone[0]) + '–' + n2(P.zone[1]) + ' ונבלם — כניסה חלקית ב-' + n2(P.entry));\n      W.up.push('אישור חזק יותר מעל ' + n2(P.addAbove));\n    } else {\n      W.up.push('אם עולה מעל ' + n2(watch) + ' ונשאר שם — להתחיל לעקוב מקרוב');\n    }\n    if (!noEdge) { W.up.push('יעד ראשון ' + n2(t1) + ' — שם לממש חלק'); W.up.push('יעד שני ' + n2(t2)); }\n  } else {\n    W.up.push('הרמה החשובה למעלה הייתה ' + n2(watch));\n  }\n\n  // If it goes down\n  var below = T.below.filter(function (l) { return l.price < b.close - 0.05 * atr; });\n  var s1 = T.support ? T.support.price : (below[0] ? below[0].price : b.close - atr);\n  var s2 = below.find(function (l) { return l.price < s1 - 0.1 * atr; });\n  var s3 = P && P.invalidation != null ? P.invalidation : (s2 ? s2.price - 0.5 * atr : s1 - atr);\n  if (stale) {\n    // The DOWN block describes the downside, in the past tense. It must not\n    // repeat the UP block's lines — that duplication reached the screen.\n    W.down.push('התמיכה האחרונה שנרשמה: ' + n2(s1) + ' — לעיון בלבד');\n    W.down.push('אין רמת ביטול פעילה על נתונים ישנים');\n  } else if (noEdge && !ended) {\n    W.down.push('אם יורד ל-' + n2(s1) + ' — לבדוק אם קונים נכנסים שם');\n    W.down.push('מתחת ' + n2(s3) + ' — התרחיש החיובי יורד מהפרק');\n  } else if (!ended) {\n    W.down.push('אם יורד ל-' + n2(s1) + ' — לבדוק אם יש קונים שעוצרים את הירידה');\n    if (s2) W.down.push('אם שובר את ' + n2(s2.price) + ' — התרחיש החיובי נחלש');\n    W.down.push('אם יורד מתחת ' + n2(s3) + ' — לבטל, לא להיכנס');\n  } else {\n    W.down.push('התמיכה החשובה הייתה ' + n2(s1));\n  }\n\n  // Why, in plain words\n  var d = c.daily;\n  if (d) W.why.push('מגמה של כמה ימים: ' + (d.trend === 'UP' ? 'עולה' : d.trend === 'DOWN' ? 'יורדת' : 'ללא כיוון'));\n  W.why.push('היום: ' + (S.trend === 'UP' ? 'עולה' : S.trend === 'DOWN' ? 'יורד' : 'ללא כיוון'));\n  var mom = momentum(A);\n  W.why.push('בדקות האחרונות: ' + ({ PUSHING: 'לוחץ למעלה', PULLBACK: 'נסוג', RECOVERY: 'מתאושש', SELLING: 'נמכר', FLAT: 'שקט' }[mom.label]));\n  W.why.push(b.aboveVwap ? 'מעל ממוצע היום' : 'מתחת לממוצע היום');\n  var vx = c.baseline ? volxTod(b, c.baseline) : null;\n  if (vx != null) W.why.push('מחזור מול אותה דקה בימים קודמים: ' + vx.toFixed(1) + '×');\n  var pres = c.pressure || pressure(A, { tactical: T, plan: P });\n  if (pres) {\n    W.pressure = pres;\n    W.why.push('בדקות האחרונות ' + (pres.side === 'buyers' ? 'הקונים חזקים יותר' : pres.side === 'sellers' ? 'המוכרים חזקים יותר' : 'הכוחות שקולים')\n      + ' (' + pres.buyPct + '/' + pres.sellPct + ')');\n    if (pres.support && pres.support.state !== 'far')\n      W.why.push('התמיכה ב-' + n2(pres.support.price) + ' ' + pres.support.verdict);\n    if (pres.resistance && pres.resistance.state !== 'far')\n      W.why.push('ההתנגדות ב-' + n2(pres.resistance.price) + ' ' + pres.resistance.verdict);\n  }\n  // Identical lines are never informative twice, whichever branch produced them.\n  var uniq = function (list) { var seen = {}; return list.filter(function (x) {\n    if (seen[x]) return false; seen[x] = 1; return true; }); };\n  W.up = uniq(W.up); W.down = uniq(W.down); W.why = uniq(W.why);\n  return W;\n}\n\n\n// ---------------------------------------------------------------- ticker state\n//\n// ONE snapshot per symbol per refresh. Every UI surface — the radar row and the\n// detail sheet — renders from this object, so a score can never differ between\n// them. Nothing downstream recalculates.\n//\n// Level roles are explicit and each level has exactly one job:\n//   watch                 the price we are waiting for\n//   entry                 where a position would actually be opened\n//   target1 / target2     profit areas, always above entry for a long\n//   tacticalInvalidation  cancels the immediate entry idea\n//   hardStop              structure is broken; the whole idea is off\n//   probUpper / probLower the two boundaries the probability question uses\nvar STATE_VERSION = 4;\n\nfunction buildTickerState(symbol, A, ctx) {\n  var c = ctx || {};\n  var now = c.now || Date.now();\n  if (!A || !A.state) {\n    return { symbol: symbol, state_version: STATE_VERSION, calculated_at: now, data_through: null,\n      valid: false, violations: [{ code: 'NO_DATA', severity: 'block', text: 'אין נתונים' }],\n      status: 'NO DATA', action: 'DO_NOT_BUY', actionText: ACTIONS.DO_NOT_BUY };\n    }\n  var S = A.state, b = S.bar, atr = b.atr20 || b.avgRange || 1;\n  var T = tactical(A);\n  var plan = executionPlan(A, { label: c.market || 'Neutral' });\n  var pres = pressure(A, { tactical: T, plan: plan });\n\n  // --- levels, derived once\n  var lv = { watch: null, entry: null, target1: null, target2: null,\n    tacticalInvalidation: null, hardStop: null, probUpper: null, probLower: null };\n  if (plan && plan.kind) {\n    lv.watch = plan.kind === 'breakout' ? plan.zone[0] : plan.zone[1];\n    lv.entry = plan.entry;\n    lv.tacticalInvalidation = plan.kind === 'breakout' ? plan.zone[0] - 0.2 * atr : plan.zone[0];\n    lv.hardStop = plan.invalidation;\n  } else {\n    lv.watch = T.resistance ? T.resistance.price : b.dayHigh;\n    lv.tacticalInvalidation = T.support ? T.support.price : b.dayLow;\n    lv.hardStop = lv.tacticalInvalidation - 0.5 * atr;\n  }\n  // A long is cancelled on the way DOWN, so the level hit first must be the\n  // entry cancellation and the structural one must sit BELOW it. Reported the\n  // other way round — entry at 187.27, structure at 187.28 — the whole setup\n  // died before its own entry rule could ever fire.\n  if (lv.tacticalInvalidation != null && lv.hardStop != null && lv.hardStop >= lv.tacticalInvalidation) {\n    lv.hardStop = lv.tacticalInvalidation - Math.max(0.02, 0.3 * atr);\n    lv.hardStopAdjusted = true;\n  }\n  // Targets are always resolved ABOVE the entry. A level at or below the entry\n  // can be the watch level or resistance, but never target 1.\n  // With no entry planned, targets are measured from the WATCH level, not from\n  // the current price — otherwise the first level above price is both the thing\n  // we are waiting for and the first target, which is meaningless.\n  // The add trigger and target 1 cannot be the same price. One says \"commit\n  // more here\", the other says \"take some off here\", and printing both against\n  // 187.47 is an instruction to do two opposite things at once.\n  var addAt = plan && plan.addAbove != null ? plan.addAbove : null;\n  var anchor = lv.entry != null ? lv.entry : (lv.watch != null ? lv.watch : b.close);\n  var aboveEntry = T.above.filter(function (x) { return x.price >= anchor + 0.3 * atr; })\n    .sort(function (x, y) { return x.price - y.price; });\n  lv.target1 = aboveEntry[0] ? aboveEntry[0].price : anchor + 1.0 * atr;\n  // A target that lands on the add trigger is pushed to the next level above it,\n  // or one ATR beyond — the trigger keeps its role, the target moves.\n  if (addAt != null && Math.abs(lv.target1 - addAt) < 0.005) {\n    var beyond = aboveEntry.filter(function (x) { return x.price > addAt + 0.005; });\n    lv.target1 = beyond[0] ? beyond[0].price : addAt + Math.max(0.02, 0.5 * atr);\n    lv.target1MovedFromAdd = true;\n  }\n  lv.target2 = aboveEntry[1] && aboveEntry[1].price >= lv.target1 + 0.3 * atr ? aboveEntry[1].price : lv.target1 + 1.0 * atr;\n  lv.probUpper = lv.watch;\n  lv.probLower = lv.tacticalInvalidation;\n\n  var prob = pathProbability(A, { tactical: T, upper: lv.probUpper, lower: lv.probLower, horizonMin: 60,\n    daily: c.daily, baseline: c.baseline, calibration: c.calibration, market: c.market, pressure: pres });\n\n  // ---- edge check. The recommendation has to agree with the model's own\n  // numbers: a low score, a coin-flip probability, low confidence and balanced\n  // flow together mean there is nothing to act on, whatever the plan says.\n  var row0 = radarRow(symbol, A, c.marketCtx || { label: c.market || 'Neutral' }, c.freshness);\n  var score0 = row0.bl ? row0.bl.confidence : 0;\n  var weak = [];\n  if (score0 <= 2) weak.push('ציון setup נמוך');\n  if (prob && prob.meaningless) weak.push('הרמות קרובות מדי למדידה');\n  if (prob && prob.notDirectional) weak.push('הרמות אינן מקיפות את המחיר — אין הסתברות כיוון');\n  if (prob && prob.up != null && Math.abs(prob.up - 50) <= 8) weak.push('הסיכויים שקולים');\n  if (prob && prob.confidence < 50) weak.push('ביטחון נמוך');\n  if (pres && pres.side === 'balanced') weak.push('קונים ומוכרים מאוזנים');\n  var structureFlat = (row0.structure === 'RANGE') && (row0.momentum === 'FLAT');\n  if (structureFlat) weak.push('אין מבנה ואין מומנטום');\n  var edge = { noEdge: weak.length >= 4, reasons: weak, score: score0,\n    probUp: prob.up, confidence: prob.confidence, flow: pres ? pres.side : 'NA' };\n\n  // ---- staleness is a hard gate, not a label.\n  // Bars that stopped arriving hours ago describe a market that has moved on.\n  // Showing an entry, a probability or a buyer/seller split from them reads as\n  // live guidance about a price that no longer exists, so none of it survives.\n  // On a one-minute feed, four minutes of lag is long enough for price to have\n  // left the entry zone entirely. An entry offered on data that old is an\n  // instruction to trade a price that may no longer exist.\n  var ENTRY_MAX_AGE = 180;                       // three minutes\n  var STALE_SECONDS = 300;                       // five minutes of missing bars\n  var ageSec = (c.staleSeconds != null) ? c.staleSeconds\n    : (b.unix != null && c.nowUnix != null ? c.nowUnix - b.unix : null);\n  var isStale = c.sessionEnded ? false\n    : (c.freshness === 'STALE' || (ageSec != null && ageSec > STALE_SECONDS));\n  // Not stale enough to freeze the whole state, but too old to act on.\n  var tooOldToEnter = !c.sessionEnded && ageSec != null && ageSec > ENTRY_MAX_AGE;\n\n  // And the session itself must be present. A window that starts at 11:02 on a\n  // day that opened at 09:30 has no session open, no session VWAP and a\n  // fraction of the volume — every figure derived from it is wrong while\n  // looking entirely clean.\n  // Measure the rows that were LOADED, not the ones analysis kept. A no-trade\n  // minute is deliberately excluded from the analysis and is still a minute we\n  // hold — counting the filtered set turned a complete session into \"186 of 335\n  // with 57 holes\".\n  var cover = sessionCompleteness((A && (A.rawBars || A.bars)) || [], {});\n  var sessionIncomplete = !c.sessionEnded && !isStale && A && A.bars && A.bars.length > 0\n    && !cover.complete;\n\n  var W = whatNow(A, { tactical: T, plan: plan, market: c.market, daily: c.daily,\n    baseline: c.baseline, calibration: c.calibration, pressure: pres, probability: prob,\n    sessionEnded: c.sessionEnded, levels: lv, edge: edge, stale: isStale });\n\n  // Nothing actionable may remain on the state when there is no edge, so the\n  // technical block underneath cannot contradict the headline.\n  if (edge.noEdge && !c.sessionEnded) {\n    lv.entry = null; lv.target1 = null; lv.target2 = null;\n  }\n  // A closed session is a reference view, not an order. The levels stay visible\n  // as context, but nothing actionable survives — otherwise the headline says\n  // the market is shut while an entry price sits underneath it.\n  if (c.sessionEnded) { lv.entry = null; lv.target1 = null; lv.target2 = null; }\n  if (isStale) { lv.entry = null; lv.target1 = null; lv.target2 = null; }\n  // Too old to act on, or built from part of a session: the levels stay as\n  // context but no entry is offered, and the headline says which it is.\n  // Staleness is the stronger statement and must not be overwritten by the\n  // weaker one: data that is five minutes old is not merely \"delayed\".\n  if (!isStale && !c.sessionEnded && (tooOldToEnter || sessionIncomplete)) {\n    lv.entry = null; lv.target1 = null; lv.target2 = null;\n    W.action = 'WAIT';\n    W.actionText = sessionIncomplete\n      ? 'סשן חלקי — לא לסחור'\n      : 'נתונים באיחור — להמתין לנר טרי';\n    W.next = sessionIncomplete\n      ? ('נטענו ' + cover.receivedMinutes + ' דקות מתוך ' + cover.expectedMinutes\n         + ' — חסרות ' + cover.missingFromOpen + ' מהפתיחה ועוד ' + cover.missingInside\n         + ' בתוך החלון, אז הפתיחה, ה-VWAP, המבנה והמחזור אינם של הסשן')\n      : ('הנר האחרון בן ' + Math.round(ageSec) + ' שניות; בפיד של דקה זה מספיק כדי שהמחיר יעזוב את האזור');\n    // The WHAT-IF lines are built before this gate and still carried \"you can\n    // enter around 338.68\" with targets underneath a headline that says not to\n    // trade. Rewrite them as observations: the levels are worth naming, the\n    // instruction is not.\n    var obs = function (list) {\n      return (list || []).map(function (x) {\n        return String(x)\n          .replace(/אפשר להיכנס/g, 'רמה לצפייה')\n          .replace(/כניסה חלקית/g, 'אזור לצפייה')\n          .replace(/לממש/g, 'רמת יעד לשעבר')\n          .replace(/להוסיף/g, 'רמת אישור');\n      }).filter(function (x) { return !/להיכנס|לקנות|לממש חלק/.test(x); });\n    };\n    W.up = obs(W.up); W.down = obs(W.down);\n    W.up.unshift(sessionIncomplete\n      ? 'הרמות מוצגות לצפייה בלבד — הסדרה חסרה ' + cover.missingTotal + ' דקות'\n      : 'הרמות מוצגות לצפייה בלבד — הנתונים אינם טריים מספיק לפעולה');\n  }\n  var row = row0;\n\n  var st = {\n    symbol: symbol, state_version: STATE_VERSION, calculated_at: now,\n    data_through: { time: b.time, date: b.date || c.date || null, unix: b.unix != null ? b.unix : null },\n    price: b.close, atr: atr,\n    status: row.status, score: row.bl ? row.bl.confidence : 0,\n    action: W.action, actionText: W.actionText,\n    structure: row.structure, momentum: row.momentum,\n    coverage: cover, sessionIncomplete: sessionIncomplete, tooOldToEnter: tooOldToEnter,\n    levels: lv, plan: plan, tactical: T, pressure: pres, probability: W.probability,\n    probabilityRaw: W.probabilityRaw || null, whatNow: W,\n    scenario: W.scenario, edge: edge, noEdge: edge.noEdge,\n    levelStates: { support: T.support ? levelState(A, T.support.price, true) : null,\n                   resistance: T.resistance ? levelState(A, T.resistance.price, false) : null },\n    row: row, sessionEnded: !!c.sessionEnded, freshness: c.freshness || 'LIVE',\n    stale: isStale, data_age_seconds: ageSec\n  };\n  // Keep the row pointing at the same snapshot so nothing can drift.\n  row.score = st.score; row.state = st; row.pressure = pres;\n  if (edge.noEdge && !c.sessionEnded) {\n    row.status = row.status === 'AVOID' ? 'AVOID' : 'WATCH';\n    row.why = 'אין יתרון — רק מעקב';\n    st.status = row.status; st.action = 'WATCH_ONLY'; st.actionText = ACTIONS.WATCH_ONLY;\n  }\n  // The row is what the board shows, so it must carry the same verdict.\n  if (isStale) {\n    row.status = 'AVOID';\n    row.why = 'נתונים לא עדכניים — לא לסחור';\n    st.status = 'AVOID';\n    st.action = 'DATA_STALE'; st.actionText = ACTIONS.DATA_STALE;\n    st.probability = null;\n    st.pressure = pres ? Object.assign({}, pres, { current: false }) : pres;\n  }\n  var v = validateState(st);\n  st.violations = v.violations; st.valid = v.valid;\n  if (!v.valid) {\n    st.status = 'AVOID';\n    st.action = 'DO_NOT_BUY';\n    st.actionText = 'המתן — מצב המודל לא עקבי';\n    st.whatNow.actionText = st.actionText;\n    st.whatNow.next = 'הנתונים סותרים את עצמם, לכן לא מוצגת הוראת מסחר. יחושב מחדש בעדכון הבא.';\n    st.whatNow.up = []; st.whatNow.down = [];\n    // Nothing actionable may survive a blocked state.\n    st.levels = Object.assign({}, st.levels, { entry: null, target1: null, target2: null });\n  }\n  return st;\n}\n\n// ---------------------------------------------------------------- validation\n// Runs before anything is shown. A blocking violation replaces the trade\n// instruction with an explicit \"state inconsistent\" message rather than\n// letting a wrong instruction reach the screen.\nfunction validateState(st) {\n  var v = [], add = function (code, severity, text) { v.push({ code: code, severity: severity, text: text }); };\n  var lv = st.levels, p = st.price, plan = st.plan, atr = st.atr;\n  var actionable = ['ENTRY_AVAILABLE', 'HOLD', 'TAKE_PARTIAL'].indexOf(st.action) >= 0\n    || ['READY', 'ACTIVE'].indexOf(st.status) >= 0;\n\n  if (lv.entry != null) {\n    if (lv.target1 != null && lv.target1 <= lv.entry) add('TARGET_BELOW_ENTRY', 'block', 'יעד ראשון ' + n2(lv.target1) + ' אינו מעל הכניסה ' + n2(lv.entry));\n    if (lv.target2 != null && lv.target1 != null && lv.target2 <= lv.target1) add('TARGET2_BELOW_TARGET1', 'block', 'יעד שני אינו מעל יעד ראשון');\n    if (lv.hardStop != null && lv.hardStop >= lv.entry) add('STOP_ABOVE_ENTRY', 'block', 'הסטופ ' + n2(lv.hardStop) + ' אינו מתחת לכניסה ' + n2(lv.entry));\n    if (lv.tacticalInvalidation != null && lv.hardStop != null && lv.hardStop > lv.tacticalInvalidation)\n      add('STOP_ORDER', 'warn', 'הסטופ המבני אינו מתחת לביטול הטקטי');\n    // an entry the price has already left behind must not read as available\n    if (actionable && p > lv.entry + 0.6 * atr) add('ENTRY_BEHIND_PRICE', 'block', 'המחיר כבר רחוק מעל אזור הכניסה');\n  }\n  if (lv.probUpper != null && lv.probLower != null && lv.probUpper <= lv.probLower)\n    add('PROB_BOUNDS', 'block', 'גבולות ההסתברות אינם בסדר הנכון');\n  if (lv.watch != null && lv.target1 != null && Math.abs(lv.watch - lv.target1) < 1e-9)\n    add('LEVEL_DOUBLE_ROLE', 'block', 'אותה רמה משמשת גם כרמת מעקב וגם כיעד');\n\n  // descriptive text must agree with where price is\n  var pf = st.pressure;\n  if (pf && pf.support && pf.support.verdict === 'נשברת' && p > pf.support.price + 0.05 * st.atr)\n    add('TEXT_CONTRADICTS_PRICE', 'block', 'נטען שהתמיכה נשברת בזמן שהמחיר מעליה');\n  if (pf && pf.resistance && pf.resistance.verdict === 'נפרצת' && p < pf.resistance.price - 0.05 * st.atr)\n    add('TEXT_CONTRADICTS_PRICE', 'block', 'נטען שההתנגדות נפרצת בזמן שהמחיר מתחתיה');\n\n  // the headline and the technical block must describe the same situation\n  var passive = ['DO_NOT_BUY', 'WATCH_ONLY', 'SESSION_CLOSED', 'SETUP_CANCELLED'].indexOf(st.action) >= 0;\n  if (passive && lv.entry != null) add('ACTION_VS_ENTRY', 'block', 'ההוראה היא להמתין בזמן שמוצגת רמת כניסה');\n  if (passive && st.whatNow && st.whatNow.up.some(function (s) { return /אפשר להיכנס|כניסה חלקית ב-/.test(s); }))\n    add('ACTION_VS_INSTRUCTION', 'block', 'ההוראה היא להמתין בזמן שמוצגת הוראת כניסה');\n  if (st.noEdge && lv.target1 != null) add('NO_EDGE_TARGETS', 'block', 'אין יתרון אבל מוצגים יעדים');\n  // exactly one entry scenario may be narrated at a time\n  if (st.whatNow) {\n    var entryLines = st.whatNow.up.filter(function (s) { return /אפשר להיכנס|כניסה חלקית ב-/.test(s); });\n    var mentionsBoth = entryLines.some(function (s) { return /עובר את/.test(s); })\n      && entryLines.some(function (s) { return /יורד לאזור/.test(s); });\n    if (mentionsBoth) add('TWO_SCENARIOS', 'block', 'מוצגים שני תרחישי כניסה במקביל');\n  }\n  // a level description must match its current state\n  if (st.levelStates && st.levelStates.support && st.pressure && st.pressure.support) {\n    if (st.pressure.support.verdict === 'נשברה' && st.levelStates.support.state !== 'broken')\n      add('LEVEL_TEXT_STALE', 'block', 'תיאור התמיכה אינו תואם את מצבה הנוכחי');\n  }\n\n  // status vs plan\n  if (st.status === 'ACTIVE' && plan && plan.state === 'FAILED') add('STATUS_VS_PLAN', 'block', 'הסטטוס פעיל בזמן שה-setup בוטל');\n  if (st.status === 'READY' && st.action === 'DO_NOT_BUY') add('ACTION_VS_STATUS', 'block', 'סטטוס מוכן מול הוראה לא לקנות');\n\n  // pressure conclusion vs its own numbers\n  if (pf && pf.agreement === 'תומך' && pf.direction === 'deteriorating')\n    add('PRESSURE_CONCLUSION', 'block', 'נטען שהלחץ תומך בזמן שהוא נחלש');\n  if (pf && pf.agreement === 'תומך' && pf.buyPct < 50)\n    add('PRESSURE_CONCLUSION', 'block', 'נטען שהקונים תומכים בזמן שהם במיעוט');\n\n  // probability vs confidence\n  var pr = st.probability;\n  // `pr.up` is null when the probability was suppressed, and `null <= 20` is\n  // true in JavaScript — which made a *withheld* probability look like an\n  // extreme one and invalidated the whole state.\n  if (pr && pr.up != null && pr.confidence < 50 && (pr.up >= 80 || pr.up <= 20))\n    add('PROB_CONFIDENCE', 'block', 'הסתברות קיצונית עם ביטחון נמוך');\n  if (pr && pr.upper != null && lv.probUpper != null && Math.abs(pr.upper - lv.probUpper) > 1e-9)\n    add('PROB_STALE_LEVELS', 'block', 'ההסתברות חושבה מול רמות אחרות מאלה שמוצגות');\n\n  // ---- staleness. A state built from bars that stopped arriving is not a\n  // valid live state, whatever else it says.\n  if (st.stale && st.action !== 'DATA_STALE' && st.action !== 'SESSION_CLOSED')\n    add('STALE_BUT_ACTIONABLE', 'block', 'הנתונים ישנים אך המסך מציג הוראה חיה');\n  if (st.stale && lv && (lv.entry != null || lv.target1 != null))\n    add('STALE_WITH_LEVELS', 'block', 'הנתונים ישנים אך מוצגת רמת כניסה');\n  if (st.stale && pr && pr.up != null)\n    add('STALE_WITH_PROBABILITY', 'block', 'הנתונים ישנים אך מוצגת הסתברות כאילו היא נוכחית');\n\n  // ---- a setup whose cancellation level the price has already crossed\n  var cancelLv = (lv && lv.tacticalStop != null) ? lv.tacticalStop\n    : (st.plan && st.plan.invalidation != null ? st.plan.invalidation : null);\n  var LIVE_ACTIONS = ['START_WATCHING', 'WAIT_FOR_CONFIRMATION', 'ENTRY_AVAILABLE', 'HOLD', 'TAKE_PARTIAL'];\n  if (!st.sessionEnded && cancelLv != null && st.price != null && st.price < cancelLv\n      && LIVE_ACTIONS.indexOf(st.action) >= 0)\n    add('CANCELLED_BUT_ACTIVE', 'block', 'המחיר כבר מתחת לרמת הביטול אך התרחיש מוצג כפעיל');\n\n  // ---- the narrative targets must be the levels block's targets\n  if (st.whatNow && Array.isArray(st.whatNow.up) && lv && lv.target1 != null && lv.target2 != null) {\n    var txt = st.whatNow.up.join(' ');\n    var m1 = txt.match(/\\u05d9\\u05e2\\u05d3 \\u05e8\\u05d0\\u05e9\\u05d5\\u05df ([\\d.]+)/);\n    var m2 = txt.match(/\\u05d9\\u05e2\\u05d3 \\u05e9\\u05e0\\u05d9 ([\\d.]+)/);\n    var near = function (a, b2) { return Math.abs(Number(a) - Number(b2)) < 0.005; };\n    if (m1 && !near(m1[1], lv.target1)) add('TARGET1_TEXT_MISMATCH', 'block', 'יעד ראשון בטקסט שונה מהיעד ברמות');\n    if (m2 && !near(m2[1], lv.target2)) add('TARGET2_TEXT_MISMATCH', 'block', 'יעד שני בטקסט שונה מהיעד ברמות');\n  }\n\n  return { valid: !v.some(function (x) { return x.severity === 'block'; }), violations: v };\n}\n\n\n// ---------------------------------------------------------------- analysis pack\n//\n// Everything another trader (or another model) needs to re-do the analysis from\n// scratch: the raw data AND the decision state, from ONE snapshot. Any field the\n// system does not have says NOT AVAILABLE rather than being quietly dropped.\nvar NA = 'NOT AVAILABLE';\nfunction nz(v, d) { return v == null || v === '' || (typeof v === 'number' && !isFinite(v)) ? NA : (d != null && typeof v === 'number' ? v.toFixed(d) : String(v)); }\n\n// Buckets belong to the exchange clock: 09:30, 09:35, ... 11:00, 11:05. The\n// grouping was already correct, but the LABEL came from the first bar that\n// happened to fall inside — so a window starting at 11:02 produced a bar called\n// \"11:02\", which is not a five-minute timeframe at all. A bucket missing some\n// of its minutes is marked rather than silently presented as whole.\nfunction aggregate(rows, minutes) {\n  var out = [], bucket = null;\n  var label = function (m) {\n    var h = Math.floor(m / 60), mm = m % 60;\n    return String(h).padStart(2, '0') + ':' + String(mm).padStart(2, '0');\n  };\n  (rows || []).forEach(function (r) {\n    var m = parseInt(r.time.slice(0, 2), 10) * 60 + parseInt(r.time.slice(3), 10);\n    var key = Math.floor(m / minutes);\n    if (!bucket || bucket.key !== key) {\n      if (bucket) out.push(bucket);\n      bucket = { key: key, time: label(key * minutes), firstBar: r.time, minutes: 1,\n        open: r.open, high: r.high, low: r.low, close: r.close, volume: r.volume, date: r.date };\n    } else {\n      bucket.high = Math.max(bucket.high, r.high); bucket.low = Math.min(bucket.low, r.low);\n      bucket.close = r.close; bucket.volume += r.volume; bucket.minutes++;\n    }\n  });\n  if (bucket) out.push(bucket);\n  out.forEach(function (b) { b.partial = b.minutes < minutes; });\n  return out;\n}\nfunction candleLine(sym, r, avgVol) {\n  var range = r.high - r.low, pct = function (x) { return range > 0 ? Math.round(x / range * 1000) / 10 : 0; };\n  var dir = r.close > r.open ? 'BULL' : r.close < r.open ? 'BEAR' : 'FLAT';\n  return [sym, r.date || '', r.time, r.open, r.high, r.low, r.close, r.volume, dir,\n    pct(Math.abs(r.close - r.open)), pct(r.high - Math.max(r.open, r.close)), pct(Math.min(r.open, r.close) - r.low),\n    Math.round(range * 10000) / 10000, avgVol > 0 ? Math.round(r.volume / avgVol * 100) / 100 : 0].join(',');\n}\nvar CANDLE_HEADER = 'symbol,date,time,open,high,low,close,volume,dir,body_pct,upper_wick_pct,lower_wick_pct,range,vol_x';\n\nfunction analysisPack(ctx) {\n  var c = ctx || {}, st = c.snap;\n  var L = [], add = function (s) { L.push(s == null ? '' : s); };\n  var head = function (t) { add(''); add('===== ' + t + ' ====='); };\n  if (!st) return 'NO SNAPSHOT — ' + NA;\n  var A = c.analysis, b = st.price != null ? st : null;\n  var bar = A && A.state ? A.state.bar : null;\n  var lv = st.levels || {}, P = st.plan, W = st.whatNow, pr = st.probability, pf = st.pressure;\n  var rows = c.rows || [], atr = st.atr;\n\n  add('FULL ANALYSIS PACK · ' + st.symbol);\n  add('Generated from one atomic snapshot: state_version ' + st.state_version + ', calculated_at ' + new Date(st.calculated_at).toISOString());\n\n  head('1. TIME & DATA STATE');\n  add('Symbol: ' + st.symbol);\n  add('Date (ET): ' + nz(st.data_through && st.data_through.date || c.date));\n  add('Last closed candle (ET): ' + nz(st.data_through && st.data_through.time));\n  add('Current price (last close): ' + nz(st.price, 2));\n  add('Quote timestamp: ' + nz(c.quoteTime) + '   (the feed is 1-minute bars; there is no separate quote stream)');\n  add('Data age / stale seconds: ' + nz(c.staleSeconds));\n  add('Freshness: ' + nz(st.freshness));\n  add('Session: ' + nz(c.session) + '   (PRE / REGULAR / AFTER / CLOSED)');\n  add('Data source: Yahoo Finance 1-minute bars via Cloudflare Worker');\n\n  head('2. MODEL DECISION');\n  add('Status: ' + nz(st.status));\n  add('Primary action: ' + nz(st.actionText) + ' [' + nz(st.action) + ']');\n  add('Setup Score: ' + nz(st.score) + '/10');\n  add('Active setup type: ' + (st.stale ? 'none (data stale)'\n    : (st.scenario ? st.scenario.kind + ' — ' + st.scenario.label : NA)));\n  add('Setup state: ' + (st.stale ? 'FROZEN (last known: ' + (P ? P.state : NA) + ') — reference only'\n    : (P ? P.state : NA)));\n  add('Reason: ' + (W ? nz(W.next) : NA));\n  add('Model state valid: ' + (st.valid ? 'yes' : 'NO — ' + (st.violations || []).map(function (v) { return v.code; }).join(', ')));\n  if (st.noEdge) add('No-edge reasons: ' + ((st.edge && st.edge.reasons) || []).join(' · '));\n\n  head('3. PRICE LEVELS AND THEIR ROLES');\n  // While frozen, every level is a record of what WAS, not a price to act on.\n  // The labels say so, because a reader skimming the pack reads the label.\n  var FROZEN = !!st.stale;\n  var role = function (name, val, note) {\n    var label = FROZEN ? 'LAST KNOWN ' + name : name;\n    var n = FROZEN ? (note ? note + ' — REFERENCE ONLY' : 'REFERENCE ONLY') : note;\n    add(label + ': ' + (val == null ? NA : val.toFixed(2) + (atr ? '  (' + fmtR((val - st.price) / atr) + ')' : '')) + (n ? '  — ' + n : ''));\n  };\n  if (FROZEN) add('NOTE: data is stale. Everything in this section is the last recorded state, not a current instruction.');\n  role('Watch level', lv.watch, FROZEN ? 'the price that was being waited for' : 'the price being waited for');\n  role('Entry zone low', P && P.zone ? P.zone[0] : null);\n  role('Entry zone high', P && P.zone ? P.zone[1] : null);\n  role('Planned entry', lv.entry);\n  role('Breakout / reclaim trigger', P && P.kind === 'breakout' ? lv.watch : (st.plan && st.plan.addAbove != null ? st.plan.addAbove : null), FROZEN ? 'confirmed an add while the setup was live'\n      : (lv.entry == null) ? 'a level to watch — there is no position to add to'\n      : 'confirms an add');\n  role('Tactical support', st.tactical && st.tactical.support ? st.tactical.support.price : null, st.tactical && st.tactical.support ? st.tactical.support.why : '');\n  role('Tactical resistance', st.tactical && st.tactical.resistance ? st.tactical.resistance.price : null, st.tactical && st.tactical.resistance ? st.tactical.resistance.why : '');\n  role('Entry cancellation (tactical)', lv.tacticalInvalidation);\n  role('Setup invalidation / structural stop', lv.hardStop);\n  role('Stop', lv.hardStop, 'same as structural invalidation; no separate money stop is computed');\n  role('Target 1', lv.target1);\n  role('Target 2', lv.target2);\n  add('Target 3: ' + NA);\n  role('Reclaim level (broken and being retested)', st.plan && st.plan.state !== 'FAILED' && A && A.state ? A.state.reclaim : null);\n  add('');\n  add('Level states (approaching / testing / held / broken / reclaimed / rejected / lost / far):');\n  ['support', 'resistance'].forEach(function (k) {\n    var s = st.levelStates && st.levelStates[k];\n    add('  ' + k + ': ' + (s ? s.price.toFixed(2) + ' — ' + s.state + ' (' + LEVEL_TEXT[s.state] + '), ' + fmtR(s.distanceR) + ', touches ' + s.touches + ', recoveries ' + s.recoveries + ', rejections ' + s.rejections : NA));\n  });\n\n  head('4. WHAT-IF' + (FROZEN ? ' — FROZEN, REFERENCE ONLY' : ''));\n  add('NOW: ' + (W ? W.actionText + ' — ' + W.next : NA));\n  add('IF UP:');\n  ((W && W.up) || []).forEach(function (s) { add('  - ' + s); });\n  if (!W || !W.up.length) add('  ' + NA);\n  add('IF DOWN:');\n  ((W && W.down) || []).forEach(function (s) { add('  - ' + s); });\n  if (!W || !W.down.length) add('  ' + NA);\n  // The same rule as FROZEN, for every state where no entry is planned. I fixed\n  // this for stale data and left the identical contradiction standing when the\n  // session is incomplete: \"Planned entry: NOT AVAILABLE\" above \"Confirms\n  // entry: partial entry on the pullback\" tells you not to trade and how to\n  // trade in the same breath.\n  var NO_ENTRY = !FROZEN && (lv.entry == null);\n  var noEntryWhy = st.sessionIncomplete ? 'the session is incomplete'\n    : st.tooOldToEnter ? 'the data is too old to act on'\n    : st.sessionEnded ? 'the session is closed'\n    : 'no entry is planned';\n  if (NO_ENTRY) {\n    add('Confirms entry: ' + NA + ' — ' + noEntryWhy);\n    add('Cancels entry: ' + NA + ' — there is no entry to cancel');\n    add('Cancels the whole setup: ' + NA + ' — ' + noEntryWhy);\n    add('After entry: ' + NA + ' — ' + noEntryWhy);\n    add('At target 1: ' + NA + ' — ' + noEntryWhy);\n    add('Confirmation strengthens above: ' + NA + ' — ' + noEntryWhy);\n    add('Exit when: ' + NA + ' — ' + noEntryWhy);\n    add('The levels above are reference only: with no entry there is nothing to add to, take off, or exit.');\n  } else if (FROZEN) {\n    // None of these may be phrased as something to do. They are the last\n    // recorded rules of a setup that is no longer live.\n    add('Confirms entry: ' + NA + ' — setup frozen (data stale)');\n    add('After entry: ' + NA + ' — setup frozen (data stale)');\n    add('At target 1: ' + NA + ' — setup frozen (data stale)');\n    add('Confirmation strengthens above: ' + NA + ' — setup frozen (data stale)');\n    add('Exit when: ' + NA + ' — setup frozen (data stale)');\n    add('LAST KNOWN entry cancellation: ' + (lv.tacticalInvalidation != null ? lv.tacticalInvalidation.toFixed(2) + '  — REFERENCE ONLY' : NA));\n    add('LAST KNOWN setup invalidation: ' + (lv.hardStop != null ? lv.hardStop.toFixed(2) + '  — REFERENCE ONLY' : NA));\n    add('These are the rules the setup had when the data stopped. They are not active.');\n  } else {\n  add('Confirms entry: ' + (P ? nz(P.ifConfirmed) : NA));\n  add('Cancels entry: ' + (lv.tacticalInvalidation != null ? 'close below ' + lv.tacticalInvalidation.toFixed(2) : NA));\n  add('Cancels the whole setup: ' + (lv.hardStop != null ? 'close below ' + lv.hardStop.toFixed(2) : NA));\n  add('After entry: ' + (P ? nz(P.nextStep) : NA));\n  add('At target 1: ' + (lv.target1 != null ? 'take partial at ' + lv.target1.toFixed(2) : NA));\n  add('Confirmation strengthens above: ' + (P && P.addAbove != null ? P.addAbove.toFixed(2) + ' (two closes); position state unknown to the system' : NA));\n  add('Exit when: ' + (lv.hardStop != null ? 'close below ' + lv.hardStop.toFixed(2) + ', or at target' : NA));\n  }\n\n  head('4b. SESSION COVERAGE');\n  var cv = st.coverage || {};\n  add('First regular bar: ' + (cv.firstBar || NA));\n  add('Last regular bar: ' + (cv.lastBar || NA));\n  add('Expected minutes: ' + (cv.expectedMinutes != null ? cv.expectedMinutes : NA));\n  add('Received unique minutes: ' + (cv.receivedMinutes != null ? cv.receivedMinutes : NA));\n  add('Coverage: ' + (cv.coveragePct != null ? cv.coveragePct + '%' : NA));\n  add('Starts at the open: ' + (cv.fromOpen ? 'yes' : 'NO — missing ' + (cv.missingFromOpen || '?') + ' minutes from 09:30'));\n  add('Holes inside the loaded window: ' + (cv.missingInside\n    ? cv.missingInside + ' minutes missing between ' + cv.firstBar + ' and ' + cv.lastBar\n      + ' — the series is not continuous, so structure, momentum and the buyer/seller split describe a broken sequence'\n    : 'none'));\n  add('Total missing minutes: ' + (cv.missingTotal != null ? cv.missingTotal : NA));\n  add('Session values usable: ' + (cv.complete ? 'yes' : 'NO — open, gap, VWAP, EMA, relative volume and any entry are derived from a partial window and are not the session\\'s'));\n\n  head('5. PROBABILITY');\n  // A suppressed probability must still say WHAT was asked and WHY there is no\n  // number. \"NOT AVAILABLE\" on its own leaves the reader unable to tell a stale\n  // feed from a question that was never answerable.\n  if (!pr) {\n    var praw = (st && st.probabilityRaw) || null;\n    add('UP: ' + NA + '   DOWN: ' + NA);\n    add('Event measured: ' + (praw && praw.upper != null\n      ? 'price reaches ' + praw.upper.toFixed(2) + ' before ' + praw.lower.toFixed(2) : NA));\n    add('Horizon: ' + (praw ? praw.horizonMin + ' minutes' : NA));\n    add('Confidence: ' + NA + ' — no probability is being claimed');\n    add('Source: ' + NA);\n    // Only the reason for the SUPPRESSION, never the half-computed scoring\n    // inputs — those describe a number we have decided not to trust.\n    var suppressWhy = (praw && (praw.notDirectional || praw.meaningless))\n      ? praw.why.join(' | ')\n      : (st && st.stale) ? 'the data is stale'\n      : (st && st.coverage && !st.coverage.complete) ? 'the session is incomplete — the inputs are not the session\\'s'\n      : 'not answerable in this state';\n    add('Why: ' + suppressWhy);\n    add('Scoring inputs: ' + NA + ' — withheld with the probability they produced');\n  } else {\n    add('UP: ' + pr.up + '%   DOWN: ' + pr.down + '%');\n    add('Event measured: price reaches ' + pr.upper.toFixed(2) + ' before ' + pr.lower.toFixed(2));\n    add('Horizon: ' + pr.horizonMin + ' minutes');\n    add('Confidence: ' + pr.confidence + '/100' + (pr.lowConfidence ? '  (LOW — displayed % is pulled toward 50)' : ''));\n    add('Source: ' + (pr.source === 'empirical' ? 'empirical, ' + pr.n + ' comparable cases in this symbol\\'s own loaded history'\n      : 'MODEL ESTIMATE — not a measured frequency' + (pr.raw != null ? ' (raw ' + pr.raw + '%, shrunk for confidence)' : '')));\n    add('Why: ' + pr.why.join(' | '));\n    if (st.coverage && !st.coverage.complete)\n      add('NOTE: these inputs come from a partial window and the probability above should not be relied on.');\n  }\n\n  head('6. BUYERS / SELLERS (inferred from candles)');\n  if (!pf) add(NA); else {\n    add('Buyers: ' + pf.buyPct + '%   Sellers: ' + pf.sellPct + '%   (window ' + pf.window + ' bars)');\n    add('Buyers: ' + pf.buyersTrend + '   Sellers: ' + pf.sellersTrend + '   Direction: ' + pf.direction);\n    add('Conclusion: ' + pf.conclusion);\n    add('Agreement with the setup: ' + pf.agreement);\n    add('METHOD: close location inside each bar weighted by that bar\\'s volume. NOT order flow. hasOrderBook=' + pf.hasOrderBook + ' hasTape=' + pf.hasTape);\n  }\n\n  head('7. ORDER BOOK / LEVEL 2');\n  var bk = c.book;\n  if (!bk || bk.error || !bk.summary || bk.summary.bid_pct == null) {\n    add(NA + ' — the Cboe book did not return data for this symbol at this time');\n  } else {\n    var s = bk.summary;\n    add('PARTIAL BOOK — CBOE ONLY');\n    add('Venues returning data: ' + (s.venues_ok.join(', ') || NA) + '   Not answering: ' + (s.venues_failed.join(', ') || 'none'));\n    add('Missing from this book: ARCA, NYSE, Nasdaq and all other venues. This is NOT the NBBO and NOT consolidated depth.');\n    add('Book timestamp: ' + nz(bk.fetched_at));\n    add('Best bid: ' + nz(s.best_bid, 2) + '   Best ask: ' + nz(s.best_ask, 2) + '   Spread: ' + nz(s.spread, 4));\n    add('Total bid depth: ' + s.bid_shares + ' shares   Total ask depth: ' + s.ask_shares + ' shares');\n    add('Imbalance: ' + s.bid_pct + '% bid / ' + s.ask_pct + '% ask  (net ' + s.imbalance + ')');\n    add('Levels available: ' + s.depth_levels + ' per side per venue (' + NA + ' beyond 5 — the public viewer publishes 5)');\n    add('');\n    add('venue,side,level,price,shares');\n    (bk.venues || []).forEach(function (v) {\n      if (v.error) { add(v.venue + ',ERROR,,,' + v.error); return; }\n      (v.asks || []).forEach(function (l, i) { add(v.venue + ',ask,' + (i + 1) + ',' + l.price + ',' + l.shares); });\n      (v.bids || []).forEach(function (l, i) { add(v.venue + ',bid,' + (i + 1) + ',' + l.price + ',' + l.shares); });\n    });\n    add('');\n    add('Depth change over the last 30-60s: ' + (c.bookPrev ? 'bid ' + (s.bid_shares - c.bookPrev.bid_shares) + ', ask ' + (s.ask_shares - c.bookPrev.ask_shares) : NA + ' (no earlier snapshot held)'));\n  }\n\n  head('8. TAPE / TIME & SALES');\n  add('TAPE NOT AVAILABLE — this feed carries 1-minute bars only. No per-trade prints, no at-bid/at-ask flags, no aggressive volume split.');\n\n  head('9. STRUCTURE & MOMENTUM');\n  // Structure is a claim about a SEQUENCE. With minutes missing in the middle\n  // the sequence is not the market's, so the reading is qualified rather than\n  // stated — a swing \"confirmed\" across a hole may never have happened.\n  var BROKEN = (st.coverage && st.coverage.contiguous === false)\n    ? '  [SERIES NOT CONTINUOUS — ' + st.coverage.missingInside + ' minutes missing inside the window; this reading is not reliable]'\n    : '';\n  add('Main structure: ' + nz(st.structure) + (A && A.state ? '  (' + A.state.reason + ')' : '') + BROKEN);\n  add('Announced trend: ' + (A && A.state ? nz(A.state.announced) : NA) + BROKEN);\n  add('Short momentum: ' + nz(st.momentum) + (st.row && st.row.mom ? '   move over last 5 bars: ' + st.row.mom.net.toFixed(2) + 'R' : ''));\n  add('Recent swings (K=3 confirmed):');\n  var sw = A ? A.swings.filter(function (w) { return w.confirmedAt <= A.bars.length - 1; }).slice(-10) : [];\n  if (!sw.length) add('  ' + NA);\n  sw.forEach(function (w) { add('  ' + w.label + '  ' + w.price.toFixed(2) + '  @' + w.time + (w.outside ? '  (outside bar — not counted as structure)' : '')); });\n\n  head('10. INDICATORS');\n  // These are all session-scoped. Computed over a partial window they are not\n  // what their names say, so every line carries the warning rather than the\n  // reader being expected to remember a note from six sections earlier.\n  var PART = (st.coverage && !st.coverage.complete) ? '  [PARTIAL WINDOW — NOT A SESSION INDICATOR]' : '';\n  if (PART) add('NOTE: the session is incomplete — every figure below describes the loaded window only.');\n  if (!bar) add(NA); else {\n    add('VWAP: ' + bar.vwap.toFixed(2) + PART + '   price is ' + (bar.aboveVwap ? 'ABOVE' : 'BELOW') + '   distance: ' + (st.price - bar.vwap).toFixed(2) + ' (' + fmtR((st.price - bar.vwap) / atr) + ')');\n    add('EMA9: ' + bar.ema9.toFixed(2) + PART + '   EMA20: ' + bar.ema20.toFixed(2) + '   state: ' + bar.align +\n      '   separation: ' + Math.abs(bar.ema9 - bar.ema20).toFixed(3) + ' (minimum for a call: ' + (0.15 * atr).toFixed(3) + ')');\n    add('Average candle range (20 bars): ' + atr.toFixed(4) + '   = 1R throughout this pack');\n    add('Relative volume (vs session average): ' + bar.volx.toFixed(2) + 'x' + PART + (bar.auction ? '  [CLOSING AUCTION BAR — excluded from scoring]' : ''));\n    add('Time-of-day normalised volume: ' + (c.baseline ? (volxTod(bar, c.baseline) != null ? volxTod(bar, c.baseline).toFixed(2) + 'x vs the same minute on ' + c.baseline.days + ' prior days' : NA) : NA + ' (no prior days loaded)'));\n    var dl = function (name, v) { return v == null ? name + ': ' + NA : name + ': ' + (st.price - v).toFixed(2) + ' (' + fmtR((st.price - v) / atr) + ')'; };\n    add('Distance from levels — ' + dl('support', st.tactical && st.tactical.support ? st.tactical.support.price : null) +\n      '   ' + dl('resistance', st.tactical && st.tactical.resistance ? st.tactical.resistance.price : null));\n  }\n\n  head('11. MARKET CONTEXT');\n  var mk = c.marketCtx;\n  if (!mk || !mk.parts || !mk.parts.length) add(NA); else {\n    add('Regime: ' + mk.label);\n    mk.parts.forEach(function (p) {\n      add('  ' + p.symbol + ': ' + p.close.toFixed(2) + '   VWAP ' + p.vwap.toFixed(2) + ' (' + (p.aboveVwap ? 'above' : 'below') + ')   EMA ' + p.align + '   trend ' + p.trend);\n    });\n    add('Sector ETF: ' + (c.sectorEtf || NA));\n    add('Does the market support this symbol: ' + (mk.label === 'Bullish' ? 'supports a long' : mk.label === 'Bearish' ? 'contradicts a long' : 'neutral'));\n  }\n\n  head('12. MULTI-DAY CONTEXT');\n  if (st.coverage && !st.coverage.complete)\n    add(\"NOTE: today's row is built from a PARTIAL session (\" + (st.coverage.coveragePct || 0)\n      + '% of the minutes since the open) — it is not a daily candle.');\n  var d = c.daily;\n  if (!d) add(NA + ' — no prior sessions loaded'); else {\n    add('Sessions loaded: ' + d.days + ' (prior: ' + d.priorDays + ')');\n    // The daily trend is computed over COMPLETED sessions only — today's row is\n    // excluded by dailyContext. Say so, because a reader looking at a partial\n    // row whose low is already below yesterday's will otherwise read the trend\n    // as a claim about it.\n    add('Daily trend: ' + d.trend + ' (' + d.trendWhy + ')'\n      + '  [computed over the ' + (d.priorDays || 0) + ' COMPLETED sessions before today'\n      + ((st.coverage && !st.coverage.complete) ? \"; today's partial row is excluded\" : '') + ']');\n    add('Previous day: high ' + nz(d.prevHigh, 2) + '  low ' + nz(d.prevLow, 2) + '  close ' + nz(d.prevClose, 2));\n    add('Gap today vs previous close: ' + ((st.coverage && !st.coverage.complete)\n      ? NA + ' — the session open is missing, so there is nothing to measure the gap from'\n      : nz(d.gap, 2)));\n    add('Average daily range: ' + nz(d.atrDaily, 2));\n    add('');\n    add('date,open,high,low,close,volume');\n    (c.dailyBars || []).forEach(function (x) { add([x.date, x.open, x.high, x.low, x.close, x.volume].join(',')); });\n    if (!(c.dailyBars || []).length) add(NA);\n    var hi = (c.dailyBars || []).reduce(function (m, x) { return Math.max(m, x.high); }, -Infinity);\n    var lo = (c.dailyBars || []).reduce(function (m, x) { return Math.min(m, x.low); }, Infinity);\n    add('');\n    add('Multi-day high/low: ' + (isFinite(hi) ? hi.toFixed(2) + ' / ' + lo.toFixed(2) : NA));\n  }\n\n  head('13. EVENTS');\n  var evs = A ? A.events.slice(-40).reverse() : [];\n  if (!evs.length) add(NA);\n  evs.forEach(function (s2) {\n    var parts = s2.events.map(function (e) {\n      return e.type + (e.level != null ? ' @' + e.level.toFixed(2) : '') + (e.confirmed === true ? ' [confirmed]' : e.confirmed === false ? ' [unconfirmed]' : '') + (e.side ? ' ' + e.side : '');\n    });\n    if (parts.length) add(s2.time + '  ' + s2.close.toFixed(2) + '  ' + parts.join(' · ') + '  (score ' + s2.score + ')');\n  });\n\n  head('14. POSITION');\n  var pos = c.position;\n  if (!pos) {\n    add('Position: NOT AVAILABLE — this application does not track positions, orders or account state.');\n    add('Quantity / average entry / P&L / current stop / invested / remaining / partials taken: ' + NA);\n  } else {\n    add('Position: ' + nz(pos.state));\n    add('Quantity: ' + nz(pos.qty) + '   Average entry: ' + nz(pos.avg, 2));\n    add('P&L: ' + nz(pos.pnl, 2) + ' (' + nz(pos.pnlPct, 2) + '%)   Current stop: ' + nz(pos.stop, 2));\n    add('Invested: ' + nz(pos.invested) + '   Remaining planned: ' + nz(pos.remaining) + '   Partials taken: ' + nz(pos.partials));\n  }\n\n  head('15. CANDLES — LAST 50 ONE-MINUTE (closed only)');\n  var avgVol = rows.length ? rows.reduce(function (a2, x) { return a2 + x.volume; }, 0) / rows.length : 0;\n  add(CANDLE_HEADER);\n  rows.slice(-50).forEach(function (r) { add(candleLine(st.symbol, r, avgVol)); });\n  if (!rows.length) add(NA);\n\n  head('16. CANDLES — LAST 20 FIVE-MINUTE (aggregated from 1-minute)');\n  var f5 = aggregate(rows, 5).slice(-20);\n  add(CANDLE_HEADER);\n  var avg5 = f5.length ? f5.reduce(function (a2, x) { return a2 + x.volume; }, 0) / f5.length : 0;\n  f5.forEach(function (r) { add(candleLine(st.symbol, r, avg5)); });\n  if (!f5.length) add(NA);\n\n  head('17. CANDLES — LAST 10 DAILY');\n  add('symbol,date,open,high,low,close,volume');\n  (c.dailyBars || []).slice(-10).forEach(function (x) { add([st.symbol, x.date, x.open, x.high, x.low, x.close, x.volume].join(',')); });\n  if (!(c.dailyBars || []).length) add(NA + ' — no prior sessions loaded');\n\n  head('END OF PACK');\n  add('Fields marked ' + NA + ' are genuinely absent from this data feed, not omitted.');\n  return L.join('\\n');\n}\n\n\n// Candidate scoring — \"is this stock worth my attention today?\"\n//\n// THREE SEPARATE QUESTIONS, and this file must never blur them:\n//\n//   Candidate Score  is this worth WATCHING today?      0-100, from history\n//   Live Interest    is it still worth live attention?  0-100, from the session\n//   Setup Score      is a trade developing RIGHT NOW?   0-10, engine.cjs\n//\n// A stock can be a strong candidate with no setup at all: a quiet coil under a\n// multi-day high is exactly that. Equally, a live symbol can hold a setup and\n// have stopped being interesting, because the range died.\n//\n// Everything here is computed from CLOSED sessions. Nothing in this file may\n// produce a live instruction, and yesterday's execution state (ACTIVE, FAILED,\n// WAITING, DO_NOT_CHASE) is deliberately not carried in: structure and levels\n// survive a session boundary, decisions do not.\n\n\n// ---------------------------------------------------------------- daily bars\n// One bar per session out of per-minute rows.\n// A US regular session is 390 one-minute bars. A set materially shorter than\n// that is a session still being collected, or one fetched mid-day — and\n// aggregating it yields a daily candle that is wrong with no outward sign.\nvar SESSION_BARS = 390, MIN_COVERAGE = 0.85;\n// Counting bars alone is the wrong test. A thinly traded name has no print in\n// some minutes and the feed omits them, so a COMPLETE session can arrive with\n// 343 bars — PGR, BLK and HON were all refused that way. What actually\n// distinguishes a complete session is that it SPANS the session: it starts at\n// the open and runs to the close. A day truncated at 13:20 fails that however\n// many bars it has.\nfunction sessionSpans(rows) {\n  if (!rows || !rows.length) return false;\n  var times = rows.map(function (r) { return r.time; }).sort();\n  return times[0] <= '09:35' && times[times.length - 1] >= '15:55';\n}\nfunction toDaily(daysRows, opts) {\n  var o2 = opts || {};\n  var auth = {};\n  (o2.authoritative || []).forEach(function (b) { auth[b.date] = b; });\n  var out = (daysRows || []).filter(function (r) { return r && r.length; }).map(function (rows) {\n    var h = -Infinity, l = Infinity, v = 0;\n    rows.forEach(function (r) { h = Math.max(h, r.high); l = Math.min(l, r.low); v += r.volume; });\n    var o = rows[0].open, c = rows[rows.length - 1].close;\n    var regular = rows.filter(function (r) { return r.time >= '09:30' && r.time <= '16:00'; });\n    var cov = regular.length / SESSION_BARS;\n    var spans = sessionSpans(rows);\n    var a = auth[rows[0].date];\n    if (a) return { date: a.date, open: a.open, high: a.high, low: a.low, close: a.close,\n      volume: a.volume, range: a.high - a.low,\n      closePos: a.high > a.low ? (a.close - a.low) / (a.high - a.low) : 0.5,\n      bars: rows.length, coverage: Math.round(cov * 1000) / 1000, complete: true, source: 'authoritative' };\n    return { date: rows[0].date, open: o, high: h, low: l, close: c, volume: v,\n      range: h - l, closePos: h > l ? (c - l) / (h - l) : 0.5, bars: rows.length,\n      coverage: Math.round(cov * 1000) / 1000, spans: spans,\n      complete: spans && cov >= MIN_COVERAGE, source: 'aggregated' };\n  });\n  // An incomplete session is dropped rather than scored: every component here\n  // reads high, low, close or volume, and all four are wrong on a partial day.\n  return out.filter(function (b) { return b.complete; });\n}\n\n// ---------------------------------------------------------------- components\n// Each returns { points, max, why } so the score can always be explained. A\n// number with no reasons attached is not usable for a decision.\n\n// Multi-day direction: higher closes AND higher lows, or the mirror.\nfunction trendComponent(daily) {\n  var out = { points: 0, max: 25, why: null, label: 'RANGE' };\n  if (daily.length < 4) { out.why = null; return out; }\n  var w = daily.slice(-4);\n  var upC = 0, upL = 0, dnC = 0, dnL = 0;\n  for (var i = 1; i < w.length; i++) {\n    if (w[i].close > w[i - 1].close) upC++; else if (w[i].close < w[i - 1].close) dnC++;\n    if (w[i].low > w[i - 1].low) upL++; else if (w[i].low < w[i - 1].low) dnL++;\n  }\n  if (upC >= 2 && upL >= 2) { out.points = 12 + Math.min(13, (upC + upL) * 3); out.label = 'UP';\n    out.why = 'מגמה רב-יומית עולה — ' + upC + ' סגירות ו-' + upL + ' שפלים עולים'; }\n  else if (dnC >= 2 && dnL >= 2) { out.points = 10 + Math.min(10, (dnC + dnL) * 2); out.label = 'DOWN';\n    out.why = 'מגמה רב-יומית יורדת — ' + dnC + ' סגירות ו-' + dnL + ' שיאים יורדים'; }\n  else { out.points = 4; out.label = 'RANGE'; out.why = 'ללא כיוון רב-יומי ברור'; }\n  return out;\n}\n\n// Enough daily movement to be worth watching at all. A name whose whole day is\n// 0.4% cannot pay for the attention, however clean its structure.\nfunction rangeComponent(daily) {\n  var out = { points: 0, max: 15, why: null, atrPct: null };\n  if (!daily.length) return out;\n  var w = daily.slice(-5);\n  var avg = w.reduce(function (s, b) { return s + b.range; }, 0) / w.length;\n  var px = w[w.length - 1].close || 1;\n  var pct = avg / px * 100;\n  out.atrPct = Math.round(pct * 100) / 100;\n  // This is the average over five sessions, not today's range. Calling it\n  // \"daily range\" made it look wrong against a single day's high-low.\n  var last = w[w.length - 1];\n  out.lastRangePct = px > 0 ? Math.round(last.range / px * 10000) / 100 : null;\n  out.window = w.length;\n  if (pct >= 3) { out.points = 15; out.why = 'טווח ממוצע רחב (' + out.window + ' ימים) — ' + out.atrPct + '% מהמחיר'; }\n  else if (pct >= 1.8) { out.points = 12; out.why = 'טווח ממוצע טוב (' + out.window + ' ימים) — ' + out.atrPct + '%'; }\n  else if (pct >= 1.0) { out.points = 7; out.why = 'טווח ממוצע בינוני (' + out.window + ' ימים) — ' + out.atrPct + '%'; }\n  else { out.points = 1; out.why = 'טווח ממוצע צר (' + out.window + ' ימים) — ' + out.atrPct + '%, קשה להוציא ממנו מהלך'; }\n  return out;\n}\n\n// A contraction into a decision point is the classic pre-move condition: the\n// last two sessions narrower than the ones before them.\nfunction coilComponent(daily, atrPct, relVol) {\n  var out = { points: 0, max: 12, why: null, coiled: false };\n  if (daily.length < 5) return out;\n  // A dead stock is narrow and quiet too. Contraction only means something when\n  // the name has enough daily range to matter and the volume has not collapsed\n  // with it — otherwise this rewards exactly the stocks worth ignoring.\n  if (atrPct != null && atrPct < 1.0) { out.why = null; return out; }\n  if (relVol != null && relVol < 0.6) { out.why = null; return out; }\n  var recent = daily.slice(-2), prior = daily.slice(-5, -2);\n  var r = recent.reduce(function (s, b) { return s + b.range; }, 0) / recent.length;\n  var p = prior.reduce(function (s, b) { return s + b.range; }, 0) / prior.length;\n  if (!(p > 0)) return out;\n  var ratio = r / p;\n  if (ratio <= 0.6) { out.points = 12; out.coiled = true; out.why = 'התכווצות טווח חדה לפני הפתיחה — ' + Math.round(ratio * 100) + '% מהממוצע'; }\n  else if (ratio <= 0.8) { out.points = 8; out.coiled = true; out.why = 'התכווצות טווח — ' + Math.round(ratio * 100) + '% מהממוצע'; }\n  return out;\n}\n\n// Where the last session closed inside its own range. A close on the high into\n// a multi-day high is the setup that most often continues.\nfunction closeComponent(daily) {\n  var out = { points: 0, max: 10, why: null };\n  if (!daily.length) return out;\n  var last = daily[daily.length - 1];\n  if (last.closePos >= 0.85) { out.points = 10; out.why = 'סגר בשיא היום — ' + Math.round(last.closePos * 100) + '% מהטווח'; }\n  else if (last.closePos >= 0.7) { out.points = 7; out.why = 'סגר בחלק העליון של היום'; }\n  else if (last.closePos <= 0.15) { out.points = 6; out.why = 'סגר בשפל היום — חולשה שמעניינת לשורט או לריבאונד'; }\n  else if (last.closePos <= 0.3) { out.points = 4; out.why = 'סגר בחלק התחתון של היום'; }\n  return out;\n}\n\n// Volume behind the last session, against its own recent average.\nfunction volumeComponent(daily) {\n  var out = { points: 0, max: 13, why: null, relVol: null };\n  if (daily.length < 3) return out;\n  var last = daily[daily.length - 1], prior = daily.slice(-6, -1);\n  if (!prior.length) return out;\n  var avg = prior.reduce(function (s, b) { return s + b.volume; }, 0) / prior.length;\n  if (!(avg > 0)) return out;\n  var rel = last.volume / avg;\n  out.relVol = Math.round(rel * 100) / 100;\n  if (rel >= 1.8) { out.points = 13; out.why = 'מחזור חריג בסשן האחרון — ×' + out.relVol; }\n  else if (rel >= 1.3) { out.points = 10; out.why = 'מחזור מוגבר — ×' + out.relVol; }\n  else if (rel >= 0.9) { out.points = 5; out.why = 'מחזור רגיל — ×' + out.relVol; }\n  else { out.points = 2; out.why = 'מחזור דל — ×' + out.relVol + ', עניין נמוך'; }\n  return out;\n}\n\n// Sitting just under a multi-day high (or over a low) is a decision point: the\n// session opens with something to resolve.\nfunction levelComponent(daily) {\n  var out = { points: 0, max: 25, why: null, levels: [] };\n  if (daily.length < 2) return out;\n  var last = daily[daily.length - 1], prior = daily.slice(0, -1);\n  var px = last.close, atr = daily.slice(-5).reduce(function (s, b) { return s + b.range; }, 0) / Math.min(5, daily.length);\n  if (!(atr > 0)) return out;\n\n  var hi = Math.max.apply(null, prior.map(function (b) { return b.high; }));\n  var lo = Math.min.apply(null, prior.map(function (b) { return b.low; }));\n  var pd = prior[prior.length - 1];\n\n  // Each level carries the session it came from. \"Yesterday\" meant 09-02 here\n  // and 09-03 in the range figure on the same card — the reader had no way to\n  // know which.\n  var pdDate = pd.date ? ' ' + pd.date.slice(5).replace('-', '/') : '';\n  out.levels = [\n    { name: 'גבוה רב-יומי', price: hi, date: null },\n    { name: 'גבוה סשן קודם' + pdDate, price: pd.high, date: pd.date },\n    { name: 'סגירת סשן קודם' + pdDate, price: pd.close, date: pd.date },\n    { name: 'נמוך סשן קודם' + pdDate, price: pd.low, date: pd.date },\n    { name: 'נמוך רב-יומי', price: lo, date: null }\n  ].filter(function (x) { return isFinite(x.price); });\n  out.priorDate = pd.date || null;\n\n  // The same guard: \"right under the multi-day high\" is meaningless when the\n  // multi-day range is a fraction of a percent.\n  var spanPct = (hi - lo) / (px || 1) * 100;\n  if (spanPct < 1.0) { out.points = 2; out.why = 'הטווח הרב-יומי צר מדי מכדי שרמה תהיה נקודת הכרעה'; return out; }\n  var dHi = (hi - px) / atr, dLo = (px - lo) / atr;\n  // WHAT THE LAST SESSION DID AT THE LEVEL, not just where it closed relative\n  // to it. A day that traded THROUGH the multi-day high and closed back under\n  // it is a rejected breakout — the opposite of a coil waiting to resolve — and\n  // scoring both as \"right under the level, decision point\" gave a failed\n  // breakout the maximum available points. Measured: AAPL reached 330.81 above\n  // a 328.40 level and closed 328.21; NVDA reached 230.40 above 229.26 and\n  // closed 228.45. Both were being ranked as pre-breakout.\n  var touchedHi = last.high >= hi - 0.05 * atr;\n  var brokeHi = last.high > hi;\n  var heldHi = px > hi;\n  var brokeLo = last.low < lo, heldLo = px < lo;\n  out.event = heldHi ? 'breakout_held'\n    : brokeHi ? 'breakout_failed'\n    : touchedHi ? 'testing_high'\n    : heldLo ? 'breakdown_held'\n    : brokeLo ? 'breakdown_recovered'\n    : dHi <= 0.35 ? 'approaching_high'\n    : dLo <= 0.35 ? 'approaching_low' : 'mid_range';\n\n  if (out.event === 'breakout_held') { out.points = 22;\n    out.why = 'פרץ את הגבוה הרב-יומי וסגר מעליו — פריצה שצריכה אישור'; }\n  else if (out.event === 'breakout_failed') { out.points = 6;\n    // Name the LEVEL and the price reached separately. Putting last.high in the\n    // parentheses read as though 83.60 were the multi-day high, while the level\n    // printed two lines below said 83.12 — the same card contradicting itself.\n    out.why = 'ניסה לפרוץ את הגבוה הרב-יומי ' + hi.toFixed(2)\n      + ', הגיע ל-' + last.high.toFixed(2) + ' ונסגר ב-' + px.toFixed(2) + ' מתחתיו — פריצה שנדחתה'; }\n  else if (out.event === 'testing_high') { out.points = 20;\n    out.why = 'נגע בגבוה הרב-יומי בלי לפרוץ — נבחן'; }\n  else if (out.event === 'approaching_high') { out.points = 25;\n    out.why = 'ממש מתחת לגבוה הרב-יומי, בלי ניסיון פריצה — נקודת הכרעה בפתיחה'; }\n  else if (dHi > 0.35 && dHi <= 1) { out.points = 18; out.why = 'קרוב לגבוה הרב-יומי'; }\n  else if (out.event === 'breakdown_held') { out.points = 16;\n    out.why = 'סגר מתחת לנמוך הרב-יומי — שבירה'; }\n  else if (out.event === 'breakdown_recovered') { out.points = 14;\n    out.why = 'שבר את הנמוך הרב-יומי ' + lo.toFixed(2)\n      + ', ירד עד ' + last.low.toFixed(2) + ' וחזר מעליו — שבירה שנדחתה'; }\n  else if (out.event === 'approaching_low') { out.points = 17;\n    out.why = 'ממש מעל הנמוך הרב-יומי — נקודת הכרעה'; }\n  else { out.points = 5; out.why = 'באמצע הטווח הרב-יומי'; }\n  return out;\n}\n\n// ---------------------------------------------------------------- the score\n//\n// candidateScore(daysRows) -> { score, reasons, structure, levels, ... }\n//\n// daysRows: an array of sessions, oldest first, each an array of 1-minute rows.\n// Every session here is CLOSED. Nothing about a live setup enters this.\nfunction candidateScore(daysRows, opts) {\n  var o = opts || {};\n  var all = (daysRows || []).filter(function (r) { return r && r.length; });\n  var daily = toDaily(all, o);\n  var dropped = all.length - daily.length;\n  if (!daily.length) {\n    return { score: null, reasons: [], structure: null, levels: [], sessions: 0,\n      incomplete_sessions: dropped,\n      note: dropped ? 'הסשנים הקיימים אינם שלמים — לא ניתן לחשב' : 'אין נתונים היסטוריים' };\n  }\n  var range = rangeComponent(daily), volume = volumeComponent(daily);\n  var parts = {\n    trend: trendComponent(daily),\n    range: range,\n    coil: coilComponent(daily, range.atrPct, volume.relVol),\n    close: closeComponent(daily),\n    volume: volume,\n    level: levelComponent(daily)\n  };\n  var got = 0, max = 0;\n  Object.keys(parts).forEach(function (k) { got += parts[k].points; max += parts[k].max; });\n  // Normalised so a symbol with only three sessions is not punished for the\n  // components that need five — its max is smaller, not its score.\n  var score = Math.round(got / max * 100);\n  // A stock whose daily range cannot pay for a trade is not a candidate, no\n  // matter how tidy its structure looks. This is a ceiling, not a deduction:\n  // the components stay honest and the verdict is capped.\n  var capped = null;\n  if (range.atrPct != null && range.atrPct < 0.8 && score > 30) { capped = score; score = 30; }\n  else if (range.atrPct != null && range.atrPct < 1.2 && score > 55) { capped = score; score = 55; }\n  var capWindow = range.window || 5;\n\n  // Reasons, strongest first, so the screen can show the top two.\n  var reasons = Object.keys(parts)\n    .map(function (k) { return { key: k, points: parts[k].points, text: parts[k].why }; })\n    .filter(function (x) { return x.text; })\n    .sort(function (a, b) { return b.points - a.points; });\n\n  var last = daily[daily.length - 1];\n  return {\n    score: score, reasons: reasons, parts: parts,\n    cappedFrom: capped,\n    capReason: capped != null ? 'הציון הוגבל: טווח ממוצע ' + capWindow + ' ימים של '\n      + range.atrPct + '% קטן מכדי להצדיק מעקב' : null,\n    structure: parts.trend.label,\n    structureWhy: parts.trend.why,\n    levels: parts.level.levels,\n    atrPct: parts.range.atrPct, avgRangeWindow: parts.range.window,\n    lastRangePct: parts.range.lastRangePct,\n    levelEvent: parts.level.event, priorDate: parts.level.priorDate,\n    relVol: parts.volume.relVol, coiled: parts.coil.coiled,\n    sessions: daily.length, incomplete_sessions: dropped,\n    incompleteNote: dropped ? dropped + ' סשנים לא שלמים לא נכללו בחישוב' : null,\n    dataSource: daily.every(function (b) { return b.source === 'authoritative'; }) ? 'authoritative'\n      : daily.some(function (b) { return b.source === 'authoritative'; }) ? 'mixed' : 'aggregated',\n    lastClose: last.close, lastDate: last.date, closePos: Math.round(last.closePos * 100),\n    daily: daily\n  };\n}\n\n// Is the interest improving or fading? The same score computed one session\n// earlier, compared with today's. Without at least three sessions there is\n// nothing to compare and the honest answer is that we do not know.\nfunction candidateTrend(daysRows) {\n  var rows = (daysRows || []).filter(function (r) { return r && r.length; });\n  if (rows.length < 3) return { direction: 'unknown', delta: null, text: 'אין מספיק ימים להשוואה' };\n  var now = candidateScore(rows), before = candidateScore(rows.slice(0, -1));\n  if (now.score == null || before.score == null) return { direction: 'unknown', delta: null, text: 'אין מספיק ימים להשוואה' };\n  var d = now.score - before.score;\n  return {\n    direction: d >= 6 ? 'improving' : d <= -6 ? 'fading' : 'steady',\n    delta: d,\n    text: d >= 6 ? 'העניין מתחזק (+' + d + ')' : d <= -6 ? 'העניין נחלש (' + d + ')' : 'ללא שינוי מהותי'\n  };\n}\n\n// ---------------------------------------------------------------- live interest\n//\n// For symbols already on the watchlist, during the session: is this still worth\n// the attention it is taking? This is NOT the setup score — a symbol can hold a\n// valid setup and still have gone quiet, and a symbol with no setup can be the\n// most alive thing on the screen.\nfunction liveInterest(A, ctx) {\n  var c = ctx || {};\n  if (!A || !A.state) return { score: null, reasons: [], verdict: 'אין נתונים' };\n  var S = A.state, b = S.bar, bars = A.bars, n = bars.length;\n  var atr = b.atr20 || b.avgRange || 1;\n  var reasons = [], score = 0;\n\n  // Movement over the last half hour, judged BOTH against the symbol's own\n  // average bar and against its price. Measuring only in ATR units made a dead\n  // stock look active: when the average bar is a cent, six cents is \"6R\" and\n  // nothing at all has happened.\n  var w = bars.slice(-30);\n  var hi = Math.max.apply(null, w.map(function (x) { return x.high; }));\n  var lo = Math.min.apply(null, w.map(function (x) { return x.low; }));\n  var swing = (hi - lo) / atr;\n  var swingPct = b.close > 0 ? (hi - lo) / b.close * 100 : 0;\n  var moving = swingPct >= 0.25;                  // a quarter percent in 30 min\n  if (swing >= 6 && moving) { score += 30; reasons.push('תנועה חזקה בחצי השעה האחרונה'); }\n  else if (swing >= 3 && moving) { score += 20; reasons.push('תנועה סבירה בחצי השעה האחרונה'); }\n  else if (swing >= 1.5 && moving) { score += 10; reasons.push('תנועה מוגבלת'); }\n  else { reasons.push('כמעט ללא תנועה בחצי השעה האחרונה (' + (Math.round(swingPct * 100) / 100) + '%)'); }\n\n  // is volume still arriving\n  var recentVol = w.reduce(function (s, x) { return s + x.volume; }, 0) / Math.max(1, w.length);\n  var dayVol = bars.reduce(function (s, x) { return s + x.volume; }, 0) / Math.max(1, n);\n  var rel = dayVol > 0 ? recentVol / dayVol : 0;\n  if (rel >= 1.3) { score += 25; reasons.push('מחזור מתגבר (×' + (Math.round(rel * 100) / 100) + ')'); }\n  else if (rel >= 0.8) { score += 15; reasons.push('מחזור יציב'); }\n  else { score += 3; reasons.push('המחזור דועך (×' + (Math.round(rel * 100) / 100) + ')'); }\n\n  // proximity to a level that matters\n  var T = c.tactical || tactical(A);\n  var near = [T.support, T.resistance].filter(Boolean).map(function (l) { return Math.abs(l.r); });\n  var d = near.length ? Math.min.apply(null, near) : Infinity;\n  if (d <= 0.5) { score += 25; reasons.push('צמוד לרמה משמעותית'); }\n  else if (d <= 1.5) { score += 15; reasons.push('קרוב לרמה'); }\n  else if (isFinite(d)) { score += 5; reasons.push('רחוק מרמות'); }\n\n  // a structure worth following\n  var st = S.announced || S.trend;\n  if (st === 'UP' || st === 'DOWN') { score += 20; reasons.push('מבנה תוך-יומי מוכרז (' + st + ')'); }\n  else { score += 5; reasons.push('ללא מבנה תוך-יומי'); }\n\n  // Nothing else can rescue a symbol that is not moving: structure and levels\n  // are only worth watching on something that travels.\n  if (!moving) { score = Math.min(score, 30); reasons.push('אין תנועה — לא מצדיק תשומת לב חיה'); }\n  score = Math.max(0, Math.min(100, score));\n  return {\n    score: score, reasons: reasons, movingPct: Math.round(swingPct * 100) / 100,\n    verdict: score >= 60 ? 'שווה תשומת לב חיה' : score >= 35 ? 'עניין בינוני' : 'כבר לא מעניין',\n    quiet: score < 35, swingR: Math.round(swing * 10) / 10, volRel: Math.round(rel * 100) / 100\n  };\n}\n\n// ---------------------------------------------------------------- suggestions\n//\n// Recommends only. Nothing here moves a symbol on its own — the trader decides,\n// and a screen that silently reshuffled the watchlist would be worse than one\n// that said nothing.\n//\n//   watch: [{ symbol, candidate, live }]\n//   scan:  [{ symbol, candidate, trend }]\nfunction suggestions(watch, scan, opts) {\n  var o = opts || {};\n  var PROMOTE_AT = o.promoteAt == null ? 60 : o.promoteAt;\n  var DEMOTE_AT = o.demoteAt == null ? 35 : o.demoteAt;\n\n  var weakest = (watch || []).filter(function (x) {\n    return x.live && x.live.score != null && x.live.score < DEMOTE_AT;\n  }).sort(function (a, b) { return a.live.score - b.live.score; });\n\n  var promote = (scan || []).filter(function (x) {\n    if (!x.candidate || x.candidate.score == null) return false;\n    if (x.candidate.score < PROMOTE_AT) return false;\n    // Improving matters as much as high: a score that is high and fading is\n    // yesterday's story.\n    return !x.trend || x.trend.direction !== 'fading';\n  }).sort(function (a, b) {\n    var d = b.candidate.score - a.candidate.score;\n    if (d) return d;\n    return (b.trend && b.trend.delta || 0) - (a.trend && a.trend.delta || 0);\n  });\n\n  var room = Math.max(0, (o.max == null ? 40 : o.max) - (watch || []).length);\n  return {\n    promote: promote.slice(0, 8).map(function (x, i) {\n      // Only pair a promotion with a removal when the list is actually full.\n      var swap = (!room && weakest[i]) ? weakest[i] : null;\n      return {\n        symbol: x.symbol, score: x.candidate.score,\n        why: (x.candidate.reasons || []).slice(0, 2).map(function (r) { return r.text; }),\n        direction: x.trend ? x.trend.direction : 'unknown',\n        directionText: x.trend ? x.trend.text : null,\n        insteadOf: swap ? swap.symbol : null,\n        insteadOfWhy: swap ? swap.live.verdict + ' (' + swap.live.score + ')' : null\n      };\n    }),\n    demote: weakest.slice(0, 8).map(function (x) {\n      return { symbol: x.symbol, score: x.live.score, why: x.live.reasons.slice(0, 2),\n        verdict: x.live.verdict };\n    }),\n    room: room,\n    note: room ? 'יש מקום — הוספה לא מחייבת הסרה' : 'המעקב מלא: הוספה מחייבת הסרה'\n  };\n}\n\n\n// ============================================================================\n// BUY DECISION CARD — an additional view. Nothing here is read by any other\n// surface, and this file reads the state the application has already built.\n//\n// The premise that shapes everything: we do NOT know the price the trader is\n// looking at in his broker. So the card must never say \"if it reaches X, buy\" —\n// that is a limit order, and it assumes we can see the market. It says instead:\n// given the price ON YOUR SCREEN RIGHT NOW, here is the decision.\n//\n// Four states only. BUY, WAIT, RECHECK, NO SETUP. RECHECK is not a refusal —\n// it means the snapshot no longer covers the price being seen, and the answer\n// has to be recomputed from newer candles. A lower price may be a better\n// entry; a higher one may be a breakout. Neither is knowable from here.\n// ============================================================================\n\nfunction buyCard(st, A, ctx) {\n  var c = ctx || {};\n  var NA = '—';\n  var n2 = function (x) { return x == null ? NA : (+x).toFixed(2); };\n\n  var out = {\n    symbol: st && st.symbol || (c.symbol || NA),\n    snapshotTime: c.snapshotTime || null,\n    lastBarTime: null, lastClose: null,\n    coverage: null, decision: 'NO SETUP', quality: NA, confidence: NA,\n    map: [], validity: null, reasons: [], levels: [], blocked: null\n  };\n  if (!st || !A || !A.state) { out.blocked = 'אין ניתוח זמין'; return out; }\n\n  var bars = (A.rawBars && A.rawBars.length) ? A.rawBars : A.bars;\n  var last = bars && bars.length ? bars[bars.length - 1] : null;\n  var b = A.state.bar;\n  var atr = (b && (b.atr20 || b.avgRange)) || 0;\n  out.lastBarTime = last ? last.time : null;\n  out.lastClose = last ? last.close : null;\n  out.coverage = st.coverage || null;\n\n  // ---- data safety comes before anything else -----------------------------\n  // An unusable session cannot produce a BUY, whatever the setup looks like.\n  var cov = st.coverage || {};\n  if (st.stale) out.blocked = 'נתונים ישנים — לא ניתן להחליט';\n  else if (st.sessionIncomplete)\n    out.blocked = 'סשן חלקי (' + (cov.receivedMinutes || 0) + '/' + (cov.expectedMinutes || 0)\n      + ' דקות) — לא ניתן להחליט';\n  else if (st.tooOldToEnter) out.blocked = 'הנר האחרון ישן מדי להחלטה';\n  else if (!last) out.blocked = 'אין נרות';\n  if (out.blocked) {\n    out.decision = 'RECHECK';\n    out.reasons.push(out.blocked);\n    return out;\n  }\n\n  // ---- what the existing analysis already decided --------------------------\n  var P = st.plan || null;\n  var lv = st.levels || {};\n  var px = out.lastClose;\n\n  // The BUY range is the zone the existing plan already identified, not a\n  // percentage invented around the last price. Without a plan there is no zone\n  // and therefore no buy.\n  var zone = (P && P.zone && P.zone.length === 2) ? P.zone.slice() : null;\n  var hasEdge = !!(P && zone && !st.noEdge);\n\n  // Validity comes from STRUCTURE, not from a band drawn around the last price.\n  // 616.42-617.08 around 616.75 is arbitrary symmetry dressed up as analysis:\n  // it says nothing about where the reading actually stops holding. The real\n  // boundaries are the levels the setup depends on — the zone, the\n  // invalidation, the nearest support and resistance. Where none of those\n  // exist, there is no validity range and none is invented.\n  var T = st.tactical || null;\n  var bounds = [];\n  var addBound = function (v, why) {\n    if (v == null || !isFinite(v)) return;\n    bounds.push({ price: +(+v).toFixed(2), why: why });\n  };\n  if (T && T.support) addBound(T.support.price, 'תמיכה');\n  if (T && T.resistance) addBound(T.resistance.price, 'התנגדות');\n  if (lv.tacticalInvalidation != null) addBound(lv.tacticalInvalidation, 'ביטול');\n  if (lv.watch != null) addBound(lv.watch, 'רמת מעקב');\n  if (P && P.zone && P.zone.length === 2) { addBound(P.zone[0], 'אזור'); addBound(P.zone[1], 'אזור'); }\n\n  var below = bounds.filter(function (x) { return x.price < px; }).sort(function (a, b2) { return b2.price - a.price; })[0];\n  var above = bounds.filter(function (x) { return x.price > px; }).sort(function (a, b2) { return a.price - b2.price; })[0];\n  var vLow = below ? below.price : null;\n  var vHigh = above ? above.price : null;\n  out.validity = (vLow != null && vHigh != null)\n    ? { low: vLow, high: vHigh, lowWhy: below.why, highWhy: above.why } : null;\n\n  // ---- the decision --------------------------------------------------------\n  if (!hasEdge) {\n    // NO SETUP is its own answer, not a weak version of one. There is nothing\n    // whose validity could be mapped, so there is no price map, no validity\n    // range and no confidence — confidence in a setup that does not exist is a\n    // number about nothing. And it is never RECHECK: RECHECK means an ACTIVE\n    // setup no longer covers the price, which presupposes a setup.\n    out.decision = 'NO SETUP';\n    out.reasons.push(st.why || 'אין setup קנייה כרגע');\n    out.map = [];\n    out.validity = null;\n    out.quality = 'אין';\n    out.confidence = NA;\n    out.hasMap = false;\n  } else {\n    var zLow = Math.min(zone[0], zone[1]), zHigh = Math.max(zone[0], zone[1]);\n    // Does the buy zone overlap the band this snapshot can speak about?\n    var overlaps = zHigh >= vLow && zLow <= vHigh;\n    out.decision = overlaps ? 'BUY' : 'WAIT';\n    if (!overlaps) out.reasons.push('אזור הקנייה (' + n2(zLow) + '–' + n2(zHigh)\n      + ') מחוץ לטווח שהכרטיס יכול לדבר עליו');\n  }\n\n  // ---- the price decision map ---------------------------------------------\n  // Only where there IS an active setup, and only where structure gives real\n  // boundaries. Read as: the number you are looking at in your broker.\n  var rows = [];\n  var canMap = hasEdge && out.validity != null;\n  out.hasMap = canMap;\n  if (canMap) {\n  if (out.decision === 'BUY') {\n    var zl = Math.max(vLow, Math.min(zone[0], zone[1]));\n    var zh = Math.min(vHigh, Math.max(zone[0], zone[1]));\n    rows.push({ from: zl, to: zh, decision: 'BUY', text: 'קונה' });\n    if (zh < vHigh) rows.push({ from: +(zh + 0.01).toFixed(2), to: vHigh, decision: 'WAIT', text: 'לא קונה' });\n    if (zl > vLow) rows.push({ from: vLow, to: +(zl - 0.01).toFixed(2), decision: 'WAIT', text: 'לא קונה' });\n  } else if (out.decision === 'WAIT' || out.decision === 'NO SETUP') {\n    rows.push({ from: vLow, to: vHigh, decision: out.decision === 'WAIT' ? 'WAIT' : 'NO SETUP',\n      text: out.decision === 'WAIT' ? 'לא קונה' : 'אין setup' });\n  }\n  rows.sort(function (x, y) { return y.from - x.from; });\n  rows.push({ above: vHigh, decision: 'RECHECK', text: 'לבדוק מחדש' });\n  rows.push({ below: vLow, decision: 'RECHECK', text: 'לבדוק מחדש' });\n  }\n  out.map = rows;\n\n  // ---- supporting readings, all taken from what already exists -------------\n  if (out.decision !== 'NO SETUP') {\n    out.quality = out.decision === 'BUY' ? 'תקין' : 'חלש';\n    out.confidence = (st.score != null)\n      ? (st.score >= 7 ? 'גבוה' : st.score >= 4 ? 'בינוני' : 'נמוך') : NA;\n  }\n\n  out.structure = st.structure || (A.state.announced || A.state.trend) || NA;\n  out.momentum = st.momentum || NA;\n  out.vwap = b ? b.vwap : null;\n  out.aboveVwap = b ? !!b.aboveVwap : null;\n  out.ema9 = b ? b.ema9 : null;\n  out.ema20 = b ? b.ema20 : null;\n  out.market = c.market || NA;\n\n  var pf = st.pressure || null;\n  if (pf) {\n    // The pressure object exposes buyPct/sellPct. I read buyers/sellers and got\n    // undefined, so the card showed \"buyers weakening\" with no numbers beside\n    // it — the exact thing the rule forbids.\n    out.buyersPct = pf.buyPct != null ? pf.buyPct : pf.buyers;\n    out.sellersPct = pf.sellPct != null ? pf.sellPct : pf.sellers;\n    out.buyersTrend = pf.buyersTrend; out.sellersTrend = pf.sellersTrend;\n  }\n\n  // Nearby levels that actually bear on a buy decision.\n  if (T) {\n    if (T.support) out.levels.push({ name: 'תמיכה קרובה', price: T.support.price });\n    if (T.resistance) out.levels.push({ name: 'התנגדות קרובה', price: T.resistance.price });\n  }\n  if (lv.watch != null) out.levels.push({ name: 'רמת מעקב', price: lv.watch });\n\n  // ---- a real explanation, not a label ------------------------------------\n  // \"אין setup קנייה כרגע\" restates the verdict. The reader needs to know WHY\n  // from the same facts printed above: where the price finished, what the\n  // structure is doing, and which side is gaining.\n  out.why = (function () {\n    var parts = [];\n    if (px != null) parts.push('המחיר ' + (c.closed ? 'סיים ב-' : 'עומד על ') + px.toFixed(2));\n    var mom = { PUSHING: 'בדחיפה', PULLBACK: 'בתוך pullback', RECOVERY: 'בהתאוששות',\n      SELLING: 'תחת מכירות', FLAT: 'ללא מומנטום' }[out.momentum];\n    var strc = { UP: 'מבנה עולה', DOWN: 'מבנה יורד', RANGE: 'בטווח' }[out.structure];\n    if (strc) parts.push(strc);\n    if (mom) parts.push(mom);\n    if (pf && pf.buyersTrend && pf.sellersTrend)\n      parts.push('הקונים ' + pf.buyersTrend + ' והמוכרים ' + pf.sellersTrend);\n    var lead = parts.join(', ') + '.';\n    var verdict = out.decision === 'NO SETUP' ? ' כרגע אין setup קנייה מספיק ברור.'\n      : out.decision === 'WAIT' ? ' התנאים לא מצדיקים קנייה במחיר הנוכחי.'\n      : out.decision === 'BUY' ? ' התנאים מצדיקים קנייה בטווח המסומן.'\n      : ' צריך נרות חדשים כדי להחליט.';\n    return lead + verdict;\n  })();\n\n  // What the trader is actually asking. When the market is shut this is not a\n  // rejection — there is simply nothing to act on.\n  out.buyNow = c.closed ? '— המסחר סגור'\n    : out.decision === 'BUY' ? 'כן, בטווח המסומן'\n    : out.decision === 'WAIT' ? 'לא'\n    : out.decision === 'RECHECK' ? '— צריך נרות חדשים'\n    : '— אין setup';\n\n  // A live price map only means something while a live price exists.\n  if (c.closed) { out.hasMap = false; out.map = []; out.validity = null; }\n  out.marketClosed = !!c.closed;\n\n  // A probability is shown only if it measures THIS decision. The path model\n  // answers \"which of two levels is touched first\", which is a different\n  // question, so it is deliberately not carried over.\n  out.probability = null;\n  out.probabilityNote = 'הסתברות לא מוצגת: המודל הקיים מודד אירוע אחר';\n\n  if (!out.reasons.length && st.why) out.reasons.push(st.why);\n  return out;\n}\n\n// The same card as plain text. Built from the card object rather than scraped\n// from the DOM, so what is copied is exactly what was decided — a copy that can\n// drift from the screen is worse than none.\nfunction buyCardText(card, opts) {\n  var o = opts || {};\n  var NA = '—';\n  var n2 = function (x, d) { return x == null ? NA : (+x).toFixed(d == null ? 2 : d); };\n  var L = [];\n  var DEC = { BUY: 'BUY — אפשר לקנות', WAIT: 'WAIT — לא קונה כרגע',\n    RECHECK: 'RECHECK — לבדוק מחדש', 'NO SETUP': 'NO SETUP — אין setup' };\n\n  L.push(card.symbol + (card.marketClosed\n    ? ' · סגירה' + (card.snapshotTime ? ' ' + card.snapshotTime : '')\n    : (card.snapshotTime ? ' · תמונת מצב ' + card.snapshotTime : '')));\n  var cov = card.coverage || {};\n  L.push('נר אחרון: ' + n2(card.lastClose, 3)\n    + (cov.receivedMinutes != null ? ' · ' + cov.receivedMinutes + '/' + cov.expectedMinutes\n      + ' נרות ' + (cov.complete ? 'תקין' : 'חסר') : ''));\n  L.push('');\n  L.push(DEC[card.decision] || card.decision);\n  if (card.blocked) L.push(card.blocked);\n  L.push('');\n  L.push('מצב קנייה: ' + card.quality);\n  L.push('ביטחון: ' + card.confidence);\n\n  if (card.hasMap && card.validity) {\n    L.push('');\n    L.push(o.live ? 'מחיר Trading שלך עכשיו' : 'מפת מחיר');\n    (card.map || []).forEach(function (r) {\n      var range = r.above != null ? ('מעל ' + n2(r.above))\n        : r.below != null ? ('מתחת ' + n2(r.below))\n        : (n2(r.from) + '–' + n2(r.to));\n      L.push('  ' + range + '  ->  ' + r.decision + ' — ' + r.text);\n    });\n    L.push('');\n    L.push('תקפות הכרטיס: ' + n2(card.validity.low) + '–' + n2(card.validity.high)\n      + ' (' + card.validity.lowWhy + ' / ' + card.validity.highWhy + ')');\n    L.push('מחוץ לטווח -> RECHECK עם הנרות החדשים.');\n    L.push('מחיר נמוך יותר עשוי ליצור setup טוב יותר, גבוה יותר עשוי ליצור פריצה.');\n  }\n\n  L.push('');\n  L.push('למה אין BUY');\n  if (card.blocked) L.push('  ' + card.blocked);\n  L.push('  ' + (card.why || (card.reasons || []).join(' ')));\n\n  L.push('');\n  L.push('נתונים');\n  L.push('  מבנה: ' + card.structure);\n  L.push('  מומנטום: ' + card.momentum);\n  L.push('  ' + (card.vwap == null ? 'VWAP: ' + NA\n    : (card.aboveVwap ? 'מחיר מעל VWAP ' : 'מחיר מתחת VWAP ') + n2(card.vwap)));\n  L.push('  EMA9 / EMA20: ' + n2(card.ema9) + ' / ' + n2(card.ema20));\n  L.push('  שוק: ' + card.market);\n\n  if (card.buyersPct != null) {\n    L.push('');\n    L.push('קונים / מוכרים: ' + card.buyersPct + '% / ' + card.sellersPct + '%');\n    var dir = [];\n    if (card.buyersTrend) dir.push('קונים ' + card.buyersTrend);\n    if (card.sellersTrend) dir.push('מוכרים ' + card.sellersTrend);\n    if (dir.length) L.push('כיוון הכוח: ' + dir.join(' · '));\n  }\n\n  if ((card.levels || []).length) {\n    L.push('');\n    L.push('רמות');\n    card.levels.forEach(function (l) { L.push('  ' + l.name + ': ' + n2(l.price)); });\n  }\n\n  L.push('');\n  L.push('החלטה');\n  L.push('  BUY NOW: ' + card.buyNow);\n  if (!card.hasMap) L.push('  מפת מחיר: '\n    + (card.marketClosed ? 'לא מוצגת כשהמסחר סגור' : 'לא מוצגת — אין setup'));\n  L.push('');\n  L.push(card.probabilityNote);\n  return L.join('\\n');\n}\n\n\n// ============================================================================\n// SCANNER REPLAY — feed the EXISTING scanner historical candles, one minute at\n// a time, and measure what happened next.\n//\n// The one property everything rests on: at minute i the scanner receives\n// rows.slice(0, i + 1) and nothing else. It cannot see a candle that had not\n// printed yet. analyze() looks one bar forward for its two-bar confirmations,\n// so when the slice ends at i that lookahead simply finds nothing and reports\n// `confirmed: null` — which is exactly what a live scanner would have had.\n//\n// Forward performance is measured from the FULL day. That is not a leak: it is\n// the evaluation, not the decision. The two are kept apart by construction —\n// replayStates() never sees the future, scoreAlert() never feeds the scanner.\n// ============================================================================\n\n// A state change worth recording, not one row per candle.\nvar TRACKED_STATES = ['QUIET', 'WATCH', 'CLOSE', 'READY', 'ACTIVE', 'AVOID', 'NO DATA'];\n\n// Which transitions count as an alert — the moment the scanner said \"this is\n// worth attention\". Everything else is recorded but not scored.\nvar ALERT_STATES = ['WATCH', 'CLOSE', 'READY', 'ACTIVE'];\n\nvar CONFIG = {\n  // Below this, an alert produced no move worth having. Kept here so it can be\n  // changed without touching the logic.\n  falseAlertMaxUpPct: 0.30,\n  falseAlertWindowMin: 30,\n  windows: [5, 15, 30, 60],\n  // ZERO, and deliberately so.\n  //\n  // I had this at 20 and it was my invention, not the scanner's: nothing in\n  // engine.cjs, layers.cjs or the radar withholds a verdict until N candles\n  // have printed. radarRow answers from the first bar. So a warm-up here hid\n  // real scanner behaviour — and on a day whose move happens before 09:50 it\n  // hid the part that matters most.\n  //\n  // Nor is prior-session data preloaded, for the same reason: the live scanner\n  // does NOT have it. sessionRows() filters the store to one date, VWAP\n  // accumulates from the day's first bar, and EMA9/EMA20 are seeded at the\n  // first close. Feeding yesterday's candles in would make the replay measure\n  // something the live scanner never sees, which is the opposite of the point.\n  // The first candles are genuinely thin — that is a fact about the scanner,\n  // and it should be visible rather than hidden.\n  warmupBars: 0\n};\n\n// ---------------------------------------------------------------- the replay\n//\n// rows: one day of 1-minute candles, chronological.\n// deps: { analyze, radarRow, momentum, tactical } — the REAL engine, passed in\n// rather than imported, so this file cannot quietly diverge from it.\n// Which inputs a run actually had, so the page can never imply more than it had.\n// Four states, exactly as specified: REAL HISTORICAL, RECONSTRUCTED, SIMULATED,\n// MISSING. Filled by the caller from what it could supply.\nfunction inputLedger(o) {\n  var has = function (v) { return v != null && !(Array.isArray(v) && !v.length); };\n  return [\n    { key: 'symbol candles', state: 'REAL HISTORICAL', note: 'the stored 1-minute bars for the day' },\n    { key: 'current candle / time', state: 'REAL HISTORICAL', note: 'the bar being replayed' },\n    { key: 'VWAP', state: 'REAL HISTORICAL', note: 'computed inside analyze() from those bars' },\n    { key: 'EMA9', state: 'REAL HISTORICAL', note: 'computed inside analyze()' },\n    { key: 'EMA20', state: 'REAL HISTORICAL', note: 'computed inside analyze()' },\n    { key: 'volume', state: 'REAL HISTORICAL', note: 'from the stored bars' },\n    { key: 'relative volume', state: 'REAL HISTORICAL', note: 'computed inside analyze()' },\n    { key: 'time-of-day volume baseline', state: has(o.baseline) ? 'RECONSTRUCTED' : 'MISSING',\n      note: has(o.baseline) ? 'volumeBaseline() over prior stored sessions' : 'no prior sessions supplied' },\n    { key: 'multi-day context', state: has(o.daily) ? 'RECONSTRUCTED' : 'MISSING',\n      note: has(o.daily) ? 'dailyContext() over prior stored sessions' : 'no prior sessions supplied' },\n    { key: 'historical calibration', state: has(o.calibration) ? 'RECONSTRUCTED' : 'MISSING',\n      note: has(o.calibration) ? 'calibrate() over prior sessions' : 'not supplied' },\n    { key: 'SPY candles', state: has(o.benchRows && o.benchRows.SPY) ? 'REAL HISTORICAL' : 'MISSING',\n      note: has(o.benchRows && o.benchRows.SPY) ? 'same day, sliced to the same minute' : 'not supplied' },\n    { key: 'QQQ candles', state: has(o.benchRows && o.benchRows.QQQ) ? 'REAL HISTORICAL' : 'MISSING',\n      note: has(o.benchRows && o.benchRows.QQQ) ? 'same day, sliced to the same minute' : 'not supplied' },\n    { key: 'market context', state: has(o.benchRows) ? 'RECONSTRUCTED' : 'SIMULATED',\n      note: has(o.benchRows) ? 'marketContext() over benchmarks as of this minute' : 'fixed Neutral' },\n    { key: 'cross-symbol context', state: has(o.benchRows && o.benchRows[o.sectorEtf]) ? 'REAL HISTORICAL'\n        : (o.sectorEtf ? 'MISSING' : 'REAL HISTORICAL'),\n      note: o.sectorEtf ? ('sector ETF ' + o.sectorEtf) : 'this symbol has no sector ETF mapping' },\n    { key: 'freshness / stale state', state: o.freshnessMode === 'derived' ? 'RECONSTRUCTED' : 'SIMULATED',\n      note: o.freshnessMode === 'derived' ? 'derived from the bar being replayed: it had just closed' : 'fixed LIVE' },\n    { key: 'session state', state: 'RECONSTRUCTED', note: 'from the bar time within the session' },\n    { key: 'end-of-session state', state: 'RECONSTRUCTED', note: 'true only on the last bar of the day' },\n    { key: 'configuration / thresholds', state: 'REAL HISTORICAL', note: 'engine defaults, unchanged' },\n    { key: 'cached / previous scanner state', state: 'REAL HISTORICAL',\n      note: 'buildTickerState is stateless per call; live keeps none either' },\n    // Verified against the function body: buildTickerState reads exactly eleven\n    // ctx keys — baseline, calibration, daily, date, freshness, market,\n    // marketCtx, now, nowUnix, sessionEnded, staleSeconds. The order book and\n    // position are read by analysisPack, which is a different surface. Marking\n    // them as gaps here would block COMPLETE over inputs the scanner never\n    // consults, which is its own kind of dishonesty.\n    { key: 'order book / Level 2', state: 'NOT CONSUMED',\n      note: 'buildTickerState does not read c.book; it belongs to the analysis pack' },\n    { key: 'position', state: 'NOT CONSUMED',\n      note: 'buildTickerState does not read c.position; the system tracks no positions at all' }\n  ];\n}\n\nfunction replayStates(rows, deps, opts) {\n  var o = opts || {};\n  var out = [], prev = null, prevA = null;\n  var K = o.engine || { K: 3 };\n  var warm = o.warmupBars == null ? CONFIG.warmupBars : o.warmupBars;\n  var sym = o.symbol || '?';\n  var lastTime = rows.length ? rows[rows.length - 1].time : null;\n\n  for (var i = 0; i < rows.length; i++) {\n    // THE rule. Only what had printed by this minute.\n    var seen = rows.slice(0, i + 1);\n    if (seen.length < warm) continue;\n\n    var A = deps.analyze(seen, K);\n    if (!A || !A.state) continue;\n\n    // --- context, reconstructed AS OF THIS MINUTE and never later.\n    // Benchmarks are sliced to the same minute, so a market regime computed\n    // here cannot contain a candle the live radar would not have had.\n    var mkCtx = { label: 'Neutral', parts: [] };\n    if (o.benchRows && deps.marketContext) {\n      var list = [];\n      Object.keys(o.benchRows).forEach(function (bs) {\n        var br = o.benchRows[bs];\n        if (!br || !br.length) return;\n        // same rule as the symbol: only bars up to this minute\n        var upto = br.filter(function (x) { return x.time <= rows[i].time; });\n        if (!upto.length) return;\n        var bA = deps.analyze(upto, K);\n        if (bA) { bA.symbol = bs; list.push(bA); }\n      });\n      if (list.length) mkCtx = deps.marketContext(list);\n    }\n\n    var ctx = {\n      market: mkCtx.label, marketCtx: mkCtx,\n      // The bar being replayed had just closed, so at that instant the feed was\n      // as fresh as it ever gets. Anything else would be inventing a delay.\n      freshness: o.freshness || 'LIVE',\n      staleSeconds: o.staleSeconds == null ? 30 : o.staleSeconds,\n      sessionEnded: !!o.sessionEnded || rows[i].time === lastTime && !!o.markLastBarClosed,\n      daily: o.daily || null, baseline: o.baseline || null, calibration: o.calibration || null,\n      now: (rows[i].unix || 0) * 1000, date: rows[i].date\n    };\n\n    // THE live path. Same function the radar calls, not a copy of its rules.\n    var snap = deps.buildTickerState(sym, A, ctx);\n    var row = snap.row || {};\n    var b = A.state.bar;\n\n    var rec = {\n      i: i, time: rows[i].time, price: rows[i].close,\n      // Everything below comes from the SNAPSHOT — the object the radar renders\n      // — so the replay cannot show a status the radar would have overridden.\n      status: snap.status, score: snap.score, why: row.why,\n      action: snap.action, actionText: snap.actionText,\n      noEdge: !!snap.noEdge, stale: !!snap.stale,\n      sessionIncomplete: !!snap.sessionIncomplete,\n      valid: snap.valid !== false,\n      violations: (snap.violations || []).map(function (v) { return v.code; }),\n      levels: snap.levels ? { watch: snap.levels.watch, entry: snap.levels.entry,\n        target1: snap.levels.target1, target2: snap.levels.target2,\n        tacticalInvalidation: snap.levels.tacticalInvalidation, hardStop: snap.levels.hardStop } : null,\n      marketLabel: mkCtx.label,\n      structure: snap.structure, momentum: snap.momentum,\n      vwap: b.vwap, ema9: b.ema9, ema20: b.ema20,\n      volume: rows[i].volume, volx: b.volx,\n      aboveVwap: b.aboveVwap,\n      planState: snap.plan ? snap.plan.state : null,\n      // the engine's own events for this candle, as they were known then\n      events: (A.state.events || []).map(function (e) {\n        return e.type + (e.level != null ? '@' + e.level.toFixed(2) : '')\n          + (e.confirmed === true ? '[confirmed]' : e.confirmed === false ? '[unconfirmed]' : '');\n      })\n    };\n    out.push(rec);\n\n    if (prev !== rec.status) {\n      rec.transition = { from: prev, to: rec.status,\n        first: prev === null,\n        reason: rec.why || (rec.events.length ? rec.events.join(' · ') : 'שינוי מצב') };\n      prev = rec.status;\n    }\n    prevA = A;\n  }\n  return out;\n}\n\n// ---------------------------------------------------------------- forward look\n//\n// What the price did AFTER a given minute. Uses the whole day on purpose: this\n// measures the alert, it does not inform it.\nfunction scoreAlert(rows, atIndex, opts) {\n  var o = opts || {};\n  var windows = o.windows || CONFIG.windows;\n  var entry = rows[atIndex];\n  if (!entry) return null;\n  var px = entry.close;\n  var out = { index: atIndex, time: entry.time, price: px, windows: {}, };\n\n  windows.forEach(function (m) {\n    var end = Math.min(rows.length - 1, atIndex + m);\n    var slice = rows.slice(atIndex + 1, end + 1);\n    var last = slice.length ? slice[slice.length - 1] : null;\n    var hi = slice.length ? Math.max.apply(null, slice.map(function (r) { return r.high; })) : null;\n    var lo = slice.length ? Math.min.apply(null, slice.map(function (r) { return r.low; })) : null;\n    out.windows[m] = {\n      bars: slice.length,\n      // A window that runs past the close is reported short rather than padded:\n      // \"+60m\" on a 15:45 alert is 15 minutes, and saying otherwise would make\n      // late alerts look better than they were.\n      complete: slice.length === m,\n      price: last ? last.close : null,\n      returnPct: last ? (last.close - px) / px * 100 : null,\n      maxUp: hi != null ? hi - px : null,\n      maxUpPct: hi != null ? (hi - px) / px * 100 : null,\n      maxDown: lo != null ? lo - px : null,\n      maxDownPct: lo != null ? (lo - px) / px * 100 : null\n    };\n  });\n\n  // Everything after the alert, to the close. \"Remaining move\" is the question\n  // of whether the scanner woke up early enough to matter.\n  var rest = rows.slice(atIndex + 1);\n  var restHi = rest.length ? Math.max.apply(null, rest.map(function (r) { return r.high; })) : null;\n  var restLo = rest.length ? Math.min.apply(null, rest.map(function (r) { return r.low; })) : null;\n  out.toClose = {\n    bars: rest.length,\n    maxUp: restHi != null ? restHi - px : null,\n    maxUpPct: restHi != null ? (restHi - px) / px * 100 : null,\n    maxDown: restLo != null ? restLo - px : null,\n    maxDownPct: restLo != null ? (restLo - px) / px * 100 : null,\n    close: rest.length ? rest[rest.length - 1].close : null\n  };\n  out.remainingMove = out.toClose.maxUp;\n  out.remainingMovePct = out.toClose.maxUpPct;\n\n  // Weak by the configured rule, and the rule is named in the result so nobody\n  // has to guess what \"false\" meant.\n  var wm = o.falseAlertWindowMin || CONFIG.falseAlertWindowMin;\n  var w = out.windows[wm];\n  var thr = o.falseAlertMaxUpPct == null ? CONFIG.falseAlertMaxUpPct : o.falseAlertMaxUpPct;\n  // Three outcomes, not two. An alert fired ten minutes before the close has no\n  // thirty-minute window at all, and calling that \"not weak\" credits it with a\n  // result it never had the time to produce. It is UNMEASURED, and says so.\n  out.measurable = !!(w && w.complete);\n  out.weak = out.measurable ? w.maxUpPct < thr : null;\n  out.verdict = !out.measurable ? 'unmeasured' : (out.weak ? 'weak' : 'ok');\n  out.weakRule = 'Max Up within ' + wm + ' minutes below ' + thr + '%'\n    + '; an alert with fewer than ' + wm + ' minutes left is unmeasured, not weak';\n  return out;\n}\n\n// ---------------------------------------------------------------- one run\nfunction runReplay(rows, deps, opts) {\n  var o = opts || {};\n  var states = replayStates(rows, deps, o);\n  var transitions = states.filter(function (s) { return s.transition; });\n  var alertStates = o.alertStates || ALERT_STATES;\n\n  // An alert is a transition INTO an attention state from something quieter.\n  var alerts = transitions.filter(function (s) {\n    return alertStates.indexOf(s.transition.to) >= 0\n      && alertStates.indexOf(s.transition.from) < 0;\n  }).map(function (s) {\n    var sc = scoreAlert(rows, s.i, o);\n    sc.status = s.status; sc.from = s.transition.from; sc.reason = s.transition.reason;\n    sc.score = s.score; sc.structure = s.structure; sc.momentum = s.momentum;\n    return sc;\n  });\n\n  var pct = function (list, f) {\n    var v = list.map(f).filter(function (x) { return x != null; });\n    return v.length ? v.reduce(function (a, b) { return a + b; }, 0) / v.length : null;\n  };\n  var best = alerts.slice().sort(function (a, b) {\n    return (b.toClose.maxUpPct || -Infinity) - (a.toClose.maxUpPct || -Infinity);\n  })[0] || null;\n  // The earliest alert that led to a real move — the one that would actually\n  // have been useful, as opposed to the one that scored best after the fact.\n  var thr = o.falseAlertMaxUpPct == null ? CONFIG.falseAlertMaxUpPct : o.falseAlertMaxUpPct;\n  var earliestUseful = alerts.filter(function (a) {\n    return a.toClose.maxUpPct != null && a.toClose.maxUpPct >= thr;\n  })[0] || null;\n\n  var ledger = inputLedger(o);\n  // NOT CONSUMED is not a gap: an input the scanner never reads cannot make the\n  // replay differ from the radar.\n  var incomplete = ledger.filter(function (x) { return x.state === 'MISSING' || x.state === 'SIMULATED'; });\n  return {\n    // A run says what it had. \"HISTORICAL PARITY INCOMPLETE\" is not a warning\n    // to be dismissed: any essential input missing means the numbers describe a\n    // scanner running on less than the radar had.\n    ledger: ledger, inputsComplete: incomplete.length === 0,\n    inputsIncomplete: incomplete.map(function (x) { return x.key + ' (' + x.state + ')'; }),\n    symbol: o.symbol || null, date: rows.length ? rows[0].date : null,\n    bars: rows.length, statesComputed: states.length,\n    states: states, transitions: transitions, alerts: alerts,\n    summary: {\n      alerts: alerts.length,\n      weak: alerts.filter(function (a) { return a.weak === true; }).length,\n      unmeasured: alerts.filter(function (a) { return !a.measurable; }).length,\n      measured: alerts.filter(function (a) { return a.measurable; }).length,\n      best: best ? { time: best.time, price: best.price, maxUpPct: best.toClose.maxUpPct } : null,\n      earliestUseful: earliestUseful\n        ? { time: earliestUseful.time, price: earliestUseful.price,\n            remainingMovePct: earliestUseful.remainingMovePct } : null,\n      avgMaxUpPct: pct(alerts, function (a) { return a.toClose.maxUpPct; }),\n      avgMaxDownPct: pct(alerts, function (a) { return a.toClose.maxDownPct; }),\n      avgRemainingPct: pct(alerts, function (a) { return a.remainingMovePct; }),\n      weakRule: 'Max Up within ' + (o.falseAlertWindowMin || CONFIG.falseAlertWindowMin)\n        + ' minutes below ' + thr + '%; alerts too close to the bell are unmeasured'\n    },\n    config: { falseAlertMaxUpPct: thr,\n      falseAlertWindowMin: o.falseAlertWindowMin || CONFIG.falseAlertWindowMin,\n      windows: o.windows || CONFIG.windows,\n      warmupBars: o.warmupBars == null ? CONFIG.warmupBars : o.warmupBars,\n      alertStates: alertStates }\n  };\n}\n\n\n</script>\n<script>\n(function(){\n  var qs=function(s){return document.querySelector(s)};\n  var SYM='', DATE='', rows=[], run=null, at=-1, timer=null, picked=null;\n  var STATUS_TXT={QUIET:'שקט',WATCH:'מעקב',CLOSE:'קרוב',READY:'מוכן',ACTIVE:'פעיל',AVOID:'להימנע','NO DATA':'אין נתונים'};\n  function j(u){return fetch(u+(u.indexOf('?')>0?'&':'?')+'ts='+Date.now(),{cache:'no-store'}).then(function(r){return r.json()})}\n  function toast(m){var t=qs('#toast');t.textContent=m;t.hidden=false;clearTimeout(toast.h);toast.h=setTimeout(function(){t.hidden=true},1800)}\n  function pct(v){return v==null?'—':(v>=0?'+':'')+v.toFixed(2)+'%'}\n  function money(v){return v==null?'—':(v>=0?'+':'')+v.toFixed(2)}\n  function cfg(){return {falseAlertMaxUpPct:parseFloat(qs('#thr').value)||0.30,\n    falseAlertWindowMin:parseInt(qs('#win').value,10)||30}}\n\n  function loadDays(){\n    SYM=(qs('#sym').value||'').trim().toUpperCase();\n    if(!SYM)return Promise.resolve();\n    return j('/days/'+SYM).then(function(d){\n      var days=d.days||[];\n      qs('#date').innerHTML=days.length?days.map(function(x){\n        return '<option value=\"'+x.date+'\">'+x.date.slice(5).replace('-','/')+' · '+(x.bars||0)+' נרות</option>'\n      }).join(''):'<option>אין ימים</option>';\n      if(days.length)DATE=days[0].date;\n    });\n  }\n  var bench={}, histDaily=null, histBaseline=null;\n  var MARKET=['SPY','QQQ'];\n  var SECTOR={NVDA:'SMH',AVGO:'SMH',AMD:'SMH',AAPL:'XLK',MSFT:'XLK',GOOGL:'XLC',META:'XLC',\n    AMZN:'XLY',TSLA:'XLY',JPM:'XLF','BRK-B':'XLF'};\n  // The benchmarks for the SAME day, so market context can be rebuilt minute by\n  // minute rather than assumed. Each is sliced to the replayed minute inside the\n  // adapter, so nothing from later in the day reaches an earlier decision.\n  function loadContext(){\n    var want=MARKET.concat(SECTOR[SYM]?[SECTOR[SYM]]:[]);\n    bench={};\n    return Promise.all(want.map(function(s){\n      return j('/day/'+s+'/'+DATE+'?format=json').then(function(d){\n        if(d.rows&&d.rows.length)bench[s]=d.rows.slice().sort(function(a,b){return a.unix-b.unix});\n      },function(){});\n    })).then(function(){\n      // prior sessions of the symbol, for the multi-day context and the\n      // time-of-day volume baseline the live radar also builds\n      return j('/days/'+SYM).then(function(d){\n        var prior=(d.days||[]).filter(function(x){return x.date<DATE}).slice(0,6).map(function(x){return x.date});\n        if(!prior.length){histDaily=null;histBaseline=null;return}\n        return Promise.all(prior.map(function(dt){\n          return j('/day/'+SYM+'/'+dt+'?format=json').then(function(r){return r.rows||[]},function(){return []});\n        })).then(function(sets){\n          var ok=sets.filter(function(x){return x&&x.length});\n          histDaily=ok.length?dailyContext(ok,{todayDate:DATE}):null;\n          histBaseline=ok.length?volumeBaseline(ok):null;\n        });\n      },function(){});\n    });\n  }\n  function loadDay(){\n    SYM=(qs('#sym').value||'').trim().toUpperCase(); DATE=qs('#date').value;\n    if(!SYM||!DATE){toast('בחר סימבול ויום');return Promise.resolve()}\n    qs('#loaded').textContent='טוען…';\n    return j('/day/'+SYM+'/'+DATE+'?format=json').then(function(d){\n      rows=(d.rows||[]).slice().sort(function(a,b){return a.unix-b.unix});\n      qs('#loaded').textContent=rows.length+' נרות · '+(rows.length?rows[0].time+'–'+rows[rows.length-1].time:'');\n      run=null; at=-1; picked=null; stateFilter='ALL'; clear();\n      qs('#ctl').hidden=rows.length===0;\n      if(rows.length)loadContext().then(function(){badges()});\n      if(rows.length){qs('#empty').innerHTML='<b>נטען '+SYM+' '+DATE+'</b><br>לחץ \"נתח יום מלא\" לתוצאות מיידיות, או \"הרץ\" ל-replay ויזואלי.';drawChart(rows.length-1)}\n    },function(e){qs('#loaded').textContent='';toast('שגיאה: '+e)});\n  }\n  function clear(){\n    ['#events','#alerts','#hEvents','#hAlerts','#note','#chart','#filt'].forEach(function(s){qs(s).hidden=true});\n    qs('#sum').innerHTML=''; qs('#pop').hidden=true;\n  }\n\n  // ---- the analysis. Uses the REAL engine through the replay adapter.\n  function analyzeFull(){\n    if(!rows.length){toast('טען יום קודם');return}\n    qs('#empty').innerHTML='<b>מריץ…</b><br>'+rows.length+' נרות, כל אחד עם רק מה שהיה עד אותה דקה.';\n    setTimeout(function(){\n      var t0=Date.now();\n      run=runReplay(rows,{analyze:analyze,radarRow:radarRow,\n          buildTickerState:buildTickerState,marketContext:marketContext},\n        Object.assign({symbol:SYM,benchRows:bench,\n          daily:histDaily,baseline:histBaseline,\n          freshnessMode:'derived',sectorEtf:SECTOR[SYM]||null},cfg()));\n      qs('#empty').hidden=true;\n      render(); badges(); toast('הושלם ב-'+((Date.now()-t0)/1000).toFixed(1)+' שנ׳');\n    },30);\n  }\n\n  function render(){\n    if(!run)return;\n    var s=run.summary;\n    qs('#sum').innerHTML=\n      box('התראות',s.alerts,'מעברים למצב תשומת לב')+\n      box('חלשות',s.weak+' / '+s.measured,s.weakRule,s.weak>s.measured/2?'bad':'')+\n      (s.unmeasured?box('לא נמדדו',s.unmeasured,'התראות קרובות מדי לסגירה — אין חלון מלא'):'')+\n      box('ההתראה הטובה ביותר',s.best?s.best.time:'—',s.best?pct(s.best.maxUpPct)+' עד הסגירה':'','good')+\n      box('המוקדמת השימושית',s.earliestUseful?s.earliestUseful.time:'—',s.earliestUseful?pct(s.earliestUseful.remainingMovePct)+' נשארו':'אף התראה לא הובילה למהלך')+\n      box('ממוצע Max Up',pct(s.avgMaxUpPct),'אחרי כל התראה','good')+\n      box('ממוצע Max Down',pct(s.avgMaxDownPct),'אחרי כל התראה','bad');\n    drawFilter(); drawEvents(); drawAlerts(); drawChart(rows.length-1);\n    qs('#note').hidden=false;\n    // A run made without the full historical context is not evidence about the\n    // radar, and must say so where the numbers are, not only in the badge.\n    qs('#note').innerHTML=(run.inputsComplete?'':'<b style=\"color:var(--down)\">NOT VALID FOR RADAR EVALUATION — נוצר ללא קלטים היסטוריים מלאים</b><br>')\n      +'הסורק קיבל בכל דקה <b>רק את הנרות שהיו עד אותה דקה</b> — '+run.statesComputed+\n      ' הרצות על '+run.bars+' נרות. הביצועים אחרי כל התראה נמדדים מהיום המלא: זו המדידה, לא ההחלטה.<br>'+\n      'הקלטה מ-<b>09:30</b>, בלי חימום ובלי טעינת סשן קודם — כי הסורק החי גם לא מקבל אותם: VWAP מצטבר מהנר הראשון של היום ו-EMA נזרע בסגירה הראשונה. חלון שחורג מסוף היום מסומן כחלקי ולא מרופד.';\n  }\n  function box(t,v,sub,cls){return '<div class=\"box '+(cls||'')+'\"><small>'+t+'</small><b>'+v+'</b><span>'+(sub||'')+'</span></div>'}\n\n  // Filter by the state a transition moved INTO. Counting is done on the\n  // unfiltered list so a state with no transitions still shows a zero rather\n  // than vanishing — an empty answer is information too.\n  var stateFilter='ALL';\n  function drawFilter(){\n    var t=run.transitions;\n    var counts={};\n    t.forEach(function(s){counts[s.transition.to]=(counts[s.transition.to]||0)+1});\n    var order=['ALL','READY','ACTIVE','CLOSE','WATCH','QUIET','AVOID'];\n    qs('#filt').innerHTML=order.map(function(k){\n      var n=k==='ALL'?t.length:(counts[k]||0);\n      var label=k==='ALL'?'הכל':(STATUS_TXT[k]||k);\n      return '<button type=\"button\" class=\"f-'+k+(stateFilter===k?' on':'')+'\" data-k=\"'+k+'\">'+\n        label+' <span class=\"n\">'+n+'</span></button>';\n    }).join('');\n    qs('#filt').hidden=false;\n    qs('#filt').onclick=function(e){\n      var b=e.target.closest('button'); if(!b)return;\n      stateFilter=b.dataset.k; drawFilter(); drawEvents(); drawAlerts();\n    };\n  }\n  function drawEvents(){\n    var all=run.transitions;\n    var t=stateFilter==='ALL'?all:all.filter(function(s){return s.transition.to===stateFilter});\n    if(!all.length){qs('#hEvents').hidden=true;qs('#events').hidden=true;qs('#filt').hidden=true;return}\n    if(!t.length){\n      qs('#hEvents').hidden=false;\n      qs('#events').hidden=false;\n      qs('#events').innerHTML='<div class=\"msg\">אין מעברים ל<b>'+(STATUS_TXT[stateFilter]||stateFilter)+'</b> ביום הזה.</div>';\n      return;\n    }\n    var h='<table><thead><tr><th>שעה</th><th>מחיר</th><th>ממצב</th><th>למצב</th><th>ציון</th><th>סיבה</th></tr></thead><tbody>';\n    t.forEach(function(s){\n      h+='<tr><td>'+s.time+'</td><td class=\"p\">'+s.price.toFixed(2)+'</td>'+\n        '<td>'+(s.transition.from?'<span class=\"st bg-'+s.transition.from.replace(' ','')+'\">'+(STATUS_TXT[s.transition.from]||s.transition.from)+'</span>':'—')+'</td>'+\n        '<td><span class=\"st bg-'+s.transition.to.replace(' ','')+'\">'+(STATUS_TXT[s.transition.to]||s.transition.to)+'</span></td>'+\n        '<td>'+(s.score==null?'—':s.score)+'</td><td style=\"white-space:normal\">'+(s.transition.reason||'')+'</td></tr>';\n    });\n    qs('#events').innerHTML=h+'</tbody></table>';\n    qs('#hEvents').hidden=false; qs('#events').hidden=false;\n  }\n  function drawAlerts(){\n    var all=run.alerts;\n    var a=stateFilter==='ALL'?all:all.filter(function(x){return x.status===stateFilter});\n    if(!all.length){qs('#hAlerts').hidden=true;qs('#alerts').hidden=true;return}\n    if(!a.length){\n      qs('#hAlerts').hidden=false; qs('#alerts').hidden=false;\n      qs('#alerts').innerHTML='<div class=\"msg\">אין התראות ל<b>'+(STATUS_TXT[stateFilter]||stateFilter)+'</b>.</div>';\n      return;\n    }\n    var best=run.summary.best?run.summary.best.time:null;\n    var w=run.config.windows;\n    var h='<table><thead><tr><th>שעה</th><th>מחיר</th><th>מצב</th>'+\n      w.map(function(m){return '<th>+'+m+'ד</th>'}).join('')+\n      '<th>Max Up</th><th>Max Down</th><th>נשאר עד הסגירה</th></tr></thead><tbody>';\n    a.forEach(function(x){\n      var cls=(x.weak===true?'weak':'')+(x.time===best?' best':'')+(x.measurable?'':' unmeas');\n      h+='<tr class=\"'+cls+'\"><td>'+x.time+'</td><td class=\"p\">'+x.price.toFixed(2)+'</td>'+\n        '<td><span class=\"st bg-'+x.status.replace(' ','')+'\">'+(STATUS_TXT[x.status]||x.status)+'</span></td>';\n      w.forEach(function(m){\n        var q=x.windows[m];\n        h+='<td class=\"'+(q.returnPct>0?'g':q.returnPct<0?'r':'')+'\" title=\"'+(q.price!=null?q.price.toFixed(2):'—')+\n          (q.complete?'':' · חלקי, '+q.bars+' נרות')+'\">'+pct(q.returnPct)+(q.complete?'':'*')+'</td>';\n      });\n      h+='<td class=\"g\" title=\"'+money(x.toClose.maxUp)+'\">'+pct(x.toClose.maxUpPct)+'</td>'+\n        '<td class=\"r\" title=\"'+money(x.toClose.maxDown)+'\">'+pct(x.toClose.maxDownPct)+'</td>'+\n        '<td class=\"p g\" title=\"'+money(x.remainingMove)+'\">'+pct(x.remainingMovePct)+'</td></tr>';\n    });\n    qs('#alerts').innerHTML=h+'</tbody></table><div style=\"padding:8px 10px;font-size:11.5px;color:var(--muted)\">'+\n      '* חלון שחורג מסוף המסחר — מדווח על מה שהיה, לא מרופד. שורה אדומה = התראה חלשה לפי הסף. ירוקה = הטובה ביותר.</div>';\n    qs('#hAlerts').hidden=false; qs('#alerts').hidden=false;\n  }\n\n  // ---- chart, with a marker per state change\n  function drawChart(upto){\n    if(!rows.length)return;\n    var vis=rows.slice(0,upto+1); if(vis.length<2)return;\n    var W=800,H=300,top=10,PH=250,padL=6,padR=48;\n    var hi=Math.max.apply(null,vis.map(function(r){return r.high}));\n    var lo=Math.min.apply(null,vis.map(function(r){return r.low}));\n    var span=(hi-lo)||1; hi+=span*0.05; lo-=span*0.05; span=hi-lo;\n    var n=rows.length, cw=(W-padL-padR)/n, bw=Math.max(1,Math.min(5,cw*0.7));\n    var y=function(p){return top+(hi-p)/span*PH};\n    var out=[];\n    for(var g=0;g<=4;g++){var p=lo+span*g/4,yy=y(p);\n      out.push('<line class=\"grid\" x1=\"'+padL+'\" x2=\"'+(W-padR)+'\" y1=\"'+yy.toFixed(1)+'\" y2=\"'+yy.toFixed(1)+'\"/>'+\n        '<text class=\"gtxt\" x=\"'+(W-padR+4)+'\" y=\"'+(yy+3).toFixed(1)+'\">'+p.toFixed(2)+'</text>')}\n    vis.forEach(function(r,i){\n      var x=padL+i*cw+cw/2, cls=r.close>r.open?'c-up':r.close<r.open?'c-dn':'c-fl';\n      out.push('<line class=\"wick '+cls+'\" x1=\"'+x.toFixed(1)+'\" x2=\"'+x.toFixed(1)+'\" y1=\"'+y(r.high).toFixed(1)+'\" y2=\"'+y(r.low).toFixed(1)+'\"/>');\n      var yo=y(r.open),yc=y(r.close);\n      out.push('<rect class=\"body '+cls+'\" x=\"'+(x-bw/2).toFixed(1)+'\" y=\"'+Math.min(yo,yc).toFixed(1)+\n        '\" width=\"'+bw.toFixed(1)+'\" height=\"'+Math.max(1,Math.abs(yo-yc)).toFixed(1)+'\"/>');\n    });\n    if(run){\n      var d=''; run.states.forEach(function(s){ if(s.i>upto)return;\n        var x=padL+s.i*cw+cw/2; d+=(d?' L':'M')+x.toFixed(1)+' '+y(s.vwap).toFixed(1)});\n      if(d)out.push('<path class=\"vwapL\" d=\"'+d+'\"/>');\n      // one marker per state change, never per candle\n      run.transitions.forEach(function(s){\n        if(s.i>upto)return;\n        if(stateFilter!=='ALL'&&s.transition.to!==stateFilter)return;\n        var x=padL+s.i*cw+cw/2, yy=y(s.price)-9;\n        var col={QUIET:'#8A94A0',WATCH:'#B7791F',CLOSE:'#C2410C',READY:'#0F766E',ACTIVE:'#1D4ED8',AVOID:'#B42318'}[s.status]||'#8A94A0';\n        out.push('<g class=\"mk\" data-i=\"'+s.i+'\"><circle cx=\"'+x.toFixed(1)+'\" cy=\"'+yy.toFixed(1)+'\" r=\"6\" fill=\"'+col+'\"/>'+\n          '<text x=\"'+x.toFixed(1)+'\" y=\"'+(yy+3).toFixed(1)+'\" text-anchor=\"middle\">'+(STATUS_TXT[s.status]||'?').slice(0,1)+'</text></g>');\n      });\n    }\n    if(at>=0&&at<=upto){var cx=padL+at*cw+cw/2;\n      out.push('<line class=\"cur\" x1=\"'+cx.toFixed(1)+'\" x2=\"'+cx.toFixed(1)+'\" y1=\"'+top+'\" y2=\"'+(top+PH)+'\"/>')}\n    var svg=qs('#svg'); svg.innerHTML=out.join(''); qs('#chart').hidden=false;\n    svg.onclick=function(e){\n      var g=e.target.closest('.mk'); if(!g)return;\n      var i=+g.dataset.i, s=run.states.find(function(x){return x.i===i});\n      var al=run.alerts.find(function(x){return x.index===i});\n      picked=i;\n      qs('#pop').hidden=false;\n      qs('#pop').innerHTML='<b>'+s.time+' · '+s.price.toFixed(2)+'</b> · '+\n        (STATUS_TXT[s.status]||s.status)+(s.transition&&s.transition.from?' (מ-'+(STATUS_TXT[s.transition.from]||s.transition.from)+')':'')+\n        '<br>'+(s.why||'')+\n        '<br>מבנה '+s.structure+' · מומנטום '+s.momentum+' · ציון '+s.score+\n        '<br>VWAP '+s.vwap.toFixed(2)+' · EMA9 '+s.ema9.toFixed(2)+' / EMA20 '+s.ema20.toFixed(2)+' · נפח ×'+(s.volx||0).toFixed(2)+\n        (al?'<br><b>אחרי ההתראה:</b> '+run.config.windows.map(function(m){return '+'+m+'ד '+pct(al.windows[m].returnPct)}).join(' · ')+\n          '<br>Max Up '+pct(al.toClose.maxUpPct)+' · Max Down '+pct(al.toClose.maxDownPct)+' · נשאר '+pct(al.remainingMovePct)\n          +(al.weak===true?' · <b>חלשה</b>':al.measurable?'':' · <b>לא נמדדה</b>'):'');\n    };\n  }\n\n  // ---- visual replay. It only moves the cursor; the analysis is already done.\n  function ensureRun(){ if(!run)analyzeFull(); return !!run }\n  function stepTo(i){\n    at=Math.max(0,Math.min(rows.length-1,i));\n    var r=rows[at], s=run?run.states.find(function(x){return x.i===at}):null;\n    qs('#at').innerHTML='נר <b>'+r.time+'</b> · <b>'+r.close.toFixed(2)+'</b>'+\n      (s?' · <span class=\"st bg-'+s.status.replace(' ','')+'\">'+(STATUS_TXT[s.status]||s.status)+'</span> · '+(s.why||''):' · לפני חימום');\n    drawChart(at);\n  }\n  function play(){\n    stop();\n    if(!run&&!ensureRun())return;\n    var sp=parseInt(qs('#speed').value,10)||5;\n    timer=setInterval(function(){ if(at>=rows.length-1){stop();return} stepTo(at+1) },Math.max(16,1000/sp));\n  }\n  function stop(){ if(timer)clearInterval(timer); timer=null }\n\n  qs('#sym').onchange=loadDays;\n  qs('#load').onclick=function(){loadDays().then(loadDay)};\n  qs('#date').onchange=loadDay;\n  qs('#full').onclick=analyzeFull;\n  qs('#start').onclick=play;\n  qs('#pause').onclick=stop;\n  qs('#next').onclick=function(){stop();if(run||ensureRun())stepTo(at+1)};\n  qs('#prev').onclick=function(){stop();if(run||ensureRun())stepTo(at-1)};\n  qs('#restart').onclick=function(){stop();at=-1;stepTo(0)};\n  qs('#jump').onchange=function(){\n    var t=this.value.trim(); if(!/^\\d{1,2}:\\d{2}$/.test(t)){toast('פורמט שעה HH:MM');return}\n    var i=rows.findIndex(function(r){return r.time>=t});\n    if(i<0){toast('אין נר בשעה הזו');return}\n    stop(); if(run||ensureRun())stepTo(i);\n  };\n  ['#thr','#win'].forEach(function(s){qs(s).onchange=function(){ if(run)analyzeFull() }});\n\n  // ---- badges. Each is earned separately, and the final one only if all are.\n  //\n  // Engine and code path are properties of the CODE and can be settled here.\n  // Historical inputs are a property of the RUN and are only known after one.\n  // Conflating them is exactly how a page comes to claim more than it has.\n  var PARITY_TEST_PASSED = true;   // set by parity_test.mjs in the build; 0 divergences\n  function badges(){\n    var sameEngine = typeof analyze==='function' && typeof radarRow==='function'\n      && typeof buildTickerState==='function'\n      && /K = \\(opts && opts\\.K\\)/.test(String(analyze));\n    var samePath = typeof buildTickerState==='function'\n      && /deps\\.buildTickerState/.test(String(runReplay));\n    var led = run ? run.ledger : inputLedger({benchRows:bench,daily:histDaily,\n      baseline:histBaseline,freshnessMode:'derived',sectorEtf:SECTOR[SYM]||null});\n    var gaps = led.filter(function(x){return x.state==='MISSING'||x.state==='SIMULATED'});\n    var inputsComplete = gaps.length===0;\n    var parity = sameEngine && samePath && inputsComplete && PARITY_TEST_PASSED;\n\n    set('#eng', sameEngine, 'Scanner Engine: LIVE SHARED ENGINE ✓','Scanner Engine: NOT SHARED');\n    set('#path', samePath, 'Radar Code Path: VERIFIED ✓','Radar Code Path: NOT VERIFIED');\n    set('#inp', inputsComplete, 'Historical Inputs: COMPLETE ✓','Historical Inputs: INCOMPLETE');\n    set('#par', parity, 'PARITY VERIFIED ✓','PARITY NOT VERIFIED');\n\n    var rows2=led.map(function(x){\n      var cls=x.state==='REAL HISTORICAL'?'ok':x.state==='RECONSTRUCTED'?'rec':x.state==='NOT CONSUMED'?'na':'bad';\n      return '<tr><td>'+x.key+'</td><td class=\"'+cls+'\">'+x.state+'</td><td class=\"n\">'+x.note+'</td></tr>';\n    }).join('');\n    qs('#parity').className='parity '+(parity?'good':'');\n    qs('#parity').innerHTML=\n      '<b>'+(parity?'PARITY VERIFIED ✓':'HISTORICAL PARITY INCOMPLETE')+'</b>'+\n      (parity\n        ? ' — אותו engine, אותו code path, כל הקלטים אמיתיים או משוחזרים, ובדיקת ה-parity עברה עם 0 הפרשים.'\n        : ' — ה-Replay קורא ל-<code>buildTickerState()</code>, אותה פונקציה שהרדאר קורא לה, ובדיקת הקוד עוברת עם '+\n          '<b>0 הפרשים</b> על קלט זהה. אבל '+gaps.length+' קלטים היסטוריים חסרים או מדומים, ולכן <b>אי אפשר לטעון '+\n          'שזה שחזור של מה שהרדאר הראה באותו יום</b>.')+\n      '<table><tr><th>קלט</th><th>מצב</th><th>מקור</th></tr>'+rows2+'</table>'+\n      (parity?'':'<div class=\"warn2\">מה שכן תקף: התנהגות הסורק על נרות הסימבול — מבנה, מומנטום, VWAP/EMA, רמות, ותזמון המעברים. '+\n        'מה שלא תקף להערכת הרדאר: כל מספר שתלוי בקלט מסומן אדום למעלה.</div>');\n  }\n  function set(sel, ok, yes, no){\n    var e=qs(sel); if(!e)return;\n    e.className=(sel==='#par'?'par ':'eng ')+(ok?'ok':'no');\n    e.textContent=ok?yes:no;\n  }\n\n  badges();\n  loadDays().then(function(){ if(qs('#date').value)DATE=qs('#date').value });\n})();\n</script>\n</body>\n</html>\n";
export const BUILD = "v138  (2026-09-05 16:01Z)";
