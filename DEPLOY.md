# bars-vault — התקנה מהטלפון

כל השלבים בדפדפן. אין צורך במחשב, ב-wrangler או בכרטיס אשראי.

## 1. יצירת מסד הנתונים (D1)

1. `dash.cloudflare.com` → תפריט → **Storage & databases** → **D1 SQL Database**
2. **Create** → שם: `bars-vault` → **Create**
3. זהו. הטבלאות נוצרות לבד בקריאה הראשונה.

## 2. יצירת ה-Worker

1. תפריט → **Compute** → **Workers & Pages** → **Create**
2. **Workers** → **Start with Hello World** → שם: `bars-vault` → **Deploy**
3. **Edit code** → לבחור הכל → למחוק → להדביק את `worker.js` → **Deploy**

## 3. חיבור ה-DB ל-Worker  ← השלב שקל לשכוח

1. בעמוד ה-Worker → **Settings** → **Bindings** → **Add binding**
2. סוג: **D1 database**
3. Variable name: **`DB`**  (בדיוק ככה, אותיות גדולות)
4. Database: `bars-vault`
5. **Deploy** / **Save**

בלי השלב הזה כל בקשה תחזיר הודעת שגיאה ברורה שאומרת בדיוק מה חסר.

## 4. תזמון אוטומטי (Cron)

Worker → **Settings** → **Triggers** → **Cron Triggers** → **Add**

שני טריגרים:

| ביטוי | מה זה עושה |
|---|---|
| `*/5 13-21 * * 1-5` | כל 5 דקות במהלך המסחר בארה"ב, א׳–ה׳. מכסה גם שעון קיץ וגם חורף |
| `30 22 * * 1-5` | 22:30 UTC כל ערב — משיכה חוזרת של 5 ימים אחורה, סוגרת חורים |

## 5. בדיקה

פתח בדפדפן, לפי הסדר:

1. `https://bars-vault.<subdomain>.workers.dev/` — צריך `ok: true` ורשימת סימבולים
2. `.../sync/NVDA` — משיכה ראשונה. צריך `rows` גדול מאפס
3. `.../day/NVDA` — CSV של היום
4. `.../status` — טריות לכל סימבול, שגיאות אם יש

## שימוש יומיומי

| כתובת | תוצאה |
|---|---|
| `/day/NVDA` | הנרות של היום האחרון השמור |
| `/day/NVDA/2026-08-31` | יום ספציפי |
| `/days/NVDA` | אילו ימים שמורים, כמה נרות בכל אחד |
| `/day/AMD` | סימבול חדש — נמשך, נשמר, ומצטרף למעקב |
| `/status` | מצב כל המערכת |
| `/backfill/NVDA` | משיכה של 5 ימים אחורה |

הוסף `?format=json` לכל `/day/...` לקבלת JSON במקום CSV.

## מגבלות ידועות

- Yahoo מחזיר נרות דקה עד 7 ימים אחורה בלבד. יום שלא נמשך תוך שבוע אבד — לכן ה-cron הלילי.
- הקוד לא נבדק מול Yahoo אמיתי מהסביבה הזו (הרשת חסומה). כל השאר נבדק מול SQLite אמיתי.
- תוכנית חינמית: 100,000 בקשות ביום, 5GB אחסון. שנה של 13 סימבולים ≈ 130MB.
